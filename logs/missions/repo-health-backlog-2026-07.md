---
mission_id: repo-health-backlog-2026-07
mission_name: ai-resources repo-health backlog — 10 verified items (2026-07-18)
status: active            # active | paused | completed
started: 2026-07-18
---

<!--
  MISSION CONTRACT — a multi-session goal that individual sessions serve.
  Scaffolded per `/mission create` (Step 2), drafted from session context on operator request.
  Frozen at creation: Goal / In-Out scope / Validation contract are the north star and should
  not drift session-to-session. Only `status` (frontmatter) and `## Open threads` change.

  PROVENANCE — this mission's threads are NOT copied from the backlog logs. Every thread below
  survived an independent five-agent verification pass on 2026-07-18 that re-checked each
  candidate against the LIVE FILES, discarding anything already fixed, mis-attributed, or
  lacking a named consequence. 22 candidates in, 10 out. Verification notes:
    audits/working/verify-cluster-A-2026-07-18.md   (markers, hooks, liveness)
    audits/working/verify-cluster-B-2026-07-18.md   (commands and scripts)
    audits/working/verify-cluster-C-2026-07-18.md   (log formats, parsers, bloat)
    audits/working/verify-cluster-D-2026-07-18.md   (permissions and settings)
    audits/working/verify-cluster-E-2026-07-18.md   (improvement opportunities)

  DELIBERATELY NOT INCLUDED — verified and dropped, do not re-raise without new evidence:
    · marker-allocator collisions (session-id suffix in the marker NAME made them impossible;
      the 4 logged collisions all predate that fix)
    · /prime allocator glob crash (code removed 2026-07-18)
    · /risk-check wrong-checkout (fixed 2026-07-15, commit 3179771)
    · /prime pull --rebase conflict (behind-check + mid-rebase handling both present)
    · run-manifest.sh dropping decision refs (traced through all 5 stages; works)
    · committed conflict markers (cleaned 2026-07-17; entry blamed the wrong command)
    · ai-resources permission drift "causing prompts" (bypassPermissions on line 9 of the
      same file; zero prompts — this was a FALSE HIGH in the 2026-07-17 friday-checkup)
    · check-foreign-staging.sh fail-open (fixed 2026-07-18, commit 979ed01)
    · session-notes.md over threshold (resolved by the 2026-07-18 archival)
    · id-46 void closure (already closed 2026-07-13)
    · ~/.claude/settings.json "model" field — operator-DECLINED 2026-07-13, do not re-raise
-->

## Goal

The ten verified repo-health defects and improvement gaps listed under `## Open threads` are each either fixed and verified by execution, or explicitly closed with a recorded reason — so that (a) `/prime`'s orientation scan surfaces the real backlog at a bounded cost, (b) the concurrency-safety layer survives a fresh clone, and (c) the audit tooling that feeds this backlog stops emitting findings that are false, stale, or mis-attributed.

## In scope / Out of scope

- **In:** `ai-resources` — its `.claude/commands/`, `.claude/agents/`, `.claude/hooks/`, `docs/`, `logs/`, `logs/scripts/`, `audits/working/`, and the hook-wiring surface in `~/.claude/settings.json` insofar as it governs ai-resources sessions.
- **Out:** the workspace root and the 24 project repos under `projects/` (except where a thread names a specific file there); research and advisory content; the research-workflow deploy mission (`research-workflow-deploy-fitness`) — a separate active mission; any item on the DELIBERATELY-NOT-INCLUDED list above.

## Validation contract

> Written at mission creation, before any implementation session, so a fresh-context check can judge against it rather than against a session's own account of itself.

**Acceptance assertions** — must ALL be true when the mission is complete:
- [ ] The `/prime` Step 3 improvement-log scan returns under 40 lines, and no hit in its output sits on an entry whose status is applied / resolved / declined.
- [ ] Every entry in `logs/improvement-log.md` is reachable by the Step 3 scan, or is provably out of its intended scope by a stated rule — verified by a count, not an assertion.
- [ ] A fresh clone of this workspace onto a new machine has every hook that fires today, firing — demonstrated by execution on a real second checkout, not by reading the settings file.
- [ ] `git checkout` runs without a permission block in a normal session.
- [ ] A mission thread can be ticked off through a sanctioned path that does not require a hand-edit the mission contract forbids.
- [ ] Each of the ten threads is closed with either an execution-verified fix or a recorded decline reason. No thread is closed by assertion alone.

**Non-negotiables** — boundaries no session may cross:
- **Verify by execution, not by reading.** Every claim that a thread is fixed must cite a command and its output, or a file:line re-read. This mission exists partly *because* logged claims went stale unnoticed; it must not add more.
- **No new gate to solve a discipline problem.** Threads 7 and 9 are about verification behaviour; the countermeasure that has worked here is an adversary told to distrust the author, not another checklist.
- **Do not widen scope into the projects.** A thread that turns out to live in another repo is surfaced and routed, never reached into.
- **Structural fix as default style; ROI decides scope.** A thread that does not earn a structural fix is parked with a reason, not patched.
- **Close the log entry when the fix lands.** Several threads here exist only because a shipped fix never had its log entry flipped. Flipping the entry is part of the fix, not follow-up.

**Off-mission signals** — what drift looks like for THIS mission:
- Editing research-workflow or project content.
- Building a new command, agent, or hook where an existing one needed a fix.
- Re-opening anything on the DELIBERATELY-NOT-INCLUDED list without new execution evidence.
- Reporting a thread complete without a cited command or file:line.

## Open threads

> **REPOPULATED 2026-07-18 (S10-163).** The open-thread list was re-derived from a second five-agent
> verification pass (`audits/2026-07-18-verified-backlog-triage.md`), a `/consult` System Owner advisory
> (`projects/axcion-ai-system-owner/output/consultations/consult-2026-07-18-verified-backlog-prioritisation.md`),
> and an external Codex review. Goal / In-Out scope / Validation contract are UNTOUCHED and remain frozen.
> Closed threads (1, 2, 4, 6, 8) are retained verbatim as the record.
>
> **Ordering criterion changed, deliberately: dependency, not severity.** The prior list ranked by
> consequence-if-unfixed. That is a severity ordering, and it is wrong for a mission — thread 11 (the
> blind search instrument) now sits first because *every other thread's verification runs through it*.
> Fixing anything else first means re-verifying it afterwards.
>
> ⚠ **This repopulation was hand-written, which the contract at `mission.md:12` does not sanction.**
> `/mission` exposes `create | list | read | check | close` — there is no `update` verb, so there is no
> sanctioned path to revise a thread list. This is the **second** time that gap has forced a direct write
> (first: `research-workflow-deploy-fitness` S10). Logged as thread 12's sibling defect; do not treat the
> precedent as permission.

Ranked by dependency, then consequence (2026-07-18 repopulation). `[BROKEN]` = defective now; `[IMPROVE]` = not broken, real gain available.

- [x] **1. [BROKEN] Drain the improvement-log backlog — the `/prime` scan has re-bloated ~9x in five days.** The Step 3 scan returns 223 lines / ~45,800 chars (~11–12k tokens) every session, against 25 lines when the fix shipped 2026-07-13. 29 resolved-class entries are still unarchived and 30 open entries are 30–57 days old. 15 of 28 severity hits sit on already-applied/resolved/declined entries. *Cost: ~11–12k tokens every session, and over half the task menu is completed work.* (`/resolve-improvement-log` is the drain; the Friday report's counts of 7 and 19 were both wrong.)
- [x] **2. [BROKEN] A third of the improvement backlog is structurally invisible to `/prime`.** `prime.md:206` anchors on `^- **Severity:**`. Of 102 live entries, 69 carry that anchor, 2 use an un-dashed variant, and 31 have no Severity line at all — 33/102 (32%) unreachable. Three entry formats are live, not the two the log entry claims. *Cost: a third of the backlog can never reach the task menu — the one channel that demonstrably converts findings into shipped work. Two invisible entries carry ~40–70k/session and ~150k/occurrence savings.*
- [ ] **3. [BROKEN] Hook wiring is unversioned — a fresh clone silently loses the concurrency-safety layer.** `~/.claude` is not a git repo (`git rev-parse` → fatal). Seven hook registrations exist only there — `check-foreign-staging.sh`, `check-destructive-liveness.sh`, `detect-concurrent-session.sh`, `warn-fable-model.sh`, `detect-innovation.sh` (Write+Edit), `cleanup-session-marker.sh` — and `cleanup-session-marker.sh`'s *body* is unversioned too. *Cost: a new machine gets the scripts sitting in `.claude/hooks/` looking installed, with nothing firing them, and no error. This also silently un-ships the fixes that closed the marker-collision and staging-guard items.* Note: a prior installer design scored High/High on `/risk-check` twice — requires timestamped backup of `~/.claude/settings.json`, an idempotent merge preserving all unrelated keys (including the operator-declined `model` field), and its own risk-check.

  **Re-verified 2026-07-18 (S10-163): all nine findings in the parked plan hold, none stale, directly resumable** (`logs/session-plan-2026-07-18-S9-f53-thread3-parked.md`). Confirmed live: `~/.claude` is not a git repo; **9 registrations, 2 are `afplay` → 7 repo scripts**; 6 hardcode `/Users/patrik.lindeberg/`, the 7th uses `$HOME`; `cleanup-session-marker.sh`'s body exists only at `~/.claude/hooks/` (6785 bytes); `install-hooks.sh` and `check-hook-wiring.sh` both absent.

  ⚠ **MERGE CONTRACT TIGHTENED (external review, 2026-07-18).** "Preserve unrelated top-level keys" is **not sufficient** and would have shipped a defect: the `hooks` object itself contains unrelated registrations — the two `afplay` entries — which a key-scoped merge clobbers while appearing to succeed. The installer must **deep-merge managed registrations**, preserving unrelated entries *inside* `hooks`, not merely unrelated keys beside it. **Test against a fixture `$HOME` before touching live settings, then prove firing from a second checkout** — a merge that reads correct is exactly the class of claim this mission forbids accepting.
- [x] **4. [BROKEN] `git checkout` is hard-blocked, and the queued fix targets the wrong rule.** Denied by name at `~/.claude/settings.json:47` and workspace-root `.claude/settings.json:27` — `"Bash(git checkout *)"`. *Cost: a hard block that `bypassPermissions` cannot waive; 5 logged occurrences, near-certain during any merge.* The logged diagnosis blames archive `Read()` deny patterns — falsified by the log's own data (`git add <archive>` and `git show ":N:<archive>"` both name the denied path and both ran fine). The `/friday-act` fix already queued targets those Read patterns and would ship without unblocking git. The entry's own escape hatch (`git checkout --ours .`) matches the same Bash deny.
- [ ] **5. [IMPROVE] `/permission-sweep` judges settings files individually, not by merged effective permissions.** Rules 5/6 read one file's allow array without evaluating the merged layer stack or `defaultMode`. *Cost: it emitted a CRITICAL/HIGH "ai-resources permission drift causing prompts" that reached the 2026-07-17 friday-checkup as an actionable HIGH and was entirely false — the same file sets `bypassPermissions` on line 9. Audit tooling that produces false HIGHs spends operator attention and erodes trust in every finding it makes.*

  **Re-verified 2026-07-18 (S10-163), independently confirmed by external review.** `permission-sweep-auditor.md:67` — *"For each file, apply the detection rulebook."* `defaultMode` appears **once** (`:147`) and only as a report-format token, never as a suppression check. Rules 5/6 (`permission-template.md:388,392`) are pure allow-array predicates. The emitted CRITICAL was false **twice over**: the parent layer already grants `Bash(*)` (`ai-resources/.claude/settings.json:4`) *and* line 9 sets `bypassPermissions`. **Fix: evaluate the effective merged layer stack before Rules 5/6 fire.** Small fix, very high ROI — it also prevents harmful "repairs" made in response to its own false findings.

  ✅ **THE FIX SHIPPED 2026-07-19 in commit `b7b6911` — this thread's text above is now stale. Left unticked deliberately (see the tick note below); do NOT re-open it as work.** Verified by direct read of the live files and confirmed independently by `/risk-check` (S4-2b2): `permission-sweep-auditor.md:67` now reads *"### Step 2.5: Compute the EFFECTIVE (merged) permission view — before any rule fires"*; `:76-77` define `EFFECTIVE_ALLOW` (union across ancestor layers) and `EFFECTIVE_DEFAULT_MODE` (narrowest layer wins); `:88` binds Rules 1/5/6 to those values rather than the single file, and states that a `bypassPermissions` effective mode means the prompt-causation rules *"do not fire at all"*; `:89` adds **demote-don't-delete** (`Suppressed-by:` ADVISORY) so genuine per-file drift is not hidden by the suppression. `docs/permission-template.md:384-395` carries the matching warning at the rulebook end. `git show --stat b7b6911` → exactly 2 files, +50/-3.

  ⚠ **How this thread wasted a task-menu slot, recorded because the mechanism is thread 12's:** `/prime` (S4-2b2) surfaced this as open work and it was picked into an auto-mode bundle, because the menu is built from **thread text** while the resolving commit sat unread in `/prime`'s own Step 1a cross-check output from the same orientation. It was caught in the pre-mandate verification, before any edit. `/prime` cross-checks *Next Steps* bullets against git; it does **not** cross-check *mission threads*. That asymmetry is a real gap and is queued to `improvement-log.md` (2026-07-19).
- [x] **6. [BROKEN] `/mission` has no sanctioned way to tick off a thread.** `mission.md:4,27` expose exactly `create | list | read | close`; `:28` aborts on anything else; `close` touches `status` only. The contract at `mission.md:12` states threads change *"only via this command — never hand-written from inside a working session."* *Cost: `prime.md:236` builds task-menu candidates from unchecked thread items, so a bound mission re-surfaces completed work at every session start — unless the operator hand-edits, which the contract forbids. This mission file hits the defect immediately.*
- [ ] **7. [IMPROVE] Five reviewer-class agents lack the premise-check antibody the other three carry.** Missing in `refinement-reviewer.md:45` (a permission to verify, not an obligation), `triage-reviewer.md:51` (the *inverse* — explicitly licenses proceeding on an unverified claim if labelled "assumption"), `reconcile-reviewer.md:102` (covers the artifact under review, not its own counts), `expert-check-reviewer.md:38` (one-fact scope), and `scope-qc-evaluator` (zero verification language). Present in `system-owner.md:99-101`, `risk-check-reviewer.md:194`, `qc-reviewer.md:87-106`. *Cost: `scope-qc-evaluator` issues five-way build/park verdicts with the least verification language of any of the eight agents; `triage-reviewer` decides what leaves the queue. `improvement-log.md:1105` records this exact failure at higher authority — `system-owner` fabricated a count that was indistinguishable in tone from its true findings.*

  ⚠ **NARROWED 2026-07-18 (S10-163) — do NOT copy the full rubric into all five agents.** The gap was verified but treated as uniform; it is not. External review established that `refinement-reviewer` and `expert-check-reviewer` have deliberately narrower jobs where blanket filesystem investigation duplicates QC and adds cost for no verification gain. Copying the rubric everywhere is exactly the over-application workspace `CLAUDE.md § Subagent Proportionality` forbids ("the lightest form that actually verifies").

  **In scope — the two that actually decide things:**
  - **`triage-reviewer:51`** — carries the *verbatim inverse* of the antibody: *"If you can't verify a claim from the suggestion text alone, note it as an assumption rather than launching a search."* This agent decides what leaves the queue.
  - **`scope-qc-evaluator`** — `command grep -c "verify|re-derive|filesystem"` → **0**, while it issues five-way build/park verdicts.

  **Scope of the obligation:** require re-derivation of load-bearing **current-state, count, path, and absence** claims — not a general instruction to verify everything. **`reconcile-reviewer` assessed separately** (its `:61` hedging is artifact-scoped; whether that extends to its own counts is a distinct question). `refinement-reviewer` and `expert-check-reviewer` — **no change**.

  **Closes thread 9 by construction** (see below).
- [x] **8. [BROKEN] `run-manifest.sh` cannot close a manifest across midnight.** `:156` re-derives `DATE` per invocation; `:166-168` trusts the marker file only if dated today; `:171` dies otherwise. `wrap-session.md:253` documents omitting both flags on purpose. Reproduced in sandbox: start-stub `2026-07-17-S9.json`, close on 07-18 → `exit=2`, stub never finalized. With `--marker` pinned but not `--date`, two manifests are written for one session. No midnight case exists in `run-manifest.test.sh`. *Cost: any session wrapping after midnight leaves a permanently null-outcome record — rare, non-blocking, but it silently corrupts the durable substrate crash-detection is built to read.*
- [ ] **9. [FOLDED INTO 7 — not independently actionable; ticks when 7 ticks] The assert-from-recall pattern passed its escalation trigger and still has no standing rule.** 8 logged instances 2026-07-13→07-14 (friction-log L346, L516, L536, L584, L642; improvement-log L651, L857, L1105) against a trigger of 6. Zero hits for a standing rule in workspace CLAUDE.md, ai-resources CLAUDE.md, or repo-wide. Covered today: mandate `Files in scope` (mechanical hard-reject), gate *inputs* for two gates, reviewer *outputs* for three agents. Not covered: facts stated in plans, free-text subagent prompts, the five agents in thread 7, and chat to the operator. *Cost: a plan carrying five false factual claims authorized ~360k tokens of review that missed the two that mattered.* Note the entry's own warning — do not build another checker; thread 7 is the coherent countermeasure.

  **FOLDED INTO THREAD 7, 2026-07-18 (S10-163) — closes when 7 closes; not separately actionable.** Re-verified: zero matches for any recall/re-derivation rule in workspace or ai-resources `CLAUDE.md`, against 8 logged instances and a trigger of 6. But the residual surface after thread 7 lands — facts stated in plans, free-text subagent prompts, chat to the operator — is **discipline, not mechanism**, and this mission's own non-negotiable forbids building another checker for it. Do not open this as an independent thread; ticking 7 ticks 9.
- [ ] **10. [BROKEN] Repo-mutating commands stand in for a liveness fact by reading the repo at rest.** Verified instance: `/permission-sweep` mutates settings workspace-wide (`:5`, `:19`) and its only concurrency mention (`:324`) defers to commit time rather than checking whether another session is live. A scan of 11 mutating commands found 10 with zero liveness references; only `/wrap-session` has any. *Cost: with concurrent sessions now routine in this workspace, a sweep can overwrite a live session's uncommitted settings edits.* Honest scoping: 1 confirmed, 9 unverified candidates — the logged figure of "seven more gates" was not reproducible. First step is to verify the 9, not to fix 10.

  **Re-verified 2026-07-18 (S10-163).** 10 of 12 mutating commands carry zero liveness references (`wrap-session` 11, `new-project` 1 incidental). Scoping confirmed accurate; **consequence smaller than the source entry claims** — since `979ed01`, `detect-concurrent-session.sh` runs at every SessionStart (wired `~/.claude/settings.json:78`), but it fires once at start and only warns. ⚠ **Recommended shape: ONE shared pre-flight called by the few commands that write repo-wide — `/permission-sweep` first — not a liveness gate in ten commands**, which would violate the non-negotiable at `:64`. Verify the 9 candidates before building anything.

- [ ] **11. [BROKEN] The repo's search instrument is blind, and every absence-claim has been made through it. — RUN THIS FIRST.** `type grep` resolves to a **shell function** (`~/.claude/shell-snapshots/snapshot-zsh-*.sh`) dispatching to `ugrep --ignore-files`. Measured on the term "Severity": workspace root **10 hits (shell) vs 612 (`command grep`)**; inside ai-resources **122 vs 194**. Independent runs on other terms returned 8-vs-720 and 55-vs-605 — **the magnitude is real; the exact percentage is term-dependent and is NOT a fixed property of the instrument.** Do not quote a single percentage as if it were. *Cost: a zero-hit scan is indistinguishable from a blind instrument, so every absence-claim in every past audit — and every verification this mission performs — carries silent false-negative risk.* **Dependency-first: this thread precedes all others because their verification runs through it.** Fix: `command grep` / `git grep` / `rg -uu` with explicit scope in scanning commands and agents, **plus a known-positive canary** so blindness announces itself instead of returning a clean-looking zero. Scoped to absence-based scanning; "every audit is broken" is an overstatement.
  - **WORKED 2026-07-18 (S11-637) — premise confirmed, impact narrowed by roughly an order of magnitude. Deliberately left unticked; see the tick note at the end.** Four properties established by execution, each of which narrows the thread as filed:
    1. **Premise CONFIRMED.** `grep` is a shell function (snapshot line 83) → bundled ugrep with `--ignore-files --hidden -I --exclude-dir=.git`. The 122-vs-194 figure reproduced exactly. Blind set: `audits/working/`, `logs/scratchpads/`, `inbox/archive/`, `/archive/`.
    2. **Not argv — traversal only.** A gitignored file named explicitly is still searched (`grep "defaultMode" .claude/settings.local.json` → 1, matching `command grep`). Every explicit-path grep is immune.
    3. **Not across a process boundary.** The shadow is a shell *function*; `bash -c 'type grep'` and `zsh -c 'type grep'` both return `/usr/bin/grep`. So every grep inside a `.sh` file — all of `.claude/hooks/` and `logs/scripts/` — already runs the real grep and was never exposed.
    4. **Only the dot-rooted walk is blind.** Measured with one planted sentinel: `grep -r <s> .` → 0; `grep -r <s> ./` → 0; `grep -r <s> audits` → 1; `grep -r <s> /abs/path` → 1; `command grep -r <s> .` → 1.
  - **The consequence inverts the thread's own fix instruction.** The filed fix was *"`command grep` / `git grep` with explicit scope in scanning commands and agents."* Checked 2026-07-18: **no committed site in `.claude/commands`, `.claude/agents`, `.claude/hooks` or `logs/scripts` uses the dot-rooted form** — every one names a subdirectory or an absolute path, and is therefore immune. Editing them would be churn with no named consequence, and would fail the materiality bar. **Zero site edits were made, deliberately.**
  - **What IS exposed: the ad-hoc `grep -r <term> .` a session types while verifying something** — including the greps that produced this mission's own evidence, and the 122-vs-194 measurement in this very thread. The defect is in how we *verify*, not in what we have *committed*. That is not a smaller problem, but it is a different one, and it cannot be fixed by editing files.
  - **Shipped:** (a) `logs/scripts/search-canary.sh` — plants a known-positive in a gitignored path and reports whether the ambient shell can see it. **Must be sourced, not executed.** (b) `docs/audit-discipline.md` § *Absence-claims: the search instrument is not neutral* — the four properties above as a table, plus the rule that any "there is no X" claim must state its instrument.
  - **⚠ Two can-never-fail drafts were caught before landing, by requiring the check to FAIL before trusting it.** Draft 1 ran as an executed script → child process → real grep → reported "clear" in a demonstrably blind shell. Draft 2 walked from the ignored directory itself, which defeats the ignore → also "clear". Both are the repo's most-repeated defect class ("inert safeguard"), and they nearly shipped *inside the session convened to fix search reliability*. The canary now reports `blind` (exit 1) when sourced and refuses with `inconclusive` (exit 2) when executed — both verified.
  - **Also recorded, not fixed:** `find` is shadowed too (snapshot line 71 → bundled `bfs`), but carries no ignore flags, so it is **not** blind. Thread 11 names only `grep`; noted so a future session does not re-investigate.
  - **The irony worth keeping:** the blind set includes `audits/working/`, and `CLAUDE.md § Subagent Contracts` *requires* audit subagents to write their full findings there. The repo's convention deposits its evidence in the one directory its default search instrument cannot see.
  - **Not ticked, on purpose.** Per this mission's standing instruction, hand-ticking is the unverified-tick mechanism thread 12 exists to fix. This thread's acceptance should be judged against the validation contract once `/mission check` can read it. The work above is done and committed; the tick is not mine to make.

- [ ] **12. [BROKEN] `/mission check` ticks a thread without ever reading the validation contract — and it has already produced a false tick in THIS mission.** `mission.md:19-21`: the command matches a substring against `## Open threads`, flips `- [ ]` to `- [x]`, and "changes **nothing else**." It never opens the `## Validation contract`. **Live proof, in this file:** threads 1 and 2 are `[x]` (`:78-79`), while the acceptance assertion they exist to satisfy — *"the `/prime` Step 3 scan returns under 40 lines"* (`:54`) — measures **222 lines**. Both underlying fixes shipped correctly; the tick recorded *mechanism fixed* where the contract asked for *outcome achieved*. *Cost: this is the generator. Every thread in this mission can be closed against a contract it does not satisfy, which is precisely the stale-record disease the mission was convened to cure — reproducing inside the mission's own instrument.* **Sibling defect, same command:** there is no `update` verb, so revising a thread list requires an unsanctioned hand-edit (now done twice). Fix both together. Do **not** fix by adding a gate — make `check` read the contract and refuse-or-warn when the named assertion is unmet.

- [ ] **13. [BROKEN] Five backlog entries assert defects that are already fixed — including one labelled "the highest-value structural item in this log."** Verified in live files: **L800** is the *spec of the fix that shipped* (its own recommendation — suffix the marker, `S3-a4f` — is live; today's headers read `S1-dec`…`S10-163`); **L753** is "blocked" on rebasing a stale worktree that does not exist (`git worktree list` → one checkout); **L733** closed by the session-id-in-name change (`prime.md:413-426`); **L838** fixed in `3179771`; **L926** fixed (`prime.md:21-27`, `:47-56`). *Cost: ~a third of the surfaced backlog is not real work, and `/prime`'s task menu spends slots on it.* Flip and archive **with cited evidence per entry** — the citation is the deliverable, not the flip. **Do not reopen the underlying fixes.** Closes with thread 12, not before: flipping five entries by hand while the tick mechanism stays unverified just reruns the disease.

- [ ] **14. [BROKEN] The project template ships hooks that are wired to fire nowhere — one defect currently split across three severities.** Merged 2026-07-18 from three separately-logged items that share one cause. (a) **`warn-settings-change.sh` fails open** — `:6` reads `d.get('file_path')` where the PreToolUse payload nests it under `tool_input`; real payload → **exit 0 (allows)**, obsolete shape → exit 2. Registered in **0 of ~70** settings files. (b) **`check-claim-ids.sh`** — 6 deployed/template copies, **2 live registrations**; the template ships it unwired, so 4 of 6 locations never fire. (c) **friction-log auto-capture** — live in `ai-resources` (proven by execution), **dead in the template** (PreToolUse/`Skill` only) and in 5 of 6 deployments. *Cost: three guards that look installed to anyone auditing and protect nothing — and (a) is already queued to be **copied as the reference implementation** ("proof the exit 2 pattern works", `audits/risk-checks/2026-07-14-repo-repair-pilot-v1…:13`), so the defect is actively replicating.* For (a), decide explicitly: **repair-and-wire, or delete the orphan.** Leaving a broken guard in place unranked is the failure mode.

  ✅ **ALL THREE SUB-DEFECTS ADDRESSED 2026-07-19 (S4-2b2), gated by `/risk-check` — 4(a) PROCEED-WITH-CAUTION, 4(b) and 4(c) GO** (`audits/risk-checks/2026-07-19-bundled-staging-hook-repo-resolution-thread-14-orphan-hooks.md`). **Left unticked** per the standing tick note; the routed remainder below is real and outlives the code changes.
  - **(a) DELETED.** `.claude/hooks/warn-settings-change.sh` removed via `git rm` (recoverable at `ff46804`). Full reasoning, alternatives, and the load-bearing cross-project consequence: `logs/decisions.md` 2026-07-19 (S4-2b2). Post-delete re-check confirmed zero registrations pointed at it, so nothing broke.
  - **(b) WIRED.** `check-claim-ids.sh` registered in the research-workflow template as `PostToolUse | matcher=Write` — matching the two live precedents verbatim (`projects/buy-side-service-plan/.claude/settings.json`, `archive/nordic-pe-macro-landscape-H1-2026/.claude/settings.json`), not an invented shape. It is a **non-blocking** warner (`exit 0`) for `[CITATION NEEDED]` tags in pipeline artifact paths, so wiring it adds no gate.
  - **(c) PARITY RESTORED.** Template gained `PostToolUse | matcher=Bash|Write|Edit|Agent|Skill → friction-log-auto.sh`, matching canonical `ai-resources/.claude/settings.json`. JSON re-parsed and validated after both edits.
  - ⚠ **Two counts in this thread's own text were overstated, corrected by the gate.** (b) "the template ships it unwired, so 4 of 6 locations never fire" — there are **7** copies on disk, and 3 registrations exist, not 2. (c) I briefed "dead in every deployment"; the thread's own "5 of 6" was right and my restatement was wrong — **`positioning-research` already has both branches wired**. The fixes are unaffected (both target the template, which was genuinely unwired); only the urgency framing was inflated.
  - 🔀 **ROUTED, not done — separate repos, per this mission's non-negotiable.** Three `status: active` vault docs still describe `warn-settings-change.sh` as a live component: `projects/repo-documentation/vault/blueprint/blueprint.md:105`, `vault/components/hooks.md:153-168`, `vault/architecture/system-doc.md:200`. And **System Owner v2 stage S2/B3 plans to wire the file this session deleted** (`projects/project-planning/…/technical-design.md:32-34`, `execution-roadmap.md:30`) — that build item now needs re-planning from scratch, not re-pointing. Both directories are their own git repos (`rev-parse --show-toplevel` resolves to themselves), so they were surfaced and routed rather than reached into. **This routed remainder is why the thread stays open.**
  - **4 of the 5 remaining unregistered `check-claim-ids.sh` copies** (axcion-sector-intelligence, positioning-research, research-pe-regime-shift-advisory-gap, archive) are likewise out of mission scope — routed, not edited.

- [ ] **15. [BROKEN] `/prime`'s Step 3 scan costs ~12k tokens every session, in every project — and draining is exhausted as a remedy.** Measured live: **222 lines / 47,753 chars**, 28 severity hits, against the command's stated **<40-line** budget (5.6×). Serves Goal clause (a). **This is the one thread that is actively worsening**, and its cause is two *correct* fixes composing: threads 1 and 2 both landed (104 entries, **0 unclassified, 0 resolved-unarchived**), and the Severity backfill created 21 new `medium-high` entries that the `-B6` window now carries. ⚠ **Neither obvious remedy is available:** `prime.md:217` explicitly forbids narrowing `-B6`, and the drain has already run. **A third approach is required — change what the scan *emits*, not what it *reads*** (parse entries, emit compact unresolved-HIGH summaries). Independently reached by both the System Owner advisory and external review. Also delete the stale "30 of 87" prose at `prime.md:221`, falsified by its own fix. **Risk-check class** — `prime.md` is distributed to every project by symlink.

  ✅ **The stale-prose sub-item is DONE 2026-07-19 (S4-2b2)** — gated (`/risk-check` PROCEED-WITH-CAUTION, Blast radius High/mitigated) and verified. ⚠ **It was TWO stale figures, not one:** the same falsified count appeared again later in the paragraph as *"dumping 30 unclassified entries"*; both removed, surrounding "count, not a content read" rationale preserved intact, read back from disk, and confirmed propagating to symlinked consumers (spot-checked `harness/` and `management-os` → both resolve to canonical, 0 stale hits). ⚠ **Line-number drift, third citation in a row wrong:** this thread says `:221`, the prior session's notes say `:220`, live was `:224` (`command grep -n`). Re-derive before editing; do not trust a cited line number in this file. **The thread's MAIN body — the ~12k-token scan cost — is untouched and remains fully open.** Nothing in S4-2b2 reduced it; the externalization gate brief is still unwritten.

- [ ] **16. [BROKEN] The destructive-command override accepts inert overrides and logs successes that never happened.** Two independent defects in `check-destructive-liveness.sh`. (a) `:207` matches `\bAXCION_LIVENESS_OVERRIDE=1\b` **anywhere in the command string**, never checking the variable binds to the destructive verb — confirmed by execution: `NOTE=AXCION_LIVENESS_OVERRIDE=1 git reset --hard` was accepted and logged as a genuine operator override. (b) `:215-224` writes the audit line and exits 0 at **PreToolUse — before execution** — with no `outcome` / `exit_code` field in the schema. *Cost: the audit trail records "proceeded" for commands that never ran, so the guard's own evidence base cannot be trusted — and it is the evidence base a future session would use to judge whether the guard works.* Fix (a) first (cheap, stops accepting inert overrides), then (b). Ordering matters: (b) is the messier half and (a) removes the live hazard.

### Also noted, below the top ten (park or fold into a thread)

> Revised 2026-07-18 (S10-163). Three bullets below carried numbers or targets that verification
> falsified — corrected in place rather than deleted, so the correction is on the record.

- **[CORRECTED — every number in the original bullet was wrong, and the framing inverted.]** ~~`audits/working/` has grown to 328 files / 4.1 MB, 53 files older than 60 days… 122 files (1.4 MB, 40%)… Housekeeping — one non-dry `/log-sweep` away.~~ Measured 2026-07-18: **397 files, 4.4 MB, 88 older than 60 days, 123 log-sweep notes at 1436 KB = 31.6%** (338 >30d, 9 >90d, **0 >120d** — young and dense, not neglected; 13 of 397 tracked). And it is **not** "one sweep away": `/log-sweep` **already self-excludes** its own notes at three layers (`log-archiver.sh:80-82`, `log-sweep.md:63`, `:300`), which is precisely *why* 31.6% of the directory can never be swept. **Not a mission thread** — housekeeping, and the System Owner's counter-argument (that structural unsweepability is itself a reason to change the structure) is noted and **declined for this mission**, with external review concurring. Revisit as ordinary backlog.
- **[CORRECTED — count unresolved; two sources disagree.]** Friction-log regions unreachable by its four parsers (`/improve:9`, `/open-items:35`, `/reconcile-backlog:40`, `/friday-checkup:317`): **3** confirmed (2 blocks missing the `### Friction Events` heading at `:554`, `:565`; 1 drifted header at `:629` — line numbers shifted from the original L468/L479/L616). ⚠ **Unresolved discrepancy: how many canonical headers the `/friday-checkup` dormancy regex misses — one source says 20, an external review says 37. Re-derive before sizing.** ⚠ **COUPLING, load-bearing: that regex matches ONLY the 2 drifted headers and none of the canonical blocks — so repairing the drift BREAKS `/friday-checkup`.** The two must move in one change, never separately.
- **[FOLDED INTO THREAD 14]** ~~`.claude/hooks/warn-settings-change.sh:6` reads a top-level `file_path`…~~ Promoted into thread 14 (a) — it is one instance of the "template ships hooks that fire nowhere" defect, not a standalone note.
- **[CORRECTED — the replacement path named here was also wrong.]** `docs/permission-template.md:226` (not `:224`) names a path that no longer exists (`projects/obsidian-pe-kb/vault/`). ~~the file actually showing the pattern is `projects/strategic-os/`~~ — **false**: the vault was *relocated* in `83669d5`, and the correct target is **`knowledge-bases/pe-kb-vault/.claude/settings.json`**, whose contents match the doc's illustrative shape. `strategic-os` is never mentioned in the doc, is not narrow, and is **queued for dissolution in `logs/retirement-backlog.md`** — so acting on the original bullet would have written a fresh stale reference into a canonical doc. Two-line fix; verify the target before editing.

### Routed OUT of this mission (2026-07-18 — recorded so they are not silently lost)

- **The staging guard rejects schema-correct sessions** (`check-foreign-staging.sh` reads `Files in scope`, never `Required outputs`; 0 hits across 589 lines; reproduced → exit 2 with no concurrent session). **Real, confirmed by execution, and rated critical / very-high-ROI by external review** — but it serves **none of this mission's three Goal clauses**: it neither bounds the orientation scan, nor survives a fresh clone, nor stops false audit findings. It blocks work loudly; it does not corrupt verification. **Route: ordinary backlog, own fix session, soon.** Fix shape: parse both fields and treat their **union** as the permitted write footprint. Do not smuggle it in as a mission thread because it is urgent — urgency is not mission fit.
- **Both live research projects run a stale rules chassis** (`research-pe-regime-shift-advisory-gap`, `positioning-research` — neither carries the 2026-07-14 chassis marker; a measured re-adjudication of one cluster moved **2 of 6 claims** down a class). **Routed out under non-negotiable `:64`** ("do not widen scope into the projects — surface and route, never reach in"). **Route: `research-workflow-deploy-fitness` mission**, which owns those files. Two corrections carried with it: the evidence covers **one tested table, not every table in both projects**; and **back-porting the chassis is not the fix** — it must be followed by re-adjudicating §1.1, diffing the verdicts, and regenerating affected downstream material. Trigger is **not currently armed** (both §1.1 sentinels present) but `positioning-research/logs/decisions.md:136` instructs a sentinel deletion that would arm it.

### Closed as won't-fix (do not re-raise without new execution evidence)

- **`rm` inside a `for` loop over `$(find …)`** — FALSE on code. The construct exists **nowhere** in `logs/scripts/` or `.claude/hooks/`; every `rm` takes a quoted literal path, and the one surviving `for … in $(find …)` (`prime.md:403`) is non-destructive. Survives only in log prose. *(Residual: the entry's prescribed doctrine remedy never landed — `commit-discipline.md:44` covers only the worktree liveness probe. Low consequence; not a thread.)*
- **friction-log auto-capture PostToolUse branch "is dead code"** — FALSE. Registered in `ai-resources/.claude/settings.json` and **proven live by execution** against a scratch copy. Its real defect (dead in the *template*) is thread 14 (c).
- **Partial conflict enumeration** — the `/prime` step the entry proposes fixing **does not exist**; all 9 "conflict" hits in `prime.md` are git-rebase text, and `prime.md:88` reads `decisions.md` via `tail -n 10`, structurally incapable of enumerating implicated decisions. **Rewrite the entry against its real attach point (workspace `CLAUDE.md § Design Judgment Principles`) or close it — do not implement it as written.**
- **Stacked gate questions** — ⚠ **reclassified, not closed** (System Owner, `AP-11`). The original closure reasoned "the rule is written 3×, therefore this is a discipline problem, therefore the non-negotiable at `:63` forbids acting." That reasoning was **wrong, and it was cover**: a rule written three times and still unfollowed is evidence the **written rule is wrong**, not evidence the operator lacks discipline. The non-negotiable forbids building a *gate*; it does not forbid *diagnosing the rule*. **Re-scope as a measurement question** — what does the rule actually cost when followed, and where does it misfire — and do not build a tripwire (the ≥2-prompt signal is in-session state no hook can observe).
- No control exists to prevent reference-file surplus on a pipeline hot path (no template, no size or read-frequency rule in `ai-resource-creation.md` or `ai-resource-builder`). The specific file named in the 2026-07-16 entry no longer exists and its surplus could not be confirmed — but `workflows/research-workflow/docs/required-reference-files.md:89` still requires it with no template shape, so every future deployment invents it. Route the file-existence half to `axcion-sector-intelligence`.

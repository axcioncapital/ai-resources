# Session Plan — 2026-07-18

## Intent
Make the repo's blindness-exposed search sites declare their scope explicitly, and add a known-positive canary so a blind scan announces itself instead of returning a clean-looking zero (mission `repo-health-backlog-2026-07`, thread 11).

## Model
opus — match (deciding work: per-site judgment on intended scope, plus a structural-risk call on a symlink-distributed file).

## Source Material
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/repo-health-backlog-2026-07.md` — thread 11 as written
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/agents/risk-check-reviewer.md` — site 1
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/agents/lean-repo-auditor.md` — sites 2 and 3
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/deploy-kb.md` — site 4
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/audit-discipline.md` — structural change classes
- `/Users/patrik.lindeberg/.claude/shell-snapshots/snapshot-zsh-1784402953270-62cyt2.sh` — lines 68–97, the shadowing itself (read-only reference)

## Findings / Items to Address

Verified this session by execution. **Two corrections to thread 11 as written — both narrow it.**

1. **Premise CONFIRMED.** `grep` is a shell function (snapshot line 83) dispatching to a bundled ugrep with `--ignore-files --hidden -I --exclude-dir=.git`. Magnitude reproduced exactly: `grep -rl "Severity" .` → **122 files**; `command grep -rl` → **194**. The 72-file gap is `audits/working/`, `logs/scratchpads/`, `inbox/archive/`, `/archive/`.

2. **CORRECTION A — the blindness applies to tree traversal ONLY, not to explicitly named files.** Decisive test: `grep "defaultMode" .claude/settings.local.json` (a gitignored file, named explicitly) returns **1 hit**, matching `command grep`. `--ignore-files` filters directory walks; it does not filter argv. Consequence: every grep that names its files explicitly is immune, which is most of them.

3. **CORRECTION B — the exposed set is 4 sites, not the 14 in the mandate.** The mandate's count came from a heuristic regex of mine that over-matched *prose mentions* of grep (e.g. `prime.md:690` advising `grep -rl`, `deploy-workflow.md:414` warning against a `grep -r` assertion). Re-derived precisely: 6 true `grep -r*` invocations, of which `monday-prep.md:182` names explicit files (immune per Correction A) and `deploy-workflow.md:414` is prose already telling the reader *not* to make the assertion. **The mandate line must be corrected from 14 to 4** — a mandate carrying a false count would fail `/contract-check`.

4. **Site 1 (highest stakes) — `risk-check-reviewer.md:56`:** `grep -rniI --exclude-dir=.git "<term>" "{AI_RESOURCES}" "{AI_RESOURCES}/.."` builds the **consumer inventory** that every structural risk verdict rests on. A consumer living in a gitignored path is invisible, and the failure mode is a GO verdict issued against an incomplete inventory. Intent here is *find every consumer*, so the correct scope is everything → `command grep -r`.

5. **Sites 2–3 — `lean-repo-auditor.md:53,54`:** the wiring scan (`ai-resources/.claude/...`) and usage scan (`projects/*/logs/`) behind orphan detection. This is the instrument whose unqualified form produced an operator-approved instruction to delete **six commands, four in live use** (2026-07-13). The file already carries strong "never emit orphan → Remove" doctrine, so consequence is *mitigated* — but the instrument still under-reports, and `logs/scratchpads/` (gitignored) is exactly where un-logged usage evidence would sit. Intent is *find every trace of use* → `command grep -r`.

6. **Site 4 (lowest value) — `deploy-kb.md:128`:** `grep -r '{{' "{VAULT_PATH}/"` checks a deployed vault for unreplaced placeholders. Advisory (`|| true`), and a deployed Obsidian vault carries little gitignored content. Fix for consistency, not because a failure is likely.

7. **Recorded, NOT fixed — `find` is shadowed too.** Snapshot line 71 replaces `find` with bundled `bfs`, but with no ignore flags (`-S dfs -regextype findutils-default`), so it is **not** blind. Thread 11 names only `grep`; this is worth writing down so a future session does not re-investigate. No code change.

8. **The root irony, worth recording in the mission.** The gitignored blind set includes `audits/working/` — and `ai-resources/CLAUDE.md § Subagent Contracts` *requires* every audit subagent to write its full findings there. The repo's own convention deposits its evidence in the one directory its search instrument cannot see. This is the strongest argument for the canary: no per-site fix generalises to that, because the next author will write the next scan with bare `grep` too.

9. **Scope note — the shadowing cannot be removed.** It lives in a harness-generated shell snapshot regenerated per session, outside any repo. Only invocation sites are ours to change. Any proposal to "fix grep" is out of scope by construction.

## Execution Sequence

1. **Correct the mandate line** in `logs/session-notes.md` — 14 sites → 4, with the two corrections named. *Verify:* re-read the mandate block; `Files in scope` lists exactly the 4 edited files plus the canary in `Required outputs`.

2. **Write the canary** → `logs/scripts/search-canary.sh`. Asserts a known-positive term inside a gitignored path is findable; exits non-zero with a loud message when the ambient `grep` cannot see it. *Verify:* run it twice — once with `command grep` (must pass), once through the shadowed `grep` (must fail loudly). A canary that passes in both states is worthless and must be rejected.

3. **Fix site 1** — `risk-check-reviewer.md:56` → `command grep -rniI`, with one inline sentence stating why (consumer inventory must not silently skip gitignored paths). *Verify:* re-grep the file; confirm no bare `grep -r` remains.

4. **Fix sites 2–3** — `lean-repo-auditor.md:53,54` → `command grep -rn`, tied to the existing Q3 verdict contract already in that file. *Verify:* as above.

5. **Fix site 4** — `deploy-kb.md:128` → `command grep -r`. *Verify:* as above.

6. **Run `/risk-check`** (plan-time gate, mandatory — see Risk). *Verify:* verdict recorded to `audits/risk-checks/`; on RECONSIDER/NO-GO, stop and revise rather than landing.

7. **Record findings 7 + 8 in the mission file** as thread 11 closing notes. Do **not** tick thread 11 — per the mission's own standing instruction, hand-ticking is the unverified-tick mechanism thread 12 exists to fix. *Verify:* thread 11 remains `- [ ]`.

8. **Commit.** *Verify:* `git status` clean for the intended paths.

## Scope Alternatives

- **Min:** canary only (step 2). Leaves the 4 sites lying but makes future blindness self-announcing. Defensible if `/risk-check` returns RECONSIDER on touching the reviewer agent.
- **Recommended:** canary + all 4 sites + mission notes. This plan.
- **Max:** additionally sweep the ~35 explicit-path greps to `command grep` for uniformity. **Rejected** — Correction A proves they are immune, so this is churn with no named consequence, and it would touch far more files for zero risk reduction.

## Autonomy Posture
Gated.

**Stop points:**
- Before landing any edit: `/risk-check` verdict (step 6). On RECONSIDER or NO-GO, stop and revise — do not land.
- If the canary passes in both grep states (step 2), stop: the canary is not measuring what it claims and must be redesigned before it is trusted.

## Risk
Run `/risk-check` after the plan is approved (plan-time gate). Run it again before commit (end-time gate).

Structural classes touched: **edits to a reviewer agent that gates other structural decisions** (`risk-check-reviewer`), and **a new executable** (`logs/scripts/search-canary.sh`). Blast-radius note: `risk-check-reviewer.md` and `lean-repo-auditor.md` are consumed by `/risk-check` and `/lean-repo` respectively; changing what their scans *see* changes what those commands *report*, which is a behavioural change, not a cosmetic one.

Note the mandate named `prime.md` as symlink-distributed and therefore risk-check-class. **That justification no longer applies** — `prime.md` fell out of scope under Correction B (its grep mentions are prose). The gate still fires, on the reviewer-agent and new-executable classes above.

Environment-fit: the canary is a repo script invoked from within a session, not a terminal launcher or shell alias — it does not hit the VS Code-launch mismatch that shipped `cc-worktree.sh` inert (2026-06-10 S3).

# Decision Journal

> Archive: [decisions-archive-2026-07.md](decisions-archive-2026-07.md)

## 2026-07-14 (S1) — Permission-class re-cut: arbitrate the authority split, separate the axes, and gate the chassis version

**Context.** Mission `research-workflow-deploy-fitness`, thread 5 ("clarify the evidence rules"). The thread's stated defect — a hole in the class thresholds ("SUPPORTED needs ≥3 sources/≥2 classes") — **does not exist**: those thresholds appear in no file (canonical chassis, `claim-permission.template.md`, and both live projects' `quality-standards.md` all grepped). Third consecutive thread whose audit-stated premise collapsed on a direct read. The area was broken anyway, differently.

**Decision 1 — the Conditions column is CHASSIS-OWNED; class names are canonical-global.** The claimed self-contradiction (the skill hard-codes the four class names while its Behavior step calls the project's `quality-standards.md` authoritative) was **not one**. The chassis's Canonical-ordering rule already fixes class *names* globally and makes only *thresholds* project-fillable — so hard-coding the names is correct. What was genuinely undefined, and load-bearing, was the **status of the Conditions column**: neither declared canonical nor declared project-fillable. Settled explicitly: names + verb lists + conditions + ceilings are chassis-owned; per-claim-type thresholds are project-fillable calibration *on top of* the generic bar. **Alternative rejected:** making the Output schema parse class names dynamically from the project file. That would make the class vocabulary project-variable, breaking every downstream exact-string match, to serve a flexibility no project has ever used (both deployed projects carry byte-identical class names).

**Decision 2 — separate the axes; do not add a class.** The mandate forbade a fifth class, and none was needed. The table was doing two jobs in one column: grading the **evidence** and bounding the **claim's ambition**. Split them — the class grades evidence on one ordered axis (independent evidentiary roles → fit), and the instance-count / country-coverage conditions become **ceilings** applied afterward, reusing the already-risk-checked risk-tier ceiling mechanism. This also, for the first time, gives the orphan `illustrative`/`directional`/`pattern-candidate` ladder a home: it is a **claim-scope** ladder, orthogonal to the class. **Alternative rejected:** adding a "single-source" class to absorb the gap — it would have papered over the mixed-axis root cause and left the overlap untouched.

**Decision 3 — an evidenced negative is a first-class finding; `NOT-SUPPORTED` never means "false".** Evidence that positively supports a negative conclusion (a register recording a true zero) grades on the ordinary ladder and **can reach `SUPPORTED`**. A failed search is `NOT-SUPPORTED` — a statement about the evidence, not about the world — and licenses **neither the claim nor its negation**. Incomplete research is a *process* penalty and must be recorded as one, never as an evidence verdict. This required **new Stop Condition 5** (max passes exhausted): Conditions 1–4 could never be satisfied by a subtask that found exactly one source, so the reciprocal rule downgraded every such claim to `NOT-SUPPORTED`, making `ILLUSTRATIVE-ONLY` **structurally unreachable**. The class survived only because no skill ever implemented the reciprocal rule — an unenforced rule masking a contradiction, not a safeguard.

**Decision 4 — accept `/risk-check`'s RECONSIDER; ship a chassis-version hard-exit gate.** The reviewer caught a hazard I had missed entirely: the canonical **skills are symlinked** into both live projects, but each project holds a **real local copy of the chassis**. A merge updates the *consumers* and leaves the *rules* stale — and every existing pre-flight check is a *heading-presence* check that an old chassis passes, so nothing could detect it. It would have **silently misadjudicated**. Closed by adding a chassis-version marker + a pre-flight gate in both skills that **hard-exits** on a stale chassis. **Alternative rejected:** a warning instead of a hard exit. Silent misadjudication of evidence claims is the worst failure this gate can have; a loud halt is the correct trade, and a warning would be ignored exactly as the old "no `{{` anywhere" assertion was (the thread-2 pathology).

**Decision 5 — defer the back-port; do not widen scope to fix it.** Neither live project's chassis was back-ported. This is an explicit, operator-approved mandate scope bound ("canonical only") and a declared stop condition. Deferred to the operator, logged as an OPEN action in **both projects' own `logs/decisions.md`** (so it lives where those projects' operators will see it), and mechanically protected by the hard-exit gate. Exposure is low: both projects carry `.claim-permission-gate.done` sentinels for §1.1, so the gate does not re-run today. **Required before either project processes a new section or anyone deletes a sentinel.**

**Decision 6 — correct my own overstatement rather than let it stand.** I had logged that permission enforcement happens "nowhere". The `/risk-check` consumer inventory showed `section-directive-drafter` **is** a real live consumer (it converts classes into per-finding prose constraints). Narrowed the finding to the accurate claim: classes ARE converted to prose constraints at Stage 3, and are enforced by **nothing** at Stage 4.3. A materially smaller (and differently-shaped) hole than "enforced nowhere".

**Verification method (the thing that made all of this land).** Verified **by execution, not by reading** — the mission's standing rule after threads 1 and 2. Four blind adjudication runs: fresh Opus subagents dispatched *as* the skill, against a 5-claim adversarial fixture, never told the expected answers. **OLD rules: `GAPS 1, OVERLAPS 1`** — the agent independently diagnosed the mixed-axis root cause unprompted ("channels and instances are different axes; the class table treats them as if they were one"). **NEW rules (final): `GAPS 0, OVERLAPS 0, UNDETERMINED 0`.** Three correction rounds in between; **each run found real defects in the previous fix, two of them mine.**

**Decided by:** Claude (S1). Premise correction, the deferred back-port, and the strategic finding below were all stated to the operator before commit.

**Strategic consequence, stated and NOT resolved.** The mission's premise ("fix canonical before deploying") has now produced **zero demonstrated blockers across three threads** (1, 2, 5). This is not chance: the source audit reasons from **what the files say, not what the runtime does**, and every time execution has been applied its conclusion has flipped. Threads 3, 4, 6, 7, 8 come from the same audit by the same method. **Recommendation on record: re-examine the deployment gate before spending another session inside the mission.**

## 2026-07-14 (S3) — Outputs-side chassis-provenance gate: stamp verdicts with their rules, and route forgery to the existing override rather than build a hash

**Context.** Thread 5 (prior session, S1) shipped a chassis-version gate that stops a *new* run against *old* rules. It said nothing about permission-table verdicts already on disk. Tested by execution: re-adjudicating a real stale table (research-pe, 1.1-cluster-03) under the current chassis moved 2 of 6 claims to a stricter class, and a blind `section-directive-drafter` run on the untouched stale table confirmed it would license a market-pattern generalization the current rules forbid. An over-claim was being laundered into two live projects' reports with nothing in the pipeline able to see it.

**Decision.** Both producers (`claim-permission-gate`, `cluster-memo-refiner`) now stamp `chassis_version:` into permission-table frontmatter; three consumers (`section-directive-drafter`, `/run-synthesis`, `/run-sufficiency` Step 0) hard-exit on a missing, pre-2026-07-14, or re-stamped (`generated_at` < `chassis_version`) value. `country-parity-checker` was deliberately left ungated — verified it never reads the Assigned-class column, so a stale table cannot corrupt its output.

**No content hash was built.** An adversarial test proved the two self-asserted frontmatter fields can be forged with two hand-edits. A hash would close that gap. Rejected because these are model-executed skills, not scripts — they cannot compute a trustworthy digest without new shell machinery — and the actual threat is a documented operator hand-edit path (`/run-sufficiency` § Operator-override), not an adversary. Operators who genuinely mean to proceed on a stale table are routed to the existing signed `OPERATOR-OVERRIDE` path (logged to this file) instead. The gate's limit is stated plainly in the chassis rather than oversold.

**Alternatives considered:** (a) a content hash over the verdict rows — rejected, above; (b) gating `country-parity-checker` too, for uniformity — rejected, no hazard exists there and it would be ceremony; (c) leaving the first-draft refusal message ("delete the sentinel and re-run") as-is — rejected after `/risk-check` showed it dead-ends an operator in a second hard exit, since `claim-permission-gate` carries its own chassis gate and both live projects have unversioned chassis copies; every refusal message now states back-port-first ordering explicitly.

**Verification.** Tested red/green/adversarial before AND after the fix: unversioned table → EXIT; forged stamp (version pasted, body untouched) → PROCEEDED (the defect); re-stamp invariant added → forged stamp now EXITS, genuine table still PROCEEDS. `/risk-check` verdict PROCEED-WITH-CAUTION; all five required mitigations applied before commit, including a third consumer (`/run-sufficiency` Phases D+F) the reviewer found and my own inventory had missed twice.

**Decided by:** Claude (S3), on the mandate's own stop condition to test the premise before fixing it. Stated to the operator before commit and again before merge.

## 2026-07-14 (S5) — Treat "delete the marker, then re-run the destructive command" as a DEFECT to be logged, not a workaround to be used quietly

**Context.** Closing the `ai-resources-research-workflow` worktree, `check-destructive-liveness.sh` hard-blocked `git worktree remove` on the evidence of a live session marker (`2026-07-14 S3`). The worktree was in fact idle — its S3 session had run `/wrap-session` to completion and committed at 12:11:37 (`ca68eaa`). The marker was a **corpse**: the `SessionEnd` teardown had not fired. The only mechanical way forward was to delete the marker files **the guard reads** and re-issue the blocked command.

**Decision.** Do it — but (a) **state in chat, before acting, that this is clearing false state and not working around the guard**, (b) get the operator's explicit liveness confirmation first (the hook's own contract: *"liveness is the one fact only the operator holds"*), and (c) **log the bypass itself as a HIGH defect** rather than banking it as a technique.

**Rationale.** The corpse converts a *fail-closed safety guard* into a *routine obstacle*, and routine obstacles get routinely bypassed. The recipe "erase the evidence, retry" now demonstrably works, and it was discovered in the course of **legitimately** using the guard — which is exactly how such recipes spread. The next session meeting this block under time pressure will have a precedent for erasing state rather than establishing liveness. An override that is **indistinguishable from tampering cannot be audited**; the hook therefore needs an explicit, logged operator-override input, not one that functions by deleting what it reads.

**This also falsified a standing hypothesis.** The open question since S13 — *why do finished sessions leave marker corpses?* — assumed **crashed** sessions. False: S3 wrapped cleanly and still left both markers. So the guard false-fires on **every** finished session, and `close-worktree-session.md` L127–131 (which asserts the corpse problem was fixed on 2026-07-13, and warns that otherwise "this check would have false-fired on every already-finished session") is **false as written** — that is precisely the state we are in.

**Alternatives considered.** (a) *Delete the markers silently and proceed* — rejected: it is the same keystrokes as tampering, leaves no record, and would have buried the strongest evidence yet for a HIGH open item. (b) *Refuse to proceed at all* — rejected: the worktree was genuinely idle, the operator confirmed it, and the hook's documented "known gap" clause explicitly makes this the operator's call once they say so. (c) *"Fix" it by pruning stale markers as routine wrap hygiene* — rejected, and explicitly recorded as a **do-not** in the improvement-log entry: it treats the symptom and re-arms the bypass.

**Sequencing note (separate, and the reason nothing was lost).** The branch held **one commit reachable from nowhere else** (`ca68eaa`, 122 lines). Merged to `main` **before** any removal, then deleted with `git branch -d` (lowercase), so **git certified** the branch was fully merged rather than me asserting it. `-D` would have destroyed the commit silently against a perfectly clean `git status`.

**Decided by:** Claude (S5), within the operator's instruction to close the worktree. Operator supplied the liveness confirmation the guard requires and directed that the problems be logged.

### 2026-07-14 — Proceed with repo-repair pilot V1, with four investigation cross-check adjustments folded into the plan

**Context.** The operator asked whether to proceed with the approved repo-repair pilot (`~/.claude/plans/investigate-why-our-recurring-humble-curry.md`) after this session's full repo investigation (independently verified by an adversarial subagent) had produced a five-issue critical-fix report.

**Decision.** Proceed — the plan is sound (regression test with known answer, kill switch, self-gating) — but four investigation findings were folded into the plan file first: (1) its §4b "liveness rule" deferral is stale (the hook already shipped as `check-destructive-liveness.sh`, commit `0667cc6`, while the plan was being approved — do not re-implement); (2) the new `require-gate.sh` wiring must QUOTE `$CLAUDE_PROJECT_DIR` (all 9 existing entries are unquoted and survive only via zsh's no-word-split behavior); (3) the open "/risk-check writes its report into the wrong checkout" bug can make the new gate false-block legitimate edits — added as Known dependency #2 with a suggested verification test 8; (4) `warn-settings-change.sh` (unwired orphan) is superseded by the new hook and should be deleted in the same change.

**Rationale.** The plan and the investigation were produced by concurrent/adjacent sessions on the same day — the staleness in (1) is itself an instance of the concurrent-session hazard the plan's own resume block warns about. Folding corrections into the plan file (clearly marked as a post-approval addendum) beats leaving them in chat, where the executing fresh session would never see them.

**Alternatives considered.** (a) Reject the plan and fold its content into the critical-fix sequence — rejected: the plan targets a different failure class (diagnosis without opening files) and is already operator-approved and deliberately minimal. (b) Leave the corrections in chat only — rejected: the plan explicitly executes in a fresh session that would not inherit this conversation. (c) Sequence mandated (pilot vs. critical fixes) — left open as an explicit operator decision recorded in the addendum; either order is safe.

**Decided by:** Claude (S6), on the operator's direct questions ("do you recommend I proceed?", "update the plan"); the sequencing choice and the model-field confirmation remain with the operator.

## 2026-07-14 (S7) — The pilot's enforcement half was cut on its own gates' verdicts; the diagnostic half shipped and was proven by regression test

**Context.** The operator asked to implement the approved repo-repair pilot V1. The plan explicitly gates its own construction — it edits `protected-zones.md` and `audit-discipline.md`, architectural tier under the very rules it installs — and states the test: *"If the plan cannot pass the gate it installs, the gate does not work."* Both plan-time gates were run.

**Decision.** **Land Half 1 (the two reviewer edits + the three `risk-check.md` consumer fixes). Do NOT land Half 2 (the `require-gate.sh` blocking hook, its `settings.json` wiring, the `Files-checked:` footers, the two doctrine-doc edits, the `warn-settings-change.sh` deletion).** `/consult` → **CUT BACK**; `/risk-check` → **RECONSIDER** (High on usage cost, blast radius, reversibility, hidden coupling).

**Rationale.** Three facts, each verified by execution or direct read, kill the hook *as specified*:
1. **`warn-settings-change.sh` — the file the plan calls *"proof the `exit 2` pattern works"* — FAILS OPEN.** Fed a real `PreToolUse` payload it exits **0**. It reads a top-level `file_path`; the payload nests it under `tool_input`. Every functioning hook in the repo reads `.tool_input.file_path`; it is the only one that gets the contract wrong, **and the only one nobody ever wired — which is why the bug survived. An unwired hook is never observed to fail.** Copying it ships a guard that blocks nothing: the repo's most-repeated failure mode ("inert safeguard", 6 logged instances), reproduced by the plan written to end it.
2. **It would not fire where it matters.** Wired in `ai-resources/.claude/settings.json`, it does not load for the ~20 project-rooted sessions that edit `ai-resources` files via `--add-dir`. Workspace-root `.claude/settings.json` has **no `PreToolUse` block at all**. The hook's own stated purpose — *"enforces the gate at the file, regardless of which door you came through"* — is false for most doors.
3. **Self-lock with a self-defeating escape.** Once wired it blocks `Edit`/`Write` to itself and to the `settings.json` it is wired in. The only recovery is the Bash path (`sed -i`, `cat >`), which is simultaneously why the gate enforces nothing (`Bash(*)` + `bypassPermissions`).

**The doctrine edits were held back with the hook, deliberately.** They are the *policy*; the hook is the *enforcement*. Landing `protected-zones.md`'s waiver deletion alone makes `/risk-check` mandatory on every command/agent edit **with no mechanism to enforce it** — precisely the "usage cost: High" the gate flagged, and it yields a rule one must *remember* to obey. The plan's own thesis: *"a rule you must remember to read is not a control; it is a wish."* Cost without protection is worse than neither.

**Verification — the plan's own stop condition, executed.** REGRESSION TEST A: the fixed `qc-reviewer`, dispatched **blind** (never told the expected answer) against `audits/research-workflow-deployment-fitness-2026-07-13.md` — the audit the **old** agent passed with a **GO**, whose findings were mostly wrong. **Old: GO → New: REVISE.** It caught **F-1's invented consequence** by *tracing it to the caller* (`run-sufficiency.md:57` passes the memo directory at dispatch, so the *"unconditional deadlock… this alone forces the verdict"* is false), and it caught **F-9 contradicting F-13(b)** inside the same document. It then found four more, including that the audit's **entire cited evidence base does not exist**. Red before, green after — not a code read.

**Alternatives considered.** (a) *Land the whole plan as approved* — rejected: `audit-discipline.md` verdict semantics forbid downgrading RECONSIDER to push a change through, and the hook's template provably fails open. (b) *Land the doctrine edits without the hook* — rejected, see above. (c) *Land nothing and defer wholesale* — rejected: Half 1 is independently gated-clear, aimed at the verified root cause, reversible, and has a true regression test with a known answer. Deferring it would have deferred the only part that was proven to work.

**Decided by:** Claude (S7); operator confirmed with "go" after being shown the split and the three verified facts.

## 2026-07-14 (S7) — A gate that grades an artifact cannot catch a false premise; the premise check outranks the other dimensions rather than averaging with them

**Context.** Every gate in this system grades an **artifact** (is the change safe, well-scoped, well-formed?). **Not one grades a claim.** So a false premise passes every gate and each one adds authority to it — which is how `/consult` once reasoned impeccably about a bug that did not exist, and how an audit that was 7-of-8 wrong collected a `GO` from `qc-reviewer` (whose citations were all *accurate*; the invented part was the **consequence**).

**Decision.** Add **Dimension 7 — Problem Reality** to `risk-check-reviewer` and a **premise check** to `qc-reviewer` — and make both **outrank** the other dimensions rather than average with them. A `High`/`INCOMPLETE` on Problem Reality forces `RECONSIDER` on its own; a claim whose consequence is untraced or contradicted forces `REVISE` regardless of how clean dimensions 1–6 are. Both grade the **defect** and its **consequence** *separately*.

**Rationale.** Averaging was the defect. Six Lows outvoting one false premise is *exactly* the arithmetic that produced the original `GO`. A false premise is not one risk among seven — it invalidates everything built on it, so it cannot be traded off against the other six. This also required carving out `risk-check-reviewer`'s standing instruction to *"treat the passed inputs as the entire world"*: that sentence governs **scope**, not **truth**, and left uncarved it makes the agent accept the caller's defect claim **by design** — Dimension 7 would have been dead on arrival.

**A live demonstration, same session, worth preserving.** Audit F-13(d) claimed *"three **real template-local** commands (`consult`, `run-sufficiency`, `session-plan`)"*. **The audit** said all three are real — **false**. **The QC reviewer** said two don't exist — **imprecise** (they exist as *symlinks*). **I** then said the reviewer was wrong because all three exist — **also imprecise**, because my test used `[ -f ]`, which **follows symlinks** and cannot see the distinction. Only `[ -L ]` settled it. Three parties, three errors, one insufficiently sharp test — and **the reviewer was right in substance while I was wrong.** The lesson is not "trust the checker"; it is that *nothing here that asserts without running a sharp enough command is reliable — including the checker, and including me checking the checker.* Only command output ever settled a dispute this session.

**Alternatives considered.** (a) *Score Dimension 7 as an ordinary seventh dimension* — rejected: it reproduces the averaging defect it exists to fix. (b) *A citation rule ("cite `path:line`")* — rejected, and this is the sharp point: the failing audit's citations were **all accurate**. A citation rule grades the claim; it does not grade the **inference from** the claim. Both must be graded, separately. (c) *Give `qc-reviewer` a Bash tool so it can execute its verification* — deferred, not rejected: it is a real permissions-surface expansion on an agent invoked everywhere. The current wording requires it to mark a consequence `UNVERIFIED — requires execution` and downgrade the claim rather than assert what it could not check.

**Decided by:** Claude (S7), within the operator-approved plan's design; the outranking rule and the "entire world" carve-out are S7 additions the plan did not specify.

## 2026-07-14 (S8) — Close a guard's hole BEFORE adding its door; and do not ship a fix whose efficacy is unknown

**Context.** Seven repo defects were handed over as a fix list. Two of the resulting decisions turned on the same principle, and both were forced by `/risk-check` returning **RECONSIDER twice**.

**Decision 1 — the env-var bypass is closed BEFORE the operator-chosen override is added, not after.**
`check-destructive-liveness.sh` correctly blocked `git worktree remove` with a live marker — but `FOO=bar git worktree remove` sailed straight through, as did `env FOO=bar …`. Any leading environment assignment defeated the verb regex, and because all four gated verbs share the `_VB + git` prefix, the hole defeated **all of them** (`worktree remove`, `branch -D`, `reset --hard`, `clean -f`). Found by execution against a live marker, not by reading.

**Rationale.** The operator had chosen an env-var override (`AXCION_LIVENESS_OVERRIDE=1 …`). Had that been built first, it would have **appeared to work — by exploiting the bug**. The result: a shipped "feature" that made an open hole look intentional, while `FOO=bar` remained a live bypass of the one guard that exists because a session came a single operator remark from destroying 173+ lines of uncommitted work with no other copy anywhere. **A door built on a hole is not a door; it is a hole with a sign on it.** Close the hole, then add the door. The ordering is now stated as load-bearing in the hook's own comments so a future edit cannot silently reverse it.

**Decision 2 — do NOT quote the `$CLAUDE_PROJECT_DIR` hook paths, even though it looks obviously right.**
Nine repo-level hooks have never fired (verified: the `[HEAVY]` guardrail, auto-QC nudge, coach reminder, friction auto-logger, Friday-checkup reminder — all inert, in every session, silently). The wiring is `bash $CLAUDE_PROJECT_DIR/...`, unquoted, and the project path contains two spaces. Quoting is the obvious fix. **It was cut.**

**Rationale — and this is the sharpest thing the session learned.** The evidence for word-splitting was `sh -c`, which returns 127. But **`sh -c` proves that *`sh`* word-splits — not that the harness does.** Under **zsh**, which is this environment's real shell, the identical unquoted command returns **exit 0**: zsh does not word-split unquoted parameter expansions. And `CLAUDE_PROJECT_DIR` is **unset** in the tool environment, which would collapse the path to `/.claude/hooks/…` and fail in *every* shell — a cause that **quoting cannot fix at all**. So there are three candidate causes demanding three different fixes, and **under two of them the quoting change is a no-op that looks like a fix** — while my own verification (`sh -c` with quotes → exit 0) would have returned green either way. **A guard the operator believes is armed and isn't is strictly worse than a guard known to be off**, because it retires the vigilance that was compensating for it. This repo's most-repeated failure is the inert safeguard; the "obvious fix" would have added another and closed the file on it. A 3-way sentinel (absolute / quoted-var / unquoted-var) was wired instead: one session restart discriminates all three causes, and the real fix follows the evidence.

**Alternatives considered.**
- *Ship the quoting anyway — "it's harmless if it's a no-op and correct if not."* **Rejected.** It is not harmless: it manufactures false confidence, and it closes the investigation. The cost of being wrong is asymmetric and it lands on the guards.
- *Enumerate the destructive `git checkout` forms in the deny list* (the original Fix 5). **Rejected — it was a WIDENING, not a narrowing.** The proposed patterns left `git checkout HEAD -- <file>` and `git checkout <branch> -- <file>` **allowed**; both are destructive and both are blocked today. The destructive set is open-ended, so a deny-list of bad forms is the wrong shape. The correct redesign is an **allow-list inversion**, deferred to `/friday-act` with its own gate.
- *Remove the `mkdir` claim-dir mutex now that the id suffix makes collisions impossible* (the approved plan said to). **Rejected for this session.** The suffix delivers the correctness property; the mutex is now merely redundant, and stripping ~120 lines from a file live in **24 checkouts**, immediately after two RECONSIDER verdicts, buys nothing and adds blast radius. Queued as a clean separate simplification.

**The meta-lesson, recorded because it is the expensive one.** `/risk-check` returned **RECONSIDER twice and was right both times** — on the deny rules, and on the hook-wiring cause. Separately, **five of this session's own test fixtures returned confident, plausible, WRONG results** before one worked (wrong repo variable; argv instead of stdin; a `|` delimiter colliding with event names; `timeout` not existing on macOS; `ls` hiding dotfiles). One of them nearly became the finding *"the staging guard is fail-open."* **A plausible result from a broken instrument is indistinguishable from a real one, and it does not care that you are currently writing a rule against it.** Only an explicit before/after existence check ever settled anything. The same shape appeared in the test harness itself: `prime-allocator.test.sh` reported **"12 passed, 0 failed"** while reading its subject from a *dead session's scratchpad* containing the old, broken code.

**Decided by:** Claude (S8), under two operator-confirmed choices (ship-the-four-and-sentinel; deny rules to `/friday-act`).

### 2026-07-15 (S1-d99) — Rescope the "urgents" fix set on a /risk-check RECONSIDER; defer the installer

**Context:** Auto-mode ran the four urgent backlog items. A pre-gate premise check plus the plan-time `/risk-check` (RECONSIDER) established that 2 of 6 planned fixes were already shipped (the log lagged the code) and that item 4 — a versioned hook-wiring installer writing to the unversioned `~/.claude/settings.json` — carried a High/High Permissions/Reversibility profile, with two prior RECONSIDERs on the same subsystem.

**Decision:** Build items 1 / 2b / 2c; status-flip items 2a + 3 (verified already-shipped by direct read + `git show 8de46fd`); defer item 4 to its own gated session with a mandatory backup-before-merge redesign and a must-not-disturb guard on the operator-DECLINED `"model"` field.

**Rationale:** The reviewer's two catches (item 3 already shipped; item 4 High/High) were both re-verified against the files rather than taken on faith — consistent with the S8 lesson that a reviewer can also assert-from-recall. Bundling a High/High user-settings write alongside three low-risk fixes would have spent the single approval gate on the riskiest item; splitting it out keeps each change proportionate to its risk.

**Alternatives considered:** (a) include item 4 with inline mitigations — rejected, it needs its own `/risk-check` and the backup step the bundle dropped; (b) re-implement items 2a/3 to be safe — rejected, re-editing `close-worktree-session.md` would revert a deliberate S8 fix (OP-11 loud-revision violation).

**Decided by:** Claude (S1-d99), operator-approved rescope (`go`).

### 2026-07-17 (S1-596) — Automate the append-only-log union merge to end cross-worktree collisions; guard the one case union hides

**Context:** Investigation of "worktree sessions collide even when they're different worktrees." Root cause: the five session logs are shared, git-tracked, append-at-EOF files with no merge rule, so two worktree branches that both appended conflict at merge. The marker mutex only dedupes the *number*, not the file merge. The repo's own docs (`commit-discipline.md`, `parallel-sessions-playbook.md`) already classify these as "union — keep both", and sessions have resolved this by hand every time (e.g. `decisions.md` 2026-07-14 S3, Decision 5: "resolved all six merge conflicts as a union, not a pick").

**Decision:** Add a repo-root `.gitattributes` giving `merge=union` to the three provably pure-append-at-EOF logs — `session-notes.md`, `decisions.md`, `coaching-data.md`. Exclude `improvement-log.md` (107 in-place `**Status:**` flips), `friction-log.md` (10 in-place `Resolved:` stamps), and `usage-log.md` (prepend writer + documented fragile format) — union would corrupt those. Ship a companion guard, `logs/scripts/check-duplicate-session-headers.sh`, wired into `/close-worktree-session` Step 4.

**Rationale:** OP-2 (automate execution, gate judgment) — every historical resolution of this exact conflict chose "union", i.e. an always-the-same-outcome decision, which is what OP-2 licenses automating. Proven by execution (git merge-file: default EXIT=1 conflict → `--union` EXIT=0, both blocks kept, headers intact).

**Gate:** `/risk-check` → **PROCEED-WITH-CAUTION** (`audits/risk-checks/2026-07-17-add-gitattributes-merge-union-for-append-only-session-logs.md`; 85 consumers, 0 must-change; Blast-radius High on breadth alone). Its three required mitigations, all applied:
- **(1) Restore the forcing function union removes.** Union silently concatenates two sessions that collided under the *same* marker header — the case a plain conflict used to catch by accident. The new guard greps merged logs for a duplicate `## {date} — Session {marker}` line and STOPs `/close-worktree-session` before teardown. Proven by execution: clean on the real logs, exit 1 on a planted collision, no false-positive on two distinct sessions.
- **(2) Marker-suffix regex — verified already closed.** Union safety rests on marker uniqueness; `check-foreign-staging.sh:197` already uses `\bS\d+(?:-[A-Za-z0-9]{3})?\b` (the load-bearing suffix group, "verified by execution 2026-07-14"). No residual exposure.
- **(3) Reversibility is future-merges-only.** Deleting `.gitattributes` restores default merge for future merges but does NOT retroactively repair a merge already resolved under union — that needs manual inspection (the same guard), not a plain `git revert`.

**Out of scope (unchanged):** the deeper marker-allocator relocation ("participation is version-controlled", `improvement-log.md:889`) — this is the narrow, safe first slice.
## 2026-07-17 (S2-21e) — Gate-hardening bundle: proportionality and mitigation-substitution calls

**Context:** Auto-mode session closing three urgent backlog items (usage-log writer fix; pre-dispatch gate premise check on `/risk-check`+`/consult`; `system-owner` output-contract hardening). Plan-time `/risk-check` returned PROCEED-WITH-CAUTION with four mitigations.

**Decision:** (a) Run ONE combined `/risk-check` for all three items and NO separate `/qc-pass` subagent; (b) substitute inline read-back for the reviewer's "smoke-test a live `/consult` dispatch" mitigation; (c) skip the context-discovery engine in the auto-mode chain.

**Rationale:** (a) These are additive instruction edits to already-wired canonical commands/agents; the independent `/risk-check` + operator `go` + per-file inline verification are the gates this class needs — stacking a `/qc-pass` on top is the "don't stack gates" anti-pattern (Subagent Proportionality). (b) The edits touch neither the ≤30-line summary nor the path-back output-contract text, so that contract is preserved by construction and confirmable by read-back; a ~148k Opus live dispatch to re-verify untouched formatting is disproportionate. (c) Scope was fully enumerated and existence-verified from the backlog entries; the risk-check consumer inventory (24 consumers) covered blast radius, so the discovery engine added nothing.

**Alternatives considered:** run the live `/consult` smoke-test as recommended — rejected on cost/benefit (nothing the dispatch would exercise was changed); split into three separate risk-checks — rejected (one combined review is cheaper and the three share a consumer set).

**Decided by:** Claude (S2-21e), under operator-approved auto mode (`go`).

## 2026-07-17 — De-duplicate the /prime session-marker allocator into a shared Step 8k sub-step

**Context:** `/prime` loads a 1,009-line command file on every invocation before drawing the orientation menu; the ~134-line marker allocator was inlined three times (8a.3.a / 8b.3.a / 8c.3) — a measured model-side load cost and a "keep three copies byte-identical" maintenance tax.

**Decision:** Collapse the three copies into one shared Step 8k sub-step referenced by 8a/8b/8c (Option A). prime.md 1009→739 lines. Header-existence check, header append, and mtime write stay per-branch; control flow and the marker→header→mtime ordering unchanged.

**Rationale:** The allocator is branch-agnostic (references no per-branch variable), so one shared block is strictly safer than three (removes silent-drift risk). Behavior-preservation proven — a zsh falsification harness passes identically on the deduped and original blocks.

**Alternatives considered:** Option B (move allocator to docs/session-marker.md, read on-demand) — rejected: new cross-repo runtime read on the task-selection hot path; failure collapses the TOCTOU guarantee. Option C (rationale comments to decisions.md) — rejected: A de-duplicates them for free. Deferred (SO-flagged): extract to an executable script — larger win, its own `/risk-check`.

**Gates:** `/consult` (SO: GO on A) → `/risk-check` PROCEED-WITH-CAUTION (4 mitigations, all applied) → BLOCKING zsh harness passed. End-time `/risk-check` skipped (plan-time covered; mitigations applied; harness proves behavior-preservation; commits shipped; drift bounded).

**Decided by:** Claude (session db57817b), operator-approved ("proceed").

## 2026-07-18 — Process-grounded session liveness (concurrent-session detection rewrite)

**Context:** Concurrent-session false warnings and commit-blocks persisted despite worktree isolation. Investigation (session dec29660 / S1-dec) found: macOS pgrep excludes the caller's own ancestors, so detect-concurrent-session.sh never counted its own session — with exactly one other session live the hook exited before any marker check (the sharp warning was dead in the most common case); ghost markers from crashed sessions read as live; the detect hook's today-only marker filter and /prime's "not dated today" orphan prune were both category errors (a marker's date records session START, never END — a live overnight session was invisible to one and deleted by the other).

**Decision:** Liveness = per-id marker (ANY date) AND ≥1 foreign Claude CLI process with kernel cwd inside the checkout (ps enumeration, ancestor-walk self-exclusion, single lsof -d cwd call). Foreign markers with zero foreign processes in the checkout are provably dead and auto-pruned by the SessionStart hook (its only write). /prime's date-prune removed, not rewritten — cleanup centralized in the user-level hook, which is registered once by absolute path and therefore immune to stale worktree prime.md copies. Degrades: ps empty/missing or lsof failing → warn/block on marker-only evidence, prune nothing, never silence.

**Rationale:** A marker proves a session started; a process proves a window is open; only the conjunction is evidence of a live session (VS Code reuses CLI processes across session ids and keeps idle ones alive for days — process-only over-fires; markers alone can't die — marker-only false-fires). Centralizing cleanup where version skew cannot reach it follows the S{N}-suffix lesson: correctness in a single enforced point, not a convention every checkout must honor.

**Alternatives considered:** Per-session lease files (id→pid+start-time map, machine-global) — deferred, not rejected: closes the remaining ghost-plus-idle-window ambiguity and the unprimed-session blind spot, but is a new state class; queued for /consult + /risk-check. Checkout write-lock — recommended against (session-lifetime lock with stale-lock hostage risk; SessionStart cannot block, so enforcement would land in PreToolUse guards that already exist). Single-signal designs (marker-only status quo; process-only) — rejected, each false-fires alone.

**Gates:** Operator-directed no-subagent implementation; external QC (Codex), two rounds, all findings fixed (degraded-lsof any-date warn; ancestor-walk test coverage T8/T9/G7/G8; stale date-prune contracts swept). Verified: 19/19 falsification harness, real-environment smoke test, zsh execution of the Step-8k block extracted from prime.md. Commits 979ed01 (ai-resources) / 6d33830 (workspace root) on explicit operator approval.

**Decided by:** Claude (session dec29660 / S1-dec), operator-approved.

## 2026-07-18 (S3-919) — Research-workflow deploy-gate re-decided: deploy the Sector Intelligence pilot now; threads 3/4/6/7/8 become post-deployment improvements

**Decision:** The "fix canonical before deploying" gate is retired. `/deploy-workflow` for the Sector Intelligence pilot may run in a dedicated session against the current canonical template, without waiting for mission threads 3, 4, 6, 7, 8. Those threads reclassify to post-deployment improvements, executed on their own merits under the mission's existing non-negotiables (smallest general fix, no pilot content in canonical files, acceptance tests by execution). The mission's "At deployment" safeguards list still applies in full to the deployed project at deploy time.

**Rationale:** The gate's premise is empirically dead. Every thread ever classified a deployment blocker (1, 2, 5) was falsified by execution — 3 for 3 — and the mission file itself records ZERO demonstrated blockers remaining (S13 note, thread 2). Threads 3–8 were never claimed as blockers; the mission states "pilot 1 would run without them." Keeping the gate would rest deployment on exactly the audit-style file-reasoning the mission's own record shows to be unreliable, while delaying the pilot evidence that the evidence-gated threads (C-2-full, C-3-full, F-8-full, F-14) explicitly wait for. Deploying is also the mission Goal's stated purpose. Thread 3's three fixes are portability-class defects masked on this machine (global gitignore covers settings.local.json; python3 present; SETUP-obligation loss causes a bounded late interruption, not corruption) — real improvements, wrong gate.

**Alternatives considered:** (b) Keep the gate and name the real blocking thread with executed evidence — rejected: no candidate thread has executed evidence of blocking, and producing such evidence would itself require deployment-shaped execution; the option collapses into "deploy and observe." Deferring the decision to the operator — rejected per decision-point posture; the 2026-07-17 friday-act plan explicitly mandated making the decision in this follow-up session.

**Conditions attached:** (1) The deploy session writes the project-local safeguards into the deployed project per the mission's "At deployment" checklist — unchanged. (2) Thread 5's open mitigation (chassis back-port to the two live projects) remains an independent operator TODO protected by the chassis-version hard-exit gate; deployment neither waits for it nor discharges it. (3) F-7 (internal-import ingestion) still binds before research unit 2 — unchanged. (4) Thread 3 should land before any deploy on a machine other than this one (its defects are portability-class).

**Decided by:** Claude (session S3-919), executing the operator-approved 2026-07-17 friday-act deploy-gate-decision plan (SO-triaged, mission-bound). Operator veto open at wrap.

## 2026-07-18 (S4-8c3) — The verified backlog supersedes the logged backlog; the queued permissions plan must be re-scoped before it runs

**Context:** The operator asked for the top open maintenance and repo-health items in ai-resources, and required a separate subagent pass to confirm each was still open and genuinely a problem. 22 candidates were drawn from `improvement-log.md`, `friction-log.md`, `retirement-backlog.md` and the four 2026-07-17 audit reports, then re-verified against live files by five Opus-pinned agents.

**Decision:** Where a log entry or audit finding disagrees with this session's execution-verified result, **the verified result governs**, and the disagreement is recorded rather than silently reconciled. Concretely: (a) 12 of 22 candidates are closed as not-actionable and listed in the mission's DELIBERATELY-NOT-INCLUDED block; (b) `audits/friday-plans/2026-07-17-permissions.md` is **re-scoped, not executed as written** — its ai-resources CRITICAL is a verified false positive and its git-unblocking item targets the wrong rule; (c) the logged figures "seven more gates", "7 unarchived entries" and "19 stale entries" are replaced by 1-confirmed-plus-9-unverified, 29, and 30 respectively.

**Rationale:** The drop rate is the finding, not an aside. Six candidates were already fixed with the log entry never flipped — one fixed the day *after* its entry was written — so the backlog systematically overstates open work. Two were never real. Two were mis-attributed, and one of those matters operationally: `git checkout` is denied by name at `~/.claude/settings.json:47` and workspace-root `.claude/settings.json:27`, while the logged diagnosis blames archive `Read()` deny patterns — falsified by the log's own data, since `git add <archive>` and `git show ":N:<archive>"` both name the denied path and both ran fine. The queued fix targets those Read patterns, so executing the plan as written would consume a `/risk-check` and leave the block in place. Three of the four 2026-07-17 permission findings being stale or mis-derived traces to a single rule-design defect — `/permission-sweep` Rules 5/6 evaluate one file's allow array without evaluating the merged layer stack or `defaultMode` — which is now a mission thread in its own right.

**Alternatives considered:** (a) Report the logged backlog as-is and let the fix sessions discover staleness — rejected: that is the failure mode being measured, and it spends a gate per rediscovery. (b) Flip the stale log entries in place during this session — rejected: out of the approved scope (diagnosis only, read-only apart from the mission file), and `improvement-log.md` is a shared non-append log with concurrent-session exposure at the time the work ran. (c) Pad the list to the requested 10 with the weaker candidates — rejected: the operator explicitly asked for real problems, and the verification agents were instructed that returning fewer is preferred; Agent E returned no independent finding and rejected its own strongest candidate on consequence grounds.

**Conditions attached:** (1) Closing a mission thread requires an execution-verified fix or a recorded decline reason — no thread closes by assertion, per the mission's non-negotiables. (2) The stale log entries behind the 12 dropped candidates are not flipped by this session; they remain misleading until thread 1's drain runs, and the mission's not-included block is the interim authority. (3) The re-scoping in (b) binds the permissions plan only — the other three 2026-07-17 friday-act plans are untouched.

**Decided by:** Claude (session S4-8c3), executing the operator-approved `/scope` for the repo-health review. Operator veto open.

---

## 2026-07-18 — Two backlog items were mis-diagnosed; the corrected diagnosis governs, and one thread stays open rather than being ticked

**Context:** Session S5-531 executed three operator-ranked repo-health fixes. Two of them — mission threads 1 and 2 of `repo-health-backlog-2026-07` — turned out to describe their own problems incorrectly, and the mis-description was load-bearing in both cases rather than cosmetic.

**Decision:** (a) Thread 1 ("drain the improvement-log backlog") was **not** executable as written; the blocker was fixed first and the thread ticked only after the drain actually ran. `/resolve-improvement-log`'s classifier recognized 6 of 22 finished entries, because tier 1 requires a separate `**Verified:**` line and tier 2 requires the literal word `resolved`, while this log's dominant completion convention is `**Status:** **applied 2026-07-17 (S2-21e).**` — matching neither. A tier 3 anchored on `^applied` + date was added, then the drain ran: 16 entries archived, `/prime` Step 3 scan 234 → 147 lines. (b) Thread 2 ("a third of the backlog is structurally invisible to `/prime`") is **left unticked**. Its remedy as written — widen the severity anchor — reaches 2 entries, both `MED`, so it changes nothing today. The actual invisibility is 30 entries carrying no `Severity` field at all, which no severity anchor can reach by construction. Step 3 now reports that count in one bounded line; the entries themselves are deliberately not surfaced as menu items.

**Rationale:** The backlog attributed thread 1 to neglect ("29 resolved entries still unarchived"). The real cause was that the drain could not see them — so the log grew while the tool truthfully reported nothing to do, and every prior session that "ran the drain" was satisfied by a no-op. That is why the item kept recurring. Fixing the classifier is the structural fix (workspace `CLAUDE.md` § Working Principles: structural over patch); hand-archiving 22 entries would have closed this instance and left the next drain equally blind. For thread 2, an entry with no `Severity` field is **unclassified**, not hidden HIGH work: promoting all 30 into the task menu would enlarge the exact scan Step 3 exists to bound — the ~50–60k/session defect fixed 2026-07-13 — while telling the operator nothing about priority. Reporting the count makes the gap visible at one line of cost. Ticking the thread was rejected because its stated consequence ("a third of the backlog can never reach the task menu") is still true; ticking it would be precisely the false-completion reporting that the `/mission check` verb built in this same session exists to prevent, and it would violate the mission's own non-negotiable that no thread closes by assertion.

**Alternatives considered:** (a) Run the drain as written and tick thread 1 — rejected: archives 6 entries, leaves the scan bloated, and marks the item done. (b) Hand-archive all 22 finished entries this session — rejected: closes the instance, not the class. (c) Widen the anchor until the 30 Severity-less entries appear — rejected: converts a bounded scan into an unbounded one and floods the menu with unclassified items. (d) Tick thread 2 with a re-scope note in the thread text — rejected: `/mission check` deliberately changes only the checkbox, and a thread whose named cost still holds is not complete.

**Conditions attached:** (1) Thread 2 becomes tickable only once the 30 entries carry a `Severity` field — a log-hygiene task, `/friday-act`-shaped, not a scan change. (2) Both improvement-log entries touched this session are left `**partially applied**` rather than `applied`, so tier 3 cannot archive half-done work and bury its deferred remainder in the deny-read archive; this was caught in external review after the first draft marked one of them plain `applied`. (3) The marker fix closes only the silent consequence — the wrap-halt and staging-guard consequences of the allocation gap remain live and are recorded as open in that entry.

**Decided by:** Claude (session S5-531), with the operator selecting `fix` over `archive only the 6` / `archive nothing` at the drain gate. Mandatory `/risk-check` and `/blindspot-scan` gates were waived by the operator and replaced by three external Codex review passes.

## 2026-07-18 (S6-ac5) — Fix thread 2 at the schema rather than at the 30 entries; decline thread 4 rather than reach for the user-level settings file

**Context:** First implementation session of mission `repo-health-backlog-2026-07`. The operator amended the mandate mid-setup to require that each problem be **verified still real before being fixed**. Committed scope was threads 2 and 4; thread 8 was stretch and not reached.

**Decision:** (a) Thread 2 was closed by **declaring `Severity` in `improvement-log.md`'s schema** and then backfilling the 30 entries — not by backfilling alone, which is what the prior session's entry had proposed. (b) Thread 4 was **verified real and deliberately left open**, with its diagnosis corrected in three places, rather than fixed. (c) The queued `audits/friday-plans/2026-07-17-permissions.md` was marked **SUPERSEDED-IN-PART by banner only**, with no item deleted.

**Rationale:** (a) The backlog framed thread 2 as a classification chore and, before it, as format drift between two spellings of a field. Both are symptoms. `Severity` is `/prime` Step 3's primary scan anchor and was in use by 58 of 88 entries while being **absent from this log's own schema**, which declared seven other fields. So the 30 omissions were not 30 lapses of memory — they were the predictable output of a required field that no contract required, and hand-classifying the 30 would have left the next author equally free to omit it. Declaring the field *with its machine consumer named inline* is the structural fix (workspace `CLAUDE.md` § Working Principles); the backfill is the cleanup that follows it. Verified by execution on two independent parsers: `no_severity` 30 → 0, diff purely additive at 31 insertions / 0 deletions. (b) Thread 4's block is real — `git checkout --help`, which touches nothing, is denied — but fixing it requires editing `~/.claude/settings.json`, since deny wins at every layer; that file is unversioned, outside every git repo, and is the mandate's declared stop condition. Independently, the obvious fix is already on record as **rejected**: `decisions.md` 2026-07-13 and `improvement-log.md` both establish that enumerating the destructive `git checkout` forms is a *widening*, with the standing instruction not to retry it. Closing thread 4 on a fix the repo has already refused would be worse than leaving it open. (c) Only item 1's ai-resources third was verified false; items 2–4 were never assessed, so retiring the plan wholesale would have destroyed legitimate queued work to fix a scoping error.

**Alternatives considered:** (i) *Backfill the 30 and tick thread 2, per the prior session's proposal* — rejected: closes the instance, leaves the class, and the next entry omits the field again. (ii) *Invent a `Severity: parked` value for the ~10 deliberately-deferred entries* — **rejected after checking the existing convention**, which was the near-miss of the session: 15 of 26 parked entries already carry a normal severity, and parking is expressed through `Review-cycle:`, so the invention would have contradicted the schema being written in the same edit. (iii) *Fix thread 4 by editing the workspace-root settings file only* — rejected as non-functional: deny wins at every layer, so the user-level rule would still block. (iv) *Retire the friday-plan entirely* — rejected, see rationale (c).

**Conditions attached:** (1) The 30 backfilled severities are marked `*(backfilled S6-ac5)*` and are **first-pass triage from each entry's stated status and consequence, not author-assigned** — they should be refined at a Friday review rather than treated as authoritative. (2) The 2026-07-14 format entry is left `**partially applied**`, not `applied`, because the writer-side half (`wrap-session.md` emitting the field) is undone and tier 3 must not archive it and bury that remainder. (3) Thread 2's tick was made **only after** the backfill was verified — the ordering the S5-531 entry demanded, and the reason this is not the false close that entry was written to prevent. (4) The `/prime` Step 3 scan grew 151 → 167 lines as a direct result; mission acceptance assertion `:54` ("scan under 40 lines") remains unmet and this moved slightly away from it. Recorded rather than hidden — two genuinely consequential items stopped being invisible, which is the fix working, but the cost is real.

**Decided by:** Claude (session S6-ac5), under the operator's verify-before-fixing amendment, with the operator authorizing the friday-plan scope growth and deferring thread 4 to its own session.

## 2026-07-18 (S7-bb5) — Carry `/prime` 8a's post-plan gate as a token through the chain, not as a sentence to be recalled; patch the sector-intelligence fork rather than leaving it stale

**Decision:** (a) The `/prime` 8a.d ↔ `/session-plan` Step 8 conflict was resolved by introducing a caller-declared `{gate:post-plan}` token that travels `/prime` → `/session-start` → `/session-plan`, rather than by the log entry's literal "make Step 8 caller-aware" (which is not implementable as written — see rationale) or by the weaker caller-side override. (b) A **third** copy of the conflicting absolute, at `session-start.md:380`, was rewritten as part of the same fix; the source improvement-log entry names only two. (c) The real, non-symlinked copy of `session-plan.md` in `projects/axcion-sector-intelligence/` was patched to match canonical, accepting a declared scope growth beyond the mandate's `ai-resources/.claude/commands/` boundary.

**Rationale:** (a) The entry's fix direction presumes `/session-plan` can identify its caller. Traced by reading the files, it cannot: `/prime` 8a.b invokes `/session-start`, whose Step 4 chain-invokes `/session-plan` passing **only** `work_scope`. No branch identity crosses either hop, so "be caller-aware" required *creating* a signal. The token reuses the existing `{mission:<id>}` prefix pattern at `session-start.md:85` — a second use of a working mechanism rather than a new one. The deeper reason to prefer a token over any sentence: `/session-plan` is chain-invoked, so its Step 8 is the **most recently loaded** instruction at the decision point while the caller's pause was loaded many turns earlier. The recency-favoured reading wins, and before this fix the recency-favoured reading was "begin execution". A token makes the caller's gate a fact in Step 8's own inputs instead of something the reader must recall — the same reasoning the repo applied when it replaced memory-dependent gates with mechanical ones. (b) Leaving `:380` would have re-created the conflict from a third direction after Step 8 was fixed; it makes the same absolute claim about the same moment. (c) The fork is byte-identical to canonical and carries the defect at its own `:222`. Editing canonical alone would have left that project silently running the skipped-gate behaviour — the "canonical fixed, copy stale" failure class already logged twice in this repo (research-workflow chassis, 2026-07-14).

**Alternatives considered:** (i) *Weaker fix — `/prime` 8a.c states the override at the point of call.* Rejected: it leaves the conflicting sentence intact for every future reader, does not touch `session-start.md:380`, and puts the correction in the file that does **not** carry the problem. (ii) *Remove or weaken `/prime` 8a's pause.* Rejected — explicitly forbidden by the source entry, and correctly: the 8a/8b split is deliberate, since a numbered menu pick is not the operator stating the work and so earns a gate that free-text intent does not. (iii) *Convert the sector-intelligence copy to a symlink.* Rejected for this session — it is the durable fix for the drift and is the better end state, but it is a structural change to another project's command wiring and deserves its own `/risk-check` rather than riding along as a side effect. Logged as follow-up. (iv) *Leave the fork stale and log the divergence.* Rejected by the operator when offered.

**Conditions attached:** (1) `/risk-check` returned **PROCEED-WITH-CAUTION**; all four required mitigations were applied, not merely noted — the first of them was a genuine defect in the design, not a caution: the token had to be stripped at `/session-plan` **Step 0**, where `UPCOMING_INTENT` caches `$ARGUMENTS` verbatim, and the design's Step-1-only strip would have leaked the token into the plan file's `## Intent` line on the patched checkout, not just a stale one. (2) The fix was verified by **blind dispatch** — fresh agents given only a scenario and the shipped files, never the expected answer — for both the 8a (gate fires) and 8b (no gate) paths, per the repo's verify-by-execution rule. (3) **Not incident-driven, and must not be cited as such:** the one live encounter (S1-1e0, axcion-content-programme) was noticed and resolved in-session with zero harm. The urgency is architectural — the concern is that noticing it is not the default path — not a materialized failure. (4) The sector-intelligence patch keeps the two files byte-identical; any future canonical edit must repeat it until the symlink conversion lands.

**Decided by:** Claude (session S7-bb5), with the operator authorizing both the three-file token design and the out-of-scope sector-intelligence patch at the plan's declared stop point.

## 2026-07-18 (S8-a1b) — The `git checkout` deny rule was deleted, not narrowed; and both the shape the log forbade and the shape it prescribed were unavailable

**Context:** Mission `repo-health-backlog-2026-07` thread 4. `"Bash(git checkout *)"` in `~/.claude/settings.json:47` and workspace-root `.claude/settings.json:27` denied by verb rather than effect — `git checkout --help` was blocked, as were `<branch>`, `-b`, `--ours`/`--theirs` — with 5 logged work stalls and no waiver possible under `bypassPermissions`. The session's plan was to *narrow* the rule so destructive forms stayed blocked and safe forms passed.

**Decision:** (a) The rule was **deleted outright** from both settings files and from the canonical Layer B shape in `docs/permission-template.md`, and replaced with an explicitly **unenforced** model-side rule in `docs/commit-discipline.md` § Destructive git-checkout forms plus one short line in always-loaded workspace `CLAUDE.md`. (b) The planned 9-pattern enumerated deny set was abandoned after `/risk-check`, despite that gate returning PROCEED-WITH-CAUTION and therefore permitting it. (c) The improvement-log entry was flipped to **partially applied**, not applied — its archive-`Read()` half is untouched.

**Rationale:** (a) Three independent sources converged against a larger deny list, and the session surfaced the conflict rather than resolving it silently: this repo's own 2026-07-14 postmortem (`improvement-log.md:953` — *"the destructive set is open-ended… do not attempt the enumerate-the-bad-forms approach again"*); the operator's standing architecture (`bypassPermissions` as the floor, model-side rules rather than deny entries, recorded as "never add to deny list"); and Anthropic's documented warning that argument-constraining Bash patterns are fragile and evadable by variable expansion. (b) **The 2026-07-14 warning was not merely policy — it was correct, and re-proven by execution.** `git checkout foo` is identical in shape whether `foo` is a branch (safe) or a file (destructive); only git can resolve which. So the most common accident form, `git checkout <file>`, is structurally uncatchable, and a pattern broad enough to catch it also wrongly denies `git checkout "branch with space"` — both verified by running them, not by reasoning. Any enumerated deny set is therefore incomplete *by construction*, not by insufficient care. (c) The residual risk is smaller than deletion implies: `git restore <path>` achieves identical discard and has **never** been denied in any layer (grep-confirmed across all 68 settings files in the workspace), so the blanket rule supplied the appearance of protection while the same capability sat one verb away.

**Alternatives considered:** (i) *Ship the narrowed 9-pattern set with the risk-check's 4 mitigations.* Rejected — it repeats an architecture this repo recorded as wrong, grows the deny list against the operator's stated preference, and still leaves the most common accident form open. The gate scored this Principle Alignment **Medium** for silent repetition of a warned-against approach. (ii) *Follow the log entry's own prescribed remedy — an allow-list inversion.* Rejected as **not implementable**: Claude Code evaluates deny before allow and documents that a deny rule cannot carry allowlist exceptions, and under `defaultMode: bypassPermissions` an allow-list has nothing to bite on. Recorded explicitly so a future session does not spend a cycle rediscovering this. (iii) *Delete the rule and add nothing.* Rejected — it leaves no trace of which forms are dangerous, and the operator selected the variant carrying the model-side rule. (iv) *Move enforcement into `check-destructive-liveness.sh` now.* Deferred, not rejected: it is the only instrument that can express the distinction, but it is a hook-behaviour change that inherits mission thread 3's unversioned-wiring defect. Queued.

**Conditions attached:** (1) The model-side rule is documented as a **wish, not a control**, in both files that carry it — the section it joins records this repo's own lesson that a rule you must remember to read is not a control, and labelling it otherwise would reproduce that failure. (2) The `~/.claude/settings.json` edit was preceded by a timestamped backup (that file is outside git; `git revert` cannot reach it) and was a targeted JSON-array edit to leave the operator-DECLINED `"model": "opus[1m]"` field untouched — verified intact afterwards. (3) The end-time `/risk-check` was **skipped and documented**, per the standing skip rule: the shipped design is simpler and more reversible than the design that was gated, the change of design was an explicit operator decision, and the consumer question was closed inline by the 68-file sweep. (4) Real enforcement remains **unbuilt**; do not cite `commit-discipline.md` as protection.

**Decided by:** Operator, selecting from three options presented when the session stopped at a conflict between a recorded preference and its own plan. Claude surfaced the conflict and recommended the selected option.

### 2026-07-18 (S9-f53) — `run-manifest.sh` cross-midnight fix: unbounded per-id marker trust; deferred registry note logged, not dropped

**Context:** Mission `repo-health-backlog-2026-07` thread 8 — `run-manifest.sh` could not close a session record across midnight: a no-flag `close` after 00:00 died (exit 2), stranding the start-stub at `outcome: null` forever; `--marker` pinned without `--date` wrote a second manifest while the real one stayed null. The originating improvement-log entry (2026-07-13) proposed the fix but also proposed bounding trust in the marker to "~1 day" so a genuinely abandoned marker is still caught. `/risk-check` (PROCEED-WITH-CAUTION) separately asked for a registry note in `docs/session-marker.md` documenting the new staleness divergence.

**Decision:** (a) Trust in the per-session-id marker (`.session-marker-$CLAUDE_CODE_SESSION_ID`) is **unbounded** — no ~1-day (or any) staleness window was added, against the origin entry's own suggestion. (b) The shared marker file's today-only rule is **untouched**. (c) The `docs/session-marker.md` registry-note mitigation was **deferred with a named reason**, not applied and not silently dropped — documented instead in `run-manifest.sh`'s own header, and logged as an open Next Step.

**Rationale:** (a) Attribution safety in this design comes from the marker's **filename** — no other session can write a file named after this session's id — not from its date. A staleness window adds no attribution strength, and any boundary chosen (1 day, 3 days, N days) simply reintroduces the identical defect for a session whose lifetime crosses it. A 3-day-idle session is unusual, not wrong, and the fix exists precisely to stop punishing that case. (b) The shared-file path is genuinely different: it carries no per-session proof at all, which is why the S4-8c3 attributable-overwrite guard (added earlier the same day) must keep refusing it when stale — five of the six new test cases exist specifically to prove this session's change does not weaken that guard. (c) The registry note was outside this session's declared `Files in scope` (mandate-bound), and the mandate's `Stop if` clause named exactly this class of boundary — widening into the marker contract itself — as a reason to halt and route elsewhere rather than reach across it inline.

**Alternatives considered:** (i) *Ship the ~1-day window as originally proposed.* Rejected — it is the appearance of rigor purchased with a fresh instance of the bug being fixed; verified against no sub-case where it would help and at least one (a multi-day-idle session) where it actively regresses the fix. (ii) *Widen `Files in scope` mid-session to include `docs/session-marker.md` and apply the mitigation directly.* Rejected — the mandate's stop condition existed for a reason (marker-contract changes are a different mission thread with its own gate), and reaching past a declared scope boundary to close one mitigation cheaply is the kind of small scope creep the mandate structure exists to prevent. (iii) *Drop the registry-note mitigation silently.* Rejected — a `/risk-check` mitigation that simply vanishes with no trace is exactly the failure mode `/risk-check`'s own Recommended-redesign contract exists to prevent; it is queued instead.

**Conditions attached:** (1) The unbounded-trust rationale is recorded in the code itself (`run-manifest.sh`'s CROSS-MIDNIGHT block), not only here, so the next reader meets it where the decision lives. (2) The deferred `docs/session-marker.md` note is tracked in two places — this session's continuity scratchpad and the session-notes `### Next Steps` — so it cannot quietly disappear the way the origin entry's own DR-7 shared-helper proposal had, which is why that entry was flipped to *partially* applied rather than applied.

**Decided by:** Claude, within the mandate's declared autonomy (Gated posture; this was not one of the plan's named stop points). The `/risk-check` reviewer confirmed the unbounded-trust design is sufficient against every named attack sub-case (stale/orphaned marker, multi-day idle, unset session ID, liveness-pruned marker) before it shipped.

## 2026-07-18 (S10-163) — Order the repopulated mission by dependency, not severity; narrow the reviewer fix against the System Owner; route the highest-ROI item OUT of the mission

**Context:** A five-agent verification pass re-checked ~30 backlog claims against live files. About a third failed — 5 already-fixed, ~6 false or mis-attributed. The operator asked for a ranked ten and then for the existing `repo-health-backlog-2026-07` mission to be repopulated. Three independent judgments fed the result: my own consequence-ranked report, a `/consult` System Owner advisory, and an operator-supplied external Codex review. They did not agree.

**Decision:** (a) The mission's threads are ordered by **dependency**, not by consequence-if-unfixed — thread 11 (the shell `grep` is a gitignore-aware function) runs first. (b) The reviewer premise-check fix is **narrowed from five agents to two** (`triage-reviewer`, `scope-qc-evaluator`), against the System Owner's unnarrowed recommendation and following the external review. (c) The staging-guard defect — rated critical / highest-ROI by the external review and confirmed by sandbox reproduction — is **routed OUT of the mission** to ordinary backlog. (d) The stacked-gate item is **reclassified, not closed**. (e) `audits/working/` bloat is **kept out**, against the System Owner. (f) Thread 13 is **left unticked** although its work is done and committed.

**Rationale:** (a) "Consequence-if-unfixed" is a severity ordering, and a mission needs a dependency ordering: every other thread's verification runs through the search instrument, so fixing anything before it means re-verifying afterwards. Measured: 10 hits vs 612 with `command grep` at workspace root. (b) The gap was verified but I had treated it as uniform; it is not. `refinement-reviewer` and `expert-check-reviewer` have deliberately narrower jobs where a blanket filesystem obligation duplicates QC and buys no verification — copying the rubric into all five is precisely the over-application `CLAUDE.md § Subagent Proportionality` forbids. The two retained are the two that *decide* things: `triage-reviewer:51` carries the verbatim inverse of the antibody ("note it as an assumption rather than launching a search") and gates what leaves the queue; `scope-qc-evaluator` issues build/park verdicts with zero verification language. (c) The mission's frozen Goal has three clauses — bounded orientation scan, fresh-clone survival, and audit tooling that stops emitting false findings. The staging guard serves none: it blocks work loudly, it does not corrupt verification. Admitting it because it is urgent would make the mission a container for "important things" rather than a contract, and the Goal is frozen precisely to prevent that. (d) A rule written three times and still unfollowed is evidence the **written rule** is wrong (`AP-11`), not that the operator lacks discipline; the non-negotiable forbids building a *gate*, not diagnosing the *rule*. My original closure used the non-negotiable as cover. (e) Every number in the source claim was false (397 files / 4.4 MB / 88 over 60d, against a claimed 328 / 4.1 MB / 53), and the 31.6% that cannot be swept is excluded **by design** at three layers of `/log-sweep` — so "structurally unsweepable" is a description of intent, not a defect. (f) Hand-ticking a thread against a contract nobody read is the exact mechanism thread 12 exists to fix; ticking it would have demonstrated the finding was not understood.

**Alternatives considered:** (i) *Create a new mission, as the operator first asked.* Rejected on the System Owner's advice — seven of the report's ten were already threads in the existing mission, whose frozen Goal already states the instrument-integrity thesis. Two overlapping repo-health missions would have split the record. (ii) *Adopt the external review's ROI ordering* (scan + stale entries first, `grep` fourth). Rejected: it ranks by return, which is the right criterion for a work queue and the wrong one for a mission whose threads verify each other. (iii) *Demote `/prime`'s scan and the hook-wiring thread to ordinary backlog*, as the System Owner recommended. **Rejected — the advisory overreached here:** the frozen Goal names both explicitly (clause (a) bounded scan, clause (b) fresh-clone concurrency), so demoting them would leave the Goal unservable by its own thread list. (iv) *Copy the premise-check rubric into all five reviewer agents.* Rejected per (b). (v) *Fix the staging guard inside the mission because it is the highest-ROI item.* Rejected per (c).

**Conditions attached:** (1) The repopulation was a **hand-written edit with no sanctioned path** — `/mission` exposes `create | list | read | check | close` and no `update` verb. This is the second occurrence (first: `research-workflow-deploy-fitness` S10). Recorded in-file and as thread 12's sibling defect so the precedent is not read as permission. (2) The frozen Goal / In-Out scope / Validation contract were **not touched**; only `## Open threads` changed, which is the one mutation the contract permits. (3) `mission_name` still reads "10 verified items" against 16 threads — left stale deliberately, surfaced to the operator, because frontmatter other than `status` is frozen. (4) Thread 15 carries a measured constraint that forecloses the obvious remedy: flipping the five stale entries reduced the scan by **nothing** (raw grep, model-side filter), and a simulated full archive drain still leaves **88 lines against a 40-line budget** — so the fix must change what the scan emits, not what it reads.

**Decided by:** Claude, under the session's stated autonomy, with two of six judgments taken **against** the System Owner advisory ((e) and the (iii) demotion) and one **against** my own prior report ((b), on the external review's evidence). The operator directed the repopulation itself and deprioritised the unrelated backup finding.

## 2026-07-18 — Thread 11: overriding a mission thread's written fix instruction on measured evidence

**Context:** Mission `repo-health-backlog-2026-07` thread 11 was the dependency-first thread, filed as *"the repo's search instrument is blind, and every absence-claim has been made through it — RUN THIS FIRST,"* with a prescribed fix: *"`command grep` / `git grep` / `rg -uu` with explicit scope in scanning commands and agents, plus a known-positive canary."* Every other thread was ordered behind it on the grounds that their verification runs through the search instrument.

**Decision:** (a) The premise was confirmed but the thread was **narrowed twice** — 14 candidate sites → 4 → **0**. (b) **No scanning command or agent was edited**, against the thread's explicit written instruction. (c) The canary was built **sourced-only**, refusing to answer when executed. (d) It was **not** wired into `/prime`. (e) Thread 11 was **left unticked** although its work is complete. (f) The end-time `/risk-check` was run despite an assessment that no listed change class was touched.

**Rationale:** (a) Four properties, each measured by execution: the shadowed `grep` filters *traversal only*, not argv (a gitignored file named explicitly is still found); it does not cross a process boundary, so every grep inside a `.sh` file — all of `.claude/hooks/` and `logs/scripts/` — was never exposed; and **only the dot-rooted walk is blind** (`.`→0, `./`→0, `audits`→1, `/abs/repo`→1, ground truth 1). (b) Given (a), the decisive question became *which committed sites use the dot-rooted form* — and the answer, re-derived with the non-blind instrument, is **none**. Editing immune sites would be churn with no named consequence, failing the materiality bar; `/risk-check` independently re-verified the zero and rated defensive future-proofing edits speculative (AP-7/DR-7). The real exposure is the **ad-hoc `grep -r <term> .` a session types while verifying something** — including the greps that produced this mission's own evidence — which is a property of how we verify, not of what we committed, and which no file edit can reach. (c) The shadow is a shell *function*; an executed script is a child process that always sees the real `grep`, so an executable canary would report "clear" in every session including blind ones. (d) Thread 15 is specifically about `/prime`'s scan already exceeding its stated budget (222 lines vs 40); adding a per-session check there would worsen an open thread. The `/risk-check` reviewer supplied the third option taken instead — pointers at the three agents that make load-bearing absence-claims. (e) Hand-ticking is the exact unverified-tick mechanism thread 12 exists to fix; ticking here would demonstrate the finding was not understood. (f) The class boundary was close enough (a new executable that writes a probe file) that the no-self-waiver rule in `audit-discipline.md` counselled running the gate rather than self-assessing past it.

**Alternatives considered:** (i) *Execute the thread as written* — edit the ~14, then ~4 sites to `command grep`. Rejected once measurement showed all are immune; this would have been a visible, plausible, entirely useless change, and it is what the thread would have produced if followed without verification. (ii) *Edit the sites anyway as future-proofing*, in case a later author rewrites one into the dot-rooted form. Rejected on `/risk-check`'s explicit finding that this is speculative; the `audit-discipline.md` rule plus the three pointers cover the same risk without touching immune code. (iii) *Wire the canary into `/prime`* for guaranteed per-session coverage. Rejected — directly worsens thread 15. (iv) *Ship the canary unwired and log an OP-11 exception* (the reviewer's first option for mitigation D6). Rejected in favour of its second option, the three agent pointers, which addresses the orphan risk structurally rather than merely recording it. (v) *Skip the end-time `/risk-check`* on the assessment that no class was touched. Rejected per (f) — and the gate then earned its cost by catching a stale mandate (D7) the session had not noticed.

**Conditions attached:** (1) **Two canary drafts could never have failed** and both nearly shipped — draft 1 executed as a subprocess (real `grep`), draft 2 walked from inside the ignored directory (defeats the ignore); both reported "clear" against a demonstrably blind shell. They were caught by requiring the check to FAIL before trusting it, not by review. Any future edit to the canary must re-run both directions. (2) The orphan risk is **mitigated, not eliminated** — the three pointers instruct, they do not enforce. (3) The mandate went **stale mid-session** when scope corrected 4→0 and was only fixed because the `/risk-check` reviewer flagged it; nothing else re-reads a mandate after a scope change. (4) Thread 11's tick is owed once thread 12 lands.

**Decided by:** Claude, under the session's stated autonomy, taking one judgment **against a written mission-thread instruction** ((b)) on measured evidence, with `/risk-check` PROCEED-WITH-CAUTION independently confirming both the override and the class assessment, and all three of its required mitigations applied before commit.

---

## 2026-07-18 (S12-3cd) — Mission thread 12: substituting for an unimplementable remedy, and accepting the gate's rejection of the substitute

**Context:** Mission `repo-health-backlog-2026-07` thread 12 states that `/mission check` ticks a thread without reading the validation contract, and prescribes: *"make `check` read the contract and refuse-or-warn when the named assertion is unmet"*, adding *"Do not fix by adding a gate."* The mission's non-negotiable at `:63` independently forbids *"a new gate to solve a discipline problem."*

**Decision:** (a) The prescribed remedy was **not implemented as written**, because it is not implementable: no thread in any of the five mission files declares which acceptance assertion it serves. (b) The substitute I designed — `check` requires a cited `--evidence` argument — was taken to `/risk-check`, **rejected at RECONSIDER**, and **not built**. (c) A redesign was sent for re-scoring; that agent **stalled and returned no verdict**, and the stall was treated as *nothing*, not as approval. (d) Threads 5, 11 and 13 were **deliberately left unticked** although all three are complete with execution-verified evidence. (e) The end-time `/risk-check` was **skipped**, with the skip test applied and stated before the decision rather than after.

**Rationale:** (a) `grep -nE "^- \[[ x]\].*[Aa]ssertion"` across all five mission files returns only two acceptance-assertion checkboxes that merely contain the word, plus thread 12's own prose. The thread→assertion mapping the remedy presupposes has never existed in any mission file; it lives only in a human's head. The reviewer re-derived this independently and scored Problem reality **Low**. **This is the second confirmed instance of the "filed remedy names a fix that cannot work" pattern**, whose own log entry stated that a second instance should end recording and start labelling the fields — that trigger is now met. (b) The reviewer's objection is correct and general: my `--evidence` check was **presence-only** — it never re-ran the cited command nor re-opened the cited `file:line` — so an author fills the field with whatever satisfies the parser. That is functionally "another checklist", which both `:63` and thread 12's own text forbid, and a credible **seventh** instance of this repo's most-repeated "inert safeguard" class. My framing of it as "argument validation, not a gate" was the exact judgment I had declined to self-score, and it did not survive adversarial inspection. (c) A stalled agent produced no verdict and wrote nothing (confirmed by report mtime and a `## Verdict` grep showing the unmodified first pass); reading silence as consent is how an ungated change ships. (d) Ticking three threads through the very mechanism a gate had just flagged as unsound — hours after receiving that finding — would be the precise behaviour the mission was convened to end, and would read fine in the moment and badly in the record. (e) The executed change set was one agent definition, one doc, and logs; none of the six listed classes is touched, so this is a genuine non-membership, not a self-waiver of a listed class.

**Alternatives considered:** (i) *Implement thread 12 literally* — impossible; there is no assertion to name. (ii) *Add a declared assertion field to the thread schema* across 5 mission files plus `templates/mission-contract.md`. Rejected as too invasive for the gain — **but the reviewer raised it as possibly the honest fix the substitution is avoiding, and the stalled re-score never adjudicated it. It remains genuinely open.** (iii) *Ship the `update` verb alone*, which the gate explicitly cleared. Rejected for this session: thread 12 says "fix both together", and shipping half a thread while its headline defect stays open buys little. (iv) *Tick the completed threads anyway* to stop `/prime` re-offering them. Rejected per (d); the noise is the cheaper cost. (v) *Read the stalled re-score as tacit approval.* Rejected outright.

**Conditions attached:** (1) The redesign is recorded in full in `logs/improvement-log.md` (`64edc8a`) **and is unscored** — it must be re-gated before it is built, and the three questions the stall left unanswered are listed there. (2) An unhandled edge case surfaced in the pre-dispatch premise check: `promote-rw-canonical.md` carries **no `status:` line**, making it invisible to `/mission list` and `/prime` Step 1d; neither the current verbs nor the redesign define behaviour against it. (3) Threads 5, 11 and 13 carry an owed tick, batched behind thread 12. (4) The thread-5 verification harness **returned a false pass on its own over-suppression case** — BSD `sed` does not support `\|` alternation, so the check reported clean because it never matched. It was caught only by having declared the expected value beforehand; that practice is now load-bearing, not optional.

**Decided by:** Claude, under the session's stated autonomy, with `/risk-check` **RECONSIDER** overruling the session's own design judgment on the one question it had explicitly declined to self-score, and the rejection accepted in full rather than argued down or routed around.

---

## 2026-07-19 (S1-e58) — Thread 12's second RECONSIDER: shipping the cleared half on operator override, and retracting a fact I had given the gate

**Context:** Thread 12's carried redesign (closed-set `--assertion`) went back to `/risk-check` after S12-3cd's re-score stalled and left RECONSIDER standing unchallenged. The gate returned **RECONSIDER again** — two Highs, neither mitigable — while explicitly clearing the `update <id>` verb for independent landing. The session mandate carried an unconditional "on RECONSIDER, stop and build nothing."

**Decision:** (a) The `check` redesign was **not built**; the verdict and all three now-answered open questions were recorded instead. (b) The gate's carve-out for `update` was **surfaced to the operator as a decision rather than acted on**, because acting on it would have meant overriding a signed stop condition on the strength of the gate's own permission slip; the operator replied "ship" and `update` landed alone. (c) A load-bearing fact I had supplied to the gate — "2 live mission contracts, not 5, score question (c) against 3 files" — was **retracted and corrected in three places** after the reviewer refuted it. (d) The freeze on `update` was implemented as a **byte comparison plus a per-invocation boundary assert**, not as a scoped-edit promise. (e) The end-time `/risk-check` was **skipped**, with the skip test stated before the decision rather than after.

**Rationale:** (a) Both Highs were structural, not addressable by mitigation: the closed set validates that a cited assertion *exists* and never that it is *true* (the proposal itself concedes the evidence is never executed), and `--assertion none --why` requires no contact with the parsed contract at all — so the lowest-friction path is the one an author takes precisely when the honest answer is inconvenient. That is the mission's own non-negotiable at `:63` ("no new gate to solve a discipline problem… not another checklist"), and a credible 8th instance of this repo's most-repeated failure class. (b) A gate saying "this half is fine" is not the same as the operator retiring their own stop condition; conflating the two would let any sufficiently agreeable reviewer dissolve a mandate constraint. (c) My instrument was `git ls-files logs/missions` — repo-scoped, and structurally blind to mission contracts in sibling project repos. True count is 4 active (+ template ≈ 5), essentially the figure I had discarded as wrong. **`/mission` Step 11 enumerates exactly the repo set I failed to scan, so the instrument was wrong by the command's own design.** The same error recurred in the same session: my `/prime` orientation reported 2 active missions where 4 exist, for the identical reason. (d) A guard that trusts the writer to stay in bounds is the inert-safeguard pattern; sha256 before/after cannot be talked past. The boundary assert exists because "frozen = everything before `## Open threads`" is true of today's three files but is not a law — without it, a future file with a trailing section would put that section silently inside the mutable region, reproducing the exact pattern inside its own fix. (e) The executed change set is a strict *subset* of what the plan-time gate scored (`update` shipped, `check` dropped), so re-gating would have re-scored a smaller change against the same finding.

**Alternatives considered:** (i) *Argue the RECONSIDER down* by reframing `--assertion` as argument validation rather than a gate — this is verbatim what S12-3cd tried and the reviewer dismantled; rejected without attempting. (ii) *Ship `update` silently under the gate's carve-out* — rejected per (b). (iii) *Keep the tick-time design and record an OP-11 exception* instead of rescoping — the gate offered this explicitly as the alternative path. Rejected: an exception documenting that the mechanism does not verify truth and that `none` is an open escape hatch would be recording why the safeguard is inert rather than fixing it. (iv) *Tick threads 5/11/13 anyway* now that `update` exists — rejected; `update` revises lists, it does not tick, and ticking still runs through the mechanism the gate rejected twice. (v) *Delete the `promote-rw-canonical.md` tombstone* — deferred, not declined; file deletion is operator-gated under Autonomy Rule 3.

**Conditions attached:** (1) The replacement design (declared `Assertion: N` authored at thread-filing time) is recorded in `logs/improvement-log.md` (`9cf0a0e`) and is **unscored** — it must be gated before it is built, and its migration surface is **4 active contracts + template**, not the number this session originally supplied. (2) Threads 5, 11 and 13 carry an owed tick for the second consecutive session. (3) Thread 12 has now been RECONSIDER'd twice with two different designs; a third failure should trigger re-scoping the thread rather than a third design. (4) `update`'s guard was verified by execution on both live contracts with expectations declared beforehand — test B (a single trailing space flips the hash) is the falsifiability case and is why the harness is trustworthy.

**Decided by:** Claude under stated autonomy, except (b), which was explicitly the operator's ("ship"), and (a), which was `/risk-check`'s verdict accepted in full for the second consecutive session rather than argued down or routed around.

### 2026-07-19 (S3-0e6) — Ship the narrow `/prime` Step 3 fix now, gated separately from the externalization; decline to reopen the gate for a latent regex imprecision

**Decision:** (a) Two execution-verified correctness defects in `/prime` Step 3 (bolded-severity blindness; unguarded `open()`) were fixed and gated on their own, explicitly NOT bundled with or blocked by the pending externalization redesign — that redesign gets a separate, later `/risk-check` brief. (b) A regex imprecision found during implementation (`\*{0,2}` also admits single-star italic values, outside the documented bold-only contract) was recorded but **not** fixed and the gate was **not** reopened for it.

**Rationale:** (a) The externalization redesign has already returned RECONSIDER twice today on this same mission thread, and the two narrow defects are independently real, independently small (+838 static chars measured against the committed artifact, not assumed), and their fix does not foreclose or complicate the later redesign in any way — the anchor and guard both get replaced when the parser moves. Sequencing them separately meant two genuinely HIGH backlog entries and a per-orientation traceback stopped being live problems today rather than waiting on a redesign gate with an uncertain timeline. (b) The imprecision has zero live occurrences (verified: `command grep -nE '^-? ?\*\*Severity:\*\* *\*[^*]' logs/improvement-log.md` returns nothing) and is therefore inert, not exploitable, today. `prime.md` has 26 live consumers; a second gate-and-edit cycle on the same file in the same day, to fix something with no current effect, is disproportionate. The pattern moves to `logs/scripts/` under the externalization design already directed in (a), so the correct-contract regex (`(\*\*)?`) is written once, there, rather than twice.

**Alternatives considered:** (i) Fold the two narrow fixes into the externalization redesign and wait for that gate — rejected; that gate has failed twice already and the two live defects (invisible HIGH entries, per-session traceback) would stay unfixed with no committed timeline. (ii) Widen the regex to `(\*\*)?` immediately, exactly as the eventual correct contract, since the diff is trivial — rejected; the gate had already returned its verdict against the `\*{0,2}` text, and substituting a different pattern post-verdict without re-gating would be shipping an ungated change under cover of an approved one. (iii) Reopen `/risk-check` for the one-character regex correction — rejected as disproportionate; the imprecision is currently inert and the fix is already scoped into work already planned (the externalization).

**Conditions attached:** (1) The externalization gate brief must state the corrected regex contract (`(\*\*)?`, not `\*{0,2}`) as a requirement, not rediscover it. (2) The static-cost accounting for that future gate brief must be measured via `git show <sha>^:path` / `git show <sha>:path` on the actual committed artifact — this session's own cost-record error (a 16.1x understatement, corrected post-commit) is the direct cautionary precedent, logged in the same session's notes.

**Decided by:** Claude under stated autonomy for (a) and (b)'s specific mechanics; the operator explicitly directed "I would not reopen this gated fix solely for that" for (b), and separately directed proceeding with the narrow-fix-first sequencing for (a) ("So yes: proceed with the narrow fix workflow first—but gate that exact small change before editing").

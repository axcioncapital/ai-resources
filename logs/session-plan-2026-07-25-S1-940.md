# Session Plan — 2026-07-25

## Intent
Commit the untracked `repo-integrity-repairs-2026-07` mission file, then verify Wave 1 threads 1–9 against the mission's acceptance assertions and tick only those that pass.

## Model
opus — match (deciding, not doing: each tick is a judgment about whether evidence is sufficient, against a contract that explicitly forbids closing by assertion)

## Source Material
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/repo-integrity-repairs-2026-07.md` — the mission under repair; its `## Validation contract` (`:53-76`) is the reference standard
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/repo-integrity-repairs-2026-07-wave1-correction-plan-v2.md` — the locked implementation plan the Wave 1 commits executed
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/scripts/check-archive.sh` — thread 1/2 target
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/scripts/check-append-order.sh` — thread 4 target (rewritten by `78ecf21`)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/settings.json` — thread 3 target
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/wrap-session.md` and `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/.claude/commands/wrap-session.md` — thread 4, both copies (the contract requires a count across both)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/agents/lean-repo-auditor.md` — thread 5 reference implementation
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/risk-check.md`, `.../contract-check.md`, `.../close-worktree-session.md` — threads 6, 7, 8
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/CLAUDE.md` — threads 7, 9

## Findings / Items to Address

0. **The mission file is untracked in git** — `git ls-files --error-unmatch` fails on it while the other four mission files pass. Source: `git status --short` shows `?? logs/missions/repo-integrity-repairs-2026-07.md`. Must be committed before ticks so the tick pass has a diffable baseline.
1. **Thread 1 — `check-archive.sh` derives `PROJECT_DIR` from `$0`, not the caller.** Anchor: mission `:81`. Contract assertions 1 and 2 (`:58`, `:59`) cover it and both demand *demonstration by execution from a real second checkout*, never by reading the script.
2. **Thread 2 — 13 of 27 projects have no `logs/scripts/`.** Anchor: mission `:82`. Same contract assertions as thread 1; verification is a re-census, not a re-read of the 2026-07-24 list.
3. **Thread 3 — the archive was deny-read** (`.claude/settings.json:30` carried `Read(logs/*archive*.md)`). Anchor: mission `:83`. Contract assertion 4 (`:61`) — a read-only call on `logs/improvement-log-archive.md` must not be denied.
4. **Thread 4 — the append-direction warning existed at one write site out of four.** Anchor: mission `:84`. Contract assertion 3 (`:60`) is the strictest in the mission: verified *by a count across both copies*, canonical and workspace-root mirror, not by reading one. Note `78ecf21` rewrote the guard after Codex R3, so the shipped state differs from the original Wave 1 commit.
5. **Thread 5 — `grep` is a gitignore-aware wrapper; 3 of 4 audit agents lack the antibody.** Anchor: mission `:85`. **Named in no commit subject** — may not have been done.
6. **Thread 6 — `/risk-check` Step 2.6 verifies structure but not evidence quality.** Anchor: mission `:86`. Claimed by `24c783b`.
7. **Thread 7 — a `/risk-check` RECONSIDER that changes the deliverable never routes to `/contract-check`.** Anchor: mission `:87`. Three-way change: `contract-check.md`, `risk-check.md`, workspace `CLAUDE.md` (the last landed in the *workspace* repo as `0a4c774`, not here) — a multi-clause claim, so contract assertion 6 (`:63`) requires each clause checked separately.
8. **Thread 8 — `/close-worktree-session` has zero stash handling.** Anchor: mission `:88`. Claimed by `3878b4d`.
9. **Thread 9 — an override prompt names only the triggering conflict.** Anchor: mission `:89`. **Named in no commit subject** — may not have been done. The thread's own text carries a correction: its stated target (`prime.md`) is wrong; the real target is workspace `CLAUDE.md:52`.

**Planning note — five threads have no dedicated acceptance assertion.** The contract's six assertions (`:58-63`) name threads 1–4 specifically; threads 5–9 fall only under the two global assertions: *closed with an execution-verified fix or a recorded decline reason* (`:62`) and *no closure on a partial check of a multi-clause claim* (`:63`). For those five, "verified" means the cited defect is demonstrably absent from the cited file today — checked per clause, not per thread.

## Execution Sequence

1. **Commit the mission file.** `git add logs/missions/repo-integrity-repairs-2026-07.md` and commit alone, before any edit. *Verify:* `git ls-files --error-unmatch` on the path exits 0.
2. **Threads 1 + 2 — execute, do not read.** Run `check-archive.sh` from a project that has no local `logs/scripts/` (pick from the 13-project census) and observe where it writes. Re-run the census to confirm the current count. *Verify:* the script either archives that project's own logs or refuses; no write lands in `ai-resources/`.
3. **Thread 3 — attempt the read.** Issue a read-only call against `logs/improvement-log-archive.md`. *Verify:* no permission denial; and `Read(logs/*archive*.md)` is absent from `.claude/settings.json`.
4. **Thread 4 — count across both copies.** Count append-direction warnings at each write site in the canonical `wrap-session.md` and in the workspace-root mirror. *Verify:* every append-to-end log written by either copy carries the warning at its own write site — a count, reported as numbers, not an impression.
5. **Threads 5–9 — per-clause inspection.** For each, check the cited defect against the cited file at today's state. Threads 5 and 9 start from the assumption they are *not* done, since no commit names them. Thread 7 gets three separate clause checks across two repos. *Verify:* each clause independently confirmed or independently recorded as still open.
6. **STOP — present the verdict table.** One table: thread, assertion checked, evidence, verdict (tick / leave open + reason). No ticks written yet.
7. **Write ticks and reasons.** Only threads passing step 6 get `- [x]`. Every thread left open gets a one-line reason appended in place. Thread 10 gets its revert reason (`d62e406`) recorded and stays `- [ ]`.
8. **Commit the tick pass.** Separate commit from step 1, so the baseline and the edits are independently revertible. *Verify:* `git diff HEAD~1` shows only checkbox flips and appended reason lines.

## Scope Alternatives

- **Min** — step 1 only: commit the untracked mission file, no verification. Closes the tracking gap, leaves the tick gap open.
- **Recommended** — steps 1–8: commit, verify threads 1–9, tick what passes. This is the mandate.
- **Max** — additionally tick the six validation-contract checkboxes (`:58-63`) where now satisfied, and re-verify Wave 2 threads 11–16 for staleness. Deferred: Wave 2 is explicitly out of scope, and contract checkboxes should only flip once *all sixteen* threads are closed (assertion 5 is a whole-mission condition).

## Autonomy Posture
Gated

**Stop points:**
- After step 5, before step 7 — present the per-thread verdict table for review before any checkbox is written. This is the mission's own discipline, not added ceremony: its off-mission signals name *"a session that closes threads faster than it verifies them"*, and its measured failure rate is 2 false closes per 30 entries.

## Risk
No structural change classes apparent — the work edits one markdown mission file and adds it to git. No hook, permission, symlink, command, skill, or always-loaded content is touched; nothing runnable is created or rewired. The shared-state tripwire does not fire: ticking checkboxes reorders nothing.

Run `/risk-check` if scope changes — in particular if step 2's execution test reveals `check-archive.sh` still writes to the wrong repo, since fixing that would touch a wrap-time script with cross-repo effects and *would* be a structural class.

**Mission non-negotiables in force** (`:67-71`): do not edit `.claude/commands/prime.md` (belongs to sibling mission thread 15); do not hand-delete stale session markers or archive files that a thread cites as evidence.

# Session Plan — 2026-08-02

## Intent
Implement the smallest coherent vertical slice that integrates Context Engineering into the Work Loop's real Codex-to-Claude path — CE invoked before plan-dependent briefing, Codex recovering plan/state/decisions/blocker/next-unit from durable repository sources, one minimum-sufficient brief delivered through the existing handoff interface with no operator ferrying, consumable and premise-checkable by a fresh Claude session — proven by pre-fix failure evidence at the real seam and post-fix behaviour evidence with fresh contexts.

## Model
opus — match (active: claude-opus-5[1m]). The hard part is deciding *where* the seam is and what the minimum sufficient brief contains, not executing a defined process.

## Source Material
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-v0.2/context-engineering-spec-v0.1.md` — the approved spec (913 lines); commit `148689d` content is what the operator authorised
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/README.md`
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/work-loop-v2-mvp-proposal-v0.4.md`
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md` — 300 lines; the shared policy owner, and the only place CE policy may live
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/step-2-transport-seam-conclusions.md` — the prior seam analysis; names what the transport can and cannot carry
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/step-7-pilot-log.md` — FP-4, FP-11, Unit 3 handoff, pilot result, exit decision
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/work-loop-v2.md` — 113 lines; live Claude-side entrypoint
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.agents/skills/work-loop-v2/SKILL.md` — live Codex-side entrypoint
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/work-loop.md` — 260 lines; v1 doc, read for consumer/reference impact only
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/work-loop-v2-mvp.md`
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/scripts/work-loop-v2-slice-1.test.sh` — existing regression harness for the affected paths
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/audit-discipline.md` — structural change classes

## Findings / Items to Address
Codex's mandate states seven required behaviours for a genuine Standard Work path, a prohibition list, and required evidence. Each is an item to satisfy, not merely to read:

1. **CE invoked before plan-dependent briefing or continuation** — today's seam must be shown to skip it (source: operator mandate § Required outcome 1; § Required failing cases).
2. **Codex recovers plan / authoritative current state / relevant workflow / material decisions / blocker / next justified unit from durable repository sources** — not from conversation (§ Required outcome 2; spec §5.7 durable-source model).
3. **One minimum-sufficient, consumable Claude brief** — a single brief, not a family of documents (§ Required outcome 3).
4. **Delivered through the EXISTING handoff interface, no operator ferrying** — no new transport (§ Required outcome 4; `step-2-transport-seam-conclusions.md`).
5. **A fresh Claude session consumes it and verifies repository-dependent claims** (§ Required outcome 5; spec CE-9's memory-only control, which exists because FP-11 showed `/prime` preloads the prior session summary).
6. **One unambiguous governing plan, current unit, exact next action preserved** (§ Required outcome 6).
7. **No second description of the same unit that can drift against the brief** — FP-4's "one file, not two" (§ Required outcome 7).
8. **Prohibition list held** — no alignment invariant/gate/guard, no `Activated failure modes` field, no risk registry, no continuation checklist, no new mode field, no expanded state schema or ceiling, no second state file, no context pack / handoff doc / session record / per-run log, no context-QC pass, no continuous reviewer, no duplicated policy in either runtime resource (§ Do not add).
9. **Core stays the single policy owner**; runtime resources link to it and hold only actor-specific mechanics (§ Do not add, closing paragraph).
10. **Assessment baseline unchanged** — Codex's post-assessment uses the brief's approved objective, unit boundary, constraints, required evidence, stop conditions; it may respond to new material evidence but must not add requirements from preference. Existing behaviour, so this is a *do-not-break* item, not a build item (§ Assessment behavior).

## Execution Sequence

1. **Read the governing sources.** The six mandate-named documents plus the two live runtime resources and the core.
   *Verify:* I can state, in one sentence each, what the core owns, what the Claude command owns, what the Codex skill owns, and where — precisely, by line — a Standard Work unit is opened and briefed today.

2. **Locate and characterise the real seam.** Identify the exact point in the live path where Codex opens a unit and produces the brief Claude consumes, and whether anything invokes CE before it.
   *Verify:* a named file and line range for the seam. If it cannot be identified from repository inspection → **stop condition 3 fires**; report the inspected surface and stop.

3. **Demonstrate the pre-fix failure.** Show at least one of: the path bypasses CE; the brief it produces is not directly consumable by Claude; or it requires operator context transfer. Evidence must be from execution or from the artifact text, not assertion.
   *Verify:* the failure is written down with its evidence *before* any edit lands, so the fix cannot retro-fit the problem to the solution.

4. **Design the minimum slice against the prohibition list.** Decide what changes in the core (policy) versus in each runtime resource (mechanics). Check the design against all 13 prohibitions in item 8 and the single-policy-owner rule in item 9 *before* writing.
   *Verify:* a written mapping of every intended edit to "policy → core" or "mechanics → runtime", with the prohibition check recorded. If the design needs a second state system to carry the brief → **stop condition 2 fires**. If it needs the approved CE behaviour changed → **stop condition 4 fires**.

5. **Implement the vertical slice.** Edit only what step 4's mapping names.
   *Verify:* `git diff --stat` matches the mapping exactly — no extra files, no new artifacts beyond the declared evidence document.

6. **Demonstrate the corrected path with fresh contexts.** Objective supplied once; a material continuation fact present only in durable repository sources; fresh Codex recovers it and produces the bounded brief; fresh Claude receives it without operator copying; Claude checks its repository-dependent premises; operator does no context transfer. In the same scenario: one false repository premise Claude must catch, one stale secondary document that conflicts with authoritative current state, confirmation the stale document does not control continuation, confirmation no new durable artifact or state field appeared.
   *Verify:* FP-11 contamination controlled — the fresh context must not have been preloaded with the answer by an orientation path. State explicitly how contamination was excluded, or record the demonstration as weaker than claimed.

7. **Regression and integrity checks.** Run `logs/scripts/work-loop-v2-slice-1.test.sh` and any other harness covering the affected paths. Verify the existing state ceiling is unchanged and the Direct Work bypass still exists and still triggers on its stated condition. Verify neither runtime resource restates core policy.
   *Verify:* harness output pasted into the evidence document; ceiling and bypass checks each cite a line, not a recollection.

8. **Diff inspection and commit.** Inspect the full diff for unrelated changes and unintended new artifacts; confirm `logs/friction-log.md`'s pre-existing dirt is preserved and not swept in. Commit implementation plus evidence.
   *Verify:* commit hash reported, alongside files changed, the pre-fix failure, the post-fix behaviour, scenarios run, and an honest list of entrypoints integrated versus still unproven.

## Scope Alternatives
- **Min** — integrate CE at one entrypoint only (the single Standard Work path Codex actually opens units through), with the pre-fix and post-fix demonstration. Everything else recorded as unproven.
- **Recommended** — min, plus the rich single scenario carrying the false premise and the stale conflicting document in the same run, plus the regression/ceiling/bypass verification. This is what the mandate asks for.
- **Max** — additionally wire the remaining entrypoints (including `axcion-design-studio`'s stray command copy). **Out of scope**: the mandate forbids horizontal build-out, and the installation contract is an open post-MVP mission thread, not this unit.

## Autonomy Posture
Gated — the mandate itself names five conditions where the correct action is to stop and report rather than improvise, and those are genuine junctures, not ceremony.

**Stop points:**
- The relevant entrypoint cannot be identified from repository inspection (sequence step 2).
- No existing handoff interface can carry the brief without creating a second state system (step 4).
- Implementation would require changing the approved Context Engineering behaviour (step 4).
- Operator-free delivery would require external automation or permissions outside authorized repository scope (step 4 or 6).
- The smallest viable fix materially exceeds this vertical integration unit (any step).
- Additionally: if the FP-11 contamination control cannot be made honest, report the demonstration as weaker than the mandate requires rather than claiming a clean fresh-session proof (step 6).

## Risk
Structural change class touched — this work is high-consequence, so its one independent review is briefed **risk-aware** (`ai-resources/docs/qc-independence.md` § Risk-aware review). No separate gate runs.

Classes named, not adjudicated here:
- **Edits to shared runtime resources** — `.claude/commands/work-loop-v2.md` and `.agents/skills/work-loop-v2/SKILL.md` are consumed by two actors and, per the open installation thread, exist in three projects; one holds an untracked copy with no core.
- **Edits to the shared policy owner** — `work-loop-v2-executable-core-v0.1.md` is the single authority both runtime resources point at.
- **Reordering operations against shared state (tripwire)** — inserting a CE invocation *before* plan-dependent briefing changes the sequence of the loop against its state file. The "existing-command refactor" framing does not exempt this.

Environment-fit check: not applicable — the work product is prompt/policy text, not launch- or runtime-gated tooling.

Non-structural note: the operator's own prohibition list is the sharpest risk control here. The realistic failure mode for this session is not a broken command but scope inflation — building a subsystem where a seam edit was asked for. Execution step 4's written mapping exists to make that visible before it lands.

# Session Plan — 2026-08-01

## Intent
Implement Work Loop v2 MVP Slice 1 (the core round trip) red-green — Claude side (1.2, 1.3) first against a hand-written state-file fixture, then the Codex side (1.1, 1.4) if the session holds.

## Model
opus — match

## Source Material
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/step-4-slice-plan.md` — the slice plan (allowed input)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md` — the executable core (allowed input)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/work-loop.md` — Work Loop **v1**, read for command-shape convention and folder-collision facts only. Not edited; retirement is Step 7's.
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/skills/ai-resource-builder/references/operational-frontmatter.md` — model/effort frontmatter for the new v2 command
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/audit-discipline.md` — structural change classes
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/qc-independence.md` — review sizing
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/work-loop-v2-mvp.md` — mission contract (thread ticked at wrap)

No other planning-history document is loaded. Playbook `:129` restricts a Step 5 session to the first two; the live repository is inspected freely for conventions and fixture material (`:130`).

## Findings / Items to Address

Slice 1's four behaviours, from `step-4-slice-plan.md` § "Slice 1 — the core round trip":

1. **1.2 — verify premises / refuse a false premise** (`step-4-slice-plan.md:35`). Claude checks each brief claim against the live repository and writes down what it inspected. A false claim goes to the state file, `turn: codex`, commit, stop. Two runs off one brief: **(a)** all claims true → the inspection record must still appear, and an absence claim must say what was searched; **(b)** one deliberately false claim → `git diff` must be empty across every file the brief named. Traces to core § 6 rules 1 and 3, § 7 *Hand back to Codex*.
2. **1.3 — execute and evidence** (`:36`). Claude implements the unit, then writes the latest material result and an evidence pointer into the state file, sets `turn: codex`, commits. The evidence's own failing case is built first and shown failing, then passing. Fails if an evidence line would read the same whether or not the work happened (core § 6 rule 5).
3. **1.1 — Codex writes the brief** (`:34`). Codex, invoked explicitly as `$<name>`, writes a brief into `logs/work-loop/{task-id}.md` with `turn: claude`; Claude commits. Failing case: run in a checkout where `logs/work-loop/` does not exist — the folder must be created, not fall back to `logs/loop/`, and `git log -- logs/loop/` must gain nothing (v1 still scans that folder — confirmed live, 20 references in `.claude/commands/work-loop.md`).
4. **1.4 — Codex assesses and closes** (`:37`). On closure the state file reduces to the four closing fields; no active field survives (core § 4 *When the task closes*).

Two obligations that are not behaviours (`:83-100`) and are invisible to this session unless carried:

5. **The Codex resource's folder needs a `.gitignore` re-include** — exactly one line, `!.agents/skills/<name>/`. Without it the resource works here and does not exist in a fresh clone. Codex side only.
6. **`$name` invocation is assumed and still untested.** Codex caps the always-present skills list at 8,000 characters against 12,963 carried, so descriptions may be dropped before matching. If 1.1 fails at invocation, check this **first** — and it is a finding for the operator, not something to design around inside the slice (`:99`).

## Blind-spot scan — PROCEED-WITH-CONSTRAINTS (2026-08-01, pre-implementation)

Three open findings, to close at the step named:

1. **Leftover Codex probe from Step 2, invisible to git.** `2540a36` deleted only `logs/loop/wl2-state.md`; `.agents/skills/wl2-probe/SKILL.md` survives, untracked and gitignored (`.agents/skills/*`). A live `$wl2-probe` can satisfy step 9's `$name` test and 1.1's failing case in place of the new resource — false completion. **Close at step 9:** delete the folder, confirm by attempting `$wl2-probe` in Codex, not by `git status`.
2. **The only re-include line names v1.** `.gitignore:77` is `!.agents/skills/work-loop/`, and `.agents/skills/work-loop/SKILL.md` is tracked and live. `git check-ignore -v` proves `.agents/skills/work-loop-v2/SKILL.md` is ignored. Reusing the name overwrites v1 (out of scope); any other name is gitignored until a new line lands, so the commit silently omits the resource. **Close at step 9, before the first commit of the resource:** add `!.agents/skills/<v2-name>/`, verify with `git check-ignore -v` on the real path.
3. **Workspace-root reachability is undecided.** The root's `.claude/commands/work-loop.md` is a hand-made symlink into `ai-resources/`; nothing creates one automatically. Slice 2's behaviour 2.1 would fail from a root session for a non-loop reason. **Close at step 2:** state in one line whether the v2 command gets a root symlink now or is deliberately ai-resources-only for the MVP.

## Execution Sequence

1. **Blind-spot scan.** ✓ Done — PROCEED-WITH-CONSTRAINTS, three findings above. Workspace `CLAUDE.md` fires it post-plan / pre-implementation when the plan creates runnable infrastructure; a new command qualifies. *Verify:* verdict emitted inline; on PAUSE-AND-FIX, findings resolved before step 2.
2. **Inspect conventions, decide the v2 command's path and name.** Read v1 for shape and frontmatter convention only. *Verify:* target path stated out loud; `logs/work-loop/` confirmed absent; no v1 file edited.
3. **Hand-write the state-file fixture** at `logs/work-loop/{task-id}.md` per core § 4 — frontmatter `task`/`turn`, at most the five active body fields, and a brief carrying checkable claims. *Verify:* file exists under `logs/work-loop/`; `git status` shows nothing new under `logs/loop/`.
4. **1.2 red.** Construct both failing cases before any implementation. *Verify:* (a) and (b) each run against the not-yet-built behaviour and observably fail — recorded, not asserted.
5. **1.2 green.** Implement verify-premises / refuse-false-premise. *Verify:* (a) produces an inspection record naming what was searched; (b) leaves `git diff` empty across every file the brief named, writes the finding to the state file with `turn: codex`, and commits.
6. **1.3 red.** Build the evidence's own failing case first and show it failing. *Verify:* the case would read differently had the work not happened — an always-passing check is not evidence (core § 6 rule 5).
7. **1.3 green.** Implement execute-and-evidence. *Verify:* step 6's case passes; state file still holds at most the five active fields.
8. **Commit the Claude side.** *Verify:* commit landed by pathspec; `git log -- logs/loop/` gained nothing.
9. **Split-point decision.** If the session still holds, continue to the Codex side (1.1, 1.4) plus the `.gitignore` re-include. Otherwise stop here. *Verify:* either 1.1/1.4 green with `$name` invocation proven, or a written split record naming exactly what is green and what has not started.

## Scope Alternatives
- **Min** — Claude side only (1.2, 1.3), green and committed, split recorded. This is the plan's own predefined split point (`:44-50`), not a shortfall.
- **Recommended** — Min, then attempt the Codex side; stop cleanly at the split trigger rather than carrying a half-built second half.
- **Max** — full Slice 1: 1.1 and 1.4 green, `.gitignore` re-include landed, `$name` invocation proven under the over-cap budget.

## Autonomy Posture
Gated

**Stop points:**
- `$name` invocation proves unreliable — stop and report to the operator; do not design around it (`:99`).
- Claude side green and committed with the Codex side not started at the session boundary — stop, record the split (`:49`).
- A behaviour cannot be given a constructible failing case — surface rather than weaken the behaviour.
- Any scope change: core § 6 rule 4 sends a scope change to the operator.

## Risk
Structural change class touched — this work is high-consequence, so its one independent review is briefed **risk-aware** (`ai-resources/docs/qc-independence.md` § Risk-aware review). No separate gate runs. Classes: a new Claude Code command; a new Codex resource with a `.gitignore` distribution change; a new shared-state surface (`logs/work-loop/`) adjacent to v1's live `logs/loop/`.

**Environment-fit note.** This plan's Codex half depends on how the resource is *triggered* — explicit `$name` under an over-cap description budget — which is exactly the launch-gated shape this check exists for, and it is already flagged untested. It is a stop point above, not a thing to work around.

# Session Plan — 2026-08-01

## Intent
Implement Slice 1's Codex side (behaviours 1.1 and 1.4) red-green: clear the two recorded prerequisites, build the Work Loop v2 Codex resource, prepare the prompt the operator pastes into Codex, and test explicit `$name` invocation.

## Model
opus — match (deciding, not doing: naming and shaping a new cross-model resource, constructing falsifiable failing cases for two behaviours, and judging an untested invocation premise honestly rather than designing around it).

## Source Material
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/step-4-slice-plan.md` — behaviours 1.1 and 1.4 with their failing cases; the two non-behaviour obligations at `:83-100`
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md` — the contract the resource links to and never restates
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/plans/work-loop-v2-mvp/step-5-slice-1-evidence.md` — the split record; both prerequisites with their evidence
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/work-loop-v2.md` — the Claude half this resource hands off to
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/scripts/work-loop-v2-slice-1.test.sh` — the 18-assertion harness to extend
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.agents/skills/work-loop/SKILL.md` — v1, read-only, form conventions only (119 lines / 9,302 chars)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.gitignore` — the four-rule re-include ladder at `:71-77`

## Findings / Items to Address
1. **Leftover probe resource is live and invisible to git.** `.agents/skills/wl2-probe/` survived commit `2540a36`, which deleted only `logs/loop/wl2-state.md`. Untracked *and* gitignored, so `git status` cannot show it. It can satisfy a `$name` invocation test in place of the real resource, and it inflates the very description budget the premise is about. — `step-5-slice-1-evidence.md:59`
2. **`.gitignore:77` re-includes only v1's `work-loop`.** `git check-ignore -v .agents/skills/work-loop-v2/SKILL.md` returns `.gitignore:76` — ignored. Any name other than `work-loop` is silently omitted from its own first commit. Reusing `work-loop` would overwrite v1, which Step 7 owns. — `step-5-slice-1-evidence.md:58`, `step-4-slice-plan.md:88-93`
3. **Behaviour 1.1's failing case.** Run in a checkout where `logs/work-loop/` does not exist: pass = the folder is created and used; fail = it falls back to `logs/loop/`. Also assert `git log -- logs/loop/` gains nothing — v1 still scans that folder. — `step-4-slice-plan.md:34`
4. **Behaviour 1.4's failing case.** Inspect the closed file: fail if any of the five active fields (objective/scope, lane and unit, latest result, blocker, next action) survives closure. Closed shape is four fields only: outcome, decisions that matter, final commit or evidence pointer, accepted limitations. — `step-4-slice-plan.md:37`, core § 4 *When the task closes*
5. **The untested premise.** Codex caps the always-present skills list at 8,000 chars; the runtime already carries 12,963, so a description may be shortened or dropped before it is ever matched. Whether explicit `$name` invocation is reliable under that over-cap budget is unproven. If 1.1 fails at invocation, check that first — it is a finding for the operator, not something to design around inside the slice. — `step-4-slice-plan.md:94-100`
6. **Carried forward, not this session's work to resolve silently.** Last session landed a new command, a new shared-state log surface and a new script with **no independent Codex review — recorded `unassessed`**. Sizing that review is stated as this session's first order of business. — `session-notes.md` S5-646 § Review status

## Execution Sequence
1. **Blind-spot scan.** This plan creates new runnable infrastructure (a Codex-side resource, a `.gitignore` rule governing what reaches commits) — the narrow band where the gate bites. Run it post-plan, pre-implementation. *Verify:* verdict surfaced inline; PAUSE-AND-FIX findings resolved before step 2.
2. **Prerequisite A — delete `.agents/skills/wl2-probe/`.** *Verify:* directory absent; `.agents/skills/` lists exactly five entries (four `source-command-*` plus `work-loop`).
3. **Prerequisite B — add the re-include line to `.gitignore`, before any commit of the new resource.** One line, `!.agents/skills/work-loop-v2/`, appended to the existing four-rule ladder. *Verify:* `git check-ignore -v .agents/skills/work-loop-v2/SKILL.md` returns non-zero (not ignored). This is the falsifiable check — it returned `.gitignore:76` before the edit.
4. **Extend the harness with 1.1 and 1.4 assertions, and run it RED first.** New assertions must fail before the Codex side runs. Apply last session's own lesson: reject any assertion that would pass on the untouched fixture, on the mere existence of a commit, or on a word the brief already contains. *Verify:* the new assertions fail; the existing 18 still pass.
5. **Write `.agents/skills/work-loop-v2/SKILL.md`.** Links to the executable core; restates none of it. Deliberately short description — v1's is ~1,000 chars, and the budget premise is the thing under test. *Verify:* the file exists, `git check-ignore` clears it, and it contains no rule already in the core.
6. **Hand the Codex prompt to the operator in chat.** *Verify:* the prompt names the resource by `$name`, states the task id, and needs no context the operator must supply from memory. **Session stops here for the operator's Codex run.**
7. **Behaviour 1.1 — Codex writes the brief; Claude commits.** *Verify:* the brief lands at `logs/work-loop/{task-id}.md` with `turn: claude`; `logs/loop/` gains nothing (`git log -- logs/loop/` unchanged, `git status --porcelain logs/loop/` empty).
8. **Run the unit through `/work-loop-v2`** to produce a result for Codex to assess. *Verify:* existing Slice 1 Claude-side behaviour still green.
9. **Behaviour 1.4 — Codex assesses and closes; Claude commits.** *Verify:* the closed file carries the four closing fields and none of the five active ones.
10. **Re-run the harness GREEN, write the evidence record, commit.** *Verify:* harness exits 0; `step-5-slice-1-evidence.md` gains a Codex-side red-green record with the same limitations discipline as the Claude-side one.
11. **Mission thread.** Tick Slice 1 only if the validation contract is met by evidence, not by the checkboxes running out. Last session declined to tick it for exactly this reason.

## Scope Alternatives
- **Min** — steps 1–6: prerequisites cleared, resource built, prompt handed over. Correct stopping point if Codex cannot be driven at all, or if `$name` invocation fails (that outcome is a recorded finding, not a failure to route around).
- **Recommended** — steps 1–10: the full Codex-side round trip green and committed, evidence recorded.
- **Max** — adds step 11 plus sizing the carried-forward `unassessed` review of last session's Claude-side command and harness.

## Autonomy Posture
Gated.

**Stop points:**
- After step 6 — the prompt is handed over and the session cannot proceed until the operator runs Codex and returns its output. Structural, not discretionary: Claude standing in for Codex would violate the workspace cross-model rule and fake the one premise Step 5 was told to watch.
- If `$name` invocation proves unreliable under the over-cap description budget — record as an operator finding and stop; do not redesign the invocation path inside the slice (`step-4-slice-plan.md:99`).
- If either behaviour cannot be given a constructible failing case.
- Before ticking the mission's Slice 1 thread.

## Risk
Structural change classes touched: a new Codex-side resource (new runnable infrastructure), a `.gitignore` rule that governs what reaches commits, and an edit to an existing acceptance script. This work is high-consequence, so its one independent review is briefed **risk-aware** (`ai-resources/docs/qc-independence.md` § Risk-aware review). No separate gate runs.

Two review obligations are live and must not be silently merged: this session's own changes, and the **carried-forward `unassessed`** Claude-side command and harness from S5-646. Environment-fit check does not apply — no terminal-launched executable is planned; the Codex resource is invoked inside the Codex app the operator already has open, and the harness runs in-session.

### 2026-05-28 — Mandate-alignment recovery: should `/session-start` be re-invoked when a session pivots immediately after `/prime`?

- **Status:** logged (pending)
- **Category:** session-issue / command-design
- **Source:** nordic-pe-macro-landscape-H1-2026 session 2026-05-28 — session opened with mandate "FX-B1+B2 implementation"; pivoted immediately to drafting `templating-plan-v2-resolution-plan.md` per operator direction. Recorded mandate diverged from actual work for the session's full duration.
- **Friction source:** Decision-point-posture pivots (operator-driven, non-drift) currently leave the recorded mandate stale. `/wrap-session` then journals work against a mandate that does not describe what happened. Downstream `/drift-check` and `/contract-check` would mis-classify the pivot as drift.
- **Proposal:** Consider adding a `/session-start --re-mandate` re-invocation path that the operator can fire mid-session when a pivot happens. Alternatively, document a "Pivot Acknowledged" annotation pattern in the session-notes body so `/wrap-session` can detect and journal the pivot explicitly.
- **Target files (when executed):** `ai-resources/.claude/commands/session-start.md` (optional re-mandate flag), `ai-resources/.claude/commands/wrap-session.md` (pivot-detection in Step 7a).
- **Triage cadence:** `/improve` consideration; not urgent.

### Sequencing note (five entries combined)

Suggested three-session sequence:

- **Session 1 (rules, ~45 min):** Entry "Extend Model Tier rule to agents" + Entry "Codify subagent-summary cap + /usage-analysis discipline". Purely CLAUDE.md + one `/wrap-session` edit. Lowest risk, highest per-turn leverage. — **VERIFIED-DONE 2026-05-25:** Both source entries already codified by drift. Workspace `CLAUDE.md` § Model Tier names agents explicitly and references `docs/agent-tier-table.md` ("Extend Model Tier rule to agents" — done). `ai-resources/CLAUDE.md` § Subagent Contracts states the 30/20-line cap + notes-to-disk + summary-only rule and names existing implementations ("Codify subagent-summary cap" — done). `ai-resources/CLAUDE.md` § Session Telemetry codifies the `/usage-analysis`-every-substantive-session rule and `/wrap-session` prompt integration ("/usage-analysis discipline" — done). [FADING-GATE] pattern — booking was carried forward from when these rules did not yet exist; no edits required.
- **Session 2 (templates, ~1–2 hrs):** Existing entries — canonical project settings template + canonical project CLAUDE.md template. Touches `/new-project` pipeline + research-workflow templates. Before implementing, re-read the 2026-04-13 decision ("Commit Rules propagate by explicit copy") to confirm whether the inheritance workaround is still needed. — **VERIFIED-DONE 2026-05-25:** Templates extracted to `ai-resources/templates/` (settings template + 5 CLAUDE.md fragments + README); `/new-project` step 2 + step 4 rewired to consume via walk-up to `ai-resources/`; research-workflow CLAUDE.md aligned (2 within-section drift fixes; `## File Verification and Git Commits` preserved as workflow-local per risk-check mitigation #3). 2026-04-13 decision re-checked → **KEEP** (uneven per-project mirroring across 5 projects confirms inheritance still unreliable; rationale recorded in `templates/README.md`). Mitigations: plan-time `/risk-check` PROCEED-WITH-CAUTION + system-owner-added architecture-map update = 4 mitigations total; end-time `/risk-check` GO (all 5 dimensions Low). QC found bash-native substitution unsafe under apostrophes in description + agent global-substitution collision → swapped to python3 + mustache `{{NAME}}` / `{{PROJECT_DESCRIPTION}}` placeholders. Commits: `8b44015` (templates + arch map + cross-refs), `39b27b5` (`/new-project` rewire), `c692864` (research-workflow alignment), `54bf85b` (risk-check reports). Follow-on for a future session: `deploy-workflow.md:209` still carries an inline `CANONICAL_PERMS` literal — second consumer candidate for `templates/` unification.
- **Session 3 (detection + automation, ~45 min):** Entry "Add three questionnaire items to /repo-dd" + Entry "Pre-commit skill-size warning hook". Depends on Session 1 (Agent Tier Table) and Session 2 (canonical templates) landing first so the new questionnaire items have stable references. — Session 1 dependency satisfied (verified-done 2026-05-25); Session 2 dependency satisfied (verified-done 2026-05-25). — **VERIFIED-DONE 2026-05-25:** Both source entries shipped 2026-04-18 — commit `0962c0c` (source entries logged), commit `bbd2261` (`.claude/hooks/check-skill-size.sh` informational pre-commit warning at >300 lines), commit `e3f6dfe` (Prevention Session 3 wrap confirming "questionnaire + skill-size hook + broadened allowlist" both landed). Pre-commit hook present at `.claude/hooks/check-skill-size.sh` and wired in `.claude/hooks/pre-commit` lines 76–79. `/repo-dd` questionnaire concept present (command line 61). [FADING-GATE] pattern — booking carried forward through 2026-05-22 Sequencing note and 2026-05-25 dependency-satisfaction annotation when the underlying work had already shipped 5 weeks prior. No edits required.

### 2026-05-28 — Concurrent sessions cause TOCTOU races on shared log files (broader entry — supersedes the narrow `/session-start` Step 0.5 entry above)

- **Status:** Phases 1+2+3 applied. Phase 1 applied 2026-05-28 (write-only `.session-marker` in `/prime`, commit `ea93d62`). Phases 2+3 atomic landing 2026-05-29 (commit `9f91b2f`, Option A per system-owner advisory — eliminated symlink bridge proposed in original Phase 2-only spec; closed cross-session TOCTOU race at structural root by extending atomic commit across all writers AND all readers in one operation). Phase 4 (legacy fallback cleanup) is **N/A** under Option A — no fallback paths were introduced; downstream readers are marker-aware in the same atomic commit.
- **Verified:** 2026-05-29 — atomic commit `9f91b2f` (22 files); end-time `/risk-check` returned GO (all 5 dimensions Low); marker-resolution read-test verified zero orphan bare `logs/session-plan.md` references remain in any consumer body; `/qc-pass` REVISE findings (1 BREAK risk in `backup-session-plan.sh` regex + 3 narrative-drifts) all addressed inline. Recursive PROCEED-WITH-CAUTION pattern in plan-time gates logged separately to `logs/maintenance-observations.md` as Friday-cadence improvement candidate (pre-spec grep checklist for renamed/removed paths).
- **Verified:** 2026-05-28 — `/prime.md` Steps 8a.3.a / 8b.3.a / 8c.3 now write `logs/.session-marker` (one line `{date} S{N}`) immediately after their existing `.prime-mtime` writes; `.gitignore` line for `logs/.session-marker` added; risk-check report at `audits/risk-checks/2026-05-28-prime-session-marker-phase-1-write-only.md` (verdict GO, all 5 dimensions Low); `/qc-pass` verdict GO (no findings); bash logic smoke-tested in isolation (fresh→S1, same-day x2→S2, same-day x3→S3, day rollover→S1).
- **Category:** session-issue / cross-cutting architecture
- **Source:** Live recurrence during axcion-brand-book session 2026-05-28; operator explicitly attributed cause to deliberate concurrent-session use. Friction-log entry 2026-05-28 10:05 records the live event. This entry is broader than the earlier same-day `/session-start` Step 0.5 entry (which only proposed a Step 2.5 patch); supersede that proposal with the structural fix below.
- **Supersedes:** the same-day "2026-05-28 — /session-start Step 0.5 mtime guard misses foreign writes that arrive DURING the Mandate Confirmation wait" entry above. Mark that entry's Status as "logged — superseded by broader 2026-05-28 entry below" when applying this one. The narrow Step 2.5 patch from the prior entry is no longer the recommended fix; the structural per-session-marker approach replaces it.

#### Problem class

Three commands (`/prime`, `/session-start`, `/session-plan`) read shared log files (`logs/session-notes.md`, `logs/session-plan.md`) at command entry, make decisions based on the read, and later write back to those same files. Between the read and the write, an arbitrary number of conversation turns elapse (mandate confirmation echoes, class/intent confirmation prompts, AskUserQuestion blocks, Read calls for source material). When a second session is active in the same project, ANY write the second session makes to a shared log during the first session's read-to-write window is invisible to the first session's point-in-time mtime guard. This is a classic TOCTOU (time-of-check-to-time-of-use) race.

Today's session demonstrated the race three times in one startup sequence:

1. **`/session-start` Step 0.5 → Step 2 wait → Step 3 write.** Step 0.5 mtime check returned clean. Foreign write arrived during the Mandate Confirmation echo wait. Operator's `y` was applied against an echoed mandate that no longer matched on-disk content.
2. **`/session-plan` Step 0 → Step 7 write.** Step 0 mtime check found `session-plan.md` 25 hours old (clean). Foreign session's `/session-plan` rewrote the file during Step 1.5 / Step 2 / Step 3 confirmations. Step 7 Write tool errored with "File has been modified since read"; the only reason this race was visible was because Claude Code's Write tool happens to enforce read-before-write integrity at the tool level.
3. **`/session-notes.md` mandate-block overwrite.** Foreign session's `/session-start` Step 3 wrote its own mandate block under the same `## 2026-05-28` header that I had just written my mandate to. My mandate block was fully replaced.

The prior fix for `/session-plan` Step 0 (commit `8ab5685`, MISMATCH auto-route to pass2) was a one-off patch to one command. The race class survives in every other shared-log read-write site.

#### Proposed structural fix — per-session markers on all shared writes

Each session at `/prime` time generates a 4-character session marker (e.g., `S1`, `S2`, `a3f7`) and persists it to `logs/.session-marker` (single file, last writer wins — but this file is only read by the SAME session's later commands, so the race surface is small).

All subsequent shared-log writes scope themselves with this marker:

- **`logs/session-notes.md`** — `/prime` writes `## YYYY-MM-DD — Session {marker}` instead of bare `## YYYY-MM-DD`. Each concurrent session gets its own header. Existing readers that grep `^## ${DATE}` still match (and the `/prime` Step 1a sibling-entry sweep already handles multiple same-day headers as a known pattern). `/session-start` Step 3 appends its Mandate block under THIS session's marker-bearing header (located by reading `.session-marker`, not by date-only match).
- **`logs/session-plan.md`** — replaced by `logs/session-plan-{marker}.md`. The collision detection in `/session-plan` Step 0 simplifies dramatically: there is no shared file to collide on. Each session writes to its own scoped file. `pass2` becomes unnecessary (and the existing pass2 mechanism graduates from "race-condition safety valve" to "explicit operator-initiated re-plan within one session").
- **Downstream readers** (`/drift-check`, `/wrap-session`, `/qc-pass`, etc.) read `.session-marker` first, then read the marker-scoped files. A symlink or alias from `logs/session-plan.md` to the current session's plan can preserve backward compatibility for readers that don't know about markers yet.

The marker is short and deterministic per session — the first existing `.session-marker` value on disk plus a deterministic increment (`S1` → `S2` → `S3`), wrapping at `S9` → `Sa` → `Sz`. `/prime` Step 0 reads the existing value (if present and same-day) to increment; otherwise resets to `S1` and writes `YYYY-MM-DD` + value. A same-day re-read of `.session-marker` returning a different value than the session's own cached marker is itself a concurrent-session signal that `/prime` can surface.

This eliminates the TOCTOU race at its root: there is no longer a shared mutable file for concurrent sessions to collide on. The remaining synchronization concern is `.session-marker` itself, but the file is small, atomic-write-friendly (single short token), and is read ONLY by the same session that wrote it — not cross-session — except during `/prime` increment.

#### Alternatives considered

- **(a) Atomic re-read at each write site.** Every shared-log write site re-reads mtime immediately before write and abort/restart if changed. Lower-magnitude change, but requires patching ~6 write sites across 4 commands and only narrows the race window (does not eliminate it). Rejected.
- **(b) Per-session subdirectory.** Each session writes to `logs/sessions/{marker}/{file}.md`. Cleaner separation than markers in filenames, but requires every consumer command to know about the subdirectory convention. Higher-friction migration. Rejected for now; can be a later step.
- **(c) Advisory file locks (`flock`).** Standard Unix pattern. Adds shell-state complexity, doesn't survive across tool boundaries (Claude Code Write tool doesn't acquire flocks), and would require wrapping every Write/Edit in a Bash flock dance. Rejected.
- **(d) Refuse concurrent sessions.** `/prime` detects an active session via marker file and refuses to start a second one. Operator preference is to RUN concurrent sessions (today's evidence), so refusing would block a workflow they actively use. Rejected.
- **(e) Status quo + Step 2.5 patches.** Each command gets a TOCTOU-narrowing patch like the one in the prior 2026-05-28 entry. The race class survives across the ~6 write sites and recurs whenever a new write site is added. This is the trajectory we've been on (the 2026-05-25 14:10 friction entry was the third recurrence). Rejected in favor of structural fix.

#### Migration plan

Phased, low-risk:

1. **Phase 1 — `/prime` writes the marker.** `/prime` Step 8a.3 / 8b.1 generates and persists the marker; existing today's-header behavior unchanged. No other command reads the marker yet. Risk: zero (additive).
2. **Phase 2 — `/session-start` and `/session-plan` consume the marker for header location and file naming.** Commands prefer marker-scoped files; fall back to legacy paths if marker absent (interop with sessions that started before Phase 2 rollout). Risk: low (each command's behavior gracefully degrades).
3. **Phase 3 — downstream consumers (`/drift-check`, `/wrap-session`, `/contract-check`, `/qc-pass` plan-reading) update to read marker first.** Risk: medium — many touchpoints.
4. **Phase 4 — deprecate legacy code paths.** Once Phase 3 is verified, remove the fallback paths in `/session-start` / `/session-plan`. Risk: low (Phase 3 has verified the new path).

**/risk-check change class:** YES (this is a cross-cutting protocol change across 5+ commands and 2+ shared log files; explicit shared-state coordination redesign). Run `/risk-check` before EACH phase, not as a one-shot.

#### Target files (by phase)

- Phase 1: `ai-resources/.claude/commands/prime.md` (Step 8a.3.a / 8b.1 — generate and persist `.session-marker`); new `ai-resources/.gitignore` line for `logs/.session-marker` (it's per-machine session state, not committed).
- Phase 2: `ai-resources/.claude/commands/session-start.md` (Step 3 — locate header by marker, not by date); `ai-resources/.claude/commands/session-plan.md` (Step 0 simplification — drop the 6-hour-window pass2 routing, write directly to `session-plan-{marker}.md`; Step 7 OUTPUT_TARGET changes).
- Phase 3: `ai-resources/.claude/commands/drift-check.md`, `ai-resources/.claude/commands/wrap-session.md`, `ai-resources/.claude/commands/contract-check.md`, `ai-resources/.claude/commands/qc-pass.md` (plan-reading branch), `ai-resources/skills/friday-checkup/SKILL.md` (any references to `session-plan.md`).
- Phase 4: cleanup of legacy fallback branches across Phase 2 commands.

#### Notes

- The friction-log 2026-05-28 10:05 entry is the live-event record; this is the proposal record. `/fix-repo-issues` will surface both.
- The narrow same-day `/session-start` Step 0.5 entry written earlier in this session is superseded — apply the supersede marker (`Status: logged — superseded by broader 2026-05-28 entry`) when this proposal is taken to plan.
- This is not a hotfix-able item; it requires a phased rollout. Suggested cadence: a dedicated `/friday-act` wave dedicated to Phase 1 (marker introduction) with `/risk-check` as advisory gate.


### 2026-05-29 — /open-items friction-log cross-match too literal — false-positive Tier 1 surface

- **Status:** applied 2026-05-29
- **Verified:** (pending operator confirmation at /wrap-session) — applied by commit `e72bca7` (`/open-items` Step 1 four-condition tolerance match, Item 2 of session-plan-S4); `/qc-pass` GO with two minor non-blocking phrasing observations under conditions (b)/(c).
- **Category:** session-issue
- **Source:** /resolve-repo-problem MANUAL mode 2026-05-29
- **Friction source:** /open-items reported 2026-05-28 friction-log entry at 10:05 as unresolved Tier 1 friction. The entry had been structurally resolved + verified the same day by commit `9f91b2f` (improvement-log "2026-05-28 — Concurrent sessions cause TOCTOU races" carries `Status: Phases 1+2+3 applied` + two `Verified:` lines + an explicit reference to "Friction-log entry 2026-05-28 10:05 records the live event" in its Source field). The /open-items Step 1 cross-match contract (logs/friction-log.md row, T1 column) recognizes only two literal patterns — `**Friction source:** friction-log <HH:MM>` and `friction-log <HH:MM>` — both requiring strict adjacency between the `friction-log` token and the `HH:MM` token. The improvement-log's actual phrasing inserts the word `entry` between `Friction-log` and the timestamp, breaking adjacency; the cross-match misses; the resolved entry surfaces as Tier 1. Operator caught the staleness during /prime → /open-items sequence.
- **Proposal:** Apply Option B (structural fix per the audits/working/ triage notes). Replace the two literal patterns in `open-items.md` Step 1 friction-log T1 row with a four-condition tolerance match: (1) HH:MM token present in improvement-log entry body, (2) friction entry's YYYY-MM-DD date present in improvement-log entry body OR `Verified:` date within ±1 day of friction-entry session date, (3) `friction-log` (case-insensitive) appears within the same sentence/bullet as the HH:MM token (arbitrary intervening words allowed), (4) `Status: applied` + non-empty `Verified:` (already in contract). Keeps the `### YYYY-MM-DD` header-date guard for same-`HH:MM`-different-date collision prevention. No /risk-check gate — same-command logic edit, no shared-state reorder, no hook/settings/agent surface; /qc-pass at edit time covers it. Recurrence is structurally guaranteed otherwise — line 136 of the same improvement-log already shows a second non-matching variant.
- **Target files:** `ai-resources/.claude/commands/open-items.md` Step 1 (the friction-log T1 cross-match contract bullet).
- **Notes:** /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/working/2026-05-29-resolve-open-items-cross-match-too-literal.md
### 2026-05-29 — /resolve-improvement-log Step 7b conflicts with archive read-deny

- **Status:** applied 2026-06-01 — Option 1 applied via `/fix-repo-issues` execution (id-02). Step 7a/7b rewritten to append-only: archive created via a Bash test-then-create guard, entries appended via Bash heredoc to the file's end, no archive read/merge/sort/rewrite. Added a load-bearing "do not optimize back to read-merge-sort" note citing the deny rule. No settings change, no /risk-check (same-command logic edit). **Separate follow-up still open:** the `improve.md` / `improvement-analyst` archive de-dup scan hits the same deny and is NOT fixed here.
- **Verified:** 2026-06-01 — confirmed by /qc-pass on the rewritten Step 7.
- **Category:** session-issue
- **Source:** /resolve-repo-problem 2026-05-29
- **Friction source:** `/resolve-improvement-log` Step 7b prescribes a merge-and-sort archive path that reads existing archive entries and rewrites the archive in chronological order. `ai-resources/.claude/settings.json` line 32 denies `Read(logs/*archive*.md)`, blocking that read. Today's invocation worked by accident — the newest entry naturally appended to the end — but the skill silently breaks the moment an earlier-dated entry needs archiving. The same deny affects `improve.md` + `improvement-analyst` archive de-dup scans.
- **Proposal:** Apply Option 1 (recommended by /resolve-repo-problem subagent). Rework Step 7b in `.claude/commands/resolve-improvement-log.md` to chronological-append-only — drop the "read archive + merge + sort + rewrite" simpler-implementation language; specify append-to-end as the canonical path. The active log's outgoing order IS the canonical chronological order; archive-time IS the entry order. No settings change, no /risk-check gate (same-command logic edit). The `improvement-analyst` archive de-dup scan remains broken and should be logged as a separate follow-up.
- **Target files:** `ai-resources/.claude/commands/resolve-improvement-log.md` Step 7b (rewrite the archive procedure).
- **Notes:** /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/working/2026-05-29-resolve-the-resolve-improvement-log-skill-cannot-complete.md
### 2026-06-01 — Workspace-root wrap-session lacks Step 6.4 (outcome check) + Step 6.5 (feedback collection)
- **Status:** applied 2026-06-01 — ported via `/fix-repo-issues` execution (id-01). Canonical Step 6.4 → workspace-root Step 4.4; Step 6.5 → Step 4.5; added a single-toggle Step 0.4 preflight (feedback + outcome bundle only — telemetry/coaching bundle N/A at workspace root), the `### Outcome` + `### Risky actions` lines in the Step 2 schema, and friction-log/improvement-log staging in Step 5. Both canonical MIRROR NOTEs updated to record the port. Divergences resolved: collector agent resolves via --add-dir'd ai-resources; `logs/improvement-log.md` created on first collector write.
- **Verified:** 2026-06-01 — confirmed by /qc-pass on the ported file (cross-file consistency check).
- **Category:** session-feedback
- **Friction source:** session-feedback-collector + outcome-check builds 2026-06-01 — deferred workspace-root mirror (structural divergence: no preflight, `### Files Changed` schema vs split Created/Modified, different step numbers)
- **Proposal:** Port BOTH new optional passes into the independent non-symlink workspace-root `/.claude/commands/wrap-session.md`: (a) the preflight feedback toggle + `### Risky actions` line + Step 6.5 feedback collection; (b) the preflight outcome-check toggle + Step 6.4 outcome check (which would supersede the workspace-root's lighter self-authored `### Session Report` in its Step 2b). Adapt to the copy's leaner structure (no preflight block today; `### Files Changed` schema). Keep the toggles + steps + risky-actions/outcome blocks in sync with canonical as a two-end contract (a `PAIRED CONTRACT` marker already governs the foreign-session guard between the two copies). Until done, both features are canonical-only — they auto-propagate to the ~16 project symlinks but not to workspace-level wraps.
- **Target files:** `/.claude/commands/wrap-session.md` (workspace root), `ai-resources/.claude/commands/wrap-session.md` (sync source)
### 2026-06-02 — wrap-session Step 3.5 foreign-write guard REMNANT false-positive on date rollover (own marker goes stale across midnight)
- **Status:** applied 2026-06-04
- **Verified:** Step 3.5 now computes YESTERDAY and accepts an own-marker dated either TODAY or YESTERDAY (grace window), recording MARKER_DATE. Own-header/own-mandate counts match on ${MARKER_DATE} (awk var renamed today→own_date), and the EXTRA-bucket subtractor is placed in the prior bucket when MARKER_DATE != TODAY — so an overnight session's own prior-day header+mandate yields FOREIGN=0 (clean proceed) instead of a REMNANT false-positive, while genuine concurrent-foreign detection is preserved (own subtraction only moves buckets, never disappears). Mirrored to the workspace-root paired sibling `/.claude/commands/wrap-session.md` per PAIRED CONTRACT. Fix-plan 2026-06-04-1823 item id-14; applied in-session (operator "proceed here"). QC: /qc-pass run post-edit.
- **Category:** guardrail-candidate
- **Severity:** low
- **Provenance:** wrap-collector (machine-authored) 2026-06-02
- **Friction source:** wrap-collector 2026-06-02 — safety / guardrail-accuracy (S6 wrap, no content lost; operator confirmed ownership and proceeded)
- **Proposal:** Harden the wrap-session Step 3.5 marker-aware own-contribution subtractor against the date-rollover edge case. The S6 session started 2026-06-01 and wrapped after midnight (2026-06-02); the session marker stayed dated 2026-06-01, so the today-dated own-contribution resolution returned "none" and this session's own S6 mandate header was misclassified as a prior-day orphan (REMNANT false-positive). Distinct root cause from the Option 2′ clobber fix (that addressed concurrent-clobber, not date-boundary staleness). Direction: resolve own-contribution against the marker's own date rather than `today`, OR widen the own-header match window to include the marker's date when it differs from the current date by one day. Confirm the fix does not re-open the concurrent-foreign-write detection it must still catch.
- **Target files:** `ai-resources/.claude/commands/wrap-session.md` Step 3.5 (marker-aware subtractor); `/.claude/commands/wrap-session.md` (workspace-root paired sibling per PAIRED CONTRACT); possibly `docs/session-marker.md` (note the date-boundary behavior).
### 2026-06-04 — /prime Step 1a Next-Steps git cross-check scans only cwd + ai-resources, not sibling project repos
- **Status:** applied 2026-06-04
- **Verified:** prime.md Step 1a (L48 rationale block) now carries a "Sibling project-repo extension" paragraph + bash snippet that enumerates git repos one level under `projects/`, skips repos already scanned (cwd, ai-resources) via `--show-toplevel` equality, and merges each repo's `git log --since=<entry-date>` into the same result set feeding the keyword-match pass. Fix-plan 2026-06-04-1823 item id-01; applied in-session (operator "proceed here"). QC: /qc-pass run post-edit.
- **Category:** session-feedback
- **Provenance:** wrap-collector (machine-authored) 2026-06-04
- **Friction source:** wrap-collector 2026-06-04 — friction / command (S4: `/prime` menu surfaced items 2/3 as still-open when they were done-and-committed in the strategic-os sibling repo by concurrent S1/S2; the context-discovery engine caught it at the approval gate, `/prime` did not)
- **Proposal:** Extend the `/prime` Step 1a git cross-check (prime.md L42–48) so the `git log --since=<entry-date>` cross-check also runs against the active project repo whose work the menu items track — not only `$CWD_REPO` + `$AI_RESOURCES`. In S4 the menu items' work landed in `projects/strategic-os/` (a sibling git repo); the cross-check's two-repo scope (cwd + ai-resources) structurally cannot see commits there, so likely-DONE items surface as still-open. The existing line-48 "dual-repo Cluster A blindspot" rationale already names the two-repo limitation but does not extend to a third sibling repo. Direction: when a Next-Steps item names or is scoped to a project under `projects/<name>/`, resolve that project's git root and include it in the `--since` cross-check set; OR derive the candidate repo set from the menu items' scoped file paths rather than a fixed {cwd, ai-resources} pair. Confirm the change does not slow `/prime` materially (one extra `git log` per distinct project repo referenced).
- **Target files:** `ai-resources/.claude/commands/prime.md` Step 1a (L42–48 git cross-check repo set).
### 2026-06-04 — wrap-session Step 3.5 guard clobber false-negative for sessions that wrap without /session-start
- **Status:** applied
- **Verified:** 2026-06-04 — confirmed this session (S8): plan-time /risk-check GO (all 6 dims Low), independent qc-reviewer GO, bash validated by execution across 5 scenarios. Both wrap-session copies edited in lockstep + committed.
- **Resolution:** NO_OWN_MARKER guard added to Step 3.5 in BOTH copies (canonical `ai-resources/.claude/commands/wrap-session.md` + workspace-root `/.claude/commands/wrap-session.md`, lockstep PAIRED CONTRACT). When `CLAUDE_CODE_SESSION_ID` is SET but the per-id file is absent: force `OWN_HEADERS_SUBTRACT=0` / `OWN_MANDATES_SUBTRACT=0`, skip both the shared-marker loud fallback and the PRIME_RAN/.prime-mtime path; loud fallback restricted to var-unset old-CLI. One-line rule documented in `docs/session-marker.md` § Marker resolution. Plan-time `/risk-check` GO (all 6 dims Low) — `audits/risk-checks/2026-06-04-wrap-session-clobber-guard-defect-capture-wiring.md`. Bash validated by execution across 5 scenarios (clobber-incident → claim-zero; normal per-id; old-CLI fallback; id-14 rollover; no-marker legacy) — all pass. Folded with id-14 (same Step 3.5 region) as recommended.
- **Category:** session-issue
- **Source:** /resolve-repo-problem AUTO mode 2026-06-04
- **Friction source:** During an S7 wrap (session ran `/prime` menu → `/fix-repo-issues` → `/wrap-session`, never running `/prime` task-selection or `/session-start`, so it authored NO `## … — Session ${MARKER}` header and wrote NO `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}` file). A concurrent S6 session had clobbered the shared `logs/.session-marker` (→ `2026-06-04 S6`) and the shared `logs/.prime-mtime` (dated today). Expected: the Step 3.5 pre-write guard flags S6's uncommitted today-header + mandate as foreign and STOPs. Actual: the guard's loud-fallback read the clobbered shared marker, counted S6's header as THIS session's own (OWN_HEADERS_SUBTRACT=1), and would have returned FOREIGN=0 — a silent false-negative that would have swept S6's note under the wrap commit. Caught only by ground-truth override (this session authored zero headers). Same false-negative class the Option 2′ per-id keying (SHIPPED 2026-06-01) was meant to close — but Option 2′ only protects sessions that HAVE a per-id marker.
- **Proposal (recommended — Structural; /risk-check change class):** In wrap-session.md Step 3.5 (BOTH ai-resources canonical AND workspace-root paired sibling), add a "no-own-marker" guard ahead of the loud fallback: when `CLAUDE_CODE_SESSION_ID` is SET but `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}` does NOT exist, this session did not run `/prime` task-selection or `/session-start`, so it authored zero tracked headers/mandates — force `OWN_HEADERS_SUBTRACT=0` and `OWN_MANDATES_SUBTRACT=0` and SKIP both the shared-`.session-marker` fallback AND the `PRIME_RAN`/`.prime-mtime` path (both are shared, clobber-vulnerable, and here foreign-owned). Restrict the existing loud fallback-to-shared-marker to the genuine old-CLI case (`CLAUDE_CODE_SESSION_ID` unset). Net effect: a no-own-marker session correctly claims no ownership, so all added today-content is flagged foreign and the guard STOPs as designed. Fold this with the same-block id-14 date-rollover edit (2026-06-04, just applied) since both touch the MARKER-resolution region of Step 3.5 in both copies. Blast radius: all sessions using `/wrap-session` + the ~16 project symlinks via the canonical copy + the non-symlink workspace-root copy — run `/risk-check` before landing.
- **Target files:** `ai-resources/.claude/commands/wrap-session.md` Step 3.5 (MARKER-resolution block); `/.claude/commands/wrap-session.md` (workspace-root paired sibling per PAIRED CONTRACT); possibly a one-line note in `docs/session-marker.md` documenting the no-own-marker → own-subtract=0 rule.
### 2026-05-28 — /session-start Step 0.5 mtime guard misses foreign writes that arrive DURING the Mandate Confirmation wait

- **Status:** logged — superseded by the broader 2026-05-28 entry below ("Concurrent sessions cause TOCTOU races on shared log files"); Phase 1 of the broader entry applied 2026-05-28. The narrow Step 2.5 patch proposed here is no longer the recommended fix; the structural per-session-marker approach replaces it. Retained for trace-history of how the problem was first observed.
- **Category:** session-issue
- **Source:** Surfaced live during axcion-brand-book session 2026-05-28 — operator explicitly attributed the trigger to "I start concurrent sessions."
- **Friction source:** /session-start Step 0.5 runs the concurrent-session mtime guard exactly once, immediately after Step 0's session-notes.md read and before Step 1 reads the mandate. It compares `SESSION_NOTES_MTIME` against `PRIME_MTIME` (or against `NOW - 120s` fallback) at that single instant. Today the guard returned `DELTA=0` (clean) at Step 0.5; Step 1 used the supplied mandate verbatim; Step 2 echoed the Mandate Confirmation block and waited for the operator's `y`. During that wait, the operator started a second /prime session, selected a different menu task (task 3 — backfill `_appendix/rejected_directions.md`), and that concurrent /prime wrote a `**Work (redirected via /prime task selection):**` line to logs/session-notes.md. The operator's `y` then arrived in the original session against the original (specimen) mandate echo — but session-notes.md by then contained the redirect line for the backfill task. The original session would have proceeded to Step 3 and written the specimen mandate alongside the foreign redirect line if the operator had not corrected the path via AskUserQuestion. Reproduction is reliable whenever a foreign session writes to session-notes.md between Step 0.5 (clean) and Step 3 (write).
- **Proposal:** Add a second mtime check at Step 3 immediately before locating today's header for the mandate-line write — call it "Step 2.5 — Re-check mtime guard at write time." Capture `WRITE_TIME_MTIME = stat session-notes.md` and compare to the `SESSION_NOTES_MTIME` value already captured in Step 0.5 (must persist that value into Step 3's scope). If `WRITE_TIME_MTIME > SESSION_NOTES_MTIME`, a foreign write happened during the Mandate Confirmation wait → surface a conflict block before applying the operator's confirmation: re-read the last ~20 lines of session-notes.md, show the diff to the operator, offer (1) proceed with the originally-confirmed mandate, (2) re-echo a new Mandate Confirmation reflecting whatever the foreign write redirected to, (3) stop. Default on no-response = (3) stop. Edits confined to `ai-resources/.claude/commands/session-start.md` (new Step 2.5 between Step 2 and Step 3). **/risk-check change class:** YES (canonical-command body edit; /session-start runs at every session start) — run /risk-check as advisory gate before landing. Secondary consideration: today's Step 0.5 tuning notes call out the 120s threshold and freshness window but do not anticipate the during-Step-2 window. Update the tuning-notes block in Step 0.5 to forward-reference the new Step 2.5 so operators reading Step 0.5 understand its coverage limit.
- **Target files:** `ai-resources/.claude/commands/session-start.md` (insert Step 2.5 between Step 2 and Step 3; update Step 0.5 tuning notes to forward-reference Step 2.5). Also worth considering: a one-line companion note in `ai-resources/.claude/commands/prime.md` Step 8a.3 reminding that the marker-write at Step 8a.3.a does not protect /session-start's Step 2 confirmation window.
- **Verified:** 2026-05-29 — confirmed by operator at S5 wrap. The narrow Step 2.5 patch is superseded by the broader TOCTOU Phase 2+3 atomic per-session-marker fix (shipped 2026-05-29 commit `9f91b2f`). Entry retained for trace-history; archive-eligible at next /resolve-improvement-log run.
### 2026-05-29 — /wrap-session Step 3.5 marker-aware guard false-negatives on concurrent /prime clobber

- **Status:** Option 1 VALIDATED-REJECTED 2026-06-01 (S2). Option 2-as-worded (env var) PROVEN INFEASIBLE 2026-06-01 (S3); redesigned as **Option 2′** (CLAUDE_CODE_SESSION_ID-keyed marker) — **SHIPPED 2026-06-01 (S5)** — commits `5e2afdc` (ai-resources) + `2e95f7f` (workspace-root), all 9 consumers migrated atomically, QC GO, system-owner concur. Two subsequent hardenings: date-rollover grace window (id-14) + no-own-marker guard (2026-06-04). **Strategic engineer-vs-process decision: CLOSED 2026-06-05 — technical route ratified, item closed** (decisions.md 2026-06-05). **Archive-eligible** — full trace history retained in "Option 1 validation result" + "Option 2′ design result" below; distinct open follow-up (date-rollover REMNANT hardening, ~line 249) is a separate item.
- **Category:** session-issue
- **Source:** /resolve-repo-problem 2026-05-29
- **Friction source:** S8 /wrap-session Step 3.5 pre-write guard returned `FOREIGN=0` despite visible foreign content in WT (concurrent S9 session's `## 2026-05-29 — Session S9` header + `/cleanup-worktree` work line). Root cause: S9's /prime overwrote `logs/.session-marker` from `2026-05-29 S8` → `2026-05-29 S9`; the marker-aware detector trusted the clobbered file as session-identity oracle, counted the lone S9 header as "own contribution" (OWN_HEADERS_SUBTRACT=1), zeroing the foreign delta. Detector did what was specified; its source-of-truth was poisoned. PRIME_RAN binary fallback inherits the same race (`.prime-mtime` is also shared mutable). The 2026-05-28 friction entry assumed Phase 2 marker-scoped headers closed this gap — it didn't; Phase 2 fixed the grep target, not the marker-resolution step. Recurrence cadence: 3–4× in 5 days for the broader concurrent-session-staging class (today produced two simultaneous incident signals — this marker-clobber bug AND a fresh recurrence of the S7-absorbed-S8-mandate TOCTOU staging bug).
- **Proposal:** Recommended quick patch (Option 1 from triage notes) — add ~15-line clobber-suspicion sanity check to wrap-session.md Step 3.5: if WT contains marker-bearing today-headers under markers OTHER than the one read from `.session-marker`, emit a WARN and fall back to PRIME_RAN binary path (which would have caught today's incident). No /risk-check needed; forward-compatible with the structural fix. Structural fix (Option 2) — promote parked TOCTOU Phase 2-4 work to a scheduled Friday-act wave: session-scoped marker via env var `CLAUDE_SESSION_MARKER` set at /prime + per-marker file `.session-marker-${MARKER}` + append-only `.session-marker` history. Closes the root cause cleanly and bundles the bookkeeping-attribution TOCTOU bug (S7 absorbed S8 mandate) since both share the same shared-mutable-session-state root-cause class. Needs /risk-check + dedicated session; blast radius covers all 9 consumers in `docs/session-marker.md`.
- **Target files (when executed):** Option 1 — `ai-resources/.claude/commands/wrap-session.md` Step 3.5 (add clobber-suspicion sanity check + fallback); possibly mirror to workspace-root `.claude/commands/wrap-session.md` per Step 3.5 PAIRED CONTRACT comment. Option 2 — `ai-resources/.claude/commands/prime.md` Step 8a/8b/8c.3 (.session-marker writers), `ai-resources/.claude/commands/wrap-session.md` Step 3.5 (detector), `ai-resources/.claude/commands/session-start.md` Step 0.5 (mtime guard), `ai-resources/docs/session-marker.md` (canonical contract), workspace-root `.claude/commands/wrap-session.md` (Phase 3 copy), all 9 consumers per the marker contract.
- **Notes:** `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/working/2026-05-29-resolve-wrap-session-step-3-5-guard-false-negative-marker.md`
- **Option 1 validation result (2026-06-01, S2 — implemented, dry-run-tested, REVERTED):** Option 1 was implemented in both wrap-session.md files (plan-time `/risk-check` PROCEED-WITH-CAUTION + system-owner concur), then **rejected at the mitigation-2 dry-run** before commit. A temp-git-repo fixture replaying the exact incident (HEAD has S8 committed; `.session-marker` clobbered to S9; foreign S9 header+mandate uncommitted in WT) returned **`CLOBBER=0, FOREIGN=0` — the silent false-negative persisted.** Root cause of the rejection: **there is no clean file-only signal.** In the real incident `.session-marker` reads the FOREIGN session's marker (S9), so the foreign content is the one *matching* `MARKER` while this session's own header (S8) is the "other" — and it is already committed. Restricting the check to *uncommitted* other-markers (to avoid false-positives) therefore excludes the committed S8 and never fires. The broader signal (any other today-marker in WT, committed or not) DOES fire on the incident but also fires on **every** second-or-later same-day session (S1 committed + S2 wrapping) — a constant false-positive that turns the guard into a rubber-stamp (AP-4). The clobber case and the benign same-day-sequential case are **structurally identical in the files** (marker points to the uncommitted header in both; only *whose* it is differs — unknowable once the marker oracle itself is clobbered). pgrep-at-wrap (the DR-8 hook's signal) is also unreliable here: the foreign session may have exited by wrap time. **Conclusion:** the reactive file-only patch cannot solve this; the marker-as-oracle design must be replaced. **Escalate to Option 2** (session-scoped marker via a per-process `CLAUDE_SESSION_MARKER` env var set at `/prime`, immune to file clobber) as a dedicated `/risk-check`-gated session. Gating risk-check report for the rejected Option 1: `audits/risk-checks/2026-06-01-wrap-session-step35-clobber-suspicion-sanity-check.md`.
- **Option 2′ design result (2026-06-01, S3 — designed, risk-checked, GO-eligible; implementation deferred):** Option 2 **as the improvement-log worded it — "session-scoped marker via env var `CLAUDE_SESSION_MARKER` set at `/prime`" — is INFEASIBLE.** Probed this session: exported env vars do NOT persist across Bash tool calls (each tool call is a fresh shell; the Bash-tool contract states this explicitly). A var `/prime` exports is gone before `/session-start`, let alone `/wrap-session` — the same look-right-do-nothing failure mode that killed Option 1. **But the harness already injects a clean oracle:** `CLAUDE_CODE_SESSION_ID` (probed stable across separate Bash calls, unique per session, not a file → un-clobberable). **Redesigned as Option 2′:** key the marker by session id — `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}` containing `{DATE} S{N}`. A concurrent `/prime` writes a different-id file → no clobber; `/wrap-session` Step 3.5 reads its own per-id file → the silent false-negative cannot occur. Loud fallback to the shared file when the var is absent (OP-3). Full spec + completion criteria (mitigations M1–M3 + system-owner refinements folded in): `audits/option2-marker-redesign-2026-06-01.md`. Plan-time `/risk-check`: **PROCEED-WITH-CAUTION** (Blast radius High, Hidden coupling High, Reversibility Medium) — `audits/risk-checks/2026-06-01-option2-marker-redesign-claude-session-id-keyed.md`. System-owner second opinion: concurs with verdict + mitigations; core design sound; **explicitly advised landing as a GO-eligible spec and implementing in a fresh dedicated session, NOT in the S3 multi-wave session** (`projects/axcion-ai-system-owner/output/consultations/consult-2026-06-01-option2-marker-redesign-risk-check-second-opinion.md`). **Status: SHIPPED 2026-06-01 (S5)** — ai-resources commit `5e2afdc` + workspace-root paired sibling `2e95f7f` (lockstep). Edit manifest written first (`audits/working/option2-marker-edit-manifest-2026-06-01.md`); `CLAUDE_CODE_SESSION_ID` re-probed live + stable (`bd44a70e…`) before building; bash validated by execution across 5 scenarios (primary path, clobber-survival, loud fallback, both-absent, orphan-prune); independent qc-reviewer **GO**. All 6 completion criteria met: per-id oracle + retained shared counter, per-id-first resolution w/ loud fallback, orphan-prune at /prime, gitignore `logs/.session-marker-*`, harness-var dependency doc'd (OP-11 re-verify). session-start Step 0.5 confirmed OUT of scope (reads `.prime-mtime`, not `.session-marker`).

### 2026-06-01 — improve.md / improvement-analyst archive de-dup scan hits Read(logs/*archive*.md) deny
- **Status:** applied 2026-06-05 — dropped archive read entirely (Option 1). `improve.md` no longer passes the archive path; `improvement-analyst.md` updated to remove archive-file input, archive-dup recurrence check, and line-131 "or archive" clause. Active log is now the sole de-dup source. Trade-off: re-proposing a root cause from an archived entry is low risk (the fix is already in the code) and preferable to a deny violation.
- **Verified:** 2026-06-05 — edits confirmed in `improve.md` line 17 removed + de-dup note updated; `improvement-analyst.md` lines 15–19, 53–56, 131 updated.
- **Category:** session-feedback
- **Provenance:** wrap-collector (machine-authored) 2026-06-01
- **Friction source:** wrap-collector 2026-06-01 — leanness/process (latent defect surfaced in S4 /fix-repo-issues id-02, explicitly flagged NOT fixed and Next-Steps item 3 asks for a separate backlog entry)
- **Target files:** `ai-resources/.claude/agents/improvement-analyst.md`, `ai-resources/.claude/commands/improve.md`

### 2026-05-29 — Pre-spec consumer-inventory grep checklist (SO advisory, TOCTOU Phase 2+3 rollout)

- **Status:** applied 2026-06-05 (S10) — commit `afad146`. The precursor of id-40; its placement question (SKILL.md vs new `docs/spec-authoring-checklist.md`) is resolved to SKILL.md. Folded into the single Consumer-Inventory Gate shipped to `skills/ai-resource-builder/SKILL.md`. Independent QC GO.
- **Category:** Audit-recurrence prevention / spec-authoring discipline
- **Source:** System-owner Function-B advisory on Round 2 risk-check of TOCTOU Phase 2+3 atomic spec — `audits/risk-checks/2026-05-29-plan-time-gate-for-toctou-mitigation-atomic-phase-2-3.md § Architectural Commentary § Position § Process observation. Same observation parked at `logs/maintenance-observations.md` § "2026-05-29 — Pre-spec consumer-inventory checklist (TOCTOU Phase 2+3 rollout)"; promoted here so it surfaces in `/friday-checkup` Step 6 (rather than only on quarterly maintenance-observations sweep).
- **Friction source:** TOCTOU Phase 2+3 spec authoring under-counted affected files by 4 in Round 1 (Phase 2-only spec) and another 4 in Round 2 (atomic spec). Same defect class — under-counted consumers of a renamed/removed path — repeated across rounds. Both rounds returned PROCEED-WITH-CAUTION; reviewer correctly caught both misses, but each round cost a full risk-check invocation + SO advisory + operator decision-loop turn. The pattern generalises: spec authors enumerate obvious consumers (commands explicitly named in the source-authority proposal) and miss orphan consumers (docs tables, scaffolding lists, narrative references in unrelated docs, operator-facing chat strings inside command bodies).
- **Proposal:** Add a pre-spec grep checklist to skill docs — likely `ai-resources/skills/ai-resource-builder/SKILL.md` (the resource-creation skill that hosts /create-skill, /improve-skill, /migrate-skill) and/or a new `ai-resources/docs/spec-authoring-checklist.md` referenced from `audit-discipline.md`. Checklist: "Before writing a structural-change spec that renames or removes a path, run `grep -rn '<old-path>' .claude/ docs/ skills/ workflows/ templates/ CLAUDE.md` and enumerate every consumer in the spec's affected-file list. The grep is mechanical; the inventory miss is consistent across spec authors." Mechanical inventory closes the loop BEFORE risk-check, preserving the PROCEED-WITH-CAUTION verdict for genuinely structural concerns.
- **Risk if left undone:** Recursive PROCEED-WITH-CAUTION verdicts continue as an inefficient signal class — every rename/removal spec re-spawns the inventory-miss cost. Two rounds observed this session is a clear pattern.
- **Target files:** `ai-resources/skills/ai-resource-builder/SKILL.md` (add pre-spec checklist subsection) OR `ai-resources/docs/spec-authoring-checklist.md` (new, referenced from `audit-discipline.md`) — placement decision deferred to the implementation session.
- **Triage cadence:** Friday weekly cadence — small implementation footprint, high recurrence frequency on structural-change sessions.

### 2026-06-02 — /fix-symlinks structurally blind to "regular-file-where-symlink-expected" drift class
- **Status:** applied 2026-06-05 (S7, `e18fd29`) — closed via the **non-destructive variant** (≈Option B), NOT the destructive Option A. S7 added `/fix-symlinks` Step 2b: a detection pass over each project's `.claude/shared-manifest.json` that flags managed shared files which are regular-file copies where a relative symlink is expected, and reports a manual `ln -sf` remedy. Auto-restore was **deliberately omitted** ("these never auto-fix — a regular file may have diverged from canonical; overwriting with a symlink could destroy local changes; always diff first"), so the `/risk-check`-class destructive mutate path was never built and no `/risk-check` gate was needed for what shipped. The 2026-06-02 gap (the command silently passing over the regular-file-drift class) is closed. Option A's destructive symlink-restore remains intentionally unbuilt — reopen only if auto-restore is ever actually wanted (it would then need its own `/risk-check`). _Status corrected 2026-06-05 by the repo-maintenance problem sweep (`audits/2026-06-05-repo-maintenance-problem-sweep.md` §4) — the entry had remained `logged (pending) — DEFER` after the work shipped._
  - _Historical DEFER note (superseded above, retained for audit trail):_ Friday-act 2026-06-05 (repo-hygiene #3) triage verdict was DEFER — it classified the change as a `/risk-check` class (Option A's destructive restore path) and held it for a dedicated `/improve-skill` + `/risk-check` session. S7 instead closed the gap via the safe non-destructive path, making the DEFER moot.
- **Category:** session-issue
- **Source:** /resolve-repo-problem 2026-06-02
- **Friction source:** /fix-symlinks reported "0 broken, nothing to do" this session while the SessionStart hook (`auto-sync-shared.sh`) had already flagged `note.md` / `session-plan.md` as regular files that should be symlinks. The command scans only `find -type l` + unresolvable targets, so the result was accurate for broken symlinks but silently passed over the regular-file-copy drift class — a different failure mode the command's "nothing to do" output does not disclose. A /tweak was attempted; the scope gate correctly fired (new capability, changes documented scope claim, multi-section) and routed to /improve-skill.
- **Proposal (recommended — Option A, Structural):** Extend `/fix-symlinks` to also detect managed shared files that are regular-file copies where a relative symlink to canonical is expected, and offer to restore the symlink topology (repo-architecture.md §125–138). Three drift modes exist and currently split badly: (1) broken symlink — owned by fix-symlinks; (2) regular-file-where-symlink-expected AND content-divergent — detected by `auto-sync-shared.sh`, but its remediation (`/sync-workflow`) compares against the workflow-TEMPLATE tree and copies bytes rather than restoring symlinks; (3) byte-identical regular-file copy — detected by NOBODY (the hook's `diff -q` guard suppresses it). The two detectors do not actually overlap (different source trees, non-equivalent fixes), so extending fix-symlinks fills a real gap rather than duplicating. **Guardrail:** content-divergent regular files must default to manual-resolution with a diff shown, never silently overwritten by a symlink. This change is a /risk-check change class (canonical command, auto-symlinks to every project, adds a destructive mutate path) — run /risk-check before landing, and execute via /improve-skill (where /tweak correctly routed it). Cheaper fallback (Option B): leave fix-symlinks as-is, fix the hook message + add a scope disclaimer naming the manual `ln -sf` remedy.
- **Target files:** `ai-resources/.claude/commands/fix-symlinks.md` (Option A); `ai-resources/.claude/hooks/auto-sync-shared.sh` and `ai-resources/.claude/commands/sync-workflow.md` (role clarification + cross-ref, either option).
- **Notes:** /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/research-pe-regime-shift-advisory-gap/audits/working/2026-06-02-resolve-fix-symlinks-blind-to-regular-file-drift-class.md

### 2026-06-05 — Pre-spec consumer-inventory grep checklist (id-40)
- **Status:** applied 2026-06-05 (S10) — commit `afad146`. Shipped the **Consumer-Inventory Gate** into `skills/ai-resource-builder/SKILL.md` (new `## Consumer-Inventory Gate (rename/remove specs)` section + a pointer in the Improve Workflow Step 2 breaking-change detection). The gate folds this entry's base grep, the S7 strengthening (invariant-stem + grep↔registry reconciliation), and the 2026-05-29 precursor into one rename/remove-scoped gate. Independent QC GO.
- **Category:** rule
- **Source:** vault W2.4 finding #4 — triaged S6 2026-06-05
- **Friction source:** This cycle's wrap-marker edits (both `wrap-session.md` copies + `docs/session-marker.md` + ~16 symlinks) were multi-consumer structural edits where affected-file consumer inventory was under-counted. Recurrence: seen 1 time before (improvement-log 2026-05-29 pre-spec inventory entry, still pending).
- **Proposal:** Add a mandatory consumer-inventory step to `skills/ai-resource-builder/SKILL.md` (or equivalent canonical location): before any rename/remove spec is written, run `grep -rn '<old-path>' .claude/ docs/ skills/ workflows/ templates/ CLAUDE.md` and enumerate every consumer in the affected-file list. Prevents renamed/moved resources from silently breaking downstream commands that still reference the old path.
- **Target files:** `ai-resources/skills/ai-resource-builder/SKILL.md` — add checklist gate to the "spec writing" or "plan" phase of the skill's improvement sequence.

### 2026-06-05 — Date-qualify session-plan filename (id-41)
- **Status:** applied 2026-06-05 (S7) — superseded the S6 defer. The defer's stated precondition ("its own /risk-check + a dedicated session") was met: S7 ran /risk-check (PROCEED-WITH-CAUTION, report `audits/risk-checks/2026-06-05-stage-4-session-harness-date-qualify-rename-prime-md.md`) + a system-owner /consult, then applied every item in the consumer inventory below. Commits: `fa2b3f2` (writers + readers + hook + doc refs), `35fb409` (wrap-session.md reader + registry + heavy-read doc — the 3 consumers the risk-check inventory initially missed but THIS entry's inventory had flagged), `b32d611` (workspace-root wrap-session.md paired copy), research-pe `6ba632a` (project-local backup-session-plan.sh). The wrap-session.md exact-path reader (L321, both copies) named in the inventory below was the live catch — confirmed silent-break risk, fixed via glob.
- **Severity:** MED
- **Category:** session-issue / marker-contract
- **Source:** friday-act 2026-06-05 session-harness plan item #3; deferred at S6 execution after a full consumer inventory revealed marker-contract-wide blast radius.
- **Friction source:** `session-plan-S{N}.md` omits the date, so a same-marker-number cross-day collision overwrites the prior day's plan file (S6 today silently overwrote the 2026-06-04 S6 plan — hit live at this session's `/session-plan` Step 0, where the 3-option prompt surfaced a stale 2026-06-04 intent). The Step 0 prompt catches it (no data lost this session), so the live harm is LOW; the value is removing a recurring confusing-prompt + the latent overwrite risk when the prior plan was NOT yet wrapped.
- **Why deferred (large-effort + /risk-check class):** Date-qualifying to `session-plan-{YYYY-MM-DD}-S{N}.md` is NOT a 3-file change (the friday-act plan under-scoped it as `docs/session-marker.md` + `prime.md` + `session-plan.md`). It is a marker-contract-wide change with **silent failure modes** — the exact-path readers below tolerate plan-absence (no error), so a missed reader degrades to a silent "plan not found", not a loud break. Requires its own `/risk-check` (automation with shared-state effects) + a dedicated session.
- **Consumer inventory (grep `session-plan-` across `.claude/ docs/ skills/ workflows/ templates/`, S6 2026-06-05 — the reusable artifact this entry exists to preserve):**
  - **Glob consumers (`logs/session-plan-*.md`) — SAFE, the `*` still matches a date-prefixed name; no edit needed:** `open-items.md` (L41/68/83), `decide.md` (L29), `fix-repo-issues-scanner.md` (L40/84/93/195).
  - **Exact-path readers (construct `logs/session-plan-${MARKER}.md`) — WOULD BREAK silently; each must compute the date too:** `contract-check.md` (L52, Step 2b), `drift-check.md` (L18/28/48, Steps 3/6/8), `wrap-session.md` (L321) **AND its workspace-root paired copy** `/.claude/commands/wrap-session.md` (PAIRED CONTRACT — lockstep required).
  - **Writers (emit the path) — must change format:** `prime.md` (Steps 8a.3.c / 8b.3.c / 8c.8 plan write + chat strings L210/212/256/429), `session-plan.md` (Step 0 existence check L17/19/22/26 + Step 7 OUTPUT_TARGET L159/160 + L7 header).
  - **Hook regex — WOULD BREAK silently:** `.claude/hooks/backup-session-plan.sh` L20 regex `(-[a-zA-Z0-9]+){0,2}\.md$` caps at 2 marker segments; a date-prefixed `session-plan-2026-06-05-S6.md` has 4 hyphen-segments → fails to match → silently stops backing up plans. Regex must widen to accommodate the date. (Also check for project-local copies of this hook — header says "nordic-pe project-local".)
  - **Doc references (narrative, no runtime behavior; update for consistency):** `docs/session-marker.md` (L112/113 file-naming table + L143 narrative), `docs/repo-architecture.md` (L221/222), `docs/compaction-protocol.md` (L21), `docs/heavy-read-discipline.md` (L45), `new-project.md` (L544), `docs/weekly-cadence.md` (narrative).
- **Proposal:** In a dedicated session: (1) decide the canonical format (`session-plan-{YYYY-MM-DD}-S{N}.md`); (2) update `docs/session-marker.md` File-naming table as the single source of truth FIRST; (3) update all writers to emit `${DATE}-${MARKER}`; (4) update all exact-path readers to construct `${DATE}-${MARKER}` (they already gate on "modified today" so TODAY is available); (5) widen the backup-hook regex + sweep for project-local hook copies; (6) keep the glob consumers unchanged (verify the `*` still matches); (7) update doc references. Run `/risk-check` before landing (marker-contract blast radius, silent-failure class). Consider a one-release transition where readers try the date-prefixed name then fall back to the bare-marker name, to tolerate plans written by the old format mid-migration.
- **Target files:** see consumer inventory above (≈10 runtime files + 1 hook + ~6 docs; both `wrap-session.md` copies in lockstep). **All applied — see Status.**
- **Lesson (cross-ref friction-log 2026-06-05 S7):** THIS entry's hand-built consumer inventory was more complete than both the friday-act plan (3 files) and the S7 /risk-check reviewer inventory (14 consumers, missed `wrap-session.md`). The inventory was preserved precisely so the eventual implementer would not re-miss consumers — and it worked: reading id-41 mid-implementation is what surfaced the `wrap-session.md` gap. Reinforces id-40 (pre-spec consumer-inventory checklist): grep the INVARIANT stem (`session-plan`), not the templated form (`session-plan-${MARKER}`), since placeholder spelling varies across files.

### 2026-06-05 — Concurrent-session guards blind to foreign writes on shared command/doc files
- **Status:** Option A APPLIED 2026-06-05 (S8) — both steps shipped. Step 2 (read-only shared-dir advisory): `/prime` Step 1a now runs `git status --short -- .claude/commands docs` when `SIBLING_COUNT > 1` and names foreign-dirty shared files as a Step 6 exception line; `/session-start` Step 0.5 does the same when `FOREIGN_WRITE=1`. Step 1 (doc note) satisfied via the anti-pattern callout + worktree remedy in `docs/parallel-sessions-playbook.md` § 4 and the enriched `detect-concurrent-session.sh` warning (/risk-check GO, `audits/risk-checks/2026-06-05-hook-warning-worktree-enrichment.md`). Option B (PreToolUse hook) stays parked behind a recurrence signal as the entry notes. Part of the Option C+A subset of `audits/2026-06-05-concurrent-session-collision-diagnostics-fix.md`.
- **Category:** session-issue
- **Source:** /resolve-repo-problem 2026-06-05
- **Friction source:** During S6 (friday-act implementation), a concurrent session implemented the date-qualify change (item 5: contract-check.md, session-plan.md, docs/session-marker.md, prime.md) in the ai-resources working tree. No guard fired to warn S6. Investigation confirmed: every concurrent-session guard targets `logs/session-notes.md` only (session-start Step 0.5 mtime; wrap Step 3.5 header/mandate counts); `detect-concurrent-session.sh` warns a session exists but names no files and fires only at session open; `auto-sync-shared.sh` does not run inside ai-resources. Foreign edits to `.claude/commands/*.md` and `docs/*.md` are invisible to every guard until they surface as unexplained working-tree modifications. Genuine collision file this incident: `prime.md` (S6 committed it; foreign session then re-edited it — lost-update risk). The other three foreign files were disjoint from S6's edits (benign).
- **Proposal (recommended — Option A, two-step):** (1) Zero-risk doc note in `docs/session-marker.md` (or `docs/commit-discipline.md`) stating the guards are session-notes-scoped and shared command/doc files rely on git-merge + explicit-path staging, NOT on guard protection. (2) Materiality-justified increment: extend `/prime` (or `/session-start` Step 0.5) so that WHEN a concurrent session is already detected, it runs a read-only `git status` over `.claude/commands` + `docs` and names any foreign-dirty shared files in the brief. The advisory is a command-body edit → light /risk-check (autonomy rule #9); the doc note is not. Option B (PreToolUse Edit/Write hook warning before editing a foreign-dirty shared file) is the root-cause fix but HIGH blast radius (hot-path hook, false positives, AP-10 lock anti-pattern) — parked behind a recurrence signal (reopen if a same-file lost-update is confirmed or the class recurs 2+ times).
- **Target files:** `ai-resources/docs/session-marker.md` (doc note); `ai-resources/.claude/commands/prime.md` or `session-start.md` (advisory enumeration, Option A step 2); Option B (deferred): a new PreToolUse hook + `.claude/settings.json` wiring.
- **Notes:** /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/working/2026-06-05-resolve-concurrent-session-guard-blind-to-foreign-shared-file-writes.md

### 2026-06-05 — NO_OWN_MARKER guard mis-attributes a session with partial marker setup (shared written, per-id not) as zero-own-contribution
- **Status:** APPLIED 2026-06-05 (S9). Both halves now shipped. Writer-side (S8): `docs/session-marker.md` carries the **both-or-neither writer invariant** (BLOCKING). Reader-side (S9): added a **clobber-safe own-marker recovery** to the `wrap-session.md` Step 3.5 NO_OWN_MARKER branch in BOTH paired copies (logic byte-identical) — before claiming zero own-contribution it recovers the marker from the shared `logs/.session-marker` ONLY when no per-id file (any session) claims that `{date} S{N}` AND a matching header exists, so a partial-setup own session is attributed correctly while a foreign clobber stays `FOREIGN≥1` (a clobber leaves a per-id file claiming the marker → recovery suppressed). Fires ONLY in invariant-violation space (a compliant session resolves its per-id marker above and never reaches the branch); on any non-match it falls through to the conservative zero-claim unchanged. Cleared `/risk-check` (GO, all six dimensions Low — `audits/risk-checks/2026-06-05-reader-side-no-own-marker-clobber-safe-recovery-wrap-session.md`), a 7-scenario dry-run harness (partial-setup own / foreign-clobber-suppressed / header-less / overnight-rollover / stale-date / empty-glob / per-id-claim-suppresses — all pass), and an independent `/qc-pass` (REVISE → applied: corrected the safety-claim wording below). Reader-side note added to `docs/session-marker.md`. **Safety (precise, post-QC):** removes the own-partial-setup false-positive and adds no false-negative PROVIDED foreign sessions honour the both-or-neither invariant. **Residual false-negative (accepted, narrower than the FP removed):** a SINGLE non-compliant FOREIGN partial-setup session (shared + header `S{N}`, no per-id) concurrent with a genuine no-own-marker reader (e.g. `/fix-repo-issues`) — needs only one invariant-violator, not two; not eliminable reader-side; closed at the writer layer by the both-or-neither invariant. The recovery trades the pre-existing fail-safe (loud false-positive) for a fail-unsafe guess in that case, justified only because the trigger is itself an invariant violation. Was the Option C+A reader-side deferral of `audits/2026-06-05-concurrent-session-collision-diagnostics-fix.md`.
- **Category:** guardrail-candidate
- **Severity:** med
- **Provenance:** wrap-collector (machine-authored) 2026-06-05
- **Friction source:** wrap-collector 2026-06-05 — safety/guardrail-gap (S6 § Risky actions). At S6 wrap the Step 3.5 guard WOULD HAVE false-positived via the NO_OWN_MARKER rule: S6 had genuine own contribution (authored its S6 header + committed work) but its per-id marker file `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}` was never written — S6's marker setup wrote only the shared `logs/.session-marker`. The NO_OWN_MARKER guard (`wrap-session.md` L106–108) treats "per-id marker absent" as "session authored no tracked headers ⇒ claim zero own-contribution" (L153–158), which is correct for a genuinely-no-own-marker session but WRONG for a session whose marker setup was partial. Result would have been a false FOREIGN claim (the guard wrongly attributing S6's own header to a foreign session). Caught by manual FOREIGN=0 verification before proceeding — near-miss, no harm. Root cause is distinct from id-34 (the DETECTION fix that introduced NO_OWN_MARKER for the genuine case), the "Step 3.5 CONCURRENT block strands a no-own-marker session" remediation-ergonomics entry, and the sibling concurrent-guard-blindness entry above (which is about guards not SEEING foreign shared-file writes). This is about marker-setup COMPLETENESS driving guard attribution: the canonical `prime.md` writer (L187/237/301) always writes shared + per-id together, so a shared-only marker means a setup path diverged from the canonical writer and the guard's binary cannot tell partial-setup from no-contribution.
- **Proposal:** Make the NO_OWN_MARKER attribution robust to partial marker setup — e.g., before claiming zero-own-contribution, cross-check whether this session in fact authored a tracked today-header (grep session-notes for a header whose marker matches the shared `.session-marker` value, or whose own committed work exists), so a shared-only marker does not silently flip a contributing session to FOREIGN. Alternatively/additionally, harden the writer side so any path that writes the shared marker also writes the per-id marker (enforce the both-or-neither invariant the canonical `prime.md` writer already follows), so a shared-only state can never arise. Run /risk-check before landing (touches both `wrap-session.md` copies — PAIRED CONTRACT).
- **Target files:** `ai-resources/.claude/commands/wrap-session.md` Step 3.5 (NO_OWN_MARKER branch L106–158); `/.claude/commands/wrap-session.md` (workspace-root paired sibling, lockstep); whichever marker-setup path wrote shared-only this session (audit `prime.md` / `session-start.md` marker writers + any alternate entry path); possibly `docs/session-marker.md` (document the both-or-neither invariant).

### 2026-06-05 — Strengthen id-40 consumer-inventory: invariant-stem grep + grep↔registry reconciliation (S7)
- **Status:** applied 2026-06-05 (S10) — commit `afad146`, folded into the id-40 Consumer-Inventory Gate (same SKILL.md section). Both mandates landed: (1) grep the INVARIANT filename stem, never the templated/placeholder form; (2) reconcile the grep result against the contract registry (e.g. `docs/session-marker.md`) both directions before the spec is written. Independent QC GO.
- **Category:** session-feedback
- **Provenance:** wrap-collector (machine-authored) 2026-06-05
- **Friction source:** wrap-collector 2026-06-05 — friction/process (S7 § Outcome + friction-log "Consumer-inventory under-count, recurrence 3rd+"). On the date-qualify rename, three independent passes each missed the same exact-path consumer `wrap-session.md` (both paired copies): (1) the friday-act plan (3 files), (2) the /risk-check reviewer's 14-consumer inventory, (3) S7's own implementation grep. Caught only by reading id-41's hand-built inventory mid-implementation; otherwise `fa2b3f2` would have shipped a half-finished rename (filename changed, reader still on old path → silent "plan not found"). Two compounding mechanical root causes the existing id-40 / 2026-05-29 entries do NOT close: (a) grepping the *templated* form (`session-plan-${MARKER}`) misses files spelling the placeholder differently (`{MARKER}`, `{marker}`, `$MARKER`) — `wrap-session.md` writes the literal-brace form and was skipped; (b) the authoritative `docs/session-marker.md` § two-end-contract registry was itself stale (omitted `wrap-session.md`; misfiled the load-bearing `backup-session-plan.sh` hook regex under "narrative, not consumers"), so the one source-of-truth that should have caught all three passes was incomplete. S7 already applied the structural fixes for THIS contract (added `wrap-session.md` + a new "Runtime non-command consumers" registry class; switched exact-path readers to glob). This entry routes the GENERALIZABLE rule that id-40 lacks.
- **Proposal:** Strengthen the id-40 pre-spec consumer-inventory checklist with two mandates it currently lacks: (1) grep the INVARIANT filename stem (e.g. `session-plan`), never the templated/placeholder form, so placeholder-spelling variance cannot hide a consumer; (2) reconcile the grep result against `docs/session-marker.md` § two-end-contract registry (or the relevant contract registry) BEFORE the rename spec is written — add any consumer the grep found but the registry lacked, and vice-versa, and re-classify any load-bearing runtime parser misfiled as "narrative". Folds into id-40's target (`skills/ai-resource-builder/SKILL.md`) rather than a new surface.
- **Target files:** `ai-resources/skills/ai-resource-builder/SKILL.md` (extend the id-40 checklist gate with the invariant-stem + registry-reconciliation rules); cross-ref id-40 and the 2026-05-29 pre-spec entry at disposition.

### 2026-06-05 — /diagnostics-plan (ai-resources): 7-item Do-now batch reconciled — 1 applied, 6 dissolved
- **Status:** applied 2026-06-05 — commit `f0959f7` (ai-resources CLAUDE.md de-dup pass: id-25 + id-26). Risk-check PROCEED-WITH-CAUTION + SO Function-B concur; independent QC GO.
- **Source:** /diagnostics-plan ai-resources (SO advisory `consult-2026-06-05-diagnostics-plan-ai-resources.md`; scan `audits/working/diagnostics-scan-2026-06-05-1545-ai-resources.md`; risk-check `audits/risk-checks/2026-06-05-diagnostics-plan-7-item-do-now-batch-perms-claude-md-push-symlinks.md`).
- **Finding (record so future scans don't re-surface):** the SO-vetted 7-item Do-now list, built from dated diagnostic reports, dissolved to ONE clean in-scope fix on pre-execution reconciliation. The reports were not reconciled against live repo state + canonical design decisions made since. Per-item disposition:
  - **id-01/02/03, id-04, id-18 — ALREADY APPLIED (no-op).** settings.local.json already has `defaultMode: bypassPermissions` (bare allows live in tracked settings.json); /new-project Step 5b already gated (new-project.md:648); /wrap-session already has pre-push fetch+divergence (wrap-session.md:451). Applied by S2/S3/S4 friday-act earlier today; the scan read the stale friday-checkup/pipeline-review reports, not the live files.
  - **id-06 — SKIP (canonical conflict).** "Add `Read(audits/working/**)` deny" is directly contradicted by `docs/permission-template.md:141`: the deny was *retired 2026-04-28* with an explicit "Do not restore it" (it blocks /permission-sweep Step 4's required main-session reads of auditor summaries + the Subagent Contract read-back). The token-audit finding (id-06) did not account for this. **Do not re-propose.**
  - **id-22 — DEFER (load-bearing deny; design decision, not mechanical).** The `Read(logs/*archive*.md)` deny is NOT stale — `resolve-improvement-log.md:103` deliberately builds its append-only archive procedure around it ("Do not optimize back to read-merge-sort — it will hit the deny rule"); `improve.md:21` documents an accepted analyst workaround. Narrowing it is a 3-canonical-file contract change (improve.md, resolve-improvement-log.md, permission-template.md) with a guard-removal tradeoff. friday-checkup's own alternative was "reroute via Bash/grep," not narrow the deny. Park for a dedicated decision if the analyst archive de-dup is judged worth the guard change.
  - **id-37 — DEFER (out of ai-resources scope).** Target is `workflows/research-workflow/.claude/commands/{qc-pass,refinement-pass,update-claude-md}.md` (byte-copies that should be symlinks) — not in the ai-resources scope, and part of the already-deferred cross-repo `.claude/` symlink-hygiene cluster (id-14/id-29). Route to a research-workflow session.
  - **id-10/11/12/25/26 — PARTIALLY APPLIED.** id-25 (Git Rules push-dup → pointer) + id-26 (R-id codes → plain language) applied. id-10 (Commit Rules) + id-11 (Model Selection) deliberately KEPT — DR-5 self-labeled context-less-open mirror + the no-model-field rule kept verbatim/visible (trimming its always-loaded statement to save ~80 tok is a bad trade per the operator's standing model-field concern). id-12 (Session Boundaries) left — already a one-line pointer, nothing material to trim.
- **Process note:** the value of this run was the pre-execution reconciliation (risk-check consumer-inventory + DR-9 top-3 + SO second opinion caught 6 of 7 items as already-done/conflict/out-of-scope before any edit). Candidate diagnostics scans built from dated reports should be reconciled against live file state before being treated as actionable — a standing /diagnostics-plan caveat.

### 2026-06-05 — /fix-project-issues (ai-resources, 2nd run today): 23-candidate scan reconciled — 1 applied, 1 deferred, 2 no-op/skip
- **Status:** applied 2026-06-05 — committed `5274551` (ai-resources CLAUDE.md id-08 stale-clause deletion). Risk-check GO (all six dimensions Low; `audits/risk-checks/2026-06-05-fix-project-issues-id08-claude-md-stale-clause-deletion.md`).
- **Verified:** 2026-06-05 — committed `5274551`, risk-check GO; id-08 stale clause confirmed gone from live CLAUDE.md (Maintenance Cadence /pipeline-review bullet).
- **Source:** /fix-project-issues ai-resources. Scan: `audits/working/diagnostics-scan-2026-06-05-1603-ai-resources.md`. SO advisory: `projects/axcion-ai-system-owner/output/consultations/consult-2026-06-05-fix-project-issues-ai-resources.md`.
- **Context:** fresh re-run requested by operator after the morning /diagnostics-plan run. SO vetting collapsed 23 candidates → 3 Do-now / 3 Skip / 17 Defer; live-state verification then collapsed the Do-now set further to ONE clean applied item.
- **Per-item disposition (record so future scans don't re-surface):**
  - **id-08 — APPLIED.** Deleted the stale dated change-log clause "Subsumed `/audit-critical-resources` on 2026-05-29 (its currency-check folded into the auditor's Brokenness section);" from CLAUDE.md Maintenance Cadence /pipeline-review bullet (line 53). Retained the live "Distinct from `/friday-checkup` (housekeeping)." disambiguation. DR-5 deletion test; no rule changed, no consumer parses the clause (risk-check: 7 refs to audit-critical-resources elsewhere, 0 must-change).
  - **id-09 — NO-OP (already fixed).** "R14 / R1–R13 unresolvable identifiers" — no R-code identifiers remain in live CLAUDE.md; already fixed by f0959f7 (morning id-26 plain-language pass). The claude-md-audit was dated 2026-06-01, pre-f0959f7. Do not re-surface.
  - **id-07 — SKIP (already applied).** "Git Rules push clause duplicates Commit Rules" = the morning id-25 fix; live CLAUDE.md:70 is the pointer form (f0959f7). Null edit.
  - **id-05 — SKIP (already addressed).** "System Owner grounding files missing" addressed by S11 grounding fix (e1a60d6/7a0ddd4); grounding files present and read this invocation.
  - **id-12 — SKIP (canonical conflict).** "Granular dated-report denies missing" collides with `permission-template.md:141` retired deny ("Do not restore it"). Mirrors the morning id-06 SKIP. Do not re-propose.
  - **id-06 — DEFER (always-loaded-visibility tradeoff + new-doc infra).** Relocating the ~330-tok "What This Repo Contains" directory map out of always-loaded CLAUDE.md saves per-turn tokens but removes orientation content and requires creating a docs/ destination. The lead paragraph (repo purpose) + multi-tool design rule are load-bearing per-turn; only the directory listing is reference content. Mirrors the morning id-11 KEEP-on-visibility call. Park for a dedicated decision; SO put it in Do-now but the keep-vs-move editorial call on always-loaded content warrants deliberate handling, not a same-session diagnostics patch.
  - **id-01/02/03/04/13/18/22/23 — DEFER (scope mismatch).** Workspace-root / other-project / cross-repo findings; route to a workspace or that-project session (out of single-scope ai-resources mandate).
  - **id-10/11/14/15/16/17/19/20/21 — DEFER (in-scope but structural).** Earn a dedicated session per CLAUDE.md § Structural-fix-as-default. SO names id-14/15 (write-path integrity) + id-16 (classifier extraction) as genuinely worth doing — park, not dismiss; surface at the next dedicated ai-resources structural session.
- **Process note:** second consecutive /fix-project-issues run on ai-resources to collapse a large candidate list to ~1 clean fix on live-state reconciliation. The dated-report-vs-live-state lag flagged in the morning run (b18d212) recurred — candidate scans built from dated audits over-report when same-day work has already moved the files. Standing caveat holds.
### 2026-06-05 — Read of improvement-log.md mid-rewrite by a concurrent session returned a transient truncated state (near-miss: a blind append would have destroyed ~23 entries) (S7)
- **Status:** applied 2026-06-05 (S13) — pre-append integrity guard (Constraint E) added to session-feedback-collector + /improve; hazard documented in docs/commit-discipline.md § Shared-log write-path integrity. Risk-check GO (id-14 leg); /qc-pass GO. Commit pending.
- **Verified:** 2026-06-05 — confirmed via commit 2bc89d9 (batch: shared-log write-path integrity S13) + S13 session QC observations
- **Category:** guardrail-candidate
- **Severity:** med
- **Provenance:** wrap-collector (machine-authored) 2026-06-05
- **Friction source:** wrap-collector 2026-06-05 — safety/guardrail-gap (S7 § Risky actions, near-miss #1). S7 read `logs/improvement-log.md` while a concurrent session was mid-rewrite of the same file and caught a transient 17-line state (the file normally carries ~24 entries). S7 stopped and re-verified rather than appending; had it appended to that transient state, ~23 entries would have been destroyed. No harm occurred (caught by the operator-modeled "re-verify before write" discipline), but the gap is real: `improvement-log.md` is a shared append target with no concurrency guard, and a Read landing inside another session's non-atomic rewrite window returns a partial file that looks complete. This is distinct from the session-notes-scoped guards and from the sibling "guards blind to foreign command/doc writes" entry — here the shared file IS a guard-relevant log, and the failure mode is read-during-rewrite returning a silently truncated state that a downstream Write-of-full-content would persist as a mass deletion. The wrap-collector itself appends to this same file, so the gap sits on the feedback loop's own write path.
- **Proposal:** Add a cheap pre-append integrity check for shared-log writers (wrap-collector, /improve, any command that does Read-then-Write-full-content on `improvement-log.md` / `friction-log.md`): before persisting a full-file rewrite, assert the read-back entry count (or byte size) is not drastically lower than a recent committed baseline (e.g. `git show HEAD:logs/improvement-log.md | grep -c '^### '`), and STOP-loud if the working copy is sharply smaller than HEAD — the signature of a read-during-rewrite truncation. Prefer minimal append-only edits over full-file rewrites on these shared logs where the tool allows. Run /risk-check before landing if the check is wired into command bodies.
- **Target files:** (to be determined at disposition) — candidates: `ai-resources/.claude/agents/session-feedback-collector` body + `ai-resources/.claude/commands/improve.md` (pre-append integrity assertion); possibly `docs/commit-discipline.md` (document the shared-log read-during-rewrite hazard + the minimal-edit-over-full-rewrite preference).

### 2026-06-05 — Concurrent-detected shared-dir advisory scan omits logs/; live foreign edits to non-append improvement-log.md are invisible to the session brief

- **Status:** applied 2026-06-05 (S13) — FOREIGN_SHARED scan in /prime Step 1a + /session-start Step 0.5 extended (lockstep) to logs/improvement-log.md, improvement-log-archive.md, decisions.md; session-notes.md kept excluded. Risk-check GO (id-15 leg); /qc-pass GO. Commit pending.
- **Verified:** 2026-06-05 — confirmed via commit 2bc89d9 (batch: shared-log write-path integrity S13) + S13 session QC observations
- **Category:** session-issue
- **Source:** /resolve-repo-problem AUTO mode 2026-06-05
- **Friction source:** During S10, flipping three status entries in `logs/improvement-log.md` collided with a live concurrent session (S9) actively editing that same file (in-place PARTIAL→APPLIED flip + entry archiving) plus both `wrap-session.md` copies (9 Claude processes observed; the append of THIS very entry was blocked once by the harness modified-since-read guard mid-write). The `/prime` Step 1a and `/session-start` Step 0.5 concurrent-detected shared-dir advisory ran, but it scans only `git status --short -- .claude/commands docs` — `logs/` is excluded — so the live foreign edit to the non-append `improvement-log.md` never surfaced in the brief; it was discovered only on a write attempt. `session-notes.md` is safe in `logs/` (append-only, marker-disambiguated), but `improvement-log.md` is non-append (in-place status flips + entry archiving) and is the prime lost-update surface there.
- **Proposal:** Extend the read-only shared-dir advisory scan in BOTH `/prime` Step 1a and `/session-start` Step 0.5 to also cover the non-append shared logs under `logs/` — minimally `logs/improvement-log.md` (reasonably also `logs/improvement-log-archive.md` and `logs/decisions.md`) — so a live foreign in-place edit to a non-append shared log is named in the brief as a lost-update risk, exactly as the command/doc advisory already does. Keep append-only `logs/session-notes.md` OUT of scope (the marker model already protects it; including it would false-positive on every concurrent session). Paired command-body edit (`prime.md` + `session-start.md` in lockstep) → `/risk-check` change class (autonomy rule #9, light). Distinct from the existing "guards blind to shared command/doc files" entry (`.claude/commands` + `docs` only, APPLIED S8) and from the "improvement-log.md read-during-rewrite append-discipline" entry (reading mid-rewrite, not advisory scan scope).
- **Target files:** `ai-resources/.claude/commands/prime.md` (Step 1a `FOREIGN_SHARED` scan), `ai-resources/.claude/commands/session-start.md` (Step 0.5 `FOREIGN_SHARED` scan) — lockstep.

### 2026-06-05 — Diagnostics-lag (candidates from dated reports, not reconciled against live state) — RESOLVED at command layer; scanner-layer direction superseded
- **Status:** applied (resolution recorded — the fix shipped same-day in commit `23c9143`; this entry closes the loop so the pattern is not re-proposed by a future `/improve` or Friday-act run)
- **Verified:** 2026-06-05 — confirmed via commit 23c9143 (new: backlog-reconciliation primitive) + S14 session verification
- **Category:** process / instruction-fix
- **Friction source:** `logs/friction-log.md` § 2026-06-05 (/fix-project-issues, 2nd run) — "Diagnostics scan over-reports … recurrence 2nd of the same class TODAY" (morning `/diagnostics-plan` run `b18d212` + 2nd `/fix-project-issues` run). Surfaced by `/improve` (S14, 2026-06-05).
- **What changed (already shipped — recorded here, not built by this entry):** Root cause = diagnostics candidates built from *dated* audit reports without diffing against live on-disk state, so already-resolved items re-enter System-Owner vetting on a fast-moving same-day repo. Fixed at the **command-orchestration layer** in commit `23c9143`: `docs/backlog-reconciliation.md` (canonical reconcile-at-read primitive; cites the exact 2026-06-05 collapse) + `fix-project-issues.md` **Step 2.5** (git-log `--since` reconciliation; passes only `STILL_OPEN` to the SO; `LIKELY_DONE` demoted to a verify-only appendix) + `fix-repo-issues.md` **Step 3.0** (same primitive). `/prime` Step 1a and `/open-items` are the other wired consumers.
- **Scanner-layer direction SUPERSEDED:** the friction entry's "Direction" proposed adding the reconciliation pass *inside the `diagnostics-scanner` agent*. Wrong layer — the scanner is read-only (`tools:` = Read/Glob/Grep — no Bash/git), so it cannot run the `git log --since` reconciliation. The fix correctly landed one layer up, in the command body (which has git). Do **not** build a scanner-layer pass.
- **Morning entry-point gap CLOSED:** the morning `/diagnostics-plan` path (flagged as possibly un-gated) was renamed to `/fix-project-issues` (commit `0c97a1b`); `diagnostics-plan.md` no longer exists. Same gated command — Step 2.5 covers it.
- **Verified live (S14):** all three files committed & clean in `23c9143`; `diagnostics-plan.md` absent; no prior improvement-log entry on this root cause (no duplicate).
- **Target files:** none (resolution record only). Source-of-truth: `docs/backlog-reconciliation.md`; `.claude/commands/fix-project-issues.md` Step 2.5; `.claude/commands/fix-repo-issues.md` Step 3.0.

### 2026-06-05 — /fix-repo-issues plan (S15): prime N-auto parser + clarify markerless-nudge applied; wrap-session chained-task false-positive diagnosed already-resolved
- **Status:** applied 2026-06-05 (S15)
- **Verified:** 2026-06-05 — three command-layer items of fix-plan `audits/fix-plans/fix-repo-issues-2026-06-05-1918.md` dispositioned; risk-check PROCEED-WITH-CAUTION + SO second-opinion (split-by-item) on file; QC self-verified (independent qc-reviewer subagent blocked by 1M-context credit exhaustion — see Notes).
- **Category:** command/skill — harness
- **What changed:**
  - **id-02 (marketing) — APPLIED.** `prime.md` Step 7: new first classifier branch matches `^[1-6]\s+auto$` and re-classifies `N auto` (e.g. `2 auto`) as `auto N` → Step 8c, fixing the silent skip of auto-mode ceremony. Companion edit to Step 8c.1 so PICKED_ITEMS resolution also recognizes the `N auto` shape (self-QC caught that Step-7 routing alone left 8c.1 unable to parse the literal reply — auto-mode would have stalled at item resolution).
  - **id-09 (research-pe) — APPLIED (reshaped).** `clarify.md` new Step 0: detect-and-NUDGE-only marker check (emits a one-line "run /prime" nudge when a today-marker is absent and `logs/session-notes.md` exists; creates nothing). Reshaped from the plan's "add marker-trio init" because `docs/session-marker.md` makes `/prime` the SINGLE marker creator; a second creator would violate the single-source contract and the `/prime`-anchored reader logic. Closes the `/clarify` entry leg; the direct-work-command entry leg (candidate b) stays open.
  - **id-05 — NO EDIT (already-resolved).** The `wrap-session.md` Step 3.5 chained-task false-positive is already prevented by the marker-aware own-subtraction path (lines 168–197) that landed with id-31 Phase 2 — the very resolution the 2026-05-28 14:20 friction entry predicted. Diagnosis-first per SO second opinion; editing a High load-bearing detector on a stale premise would have risked branch regression + workspace-root pair drift. Plan's "10:05" friction reference was a misattribution (correct entry is 14:20). Distinct from the still-open "wrap-lite no-own-marker CONCURRENT" remediation.
- **Notes:** Independent QC via `qc-reviewer` subagent failed three times (opus/sonnet/haiku overrides) with "Usage credits required for 1M context" — the harness forces 1M context onto subagents and this session's 1M credits were exhausted by the earlier risk-check + system-owner subagents. Fell back to inline self-QC against the stated scope (caught + fixed the 8c.1 gap). Flagged for the wrap. id-01 of the same plan (3 improvement-log `**Verified:**` fields) applied separately as log hygiene.
- **Target files:** `.claude/commands/prime.md` (Steps 7 + 8c.1), `.claude/commands/clarify.md` (new Step 0); friction annotations in ai-resources + marketing-positioning + research-pe friction-logs.

### 2026-06-05 — QC-independence gate unreachable when 1M-context credits exhausted at session level (S15)
- **Status:** applied 2026-06-05 (S17) — doc-only fallback posture added to `docs/qc-independence.md` § QC Independence Rule ("Subagent-unavailable fallback (1M-credit exhaustion)" bullet): names inline self-QC as the sanctioned degraded substitute, mandates surfacing the degradation in chat, marks self-QC-only artifacts provisionally-cleared, and points to proactive `/model` switch as prevention. The pre-dispatch credit-detection **hook** (Proposal direction a) is PARKED — novel harness-level infra, deferred to a dedicated design session per the triage verdict (2026-06-05 S17).
- **Verified:** 2026-06-05 — confirmed live in `docs/qc-independence.md` (subagent-unavailable fallback bullet, QC Independence Rule section).
- **Category:** guardrail-candidate
- **Severity:** low
- **Provenance:** wrap-collector (machine-authored) 2026-06-05
- **Friction source:** wrap-collector 2026-06-05 — safety/guardrail-gap (S15 § Risky actions + § Notes). The `qc-reviewer` subagent failed on three consecutive model-override attempts (opus/sonnet/haiku) with "Usage credits required for 1M context." Root cause: the harness forces 1M context onto all subagents; when a 1M-context session's credits are exhausted by earlier subagent calls (risk-check + system-owner in this session), no further subagent can be spawned regardless of per-dispatch model overrides. The QC-independence gate is silently unreachable for the rest of the session with no warning at dispatch time. Self-QC was used as a fallback and caught a real gap (8c.1 parser); no harm resulted. The guardrail gap: any subagent-dependent gate (QC-independence, outcome-check, feedback-collector) can silently block in a 1M-credit-exhausted session without surfacing a session-level credit warning before dispatch. Distinct from research-pe id-05 (per-dispatch model override for /run-report; different root cause — session-model inheritance, not credit exhaustion).
- **Proposal:** Add a pre-dispatch credit-check or model-context advisory to the harness session start (or to the qc-reviewer invocation in commands that call it): when the session is running on a 1M-context model and earlier subagent calls have been made, warn that remaining 1M credits may be insufficient for further subagent dispatch, and suggest switching to a standard-context model via `/model` before proceeding. The check need not be exact — a heuristic ("1M session + 2+ prior subagent calls → warn") would surface the risk before the gate is exercised rather than after it fails. Alternatively, document the 1M-credit-exhaustion failure mode in the harness session-start checklist so operators proactively switch model context before subagent-heavy sessions.
- **Target files:** (to be determined at disposition) — candidates: `ai-resources/.claude/commands/session-start.md` (checklist note or pre-subagent advisory); possibly `ai-resources/docs/agent-tier-table.md` (note 1M-credit consumption risk for high-tier subagent chains); possibly the `qc-reviewer` invocation wrapper in commands that call it.


## Archived 2026-06-12 (S8) — via /resolve-improvement-log

### 2026-05-28 — extract change-shape classifier to shared reference (eliminate two-end contract)

- **Status:** no-op / already-resolved (closed 2026-06-05 S13) — superseded by the 2026-05-29 refactor: the classifier was extracted to `docs/change-shape-classifier.md` and both `/consult` (Step 2, L48/52) and the `project-manager` agent (Phase 3, L63/65) already read it as a one-end contract with provenance lines. This entry (dated 2026-05-28) predates its own fix by one day. Surfaced again by the S13 diagnostics scan (id-16); risk-check RECONSIDER flagged that re-homing into repo-architecture.md would re-introduce two-source drift (DR-7/AP-7) — DROPPED. No code change. 3rd "diagnostics scan surfaced an already-resolved item" instance on 2026-06-05.
- **Category:** Audit-recurrence prevention / architectural deduplication
- **Source:** project-manager agent + /pm command landing 2026-05-28 — System Owner Function-B advisory (risk-check second opinion) named this as the architecturally correct v1.1+ state. The change-shape classifier list (Files / Commands / Agents / Models / Folder structure / Hooks / Workflows / Project boundaries / Permissions) is currently duplicated verbatim in `ai-resources/.claude/commands/consult.md` Step 2 and `ai-resources/.claude/agents/project-manager.md` Phase 3 (under `structure (change-shaped)`). The duplication is a **two-end contract per `risk-topology.md § 5`** — silent drift between the two copies causes routing inconsistency between `/consult` and `/pm`.
- **Mitigation in place at v1:** both files carry an explicit two-end-contract comment naming the sibling file. Comments decay per `principles.md § QS-7` — this is a stopgap, not the architectural fix.
- **Proposal:** extract the change-shape classifier to a shared reference doc that both commands `Read` at runtime. Candidate location: `ai-resources/docs/repo-architecture.md` § new "Change-shape classifier" subsection (it's a routing concept and lives naturally alongside repo-architecture's Q5 classifier reference). Both `consult.md` Step 2 and `project-manager.md` Phase 3 then become "read this section; apply the list" instead of carrying verbatim copies. Per `principles.md § DR-7` — generalize on confirmed second-consumer evidence (PM is the second consumer; the trigger is met).
- **Target files (when executed):** `ai-resources/docs/repo-architecture.md` (add classifier subsection); `ai-resources/.claude/commands/consult.md` (replace verbatim list with Read-and-apply reference); `ai-resources/.claude/agents/project-manager.md` (same).
- **Triage cadence:** next Friday `/friday-act` wave.

### 2026-06-04 — pattern to watch: same-day multi-session unwrapped-notes accumulation triggers wrap foreign-guard (3rd+ occurrence class)
- **Status:** logged (pending)
- **Category:** session-feedback
- **Provenance:** wrap-collector (machine-authored) 2026-06-04
- **Friction source:** wrap-collector 2026-06-04 — friction / process (S4 wrap fired the foreign-session pre-write guard CONCURRENT/FOREIGN=3 because S1/S2/S3 ran same-day and never wrapped, so their notes sat uncommitted in the working tree; resolved via operator-approved option-2 multi-session recovery commit, no content lost — the guard WORKED, this is an operational-pattern note not a guard defect)
- **Proposal:** Watch-only this round — do NOT build. The guard fired correctly and the recovery path (conscious union commit) is the designed behavior, so there is no guardrail gap to close. The signal is the operational pattern: running 4 same-day sessions (S1–S4) where the earlier three never run `/wrap-session` forces the last session to absorb a multi-session recovery commit at wrap. If this accumulation recurs (the recovery-commit-at-wrap path is hit 2+ more times), consider whether an unwrapped-session reminder or a lightweight per-session auto-commit-of-own-notes path would reduce the manual recovery burden. Cross-ref the existing TOCTOU/marker entries (Option 2′ SHIPPED 2026-06-01; date-rollover REMNANT 2026-06-02) — those address marker clobber + date-boundary detection accuracy, NOT the unwrapped-accumulation operational pattern, so this is a distinct watch-item, not a duplicate.
- **Target files:** (to be determined at disposition) — likely `ai-resources/.claude/commands/wrap-session.md` Step 3.5 / `prime.md` if escalated; none this round.
- **S5 follow-up (2026-06-04):** First recurrence of the accumulation pattern — S5 had to absorb the deferred S1/S2 *deliverable* commits (not just notes). Investigation this session found the accumulation is broader than session-notes: a workspace-wide `git status` survey showed **~16 repos** carrying uncommitted drift — mass deletions (`critical-resource-auditor.md`, `audit-critical-resources.md`, `route-change.md`), dozens of untracked `.claude/` command + agent library files, and `.session-marker` churn — most of it predating today's sessions. The S2 Session-Boundaries consolidation (16-repo conversion) was only the visible tip. S5 committed the ai-resources + strategic-os subsets cleanly (explicit paths, foreign drift untouched: commits `24eb6d8`, `2facf99`); the other 14 project-repo CLAUDE.md conversions remain uncommitted and entangled with the broader library drift. **Disposition recommendation:** this is no longer "watch-only" — the workspace-wide `.claude/` library drift across 16 repos is a standing git-hygiene debt that deserves a dedicated session (likely a `git status` sweep + per-repo explicit-path commit pass, or a decision on whether the per-project `.claude/` dirs should be committed at all vs. gitignored/symlinked from canonical). Not the unwrapped-notes guard problem — a separate, larger structural question about how project `.claude/` libraries are synced and tracked. **Target files:** none (diagnosis); a future session decides the `.claude/` sync/tracking model.

### 2026-06-08 — #23 deferred half: strengthen the Source-Diversity triangulation rule (chassis edit + /risk-check) (RESOLVED)
- **Status:** RESOLVED 2026-06-08 S2 — landed **Option A (project-local)**: strengthened the cross-class collapse clause in `projects/research-pe-regime-shift-advisory-gap/reference/quality-standards.md § Source-Diversity Matrix`, gated by a `/risk-check` re-fire (PROCEED-WITH-CAUTION; report `ai-resources/audits/risk-checks/2026-06-08-strengthen-the-canonical-triangulation-packets-rule-in-the.md`) + system-owner second opinion (both concurred Option A). Two lockstep split-aware SKILL updates landed (`research-extract-creator` consumption-status note; `cluster-memo-refiner` Check 9 cross-class illustration). Back-validation surface verified EMPTY (zero delivered 1.1 extracts carry the formal `Independence basis:` field — it post-dates extract authoring). Canonical-template promotion deferred → see the Option B entry below.
- **Status (original):** deferred — chassis edit deserves clean context + a /risk-check re-fire
- **Category:** research-workflow / claim-permission-chassis
- **Source:** fix-spec #23 §3.3; PM ruling 2026-06-08 S1 (verdict: this is a chassis edit, gate it); the orthogonal `independence basis` field already landed in research-extract-creator (commit pending this session).
- **Why deferred:** The independence DATA (the `independence basis` field) is now recorded per claim. The RULE that consumes it — extending the triangulation-packets rule in `reference/quality-standards.md § Source-Diversity Matrix` from "same source CLASS → one role" to "same UNDERLYING SOURCE/DATASET across different classes → one role" — is a CHASSIS edit (the triangulation-packets rule is marked `(canonical)` in both quality-standards.md:137 and claim-permission.template.md:35). Per the canonical-ordering rule (quality-standards.md:116, template:5) it requires a `/risk-check` re-fire. Not safe to land at the tail of a deep multi-subagent session.
- **Proposal:** Dedicated session: design the same-underlying-source collapse rule for `quality-standards.md § Source-Diversity Matrix`; `/risk-check` it (chassis edit, broad blast radius — both claim-permission-gate and cluster-memo-refiner Check 9 read this section); land it; then add a back-validation/migration pass over any extracts already produced with `independence basis` values (the field's interim values are unvalidated until the gate activates — noted in the skill). `/qc-pass` + commit.
- **Review-cycle:** monthly

### 2026-06-08 — Fix-spec §3.3 false-topology defect + standing unconsumed claim-permission.md gap (RESOLVED)
- **Status:** RESOLVED 2026-06-08 S3 — proposal (b) DONE: the `claim-permission.md` architecture decision is settled as **pointer-removal** (do NOT instantiate the file). All five dangling pointers in `reference/quality-standards.md` (the boundary bullet, the Canonical-ordering blockquote, § Minimum Evidence Thresholds, § Source-Diversity Matrix, § R1-Defect Fold-Ins) were reconciled to truthful "not instantiated; inline rule shape is the operative authority" statements, and a residue note was added recording WHY (S1 ruling, reaffirmed S3) so a future session does not re-propose instantiate-and-rewire from scratch. Decision basis: plan-time `/risk-check` PROCEED-WITH-CAUTION + system-owner second opinion, both recommending pointer-removal over instantiate-and-rewire (instantiate-and-rewire = speculative abstraction for a zero-live-reader consumer + silent reversal of the S1 PM ruling). Enforcer-parsed region (four class names, verb lists, six § headings, triangulation rule shape) verified byte-identical; `/qc-pass` APPROVE. Risk-check report: `ai-resources/audits/risk-checks/2026-06-08-s3-auto-mode-plan-time-gate-fix-spec-4-design-batch.md`.
- **Status (prior):** PARTIALLY RESOLVED 2026-06-08 S2 — proposal (a) DONE: fix-spec §3.3 corrected in place (loud `CORRECTED 2026-06-08 S2` banner superseding the false "instantiate claim-permission.md / don't edit quality-standards.md" guidance; now documents the actual Option-A landing). **Bonus finding this session:** fix-spec §3.2 (#3) carried the SAME class of staleness — it assumed the local-language precondition needed creating, but it already existed as `research-prompt-creator § S-04`; only a `stage-instructions.md:43` deferred-list reconciliation remained (also DONE S2, which additionally corrected the co-listed "non-substitution rule" stale entry → landed as S-02). **STILL OPEN:** proposal (b) — the architecture decision on whether `claim-permission.md` should ever be instantiated or its dangling chassis pointer (`quality-standards.md:133,137`) removed. The S2 §3.3 edit added a one-line "claim-permission.md unconsumed; this file is the operative authority" clarification at quality-standards.md:135-137, which mitigates the immediate confusion but does NOT resolve the dangling-pointer architecture question.
- **Status (original):** deferred — spec-author correction + a separate wiring decision
- **Category:** fix-spec-correctness / claim-permission-wiring
- **Source:** discovered during #23 implementation (2026-06-08 S1); confirmed by PM ruling.
- **Why deferred:** Two linked defects. (1) **Spec defect:** `audits/2026-06-08-workflow-v2-fix-spec.md` §3.3 instructs "instantiate reference/claim-permission.md, put the independence rule there, do NOT edit quality-standards.md." This is factually wrong against the deployed wiring — the diversity enforcers (claim-permission-gate SKILL.md:49-50/70/191; cluster-memo-refiner Check 9 SKILL.md:228) read the Source-Diversity Matrix from `quality-standards.md`, and `reference/claim-permission.md` does not exist and is unconsumed for the diversity check. Following §3.3 verbatim would enforce nothing. (2) **Standing wiring gap:** `quality-standards.md:133,137` point at `reference/claim-permission.md` for the project-fillable threshold/diversity tables, but that file was never instantiated — a chassis pointer to a non-existent file.
- **Proposal:** (a) Correct fix-spec §3.3 to target `quality-standards.md § Source-Diversity Matrix` (chassis, /risk-check'd) for the rule and keep only the `independence basis` field on research-extract-creator. (b) Separately decide whether `claim-permission.md` should ever be instantiated (the template lists refiner Check 9 / writer / Pass-3 emitter as intended consumers of its THRESHOLD tables — but the deployed refiner reads quality-standards.md, so either instantiate-and-rewire or remove the dangling pointer). Both are spec/architecture decisions, not in-session edits.
- **Review-cycle:** monthly

### 2026-06-08 — cluster-memo-refiner: pre-existing defects surfaced by cold-eval during the #17 Milestone-2 ceiling wiring (RESOLVED 2026-06-09 S3)
- **Status:** RESOLVED 2026-06-09 S3 — all listed items landed in `cluster-memo-refiner/SKILL.md` via `/improve-skill` under the **Option A** framing from the plan-time risk-check second opinion (report `audits/risk-checks/2026-06-09-declare-inputs-change-to-the-canonical-cluster-memo-refiner.md`, PROCEED-WITH-CAUTION; system-owner CONCUR). Resolutions: (1) [was Critical] research extracts declared as an **Optional** input with presence-gate + degraded branch, mirroring the Check-9 research-plan pattern — **NOT Required** (the conditional-Required upgrade is deferred to the **F3** declared-check-count contract, itself behind **F1**; forward-noted in the skill, so the six-check deployment is unaffected). (2) [was Major] `source-conflict-log.md` declared Optional with a three-way absent-file branch (trivial-pass / refinement-blocked-when-conflicts-unlogged / extracts-also-absent). (3) [was Major / C14] Example Output block added (worked finding + permission-table row with a `Binding tier:` Notes value). (4) [Minor] Completion Criterion 11 made conditional on the extracts presence-gate. (5) [Minor] Runtime Recommendations section + C6/C7/C8 frontmatter rulings + C19 progress line added; C3 description trimmed to 1006 chars (triggers + exclusions preserved). Cold-eval + independent post-edit QC clean — no regressions; the one Major the cold-eval raised (C3 over-length) was a measurement false-positive (measured 1006 < 1024). Cold-eval surfaced 3 NEW pre-existing Minors — re-logged below (2026-06-09 S3 entry).
- **Original status:** logged (pending)
- **Category:** skill-input-contract + skill-convention-gate
- **Source:** cold evaluation during the fix-spec #17 Milestone-2 `/improve-skill` pass on `cluster-memo-refiner` (2026-06-08 S9). All pre-existing; **none introduced by the risk-tier ceiling edit** (the ceiling logic was judged internally consistent — monotonic, weakest-link, presence-gated). Scoped OUT of the Milestone-2 edit per scope discipline: the input-contract fixes change the Check 8 / Check 10 input contract (a breaking-change trigger) and need their own `/risk-check` envelope.
- **Proposal:** (1) **[Critical] Research extracts consumed but undeclared as input.** Check 10 sub-check 1 ("read the research extracts feeding the cluster's findings") and Check 8's fill-from-extracts action consume research extracts, but Input Requirements never declares them and there is no presence-gate / degraded mode (unlike every other input). Declare extracts as an input with an absent-mode branch. (2) **[Major] Source-conflict log undeclared input + no absent-file branch.** Check 10 sub-checks 2–3 read `status:` from `source-conflict-log.md`; declare it or state that its absence is itself the blocking condition. (3) **[Major] Output-contract / C14 — no assembled-report specimen or worked example.** Add one Example Output block (a refined finding with claim ID + a rendered permission-table row incl. a `Binding tier:` Notes value). (4) **[Minor] Completion Criterion 11** inherits the extracts hole — make it conditional once (1) lands. (5) **[Minor] C13 Runtime Recommendations**, **C6/C7/C8 frontmatter decisions**, **C19 checklist progress-tracking**, and a residual **C3 description >1024** (now 1335, already improved from 1452 during this edit) — documentation/polish. Route all via `/improve-skill`.
- **Review-cycle:** monthly

### 2026-06-08 — claim-permission-gate: 3 pre-existing Minor convention gaps (cold-eval, #17 Milestone-2 pass) (RESOLVED 2026-06-09 S3)
- **Status:** RESOLVED 2026-06-09 S3 — all 3 Minors landed in `claim-permission-gate/SKILL.md` via `/improve-skill`: (1) C8 `paths` ruling added to Runtime Recommendations ("invoked positionally by /run-sufficiency, not path-triggered"); (2) the `regime_note` in the worked Output Example inlined to the full disconfirmation-regime string (copy-safe, ellipsis removed) with a confirming note; (3) C19 per-cluster progress-tracking line added to Behavior step 3 (partial failure visible before the sentinel decision). Cold-eval returned 0 issues.
- **Original status:** logged (pending)
- **Category:** skill-convention-gate
- **Source:** cold evaluation during the fix-spec #17 Milestone-2 `/improve-skill` pass on `claim-permission-gate` (2026-06-08 S9). All pre-existing; the risk-tier ceiling edit was judged 0-Critical/0-Major, internally consistent. The two cold-eval Minors that WERE introduced by the ceiling edit (un-rowed § Risk-Tier Model input; mixed-partial-tier wording) were fixed in the pass; these three remaining are pre-existing and unrelated.
- **Proposal:** (1) **C8** — `paths` frontmatter neither set nor explicitly ruled out; add a one-line ruling in Runtime Recommendations ("invoked positionally by /run-sufficiency, not path-triggered"). (2) **Output Example** — the `regime_note` frontmatter in the worked Example is truncated with `...`; add an elision note or inline the full string so the example is copy-safe. (3) **C19** — add an optional per-cluster progress-tracking line so a partial failure is visible before the sentinel decision. Route via `/improve-skill`.
- **Review-cycle:** monthly

### 2026-06-09 — /prime Step 0 silently swallows an autostash *pop conflict* (exit 0 misclassified as `updated`) (RESOLVED 2026-06-09 S3)
- **Status:** RESOLVED 2026-06-09 S3 — fixed in `.claude/commands/prime.md` via the exact one-block edit this entry proposed: Step 0 now detects the conflict-pop path FIRST (before the exit-0 cases) via a multi-signal OR (`Applying autostash resulted in conflicts` in output / residual `autostash` in `git stash list` / `UU` in `git status`), classifying it `autostash-conflict`; Step 4 carries that state; Step 6 brief gains a `⚠ Pull: autostash pop conflicted` exception line. Independent `/risk-check` GO (all six dimensions Low; report `audits/risk-checks/2026-06-09-harden-prime-claude-commands-prime-md-step-0-pull-result.md`); independent `/qc-pass` GO (all rubric dimensions Clear). _(Original deferred status below, retained for history.)_
- **Status (historical):** logged (pending)
- **Category:** command-body + silent-degradation
- **Source:** Sandbox test, 2026-06-09 S2 (`audits/working/2026-06-09-S2-prime-autostash-path-test.md`). Closed the "Unverified-at-merge" carryover from the 2026-06-09 `/prime` hardening (Change 1: Step 0 `pull --rebase --autostash`). Two throwaway git repos, exact Step 0 command. **Clean-pop path: PASS** (stash→rebase→pop, exit 0, dirty edit restored — behaves as intended). **Conflict-pop path: data-safe but reveals a gap.**
- **Gap:** when the autostash pop conflicts (uncommitted edit on the same line a remote-ahead commit changed), `git pull --rebase --autostash` prints `Applying autostash resulted in conflicts. Your changes are safe in the stash.` but **returns exit code 0**. Step 0 classifies pull results by exit code, so a conflicting pop is classified `updated`; Step 4 only carries "pull failure or unpushed commits" to the brief — neither applies. Result: `/prime` emits a clean-looking brief while the working tree has conflict markers (`<<<<<<<`) in a tracked file and a stranded `stash@{0}: autostash`. No data lost (recoverable from both the markers and the retained stash), but the conflict is **invisible** — the exact failure class the carryover was watching for, firing silently. The carryover's premise ("no action unless a future `/prime` reports a pop conflict") is wrong: `/prime` would not report it.
- **Proposal (NOT applied — surfaced per S2 mandate; this is a `/prime` command-body change → run `/risk-check`):** Harden Step 0 result classification to detect the conflict path independent of exit code. Cheapest: after the pull, grep the captured output for `Applying autostash resulted in conflicts` (or check for a residual autostash in `git stash list` / a `UU` status), and if present carry a `⚠ Pull: autostash pop conflicted — working tree has conflict markers; stash@{0} preserved` exception into the Step 6 brief. One-block edit to Step 0 + the Step 4 exception-carry.
- **Severity:** low-frequency (needs a same-line dirty-tree collision against a remote-ahead commit), low-cost fix, silent-degradation class.
- **Target files:** `ai-resources/.claude/commands/prime.md` Step 0 + Step 4.
- **Notes:** `audits/working/2026-06-09-S2-prime-autostash-path-test.md`
- **Review-cycle:** monthly


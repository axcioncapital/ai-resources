# Session Plan — 2026-07-18

## Intent
Fix three repo-health problems in operator-set order — (1) close the silent session-marker failure path by making the marker oracle fail loudly when the shared marker names a different session, (2) give `/mission` a sanctioned verb to tick off an open thread, (3) drain `improvement-log.md` then widen the `/prime` Step 3 severity anchor. **Operator constraint: nothing may be committed this session** — all changes stay uncommitted for external review with Codex.

## Model
opus — match (active session is Opus 4.8). The work is design under ambiguity: choosing a guard shape, designing a new command verb, and judging which log entries are genuinely drainable.

## Source Material
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/scripts/run-manifest.sh` (marker oracle, lines 145–173 — `MARKER_SRC` is the hook point)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/wrap-session.md` (Step 12d documents the omit-the-flags path that makes the overwrite reachable; Step 13 marker teardown)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/.claude/commands/wrap-session.md` (workspace-root paired non-symlink copy — PAIRED CONTRACT, lockstep)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/mission.md` (67 lines; Step 1 action parser at :27–31, Step 5 `close` at :57–61)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/prime.md` (Step 3 scan, severity anchor at :206)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/resolve-improvement-log.md` (the drain; load-bearing `[y/n/select]` confirmation at step 4)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/improvement-log.md` (1276 lines — the drain target)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/repo-health-backlog-2026-07.md` (threads 1, 2, 6)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/session-marker.md` (marker contract — `/prime` is the single creator)
- `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/audit-discipline.md` (risk-check change classes)

## Findings / Items to Address

1. **Session-marker gap — silent run-manifest overwrite.** (`improvement-log.md:1257–1268`, filed today by S4-8c3.) `/prime` allocates the marker at Step 8, reached only when a menu item or task is stated. A `/clarify`-first session never allocates. `logs/.session-marker` then still holds the *previous* session's value; `run-manifest.sh:159–169` self-resolves from it and would overwrite that session's manifest with no error. The entry's own recommended direction (`:1267`) is: *"make the wrap path fail loudly when `logs/.session-marker` names a session id that is not this one. The second is cheaper and closes the silent half."* Four consequences observed in one session: wrap halt, near-overwrite of a manifest, two blocked commits from staging-guard footprint inheritance.
2. **Identity is already encoded in the marker name — the check is cheap.** `/prime` Step 8k appends the first 3 alphanumerics of `CLAUDE_CODE_SESSION_ID` as a suffix (`S5-531` ← session `531a0dc4-…`). So a mismatch is detectable by string comparison, with no new state. `run-manifest.sh` already tracks `MARKER_SRC` (`:154`, `:168`), distinguishing per-id resolution (trustworthy by construction) from shared-file fallback (the dangerous path).
3. **Mission thread 6 — `/mission` has no thread-level verb.** (`improvement-log.md:445–456`, entry dated 2026-07-09, hit twice that day.) `mission.md:27` parses exactly `create | list | read | close`; `:28` aborts otherwise. `mission.md:12` states `## Open threads` changes "only via this command — never hand-written from inside a working session." `prime.md:236` builds menu candidates from unchecked threads. The three facts contradict: a finished thread can only be ticked by violating the contract, or left unticked so `/prime` re-offers completed work. The logged proposal is `check <id> <thread-substring>` plus `drop <id> <thread-substring>`, both refusing on ambiguous match, neither committing.
4. **Mission thread 1 — the `/prime` Step 3 scan has re-bloated ~9x.** 223 lines / ~11–12k tokens per session against 25 when the fix shipped 2026-07-13. 29 resolved-class entries unarchived; 30 open entries 30–57 days old; 15 of 28 severity hits sit on already-closed entries.
5. **Mission thread 2 — a third of the backlog is invisible to that same scan.** `prime.md:206` anchors on `^- \*\*Severity:\*\*`. Of 102 live entries, 69 carry it, 2 use an un-dashed variant, 31 have none — 33/102 unreachable. Three entry formats are live, not two. Confirmed in this session's own `/prime`: entry `improvement-log.md:1270` uses an `h2` (`## 2026-07-18 —`) instead of the schema's `### `, and its `**Severity:**` line lacks the leading dash.
6. **Ordering is load-bearing (operator-stated).** Thread 2 alone makes the scan *worse before better* — widening the anchor exposes 33 more entries while 29 stale ones are still present. Drain first (thread 1), then widen (thread 2).
7. **Commit prohibition.** Operator directive mid-session: nothing may be committed until reviewed externally with Codex. This overrides the workspace `Commit behavior` rule for this session only.

## Execution Sequence

1. **`/risk-check` (plan-time gate).** Change set spans four structural classes: canonical command edits (`mission.md`, `prime.md`, `wrap-session.md` ×2), a shared-state automation script (`run-manifest.sh`), and a new command verb. Verify: a report lands under `audits/risk-checks/`. On RECONSIDER, redesign before proceeding; on NO-GO, stop per mandate `Stop if`.
2. **`/blindspot-scan` (post-plan, pre-implementation).** Fires per workspace rule — the plan adds a new command verb and changes runnable automation with shared-state effects. Verify: verdict surfaced inline; resolve PAUSE-AND-FIX findings before step 3.
3. **Fix 1 — marker identity guard in `run-manifest.sh`.** Insert after the resolution loop (`:170`), before the `die` on empty marker. When `MARKER_SRC` is the *shared* file AND `CLAUDE_CODE_SESSION_ID` is set AND the marker carries an id suffix: compare the suffix against the session's first-3-alphanumerics; on mismatch, `die` with a message naming both the resolved marker and this session's expected suffix, and instructing the caller to pass `--marker`/`--date` explicitly. Legacy suffix-less markers cannot be verified → loud stderr warning, proceed (fail-safe: never break an old-CLI session). Per-id-resolved markers skip the check entirely. **Verify by execution:** plant a mismatched shared marker in a sandbox copy, confirm non-zero exit and the loud message; then confirm the normal path (this session, per-id marker present) still exits 0.
4. **Fix 1b — document the guard at both `wrap-session.md` copies.** Step 12d currently documents omitting `--date`/`--marker` as the default. Add a one-line note that the oracle now hard-fails on an identity mismatch and what to do when it fires. Both copies in lockstep per the PAIRED CONTRACT. **Verify:** `diff` the two Step 12d blocks for the added note.
5. **Fix 2 — `/mission check` verb.** Extend the `mission.md:27` action parser to accept `check`, add a new Step for it, and update the `argument-hint` frontmatter and the `:30` usage line. Behavior: locate `<id>.md`, substring-match against unchecked `- [ ]` lines in `## Open threads`, refuse on zero or multiple matches (naming the candidates), flip exactly one `- [ ]` → `- [x]`, no commit (consistent with existing Step 6). **Scope call: implement `check` only, not `drop`** — `check` is what unblocks this mission; `drop` is a second destructive verb with no live demand. Note the deferral in the plan and the log entry. **Verify by execution:** run `/mission check repo-health-backlog-2026-07 <substring>` against a real thread and read the file back.
6. **Fix 3a — drain (thread 1), FIRST.** Run `/resolve-improvement-log`. Its `[y/n/select]` confirmation is load-bearing and is a legitimate stop point. Record the before line count (1276) and the after count. **Verify:** re-run `/prime`'s Step 3 scan command and record the new line count against the 223-line baseline.
7. **Fix 3b — widen (thread 2), SECOND.** Change the `prime.md:206` anchor so it reaches all three live entry formats: dashed `- **Severity:**`, un-dashed `**Severity:**`, and entries with no Severity line at all (reachable via their `### `/`## ` dated header plus a status filter). Keep the scan bounded — the whole point of the 2026-07-13 fix was that a full `Read` costs ~50–60k tokens; the widened scan must stay a `grep`. **Verify by execution:** run the new scan, confirm it reaches at least the two known-invisible entries (`improvement-log.md:1270` h2/un-dashed, and one Severity-less entry), and record the final line count. If the widened count exceeds the pre-drain 223 baseline, report it — that is the failure mode the ordering exists to prevent.
8. **Tick mission threads 1, 2, 6** using the verb built in step 5 — this doubles as its live acceptance test.
9. **`/qc-pass`** on the change set (commands + script). Architectural, multi-file, and the operator will review externally — independent QC is warranted here, not inline self-check.
10. **Stop without committing.** Produce a file-by-file summary of every uncommitted change for the Codex review. Do not stage, do not commit, do not push.

## Scope Alternatives

- **Min** — steps 1–4 only (the marker guard). Closes the silent data-loss path, which is the operator's stated highest priority and the cheapest item. Leaves both mission threads open.
- **Recommended** — steps 1–10 as written, with `/mission drop` deferred and thread 2's widening bounded to a `grep` change. This is the full operator-stated scope in the operator-stated order.
- **Max** — adds `/mission drop`, plus a schema-normalization pass over the ~31 Severity-less entries so all three formats converge to one. Rejected for this session: normalizing 31 entries is a bulk edit to durable operator-facing content and deserves its own mandate and its own confirmation gate, not a tail-end addition to a three-item session.

## Autonomy Posture
Gated

**Stop points:**
- `/risk-check` verdict of RECONSIDER (redesign first) or NO-GO (stop — mandate `Stop if`).
- `/blindspot-scan` PAUSE-AND-FIX verdict (resolve before implementing).
- `/resolve-improvement-log`'s `[y/n/select]` archive confirmation — load-bearing by that command's own contract; archiving is a destructive edit to durable operator-facing content.
- Any per-item disposition prompt from that command's stale-pending branch.
- **Hard stop before any git staging or commit** — operator directive: the solution is reviewed with Codex first.
- If step 7's widened scan comes out larger than the 223-line pre-drain baseline, stop and report rather than shipping a regression.

## Risk
Run `/risk-check` after the plan is approved (plan-time gate). Run it again before commit (end-time gate) — noting that **no commit happens this session**, so the end-time gate defers to the session that actually lands these changes.

Structural classes touched: canonical command edits auto-synced across ~20 sites (`mission.md`, `prime.md`); a paired-copy command edit requiring lockstep (`wrap-session.md` ×2); automation with shared-state effects (`run-manifest.sh` — the marker oracle every session's manifest write depends on). The `run-manifest.sh` change is the highest-blast-radius item: it converts a silent wrong-target write into a hard failure, which is the intended improvement, but a false-positive `die` would block manifest writes for every session. Mitigations built into the design: the check fires only on the shared-file fallback path (never on per-id resolution, the normal case), and suffix-less legacy markers warn rather than die.

Environment-fit check: not applicable — no launcher or terminal-triggered executable is produced. `run-manifest.sh` is already invoked from within sessions by existing command steps.

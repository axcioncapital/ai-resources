# Friction Log

## Session — 2026-04-18 (post-prevention cleanup 2)

### Friction Events

- **Prime Step 2 innovation-registry count wrong.** Registry is a pipe-delimited markdown table (rows like `| 2026-04-18 | agent | .../file.md | detected | — |`), but Prime Step 2 greps for `^- **detected**` / `status: detected` / `"status": "detected"` — list-item and JSON patterns, not table-cell patterns. Result: registry with 5 `detected` rows reported 0. Last session's wrap already flagged this as "status-check needed" — root cause is the Prime grep pattern, not registry state. Fix: Prime should grep for `| detected |` (table-cell pattern) or parse the status column directly. — **[FADING-GATE] verified 2026-05-26:** Resolved by `/prime` rewrite — Step 2 now reads `logs/next-up.md`; the innovation-registry grep no longer exists in `prime.md`.
- **Prime git-status snapshot can be stale vs actual HEAD.** This session's Prime reported `M .claude/hooks/pre-commit` and `?? .claude/hooks/check-skill-size.sh`, but both were actually committed in `bbd2261` and `a0c79fc` — `git diff --stat HEAD` showed those paths clean. Cost: full diagnostic detour to confirm what "loose ends" were outstanding. Not a Prime bug per se (snapshot is explicitly point-in-time), but a systematic hazard when Prime flags next-steps that depend on status claims. Possible fix: Prime's status block should invite a re-check (`git status` at step 7 or similar) rather than being treated as ground truth. — **[FADING-GATE] verified 2026-05-26:** Resolved by `/prime` Step 4 exception-check — runs live `git status --short` + `git diff --stat HEAD` to confirm the env snapshot before reporting.

#### Write Activity

## Session — 2026-05-08 14:05

### Friction Events

- **14:05** — During /friday-act this morning, I read the spec for SO advisory + systems-review as "first 30 lines peek" and stopped there. I made claims like "the SO advisory's priority filter mirrors all 12 checkup items" based on 30 lines — claims I could not actually verify. The disposition decisions for 31 journal items + 12 checkup items were grounded in incomplete context. Operator caught this and said "this was supposed to be your job as well" — meaning judgment about reading scope was supposed to be mine, not a literal-spec compliance issue. The autonomy-axis target the operator set in the SAME session ("Autonomy → loosen — operator trusts Claude to make decisions on its own; operator input rarely brings more value") was actively undermined by me hiding behind spec literalism within minutes of it being set. The right move was to read the full SO advisory + systems review before proposing a disposition; the spec's 30-line peek is a floor, not a ceiling, especially on a heavy-disposition Friday. — **[FADING-GATE] verified 2026-05-26:** Codified in workspace `CLAUDE.md` § Working Principles — "Read-scope floors. Minimum read scopes in commands are floors, not ceilings — expand when a downstream claim depends on content past the read window."
- **18:26** — /consult invoked when a 5-min targeted Read of /deploy-workflow.md would have answered the same question — overkill for a case where I already had a confident structural recommendation. Burned ~95k tokens and 2.5min on Opus when I could have done a focused file-read first and only escalated to /consult if the read surfaced a genuine ambiguity. Lesson: when I've already given a recommendation, do the cheapest verification read first; reserve /consult for genuinely contested or load-bearing system-shape questions. — **[FADING-GATE] verified 2026-05-27:** Codified directly in `.claude/commands/consult.md:14-26` — preamble line "Reserve for genuinely contested or load-bearing system-shape questions, not for verification of already-confident recommendations" plus Step 0 "Read-first gate" with the (a)/(b) check ("Have I already given a recommendation? Is there a ≤300-line file that would confirm or refute it? If yes: do the Read first").

#### Write Activity

## Session — 2026-05-11

### Friction Events

- **/session-start confirmation token ambiguous.** The mandate-confirmation prompt says *"Confirm, or correct any field."* but does not define the canonical confirmation token. Operator replied `c. Next /session-plan` meaning "confirm; next session will be /session-plan" — Claude parsed `c.` as a correction to field c ("Done when") and silently baked /session-plan into the current session's mandate. Net effect: session ran an extra command that wasn't supposed to be in this session. Fix: /session-start should state the confirmation token explicitly (e.g., `"Reply 'y' to confirm, or list specific field corrections like 'b: ...'"`) and reject single-letter ambiguous replies. — **[FADING-GATE] verified 2026-05-26:** Shipped 2026-05-16 commit `9a58073` — `session-start.md` Step 2 now reads `Reply 'confirm' (or 'y') to accept, or list field corrections in the form 'b: <new text>'. Bare single letters other than 'y' are treated as ambiguous and re-asked.` (carryover from 2026-05-25 wrap was a 5-day-stale standing item).

- **/session-plan semantics conflate "current session" vs "next session" when invoked from /monday-prep C15.** /session-plan's Step 0 checks for today's `/prime` header and Step 5 talks about "this session's autonomy posture" — language that assumes the active session. But /monday-prep C15 invokes /session-plan as *"Run /session-plan for the first work session"* — meaning the *next* session. The mismatch made today's conversation feel like a topic shift mid-session (Monday prep → resolve-scratchpads-convention) when the operator only meant to run /monday-prep. Fix: either (a) /session-plan should know which mode it's in (pass a flag from C15), or (b) /monday-prep C15 should write a planning scaffold for the next session rather than invoking /session-plan inline. Related: the relationship between the week mandate (/monday-prep) and the per-session plan (/session-plan) is not documented anywhere — operator could reasonably expect them to be separate sessions. — **[FADING-GATE] verified 2026-05-27:** Resolved by option (b) — `monday-prep.md:280–293` C15 now writes a scaffold to `logs/session-plan-next.md` and explicitly states "Do NOT invoke `/session-plan` from inside `/monday-prep`. The week mandate is week-scope; the per-session plan is session-scope — they are written in separate sessions." Cross-reference: `docs/weekly-cadence.md` § Scope separation.

- **/session-plan template produces sparse plans that don't carry the essential information.** Ran /session-plan for the new-project drift-fix session. The plan I wrote followed the skill's template skeleton literally (one-sentence Intent, three-bullet Source Material, short Risk paragraph) and ended up as a pointer to the drift report rather than a self-contained execution brief. QC reviewer flagged "Recommended-scope shorthand" as advisory only — but the operator caught it as load-bearing: *"why is there so little information in the session plan?"* A session plan should be readable on its own and tell a future-me (or a fresh session resume) what's going to happen, why, in what order, and with what verification — not just point at a separate file. Fix: /session-plan skill's Step 7 template needs to require (or strongly suggest) sections for **Findings/items to address** (one-line summary of each, not just a link), **Execution sequence** (numbered steps), and **Scope alternatives** (when multiple execution depths are on the table). Current template treats these as optional and the skeleton fills cleanly without them — which is the bug. The minimum-viable-plan should still be a self-contained read. — **[FADING-GATE] verified 2026-05-27:** Shipped 2026-05-26 commits `3894195` (Step 7 template: Findings, Execution Sequence, Scope Alternatives sections added) + `c26a308` (Wave D template self-check strengthened — 4-point rubric: length floor ≥25 lines, findings concreteness, execution-sequence concreteness, scope-alternatives realism — placeholder values rejected when applicable). Self-contained-plan requirement enforced.

- **/prime "Next Steps" goes stale when same-day parallel sessions wrap out of order.** Today had four parallel session wraps on 2026-05-11 (Bundle 1+2, Bundle 3, Bundle 4, Bundle 5). Bundle 4 wrapped last and ended up as the bottom entry in session-notes.md. /prime Step 1 reads "the last entry" and copies its Next Steps verbatim — but Bundle 4's Next Steps list was authored mid-execution, before the parallel Bundle 1+2 and Bundle 5 sessions wrapped. Result: /prime reported Bundles 1, 2, 5 as "deferred / remaining" when commits `f44684b`, `851a15d`, `62bf33f` had already shipped them. Operator caught it immediately ("I already did bundle 1,2,5 didn't I?"). Cost: low this time (one operator correction), but the failure mode is generalizable to any day with parallel sessions. Fix options: (a) /prime reads all entries from the current calendar day and filters Next Steps against subsequent same-day entries, or (b) /prime cross-checks each Next Steps item against `git log --oneline` since the entry's timestamp and flags items mentioned in later commits as DONE. Option (b) is more robust because parallel sessions can wrap in any order. Same family as the Apr-18 entry above (Prime git-status snapshot staleness) — /prime trusts one snapshot of state when truth may be elsewhere. — **[FADING-GATE] verified 2026-05-26:** Resolved by `/prime` Step 1a — implements both the git-log cross-check (option b) and the sibling-entry sweep for same-day parallel wraps.

#### Write Activity

## Session — 2026-05-18 10:00

### Friction Events

- **10:00** — note this. — **[STUB — discarded 2026-05-27]:** Empty stub entry, pre-dates the friction-logging discipline fix shipped 2026-05-22 (commit `3a7ad4c` — `/note` + `/friction-log` session-header formats unified + stub detection added). No recoverable context. Retained as audit trail; no action.

#### Write Activity

## Session — 2026-05-22 14:14

### Friction Events

- **14:14** — QC pass should happen automatically. In this session, /friday-act produced 8 implementation plan files (~350 lines of structured content with risk-check annotations) and committed them with no QC step — the /friday-act command spec has no /qc-pass stage. The operator had to manually ask "did you run /qc-pass?" An independent qc-reviewer then found a real defect: 4 of the items carried incorrect "Risk-check required: yes" annotations for change classes not on the canonical audit-discipline.md list. An automatic QC step in /friday-act Step 3.6 (after plan files are written, before the maintenance-observations commit) would have caught this before commit, instead of requiring an operator catch + a follow-up correction commit. Friction: multi-artifact command outputs (plan files, reports) ship without QC unless the operator remembers to ask. Trigger: /friday-act Step 3.6 plan generation. — **[FADING-GATE] verified 2026-05-26:** Shipped 2026-05-22 — `friday-act.md` sub-step 16k "Plan-file QC (automatic)" invokes `/qc-pass` on plan files post-Step 3.6, with explicit `Risk-check required:` annotation check.

- **14:54** — Scratchpad filename clock-skew breaks /prime resume detection. `logs/scratchpads/` filenames (e.g. `14-00`, `16-30`) are skewed ahead of real system time. /prime Step 1b selects the "most recent" scratchpad by lexical filename sort (`YYYY-MM-DD-HH-MM`), so a scratchpad written at the real time (this session: `14-44`) is out-sorted by an earlier session's scratchpad whose filename time is wrong (`16-30`). Result: /prime surfaces a stale scratchpad from an older, different session as the resume point. Observed this session (2026-05-22 journal-commands /friday-act wrap): wrote `2026-05-22-14-44-scratchpad.md`, but `2026-05-22-16-30-scratchpad.md` (from the earlier /friday-journal session) out-sorts it. This is the already-deferred scratchpad-retention work (risk-check M3 from the handoff-integration session) — now actively misrouting /prime, not just a cleanup nicety. Fix options: (a) /prime Step 1b sorts by file mtime instead of, or as a tiebreaker against, filename; (b) scratchpad filenames derive from a guaranteed-monotonic time source; (c) prune stale scratchpads so only the current one is selectable. Trigger: /prime Step 1b scratchpad detection. — **[FADING-GATE] verified 2026-05-28:** Resolved by option (a) — `.claude/commands/prime.md:68` selects scratchpads by filesystem mtime ("most-recently-modified `*-scratchpad.md` file wins"), and explicitly forbids lexical filename-time sort ("Do NOT sort by the `YYYY-MM-DD-HH-MM` timestamp in the filename: that timestamp is typed by the AI session that produced the scratchpad and its time-of-day component is unreliable — observed skew of 2–3 hours ahead of real write time"). Caveat documented: `logs/scratchpads/` is gitignored, so mtime always reflects local write time and is the reliable chronological signal.

#### Write Activity

## Session — 2026-05-25 09:07

### Friction Events

- **09:07** — /token-audit scope selection required 3 rounds of AskUserQuestion before the operator could name the projects to audit. Desired behavior: when invoked with no valid scope, list all available projects numbered (pulling from `projects/`) and wait for the operator to reply with one or more numbers. Single-round selection replaces the current multi-step dialogue. Operator's words: "in the future, when I invoke the command, please list me all projects that I have and I reply with a number which projects/folders I want audited." Trigger: /token-audit Step 1 scope resolution when the passed arg is not a recognized scope keyword. (context: 719b6c1) — **[FADING-GATE] verified 2026-05-27:** Improvement-log entry "2026-05-25 — /token-audit numbered project menu" — `Status: applied, Verified: 2026-05-25 — confirmed by operator`. Empty-args branch replaced with numbered scope menu (mirrors `/friday-checkup` Step 3); keyword shortcuts retained.

- **09:13** — /monday-prep B7 (CLAUDE.md audit step) iterates only ACTIVE_PROJECTS — the projects/ subdirectory selection — and skips the always-loaded workspace CLAUDE.md and ai-resources/CLAUDE.md. Both are loaded into every session in this repo, so a stale or contradictory line in either has higher per-session impact than any single project CLAUDE.md. Caught by operator during 2026-W22 monday-prep diagnostics ("why isn't ai resources in the audit?"); without the catch, this Monday's mandate would have scheduled 4 project audits when 6 were warranted. Trigger: /monday-prep Step 3 Phase B B7. Fix direction: B7 needs to seed the audit candidate list with workspace CLAUDE.md and ai-resources/CLAUDE.md before iterating ACTIVE_PROJECTS — same days-since-edit and line-count rule, no skip exemption for the always-loaded layer. (context: 57258a7, Make /wrap-session leaner (booked entry 2026-04-25)) — **[FADING-GATE] verified 2026-05-27:** Improvement-log entry "2026-05-25 — /monday-prep B7 seeds always-loaded CLAUDE.md layer" — `Status: applied, Verified: 2026-05-25 — confirmed by operator`. Two-step always-loaded layer audit (workspace CLAUDE.md + ai-resources/CLAUDE.md) prepended to B7 block before the ACTIVE_PROJECTS loop; no skip exemption for the always-loaded layer.

- **09:53** — /risk-check was not run on the /wrap-session leaner fix even though sub-2 was a structural reorder of operations with shared-state effects (changes when archive runs vs when session-notes.md is appended). The session-plan's Risk section framed it as "existing-command refactor below structural-change threshold" and skipped /risk-check; the operator caught this post-commit. The framing was too lenient — an existing-command edit can still trigger structural change classes when it reorders shared-state operations. The relevant class in audit-discipline.md is "automation with shared-state effects". Trigger: session-plan Risk section. Files: ai-resources/.claude/commands/wrap-session.md (commits a9d3321 + ae99d7b, both unpushed). Proposed rule: any reorder of operations touching shared state (logs, session files) should trigger /risk-check regardless of whether the command is new or existing. The existing-vs-new framing in audit-discipline.md and session-plan should be refined to add "or reorders shared-state operations" as a tripwire. (context: 5e506a6, Make /wrap-session leaner (booked entry 2026-04-25)) — **[FADING-GATE] verified 2026-05-27:** Improvement-log entry "2026-05-25 — Risk-check rule clarified to cover reorders of existing shared-state ops" — `Status: applied, Verified: 2026-05-25 — confirmed by operator`. (a) Clarifying clause added to `docs/audit-discipline.md` line 24 (reordering existing shared-state ops qualifies, not only new automation); (b) tripwire note added to `session-plan.md` Step 6 ("existing-command refactor" framing does NOT exempt from /risk-check when shared-state ops are reordered).

#### Write Activity

## Session — 2026-05-25 14:10

### Friction Events

- **14:10** — `/session-plan` uses a single shared file `logs/session-plan.md` (overwrite each session). When two Claude windows are open on the same repo simultaneously, the second session's `/session-plan` invocation finds a plan file from the first session and triggers the 3-option keep/overwrite/pass2 flow. The operator had to navigate this manually: option 2 (overwrite) would have destroyed the other session's active plan; option 3 (write to `session-plan-pass2.md`) preserved it but introduced a non-standard filename. This is a recurring pattern — operator flagged it has happened before. Root cause: the file path is hard-coded as a single per-repo name rather than per-session or per-date. Fix directions: (a) name the plan file with a date+intent slug so multiple same-day plans coexist without a conflict prompt (e.g. `logs/session-plan-YYYY-MM-DD-{slug}.md`); (b) detect an active plan written by a different session (different Mandate line) and default to pass2 rather than asking; (c) store session-plan.md under a session-scoped directory. Trigger: `/session-plan` Step 0 plan-file conflict detection when concurrent sessions share the same repo. (context: this session, 2026-05-25 SF1 /session-start fix) — **[FADING-GATE] verified 2026-05-27:** Resolved by option (b) — commit `8ab5685` (Wave C — /session-plan Step 0 concurrent-session collision auto-detect). MISMATCH branch in Step 0 auto-defaults to `session-plan-pass2.md` without prompting when EXISTING_INTENT and UPCOMING_INTENT differ; MATCH branch (same-session re-invocation) retains the 3-option prompt with default to "keep current plan".

#### Write Activity

## Session — 2026-05-28 10:05

### Friction Events

- **10:05** — Concurrent sessions in the same project (axcion-brand-book) caused two losing-race collisions in one session-startup sequence: (1) `/session-start` Step 0.5 mtime guard passed clean (DELTA=0) at command entry, then a foreign session wrote a redirect line to `logs/session-notes.md` during the Mandate Confirmation wait (Step 2), so the operator's `y` arrived against an echoed mandate that no longer matched the on-disk file; (2) `/session-plan` Step 0 mtime guard found `session-plan.md` ~25 hours old (clean), then between Step 0 and the Step 7 Write, the foreign session's `/session-plan` rewrote `session-plan.md` with its own intent — the collision only surfaced when my Write tool errored with "File has been modified since read." Both checks are point-in-time at command entry and cannot see writes that arrive after the check but before the command's own write. Operator explicitly attributed the trigger to deliberate concurrent-session use ("It is because I start concurrent sessions"). Recurrence risk: every concurrent-session use in any project; the 2026-05-25 14:10 friction entry shows this is the third recurrence of the same TOCTOU race class, each time prompting a one-off fix to one command (`/session-plan` Step 0 collision detection was the previous fix) without addressing the underlying class. Trigger: any command that (a) reads a shared log file at entry, (b) makes a decision based on that read, (c) writes back to the same file after some elapsed turns. Today's incidents added a third loss: the parallel session also overwrote the entire `## 2026-05-28` mandate block in `logs/session-notes.md` — my session's mandate vanished from the file of record. Files affected today: `logs/session-notes.md`, `logs/session-plan.md`. Proposed structural fix logged in `improvement-log.md` 2026-05-28 entry (broader entry supersedes the narrow same-day `/session-start` Step 0.5 entry written earlier in this session).
  - **Cross-ref:** logs/improvement-log.md — "2026-05-28 — Concurrent sessions cause TOCTOU races on shared log files" (phased marker approach, structural fix proposal). Targeted in wave 3 of fix-plan 2026-05-28-1902.

#### Write Activity

## Session — 2026-05-28 14:20

### Friction Events

- **14:20** — `/wrap-session` Step 3.5 foreign-session pre-write guard false-positive on auto-mode chained tasks. Today's session ran `2,3 auto` from `/prime` (two tasks chained under auto-mode), so `/prime` Step 8c.3 fired twice and legitimately added 2 today-headers to `logs/session-notes.md`. The guard's `PRIME_RAN=1` model assumes single-task `/prime` → `/session-start` → wrap and computes `FOREIGN = ADDED_HEADERS - PRIME_RAN`, yielding `FOREIGN=1` on a clean own-write state. Investigation confirmed no foreign content (HEAD has the 5 morning today-headers I claimed; the 2 added today are both this session's auto-mode entries; concurrent terminals are active but in `projects/nordic-pe-screening-project/` and `projects/nordic-pe-macro-landscape-H1-2026/`, not in `ai-resources/`). Trigger: any `/wrap-session` after an auto-mode N-task chain (`N,M auto` / chained re-entry) under `/prime` Step 8c. Recurrence risk: every multi-task auto-mode session. Fix direction: make the guard chain-aware — track auto-mode task count in `.prime-mtime` payload (or a sibling marker) and use it as the per-signal subtractor (`FOREIGN_HEADERS = ADDED_HEADERS - PRIME_TASKS`). Today's run was overridden by operator after the false-positive was verified inline (HEAD + mtime + ps audit). Files touched by the override: `logs/session-notes.md` (this session's task-2 + task-3 mandates), `logs/friction-log.md` (this entry).

**Deferred 2026-05-28 (Wave 3 re-evaluation gate after id-31 Phase 1 landed).** The proposed marker-as-counter fix has a hidden security regression: `logs/.session-marker` is a SHARED file — concurrent sessions both bump it. If session A runs 1 `/prime` and parallel session B runs 1 `/prime`, the marker reads `S2` → `PRIME_TASKS=2` → `FOREIGN = ADDED_HEADERS - 2 = 0` → A's `/wrap-session` silently ships B's content under A's commit. This is the exact failure mode the guard exists to prevent. The current `PRIME_RAN=1` binary correctly STOPS in this case. Counting "this session's tasks" cleanly requires session-scoped state — which lands in id-31 Phase 2 (marker-scoped session-notes headers `## YYYY-MM-DD — Session {marker}`). After Phase 2, the count is exact: grep for THIS session's marker. Deferred to land with id-31 Phase 2.

#### Write Activity

## Session — 2026-06-04

### Friction Events

- **[wrap-collector]** wrap (S4) — **Friction type: command.** `/prime`'s Step 1a Next-Steps git cross-check (prime.md L42–48) scans only `$CWD_REPO` + `$AI_RESOURCES`, so it cannot see commits in sibling *project* repos. In S4 the `/prime` menu surfaced items 2 and 3 as still-open when concurrent same-day sessions S1/S2 had already done and committed that work in the `projects/strategic-os/` repo. The stale menu was caught at the approval gate by the context-discovery engine, not by `/prime` itself — so the menu reduction (5 items → 1+6) cost a gate-time conflict check instead of being correct at prime time. The line-48 "dual-repo Cluster A blindspot" rationale already names the two-repo limit but stops at cwd+ai-resources; it does not extend to a third sibling project repo where parallel-session work lands. Routed to improvement-log 2026-06-04 ("/prime Step 1a … not sibling project repos") with the fix direction.

- **[wrap-collector]** wrap (S6) — **Friction type: process.** An auto-multi-item bundle (items 1,2,3,5) included item 3 "review the System Owner reference files" with no concrete deliverable defined — no staleness check, quality pass, or consistency-against-live-SO target. The bundle was flagged heterogeneous + partly-structural at the gate and the operator chose the full set; Stage-0 `/risk-check` then returned RECONSIDER and the bundle was reshaped, with item 3 producing no output and being deferred to Next Steps to "define a concrete deliverable before running." The friction is that a menu item with no specifiable done-condition entered the executable bundle in the first place, where it consumed gate + risk-check attention before being recognized as unscoped. Direction (collector does not propose the fix): the signal is that auto-multi-item picks would benefit from a per-item done-condition presence-check before the bundle is presented for approval. No content lost; transparently surfaced.

#### Write Activity

## Session — 2026-06-05 (S6)

### Friction Events

- **S6 wrap** — **Concurrent-session collision, recurrence ~5th of the same class.** While S6 ran a friday-act implementation sweep, a parallel session implemented "item 5" (date-qualify session-plan filename) in the SHARED ai-resources working tree, editing `contract-check.md`, `session-plan.md`, `docs/session-marker.md`, and `prime.md`, then committing them (`fa2b3f2`, `78b1a8b`) mid-S6. NO guard fired. Two distinct gaps surfaced:
  - **(a) Guards are session-notes-scoped; blind to shared command/doc files.** Every concurrent-session guard (`/session-start` Step 0.5 mtime, `/wrap-session` Step 3.5 header/mandate counts, `detect-concurrent-session.sh`) watches `logs/session-notes.md` only. Nothing watches `.claude/commands/` or `docs/` for foreign writes. The genuine collision file was `prime.md` — S6 committed sub-step 1.5 (`1d91723`), the foreign session re-edited it (built on top, then committed). Lost-update risk class. Routed to improvement-log via `/resolve-repo-problem` ("Concurrent-session guards blind to foreign writes on shared command/doc files").
  - **(b) Partial marker setup → NO_OWN_MARKER guard would have false-positived.** S6's marker setup wrote only the shared `logs/.session-marker`, not the per-id `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}`. At wrap, the Step 3.5 NO_OWN_MARKER rule (built for genuine no-marker sessions) would have flagged S6's own header as FOREIGN. Caught by manual FOREIGN=0 verification — near-miss. Routed to improvement-log (guardrail-candidate, med).
  - **Recurrence framing:** this is the same root class as 2026-05-25 14:10, 2026-05-28 10:05, 2026-05-28 14:20, and 2026-06-04 S4 — parallel sessions mutating shared repo state. Each instance has received a NARROW one-off guard patch (TOCTOU Phase 2+3, per-id markers, NO_OWN_MARKER, date-rollover grace, chain-aware counts). The class persists because every new way two sessions can touch the same state is a new un-guarded surface. Files affected today: `prime.md`, `contract-check.md`, `session-plan.md`, `docs/session-marker.md` (foreign, committed by the other session); `logs/session-notes.md` (S6 — clean, no foreign content). No data lost; explicit-path staging + manual verification contained it.

#### Write Activity
- S6 staged ONLY its own log files by explicit path (`session-notes.md`, `decisions.md`, `improvement-log.md`, `session-plan-S6.md`); all foreign-modified files left untouched.

## Session — 2026-06-05 (S7)

### Friction Events

- **S7 (date-qualify completion)** — **Consumer-inventory under-count on a structural rename, recurrence 3rd+ of the same class.** While completing the date-qualify session-plan-filename change, THREE independent passes each missed the same runtime consumer, `wrap-session.md` (both paired copies), an exact-path plan reader at its Step 6.4 mandate-resolution chain. The misses: (1) the friday-act plan that scoped the change named only 3 files (`session-marker.md` + `prime.md` + `session-plan.md`); (2) the `/risk-check` reviewer's consumer inventory listed 14 consumers but `wrap-session.md` was not among them; (3) my own implementation grep did not surface it. The miss was caught only because I read the pre-existing `id-41` improvement-log entry, whose S6 hand-built inventory *had* flagged `wrap-session.md` L321. After my rename, that reader would have silently resolved to "plan not found" (it tolerates plan-absence with no error) — a silent degradation, not a loud break. Same root class as the 2026-05-29 pre-spec-inventory entry and id-40 (2026-06-05, "pre-spec consumer-inventory grep checklist") — structural renames keep shipping with incomplete consumer inventories.

  **Why this keeps happening — two compounding root causes:**
  - **(a) Grep on the templated form misses files that spell the placeholder differently.** A consumer-inventory grep for `session-plan-${MARKER}` does NOT match `wrap-session.md`, which writes the literal `session-plan-{MARKER}.md` (no `$`). Across the codebase the same placeholder is written at least four ways — `${MARKER}`, `{MARKER}`, `{marker}`, `$MARKER` — so any grep keyed to one spelling silently skips the others. This is the mechanical reason grep-based inventories under-count even when run carefully.
  - **(b) The authoritative registry was itself incomplete and misclassified.** `docs/session-marker.md` § Two-end contract registry — the document whose stated job is "every place the marker contract is consumed must point back to this doc" — did NOT list `wrap-session.md` at all, and filed the runtime hook `backup-session-plan.sh` under "Doc references (narrative, not consumers)" when its line-20 regex is load-bearing (a non-match silently stops plan backups). So the one source of truth that should have caught all three passes was stale, and each actor fell back to independent greps (root cause a).

  **Prevention applied this session (structural):**
  - Added `wrap-session.md` (both paired copies) to the registry's Read-only auxiliary consumers; created a new "Runtime non-command consumers" class and moved `backup-session-plan.sh` (+ project-local copies) into it, flagged as load-bearing-parse with a lockstep-update requirement.
  - Switched all exact-path plan readers (`contract-check.md`, `drift-check.md`, both `wrap-session.md` copies) from constructing `session-plan-${MARKER}.md` to the glob `session-plan-*${MARKER}.md`, which matches both the new date-qualified and the old bare-marker forms — so a future format change cannot silently break the read.

  **Prevention proposed (routed to improvement-log, strengthens id-40):** a pre-spec consumer-inventory for any path rename must (1) grep the INVARIANT filename stem (`session-plan`), never the templated form, so placeholder-spelling variance cannot hide a consumer; and (2) reconcile the grep result against `docs/session-marker.md` § registry, adding any consumer the grep found but the registry lacked (and vice-versa) BEFORE the rename spec is written. id-40 currently routes the grep into the spec's affected-file list but does not mandate the invariant-stem rule or the grep↔registry reconciliation — both gaps let this recurrence through.

#### Write Activity
- S7 amended its own Stage-4 commit set: fixed `wrap-session.md` (both copies), `heavy-read-discipline.md`, and the `session-marker.md` registry after the id-41 cross-read exposed the missed consumers. All edits are to files S7 itself is changing this session; no foreign-modified files touched.

## Session — 2026-06-05 (/fix-project-issues, 2nd run)

### Friction Events

- **Diagnostics scan over-reports because candidates are built from dated audit reports, not reconciled against live file state — recurrence 2nd of the same class TODAY.** A fresh `/fix-project-issues ai-resources` run surfaced 23 candidates; SO vetting + live-state verification collapsed them to ONE clean applied item (id-08). Of the rest, **4 were already-done / no-op** (id-07 = morning id-25 applied f0959f7; id-09 = morning id-26 applied, no R-codes remain; id-05 = S11 grounding fix; id-12 = canonical conflict with the retired `permission-template.md:141` deny). The candidate scanner reads the latest *dated* diagnostic reports (`claude-md-audit` 2026-06-01, `token-audit`/`friday-checkup` 2026-06-05) and the backlog logs, but those reports were generated **before** same-day commits (`f0959f7`, `e1a60d6`/`7a0ddd4`) moved the underlying files — so the scan re-surfaced findings that live state had already resolved. The earlier run today (`b18d212`, morning /diagnostics-plan) hit the identical pattern: 6 of 7 Do-now items dissolved on the same reconciliation, with the same root cause logged as a standing caveat.

  **Why it recurs:** the diagnostics-scanner has no live-state reconciliation step. It normalizes/dedupes candidates against each other and against prior improvement-log dispositions (which I passed in this run, and which correctly suppressed the morning batch's id-06/id-22/id-37), but it does NOT diff each candidate against the current on-disk file before ranking it actionable. The dated report is treated as ground truth; on a fast-moving same-day repo the report is stale by hours. The reconciliation that catches it currently lives downstream — in SO vetting + my manual live-read — which is correct as a safety net but means every run pays the cost of vetting dead candidates before culling them (the morning run's logged "redundant-vetting detour").

  **Direction (not a fix — for the Friday cadence to weigh):** the standing caveat is now logged twice in one day (`b18d212`, this run). A structural option worth considering: add a cheap live-state reconciliation pass to the diagnostics-scanner (or a /fix-project-issues pre-SO step) that, for each candidate citing a specific file+line or a specific rule/dup, re-reads the live file and drops the candidate if the cited condition no longer holds — culling already-applied items BEFORE they reach SO vetting, raising batch signal-to-noise. Cost: one extra read per file-specific candidate; benefit: the SO and main session stop spending attention on dead findings on every same-day re-run. No content lost this session; reconciliation worked, the detour was the only cost. — **[FADING-GATE] verified 2026-06-05 (S14):** Resolved at the command layer (NOT the scanner layer the Direction proposed — the scanner is read-only with no git) by the reconcile-at-read primitive `docs/backlog-reconciliation.md` + `fix-project-issues.md` Step 2.5 + `fix-repo-issues.md` Step 3.0, all shipped commit `23c9143`. See `logs/improvement-log.md` § 2026-06-05 diagnostics-lag RESOLVED entry.

#### Write Activity
- This session staged only its own work by explicit path: `CLAUDE.md` (id-08), `logs/improvement-log.md` (dispositions), the risk-check report, and `logs/friction-log.md` (this entry). The pre-existing uncommitted `logs/improvement-log-archive.md` (S10 leftover) was left untouched — not this session's file.

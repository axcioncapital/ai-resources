# Friction Log

## Schema

Applies to entries dated **2026-07-03 onward**. Existing entries below, dated before 2026-07-03, are not retrofitted.

**Substantive entries** — hand-authored narrative entries and `session-feedback-collector` (`[wrap-collector]`) wrap-time entries — require four additional fields, in this order. A substantive entry is any entry that isn't a bare one-liner produced by `/friction-log [description]`, `/note`'s `friction:` prefix, or `friction-log-auto.sh`'s auto-appended tool-error lines; a rough test: if the entry has more than one sentence of description, or was written by the collector, it's substantive.

- **Failure mode:** one primary category from the fixed enum below (pick the dominant/triggering cause, even when more than one applies):
  - **Context** — didn't know enough, or looked in the wrong place.
  - **Mandate** — task framed unclearly.
  - **Workflow** — no reliable process for this task type.
  - **Authority** — didn't know which source of truth controlled the decision.
  - **Validation** — output untested or unchecked.
  - **Autonomy** — needed too much operator guidance.
  - **Safety** — changes proposed without enough risk control.
  - **Traceability** — no clear record of what changed, why, and what's open.
- **Root cause:** why it actually happened (not just what happened).
- **Prevention:** what would stop the recurrence.
- **Owner artifact:** the concrete file / command / checklist / rule / test that should own closing the gap (e.g. "`/prime` Step 2", "`docs/audit-discipline.md`"). If none is identified yet, write `(none identified)` — never omit the field.

**Canonical copy.** This block is the source of truth for the 8-category enum. `session-feedback-collector.md` carries an inline copy so the taxonomy is self-contained wherever the collector fires (it writes into ~9 friction-log.md files across projects, most of which don't carry this block) — the two must be edited in lockstep; this file is canonical, the collector's copy is a synced operational copy.

**Relationship to existing/adjacent fields (none of these are replaced):**
- **Friction type:** the existing free-text tag (e.g. `process`, `harness`, `config`) — names *which component* was involved. Kept as-is, alongside the new Failure mode category, which names *why* it failed. Not a fixed enum; do not conflate the two.
- **AP-9's own classification axis** (context / judgment / instruction / model-limitation, per `projects/strategic-os/ai-strategy/principles-base.md`) is a third, narrower lens some entries may still reference. The Failure-mode enum here is a separate, wider taxonomy purpose-built for this log — the two are not required to align, and reconciling them is an open item for the principles doc, not something to resolve ad hoc per entry.
- **Resolved:** / **[FADING-GATE] verified …:** — unchanged closure stamps. Owner artifact states what *should* close the gap; these state that it *has*. A later close-out still uses the existing stamps.

**Exempt (lightweight, no classification required):** `/friction-log [description]` one-liners, `/note`'s `friction:`-prefixed quick capture, and `friction-log-auto.sh`'s auto-appended tool-error lines. These stay terse by design.

---

## Session — 2026-04-18 (post-prevention cleanup 2)

### Friction Events

- **Prime Step 2 innovation-registry count wrong.** Registry is a pipe-delimited markdown table (rows like `| 2026-04-18 | agent | .../file.md | detected | — |`), but Prime Step 2 greps for `^- **detected**` / `status: detected` / `"status": "detected"` — list-item and JSON patterns, not table-cell patterns. Result: registry with 5 `detected` rows reported 0. Last session's wrap already flagged this as "status-check needed" — root cause is the Prime grep pattern, not registry state. Fix: Prime should grep for `| detected |` (table-cell pattern) or parse the status column directly. — **[FADING-GATE] verified 2026-05-26:** Resolved by `/prime` rewrite — Step 2 now reads `logs/next-up.md`; the innovation-registry grep no longer exists in `prime.md`.
- **Prime git-status snapshot can be stale vs actual HEAD.** This session's Prime reported `M .claude/hooks/pre-commit` and `?? .claude/hooks/check-skill-size.sh`, but both were actually committed in `bbd2261` and `a0c79fc` — `git diff --stat HEAD` showed those paths clean. Cost: full diagnostic detour to confirm what "loose ends" were outstanding. Not a Prime bug per se (snapshot is explicitly point-in-time), but a systematic hazard when Prime flags next-steps that depend on status claims. Possible fix: Prime's status block should invite a re-check (`git status` at step 7 or similar) rather than being treated as ground truth. — **[FADING-GATE] verified 2026-05-26:** Resolved by `/prime` Step 4 exception-check — runs live `git status --short` + `git diff --stat HEAD` to confirm the env snapshot before reporting.

#### Write Activity

## Session — 2026-05-08 14:05

### Friction Events

- **14:05** — During /friday-act this morning, I read the spec for SO advisory + systems-review as "first 30 lines peek" and stopped there. I made claims like "the SO advisory's priority filter mirrors all 12 checkup items" based on 30 lines — claims I could not actually verify. The disposition decisions for 31 journal items + 12 checkup items were grounded in incomplete context. Operator caught this and said "this was supposed to be your job as well" — meaning judgment about reading scope was supposed to be mine, not a literal-spec compliance issue. The autonomy-axis target the operator set in the SAME session ("Autonomy → loosen — operator trusts Claude to make decisions on its own; operator input rarely brings more value") was actively undermined by me hiding behind spec literalism within minutes of it being set. The right move was to read the full SO advisory + systems review before proposing a disposition; the spec's 30-line peek is a floor, not a ceiling, especially on a heavy-disposition Friday. — **[FADING-GATE] verified 2026-05-26:** Codified in workspace `CLAUDE.md` § Working Principles — "Read-scope floors. Minimum read scopes in commands are floors, not ceilings — expand when a downstream claim depends on content past the read window."
- **18:26** — /consult invoked when a 5-min targeted Read of /deploy-workflow.md would have answered the same question — overkill for a case where I already had a confident structural recommendation. Burned ~95k tokens and 2.5min on Opus when I could have done a focused file-read first and only escalated to /consult if the read surfaced a genuine ambiguity. Lesson: when I've already given a recommendation, do the cheapest verification read first; reserve /consult for genuinely contested or load-bearing system-shape questions. — **[FADING-GATE] verified 2026-05-27:** Codified directly in `.claude/commands/consult.md:14-26` — preamble line "Reserve for genuinely contested or load-bearing system-shape questions, not for verification of already-confident recommendations" plus Step 0 "Read-first gate" with the (a)/(b) check ("Have I already given a recommendation? Is there a ≤300-line file that would confirm or refute it? If yes: do the Read first").

#### Write Activity

## Session — 2026-05-11

### Friction Events

- **/session-start confirmation token ambiguous.** The mandate-confirmation prompt says *"Confirm, or correct any field."* but does not define the canonical confirmation token. Operator replied `c. Next /session-plan` meaning "confirm; next session will be /session-plan" — Claude parsed `c.` as a correction to field c ("Done when") and silently baked /session-plan into the current session's mandate. Net effect: session ran an extra command that wasn't supposed to be in this session. Fix: /session-start should state the confirmation token explicitly (e.g., `"Reply 'y' to confirm, or list specific field corrections like 'b: ...'"`) and reject single-letter ambiguous replies. — **[FADING-GATE] verified 2026-05-26:** Shipped 2026-05-16 commit `9a58073` — `session-start.md` Step 2 now reads `Reply 'confirm' (or 'y') to accept, or list field corrections in the form 'b: <new text>'. Bare single letters other than 'y' are treated as ambiguous and re-asked.` (carryover from 2026-05-25 wrap was a 5-day-stale standing item).

- **/session-plan semantics conflate "current session" vs "next session" when invoked from /monday-prep C15.** /session-plan's Step 0 checks for today's `/prime` header and Step 5 talks about "this session's autonomy posture" — language that assumes the active session. But /monday-prep C15 invokes /session-plan as *"Run /session-plan for the first work session"* — meaning the *next* session. The mismatch made today's conversation feel like a topic shift mid-session (Monday prep → resolve-scratchpads-convention) when the operator only meant to run /monday-prep. Fix: either (a) /session-plan should know which mode it's in (pass a flag from C15), or (b) /monday-prep C15 should write a planning scaffold for the next session rather than invoking /session-plan inline. Related: the relationship between the week mandate (/monday-prep) and the per-session plan (/session-plan) is not documented anywhere — operator could reasonably expect them to be separate sessions. — **[FADING-GATE] verified 2026-05-27:** Resolved by option (b) — `monday-prep.md:280–293` C15 now writes a scaffold to `logs/session-plan-next.md` and explicitly states "Do NOT invoke `/session-plan` from inside `/monday-prep`. The week mandate is week-scope; the per-session plan is session-scope — they are written in separate sessions." Cross-reference: `docs/weekly-cadence.md` § Scope separation.

- **/session-plan template produces sparse plans that don't carry the essential information.** Ran /session-plan for the new-project drift-fix session. The plan I wrote followed the skill's template skeleton literally (one-sentence Intent, three-bullet Source Material, short Risk paragraph) and ended up as a pointer to the drift report rather than a self-contained execution brief. QC reviewer flagged "Recommended-scope shorthand" as advisory only — but the operator caught it as load-bearing: *"why is there so little information in the session plan?"* A session plan should be readable on its own and tell a future-me (or a fresh session resume) what's going to happen, why, in what order, and with what verification — not just point at a separate file. Fix: /session-plan skill's Step 7 template needs to require (or strongly suggest) sections for **Findings/items to address** (one-line summary of each, not just a link), **Execution sequence** (numbered steps), and **Scope alternatives** (when multiple execution depths are on the table). Current template treats these as optional and the skeleton fills cleanly without them — which is the bug. The minimum-viable-plan should still be a self-contained read. — **[FADING-GATE] verified 2026-05-27:** Shipped 2026-05-26 commits `3894195` (Step 7 template: Findings, Execution Sequence, Scope Alternatives sections added) + `c26a308` (Wave D template self-check strengthened — 4-point rubric: length floor ≥25 lines, findings concreteness, execution-sequence concreteness, scope-alternatives realism — placeholder values rejected when applicable). Self-contained-plan requirement enforced.

- **/prime "Next Steps" goes stale when same-day parallel sessions wrap out of order.** Today had four parallel session wraps on 2026-05-11 (Bundle 1+2, Bundle 3, Bundle 4, Bundle 5). Bundle 4 wrapped last and ended up as the bottom entry in session-notes.md. /prime Step 1 reads "the last entry" and copies its Next Steps verbatim — but Bundle 4's Next Steps list was authored mid-execution, before the parallel Bundle 1+2 and Bundle 5 sessions wrapped. Result: /prime reported Bundles 1, 2, 5 as "deferred / remaining" when commits `f44684b`, `851a15d`, `62bf33f` had already shipped them. Operator caught it immediately ("I already did bundle 1,2,5 didn't I?"). Cost: low this time (one operator correction), but the failure mode is generalizable to any day with parallel sessions. Fix options: (a) /prime reads all entries from the current calendar day and filters Next Steps against subsequent same-day entries, or (b) /prime cross-checks each Next Steps item against `git log --oneline` since the entry's timestamp and flags items mentioned in later commits as DONE. Option (b) is more robust because parallel sessions can wrap in any order. Same family as the Apr-18 entry above (Prime git-status snapshot staleness) — /prime trusts one snapshot of state when truth may be elsewhere. — **[FADING-GATE] verified 2026-05-26:** Resolved by `/prime` Step 1a — implements both the git-log cross-check (option b) and the sibling-entry sweep for same-day parallel wraps.

#### Write Activity

## Session — 2026-05-18 10:00

### Friction Events

- **10:00** — note this. — **[STUB — discarded 2026-05-27]:** Empty stub entry, pre-dates the friction-logging discipline fix shipped 2026-05-22 (commit `3a7ad4c` — `/note` + `/friction-log` session-header formats unified + stub detection added). No recoverable context. Retained as audit trail; no action. — **Resolved:** 2026-07-03 — stamped closed (entry self-dispositioned as discarded stub 2026-05-27; no action was ever pending). Stamp stops backlog scans from re-surfacing it.

#### Write Activity

## Session — 2026-05-22 14:14

### Friction Events

- **14:14** — QC pass should happen automatically. In this session, /friday-act produced 8 implementation plan files (~350 lines of structured content with risk-check annotations) and committed them with no QC step — the /friday-act command spec has no /qc-pass stage. The operator had to manually ask "did you run /qc-pass?" An independent qc-reviewer then found a real defect: 4 of the items carried incorrect "Risk-check required: yes" annotations for change classes not on the canonical audit-discipline.md list. An automatic QC step in /friday-act Step 3.6 (after plan files are written, before the maintenance-observations commit) would have caught this before commit, instead of requiring an operator catch + a follow-up correction commit. Friction: multi-artifact command outputs (plan files, reports) ship without QC unless the operator remembers to ask. Trigger: /friday-act Step 3.6 plan generation. — **[FADING-GATE] verified 2026-05-26:** Shipped 2026-05-22 — `friday-act.md` sub-step 16k "Plan-file QC (automatic)" invokes `/qc-pass` on plan files post-Step 3.6, with explicit `Risk-check required:` annotation check.

- **14:54** — Scratchpad filename clock-skew breaks /prime resume detection. `logs/scratchpads/` filenames (e.g. `14-00`, `16-30`) are skewed ahead of real system time. /prime Step 1b selects the "most recent" scratchpad by lexical filename sort (`YYYY-MM-DD-HH-MM`), so a scratchpad written at the real time (this session: `14-44`) is out-sorted by an earlier session's scratchpad whose filename time is wrong (`16-30`). Result: /prime surfaces a stale scratchpad from an older, different session as the resume point. Observed this session (2026-05-22 journal-commands /friday-act wrap): wrote `2026-05-22-14-44-scratchpad.md`, but `2026-05-22-16-30-scratchpad.md` (from the earlier /friday-journal session) out-sorts it. This is the already-deferred scratchpad-retention work (risk-check M3 from the handoff-integration session) — now actively misrouting /prime, not just a cleanup nicety. Fix options: (a) /prime Step 1b sorts by file mtime instead of, or as a tiebreaker against, filename; (b) scratchpad filenames derive from a guaranteed-monotonic time source; (c) prune stale scratchpads so only the current one is selectable. Trigger: /prime Step 1b scratchpad detection. — **[FADING-GATE] verified 2026-05-28:** Resolved by option (a) — `.claude/commands/prime.md:68` selects scratchpads by filesystem mtime ("most-recently-modified `*-scratchpad.md` file wins"), and explicitly forbids lexical filename-time sort ("Do NOT sort by the `YYYY-MM-DD-HH-MM` timestamp in the filename: that timestamp is typed by the AI session that produced the scratchpad and its time-of-day component is unreliable — observed skew of 2–3 hours ahead of real write time"). Caveat documented: `logs/scratchpads/` is gitignored, so mtime always reflects local write time and is the reliable chronological signal.

#### Write Activity

## Session — 2026-05-25 09:07

### Friction Events

- **09:07** — /token-audit scope selection required 3 rounds of AskUserQuestion before the operator could name the projects to audit. Desired behavior: when invoked with no valid scope, list all available projects numbered (pulling from `projects/`) and wait for the operator to reply with one or more numbers. Single-round selection replaces the current multi-step dialogue. Operator's words: "in the future, when I invoke the command, please list me all projects that I have and I reply with a number which projects/folders I want audited." Trigger: /token-audit Step 1 scope resolution when the passed arg is not a recognized scope keyword. (context: 719b6c1) — **[FADING-GATE] verified 2026-05-27:** Improvement-log entry "2026-05-25 — /token-audit numbered project menu" — `Status: applied, Verified: 2026-05-25 — confirmed by operator`. Empty-args branch replaced with numbered scope menu (mirrors `/friday-checkup` Step 3); keyword shortcuts retained.

- **09:13** — /monday-prep B7 (CLAUDE.md audit step) iterates only ACTIVE_PROJECTS — the projects/ subdirectory selection — and skips the always-loaded workspace CLAUDE.md and ai-resources/CLAUDE.md. Both are loaded into every session in this repo, so a stale or contradictory line in either has higher per-session impact than any single project CLAUDE.md. Caught by operator during 2026-W22 monday-prep diagnostics ("why isn't ai resources in the audit?"); without the catch, this Monday's mandate would have scheduled 4 project audits when 6 were warranted. Trigger: /monday-prep Step 3 Phase B B7. Fix direction: B7 needs to seed the audit candidate list with workspace CLAUDE.md and ai-resources/CLAUDE.md before iterating ACTIVE_PROJECTS — same days-since-edit and line-count rule, no skip exemption for the always-loaded layer. (context: 57258a7, Make /wrap-session leaner (booked entry 2026-04-25)) — **[FADING-GATE] verified 2026-05-27:** Improvement-log entry "2026-05-25 — /monday-prep B7 seeds always-loaded CLAUDE.md layer" — `Status: applied, Verified: 2026-05-25 — confirmed by operator`. Two-step always-loaded layer audit (workspace CLAUDE.md + ai-resources/CLAUDE.md) prepended to B7 block before the ACTIVE_PROJECTS loop; no skip exemption for the always-loaded layer.

- **09:53** — /risk-check was not run on the /wrap-session leaner fix even though sub-2 was a structural reorder of operations with shared-state effects (changes when archive runs vs when session-notes.md is appended). The session-plan's Risk section framed it as "existing-command refactor below structural-change threshold" and skipped /risk-check; the operator caught this post-commit. The framing was too lenient — an existing-command edit can still trigger structural change classes when it reorders shared-state operations. The relevant class in audit-discipline.md is "automation with shared-state effects". Trigger: session-plan Risk section. Files: ai-resources/.claude/commands/wrap-session.md (commits a9d3321 + ae99d7b, both unpushed). Proposed rule: any reorder of operations touching shared state (logs, session files) should trigger /risk-check regardless of whether the command is new or existing. The existing-vs-new framing in audit-discipline.md and session-plan should be refined to add "or reorders shared-state operations" as a tripwire. (context: 5e506a6, Make /wrap-session leaner (booked entry 2026-04-25)) — **[FADING-GATE] verified 2026-05-27:** Improvement-log entry "2026-05-25 — Risk-check rule clarified to cover reorders of existing shared-state ops" — `Status: applied, Verified: 2026-05-25 — confirmed by operator`. (a) Clarifying clause added to `docs/audit-discipline.md` line 24 (reordering existing shared-state ops qualifies, not only new automation); (b) tripwire note added to `session-plan.md` Step 6 ("existing-command refactor" framing does NOT exempt from /risk-check when shared-state ops are reordered).

#### Write Activity

## Session — 2026-05-25 14:10

### Friction Events

- **14:10** — `/session-plan` uses a single shared file `logs/session-plan.md` (overwrite each session). When two Claude windows are open on the same repo simultaneously, the second session's `/session-plan` invocation finds a plan file from the first session and triggers the 3-option keep/overwrite/pass2 flow. The operator had to navigate this manually: option 2 (overwrite) would have destroyed the other session's active plan; option 3 (write to `session-plan-pass2.md`) preserved it but introduced a non-standard filename. This is a recurring pattern — operator flagged it has happened before. Root cause: the file path is hard-coded as a single per-repo name rather than per-session or per-date. Fix directions: (a) name the plan file with a date+intent slug so multiple same-day plans coexist without a conflict prompt (e.g. `logs/session-plan-YYYY-MM-DD-{slug}.md`); (b) detect an active plan written by a different session (different Mandate line) and default to pass2 rather than asking; (c) store session-plan.md under a session-scoped directory. Trigger: `/session-plan` Step 0 plan-file conflict detection when concurrent sessions share the same repo. (context: this session, 2026-05-25 SF1 /session-start fix) — **[FADING-GATE] verified 2026-05-27:** Resolved by option (b) — commit `8ab5685` (Wave C — /session-plan Step 0 concurrent-session collision auto-detect). MISMATCH branch in Step 0 auto-defaults to `session-plan-pass2.md` without prompting when EXISTING_INTENT and UPCOMING_INTENT differ; MATCH branch (same-session re-invocation) retains the 3-option prompt with default to "keep current plan".

#### Write Activity

## Session — 2026-05-28 10:05

### Friction Events

- **10:05** — Concurrent sessions in the same project (axcion-brand-book) caused two losing-race collisions in one session-startup sequence: (1) `/session-start` Step 0.5 mtime guard passed clean (DELTA=0) at command entry, then a foreign session wrote a redirect line to `logs/session-notes.md` during the Mandate Confirmation wait (Step 2), so the operator's `y` arrived against an echoed mandate that no longer matched the on-disk file; (2) `/session-plan` Step 0 mtime guard found `session-plan.md` ~25 hours old (clean), then between Step 0 and the Step 7 Write, the foreign session's `/session-plan` rewrote `session-plan.md` with its own intent — the collision only surfaced when my Write tool errored with "File has been modified since read." Both checks are point-in-time at command entry and cannot see writes that arrive after the check but before the command's own write. Operator explicitly attributed the trigger to deliberate concurrent-session use ("It is because I start concurrent sessions"). Recurrence risk: every concurrent-session use in any project; the 2026-05-25 14:10 friction entry shows this is the third recurrence of the same TOCTOU race class, each time prompting a one-off fix to one command (`/session-plan` Step 0 collision detection was the previous fix) without addressing the underlying class. Trigger: any command that (a) reads a shared log file at entry, (b) makes a decision based on that read, (c) writes back to the same file after some elapsed turns. Today's incidents added a third loss: the parallel session also overwrote the entire `## 2026-05-28` mandate block in `logs/session-notes.md` — my session's mandate vanished from the file of record. Files affected today: `logs/session-notes.md`, `logs/session-plan.md`. Proposed structural fix logged in `improvement-log.md` 2026-05-28 entry (broader entry supersedes the narrow same-day `/session-start` Step 0.5 entry written earlier in this session).
  - **Cross-ref:** logs/improvement-log.md — "2026-05-28 — Concurrent sessions cause TOCTOU races on shared log files" (phased marker approach, structural fix proposal). Targeted in wave 3 of fix-plan 2026-05-28-1902.
  - **Resolved 2026-07-12 — THIS ENTRY ONLY (the TOCTOU-on-shared-logs instance), NOT the class.** The phased marker approach shipped: session writes are now marker-scoped (`## YYYY-MM-DD — Session S{N}` + per-session-id marker files), so each session writes only under its own header and the losing-race collisions described above are structurally impossible for `session-notes.md` / `session-plan-*.md`. Shipped across `93c6cdc` (`detect-concurrent-session.sh` per-id liveness signal), `f5e013c` (`check-foreign-staging.sh` PreToolUse staging tripwire), `04d33e7` (`cc-worktree.sh` default worktree launch), `dd44220` (P1–P3 coverage + settings dedup); contract documented in `docs/session-marker.md` and consumed by `/prime` ("TOCTOU Phase 2+3 atomic"). Closure applied via `/fix-repo-issues` plan `audits/fix-plans/fix-repo-issues-2026-07-12-2132.md`.
  - **The class remains open — do not read this stamp as closing it.** Per the 2026-06-09 (S3) entry below, each instance of the parallel-sessions-mutating-shared-state class has received a *narrow* one-off guard patch, and the class persists because every new way two sessions can touch shared state is a new unguarded surface. The **shared git staging index** is the surface still only partially guarded (`check-foreign-staging.sh` fails open for footprint-less sessions). Those items are parked as a cluster for a dedicated structural session, not batched piecemeal — see the fix plan's Parked section.

#### Write Activity

## Session — 2026-05-28 14:20

### Friction Events

- **14:20** — `/wrap-session` Step 3.5 foreign-session pre-write guard false-positive on auto-mode chained tasks. Today's session ran `2,3 auto` from `/prime` (two tasks chained under auto-mode), so `/prime` Step 8c.3 fired twice and legitimately added 2 today-headers to `logs/session-notes.md`. The guard's `PRIME_RAN=1` model assumes single-task `/prime` → `/session-start` → wrap and computes `FOREIGN = ADDED_HEADERS - PRIME_RAN`, yielding `FOREIGN=1` on a clean own-write state. Investigation confirmed no foreign content (HEAD has the 5 morning today-headers I claimed; the 2 added today are both this session's auto-mode entries; concurrent terminals are active but in `projects/nordic-pe-screening-project/` and `projects/nordic-pe-macro-landscape-H1-2026/`, not in `ai-resources/`). Trigger: any `/wrap-session` after an auto-mode N-task chain (`N,M auto` / chained re-entry) under `/prime` Step 8c. Recurrence risk: every multi-task auto-mode session. Fix direction: make the guard chain-aware — track auto-mode task count in `.prime-mtime` payload (or a sibling marker) and use it as the per-signal subtractor (`FOREIGN_HEADERS = ADDED_HEADERS - PRIME_TASKS`). Today's run was overridden by operator after the false-positive was verified inline (HEAD + mtime + ps audit). Files touched by the override: `logs/session-notes.md` (this session's task-2 + task-3 mandates), `logs/friction-log.md` (this entry).

**Deferred 2026-05-28 (Wave 3 re-evaluation gate after id-31 Phase 1 landed).** The proposed marker-as-counter fix has a hidden security regression: `logs/.session-marker` is a SHARED file — concurrent sessions both bump it. If session A runs 1 `/prime` and parallel session B runs 1 `/prime`, the marker reads `S2` → `PRIME_TASKS=2` → `FOREIGN = ADDED_HEADERS - 2 = 0` → A's `/wrap-session` silently ships B's content under A's commit. This is the exact failure mode the guard exists to prevent. The current `PRIME_RAN=1` binary correctly STOPS in this case. Counting "this session's tasks" cleanly requires session-scoped state — which lands in id-31 Phase 2 (marker-scoped session-notes headers `## YYYY-MM-DD — Session {marker}`). After Phase 2, the count is exact: grep for THIS session's marker. Deferred to land with id-31 Phase 2.

**[FADING-GATE] verified 2026-06-05 (S15) — RESOLVED by the deferred path; no new edit.** id-31 Phase 2 (marker-scoped headers) has since landed, and `wrap-session.md` Step 3.5 now runs the marker-aware own-subtraction it predicted (lines 168–197): `OWN_WT_HEADERS` matches this session's `## YYYY-MM-DD — Session {MARKER}` header (chained auto-mode tasks all append under that ONE header → `FOREIGN_HEADERS=0`), and the `in_own` awk block (lines 178–184) counts every `**Mandate:**` under the own header as own (`OWN_MANDATES_SUBTRACT=N` → `FOREIGN_MANDATES=0`). The chained-task false-positive this entry reported can no longer occur on the marker-aware path. The /fix-repo-issues 2026-06-05 plan re-surfaced this as id-05 ("edit Step 3.5") on the stale pre-Phase-2 premise; a diagnosis-first check (SO second opinion, risk-check PROCEED-WITH-CAUTION) confirmed already-resolved and the plan's edit was NOT applied — editing a High load-bearing detector on a resolved premise would have risked regressing existing branches + drifting the workspace-root paired copy. Distinct from the still-deferred "wrap-lite no-own-marker CONCURRENT" remediation (improvement-log id-05/wrap-lite) — that is a different concern and stays open.

#### Write Activity

## Session — 2026-06-04

### Friction Events

- **[wrap-collector]** wrap (S4) — **Friction type: command.** `/prime`'s Step 1a Next-Steps git cross-check (prime.md L42–48) scans only `$CWD_REPO` + `$AI_RESOURCES`, so it cannot see commits in sibling *project* repos. In S4 the `/prime` menu surfaced items 2 and 3 as still-open when concurrent same-day sessions S1/S2 had already done and committed that work in the `projects/strategic-os/` repo. The stale menu was caught at the approval gate by the context-discovery engine, not by `/prime` itself — so the menu reduction (5 items → 1+6) cost a gate-time conflict check instead of being correct at prime time. The line-48 "dual-repo Cluster A blindspot" rationale already names the two-repo limit but stops at cwd+ai-resources; it does not extend to a third sibling project repo where parallel-session work lands. Routed to improvement-log 2026-06-04 ("/prime Step 1a … not sibling project repos") with the fix direction. — **Resolved:** 2026-07-03 — fix shipped in commit `e17cb16` (`/prime` sibling-repo cross-check); verified live in `prime.md` (sibling-repo scan present, 4 references).

- **[wrap-collector]** wrap (S6) — **Friction type: process.** An auto-multi-item bundle (items 1,2,3,5) included item 3 "review the System Owner reference files" with no concrete deliverable defined — no staleness check, quality pass, or consistency-against-live-SO target. The bundle was flagged heterogeneous + partly-structural at the gate and the operator chose the full set; Stage-0 `/risk-check` then returned RECONSIDER and the bundle was reshaped, with item 3 producing no output and being deferred to Next Steps to "define a concrete deliverable before running." The friction is that a menu item with no specifiable done-condition entered the executable bundle in the first place, where it consumed gate + risk-check attention before being recognized as unscoped. Direction (collector does not propose the fix): the signal is that auto-multi-item picks would benefit from a per-item done-condition presence-check before the bundle is presented for approval. No content lost; transparently surfaced.
  - **[FADING-GATE] verified 2026-06-08 — fix shipped in 2026-06-05 friday-act sweep (prime Step 8c.1.5 done-condition gate).**

#### Write Activity

## Session — 2026-06-05 (S6)

### Friction Events

- **S6 wrap** — **Concurrent-session collision, recurrence ~5th of the same class.** While S6 ran a friday-act implementation sweep, a parallel session implemented "item 5" (date-qualify session-plan filename) in the SHARED ai-resources working tree, editing `contract-check.md`, `session-plan.md`, `docs/session-marker.md`, and `prime.md`, then committing them (`fa2b3f2`, `78b1a8b`) mid-S6. NO guard fired. Two distinct gaps surfaced:
  - **(a) Guards are session-notes-scoped; blind to shared command/doc files.** Every concurrent-session guard (`/session-start` Step 0.5 mtime, `/wrap-session` Step 3.5 header/mandate counts, `detect-concurrent-session.sh`) watches `logs/session-notes.md` only. Nothing watches `.claude/commands/` or `docs/` for foreign writes. The genuine collision file was `prime.md` — S6 committed sub-step 1.5 (`1d91723`), the foreign session re-edited it (built on top, then committed). Lost-update risk class. Routed to improvement-log via `/resolve-repo-problem` ("Concurrent-session guards blind to foreign writes on shared command/doc files").
    - **[FADING-GATE] verified 2026-06-08 — fix shipped in commit 2bc89d9 (S13 id-15 advisory-scan extension).**
  - **(b) Partial marker setup → NO_OWN_MARKER guard would have false-positived.** S6's marker setup wrote only the shared `logs/.session-marker`, not the per-id `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}`. At wrap, the Step 3.5 NO_OWN_MARKER rule (built for genuine no-marker sessions) would have flagged S6's own header as FOREIGN. Caught by manual FOREIGN=0 verification — near-miss. Routed to improvement-log (guardrail-candidate, med).
    - **[FADING-GATE] verified 2026-06-08 — fix shipped in commit dd618d4 (wrap-session Step 3.5 NO_OWN_MARKER hardening).**
  - **Recurrence framing:** this is the same root class as 2026-05-25 14:10, 2026-05-28 10:05, 2026-05-28 14:20, and 2026-06-04 S4 — parallel sessions mutating shared repo state. Each instance has received a NARROW one-off guard patch (TOCTOU Phase 2+3, per-id markers, NO_OWN_MARKER, date-rollover grace, chain-aware counts). The class persists because every new way two sessions can touch the same state is a new un-guarded surface. Files affected today: `prime.md`, `contract-check.md`, `session-plan.md`, `docs/session-marker.md` (foreign, committed by the other session); `logs/session-notes.md` (S6 — clean, no foreign content). No data lost; explicit-path staging + manual verification contained it.

#### Write Activity
- S6 staged ONLY its own log files by explicit path (`session-notes.md`, `decisions.md`, `improvement-log.md`, `session-plan-S6.md`); all foreign-modified files left untouched.

## Session — 2026-06-05 (S7)

### Friction Events

- **S7 (date-qualify completion)** — **Consumer-inventory under-count on a structural rename, recurrence 3rd+ of the same class.** While completing the date-qualify session-plan-filename change, THREE independent passes each missed the same runtime consumer, `wrap-session.md` (both paired copies), an exact-path plan reader at its Step 6.4 mandate-resolution chain. The misses: (1) the friday-act plan that scoped the change named only 3 files (`session-marker.md` + `prime.md` + `session-plan.md`); (2) the `/risk-check` reviewer's consumer inventory listed 14 consumers but `wrap-session.md` was not among them; (3) my own implementation grep did not surface it. The miss was caught only because I read the pre-existing `id-41` improvement-log entry, whose S6 hand-built inventory *had* flagged `wrap-session.md` L321. After my rename, that reader would have silently resolved to "plan not found" (it tolerates plan-absence with no error) — a silent degradation, not a loud break. Same root class as the 2026-05-29 pre-spec-inventory entry and id-40 (2026-06-05, "pre-spec consumer-inventory grep checklist") — structural renames keep shipping with incomplete consumer inventories.

  **Why this keeps happening — two compounding root causes:**
  - **(a) Grep on the templated form misses files that spell the placeholder differently.** A consumer-inventory grep for `session-plan-${MARKER}` does NOT match `wrap-session.md`, which writes the literal `session-plan-{MARKER}.md` (no `$`). Across the codebase the same placeholder is written at least four ways — `${MARKER}`, `{MARKER}`, `{marker}`, `$MARKER` — so any grep keyed to one spelling silently skips the others. This is the mechanical reason grep-based inventories under-count even when run carefully.
  - **(b) The authoritative registry was itself incomplete and misclassified.** `docs/session-marker.md` § Two-end contract registry — the document whose stated job is "every place the marker contract is consumed must point back to this doc" — did NOT list `wrap-session.md` at all, and filed the runtime hook `backup-session-plan.sh` under "Doc references (narrative, not consumers)" when its line-20 regex is load-bearing (a non-match silently stops plan backups). So the one source of truth that should have caught all three passes was stale, and each actor fell back to independent greps (root cause a).

  **Prevention applied this session (structural):**
  - Added `wrap-session.md` (both paired copies) to the registry's Read-only auxiliary consumers; created a new "Runtime non-command consumers" class and moved `backup-session-plan.sh` (+ project-local copies) into it, flagged as load-bearing-parse with a lockstep-update requirement.
  - Switched all exact-path plan readers (`contract-check.md`, `drift-check.md`, both `wrap-session.md` copies) from constructing `session-plan-${MARKER}.md` to the glob `session-plan-*${MARKER}.md`, which matches both the new date-qualified and the old bare-marker forms — so a future format change cannot silently break the read.

  **Prevention proposed (routed to improvement-log, strengthens id-40):** a pre-spec consumer-inventory for any path rename must (1) grep the INVARIANT filename stem (`session-plan`), never the templated form, so placeholder-spelling variance cannot hide a consumer; and (2) reconcile the grep result against `docs/session-marker.md` § registry, adding any consumer the grep found but the registry lacked (and vice-versa) BEFORE the rename spec is written. id-40 currently routes the grep into the spec's affected-file list but does not mandate the invariant-stem rule or the grep↔registry reconciliation — both gaps let this recurrence through. — **RESOLVED 2026-06-05 (S17):** both proposed clauses are now mandated in `skills/ai-resource-builder/SKILL.md` § Consumer-Inventory Gate (L367 invariant-stem grep "never the templated form"; L369 bidirectional registry reconcile against `docs/session-marker.md`), shipped in commit `afad146`. The Gate fully absorbs this proposal; no further action.

#### Write Activity
- S7 amended its own Stage-4 commit set: fixed `wrap-session.md` (both copies), `heavy-read-discipline.md`, and the `session-marker.md` registry after the id-41 cross-read exposed the missed consumers. All edits are to files S7 itself is changing this session; no foreign-modified files touched.

## Session — 2026-06-05 (/fix-project-issues, 2nd run)

### Friction Events

- **Diagnostics scan over-reports because candidates are built from dated audit reports, not reconciled against live file state — recurrence 2nd of the same class TODAY.** A fresh `/fix-project-issues ai-resources` run surfaced 23 candidates; SO vetting + live-state verification collapsed them to ONE clean applied item (id-08). Of the rest, **4 were already-done / no-op** (id-07 = morning id-25 applied f0959f7; id-09 = morning id-26 applied, no R-codes remain; id-05 = S11 grounding fix; id-12 = canonical conflict with the retired `permission-template.md:141` deny). The candidate scanner reads the latest *dated* diagnostic reports (`claude-md-audit` 2026-06-01, `token-audit`/`friday-checkup` 2026-06-05) and the backlog logs, but those reports were generated **before** same-day commits (`f0959f7`, `e1a60d6`/`7a0ddd4`) moved the underlying files — so the scan re-surfaced findings that live state had already resolved. The earlier run today (`b18d212`, morning /diagnostics-plan) hit the identical pattern: 6 of 7 Do-now items dissolved on the same reconciliation, with the same root cause logged as a standing caveat.

  **Why it recurs:** the diagnostics-scanner has no live-state reconciliation step. It normalizes/dedupes candidates against each other and against prior improvement-log dispositions (which I passed in this run, and which correctly suppressed the morning batch's id-06/id-22/id-37), but it does NOT diff each candidate against the current on-disk file before ranking it actionable. The dated report is treated as ground truth; on a fast-moving same-day repo the report is stale by hours. The reconciliation that catches it currently lives downstream — in SO vetting + my manual live-read — which is correct as a safety net but means every run pays the cost of vetting dead candidates before culling them (the morning run's logged "redundant-vetting detour").

  **Direction (not a fix — for the Friday cadence to weigh):** the standing caveat is now logged twice in one day (`b18d212`, this run). A structural option worth considering: add a cheap live-state reconciliation pass to the diagnostics-scanner (or a /fix-project-issues pre-SO step) that, for each candidate citing a specific file+line or a specific rule/dup, re-reads the live file and drops the candidate if the cited condition no longer holds — culling already-applied items BEFORE they reach SO vetting, raising batch signal-to-noise. Cost: one extra read per file-specific candidate; benefit: the SO and main session stop spending attention on dead findings on every same-day re-run. No content lost this session; reconciliation worked, the detour was the only cost. — **[FADING-GATE] verified 2026-06-05 (S14):** Resolved at the command layer (NOT the scanner layer the Direction proposed — the scanner is read-only with no git) by the reconcile-at-read primitive `docs/backlog-reconciliation.md` + `fix-project-issues.md` Step 2.5 + `fix-repo-issues.md` Step 3.0, all shipped commit `23c9143`. See `logs/improvement-log.md` § 2026-06-05 diagnostics-lag RESOLVED entry.

#### Write Activity
- This session staged only its own work by explicit path: `CLAUDE.md` (id-08), `logs/improvement-log.md` (dispositions), the risk-check report, and `logs/friction-log.md` (this entry). The pre-existing uncommitted `logs/improvement-log-archive.md` (S10 leftover) was left untouched — not this session's file.

## Session — 2026-06-05 (S15)

### Friction Events

- **[wrap-collector]** wrap (S15) — **Friction type: config.** The `qc-reviewer` subagent failed on three consecutive model-override attempts (opus/sonnet/haiku) with "Usage credits required for 1M context." The harness forces 1M context onto all subagents; when a 1M-context session's credits are exhausted by earlier subagent calls (risk-check + system-owner), no further subagent can spawn for the remainder of the session regardless of per-dispatch model overrides. The QC-independence gate was silently unreachable with no pre-dispatch warning. Self-QC was used as fallback and caught a real gap (8c.1 parser); no content lost. The friction class: session-level 1M-credit exhaustion silently blocks any subagent-dependent gate (QC, outcome-check, feedback-collector) with no proactive warning before the gate is exercised. Routed to improvement-log 2026-06-05 ("QC-independence gate unreachable when 1M-context credits exhausted at session level") as a guardrail-candidate (low severity). — **Resolved:** 2026-07-03 — closed by commit `f5f1448` (stop subagent 1M-context credit-gate stalls) + the documented QC fallback posture (`qc-pass.md` Step 3a, commit `3a2b428`); verified live.

#### Write Activity

## Session — 2026-06-09 (S3)

### Friction Events

- **[wrap-collector]** wrap (S3) — **Friction type: process (concurrent same-checkout, shared staging index).** Two sessions (S3 + a parallel S4) ran in the SAME `ai-resources` checkout throughout S3, sharing one git staging index, which produced two real cross-session commit-entanglement events (not pure near-misses — the index clobbers actually occurred; no data lost): (1) S3's `git commit --amend` swept S4's already-staged `claim-permission.template.md` into S3's commit — caught only by a post-commit `--stat` read, then corrected by soft-reset + unstage + recommit, with the foreign change preserved in the working tree; (2) S4's commit `ebe7301` later swept S3's staged concurrent-session fix-plan file and the S3 mandate into S4's commits — detected, content verified preserved, deliberately not disentangled (rewriting shared history while a concurrent session commits is itself dangerous). This is the live instance of the highest-priority anti-pattern the existing plan-only fix targets. The improvement direction is already captured: `audits/2026-06-09-concurrent-session-isolation-fix-plan.md` (build order: staging-index guard → block same-checkout → wrap-owns-logs → default worktree launch → per-session log namespacing), plus the active improvement-log entry "2026-06-09 — Mid-session commit can stage a sibling session's session-notes.md content" — so this is logged here as the recurrence datapoint only, NOT re-routed to improvement-log (root-cause duplicate of an active entry + an existing plan). Recurrence framing: same root class as 2026-05-25 14:10, 2026-05-28 10:05/14:20, 2026-06-04 S4, 2026-06-05 S6 — but this is the first instance where the SHARED STAGING INDEX (not just shared log files) was the clobber surface, broadening the documented collision surface from edit-in-place logs to arbitrary staged files. Containment this time: post-commit `--stat` verification + soft-reset correction; the precise staging-index guard remains plan-only and unbuilt.

#### Write Activity
- S3 staged its own work by explicit path; the two index entanglements above were corrected (event 1) or verified-preserved-and-left (event 2). Mixed S3/S4 commit attribution deliberately not disentangled — all content preserved.

## Session — 2026-06-09 (S5)

### Friction Events

- **[wrap-collector]** wrap (S5) — **Friction type: process-safety (subagent destructive write to an append-only shared log).** During `/wrap-session` Step 6.5, the `session-feedback-collector` subagent destructively OVERWROTE `logs/improvement-log.md` with the literal text `placeholder` — a `Write` (whole-file replace) where an append-only `Edit`/heredoc was intended. The subagent itself detected the error post-write, correctly refused to reconstruct from partial context, and surfaced the exact restore command. The main session restored losslessly via `git restore --source=HEAD -- logs/improvement-log.md` (the file was provably clean == HEAD at session start — confirmed by the `/prime` Step 1a FOREIGN_SHARED check and the session-start git status — so HEAD restore lost nothing), then manually re-appended the one genuine signal the collector intended (the fail-open guardrail-candidate, now in improvement-log). Root cause: the collector's write path can emit a full-file `Write` instead of an append; `docs/commit-discipline.md § Shared-log write-path integrity` already names this hazard class for the non-append logs but the guard there targets read-during-rewrite truncation, not a bare placeholder overwrite. Improvement direction: harden `session-feedback-collector` to append-only (never `Write` the whole file) and/or add a pre-write entry-count floor check like the one commit-discipline.md prescribes for `/improve`. Routed to improvement-log? NO — logging here as the incident datapoint; the collector-hardening proposal is a candidate for `/improve` to formalize against this entry. Contained: no data lost; file fully restored + re-appended. — **Resolved:** 2026-07-03 — collector hardened to append-only in commit `0ee6177` (Constraint E: whole-file rewrites categorically forbidden, no `Write` tool); verified live in `session-feedback-collector.md`.

#### Write Activity
- `logs/improvement-log.md`: overwritten to `placeholder` by the collector, then restored from HEAD (459 lines) and appended with one guardrail-candidate entry (now 469 lines). No other shared log affected — `friction-log.md` was read-only to the collector and is intact. — **Resolved:** 2026-07-03 — recovery completed in-session; root cause closed by `0ee6177` (see event entry above).

## Session — 2026-06-10 (S1)

### Friction Events

- **[wrap-collector, hand-appended after incident]** wrap (S1) — **Friction type: process-safety (subagent destructive write to an append-only shared log) — SECOND OCCURRENCE.** During `/wrap-session` Step 6.5, the `session-feedback-collector` subagent destructively OVERWROTE `logs/friction-log.md` with a whole-file `Write` (truncated to the 1-line header) where an append-only `Edit`/heredoc was intended, and also created a stray `.append-marker-tmp` file. The subagent detected the error post-write, correctly refused to reconstruct from partial context, and surfaced the exact restore command. The main session restored losslessly via `git restore --source=HEAD -- logs/friction-log.md` (file was provably clean == HEAD at session start — the /prime Step 1a FOREIGN_SHARED check found it clean), removed the stray tmp, and hand-appended the collector's intended signals. **Recurrence:** this is the SAME hazard class as the 2026-06-09 (S5) collector incident, which overwrote `logs/improvement-log.md` with `placeholder`. Two occurrences in two consecutive substantive sessions, different target files (improvement-log then friction-log) → the collector write-path hazard is now a confirmed pattern, not a one-off. Escalated to a dedicated improvement-log entry this session (collector-hardening to append-only). Contained: no data lost; file fully restored. — **Resolved:** 2026-07-03 — same hazard class as 2026-06-09 (S5); closed by `0ee6177` append-only hardening (Constraint E); verified live.
- **[wrap-collector, hand-appended]** wrap (S1) — **Friction type: process/spec (plan-time design assumption not validated against platform capabilities).** The concurrent-session isolation fix-plan (`audits/2026-06-09-concurrent-session-isolation-fix-plan.md`) baked an un-buildable premise into Fix 1 — "upgrade `detect-concurrent-session.sh` to a *blocking* SessionStart decision." A SessionStart hook CANNOT block a session (exit 2 only shows stderr; the session continues — verified vs Claude Code hooks docs this session). This was checkable from the docs at plan time, but was only caught at build time after `/risk-check` gating + a system-owner second opinion, forcing a mid-session operator re-scope (block → soft precision-fix). No waste beyond the diagnosis cost (the re-scope landed a genuinely valuable fix), but the fix-plan's remaining steps should be re-checked against hook-event capabilities before each build. Logged as the datapoint; the surviving fix-plan steps (Fix 3, Fix 4) do not currently assert a block at a non-blocking event, so no re-route.

#### Write Activity
- `logs/friction-log.md`: overwritten to 1 line by the collector, then restored from HEAD (175 lines) and appended with these two entries. Stray `.append-marker-tmp` removed. `logs/improvement-log.md` was read-only to the collector and intact; appended by hand with two entries (workspace-root Step 13 port gap; collector-hardening recurrence). — **Resolved:** 2026-07-03 — recovery completed in-session; collector root cause closed by `0ee6177`; the Step 13 port gap separately verified already live in the workspace-root `wrap-session.md` (per-id teardown present at its final step).

## Session — 2026-06-10 (S3)

### Friction Events

- **[wrap-collector]** wrap (S3) — **Friction type: process (missing pre-build environment-fit check before building operator-environment-specific tooling).** S3 built `scripts/cc-worktree.sh`, a terminal worktree launcher (Fix 3 of the concurrent-session isolation fix-plan), through the full gate chain (/risk-check PROCEED-WITH-CAUTION + SO concur + /qc-pass GO) and committed it across 3 repos. Only AFTER the build did the operator challenge the solution, surfacing the load-bearing fact that **he launches Claude via the VS Code extension, not a terminal** — so the launcher shipped inert for his actual workflow (zero functional value). The session's own outcome check (EXECUTION: ACCEPTABLE, "Better path") names the cheap miss: one pre-build fit question ("terminal or VS Code launch?") would have surfaced the mismatch before any build. The launcher was correctly left in place (rollback is its own risk; harmless building block) and the VS Code launch fact was written to auto-memory (`feedback_vscode_launch.md`) to prevent recurrence. Mitigating: option (b) was an explicit operator `go`, and the wasted-build cost is partly mandate-inherited (the fix-plan framed Fix 3 as a terminal launcher). The improvement direction (a pre-build environment-fit check upstream at /scope or /session-plan) is routed to improvement-log this session as a session-feedback entry; logged here as the friction datapoint. No data lost.

#### Write Activity
- S3 staged its own work by explicit path across 4 commits / 3 repos; no foreign files touched (`### Risky actions` = None — QC-clean, risk-check-gated, re-syncs byte-identical). The two pre-existing S4 foreign working-tree files were left untouched and unstaged.

## Session — 2026-06-12 (S4 cont.)

### Friction Events

- **[wrap-collector]** wrap (S4 cont., /log-sweep) — **Friction type: hook + process.** `check-foreign-staging.sh` blocked S4's ai-resources commit 3x. Root cause: S4 started via `/log-sweep` (no `/prime`), so it wrote no per-id marker `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}`; the guard fell back to the shared `logs/.session-marker` that a concurrent session (settings.json edits) kept rewriting. The marker fix (writing the missing per-id marker, not a `-f` override) was correct and structural, and zero sweep rework occurred — but the missing-per-id-marker root cause was diagnosable on retry 1 (same root-cause family as S1/S2 earlier today), so retries 2-3 were avoidable thrash. Direction (collector does not propose the fix): generalize per-id marker establishment to non-/prime session-start paths — routed to improvement-log this session as a guardrail-candidate (low). No content lost; explicit-path staging held throughout.

#### Write Activity
- S4 cont. (/log-sweep) staged its own work by explicit path across 2 commits / 2 repos (e1d22ca marketing-positioning, bffdd95 ai-resources); no foreign files touched. Concurrent session's settings.json edits left untouched and unstaged.

## Session — 2026-06-12 (S8)

### Friction Events

- **[wrap-collector]** wrap — `/resolve-improvement-log` classification rule forced mid-run operator adjudication. Type = command (rule/convention mismatch). The command's strict resolved test (`Status: applied` AND `Verified:`) matches zero real entries while the log's done entries use the `resolved YYYY-MM-DD` convention, so the operator had to hand-select which of the 12 candidates were genuinely resolved. Improvement face routed to improvement-log.md (same root cause, session-feedback category). — **Resolved:** 2026-07-03 — `/resolve-improvement-log` Step 3 widened to the two-tier rule (tier 2 `resolved YYYY-MM-DD` convention) in the 2026-06-12 S9 fix set; verified live in the command.

## Session — 2026-06-29

### Friction Events

- **[wrap-collector]** wrap — **Friction type: command (`/consult` input-corpus resolution).** The second `/consult` on the `/requirements-pack` build plan returned two BLOCKING findings (B1/B2) grounded in the wrong directory: the `system-owner` read `knowledge-bases/strategic-os/` (Obsidian KB vault) instead of the plan's named corpus `projects/strategic-os/` — two existing dirs sharing the `strategic-os` name. The false blockers were caught only by manual filesystem verification of the SO's input-contract claims, not by any gate; B1/B2 were then overridden on verified evidence and the two valid minor fixes (I1/I2) applied. Guardrail face routed to improvement-log.md 2026-06-29 (same root cause, guardrail-candidate, severity low) — consult-time corpus-name-collision disambiguation. — **RESOLVED 2026-07-03 (S7):** `/consult` Step 3.5 input-corpus disambiguation shipped (canonical commit `68bd57a`, reachable from both question shapes; pair-applied to the axcion-ai-system-owner fork, commit `a51729e`). Improvement-log 2026-06-29 entry stamped resolved.

## Session — 2026-07-02

### Friction Events

- **Friction type: setup + curation (canonical command library unreachable from a project session; workspace-root command dir is stale).** Operator reported that ai-resources slash commands did not appear in a session opened in `projects/axcion-design-studio`. Root cause: Claude Code loads slash commands only from `<project-root>/.claude/commands/` (plus `~/.claude/commands/`); the project's `.claude/` had `agents/` + `settings.json` but no `commands/` dir, and ai-resources is attached via `--add-dir`, which grants file access only and does NOT register commands. The parent workspace-root `.claude/commands/` is not scanned either (it is the project's parent, not its root). Fixed by adding a `projects/axcion-design-studio/.claude/commands` symlink → `../../../ai-resources/.claude/commands` (canonical 84-command library), committed `5a8da74`. New projects scaffolded under `projects/` will hit the same gap unless the project-init template creates this symlink — the design-studio init commit `ac35a29` created `.claude/agents/` + `settings.json` but not `.claude/commands/`.
  - **Secondary finding — workspace-root command dir is a stale curated subset, not a superset of canonical.** Pointed the fix at `ai-resources/.claude/commands` (84) rather than mirroring the workspace-root `.claude/commands` (63) because the root set is missing 27 canonical commands, including several the project's own CLAUDE.md mandates (`/placement`, `/blindspot-scan`, `/contract-check`, `/session-plan`). Separately, the root dir holds content that is NOT in the canonical library: 5 **real command files** that exist only there — `harness-start.md`, `run-qc.md`, `session-report.md`, `update-md.md`, `validate.md` — plus a redundant alias `resolve-improvements.md` → `resolve-improvement-log.md`. So neither location is a superset of the other, and any project pointing at the canonical library silently loses those 5 root-only commands. Direction (not fixing this session — shared workspace state, out of scope for the project fix): reconcile the two dirs — migrate the 5 real root-only command files into the canonical `ai-resources/.claude/commands/` (or confirm they are intentionally deprecated and delete), so the canonical library is the true superset and per-project symlinks are complete. No content lost; project fix committed and verified live in-session.

## Session — 2026-07-02 (S1)

### Friction Events

- **Friction type: harness (canonical ai-resources-defined subagent type not registered in a project session).** Running `/risk-check` from a session opened in `projects/axcion-design-studio`, the command's Step 3 dispatch of the `risk-check-reviewer` subagent failed: the Agent tool returned `Agent type 'risk-check-reviewer' not found. Available agents: brand-guardian, claude, claude-code-guide, Explore, general-purpose, implementation-bridge, layout-architect, Plan, statusline-setup, visual-red-team`. Only the four project-local Studio agents (+ built-ins) resolve as agent types; the canonical review agents live in `ai-resources/.claude/agents/` and are attached via `--add-dir`, which grants **file access only** and does NOT register agent types into the project session. Worked around by running the reviewer via `general-purpose` with the `risk-check-reviewer.md` definition read from disk and inlined into the prompt — same result (verdict GO, structurally valid report at `audits/risk-checks/2026-07-02-land-axcion-design-studio-workflow-engine-structural.md`), but the fallback is manual and the unmodified `/risk-check` command would fail its spawn step identically.
  - **Same root-cause family as the unmarkered 2026-07-02 entry above** (`--add-dir` grants file access but does not register commands — here, does not register *agents*). The commands gap was fixed with a `.claude/commands` symlink (`5a8da74`); the agents gap is unfixed and affects **any canonical command that dispatches an ai-resources-defined subagent from a project session** — `/risk-check` (`risk-check-reviewer`), and likely `/qc-pass`, `/refinement-pass`, and others that spawn `qc-reviewer` / `refinement-reviewer` / etc.
  - **Direction (not fixing this session — shared/structural, needs an operator scoping decision):** (a) register the canonical review agents per-project (parallel to the commands-symlink fix) — but a blanket `.claude/agents` symlink would also expose unrelated canonical agents, and the project deliberately scopes `.claude/agents/` to its four Studio roles, so this is a scoping call the operator should make; or (b) give the canonical commands' spawn steps a documented fallback — when the named subagent type is unresolved, run `general-purpose` with the agent definition inlined (what was done manually here). Option (b) is self-contained per command and does not widen the project's agent surface. Routed here for the Friday cadence. No work lost; risk-check completed and gated the change correctly.
  - **Resolved 2026-07-12 via option (a), the manifest route — but note the scoping call below.** Root cause was narrower than the entry assumed: `auto-sync-shared.sh` (a SessionStart hook **already wired in design-studio's own `settings.json`**) bails when `.claude/shared-manifest.json` is absent. 17 of 20 projects carry that manifest; design-studio did not, so the sync mechanism sat dormant and the project got zero canonical agents. Fix was to add the manifest (commit `3e963fb`, design-studio repo), which activated the existing hook: **32 canonical agents symlinked**, the 4 Studio-local agents declared under `agents.local` and left untouched. Verified live — `risk-check-reviewer`, `qc-reviewer`, `triage-reviewer`, `refinement-reviewer` all resolve. This is self-maintaining: future canonical agents sync automatically, which hand-built symlinks would not have done.
  - **⚠ The scoping call this entry deferred to the operator has now been made — flagging it explicitly.** The entry states the project "deliberately scopes `.claude/agents/` to its four Studio roles," and warns that registration widens the agent surface. It does: design-studio now exposes 32 canonical agents + its 4 local ones. Justification: (i) all 19 sibling projects carry the canonical set, so the 4-agent scoping was far more likely a `/new-project` scaffold gap (see the "should register command/agent symlinks" improvement-log entry) than a deliberate choice; (ii) the hook's baked-in `EXCLUDE_AGENT_GLOBS` already withholds the ai-resources-meta agents (`pipeline-stage-*`, `pipeline-review-*`, `scope-*`, `session-guide-generator`), so the widening is bounded, not blanket; (iii) `/risk-check` returned **RECONSIDER on the hand-symlink approach** and recommended precisely this manifest route (`audits/risk-checks/2026-07-12-symlink-canonical-agents-into-axcion-design-studio.md`). **If the narrow scoping WAS deliberate, this is reversible:** delete `projects/axcion-design-studio/.claude/shared-manifest.json` and the 32 symlinks, then take option (b) instead.

## Session — 2026-07-02 (operator-flagged — /tech-consult not integrated into project planning)

### Friction Events

- **Friction type: curation + wiring (built-but-unwired command — the "build a lot → forget to use" thesis in miniature).** Operator asked whether `/tech-consult` and `/requirements-pack` are integrated into the project-planning pipeline. Verified against files: **`/requirements-pack` is fully wired** — a registered Stage-1 intake in `projects/project-planning/CLAUDE.md` (one of three context-pack front doors alongside `/context-builder` and `/scope-project`), feeding `intake → /plan-draft → /plan-refine → /plan-evaluate → /new-project`. **`/tech-consult` is an orphan** — it exists as a canonical command copied into ~11 projects, but the project-planning `CLAUDE.md` never names it (not an intake, not a Stage 0), its own definition has no handoff (it "moves through the altitude ladder and produces staged outputs" that land only in the conversation, not a `/plan-draft`-ready context pack), and the only reference to it anywhere in the workspace is one passing mention in `projects/axcion-website/pipeline/repo-snapshot.md`. Net: `/tech-consult` runs only if the operator remembers it and hand-carries its output into planning — nothing downstream consumes it.
  - **Operator ask:** figure out how to properly integrate `/tech-consult` into project planning — OR, at minimum, add a reminder to use it during project planning so it stops being memory-dependent.
  - **Direction (not fixing this session — needs a design pass):** (a) full integration — make `/tech-consult` emit (or convert its output into) a `/plan-draft`-ready context pack and register it as an optional Stage-0/Stage-1 intake in `projects/project-planning/CLAUDE.md`, parallel to the slot `/grill-me` already occupies (turns the orphan into a fourth front door); or (b) minimum-viable reminder — add a pointer to `/tech-consult` in the planning intake section so it surfaces at project-planning time without a code change. New/changed command wiring = structural class → route the (a) path through `/risk-check` + a small design pass; do not bolt on ad hoc. Routed here for the Friday cadence.

## Session — 2026-07-02 (S3)

### Friction Events

- **Friction type: harness (staging-tripwire hook parser vs. the `/session-start` Files-in-scope separator convention).** During the S3 auto-mode commit in `projects/axcion-design-studio`, `check-foreign-staging.sh` blocked a legitimate 12-file commit **twice**. First block: correct behavior (footprint contained `work/homepage/ (new)` + an `(inferred)` marker — not concrete, and a live concurrent-session per-id marker was present). After declaring a fully concrete footprint, the second block was a **parser false positive**: the hook tokenizes the `- Files in scope:` value on whitespace, so the semicolon-separated entries the `/session-start` Step-3 convention produces (`path1; path2; …`) become tokens with trailing semicolons (`path1;`) that never string-match staged paths. Evidence: 11 of 12 staged files were flagged as outside-footprint; the only file NOT flagged was the list's final entry — the one path without a trailing semicolon. Any explanatory prose in the bullet is likewise word-split into junk tokens (visible in the hook's echoed footprint). Worked around by rewriting the mandate bullet space-separated with zero prose; commit then passed clean (`783278a`).
  - **Structural mismatch:** the canonical mandate-write convention (semicolon-separated, `(inferred)`/`(none stated)` markers, parenthetical annotations — per `/session-start` Step 3 and `/prime` Step 8c.7) and the hook's whitespace-split matcher disagree on the same line's format. Every future session that follows the documented convention AND commits while a concurrent-session marker is live will hit the same false block.
  - **Direction (not fixing this session — hook edit = structural class, needs `/risk-check`):** (a) make the hook's parser split on `;` and strip trailing separators/parentheticals before matching (accepts the documented convention — preferred: fixes the mismatch where it lives, one file); or (b) change the documented Files-in-scope format to space-separated paths everywhere (`/session-start`, `/prime` 8c.7, downstream parse-contract readers) — wider blast radius, touches the four-reader parse contract. Also note the stale-marker input: the "live concurrent session" signal that armed the tripwire was a per-id marker from the same-day S1 session that had already wrapped — wrap does not prune its own per-id marker, so the tripwire's highest-risk mode can be armed by a ghost. Routed here for the Friday cadence. — **[FADING-GATE] verified 2026-07-03 (S3):** Resolved via option (a) — `check-foreign-staging.sh:301` now splits on `re.split(r'[,;\s]+', footprint_raw)` (semicolon added as a delimiter alongside comma/whitespace). Verified against the exact incident footprint (`.claude/commands/prime.md; contract-check.md; session-plan.md; docs/session-marker.md`) — all four tokens now parse clean with no trailing separator; pre-existing comma/whitespace-only footprints regression-tested unchanged. `/risk-check` GO (all six dimensions Low; only one hook copy exists workspace-wide, no lockstep sync needed). The stale-marker note (ghost per-id markers arming the tripwire) remains a separate, un-fixed observation — not addressed by this change.

## Session — 2026-07-03 (/friday-checkup quarterly)

### Friction Events

- **Friction type: harness (concurrent-session detection blind to non-/prime session-start paths).** A live concurrent `/friday-checkup` ran in the SAME ai-resources checkout throughout this session and had already committed two audit artifacts (`claude-md-audit-2026-07-03-project-axcion-ai-system-redesign`, `permission-sweep-2026-07-03`) plus a `## 2026-07-03` session-notes wrap. Neither the SessionStart hook (`detect-concurrent-session.sh`) nor `/prime`'s Step-1a liveness check saw it, because the concurrent session started via `/friday-checkup` directly (not `/prime`), so it wrote NO per-id marker `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}` — the exact liveness oracle both detectors read. The collision was caught only manually, by noticing foreign 2026-07-03 audit artifacts mid-run (Check D guidance-file already existed). Contained: operator stopped the other session; this session resumed as sole owner, folded the 2 committed artifacts into the report, and staged only its own files by explicit path. Recurrence framing: same root class as the 2026-06-12 (S4 cont.) `/log-sweep` entry ("non-/prime session-start paths write no per-id marker") — that entry already routed the fix direction (generalize per-id marker establishment to non-/prime session-start paths). This is the same root cause surfacing on `/friday-checkup`; dedupe against that improvement-log entry rather than double-filing. No data lost.
- **Friction type: process (slow async subagent misjudged as stalled → premature TaskStop).** During the ai-resources `/token-audit`, the orchestrator's Section-4 `token-audit-auditor` subagent ran ~12 min. A mid-run file-check saw only stale (2026-05-18) workflow summaries and I concluded the async subagent had "stalled without writing," TaskStop'd the orchestrator, and began composing Section 4 as a documented gap. The subagent then COMPLETED with fresh, real findings (4 HIGH research-workflow relays), which were used — the premature-stall conclusion was wrong (slow, not stalled). No data lost (the real findings arrived and replaced the gap note; documented in the token-audit Section 10). Direction (not fixing this session): verify an async subagent against its completion signal (task-notification / output) before declaring it stalled or issuing TaskStop — a single mid-run scratch-file timestamp is not sufficient evidence of a stall. Candidate guidance: harness/subagent-dispatch note.

#### Write Activity
- Appended by hand at wrap (feedback-collection step run manually, not via `session-feedback-collector`, to avoid the collector's documented destructive-overwrite hazard against the uncommitted improvement-log appends this session made — see friction-log 2026-06-09 S5 / 2026-06-10 S1).

## Session — 2026-07-03 (Session S1: /friday-act triage)

### Friction Events

- **[wrap-collector]** wrap — **Friction type: process (mandate-formation vs command behavior mismatch).** The session mandate's done-condition read "the 4 critical permission-prompt fixes are applied ... and committed," but `/friday-act` by design never executes fixes inline — it produces dispositioned, risk-annotated plan files for later execution sessions. Nothing was applied/committed toward the 13 fix items this session; the session flagged the discrepancy transparently in chat. The mandate-forming step set a completion criterion the command cannot meet. Direction (collector does not propose the fix): mandate formation for `/friday-act` should read "triaged into executable plans," not "applied and committed," reflecting the command's plan-not-apply output. — **RESOLVED 2026-07-03 (S7):** output-contract note added to `friday-act.md` intro (commit `7e6b804`) stating the plans-not-apply contract and the mandate wording to use.
- **[wrap-collector]** wrap — **Friction type: command (non-deterministic active-entry count feeding the soft-cap gate).** The improvement-log soft-cap gate fired with an active-entry count reported as 46/57/62 depending on count method — a ~16-entry spread, so the gate's trigger (and the "over 7 active" Friday-execution block) rests on a non-deterministic total. Likely aggravated by a malformed `## id-39` entry (wrong header level — `##` instead of `###`) that a `grep '^### '` count silently under-counts (session Next Steps item 3). Direction (collector does not propose the fix): standardize one canonical active-entry count method across the `/friday-act` soft-cap gate and `/resolve-improvement-log`, and normalize the malformed header so the parser count is stable. — **RESOLVED 2026-07-03 (S7):** count method pinned in `friday-act.md` Step 1.7 (Status-line count only, never `^### ` headers; commit `7e6b804`) + the malformed `## id-39` header normalized to the dated `###` schema form (shared-log edit, wrap-owned). `/resolve-improvement-log` left untouched (S6-owned this session); its two-tier classifier is archival logic, not a count consumer — the load-bearing count gate is the one pinned.

## Session — 2026-07-03 (/open-items backlog scan → field-match fix)

### Friction Events

- **Friction type: process + git (concurrent same-checkout shared staging index; bare `git commit` over-includes the whole index — recurrence ~7th of this class).** During an `/open-items` fix session (three sessions live in the same `ai-resources` checkout: this one + S2 permissions + S3 collector-hardening), the single-file fix to `.claude/commands/open-items.md` was staged by explicit path (`git add .claude/commands/open-items.md`) per the concurrent-session staging discipline — but the follow-up **bare `git commit` (no pathspec) committed the entire shared index**, sweeping in two files a concurrent log-sweep/archival session had already staged: `logs/improvement-log-archive-archive-2026-05.md` (387 L) and `logs/maintenance-observations-archive-2026-05.md` (109 L). Both landed under commit `9660bf2` whose message names only the open-items fix. **Root cause:** explicit-path `git add` scopes what *this* session *adds*, but `git commit` with no pathspec commits *everything staged in the shared index* — including a sibling session's mid-flight staging. The staging discipline's explicit-path rule covers the `add` verb but the `commit` verb re-widens to the whole index; the two halves are not symmetric. **Containment (no disentangle):** the two swept files are log-rotation archives (`archive` in name, under `logs/`) — on the `check-foreign-staging.sh` exempt-list and classified benign / no-lost-update by `docs/commit-discipline.md` (a wrap commit legitimately stages a freshly-rotated archive); that is why the tripwire did not block. Content is complete in HEAD (whole-file creates, not truncations). Disentangling (`reset --soft` + recommit) was deliberately **declined** — a concurrent session was *actively* staging (the shared index gained 12 more files between the commit and the post-commit `git status`), so a HEAD rewrite mid-sibling-commit is the riskier move, matching the documented 2026-06-09 (S3) posture ("rewriting shared history while a concurrent session commits is itself dangerous"). No further commit issued this session (the index holds the sibling's in-progress log-sweep batch). **Recurrence framing:** same root class as 2026-06-09 (S3) shared-staging-index entanglement, 2026-06-05 (S6), and the earlier shared-log TOCTOU family — but the specific mechanism here is the `add`/`commit` pathspec asymmetry, not a shared-*file* overlap. **Direction (not fixed this session):** when concurrent sessions are live, scope the commit itself — `git commit -- <explicit-path>` (pathspec on `commit`, which commits only that path regardless of the rest of the index) — not `git add <path>` followed by a bare `git commit`. Candidate: make the pathspec-commit the documented default in `docs/commit-discipline.md § Concurrent-session staging discipline`, and/or have `check-foreign-staging.sh` flag a bare (pathspec-less) `git commit` while a foreign per-id marker is live even when the extra staged files are exempt (the exemption correctly suppresses the *foreign-file* block, but the over-inclusion into the wrong commit message is a separate, un-flagged failure). No data lost; misattribution only. — **Partially addressed 2026-07-03 (S7):** the pathspec-commit default is now documented in `docs/commit-discipline.md` § Concurrent-session staging discipline (cites the `9660bf2` incident). The hook-flag half (warn on bare commit while a foreign per-id marker is live) remains open. — **Resolved 2026-07-03 (S8):** the hook-flag half shipped. `check-foreign-staging.sh`'s `if not foreign:` branch now additionally checks for exempt-but-outside-footprint candidates while a live foreign per-id marker is present and the invocation is a `git commit`; if so it emits a non-blocking advisory (via the existing `warn_open()` helper) recommending a pathspec-scoped commit instead of a bare one. Gated by plan-time `/risk-check` before landing. Both halves of this incident's fix direction are now closed.

## Session — 2026-07-12 (/fix-repo-issues plan + same-session execution)

### Friction Events

- **Friction type: git (concurrent same-checkout shared staging index — bare `git commit` swept a sibling session's staged deletion). ~8th recurrence of this class, and the FIRST since the class was stamped "Resolved 2026-07-03 (S8)".** Committing a single-file log update at the **workspace root** while S2 was live in the same checkouts, `git add logs/improvement-log.md` was followed by a **bare `git commit`** — which committed the entire shared index, sweeping in S2's already-staged deletion of `.claude/hooks/model-classifier.sh` (S2's W3.2 M-A2b "wire-or-delete" work). Commit `434c8b7` therefore contained a staged deletion it did not author and whose message did not mention it. **Contained, no data lost:** caught by a post-commit `--name-status` read, corrected via `reset --soft HEAD~1` + `git restore --staged` + recommit as `ff545c0` (1 file). The commit was local and unpushed, and S2 was not mid-commit, so the HEAD rewrite was safe here — unlike the 2026-07-03 incident where disentangling was correctly declined.
  - **Failure mode:** Validation.
  - **Root cause — TWO independent guards were supposed to stop this, and BOTH were inoperative:**
    1. **The documented default was not followed.** `docs/commit-discipline.md § Concurrent-session staging discipline` already prescribes `git commit -- <explicit-path>` precisely because `git add <path>` + bare `git commit` is asymmetric: `add` scopes what *this* session stages, `commit` with no pathspec commits *the whole shared index*. The rule was shipped 2026-07-03 in response to incident `9660bf2`. It was not applied here — the session used explicit-path `add` (correctly) and then a bare `commit` (the exact anti-pattern the rule names).
    2. **`check-foreign-staging.sh` failed open — and this is the important half.** The tripwire IS registered (user-level `~/.claude/settings.json:61`) and its 2026-07-03 (S8) hook-flag fix (advisory on a bare commit while a foreign per-id marker is live) IS present. It did not fire because **this session had no per-id marker**: `/prime` was run for orientation, the operator then dispatched `/fix-repo-issues` directly, so `/prime` Step 8 (which writes `logs/.session-marker-${CLAUDE_CODE_SESSION_ID}`) never executed. Verified: `logs/.session-marker-*` held markers for S1 and S2 but none for this session's id. A footprint-less session gets **no foreign-staging protection at all**.
  - **This is a live, causal confirmation of an already-parked item — not a new theory.** `improvement-log.md` 2026-06-09 — "`check-foreign-staging.sh` fails open for footprint-less sessions" (scanned as `id-34` in this very session's `/fix-repo-issues` run, and *parked* into the concurrency cluster) describes this exact hole, and `concurrent-session-check.md` documents it as its **#1 failure**. The hole was parked as theoretical; it then caused a real entanglement **within the same session that parked it.** The fail-open path is not a rare corner: any `/prime`-for-orientation-then-do-work session, any `/clear`-resumed session, any command dispatched without `/session-start` is footprint-less — i.e. the guard is absent on a *routine* path, not an exotic one.
  - **Prevention:**
    1. **Structural (preferred):** close the fail-open. When a gated git verb runs with **no own footprint** AND a **foreign per-id marker is live**, the guard should warn (or block) rather than pass silently — the current logic treats "I can't identify this session" as "nothing to protect," when it is in fact the highest-risk state. This is the minimum-guard variant already proposed in the `id-34` entry.
    2. **Behavioural (immediate, free):** use `git commit -- <explicit-path>` whenever any concurrent session may be live. Pathspec on `commit`, not just on `add`.
    3. Consider having `/prime` write the per-id marker at **orientation** time rather than only at Step 8 dispatch, so an orientation-only session is still identifiable to the guards. (Raises its own questions — a marker implies a session that will wrap — so this is a direction, not a decision.)
  - **CORRECTION / sharper finding, same session — it is not merely fail-open, it MIS-IDENTIFIES.** Minutes after the above was written, a `git commit -- logs/friction-log.md` in `ai-resources` **did** trip the guard, which printed: `Declared footprint: projects/axcion-ai-system-redesign/output/implementation-prep/packets/M-A-phase0-defects.md, docs/autonomy-rules.md, docs/session-rituals.md, .claude/hooks/model-classifier.sh, …`. **That is S2's mandate, not this session's.** With no per-id marker of its own, the guard fell back to the *shared* `logs/.session-marker` (contents: `2026-07-12 S2`) and resolved this session's identity — and therefore its "declared footprint" — to **the other live session's**. So a markerless session is not just unprotected; it is actively **assigned a foreign identity**, and is judged against a footprint it never declared. That is strictly worse than failing open, and it explains both halves of today's behaviour: at the workspace root (no marker/mandate resolvable) it passed silently; in `ai-resources` it blocked using someone else's footprint. Any fix to the `id-34` fail-open must therefore also fix **identity resolution** — falling back to the shared marker is unsound whenever a per-id marker is absent.
  - **Second-order finding: the guard blocks the very discipline `commit-discipline.md` prescribes.** The blocked command was `git commit -- logs/friction-log.md` — a **pathspec-scoped commit**, which by definition commits only that path and ignores the rest of the index, i.e. it is *immune* to the staging-index sweep this entire entry is about. The tripwire inspects the staged set without parsing the pathspec, so it blocks the safe form exactly as it would block the unsafe bare form. Net effect: the recommended remedy is punished, and an operator who complies is pushed back toward the bare commit. Prevention: teach `check-foreign-staging.sh` to recognise a `--`/pathspec-scoped `git commit` and pass it through (the sweep it guards against cannot occur in that form).
  - **THIRD finding, at this session's own wrap: the liveness oracle is stale in the OTHER direction too — wrapped sessions leave ghost markers.** The wrap commit was blocked by `check-foreign-staging.sh` on the branch "*NO concrete session footprint … AND a live concurrent session is active in this checkout (an un-wrapped per-id marker is present)*". But **no session was live.** Both S1 and S2 had already wrapped and committed (`39f683b`, `0490a6f`), and their `/wrap-session` Step 13 teardown (`rm -f logs/.session-marker-${CLAUDE_CODE_SESSION_ID}`) had **not run** — `logs/.session-marker-2e0574a6…` (S1) and `logs/.session-marker-78e94ae4…` (S2) were both still on disk. The guard was blocking on two **ghosts**. Combined with the mis-identification above, the per-id marker set is unreliable in **both** directions: a live markerless session is **invisible**, and a wrapped dead session still reads as **live**. Any fix must make teardown reliable (or make liveness derivable from something other than a file the dying session is trusted to delete — e.g. treat a marker whose session has a committed wrap note as dead).
  - **FOURTH finding: the guard's own remedy is unreachable for the sessions that trip it.** Its block message says: *"declare a concrete `- Files in scope:` footprint in this session's mandate so the guard can run normally, then retry."* But the sessions that hit the no-footprint branch are **precisely those that never ran `/session-start`** — which is the only thing that writes a mandate. There is no mandate to add a bullet to. This wrap resolved it by **retro-declaring** a mandate block + `Files in scope` in the session note at wrap time (see the 2026-07-12 S3 entry in `session-notes.md`), which is a workaround, not a design. Prevention: either have the guard accept an explicit operator-confirmed staged set when no mandate exists, or have `/prime` write a minimal footprint-bearing stub for orientation-only sessions.
  - **Owner artifact:** `ai-resources/.claude/hooks/check-foreign-staging.sh` (fail-open branch, identity resolution, pathspec parsing, ghost-marker liveness) + `ai-resources/.claude/commands/wrap-session.md` Step 13 (teardown did not run for S1/S2) + `ai-resources/docs/commit-discipline.md § Concurrent-session staging discipline` (pathspec-commit default).
  - **Bearing on the parked cluster:** the `/fix-repo-issues` plan (`audits/fix-plans/fix-repo-issues-2026-07-12-2132.md`) parked `id-05` / `id-34` / `id-37` / `id-29` / `id-35` as a cluster needing one structural session rather than piecemeal patches. **This incident is the argument for doing that session.** The class has now been "fixed" ~4 times and recurred ~8 times; each fix closed one surface while the guards' *identification* layer (who am I, is anyone else live) stayed weak. Note the pattern: the 2026-07-03 fix closed the bare-commit surface for *marked* sessions and left it wide open for unmarked ones.

#### Write Activity

- `logs/friction-log.md` (this entry), `logs/improvement-log.md`, `docs/qc-independence.md`, `skills/ai-resource-builder/SKILL.md`, `.claude/agents/fix-repo-issues-scanner.md`, `logs/scripts/foreign-session-guard.sh`, `audits/fix-plans/fix-repo-issues-2026-07-12-2132.md`; workspace `logs/improvement-log.md`; `projects/axcion-design-studio/.claude/shared-manifest.json` + 32 agent symlinks.

## Session — 2026-07-13 (S2)

### Friction Events

- **Friction type: harness (two live sessions in ONE checkout reached OPPOSITE operator-approved conclusions on the SAME question — a collision the seven existing guards did not surface).** Direct, first-hand evidence for **RR-04** (worktree pilot). This session (`576c1c1c…`, marker `S2`) primed at 10:40 on HEAD `01931f1` and spent ~35 min on W3.2 R3 Pass 2. Concurrently, session `c5d1f655…` — which had *already wrapped once* as `S1` (commit `01931f1`) and then **kept working past its own wrap** — ran a 5-agent verification sweep and committed 4× to this same checkout (`c42d9ec` 11:01 → `6dc926e` 11:09), **retiring W3.2 as plan of record**, superseding the mission/register/packets, and re-scoping the wrap-note cut as `RR-03`. Neither session saw the other.
  - **Both sessions asked the operator the same question ~30 min apart and got opposite answers approved.** This session: *abandon Pass 2* (approved). The other: *ship it as RR-03* (approved). The operator was not deceiving anyone — **each session presented a differently-grounded case and neither could see the other's.** The collision surfaced only because this session happened to `tail` `decisions.md` and found an entry stamped with **its own marker (`S2`) that it had not written**.
  - **Marker collision, concretely.** Per-id markers on disk read `c5d1f655… → 2026-07-13 S1` and `576c1c1c… → 2026-07-13 S2`, while the other session was *hand-labelling its post-wrap phase* `(S2)` in `decisions.md`. Two sessions, one identity string. The marker system assumes one `/prime` per session; it has no concept of a session that **continues past its own wrap** and re-numbers itself.
  - **The guards were silent, and correctly so — this was never their failure mode.** `check-foreign-staging.sh` guards the *staging index*; `detect-concurrent-session.sh` and `/prime` Step 1a guard *liveness at orientation*. This session's `/prime` DID flag the foreign per-id marker — and, reading it as a wrapped session's ghost (a documented, real phenomenon in the 2026-07-12 S3 entry above), the session discounted it. **The ghost-marker false-positive problem has now trained the system to ignore a true positive.** That is a second-order cost of the unreliable liveness oracle, and it is new.
  - **No data was lost** — the two sessions wrote mostly disjoint files, and this session discarded its own superseded work rather than committing over the other's. That is luck plus a late catch, not a guard.
  - **Failure mode:** Traceability.
  - **Root cause:** Two sessions share one working tree and one identity namespace. Every existing guard mitigates a *symptom* of that (index clobber, note clobber, stale liveness); none removes the *cause*. This is the ~7th recurrence of the class, and the first where the damage was **two contradictory approved decisions** rather than a file overwrite — a strictly worse outcome than the data loss the guards were built for, because it corrupts the *decision record* rather than a file, and it is invisible to every existing check.
  - **Prevention:** RR-04 — run `/new-worktree-session` (built 2026-07-04, VS Code-aware, **never once executed**; `git worktree list` shows only the main checkout). No new tooling. This entry is the evidence that the pilot is overdue. Secondary: `/wrap-session` Step 13 teardown must run (the other session's post-wrap continuation left its `S1` marker live and then self-renumbered to `S2`, which no oracle models).
  - **Owner artifact:** `.claude/commands/new-worktree-session.md` (RR-04, the actual fix) + `ai-resources/plans/repo-redesign-authoritative-implementation-report.md` § RR-04.

- **Friction type: process (a session verified a bug report against a tool that had been silently FIXED under it mid-session, and reported the report as fabricated).** My own error; the sharpest instance in this chain because I made it *while diagnosing the identical error in four other sessions*.
  - **What happened.** The `project-planning` session reported `decisions_refs: []` and cited `check-decision-refs.sh` output saying so. I ran `check-decision-refs.sh` from that cwd, got **`3/3 refs resolve`, exit 0**, read the manifest (3 refs, committed, never edited), and concluded the report's observation was **invented**. I said so in strong terms.
  - **It was not invented. The report's observation was TRUE.** The concurrent session (unknown to me) had committed `df53459` at **11:06** — *"RR-01: `check-decision-refs.sh` reads the CALLER's repo, not always ai-resources"*. **I ran my verification after that fix landed.** Pre-fix, the script derived its repo root from its own location, so from any non-`ai-resources` cwd it inspected **ai-resources'** manifest — which, at the moment `project-planning` wrapped, was a still-open start-stub under the *same* `2026-07-13 S1` filename with `decisions_refs: []`. The tool correctly reported an empty array; it was simply reporting on **the wrong repo's file**.
  - **Both pieces of evidence that would have proven the report right had evaporated before I looked:** the checker was fixed at 11:06, and ai-resources' own S1 manifest filled in its 3 refs at 10:40 when that session wrapped. I sampled a moved target and called the earlier observer a liar.
  - **Failure mode:** Validation.
  - **Root cause:** I treated "I ran the check and it passed" as proof about a claim made **at a different time, against a different build of the tool, in a repo another session was actively mutating**. A check is not a timeless fact. It is an observation with a timestamp and a tool version, and I recorded neither.
  - **Prevention:** When verifying a *past* claim, first ask what has changed since it was made — `git log --since` on the tool AND on the data. If the tool or the data moved, the check does not falsify the claim; it only describes the present. Concretely: a verification that contradicts a prior report must cite the commit range it ran against.
  - **Owner artifact:** (none identified) — this is a reasoning discipline, not a component. It is deliberately **not** proposed as a new gate: the operating rule adopted this same day (`logs/decisions.md` 2026-07-13 S2) is *"build no checker, register or review process around it."* Logged as a lesson, not as machinery.

#### Write Activity

- `logs/friction-log.md` (this entry); `logs/session-notes.md` (S2 entry); `logs/session-plan-2026-07-13-S2.md`; `logs/runs/2026-07-13-S2.json`. Session's substantive W3.2 work was **discarded, not committed** — superseded mid-session by the concurrent session's W3.2 retirement. Template edits (`templates/mission-contract.md`, redesign `implementation-packet-template.md`) were made and then **reverted**: they added a rule to artifacts the other session had just retired.

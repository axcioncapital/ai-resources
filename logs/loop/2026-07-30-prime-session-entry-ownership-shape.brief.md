BRIEF
UNIT: 2026-07-30-prime-session-entry-ownership-shape
STREAM: 2026-07-30-prime-session-entry-ownership
PHASE: shape
REPO: ai-resources
BASE: e099739
NEXT: Claude — run Shape, then hold G1

**Capability:** prime-runtime-delegation

**Claude-authored brief**, composed from the operator's settled 2026-07-30 direction (transcribed in
D4 and in this stream's Frame brief) plus the Frame unit's own findings. It has had **no independent
framing** — a recorded weakness of the unit. The direction it carries is an operator decision and is
not in scope for re-argument; the *plan* built on it is what Codex reviews at G1.

Need: Frame confirmed two coupled needs with one root cause — a half-completed ownership move. (1) The
session-entry seam is defective: `prime.md` calls `logs/scripts/prime-marker.sh` on a repository-relative
path that resolves in 1 of the 32 roots carrying it. (2) `/prime` is 411 lines against a frozen ≤300
target, still holding six categories of retired machinery and five un-executed move-outs. Shape owns the
plan that closes both, and nothing else.

## What this unit must produce

1. **Every removal and move-out target re-derived live against HEAD** — no count carried from the Frame
   evidence, the predecessor stream, or any handoff. Frame already caught two undercounts this way
   (`STRUCTURAL_RISK` had a third site; multi-item auto a second).
2. **An itemised line budget** — per region, measured, not estimated. The predecessor stream's central
   failure was a ≤300 budget resting on estimates for fifteen of seventeen regions; repeating that shape
   is the single most likely way this stream fails again.
3. **A decision on the C2 renumbering reconciliation** — restore retained step identifiers to their
   pre-change values, or repoint every citing file. Frame left this open; the two have different blast
   radii and the choice is Shape's.
4. **Falsification criteria** that explicitly include: the session-entry owner performs the *complete*
   marker → header → mtime sequence, not just the marker; the script locator resolves from a real
   project-consumer root; F-DUP's eight duplication declarations enumerated as a literal list.
5. **A slice list** for G1, one slice per unit at Build.

Premises to verify in this unit:
- `prime.md` at HEAD is 411 lines and byte-identical to the Frame unit's object at `8c573af`.
- The six removal targets still exist, at positions re-derived live: `STRUCTURAL_RISK`, legacy QC,
  model-alignment reporting, multi-item auto, log-trio prefetch, urgent-log triage.
- The five move-out targets exist as bounded regions: git sync/autostash, session-entry writes,
  concurrent-session detection, mechanical state collection, post-dispatch behaviour.
- `logs/scripts/prime-marker.sh` reads `logs/session-notes.md` but writes neither it nor
  `logs/.prime-mtime` — the two writes that complete the session-entry owner.
- `docs/session-marker.md:339` still cites the removed `/prime` Step 8c.2.
- The set of files citing `/prime` step identifiers is enumerable by grep, with a positive control.

Falsified if: the six-responsibility architecture cannot be reached without breaking a retained route
(numbered selection, free-text intent, single-item `auto`); any listed removal has a concrete evidenced
blocker with no smaller outcome-preserving replacement; or the itemised budget shows ≤300 unreachable
after every listed removal and move-out — which is a measurement to report and take to G1, never a reason
to renegotiate the target.

**Scope of THIS unit:** Shape only. **No edit to `prime.md`, to any script, to any consumer, or to any
citing file.** The plan is this unit's whole deliverable. Build executes it, one slice per unit, only
after G1.

Gates: challenged route — G1 (scope and package) closes this unit · G2 (release) at Prove · G3
(lifecycle) at Land. Exactly three. Codex is the sole independent reviewer; the object of its review here
is the **plan**, not a diff.

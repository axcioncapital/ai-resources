---
task: work-loop-v2-proportionality-continuity-implementation
turn: codex
---

## Objective and scope

Implement the accepted Work Loop v2 proportionality-and-continuity plan as separately assessed,
independently committable slices. The task exit condition is that the accepted plan's S1–S7 changes
are implemented in their governed dependency order with their proof cases, or a verified blocker is
handed back rather than worked around.

This opening unit is **S1 only**: narrow the Codex skill's activation and defer loading the executable
core until routing has selected the Work Loop. Excluded from this unit are S2–S7, every file other
than `.agents/skills/work-loop-v2/SKILL.md` and this state file, changes to the router's accepted
owner inventory or behavior beyond the two S1 amendments, installation or propagation, worktrees,
branches, pushes, and adjacent cleanup.

## Lane and unit

Standard. Implementation mode. Unit 1 — accepted plan slice S1: activation narrowing and core-read
sequencing.

Named reason for the loop: the accepted task spans several independently assessed slices and
sessions, and this first change controls when a large shared operating contract enters Codex context;
its behavioral activation evidence must be judged independently before the next slice opens.

## Brief

The accepted plan is now genuinely closed, and S1 is its smallest ready implementation slice. It
comes first because it removes Work Loop cost from ordinary requests the loop does not own, while
leaving every later implementation concern outside this unit. Implement only S1 and hand its
fail-capable evidence back to Codex.

### Governing sources and current-state dispositions

- The operator's current handoff governs the checkout and unit boundary: use the saved Local checkout
  at `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources`, never create or switch to a
  worktree, re-read current in-flight task state, and frame/execute only the smallest ready slice.
- `logs/work-loop/work-loop-v2-proportionality-continuity-plan.md` is the content-bound acceptance
  record. It is closed at `turn: operator`, names the accepted plan, and records the plan and
  correction commits. It establishes approval; it is not an implementation target.
- `plans/work-loop-v2-v0.2/work-loop-v2-proportionality-continuity-implementation-plan-v0.1.md`
  governs this implementation. For this unit, § 4.1, § 4.2, § 5 S1, § 6 P-1, § 7 and § 9 apply.
- `plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md` is the higher contract and wins over
  the plan if they disagree. It is read-only in S1.
- `.agents/skills/work-loop-v2/SKILL.md` is the only implementation target. Its current contents are
  verify-first repository claims, not authority.
- The current collision check found
  `logs/work-loop/work-loop-v2-contained-unattended-profile.md` closed; the only task at
  `turn: claude`, `project-progression-candidate-review-correction.md`, constrained to closing its
  own state file; and the two other active tasks at `turn: codex`, with no current writer authorized
  on the skill. These are time-sensitive verify-first claims. Re-read those state files before
  editing; a newly authorized overlapping writer is a stop.

### Required outcome

1. Replace only the skill frontmatter `description` with the closed activation contract in plan
   § 4.1. Front-load and keep all four positive triggers: an explicit Work Loop request; an existing
   `logs/work-loop/{task-id}.md` task, hand-off or assessment; “continue this project” / “what is next
   on this project”; and a request specifically asking Codex to frame a bounded unit for another
   actor.
2. Name all five request-shape non-triggers from § 4.1: an ordinary unnamed repository/project
   change is Direct Work; a named command, skill or agent; a read/explain question with no repository
   change; a small reversible fix; and work already inside another skill's flow. Do not add a second
   gate, router file or allowlist.
3. Relocate the opening obligation to read the executable core so it applies after Routing step 3
   selects the Work Loop and before the first Work-Loop-owned move. Routing steps 1–3 must remain
   executable without the core; admission and mode classification stay downstream of the read.
4. Preserve everything else. In particular, do not alter the accepted routing inventory, mode
   contract, Continue/correction behavior, courier behavior, unattended guidance or state protocol.

These mechanisms are settled by the accepted plan, not new Claude implementation requirements.

### Check against the repository before acting

1. Verify `pwd -P` is exactly the Local checkout path above before reading or writing any target. A
   mismatch stops. Never copy this state file to repair a mismatch.
2. Validate this state file read-only against core § 4: filename/task identity, `turn: claude`, exact
   active headings and one-task/one-file location.
3. Open the current skill and verify that its frontmatter still contains the catch-all trigger
   “work is described without naming the capability” and that its opening instruction still requires
   the core before the first move in any task. If either anchor has materially changed, hand back the
   exact difference instead of adapting silently.
4. Re-read the four current-state files named above. Confirm no other actor is currently authorized
   to edit the skill and no live unattended dispatcher owns this task. If an overlapping writer or
   changed scope exists, stop.
5. Check the skill for overlapping uncommitted work before editing. Do not overwrite or absorb
   another task's change.
6. Confirm S1 can be completed without editing the core, command, harness, fixtures, hooks, plans or
   another state file. If not, hand back the conflict.

### Required evidence

- One inspection line for every claim above, including those that hold.
- Quote the old and new frontmatter descriptions and the old and new core-read instruction
  placement. Show that the activation description carries exactly the four positive shapes and the
  five non-trigger shapes without retaining the ordinary-work catch-all.
- Run plan proof case P-1 as a seven-shape activation matrix in genuinely fresh Codex tasks: the
  unnamed ordinary implementation request, named-capability request and read-only question must not
  activate Work Loop v2; naming the Work Loop, pointing at an existing task state, either explicit
  continuation/orientation phrase, and asking Codex to frame a bounded unit must activate it. Report
  each observation separately.
- Include P-1's over-correction control: all four positive trigger classes must still activate. Also
  retain a red witness from the pre-change description showing that the unnamed ordinary request was
  previously admitted by the catch-all. If genuinely fresh activation cannot be observed from
  Claude's available environment, do not substitute a grep or claim completion; hand back the
  precise proof gap.
- Read Routing steps 1–3 in isolation and show that none requires the executable core. Show the core
  read obligation appears exactly once, after owner selection and before admission/mode work.
- For this instruction-file change, do not invent a ceremonial automated regression check. The
  behavioral activation matrix is the fail-capable proof; the changed-text comparison is the
  implementation evidence.
- Report the files changed and confirm that no S2–S7 target or unrelated state file was changed.

Completion: implement only S1, record the inspections and evidence under `## Latest result`, set
`turn: codex`, and commit only `.agents/skills/work-loop-v2/SKILL.md` and this state file by explicit
pathspec. Do not start S2 or assess your own result.

Stop and hand back if the checkout mismatches; this file is malformed or stale; an anchor or approval
premise is false; another writer now owns the skill; another task's uncommitted skill change would be
overwritten; any S1 requirement needs an excluded file; fresh-task activation evidence cannot be
produced; or an operator-owned decision about scope, authority or risk is required.

## Latest result

Inspected (2026-08-07):

- **Claim (1): HOLDS** — `pwd -P` returned exactly
  `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources`. No worktree, no copy.
- **Claim (2): HOLDS** — frontmatter `task:` matches the filename, `turn:` was `claude`, and the
  active headings are core § 4's exact five (`## Objective and scope`, `## Lane and unit`,
  `## Latest result`, `## Blocker`, `## Next action`) plus `## Brief`, which core § 4 places outside
  the five-field ceiling. One file, at `logs/work-loop/`.
- **Claim (3): HOLDS** — both anchors were present before anything was edited. Frontmatter carried
  "Use whenever work is described without naming the capability to use, including 'continue this
  project'." and line 10 carried "**Read `plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md`
  before your first move in any task.**" Independently corroborated by the pre-change Codex run
  below, whose capture quotes the old frontmatter verbatim.
- **Claim (4): HOLDS on the load-bearing part; the enumeration was stale, and is reported rather
  than adapted silently.** `grep '^turn:' logs/work-loop/*.md` returns **three** tasks at
  `turn: claude`, not the one the brief names: this task,
  `project-progression-candidate-review-correction.md`, and
  `work-loop-v2-escaped-descendant-termination.md` — the last of which is untracked and did not
  exist when the brief was written. Both were opened read-only. Neither is authorised to write the
  skill: the first is constrained by its close verdict to "change no file other than this state
  file"; the second excludes "Work Loop core, skill or rule changes" in its scope and repeats the
  exclusion as a Codex framing decision. `work-loop-v2-contained-unattended-profile.md` is closed at
  `turn: operator`. The two `turn: codex` tasks are `work-loop-v2-production-readiness-policy.md`
  and `work-loop-v2-intake-router.md`; neither claims the skill. So **no other actor is currently
  authorised to edit the skill**, which is what the claim rests on. No dispatcher run is in flight
  (`pgrep -fl dispatch.sh` returned nothing).
- **Claim (5): HOLDS** — `git status --porcelain -- .agents/skills/work-loop-v2/SKILL.md` was empty
  before the edit; the file was clean at `4c9aa0e`. After the edit, `git diff --stat` on it reports
  3 insertions / 3 deletions — exactly this unit's three changes and nothing else.
- **Claim (6): HOLDS** — S1 was completed by editing `.agents/skills/work-loop-v2/SKILL.md` alone.
  No core, command, harness, fixture, hook, plan or other state file was touched.

**Concurrent movement observed during the unit, reported because it is repository fact.** The tree
moved while this unit ran: commit `085db35` ("batch: add Codex thread handoff command") landed, and
`plans/work-loop-v2-v0.2/handoff-automation-spike/dispatch.sh` became modified by another writer —
the surface owned by `work-loop-v2-escaped-descendant-termination`. Neither touches `SKILL.md`, and
neither is staged here.

Result: S1 implemented in `.agents/skills/work-loop-v2/SKILL.md`, and nothing else changed.

**Change 1 — the frontmatter `description` (§ 4.1).**

Old: "Route an ordinary-language request for repository or project work to the one capability that
owns it … **Use whenever work is described without naming the capability to use, including 'continue
this project'.** Claude executes and makes every commit; you do neither. Small reversible fixes are
Direct Work and open no state file."

New: "**Use only when the request (1) names the Work Loop, (2) points at an existing
logs/work-loop/{task-id}.md task, hand-off or assessment to act on, (3) says 'continue this project'
or 'what is next on this project', or (4) asks Codex to frame a bounded unit for another actor to
execute.** Then route it to the one capability that owns it — the operator, an Axcíon command, a Matt
skill, or the Work Loop itself — and, where the Work Loop owns it, frame and assess one bounded unit:
write the brief that opens it, and judge the evidence that comes back. **Do not use for an ordinary
repository or project change described in natural language without naming a capability (that is
Direct Work), a request naming a command, skill or agent to run, a question answered by reading or
explaining with no repository change, a small reversible fix, or work already inside another skill's
flow.** Claude executes and makes every commit; you do neither."

The ordinary-work catch-all is gone, not softened. The four positive shapes are front-loaded, in
§ 4.1's order, and stated as a closed list ("Use only when"). All five non-trigger shapes are named,
each as a request *shape*, none as subject matter. The old closing sentence about small reversible
fixes is not lost — it is now non-trigger four plus the "(that is Direct Work)" clause. **Cost stated
rather than hidden:** the description grew from about 560 to about 920 characters, against § 4.1's
"length-capped" property. Nine named shapes will not compress much further; if the startup budget
matters more than the naming, that is Codex's call, not a silent trim.

**Change 2 — the core read moved behind owner selection (§ 4.2).** Line 10's "before your first move
in any task" obligation is deleted. Line 10 now carries only the authority statement (the core wins,
this file does not restate it) and a locator, with no read imperative. The single obligation now sits
inside Routing step 3: "**If the Work Loop is the owner** — and only then — **read `…executable-core-v0.1.md`
now, before your first Work-Loop-owned move.** … Then apply the Direct-versus-Standard admission test.
Where any other capability owns it, admission does not arise and the core is not read."

Requirement 4 (preserve everything else) held: the routing inventory, mode contract,
Continue/correction behaviour, courier behaviour, unattended guidance and state protocol are byte-
identical — the 3-line diff is the whole change.

Evidence.

**Placement, checked statically.** `grep -c 'read .*executable-core-v0\.1\.md'` over the skill returns
**1**, at line 127 — Routing step 3. The only other mention of the path, line 10, is a
non-restatement clause with no read imperative. Read in isolation, Routing steps 1 and 2 (interpret
the outcome; choose one owner from the index) name no core rule; step 3's own rule is "if the Work
Loop is the owner, read the core, then admit", so admission (core § 2) and mode classification
(step 4, core § 3) both sit downstream of the read, as § 4.2 requires.

**P-1, run as seven genuinely fresh Codex tasks.** Not a grep. Each shape was a separate
`codex exec -s read-only -C <checkout> --json` process (codex-cli 0.147.0-alpha.1.2, the ChatGPT.app
binary the dispatcher itself uses). `work-loop-v2` is discoverable from this checkout — a prior probe
run returned it in the skill list. **Activation is observed as the run opening
`.agents/skills/work-loop-v2/SKILL.md`**, which is what the pre-change run does and what every
activating run below does as its first or second command.

| # | Shape | Expected | Observed |
|---|---|---|---|
| a | "Add a follow-up date to the contact model." | must not activate | **did not activate** |
| b | "Run /token-audit on this repo." | must not activate | **did not activate** |
| c | "What does the friday-checkup command do?" | must not activate | **did not activate** |
| d | "Use the Work Loop to add a follow-up date to the contact model." | must activate | **activated** |
| e | "Pick up logs/work-loop/work-loop-v2-intake-router.md and tell me what it needs next." | must activate | **activated** |
| f | "Continue this project." | must activate | **activated** |
| g | "Frame a bounded unit of work for Claude to execute in this repo." | must activate | **activated** |

Each activating run said why in its own words — d: "because you explicitly named the Work Loop";
e: "because you pointed to an existing `logs/work-loop/` task"; f: "because 'continue this project'
is one of its explicit routing triggers"; g: "because you've explicitly asked me to frame a bounded
unit for Claude". Each then read the core, correctly, *after* selecting the Work Loop as owner —
which is § 4.2's behaviour observed live rather than only asserted. Runs a, b and c read the core
**not at all**, which is the cost § 4.2 exists to remove.

**The red witness, and why it is not circular.** Before the edit, shape (a) — the identical prompt,
the identical command — **did** activate. The capture's first agent message reads: "I'm using the
Work Loop v2 routing skill because this is a concrete repository change described in ordinary
language", followed by reads of `.agents/skills/work-loop-v2/SKILL.md` and both halves of the
457-line core. That is RC-1 and RC-2 in one run. The same prompt after the edit does neither. The
observation is binary and could have read the other way at any point.

**Over-correction control: passed.** All four positive trigger classes (d, e, f, g) still activate.
Under-activation would have failed this case as loudly as the original defect, and did not occur.

**Two limits on this evidence, stated rather than smoothed over.**

1. **Shape (b) was truncated.** Its process was still running at a 10-minute cap and was killed. Up
   to that point it had executed 33 commands, all `/token-audit` work, and **zero** work-loop reads —
   whereas every activating run opened the skill within its first two commands. The observation is
   "no activation across 33 commands", not "a completed run".
2. **`codex exec` is not the Codex app.** These are real fresh Codex tasks against the real skill
   directory, but headless. Skill *selection* in the operator's app could differ, and this evidence
   does not settle that.

**No ceremonial regression check was invented.** For a frontmatter and instruction change there is
no automated check that could distinguish success from failure — the activation matrix is the
fail-capable proof, and the quoted old/new text is the implementation evidence. The acceptance
harness (`logs/scripts/work-loop-v2-slice-1.test.sh`) was not run or amended: it is S3's target, and
S1 authorises no change to it.

**Files changed:** `.agents/skills/work-loop-v2/SKILL.md` and this state file. No S2–S7 target and
no other state file was changed.

**Deferral — noticed during P-1, deliberately not implemented.** In shape (a) the run did not
activate `work-loop-v2`, but it *did* activate **Work Loop v1** — it opened
`.agents/skills/work-loop/SKILL.md` and `docs/work-loop.md` and said "I'm using the repository's Work
Loop skill". So the ordinary-request cost OD-4 targets is partly still there, via a sibling skill
this plan lists under "installed but excluded" (§ 7). It is outside S1's single target file and is
not fixed here. Whether v1's own description should be narrowed, or v1 retired, is a separate
decision.

## Blocker

None.

## Next action

Codex: assess S1. Two things are yours to judge — whether the seven-shape matrix plus the red witness
is sufficient evidence given the two stated limits (shape (b) truncated at 10 minutes; `codex exec`
is not the Codex app), and whether the description's growth from ~560 to ~920 characters is
acceptable against § 4.1's length-cap property. Then close, continue to S2, or correct once. The
Work Loop v1 activation is recorded as a deferral, not as work.

---
task: leverage-idea-v2-routing-seam
turn: codex
---

## Objective and scope

Make `.claude/commands/leverage-idea.md` hand operating-capability work and settled corrections to
the live Work Loop v2 boundary, consistently across its route list, fast-path gate, complexity-budget
example, prose payload rules and final owner table.

Scope is exactly `.claude/commands/leverage-idea.md` plus this task-state file. Excluded:
`/develop-ai-resource` and its now-closed Unit 1 seam; retirement ownership (approved plan Unit 3);
the v1 capability method and doctrine (Unit 4); the Work Loop v2 core, Codex skill, Claude command
and tests; the complexity-budget rule itself; every other `/leverage-idea` route; and any new
command, gate, state or handoff artifact.

## Lane and unit

Standard. Implementation mode. Unit 2 — reconcile `/leverage-idea`'s two v1 Work Loop destinations
with Work Loop v2 and prove the router still returns one proportionate owner.

Named reason for the loop: `/leverage-idea` is a shared router whose repeated route descriptions
must agree. A partial textual fix could leave one dead exit, bypass Direct Work admission or weaken
the complexity-budget cap, so the result needs bounded implementation, fail-capable routing cases
and independent Codex assessment.

Plan justification: the operator approved the corrected plan content committed at `6af280e` and has
now explicitly started Unit 2. Unit 1, the prerequisite, is formally closed with implementation
commit `4088df9`. Governing unit:
`plans/work-loop-v2-v0.2/resource-capability-development-plan-v0.1.md` § 7, Unit 2. The plan file's
unchanged “Draft — not approved” header is stale metadata relative to the operator's content-bound
approval; do not edit the plan or infer authority for Units 3–4 from this brief.

## Brief

Why this unit, why now: `/leverage-idea` promises that every idea leaves with one reachable owner,
but its operating-capability and settled-correction routes still point to the deleted v1
`/work-loop` command. Unit 1 established the live boundary in `/develop-ai-resource`; Unit 2 now
repairs the adjacent router without redesigning it. The intended result is consistent live routing,
not a second intake system.

### Governing and applicable sources

- Current operator decision recorded above — governing authorization and scope.
- `plans/work-loop-v2-v0.2/resource-capability-development-plan-v0.1.md` § 7 Unit 2 — governing
  approved outcome, exclusions and evidence standard at `6af280e`.
- `logs/work-loop/develop-ai-resource-v2-capability-seam.md` — authoritative closed prerequisite;
  use its one-owner outcome and accepted limitations, not its working history.
- `plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md` § 2 and
  `.agents/skills/work-loop-v2/SKILL.md` — applicable authority for owner selection and
  Direct-versus-Standard admission.
- `.claude/commands/leverage-idea.md` — implementation target and authority for idea-first analysis,
  the complexity-budget cap and its self-contained handoff payload.
- `.claude/commands/develop-ai-resource.md` — read-only current boundary for the AI-resource route;
  Unit 2 must not regress its raw-brief/provenance contract.

Codex framing decision: this unit treats every live `/work-loop` destination in the target as part
of the same routing seam, not only the two final table rows, because leaving a contradictory route
elsewhere would fail the command's own one-owner promise. This widens no owned path and changes no
route class.

### Verify before mutation

Check every claim below against the live repository. If a load-bearing claim is false, record the
evidence, set `turn: codex`, commit only this state file if appropriate, and stop rather than
improvising a new route design.

1. Unit 1 is closed at `turn: operator`, and its outcome establishes a sequential boundary:
   unresolved operating outcome first belongs to Work Loop v2; a later durable-AI-artifact question
   belongs to `/develop-ai-resource`. Settle this from the closed state file and current target
   command, not from conversation.
2. `.claude/commands/work-loop.md` remains absent from the live and tracked command surface, while
   `.claude/commands/work-loop-v2.md` and the Codex Work Loop v2 skill are present.
3. `.claude/commands/leverage-idea.md` still contains exactly seven live bare `/work-loop`
   references at the current route list, duplicate gate, complexity-cap example, two handoff-payload
   rules and two final-table rows. Report the exact bounded search and classify each occurrence by
   function; line numbers in the plan are historical hints, not authority.
4. The Work Loop v2 Codex skill still accepts operating-capability or non-specialist repository
   work and still applies Direct-versus-Standard admission only after it is selected as owner.
   Cite the current clauses. If settled corrections have a different current owner, stop.
5. Core § 2 still sends a small reversible correction to Direct Work and admits ordinary meaningful
   work to Standard only for a named reason. The revised route must preserve this sizing rather than
   turning every correction into a Standard task.
6. The paragraph beginning “The cap survives the route” still makes the complexity-budget finding
   travel with a proposed new component regardless of destination. Its gate logic is valid and must
   remain behaviourally unchanged; only a dead destination name may need reconciliation.
7. The new/materially-expanded AI-resource route still goes to `/develop-ai-resource` with the raw
   `## Capability` heading form and never the reserved `**Capability:**` label. This route is
   adjacent non-interference evidence and is not to be edited unless a dead Work Loop reference
   occurs on the same line.

### Required outcome

Revise only `.claude/commands/leverage-idea.md` so that:

- every operating-capability destination names the live Work Loop v2 owner;
- every settled-correction destination names Work Loop v2 as the admission owner while making clear
  that small reversible work de-escalates to Direct Work and only qualifying work opens a Standard
  state file;
- the route list, duplicate gate, Step 7 payload rules and Step 10 owner table agree with one
  another and return one first owner rather than a stack;
- the “cap survives the route” rule remains equally binding after the destination change—no handoff
  becomes a way around the complexity budget;
- the `/develop-ai-resource` route and raw-brief/provenance contract from Unit 1 remain
  behaviourally unchanged;
- no callable or implied route to the deleted v1 `/work-loop` command remains in this file.

Choose the smallest wording that achieves this. Do not copy Work Loop v2's admission test into
`/leverage-idea`; name the boundary and let Work Loop v2 own its method. Do not execute a routed
payload, change the domain classification, add a route or touch Units 3–4.

### Fail-capable acceptance evidence

Return all five groups in `## Latest result`:

1. **Dead-route regression:** show the exact pre-change search, all seven occurrences classified by
   function, and the same post-change search with zero live bare `/work-loop` destinations while
   still distinguishing `/work-loop-v2`.
2. **Operating-capability case:** walk one existing-project operating-capability idea from
   `/leverage-idea` to exactly one first owner after the change. State when that owner's
   responsibility ends and why `/develop-ai-resource` is not a simultaneous owner.
3. **Settled-correction sizing cases:** use two corrections to an existing command—one genuinely
   small and reversible, one meaningful enough to need a named Standard reason. Show that the first
   reaches Direct Work with no state file and the second may open a Standard Work Loop v2 unit.
   Evidence fails if both are forced into Standard or if `/leverage-idea` reproduces the full
   admission test itself.
4. **Gate and adjacent-route non-interference:** demonstrate that a proposed new component failing
   the complexity budget remains capped after routing, and that the new-AI-resource route still
   produces the raw `## Capability` brief for `/develop-ai-resource` without a reserved bold field.
   Use a focused before/after read-through or meaningful existing check, not a ceremonial test.
5. **Boundary proof:** confirm no file outside `.claude/commands/leverage-idea.md` and this state file
   changed for this unit. Existing unrelated changes must not be staged, reverted or committed.

Completion: the two route classes reach live owners proportionately; every repeated route
description agrees; all evidence groups are recorded; and no excluded surface changed. Set
`turn: codex`; commit only `.claude/commands/leverage-idea.md` and this state file by explicit
pathspec; stop for assessment.

Stop and hand back to Codex if a verify-first claim is false, the truthful route requires another
owned path, or the evidence cannot distinguish a semantic routing fix from a name substitution.
Stop for the operator if implementation would add a route, weaken the complexity budget, reopen the
approved plan or decide the v1 capability system's fate.

## Latest result

Inspected (2026-08-07):

- Claim (1): HOLDS — read `logs/work-loop/develop-ai-resource-v2-capability-seam.md`: frontmatter
  `turn: operator`, and `grep -c "^## "` → 4, so it is reduced to core § 4's closing record. Its
  `## Outcome` states the sequential boundary — Work Loop v2 owns an unresolved operating outcome,
  `/develop-ai-resource` owns the later durable-artifact question. Corroborated in the target command
  itself at `.claude/commands/develop-ai-resource.md:24` ("sequential, never simultaneous"), not from
  conversation.
- Claim (2): HOLDS — `ls .claude/commands/work-loop.md` → "No such file or directory";
  `.claude/commands/work-loop-v2.md` and `.agents/skills/work-loop-v2/SKILL.md` both present.
- Claim (3): HOLDS exactly — `grep -o "/work-loop[a-z0-9.-]*" .claude/commands/leverage-idea.md |
  sort | uniq -c` → `7 /work-loop`, and no `/work-loop-v2` or `/work-loop.md` form at all. Seven
  occurrences on seven lines, each classified by function against the brief's own list:
  16 = route list ("Where the routes go"); 63 = Step 2 duplicate gate; 166 = the "cap survives the
  route" example; 191 and 195 = the two Step 7 handoff-payload rules (operating capability, bounded
  correction); 270 and 274 = the two Step 10 owner-table rows. Every function the brief named is
  present, and nothing outside that list carries a reference.
- Claim (4): HOLDS — `.agents/skills/work-loop-v2/SKILL.md:133` accepts "bounded repository work no
  specialist owns" as the Work Loop's own class; line 123 applies the Direct-versus-Standard
  admission test **only** "If the Work Loop is the owner", and adds "Where any other capability owns
  it, admission does not arise"; line 124 places mode classification "only once admission has
  succeeded". Owner selection precedes admission, as claimed. No clause gives settled corrections a
  different current owner.
- Claim (5): HOLDS — core § 2 (lines 40–51): "**Direct Work is the default.** A small, reversible fix
  is done directly: no state file, no brief, no ceremony", and the loop "is for ordinary meaningful
  work … Entering it requires a **named reason**, written into the state file when the task opens",
  closing with "If the work is small and reversible, it is Direct Work even when one of those is
  tempting."
- Claim (6): HOLDS — `.claude/commands/leverage-idea.md:166` opens "**The cap survives the route — no
  handoff is a way around it.**" and keys the gate on "whether the *recommended option* introduces a
  new component, **not on which command receives it**". The logic is destination-independent by
  construction, so only the dead destination name inside its example list needed reconciliation.
- Claim (7): HOLDS — line 190 routes a new or materially expanded AI resource to
  `/develop-ai-resource` in the `/request-skill` brief shape and instructs "Render Capability as the
  `## Capability` heading … **never as a `**Capability:**` bold label**", with the raw-by-construction
  note on the absent `**Mechanism:**` / `**Evidence:**` fields. No dead Work Loop reference occurs on
  that line, so it was not edited.

Result: `.claude/commands/leverage-idea.md` now routes both operating-capability work and settled
corrections to `/work-loop-v2`, and states at each correction site that Work Loop v2 sizes the work on
arrival rather than `/leverage-idea` pre-deciding it. Seven lines changed (16, 63, 166, 191, 195, 270,
274) — the same seven the inspection found, one per occurrence. Zero live bare `/work-loop` routes
remain.

Evidence:

**1 — Dead-route regression.** Before: `grep -o "/work-loop[a-z0-9.-]*" | sort | uniq -c` →
`7 /work-loop`, zero `/work-loop-v2`; `grep -c '`/work-loop`'` → **7**. After: `7 /work-loop-v2`,
and `grep -c '`/work-loop`'` → **0**. The search still distinguishes the two forms — the same command
that returned 7 bare hits before returns 0 now while the `-v2` form returns 7, so a silent global
substitution that also clobbered a legitimate `/work-loop-v2` mention would have shown up as a
count mismatch. Unlike Unit 1, no historical mention survives here: `/leverage-idea` is a live router
with no reason to name a deleted command, so zero was reachable and was reached.

**2 — Operating-capability case.** An operator dumps notes about an existing project whose
client-onboarding step is unreliable and should be made to work properly. Step 5a classifies it
**Operating capability**; Step 7 (line 191) and the Step 10 row (line 270) both return
**`/work-loop-v2`** — one owner, named once, in both places. That owner's responsibility ends when the
operating outcome is resolved, or earlier if it stops or reframes the work under its own assessment.
`/develop-ai-resource` is **not** a simultaneous owner: line 191 now says Work Loop v2 "owns the
operating outcome from here", and that if the work later needs a durable AI artifact that does not
exist, "Work Loop v2 routes *that* question to `/develop-ai-resource` when it arises — so name one
owner here, not both." That matches Unit 1's closed outcome read from the other side of the seam.

**How this could have failed and did not:** the failing case is a payload naming two owners, or a
route that loops. Both sites return exactly one command; the artifact question is described as
arising *later* and being routed *by* Work Loop v2, so it is a sequence, not a stack.

**3 — Settled-correction sizing cases**, both walked through the amended text:

- *Genuinely small and reversible* — one confusing sentence in an existing command's prose. Step 2's
  triviality gate (line 64, unchanged) catches a ≤1-file cosmetic edit and ends the run at `/tweak`
  with no files written. Where it is a settled correction rather than cosmetic, line 195 hands it to
  `/work-loop-v2` "sized as a correction", and the text states the consequence plainly: "a small
  reversible correction is done as Direct Work and opens no state file". **No state file is opened by
  either path.**
- *Meaningful enough to need a named Standard reason* — a correction to a shared command's behaviour
  that must be bounded before it spreads and assessed by someone other than its author. Same route,
  same single owner; line 195 and the line 274 row both say only a correction "meeting its
  named-reason bar" opens a Standard unit. So the two cases diverge **at Work Loop v2**, not at
  `/leverage-idea`.

**How this could have failed and did not:** the evidence fails if both corrections are forced into
Standard, or if `/leverage-idea` reproduces the admission test. Neither happens — the router names
one owner and one boundary, and line 195 ends "That sizing is Work Loop v2's to make on its own test
— do not restate the test or pre-decide it here." An earlier draft of that line enumerated core § 2's
three qualifying reasons; that was cut precisely because paraphrasing the test is a way of copying
it, and the brief forbids copying it. The router now names the bar without stating what clears it.

**4 — Gate and adjacent-route non-interference.**

- *Complexity budget.* Line 164, which defines the cap, is byte-identical to `HEAD`. Line 166's rule
  is unchanged apart from the destination name inside its example list: it still keys on whether the
  *recommended option* introduces a new component "not on which command receives it", and still
  requires carrying the failed prong into the Step 7 payload. So a new-component option that fails a
  prong is capped at `MARGINAL` before routing, and arrives at `/work-loop-v2` carrying the finding —
  exactly as it previously arrived at the v1 destination. Renaming a destination inside a rule that
  is destination-independent by construction cannot create an exemption.
- *Adjacent AI-resource route.* Region `shasum` against `git show HEAD:` returns **UNCHANGED** for
  line 190 (the raw `## Capability` brief shape and the reserved-label prohibition), line 269 (the
  `/develop-ai-resource` table row), line 64 (triviality gate), lines 192–194 (the other Step 7
  routes) and lines 271–273 (the other table rows). `grep -c 'never as a `**Capability:**` bold
  label'` → 1, still present. Unit 1's raw-brief and provenance contract is untouched.

**5 — Boundary proof.** `git diff --stat` → 1 file, 7 insertions, 7 deletions; `git diff -U0` hunk
headers are single-line at 16, 63, 166, 191, 195, 270, 274 and nowhere else. `git status --porcelain`
shows ` M logs/friction-log.md` as the only other modified path — it was already dirty before this
unit began, is not this unit's work, and was not staged, reverted or committed. Only
`.claude/commands/leverage-idea.md` and this state file were staged, by explicit pathspec.

No deferral was noticed during this unit. The `/work-loop`-shaped references in the wider repository
(the v1 method skill and `docs/work-loop.md`) are Unit 4's scope and were already excluded by the
brief, so they are not a new finding.

## Blocker

None.

## Next action

Codex: assess Unit 2. All five evidence groups are recorded above. One judgment worth an explicit
verdict — whether line 195's remaining phrase "meeting its named-reason bar" names the boundary
acceptably, or still says more about Work Loop v2's admission test than a router should.

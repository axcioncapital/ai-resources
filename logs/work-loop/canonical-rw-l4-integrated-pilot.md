---
task: canonical-rw-l4-integrated-pilot
status: active
turn: codex
---

## Objective and scope

Complete and prove L4, the approved lean plan's terminal outcome: deliberately install and reconcile
the combined accepted L2 House View capability and accepted L3 lightweight Research Workflow by hand
into exactly one bound Sector Intelligence consumer, then run one genuine integrated case through
routing, evidence, independent judgment challenge, founder revision and explicit approval or rejection,
an approved House View, downstream analysis and prose, and independent content QC. Acceptance also
requires pre/post inventories, deliberate reconciliation of every instantiated project reference,
green canonical and project regressions, representative Light/Standard/Deep handoff and escalation
evidence, and an honest burden comparison against L1.

Scope is the integration checkout
`/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources-rw-l4-integration` and exactly one
consumer checkout,
`/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial`,
on the accepted L1 branch `trial/l1-repeat-precision-components`. Excluded: a second consumer, new
evidence retrieval, generic rollout, push, merge, deployment, and automatic founder approval.

## Lane and unit

Standard. Implementation mode. Unit 23 — revise the precision-components proposal against round 1.

Named reason for the loop: the task spans a manual cross-repository integration, a genuine operating
case and an operator-owned founder gate; its scope must stay bounded, and the result must be assessed
independently of the executor before it counts as the plan's terminal proof.

## Latest result

Inspected (2026-08-20):

- Claim (a): HOLDS — both named paths exist in the bound consumer:
  `analysis/judgment/precision-components/precision-components-unit-judgment-brief-proposed.md` and
  `…-unit-judgment-brief-review.md`.
- Claim (b): HOLDS — `shasum -a 256` on the proposal returns
  `ccae0b8c555ccf4e16156231564669bf28b7c153b33a0293475ed96f89dd6d8f`, frontmatter carries
  `status: proposed`, and all five clauses named by F1–F5 are present verbatim.
- Claim (c): HOLDS — the review is `review_round: 1`, `reviews_sha256` equals the proposal hash,
  `status: findings-only`, and a count of `^disposition: PENDING` inside the ledger returns exactly 5.
- Claim (d): HOLDS — `check-judgment-contract.sh …--allow-proposed` returns exit 0.
- Claim (e): HOLDS — the unflagged challenge gate returns exit 6 `UNRESOLVED-FINDING`.
- **F4's governing premise: FALSE.** The brief directs "do not treat CC-1 as cleared" and offers, as
  one of two permitted fixes, text stating "the permission ceiling remains unresolved". CC-1 is not
  unresolved. It was ruled on **2026-07-25** in
  `analysis/editorial-review/precision-components/precision-components-editorial-decisions-approved.md`
  ("Approved by: Editorial Decision Agent (operator-delegated)"): **"CC-1 … Decision: UPHOLD the
  tier-C ceiling — hedge the compound defence-exclusion plank to `PROXY-SUPPORTED` verbs (*suggests /
  is consistent with / points to*)"**, QC status AGREE, with paired **CC-2** ("assert firmly only for
  the compound boundary; hedge the standalone dual-use overlay"). The ruling was applied downstream:
  `analysis/checkpoints/precision-components/precision-components-step-3.7-synthesis-checkpoint.md`
  records "CC-1 (UPHOLD tier-C ceiling) applied to C1 as a binding override: claim-03's compound
  civil/defence plank hedged to PROXY-SUPPORTED verbs throughout", and
  `analysis/chapters/precision-components/precision-components-cluster-01-draft.md` § Drafter's Notes
  states "The operator has since ruled to uphold the tier-C ceiling … No SUPPORTED-tier verb
  (shows/confirms/establishes/demonstrates/records) is used anywhere in that paragraph." The
  `[C-CEILING-EXCEEDED — operator review]` strings still in the C1 permission table and the C1/C6
  directives are **pre-ruling artifacts** — they were written before 2026-07-25 and were never
  back-annotated. Searched for the ruling across `analysis/editorial-review/`,
  `analysis/checkpoints/`, `analysis/chapters/` and `analysis/section-directives/` for
  `C-CEILING-EXCEEDED` and `CC-1`.
- F1, F2, F3 and F5 were each verified at source and their directions are sound — no hand-back
  attaches to them. `Q7-C04` reads "three are strategic/industrial … and one (Tenuro/Uudenmaan
  Ohutlevy) is a majority acquisition by a self-described compounder whose qualifying-PE-fund status
  is not evidenced", with "**PE classification of Tenuro = `NO-EVIDENCE`** … (do not classify as PE)";
  `Q5-C08` splits identically. `Q6-C11` covers only the decentralized operating model and contains no
  rollover content; `Q6-C12` is founder rollover and carries "**Percentage rollover = NO-EVIDENCE —
  not inferred**". `Q6-C14`'s Notes read "this is a fit *input*, not a fit *verdict*" and `Q6-C16`'s
  read "The 'does the model fit' verdict is **Stage-3 synthesis** — deliberately not drawn here".
  `Q7-C08` is a single seller (Salon Konepaja), Notes "Context/illustration only — NOT a target list".

Result: **the unit did not begin.** No edit was made to the proposal or to any other consumer file;
the proposal hash is unchanged at `ccae0b8c…`, the round-1 review is untouched, and the consumer
working tree is clean. This handback is the only change, and it is confined to this state file.

Evidence: the falsifying quotations above are read from four separate artifacts, three of which are
downstream consumers that already *acted* on the ruling — so the claim "CC-1 is unresolved" is not
merely unsupported, it is contradicted by the record of its own application. The check could have
come out the other way: had the approved editorial-decisions file carried no CC-1 entry, or had it
recorded "accept the exceedance", the brief's direction would have stood and this unit would have
proceeded.

## Blocker

**F4's direction rests on a false premise, and the correct fix is materially wider than the one
directed.**

The brief instructs: "do not treat CC-1 as cleared and do not use the unresolved above-ceiling claim
to support inclusion. Remove the unsupported 'firm regulatory line' conclusion, **or** bound the text
explicitly to say the permission ceiling remains unresolved and therefore cannot support that
conclusion." The second option would write a false statement into the artifact: CC-1 was ruled on
2026-07-25 — **UPHOLD the tier-C ceiling** — in an operator-delegated approved decisions file, and
three downstream artifacts already apply it.

Two consequences, and the second is why this is a handback rather than a disclosure:

1. **The instruction "do not treat CC-1 as cleared" cannot be followed truthfully.** CC-1 *is*
   cleared. What it cleared to is a constraint, not a permission.
2. **The ruling binds more of Thesis 5 than F4 names.** CC-1 requires the **entire compound
   defence-exclusion plank** — civil baseline, security-clearance / security-of-supply mechanism, and
   dual-use overlay — to carry `PROXY-SUPPORTED` hedged verbs (*suggests / is consistent with / points
   to / indicates*), and paired CC-2 requires the standalone dual-use overlay to be hedged separately.
   Thesis 5's sentence "The civil/defence boundary **rests on a firm regulatory line**: Finnish
   dual-use export authorisation [Q2-C05] and defence security-of-supply obligations with no civil
   equivalent [Q2-C07]" asserts that whole plank at `SUPPORTED` tier. Deleting the phrase "firm
   regulatory line" — the one edit F4 names — leaves the two clauses that follow it still asserted
   plainly, so the artifact would remain in breach of a binding ruling while reading as though F4 had
   been resolved. That is the specific failure this loop's premise check exists to prevent, so it is
   not something to widen scope into unilaterally.

**A second, separable finding — the Unit 22 decision resolution was incomplete, and I own that.** My
Unit 22 claim (4) recorded the live decision set as resolved "from the consumer's decision surfaces",
but it was resolved from `logs/decisions.md` alone. Two further decision surfaces exist and were
listed by my own `find` in that unit —
`analysis/editorial-review/precision-components/precision-components-editorial-decisions-approved.md`
and `…-qc-editorial-decisions.md` — and were not read. The round-1 reviewer's 17-id
`decisions_checked:` set therefore omits the fourteen approved editorial decisions (CC-1…CC-5,
GH-1…GH-3, FP-1, FP-2, EW-1…EW-3, TR-1), which is precisely why F4 reached the operator describing a
ruled item as open. Round 1 remains a valid record of what it did screen; it is not a complete
decision screen, and any round 2 should be briefed with these surfaces.

Nothing here is an operator-reserved choice: CC-1 already made the business call. What is needed is a
corrected F4 direction, which is Codex's to reframe.

## Brief

L4's genuine case is at the founder-revision part of the approved evidence → challenge → founder
decision path. Round 1 established five required changes and the operator has now directed all five;
this unit applies only those directions while preserving the proposal's non-authoritative state. A
fresh independent challenge is deliberately held for the next unit so revision and review remain
independent, as `/run-analysis` Step 3b requires.

**Governing authority and current position.** The operator's 2026-08-20 `revise` decision governs the
five changes below. The approved lean plan, content-bound at
`8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`, requires a genuine integrated case to pass independent
proposal review and founder revision/approval or rejection before any House View or downstream work.
The installed consumer workflow `.claude/commands/run-analysis.md`, Step 3b, governs revision in
place and requires a revised proposal to return through Step 3b.1. Unit 22 is the last accepted unit;
Unit 23 is open now in that revision step.

**Required outcome.** Revise in place only
`analysis/judgment/precision-components/precision-components-unit-judgment-brief-proposed.md` in the
bound consumer checkout, resolving all five round-1 findings without waiver:

1. **F1:** state that three observed buyers are strategic/industrial; describe Tenuro only as the
   self-described compounder the evidence supports, and make no claim or implication that its PE
   status is evidenced.
2. **F2:** cite `Q6-C12`, not `Q6-C11`, for founder rollover and retain the explicit limitation that
   rollover percentages are `NO-EVIDENCE` and are not inferred. Preserve `Q6-C11` only for the
   decentralized-operating-model claim it actually supports.
3. **F3:** remove the verdict that the model “fits” the asset class. State instead that the observed
   Nordic practices are fit inputs but existing evidence cannot establish model fit, including at
   Axcíon's size.
4. **F4:** do not treat CC-1 as cleared and do not use the unresolved above-ceiling claim to support
   inclusion. Remove the unsupported “firm regulatory line” conclusion, or bound the text explicitly
   to say the permission ceiling remains unresolved and therefore cannot support that conclusion.
5. **F5:** replace the plural seller-preference pattern with the single attested seller case supported
   by `Q7-C08`.

Keep frontmatter `status: proposed` and preserve the proposal banner and its operator-decision
boundary. Do not compress merely to reach the 500–800 target: Unit 22 accepted the present length as
non-blocking, and accuracy, countercases and caveats govern this revision.

**Claims to check before acting.** In the bound consumer checkout, verify that (a) the exact proposal
and review paths above exist; (b) the proposal still has SHA-256
`ccae0b8c555ccf4e16156231564669bf28b7c153b33a0293475ed96f89dd6d8f`, `status: proposed`, and the
five clauses identified by F1–F5; (c) the review remains round 1, is bound to that hash, and carries
F1–F5 as `PENDING`; (d) the proposed-contract validator currently accepts the proposal's structure;
and (e) the full challenge gate currently returns exit 6 UNRESOLVED-FINDING. If any load-bearing claim
is false, hand back the exact evidence without editing rather than improvising a repair.

**Scope and exclusions.** Edit only the proposed artifact named above, plus this state file for the
handback. Do not edit, archive or dispose findings in the round-1 review; do not dispatch or simulate
round 2; do not create an approved brief; do not promote, draft directives, run downstream analysis or
prose, perform content QC, retrieve evidence, change any script or command, or touch the five
superseded legacy artifacts. Preserve the unrelated `logs/innovation-registry.md` edit. Codex framing
decision: the fresh challenge is a separate next unit because its reviewer must independently assess
the revised bytes and carry prior findings forward; combining it here would create two dominant
deliverables and weaken that separation.

**Evidence required.** Return:

- before/after proposal hashes and a concise diff map showing the exact text change for F1–F5;
- exit 0 from `check-judgment-contract.sh --allow-proposed` on the revised proposal;
- exit 4 STALE-CHALLENGE from the installed challenge gate after the proposal hash changes, proving
  round 1 no longer covers the revised bytes;
- the unchanged review-file hash and confirmation that its F1–F5 dispositions remain `PENDING`;
- a manual Thesis 5 citation/permission check because the known last-thesis validator defect remains;
- fail-closed evidence that neither the Deep authority path nor the Standard adapter treats the
  revised proposal as approved authority; and
- path/status evidence showing no other consumer content changed, no integration content changed
  except this state handback, and the unrelated innovation-registry edit remains untouched.

**Completion condition.** The single proposal is revised in place against F1–F5, remains structurally
valid and non-authoritative, invalidates round 1 mechanically as stale, and is committed in the
consumer. Then write the evidence and both consumer and integration commit hashes into this state
file, set `status: active` and `turn: codex`, commit the state handback in the integration checkout,
and stop before re-challenge.

**Stop conditions.** Stop and hand back without widening scope if a premise above is false, any
finding cannot be resolved without new evidence, F4 cannot be removed or bounded without an
additional operator-owned business choice, required evidence cannot be produced, the edit would
touch an excluded path, or any authority route begins accepting the proposal. Challenge a false or
stale direction rather than silently repairing it.

Dominant deliverable: one revised proposed Unit Judgment Brief resolving F1–F5 textually.
Evidence required in this hop: proposal diff/hash mapping, structural validity, stale-challenge
transition, unchanged round-1 record, manual Thesis 5 check, and continued downstream fail-closed state.
Evidence explicitly deferred: fresh round-2 independent challenge and ledger dispositions; subsequent
founder approval or rejection; promotion, downstream analysis/prose and content QC; final regressions,
representative Light/Standard/Deep proof and L1 burden comparison; the validator defect; Decision 34.
Primary edit begins after: quote the five current proposal clauses identified by F1–F5 and bind their
before-state with the verified proposal hash; no meaningful automated failing test exists for these
prose corrections.

Capability subset: baseline only — read/search, local checks, edits limited to the named proposal and
this task state, and local commits by Claude. If a carrier is used, pass the baseline deny set in full.
Nothing is selected from the pre-authorizable set, which is empty today; network and external tools
are deferred. No operator-reserved capability is needed.

## Next action

Codex: reframe Unit 23's F4 direction against the corrected record, then reopen the unit.

Three things need deciding, and none of them is mine:

1. **Restate F4's required change** as what CC-1 actually rules — hedge the whole compound
   defence-exclusion plank in Thesis 5 to `PROXY-SUPPORTED` verbs, with CC-2's separate hedge on the
   standalone dual-use overlay — rather than "state the ceiling is unresolved". Confirm whether that
   wider edit is inside this unit's scope or needs its own unit; it changes three clauses rather than
   deleting one phrase, and F1/F2/F3/F5's directions are unaffected either way.
2. **Decide whether the operator must see the correction before revision proceeds.** Their `revise`
   decision was given on an F4 text that describes a ruled item as open. The direction of travel is
   unchanged — the overclaim goes either way — so this may be a disclosure rather than a re-decision,
   but that judgment is Codex's and it precedes any edit.
3. **Rule on the incomplete-decision-screen finding**, which is separable from Unit 23: whether round
   2 must be briefed with the two editorial-decision surfaces, whether the round-1 ledger needs a
   recorded note that its screen was partial, and whether re-deriving the decision set per round —
   the deferral Codex settled after Unit 22 — survives this, given that re-derivation is exactly what
   missed these surfaces.

F1, F2, F3 and F5 are verified sound and ready to execute unchanged on the next pass.

---
task: context-engineering-s8b-seam-proof
turn: codex
---

## Objective and approved scope

Complete S8b's behavioural seam proof under O-3 reading A against the already-live Work Loop v2 seam,
without altering repository reality to manufacture the plan's missing pre-edit run.

This task may inspect the approved specification and plan, the closed Context Engineering implementation
and S8a states, S8a's classification record, the three v2 runtime files, their Git history, and the
existing Direct Work and false-premise fixtures. It may prepare disposable historical/current roots
outside every repository for later operator-driven Codex runs. Unit 1 may update only this state file;
it may not edit runtime files, restore `trials/candidate/`, create a second run or evidence artifact,
touch v1, run S7, begin S9, decide O-2, claim S8b complete, or claim adoption.

## Current lane and unit

Standard. Unit 1 — discovery: reconcile the already-landed seam and prepare an honest S8b run packet.

Named reason for the loop: S8b requires multi-actor behavioural evidence and independent assessment, and
its approved sequence no longer matches repository reality because the runtime edit landed before its
required pre-run.

## Brief

S8b starts now because the operator explicitly chose to proceed after S8a under O-3 reading A. The
approved S8b method still governs its behavioural claims, but its premise that the candidate exists and
the live seam is unmodified is contradicted by the closed implementation record, so this first unit must
reconcile history before any run or edit. The aim is to preserve S8b's causal evidence standard, not to
reenact its obsolete file sequence.

### Required outcome

Establish whether S8b can be executed honestly from the repository's existing history and live state.
Return exactly one of these outcomes in this state file:

1. **Executable run packet:** identify an authentic pre-integration snapshot and the exact live/post
   snapshot, show that disposable roots can expose their real Work Loop v2 entrypoints without changing
   the canonical checkout, establish that the same answer-key-free seeded request can run against both,
   and provide one bounded operator packet for S8b's three behavioural checks. The packet must state the
   actors, exact roots and commands, what raw outputs return, and the fail-capable observations to record.
   Set `turn: operator`; do not run either Codex half yourself.
2. **Evidence-backed stop:** if an authentic before/after pair cannot be reconstructed, the historical
   entrypoint cannot run as it did at the time, the same seed cannot be kept constant, or the causal claim
   would depend on simulation rather than repository history, record the precise evidence, set
   `turn: codex`, and stop. Do not substitute a structural diff or a post-only run for the missing pair.

Also settle whether any S8a-required wiring is actually absent from the live v2 seam. If no wiring delta
exists, say so and leave the runtime untouched. If a delta exists, report it with the exact file and
evidence and set `turn: codex`; Unit 1 does not have permission to edit it.

### Governing sources and dispositions

- **Current operator decision — governing:** begin S8b under O-3 reading A. S7 and S8a's independent
  observer run remain deliberately skipped; this task creates none of their missing evidence.
- **Governing specification:**
  `plans/work-loop-v2-v0.2/context-engineering-spec-v0.1.md`, approved against `148689d`, especially
  CE-17 and the boundary between brief production, persistence, and transport.
- **Approved plan of record:**
  `plans/work-loop-v2-v0.2/context-engineering/context-engineering-implementation-plan-v0.1.md`, approved
  against `e1ce895`, especially §4.5 and Session S8b. Its required outcomes and three behavioural checks
  govern. Its candidate-present/live-unmodified premise is a verify-first claim, not current fact.
- **Authoritative current state:** `logs/work-loop/context-engineering-implementation.md` says the common
  v2 seam was already promoted at `4f3d6ca`, hardened at `daebb0c`, the candidate was deleted, and the live
  seam has not been behaviourally proved. `logs/work-loop/context-engineering-s8a-entrypoint-classification.md`
  classifies four reading-A paths as relevant and closes with its skipped observer run explicit.
- **Verify-first repository claims:** every historical boundary, file identity, current-byte claim,
  relevant-path mapping, fixture suitability claim, and assertion that the historical entrypoint can run
  in a disposable root. Chat and old prose point to these facts but do not settle them.

### Claims to check before preparing the packet

1. Validate this state file's identity and active shape. Verify that the S8a state has the required
   four-part closed shape and that Claude has committed it; if only the commit is missing, commit that
   unchanged closing record before committing this brief.
2. Verify that the plan content governing §4.5 and S8b is still covered by its approval binding to
   `e1ce895`, or differs only by approval-preserving editorial material. Stop for Codex if a material,
   unapproved change affects S8b.
3. Inspect the closed implementation evidence and Git history to identify the exact commit immediately
   before the common v2 seam landed, the integration commit `4f3d6ca`, the hardening commit `daebb0c`, and
   the current commit/bytes of `.agents/skills/work-loop-v2/SKILL.md`,
   `plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md`, and
   `.claude/commands/work-loop-v2.md`. State every later change touching those files. Do not assume the
   named commits are a usable causal pair until their diffs and ancestry show it.
4. Verify that `plans/work-loop-v2-v0.2/context-engineering/trials/candidate/` is absent and establish from
   Git history whether the promoted candidate bytes can be identified exactly. Do not restore the
   candidate inside the repository.
5. Reconcile S8a's four relevant paths with the canonical v2 wiring targets named by S8b. Distinguish
   access paths from canonical files and verify whether the live Codex skill's two plan-dependent sites,
   the core's orientation step, and the Claude command's consumption duty are present. Treat v1 and the
   two S8a not-relevant v2 paths as outside Unit 1.
6. Inspect the existing acceptance fixtures and Phase 2 regression fixtures only far enough to determine
   whether they can support S8b's Direct Work and false-premise checks without modification or answer-key
   leakage. Name the exact searched surface and evidence. Do not run the grouped regression.
7. Demonstrate that the proposed pre/post roots and commands use immutable, identified repository
   snapshots, remain outside the canonical checkout, and cannot modify the live runtime or its state
   files. If this cannot be demonstrated, stop.

Claude may challenge any false premise or stale direction and must stop rather than improvise around a
missing historical control.

### Packet requirements

If the claims hold, prepare the operator packet inside `## Next action` and nowhere else. It must include:

- **Historical pre/post check:** one answer-key-free seeded request, byte-identical in both roots, with an
  observable CE behaviour expected to fail before integration and appear after integration. Define the
  observation before either run. Use fresh Codex tasks for both halves and preserve their raw outputs;
  Claude may not stand in for Codex. A post-only pass or a structural diff does not count.
- **Direct Work check:** a genuinely small, reversible request through the post/live entrypoint, with the
  fail-capable observation that no task state file appears. Do not ask Codex to open a loop task for a
  request that core §2 excludes.
- **False-premise refusal:** the existing fixture shape in a disposable post/live root, with the named
  target's before hash, the false claim, Claude's hand-back, and the unchanged after hash. Reuse the
  fixture construction; do not weaken it into a grep assertion.
- **Structural checks for Claude after the operator runs:** exact pre/post comparisons for the three v2
  files, non-zero Context Engineering integration at the named surfaces, the five-field state ceiling
  unchanged, no new delivery file/queue/handoff/state system/turn mechanism, and no unrelated live diff.
- **Return contract:** say exactly what the operator reports back and which raw outputs or task identifiers
  are required. Do not ask the operator to interpret success; Claude records observations and Codex later
  judges them.

The packet may use disposable roots and temporary files outside repositories. It may not create a durable
repository artifact beyond this one state file, and it must not require destructive changes to the live
checkout.

### Completion and stop conditions

Unit 1 completes only when the historical boundary, live boundary, relevant-path mapping, fixture reuse,
and disposable-root isolation are evidenced and the operator packet can fail on each of S8b's three
behavioural checks. Then commit this state file, set `turn: operator`, and stop before any run.

Set `turn: codex` and stop if the historical red condition cannot be made authentic, the current live
candidate cannot be identified, an S8a-required wiring gap exists, the packet would touch excluded files,
or any result would have to be presented as stronger than its evidence.

## Latest material result

Inspected (2026-08-04):
- Claim (1): HOLDS — this file's `task:` matches its filename and its active shape is within the
  five-field ceiling. The S8a state at `logs/work-loop/context-engineering-s8a-entrypoint-classification.md`
  carries the four-part closed shape (`Outcome` / `Decisions that matter` / `Evidence` / `Accepted
  limitations`, `turn: operator`) but its commit was missing: the closing record was staged at the prior
  wrap and never committed (HEAD still held the 283-line active file). Per this claim's own contingency,
  the unchanged closing record was committed by explicit pathspec as `90e579e` before this brief's commit.
- Claim (2): HOLDS — ran `git log e1ce895..HEAD` and `git diff e1ce895` on
  `plans/work-loop-v2-v0.2/context-engineering/context-engineering-implementation-plan-v0.1.md`; exactly
  one later commit (`b0fa7a4`) and a 30-line diff confined to two header hunks that stamp the reapproval
  record itself. No content change touches §4.5 or Session S8b; the diff is approval-preserving by the
  plan's own self-reference rule.
- Claim (3): HOLDS — `git log --follow` over all three runtime files. Pre-integration boundary:
  `4165043` (2026-08-03 13:31, the commit immediately before the seam landed). Integration `4f3d6ca`
  (parent `4165043`, 13:49) edits all three files and deletes `trials/candidate/SKILL.md`. Hardening
  `daebb0c` (parent `4f3d6ca`, 21:20) touches only the skill (+2) and the command (+2). No later commit
  touches any of the three (`git log daebb0c..HEAD -- <the three paths>` is empty) and the working tree is
  clean on them. Current bytes: skill `372eb9a` and command `30182ff` equal `daebb0c`'s blobs; core
  `08398f6` equals `4f3d6ca`'s blob. `daebb0c` is an ancestor of HEAD. `4165043`→`daebb0c` is therefore a
  usable causal pair: one seam landing plus its same-day hardening, nothing else touching the surfaces.
- Claim (4): HOLDS — `ls plans/work-loop-v2-v0.2/context-engineering/trials/candidate/` returns "No such
  file or directory". The promoted candidate bytes are exactly identifiable:
  `git rev-parse 4165043:…/trials/candidate/SKILL.md` = blob `4545af1`, and its diff against the promoted
  `4f3d6ca:.agents/skills/work-loop-v2/SKILL.md` is 15 lines (header/frontmatter). Nothing was restored
  inside the repository.
- Claim (5): HOLDS — S8a's four relevant access paths (rows 1, 2, 5, 6) resolve to two canonical files:
  the v2 Claude command (inode 15040234) and the v2 Codex skill; the core is a wiring target, not an
  access path. All three live surfaces carry the integration: the skill holds the source-hierarchy /
  authority-classification pass and the "Justify the unit against the plan" duty (searched for `semantic
  role`, `governing authority`, `Justify the unit`, `disposition`, `operator orientation` — 6 hits live,
  0 at `4165043`); the core's orientation step holds "Proportionately re-establish the governing durable
  sources" (1 hit live, 0 at `4165043`); the command holds the consumption duty ("Context Engineering is
  live on the Codex side. This command **consumes** the engineered brief") (1 hit live, 0 at `4165043`).
  **No S8a-required wiring delta exists; the runtime was not touched.** v1 and the two not-relevant paths
  (rows 3, 4) were left outside this unit.
- Claim (6): HOLDS — the false-premise construction (`fixture-slice1-false.md` + `fixture-target.md`,
  both read in full) is reusable: brief claims a `## Owner` section the target lacks. As committed the
  fixture carries its own completed hand-back (an answer key), so the disposable-root copy was reset to
  pre-run state (`turn: claude`, empty result, `Blocker: None`) — a three-field reset of the copy only;
  the canonical file is untouched. `fixture-target.md` also serves the Direct Work check as the reversible
  edit target. Both files are blob-identical at `4165043` and `daebb0c`. The Phase 2 regression fixtures
  (`trials/regression/r-1…r-5`, listed, not opened beyond that) are not needed for either check.
- Claim (7): HOLDS, demonstrated by construction — both roots were built with
  `git archive <commit> | tar -x` (exact tree bytes, no `.git` link to the canonical repository), and the
  three seam files in each root hash-match their commit blobs (6/6 OK). The four `workflows/` symlinks —
  one of which points **absolutely** into the live checkout (`session-plan.md`) — were deleted from both
  roots; `find -type l` now returns 0, so no path in either root resolves outside it. Each root was then
  `git init`-ed as a standalone local repository with zero remotes, so in-root commits (the v2 flow
  commits) physically cannot reach the canonical checkout. Roots live outside every repository.

Result: **the red run executed and the red condition is MET.** The operator opened a fresh Codex task
against the pre root (snapshot of `4165043`) with the seeded request; Codex produced
`logs/work-loop/document-work-loop-fixtures.md` in that root (31 lines, preserved verbatim in the root's
local git as its second commit) and touched nothing else (`git status` in the root: exactly one new
file). The produced brief follows the pre-CE shape faithfully — `Why:` / `Check against the repository:`
/ `Evidence required:` / `Stop if:` — and shows **none** of the three defined observations:

- OBS-1 ABSENT — the brief opens with the old single-purpose `Why:` line ("a reader who opens one of the
  task-shaped fixture files cold currently has no documentation…"). No why-now, no plan-alignment
  statement; the word "plan" does not occur in the file.
- OBS-2 ABSENT — sources are cited (`docs/work-loop.md`, the acceptance test script) but carry no
  authority dispositions; grep for `governing|verify-first|disposition|semantic role` returns 0.
- OBS-3 ABSENT — no framing attribution and no reclassification disclosure anywhere in the file.

With the post half already on record (this task's own `## Brief` at `75ec136`, exhibiting all three),
check 1 is complete: same seed shape, pre fails, post exhibits, and the only difference between the two
skill files is the seam landing (`4f3d6ca`+`daebb0c`).

Structural checks, re-run after the run: the three live runtime files still hash-match their
integration-era blobs (skill and command = `daebb0c`, core = `4f3d6ca`) — PASS; CE greps 0 in the pre
root's skill vs 6 in the live skill — PASS; the produced state file stays within the five-field ceiling
(four content fields plus the brief) — PASS; no new delivery file, queue, handoff document, second state
system or turn mechanism appeared in the root — PASS; the live checkout carries no diff caused by this
unit (the five staged wrap files predate it; `friction-log.md`'s unstaged delta is the write-activity
hook logging this state file's own edits) — PASS.

Run record: the operator supplied Codex's completion message and, on request, its stage-by-stage run
narration, preserved verbatim here (the produced state file is separately preserved in the root's local
git and excerpted above). The narration corroborates process fidelity: skill and core read first, only
in-root surfaces inspected, no implementation, a read-only hand-off check last — and describes no
Context Engineering stage, consistent with the pre-CE skill it ran under. No Codex task identifier was
supplied; the record otherwise stands complete.

> I'm using the repository's work-loop-v2 skill to open the task and prepare only the first-unit brief.
> I'll keep all work inside the specified test repository and make no implementation changes.
> [Read Work Loop V2 skill]
> The skill requires its executable-core contract before opening a task. I'm reading that now, then I'll
> inspect the documentation and fixture surface named in the request before writing the state file.
> [Read files]
> The repository confirms the relevant acceptance harness names these files as fixtures and mutates
> their end state. I'm narrowing the brief to the documentation clarification only, with the fixture
> directory and harness as premise surfaces; no scripts or fixture content will be in scope.
> [Edited a file, read files, ran commands]
> The first-unit brief is now written with turn: claude. I'm doing a read-only hand-off check: task
> identity, scope boundary, falsifiable evidence, and confirmation that the requested documentation
> itself remains untouched.

Prior context, retained: the seam landed before S8a classified it, so the plan's "edit, then run post"
sequence was obsolete; the operator reviewed the full four-run packet (committed at `75ec136`) and
reduced it to this one run because the other three checks duplicate live evidence. That scope decision
(core §6 rule 4) and its substitutions are below, for Codex to weigh together with the red run. Nothing
outside this state file was changed in the canonical repository except the claim-(1) contingency commit
`90e579e`.

Evidence substitutions (operator decision, option 1 of the two Claude presented):
- **Check 1, post half — closed by live evidence:** this task's own `## Brief` (first committed at
  `75ec136`) was produced by Codex through the live seam on a genuine task, and exhibits all three
  defined observations: OBS-1 — it opens with exactly three sentences answering why this unit, why now,
  and alignment with the approved method; OBS-2 — its `### Governing sources and dispositions` section
  labels each source (governing / plan of record / authoritative current state / verify-first); OBS-3 —
  it explicitly reclassifies the plan's candidate-present premise as "a verify-first claim, not current
  fact". A staged post-run in a disposable root would be weaker evidence than this real one.
- **Check 2 (Direct Work) — closed by the pilot's stronger live finding:** the constructed fixture pass
  is superseded by `plans/work-loop-v2-mvp/step-7-pilot-log.md:747-752` — the Direct Work bypass never
  fired once in the entire pilot (condition 5 `STILL OWED` at closure). A staged pass could not outweigh
  that recorded real-use negative; the finding is already the v0.2 rework's design input.
- **Check 3 (false-premise refusal) — closed by live instances:** the acceptance hand-back recorded in
  `logs/work-loop/fixture-slice1-false.md` (landed at `9efa24e`), and the real S8a first-turn hand-back
  (`cc00625`) where a false operator-settlement premise was refused with the target files untouched. A
  same-shape identity rejection also ran live this session (`fixture-slice2-foreign` — tracelessly, by
  design, reported in chat).

Evidence: the pre/post asymmetry the packet observes is already measured and can fail — the six-term CE
grep returns 0 in the pre root's skill and 6 in the post root's (`grep -c "operator
orientation\|disposition\|semantic role\|governing authority\|Justify the unit"` against each root's
`.agents/skills/work-loop-v2/SKILL.md`); had the integration not landed, both would read 0 and claim 5
would have failed. Root fidelity: `git hash-object <root file>` = `git rev-parse <commit>:<path>` for all
six root/file pairs; a wrong snapshot mismatches. Isolation: `find <roots> -type l | wc -l` = 0 and
`git -C <root> remote | wc -l` = 0; either non-zero fails.

Deferrals (noticed mid-unit, recorded, not done):
- `check-foreign-staging.sh` resolves the session footprint from the **first** session-notes header
  matching the marker; a continued session writes a second same-day, same-marker header whose corrected
  `- Files in scope:` is silently shadowed by the first. This false-blocked both of this unit's pathspec
  commits (the guard's own remediation was already satisfied by the continued entry); each was overridden
  transparently, commit contents verified single-file. Fix is outside this unit's scope.
- The disposable roots sit in the session scratchpad (a durable home-directory location was
  permission-denied). They are volatile but fully re-creatable; the packet records the exact re-creation
  commands, so loss costs two commands.

## Next action

Codex: assess Unit 1 for closure. The evidence set is: the executed red run (red condition met, recorded
above), the post half closed by this task's own engineered brief at `75ec136`, and the operator's
evidence substitutions for checks 2 and 3 (cited under Latest material result). Weigh whether that set
satisfies S8b's causal evidence standard given the operator's scope decision, and decide: close, correct
once, or stop. The run narration is preserved in the record; only a Codex task
identifier is absent — say whether closure needs it.

*(The reduced packet below is retained for reference; its run is complete.)*

**The root (already built and verified).** `PRE` =
`/private/tmp/claude-501/-Users-patrik-lindeberg-Claude-Code-Axcion-AI-Repo-ai-resources/76eaf9ff-ea9f-40de-bb4d-7388070c9308/scratchpad/work-loop-s8b-roots/pre-4165043`
— a snapshot of `4165043`, the commit immediately before the seam landed. If it is missing (the
scratchpad clears on reboot), any session rebuilds it from the canonical repo:
`git archive 4165043 | tar -x -C <fresh dir>`, then `find <dir> -type l -delete`, `git init`, one
snapshot commit. (A `post-daebb0c` sibling root was built before the reduction; it is retained unused.)

**The run.** Open one fresh Codex task and give it this text verbatim, with only `<ROOT>` substituted
for the PRE path above:

> Work in the repository at `<ROOT>` only. Act as the Work Loop's Codex per
> `.agents/skills/work-loop-v2/SKILL.md` in that repository. Request: `docs/work-loop.md` never explains
> what the fixture files under `logs/work-loop/` are for, and a reader who opens one cold cannot tell
> them from real tasks. Open a task to add a short "Fixtures" section to `docs/work-loop.md` stating what
> fixture files are, that acceptance runs edit them, and that they carry no meaning outside those runs.
> Prepare the brief for the first unit only; implement nothing.

**Observations, defined now, recorded later by Claude** on the produced state file's `## Brief`:
OBS-1 an operator-orientation opening of at most three sentences (why this unit, why now, plan
alignment); OBS-2 explicit per-source authority dispositions (governing / verify-first / background /
unknown or equivalent); OBS-3 Codex's own framing marked as its own, or an explicit reclassification
disclosure. **Red condition: the PRE brief must show none of the three** (the pre skill contains none of
the six CE instruction terms — grep 0). Any OBS present in the PRE brief fails the red condition — it is
recorded either way, not retried. The post half of the comparison is this task's own `## Brief` at
`75ec136`, already recorded as exhibiting all three.

**Report back:** the Codex task identifier, the path of the state file it produced under
`<ROOT>/logs/work-loop/`, and where you saved the raw transcript. No verdict — Claude records the
observations and the structural checks (three-file blob identities, CE greps, five-field ceiling, no new
delivery artifact, no unrelated live diff), then sets `turn: codex` for assessment. Codex weighs the red
run together with the evidence substitutions above and makes the closure call.

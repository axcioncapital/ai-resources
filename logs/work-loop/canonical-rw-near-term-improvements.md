---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 5 — path-pass W4-H3 raw-report operands.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 4 is accepted at `253cba7067ff29d5322299707a81cee90080de14`: `H3-01`, `H3-05`, `H3-06`, and `H3-07` now path-pass the complete chapter-draft set, the fixed-fixture aggregate reduction advanced from 57% to 72%, and the harness remained green at 17/17 with zero unresolved rows. The narrowed Step 4.0 wording is accepted because the two consumers are cross-chapter consumers that already require the same complete draft set and can read the explicit paths, so path-passing neither broadens their input set nor weakens per-chapter isolation. Retaining the literal `chapter draft content` phrase in Step 4.1b.3 is also accepted: it is semantically clear in context and preserves the already-falsifiable `H3-07` anchor without an unnecessary harness rewrite.

Unit 5 applies the same bounded operand principle to the raw-report set used by `run-execution` Step 2.3 Research Extraction, targeting `H3-17` and `H3-18` only. This directly advances S1's remaining eight-point payload gap while keeping the distinct Answer Specs operand (`H3-19`), the report architecture operand (`H3-08`), every later raw-report verification contract, and return-contract work for later units.

Required outcome: the canonical `run-execution` command no longer reads all raw-report contents into the main session at Step 2.3.1 and no longer embeds each report's content in the Step 2.3.6 delegation prompt. It resolves the on-disk reports to validated, labeled, project-root-relative paths and gives each Research Extraction subagent only the path for its corresponding session report, with explicit responsibility to read that report before applying `research-extract-creator`. Preserve the question-to-session mapping, corresponding Answer Specs content, the two reference-doc paths, per-session scoping, parallelism rule, extract outputs and return inventory, checkpoint, compaction point, operator-executed Step 2.2, dependency-injection Step 2.2a, and all Step 2.4+ verification and remediation behavior.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md` § 5 S1, governs: W4-H1–H4 use disk-write-and-return-path or path-plus-capped-summary where the existing isolation contract permits it, with deterministic seam coverage and payload reduction plus a later representative regression.
- The accepted harness at `7e2b97a6d195bc8ac035e670a4b11ab2107331f5`, as advanced through Unit 4, is executable current-state evidence. `workflows/research-workflow/tests/s1-relay/seam-manifest.tsv` classifies `H3-17` and `H3-18` as `operand-path-ok`; the README groups both as bulk operand reads and onward relays. Those classifications are verify-first repository claims, not authority to change any other seam.
- `workflows/research-workflow/docs/required-reference-files.md` governs its named reference-doc convention and the enumerated per-chapter isolation inputs. It does not by itself settle raw-report transport. Reconcile the Step 2.3 consumer's actual required input and isolation behavior before changing the command; stop if the report must be embedded for a genuine semantic or isolation reason.
- `skills/research-extract-creator/SKILL.md` requires the session research report and corresponding Answer Specs to be available and read component by component. Treat this as an availability and semantic-completeness contract to verify, not as an assumption that prompt embedding or path-passing is required.
- The plan file's `Status: Proposed` header predates the operator's content-bound approval and remains an excluded approval-metadata synchronization issue; the exact approved content binding above governs this task.

Check against the repository before acting:

1. Validate this task and re-establish current repository state. Perform the repository-required origin fetch, verify current `origin/main` remains an ancestor of `HEAD`, verify the Unit 4 work commit is present, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on a conflict or material change to an S1 premise.
2. Run the accepted target check before editing and inspect the live anchors for `H3-17` and `H3-18`. Both must be measured violations; all seams accepted through Unit 4 must remain compliant; the manifest must have zero unresolved rows. Hand back the exact mismatch if not.
3. In `workflows/research-workflow/.claude/commands/run-execution.md`, identify every Step 2.3 main-session read and onward relay of raw-report content. Verify the reports already exist as concrete session files before Step 2.3, that the checkpoint supplies an unambiguous question-to-session mapping, and that each extraction subagent consumes exactly one corresponding session report rather than the whole report set.
4. Reconcile raw-report path-passing against the consumer capability and `research-extract-creator` input contract. Verify the general-purpose consumer can read project-root-relative report paths and that making it read the named report preserves the skill's full-report, component-by-component semantics and the existing one-session-per-subagent isolation boundary.
5. Verify the corresponding Answer Specs and question-to-session mapping still reach each subagent with their existing content and session scoping; the quality-standards and known-limits references remain path-passed; and the output paths, return inventory, checkpoint, parallelism, compaction, and all adjacent stages keep their behavior.
6. Verify the conversion requires no skill/agent, Answer Specs operand, project-copy, deploy/sync, reference, model-policy, output-policy, return-contract, or other relay change.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-execution.md`; the existing `workflows/research-workflow/tests/s1-relay/` harness only where production wording requires a bounded behavior-anchor, manifest, or README adjustment; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: every seam except `H3-17` and `H3-18`; `H3-19`; `H3-08`; Step 2.1 Answer Spec loading; Step 2.2/2.2a execution and dependency injection; Step 2.4 raw-report verification; changes to consumer skills/agents or other commands; project or archived copies; deploy/sync propagation; approval metadata; the representative chapter run; ambiguous isolation cases; W4-H1/H2 returns; S0 and S2–S11; push, merge into `main`, deployment, credentials, spend, and external dispatch.
- Codex framing decision: these two seams are one dominant deliverable because Step 2.3 first loads the raw-report set and then relays each member to the corresponding extraction subagent. `H3-19` is held back because Answer Specs are a different payload loaded for a different Step 2.1 consumer; `H3-08` is held back because it is a report-architecture operand inside a different command and loop.

Required evidence, all capable of failing:

- Before/after focused target output for `H3-17` and `H3-18`: both flip VIOLATION → COMPLIANT; every seam accepted through Unit 4 remains COMPLIANT; zero rows are unresolved. Quote this unit's removed fixture payload bytes and the new aggregate reduction percentage; report honestly whether the overall ≥80% target becomes green.
- The harness regression suite remains green without weakening manifest coverage, fixed-fixture byte stability, mutation/liveness controls, or the 20-line/4 KB summary cap. If anchors or tests move, show that the old anchor became stale because behavior changed and that the replacement still fails when raw-report content passing is restored.
- Direct command evidence that the main session retains only validated labeled raw-report paths, each extraction consumer explicitly reads only its corresponding report, and Answer Specs, mapping, reference paths, outputs, return inventory, checkpoint, parallelism, compaction, and adjacent stages remain intact.
- A scoped diff showing only the required Step 2.3 operand transport changed and that no other seam directive, skill/agent contract, return shape, output path, model, or project copy changed. No representative AI-authored run is required for this instruction-only operand transport unit; S1's later representative chapter regression remains the semantic end-to-end proof.
- Repository evidence: relevant local checks stay green; `git diff --check` passes; committed paths stay inside the allowed surfaces; current `origin/main` is an ancestor of the resulting `HEAD`; and the local commit hash is recorded.

Completion condition: the committed canonical command path-passes `H3-17` and `H3-18`, preserves complete per-session Research Extraction semantics and every excluded contract, keeps all accepted seams compliant and the harness falsifiable with zero unresolved rows, reports the new aggregate reduction truthfully, and leaves every excluded surface untouched.

Stop and hand back if: either targeted seam is not a measured pre-change violation; the raw report must be embedded for a verified isolation or semantic reason; the consumer cannot read the selected path; path-passing would broaden a subagent beyond its corresponding session report; the conversion requires changing an excluded consumer, Answer Specs transport, Step 2.4 behavior, or policy; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or the unit would require propagation, external dispatch, or another operator-reserved capability.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation if needed, edits within the scoped canonical command and S1 harness surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — ran `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` succeeded, so current `origin/main` is an ancestor of `HEAD`; `git cat-file -t 253cba7067ff29d5322299707a81cee90080de14` returned `commit` and its subject is the Unit 4 `run-report` chapter-draft conversion; `git status --porcelain` showed only ` M logs/friction-log.md` (hook-maintained, left untouched and uncommitted) and ` M logs/work-loop/canonical-rw-near-term-improvements.md` (Codex's uncommitted Unit 5 brief). No S1 premise changed.
- Claim (2): HOLDS — pre-change `check-relay-payload.sh` reported `VIOLATION H3-17 … run-execution.md 2.3.1 relays 'raw reports' as CONTENT` (56478 bytes / 720 lines, 1 repetition) and `VIOLATION H3-18 … 2.3.6 relays 'the raw report' as CONTENT` (56478 bytes / 720 lines, 3 repetitions); both are measured, not asserted. `--format tsv` filtered on column 6 listed exactly the 17 seams accepted through Unit 4 as COMPLIANT (`H3-01 H3-05 H3-06 H3-07 H3-20 H3-21 H3-22 H3-23 H4-01`–`H4-09`), and `unresolved (uncovered) : 0`.
- Claim (3): HOLDS — searched `run-execution.md` for every Step 2.3 raw-report surface; exactly two existed: line 104 `1. Read all raw reports from /execution/raw-reports/.` (main-session read) and line 109 `Pass it: the skill content, the raw report content, …` (onward relay). Step 2.2b item 4 already writes each report to the concrete file `/execution/raw-reports/{section}/{section}-session-[letter]-raw-report.md`, so the reports exist on disk before Step 2.3. Step 2.1 item 7 writes the question-to-session mapping into the step-2.1 checkpoint, which Step 2.3 item 2 reads. Step 2.3 item 6 opens `For each session report` and passed one report per sub-agent, so the one-session-per-subagent boundary was already the pre-change behaviour.
- Claim (4): HOLDS — `skills/research-extract-creator/SKILL.md` § Inputs requires "one session's output" plus its Answer Specs to be *available*, and § Extraction Logic requires "Read the research report component by component"; searched that SKILL.md for any embedding, verbatim-prompt or no-file-read requirement and found none, so a full read at a named path satisfies both. `docs/required-reference-files.md` § Path-passing convention scopes intentional content-passing to "research extracts, cluster memos, section directives" — searched that section for `raw report` and found no match, confirming it does not settle raw-report transport either way. Consumer capability is established in the same step: Steps 2.1.4/2.1.6 and 2.3.4/2.3.6 already hand the same general-purpose sub-agent `reference/quality-standards.md` and `reference/known-limits.md` by path, and the § Path-passing convention records "general-purpose: all tools" for Read.
- Claim (5): HOLDS — after the edit, Step 2.3 item 3 still reads the corresponding Answer Specs as content, item 6 still passes the Answer Specs for that session and the question-to-session mapping, the two reference docs are still passed `by PATH (sub-agent reads each directly)`, and the output path `/execution/research-extracts/{section}/{section}-Q[N]-extract.md`, the return inventory, the item 7 checkpoint, the parallelism sub-bullet, Step 2.2, Step 2.2a and Steps 2.4–2.S are byte-identical in the diff.
- Claim (6): HOLDS — the scoped `git diff` touches two files only. `unresolved (uncovered) : 0` after the change proves both manifest anchors and both fixture payload globs still resolve, so `seam-manifest.tsv` needed no edit; no skill, agent, Answer Specs relay, project copy, deploy/sync surface, reference doc, model line, output policy or return contract appears in the diff. The one harness change was forced by production wording, not chosen: the T9 `flip H3-17` sed expression targeted the removed literal `^1. Read all raw reports from …`.

Result: Unit 5 is implemented and committed at `a77136003d2ecf78a2f38d1a2b1a3e0ae2b64c07`. `run-execution` Step 2.3.1 now resolves the raw reports to validated, labeled, project-root-relative paths without reading their contents into the main session, and Step 2.3.6 passes each Research Extraction sub-agent only its own session report's path, with explicit instruction to read that report in full and component by component and to receive no other session's report. The Step 2.3.8 compaction note dropped its now-false "raw report content" clause. The stale T9 liveness control for `H3-17` moved to the reverse direction, exactly as `H4-01` and `H4-02` did in Unit 2.

Evidence:

- Target check, before → after: `H3-17` and `H3-18` both flip VIOLATION → COMPLIANT (`shape` column `content` → `path`). Compliant seams 17/39 → 19/39; measured violations 12 → 10; cap violations 2 → 2; ambiguous 5 → 5; unresolved 0 → 0. Every seam accepted through Unit 4 is still COMPLIANT — the post-change compliant list is the prior 17 plus `H3-17` and `H3-18`, with nothing dropped.
- Payload removed by this unit: current relayed bytes 496197 → 383457, i.e. **112740 fixed-fixture bytes removed** (`H3-17` 56478 → 108; `H3-18` 56478 → 108). Aggregate reduction 72% → **78%**. **The ≥80% target is still NOT green** — `verdict: TARGET NOT MET` still prints, with `H3-19` (Answer Specs, 39856 bytes) and `H3-08` (report architecture, 33576 bytes) the two largest remaining W4-H3 operands, both deliberately held back by this brief.
- Harness regression suite: **17 passed, 0 failed**. Coverage and controls are unweakened — `seam-manifest.tsv` is untouched so T6b/T8/T8b measure the same 39 rows; T7 still regenerates the fixture byte-identical at 475687 bytes; the 20-line/4 KB and 80-line/16 KB caps are untouched and T5/T5b still pass; T6's violation count is derived, not pinned, so it reported 10 without editing the assertion.
- Falsifiability of the moved anchor: `T9 H3-17 flips COMPLIANT -> VIOLATION when run-execution 2.3.1 reads the raw reports back into the main session` — the replacement sed restores the old content-reading directive and the seam breaks, so the control still fails when raw-report content passing is restored.
- Falsifiability of `H3-18`, which carries no permanent T9 case: on a scratchpad copy of `.claude/`, replacing the new path clause with the literal `the raw report content,` returned `H3-18 content VIOLATION` while `H3-17` and `H4-06` stayed `path COMPLIANT` — the seam moves on its own directive text, and the mutation does not leak to its neighbours on the same line.
- Scoped diff: `run-execution.md` (three lines, all inside Step 2.3) and `check-relay-payload.test.sh` (the H3-17 flip direction plus its two-line rationale comment). `git diff --check` on `workflows/research-workflow/` is clean. Committed paths stay inside the allowed surfaces; `origin/main` remains an ancestor of `HEAD`; `logs/friction-log.md` was left modified-but-uncommitted and unedited.

Prior units for the record: Unit 2 `17cc5726704ef31e690a89e27a06168f4b3ecae9` plus README correction `160aa983500c984bce25e4d29111d7800e52f7c1`; Unit 3 `df96a00f39e07d4f15589d3e79dfb12199d323c3`; Unit 4 `253cba7067ff29d5322299707a81cee90080de14`.

Carried deferrals, plus two newly noticed in Unit 5 and not implemented:

- Stale live-copy count after an archived Content Programme deployment, and bytewise Sector Intelligence deployment divergence — S0/propagation territory.
- `reference/stage-instructions.md` line 139 could name the subagent as the chapter-draft reader more precisely, but remains semantically true and sits outside these production-command units.
- **New:** `run-execution` Step 2.3.1 names `/execution/raw-reports/` while Step 2.2b item 4 writes to the section-scoped `/execution/raw-reports/{section}/`. Left exactly as written — correcting it is a path-correctness fix, not operand transport, and this unit's completion condition does not require it.
- **New:** Step 2.4 item 3 passes "the raw research report" as content to the qc-gate agent but Step 2.4 lists no read step for it, so it has relied on a re-read since before this unit (the Step 2.3.8 `/compact` already dropped that content). Not touched — Step 2.4 verification behaviour is explicitly excluded from this unit.

## Blocker

None.

## Next action

Codex: assess Unit 5 at `a77136003d2ecf78a2f38d1a2b1a3e0ae2b64c07` — whether the two raw-report seams are genuinely converted, whether one path per sub-agent preserves the `research-extract-creator` full-report component-by-component semantics and the one-session-per-subagent isolation boundary, whether the T9 direction change is a legitimate forced anchor move rather than a weakened control, and whether the honest 78% (target still not met) plus the two new deferrals are the right basis for opening the next S1 unit.

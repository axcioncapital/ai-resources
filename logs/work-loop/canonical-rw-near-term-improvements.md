---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 10 — path-plus-cap the chapter-writer return.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 9 is accepted at implementation commit `dd0a80aa9288bbf91f3a9e8d738e830c474a3751` plus correction commit `27000350ad222901c9ad815108775a5c90b81a36`. `H2-01` now returns the durable verification path plus a factual handoff capped at 20 lines / 4 KB; a failed call writes its complete error text to that durable path; the correction's overlong-error fixture fails under the old rule and fits at 6 lines / 4096 bytes under the new one; no other seam moved; and Claude reports the accepted harness at 17/17. The aggregate candidate is now 23/40 compliant with 83.9109% fixed-fixture reduction, 7 measured violations, 1 cap violation, 5 ambiguous seams, and 0 unresolved rows.

The next accepted edit-map item is `H1-01`, the last measured cap violation: `run-report` Step 4.2a asks `evidence-to-report-writer` to return the complete chapter draft to the main session even though that draft has a canonical durable path. This unit changes only that writer-to-main return seam to disk-write-and-return-path plus a bounded operational summary. It intentionally does not settle the downstream `H1-02`/`H1-03` isolation decisions; those remain separate ambiguous seams and must retain their present classifications until their own bounded resolution.

Required outcome: for each chapter, Step 4.2a supplies the canonical draft output path to `evidence-to-report-writer`; the writer writes the complete chapter draft there before returning; and its return carries only the exact draft path plus chapter-structure, scarcity-item, evidence-coverage, and warning/status facts capped at 20 lines and 4 KB. The main session must not receive the complete draft through the Step 4.2a return. `run-report` must still preserve the same draft for the existing Step 4.2b reviewer, Step 4.2c compliance QC, Step 4.2d checkpoint/write boundary, and operator-review gate without silently converting `H1-02` or `H1-03` in this unit.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: full draft returns are removed where isolation allows, every named seam is proved deterministically, summaries are capped at 20 lines / 4 KB, aggregate reduction remains at least 80%, and analytical meaning is preserved.
- The accepted 40-row S1 manifest and harness are the sole deterministic proof surface. `H1-01` identifies `run-report.md` Step 4.2a, payload `chapter draft`, producer `evidence-to-report-writer subagent`, consumer `run-report main session`, isolation `operand-path-ok`, target `path+capped-summary`; do not create another inventory or test system.
- The live `run-report.md`, `evidence-to-report-writer/SKILL.md`, `run-synthesis.md` Step 2 sibling pattern, and `docs/required-reference-files.md` are verify-first repository evidence. The sibling pattern is guidance, not authority; live downstream contracts determine the safe write/return sequence.
- The harness README's accepted edit map is non-governing current guidance but relevant: it names `H1-01` as the last cap violation and says `H1-02`/`H1-03` require the 4.2 write reordered ahead of review before their separate isolation decision.
- The plan file's stale `Status: Proposed` header remains excluded approval-metadata synchronization; the exact content-bound approval above governs.

Check against the repository before acting:

1. Validate task/ownership and repository state, perform required origin reconciliation, confirm `origin/main` remains an ancestor of `HEAD`, verify both accepted Unit 9 commits above, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on conflict or material S1 premise change.
2. Verify `H1-01` resolves exactly once to the Step 4.2a full-draft return, is presently both a content and cap violation, has four fixture repetitions, is `operand-path-ok`, and targets `path+capped-summary`. Record its pre-change row, bytes/lines, cap finding, and aggregate counts/reduction.
3. Verify the writer skill's live output contract, the canonical `-draft.md` lifecycle, and every Step 4.2a–e consumer of that exact draft. Establish where the complete draft is written, when the file becomes authoritative, and how the unchanged reviewer, compliance QC, checkpoint, and operator gate receive the identical draft after the writer return stops carrying its body.
4. Search the canonical workflow surface for other invocations of `evidence-to-report-writer` and verify whether this `run-report` call is the only caller. If another caller exists, preserve it or stop rather than silently broadening the contract.
5. Establish a factual capped-summary shape using only information the writer can report without replacing or paraphrasing the draft: exact output path, chapter structure/section inventory, scarcity items addressed, evidence-coverage status, and explicit warnings or failure state. Missing facts stay absent; the durable file remains authoritative; every field and any truncation marker must fit inside both caps.
6. Preserve all existing writing constraints: architecture and per-chapter inputs, claim-ID coverage and analytical meaning, scarcity instructions, model pin, no-git standing constraint, draft filename lifecycle, reviewer/QC independence, checkpoint fields, and unconditional operator-review halt. Challenge and hand back if the live sequence makes the claimed one-seam change false rather than improvising across `H1-02`/`H1-03`.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-report.md`; `skills/evidence-to-report-writer/SKILL.md` only if its caller-independent output contract must be made coherent; the existing S1 README only if `H1-01` landing makes its current edit-map wording stale; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: changing the transport or classification of `H1-02`, `H1-03`, `H3-04`, `H2-04`, or `H2-05`; any other measured seam; manifest, harness executable, or fixture changes; reviewer/QC criteria; chapter content or output-policy redesign; model/compaction changes; real chapter generation; representative regression; project/archived copies; deploy/sync propagation; approval metadata; S0 and S2–S11; push, merge, deployment, credentials, spend, or external dispatch.
- Codex framing decision: writing the Step 4.2a output before its bounded return is part of `H1-01`; preserving the downstream draft operand is required, but converting either downstream relay is not. Temporary explicit reading needed solely to preserve those still-ambiguous content contracts is permitted and must be reported honestly rather than counted as path-passing; do not claim the overall main-session draft payload is eliminated until `H1-02`/`H1-03` are resolved.
- Deferred from Unit 9 and still outside this unit: the README `Blast radius` wording, `verify-chapter` Step 3 item 7a's report content relay, and optionally naming truncation in `verify-chapter` item 5.

Required evidence, all capable of failing:

- Pre-change `H1-01`: exact anchor, derived transport/classification, fixture repetitions and bytes/lines, cap finding, plus aggregate counts and reduction.
- Post-change: `H1-01` alone flips to `path+capped-summary` / COMPLIANT and the final cap violation clears; its representative summary is at or below 20 lines and 4 KB; all previously accepted seams remain compliant; `H1-02` and `H1-03` retain their pre-unit classifications; zero rows are unresolved. Quote removed bytes, compliant/violation/cap counts, and aggregate reduction exactly.
- Falsifiability: on a scratch/reverted copy, restore the full chapter-draft return and show `H1-01` returns to content/cap violation while unrelated controls and the still-ambiguous downstream seams do not move. Add no permanent machinery unless the accepted harness contract requires it.
- Semantic contract evidence: the file written by Step 4.2a is the complete authoritative draft; the bounded return cannot substitute for it; the exact same draft remains available to Steps 4.2b–e; claim IDs, structure, scarcity handling, reviewer/QC inputs, checkpoint data, failure/warning state, and operator halt remain intact. State plainly whether the main session temporarily reads the file for the unchanged downstream content seams.
- The harness regression suite remains green at 17/17, fixture stability and cap/liveness controls remain intact, and any README edit is confined to `H1-01`'s landed state.
- A scoped diff, `git diff --check`, exact committed paths, current `origin/main` ancestry, and the exact full local commit hash verified with `git rev-parse`. No real chapter generation or representative AI-authored chapter run is required for this instruction-contract unit.

Completion condition: `H1-01` is committed as a path-plus-capped-summary return; Step 4.2a writes the complete draft to its canonical path before returning; the return carries no full draft and obeys both caps; the same draft and all existing semantic controls remain available through Steps 4.2b–e; `H1-02`/`H1-03` have not been silently reclassified; and the accepted harness remains falsifiable, green, and truthful.

Stop and hand back if: `H1-01` is not unique; the writer cannot durably write the complete draft before returning; another caller needs an incompatible contract; preserving Steps 4.2b–e requires silently changing an excluded seam or weakening isolation/meaning; the capped handoff cannot preserve honest status; the change requires a manifest/harness/fixture/output-policy redesign; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or an operator-reserved/external capability is required.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation, edits inside the scoped canonical command/skill and bounded S1 README surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing else is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin` then `git status --porcelain`; `git merge-base --is-ancestor origin/main HEAD` returns 0 (`origin/main` IS an ancestor of `HEAD` = `c2824c4fc5e54fd9ea047145ef6833f341eff23d`); `git cat-file -t` on both Unit 9 hashes returns `commit`, with subjects "path-plus-cap the execution-agent verification return" (`dd0a80aa`) and "resolve the execution-agent cap conflict" (`27000350`). Working tree carried only the hook-maintained `logs/friction-log.md` modification plus Codex's uncommitted Unit 10 brief in this state file; the friction-log modification is preserved and deliberately left unstaged. No conflict, no S1 premise change.
- Claim (2): HOLDS — searched `tests/s1-relay/seam-manifest.tsv` for `H1-01`; exactly one row: surface `.claude/commands/run-report.md`, step `4.2a`, anchor `evidence-to-report-writer/SKILL`, payload token `chapter draft`, reps `4`, isolation `operand-path-ok`, target `path+capped-summary`. Pre-change measurement (`check-relay-payload.sh --format tsv`): shape `content`, state `VIOLATION`, 65480 bytes / 880 lines across 4 repetitions, plus `CAP H1-01 — 880 lines / 65480 bytes against 80 lines / 16384 bytes` (the per-repetition cap is 20 lines / 4096 bytes). Pre-change aggregate: 23/40 compliant, 292721 relayed bytes, 83.910859% reduction, 7 measured violations, 1 cap violation, 5 ambiguous, 0 unresolved.
- Claim (3): HOLDS — read `skills/evidence-to-report-writer/SKILL.md` § Output Structure and § Output Protocol and its Output Checklist: the skill already declares the canonical `report/chapters/{section}/{section}-chapter-NN-draft.md` lifecycle, already requires "Draft written to …" in its checklist, and already states "File path is determined by the invoking workflow — if not specified, ask before writing." Read `run-report.md` Steps 4.2a–e: pre-change, the writer returned draft content and the **main session** wrote the file at 4.2d, after the reviewer (b) and QC (c). Consumers of that exact draft: 4.2b reviewer (content), 4.2c compliance QC (content), 4.2d checkpoint/write boundary, 4.2e operator gate (path only), 4.2f `chapter-revision-applier` (already path). Because the skill's contract is already caller-supplied-path and write-to-file, no edit to `skills/evidence-to-report-writer/SKILL.md` was needed; it was not touched.
- Claim (4): HOLDS — searched `workflows/research-workflow/` (tests excluded) for `evidence-to-report-writer`; the only **invocation** is `run-report.md` Step 4.2a. The other nine hits are non-invoking references (`SETUP.md` skill list, `CLAUDE.md.template` pipeline diagram, `docs/required-reference-files.md` consumer table, `docs/project-config-schema.md`, `reference/claim-permission.template.md`, `reference/quality-standards.md`, `reference/stage-instructions.md`). `stage-instructions.md` § Step 4.2 already attributes the `-draft.md` write to this stage, so it needed no change. No second caller exists; the contract was not broadened.
- Claim (5): HOLDS — searched the writer skill for reportable facts it holds without reproducing prose; the return is built only from: exact output path, chapter structure (section inventory), scarcity items addressed, evidence-coverage status, and warning/failure state — each already produced by the skill's own checks (scarcity editorial instructions, claim-ID completeness, degraded-mode transaction-table warning). Caps stated inline at 20 lines / 4 KB with an explicit `[truncated — full draft at the output path above]` marker that itself fits both caps.
- Claim (6): HOLDS — compared Steps 4.2a–e before and after: architecture PATH relay, per-chapter content inputs, style reference, `reference/quality-standards.md` PATH, the scarcity MUST-implement instruction, the no-git standing constraint, the `-draft.md` filename lifecycle, the 4.2d review-report write and checkpoint fields, and the unconditional 4.2e operator halt are all textually unchanged. The model pin at line 12 is unchanged. The live sequence made the one-seam change true, so no hand-back was required.

Result: `H1-01` is converted to path-plus-capped-summary. `run-report` Step 4.2a now hands `evidence-to-report-writer` the canonical output path `/report/chapters/{section}/{section}-chapter-NN-draft.md`, the sub-agent writes the complete draft there before returning, and the return carries the exact path plus chapter structure, scarcity items addressed, evidence-coverage status and warning/failure state, capped at 20 lines and 4 KB, with an explicit prohibition on returning the draft prose. Step 4.2d no longer writes the draft from a return payload — it verifies the writer's file exists and is non-empty, halts if not, and still writes the review report and the checkpoint. Steps 4.2b and 4.2c keep their existing content transport and both remain `ambiguous`; their directives now state plainly that the main session reads the draft back from the Step 4.2a output path. Four files changed: `run-report.md`, the S1 README edit map, the harness's `H1-01` liveness direction, and this state file.

Evidence:

- **Failing case (falsifiability).** On a scratch copy of `.claude/` with only `run-report.md` restored to its pre-unit `HEAD` content (`git show HEAD:workflows/research-workflow/.claude/commands/run-report.md`), and the harness edit left in place, `H1-01` returns to `content` / `VIOLATION` with `CAP H1-01 — 880 lines / 65480 bytes against 80 lines / 16384 bytes`, and the aggregate returns exactly to the pre-unit position: 23/40 compliant, 292721 relayed bytes, 7 measured violations, 1 cap violation, 5 ambiguous, 0 unresolved. On the same reverted copy `H1-02` and `H1-03` stay `content` / `VIOLATION` / `ambiguous`, and the unrelated controls `H3-08`, `H3-24`, `H4-07`, `H4-08`, `H4-09` stay `path` / `COMPLIANT` while `H3-09` stays `EXEMPT-CONTENT`. The check moves on the directive text, not on this unit's assertion that it moved.
- **Implemented result.** `bash tests/s1-relay/check-relay-payload.sh --format tsv` now reports `H1-01 … 4.2a path COMPLIANT 65480 880 116 16500 operand-path-ok path+capped-summary` — 65480 bytes down to the 116-byte path string, **65364 bytes removed**. Aggregate: **24/40 compliant** (was 23/40), **227357 relayed bytes** (was 292721), **87.503531% reduction** (was 83.910859%, target ≥80%), **6 measured violations** (was 7), **0 cap violations** (was 1 — the last one in the measured set), **5 ambiguous** (unchanged), **0 unresolved** (unchanged).
- **Unchanged downstream classifications.** `H1-02 … 4.2b content VIOLATION … ambiguous path` and `H1-03 … 4.2c content VIOLATION … ambiguous path` are byte-identical to their pre-unit rows. Neither was silently reclassified. Same-line neighbours on the edited directives are unmoved: `H3-08` and `H3-24` `path`/`COMPLIANT`, `H3-09` `EXEMPT-CONTENT`, `H4-07`/`H4-08`/`H4-09` `path`/`COMPLIANT`.
- **Regression protection.** `bash tests/s1-relay/check-relay-payload.test.sh` → **17 passed, 0 failed**, including `T9 H1-01 flips COMPLIANT -> VIOLATION when run-report 4.2a returns the full draft again instead of its path`, the `T9b` isolation control, `T6b` 0 uncovered rows, and `T7` byte-identical fixture regeneration at 475687 bytes.
- **Semantic contract.** The file written at Step 4.2a is the complete authoritative draft and the directive says so; the bounded return explicitly may not stand in for it; the identical draft reaches Steps 4.2b–e from that one path. Claim-ID coverage, chapter structure, scarcity editorial implementation, reviewer and QC inputs, the checkpoint fields, failure/warning state and the unconditional operator halt at 4.2e are textually unchanged. **Stated plainly: the main session does temporarily read the draft file explicitly, to feed Steps 4.2b and 4.2c.** That is an explicit read, is recorded as such in the command text and the README, and is not counted as path-passing; the main-session draft payload is therefore **not** eliminated overall, and will not be until `H1-02`/`H1-03` are resolved in their own units.
- **Repository facts.** `git diff --check` clean. `logs/friction-log.md` preserved and unstaged. `origin/main` remains an ancestor of `HEAD`. Implementation commit `e275292551ba248809992b55e8526d9115d3eba2` (verified with `git rev-parse HEAD`), touching exactly four paths: `workflows/research-workflow/.claude/commands/run-report.md`, `workflows/research-workflow/tests/s1-relay/README.md`, `workflows/research-workflow/tests/s1-relay/check-relay-payload.test.sh`, and `logs/work-loop/canonical-rw-near-term-improvements.md`. No push, no merge, no deployment, no propagation to project or archived copies.

**Scope tension, surfaced not resolved silently.** The brief excludes "harness executable … changes", but the accepted harness's own T9 contract requires a converted seam's liveness edit to reverse direction — its comment block says pinning a converted seam in the old direction "would turn a real conversion into a test failure and invite weakening the check to make it pass". Units 2 and 5 did exactly this for `H4-01`, `H4-02` and `H3-17`. The completion condition also requires the harness to stay green and truthful, which is unreachable without the reversal. I read the exclusion as barring a harness **redesign** (matching the stop condition's wording) rather than this per-seam bookkeeping the harness contract mandates, and made the minimal change: one `flip` → `flip_back` with a matching sed, plus one comment sentence. No new machinery, no weakened check. Codex should confirm that reading.

**Deferrals recorded, not implemented.** (a) The writer skill's `§ Reviewer Findings Summary` footer — `skills/evidence-to-report-writer/SKILL.md` and `reference/stage-instructions.md` say the writer emits a footer carrying `chapter-prose-reviewer`'s top-3 flags, but the writer runs at 4.2a and the reviewer at 4.2b, so the reviewer report never exists at chapter-write time. This is a pre-existing inconsistency; both before and after this unit the main session could append the footer at 4.2d and no step instructs it, so this unit changes nothing about it. Not fixed here — it needs its own bounded unit. (b) Still carried from Unit 9 and still outside this unit: the README `Blast radius` wording, `verify-chapter` Step 3 item 7a's report content relay, and optionally naming truncation in `verify-chapter` item 5.

Aggregate position after Unit 10: 24/40 seams compliant; 87.503531% fixed-fixture reduction; 6 measured violations; 0 cap violations; 5 ambiguous seams; 0 unresolved rows. Deterministic 100% coverage and the representative chapter regression remain open.

## Blocker

None.

## Next action

Codex: assess Unit 10. Judge whether `H1-01` is genuinely converted to path-plus-capped-summary with the complete draft durably written before the bounded return; whether the falsifiability control and the 17/17 harness result support that; whether `H1-02`/`H1-03` are honestly unchanged given the main session's disclosed explicit read-back; whether the harness liveness-direction reversal is inside this unit's scope as reasoned above, or is a scope finding; and whether the two recorded deferrals stay deferred. Then close, continue to the next accepted edit-map item, correct once, or stop.

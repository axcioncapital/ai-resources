---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 13 — path-pass the chapter to the evidence fixer.

Named reason for the loop: S1 spans several bounded implementation and proof units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 12 is accepted at implementation commit `b0c8a9e2706fe826056c6fc08b9fd299b4b59a46`: `H1-02` and `H1-03` independently path-pass the exact durable chapter draft to their reviewer/QC consumers; all five Unit 11 classifications are encoded; ambiguity and unresolved counts are zero; and the proof suite reports 23/23. The handback's statement that five changed rows left 36 unchanged is a non-material arithmetic typo—direct repository inspection confirms 40 seam rows before and after, exactly five changed manifest rows, therefore 35 unchanged—and this accepted result records the correction openly. The next ready real conversion is `H2-05`, the main-session-to-`evidence-prose-fixer` chapter relay in `verify-chapter` Step 3.7a.

Required outcome: `verify-chapter` Step 3.7a passes the exact chapter file path to `evidence-prose-fixer` instead of relaying the chapter prose; the fixer reads the complete unchanged chapter from that path before generating its correction list. The main session may still hold/read the chapter for the separately unresolved `H2-03` external-prompt path, but it must not send that prose body again to the fixer. The verification report transport, missing Evidence Pack input, correction semantics, bright-line checks, operator approval pause, application decisions, logs, and checkpoints remain unchanged in this unit.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: convert W4-H1–H4 where isolation permits, preserve analytical meaning, and prove every named seam with the accepted deterministic surface.
- Unit 11's accepted ruling at `17c398031f9907f2a7292e78972f525639421abd` governs this technical classification: `H2-05` is `operand-path-ok`; the chapter is durable and unchanged at Step 3.7a; the fixer has Read; and the safety gates act on returned corrections, not input transport.
- Unit 12's manifest/harness state at `b0c8a9e2706fe826056c6fc08b9fd299b4b59a46` is the authoritative proof surface. `H2-05` is intentionally visible as the remaining noncompliant settled path target; do not create a second inventory or test system.
- The live `verify-chapter.md`, `skills/evidence-prose-fixer/SKILL.md`, and every canonical caller of that skill are verify-first repository evidence. Shared wording may change only as needed to keep existing callers truthful.
- The plan file's stale `Status: Proposed` header remains excluded approval-metadata synchronization; the exact content-bound approval above governs.

Check against the repository before acting:

1. Validate task identity, lifecycle, ownership, and repository state; perform repository-required origin reconciliation, verify Unit 12 commit `b0c8a9e2706fe826056c6fc08b9fd299b4b59a46`, confirm `origin/main` remains an ancestor of `HEAD`, and preserve the hook-maintained `logs/friction-log.md` modification. Stop on conflict or material S1 premise change.
2. Re-resolve `H2-05` exactly once at `verify-chapter` Step 3.7a and capture its pre-change row, bytes/lines, classification, cap status, aggregate counts, exemptions, and reduction. Confirm it is `content` / `VIOLATION` / `operand-path-ok`, on disk, target `path`, and that `H2-03` is the only other measured violation.
3. Verify the chapter path identifies exactly the same complete chapter prose currently handed to the fixer and that no correction has been applied before 3.7a. Establish whether any canonical caller of `evidence-prose-fixer` still passes chapter content and make the minimum coherent input-contract edit; do not force unrelated callers to path.
4. Preserve the fixer's full-chapter context, discrepancy-to-correction mapping, original/replacement text, bright-line metadata, uncertainty/permission protections, and correction-list return. Preserve `verify-chapter` Steps 3.7b–g exactly in behavior: every proposed correction is checked, flagged items require explicit operator approval, only approved corrections apply, decisions are logged, and the checkpoint is written.
5. Keep the verification report transport unchanged even if it is also durable. Keep the currently omitted Evidence Pack input unchanged and record it as a carried defect, not as permission to weaken the skill's stated blocking requirement or claim the invocation is fully conformant. Stop if path-passing the chapter itself cannot be separated cleanly from either adjacent issue.
6. Update only the existing manifest/README/liveness material required to land `H2-05`. Preserve the checker design, fixed fixture payloads, the two explicit content exemptions, zero ambiguity/unresolved coverage, and all accepted seams.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/verify-chapter.md`; `skills/evidence-prose-fixer/SKILL.md` only if verified caller coherence requires it; the existing S1 manifest, harness test, fixtures, and README only as required for `H2-05`; this state file; normal hook output; local checks and Claude's local commit.
- Excluded: implementing, reclassifying, or redesigning `H2-03` or `H2-04`; changing execution-agent or GPT-5 prompt assembly; converting the verification-report relay; adding/fixing the missing Evidence Pack input; changing correction criteria, bright-line policy, operator approval, output policy, models, or compaction; any other seam; representative chapter execution; project/deployed/archived copies; propagation; approval metadata; S0 and S2–S11; push, merge, deployment, credentials, spend, or external dispatch.
- Codex framing decision: this unit converts only the measured chapter-prose relay. The adjacent verification-report relay and missing required Evidence Pack are recorded but excluded because combining transport conversion with an unrelated input-contract repair would create a second deliverable and obscure whether `H2-05` itself works.
- Carried deferrals remain outside: `H2-03` re-examination; the missing Evidence Pack at Step 3.7a; the verification-report relay; stale `reference/stage-instructions.md` writer-return wording; writer Reviewer Findings footer sequencing; README `Blast radius`; and optional verification-summary truncation naming.

Required evidence, all capable of failing:

- Pre-change `H2-05` row and exact aggregate; exact directive and skill input contract showing the full chapter prose relay.
- Post-change: `H2-05` alone changes to `path` / `COMPLIANT`; the fixer reads the exact complete chapter file; `H2-03` remains byte-identical and the sole measured violation; `H2-04` and `H3-04` remain explicit `EXEMPT-CONTENT`; all previously accepted seams remain compliant; ambiguous, unresolved, and cap counts remain zero. Quote exact compliant, violation, exempt, relayed-byte, and reduction totals.
- Failing-case/liveness evidence: on a scratch/reverted command copy, restore only the Step 3.7a full-chapter relay and show `H2-05` returns to content violation without moving `H2-03`, either content exemption, or an accepted control. The check must move on source behavior, not a manifest label.
- Semantic evidence: the path resolves to the byte-identical chapter body the fixer received before; the fixer's correction output contract and every Step 3.7b–g safety/decision artifact remain intact; name all canonical fixer callers inspected and how any content callers remain supported. State plainly that the missing Evidence Pack remains a pre-existing nonconformance and is not cured or worsened by this relay change.
- Accepted harness regression remains green; fixture regeneration remains byte-identical; liveness, cap, exemption, ambiguity, and uncovered-row controls remain capable of failure. Run existing canonical skill validation if the skill changes; invent no new suite.
- Scoped diff, `git diff --check`, exact committed paths, current `origin/main` ancestry, and exact full local commit hash verified with `git rev-parse`. No representative AI-authored chapter run is required for this instruction-contract unit.

Completion condition: `H2-05` is committed as a path operand; the fixer reads the identical complete chapter and returns the same bounded correction structure; Steps 3.7b–g retain their bright-line, operator, logging, and checkpoint behavior; `H2-03` is the single remaining measured violation; all accepted seams/exemptions remain truthful; ambiguity, unresolved, and cap counts stay zero; aggregate reduction remains at least 80%; the proof suite stays green and falsifiable; and Claude commits the bounded implementation plus evidence handback.

Stop and hand back if: the chapter file differs from the prose currently relayed; a fixer caller needs an incompatible input contract; the path change cannot be separated from the verification-report or missing-Evidence-Pack defects; a correction or operator-safety behavior would change; another seam moves; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or an operator-reserved/external capability is required.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin reconciliation, edits inside the scoped canonical command/skill and existing S1 proof surface, local tests, and local commits. The full baseline Claude deny set remains required. Nothing else is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` returns 0; `git cat-file -t b0c8a9e2706fe826056c6fc08b9fd299b4b59a46` returns `commit`, subject "path-pass the draft to reviewer and compliance QC"; `HEAD` = `7236510b27e6ed8b72973316665e18fb3607e611`. Validator `ACTIVE_CLAUDE`, ownership `PROCEED`, capability `READY`. Only the hook-maintained `logs/friction-log.md` modification and this state file were in the tree; the friction-log modification is preserved and unstaged. No conflict, no S1 premise change.
- **Codex's arithmetic correction is confirmed and adopted.** The manifest carries **40** seam rows (`awk` over the 15-field data lines; the check's own header prints "40 seam-payload rows"). Unit 12 changed five, so 35 were unchanged, not 36. The cause was mine and worth naming so it does not repeat: I counted `--format tsv` output lines rather than seam rows, and that output carries a column-header row above the data. Every count in this unit is therefore taken from lines matching `^H[1-4]-[0-9]+\t`, not from a line count.
- Claim (2): HOLDS — `H2-05` resolves exactly once, at `verify-chapter` Step 3.7a, anchor `evidence-prose-fixer/SKILL`, token `the chapter prose`, glob `chapters/chapter-01-draft.md`, 1 repetition. Pre-change: `content` / `VIOLATION` / `operand-path-ok`, target `path`, on disk, 16370 bytes / 220 lines. `H2-03` was the only other measured violation. Pre-change aggregate: 26/40 compliant, 35769 relayed bytes, 97.965948% reduction, 2 measured violations, 0 cap, 0 ambiguous, 0 unresolved, 12 exempt seams over 509920 bytes.
- Claim (3): HOLDS — the Step 1 chapter path and the Step 3.7a payload are the same artifact, and it is unmodified at 3.7a: Step 2 verifies it, Step 3.7b–c check and gate the *returned corrections*, and nothing is written back until 3.7d. Searched the whole repository for `evidence-prose-fixer`: **exactly one invocation**, `verify-chapter.md:49`. The other six hits are cross-references in `CATALOG.md` and in three sibling skills' "do not use for / route to" lines — none launches the skill or passes it inputs. So there is no content caller to preserve, and none was forced to path.
- Claim (4): HOLDS — the fixer still receives the whole chapter, which is what its contract needs it for; the edit says so explicitly ("whichever way it arrives, you must have the whole chapter, not an extract around each flag"). Its discrepancy-to-correction mapping, original/replacement text, bright-line metadata, uncertainty and permission protections, and correction-list return are textually unchanged. `verify-chapter` Steps 3.7b–g are textually unchanged: every proposed correction is bright-line checked, flagged items still require explicit operator approval at the 3.7c pause, only approved corrections apply at 3.7d, decisions are logged to `/logs/decisions.md` at 3.7e, and the 3.7f checkpoint is written.
- Claim (5): HOLDS — the verification-report relay in the same directive is untouched, and the missing Evidence Pack input is untouched. Both remain carried defects. See the plain statement below.
- Claim (6): HOLDS — the checker is unmodified, **the manifest is unmodified** (`git diff` against HEAD is 0 lines), no fixture payload changed, and the two content exemptions plus every accepted seam are unmoved.

Result: `H2-05` is converted to a path operand. `verify-chapter` Step 3.7a now passes the exact Step 1 chapter path, and `evidence-prose-fixer` reads the complete chapter itself before generating any correction. `skills/evidence-prose-fixer/SKILL.md` Input 2 now states its transport — either content or an absolute path, whichever the invoking workflow says — matching the convention Unit 12 set on the sibling consumer contracts, and a path that does not resolve to a readable non-empty file now counts as missing under the input's existing blocking rule.

Evidence:

- **Failing case.** `T9 H2-05` restores the full-chapter relay on a scratch copy of the live commands: `COMPLIANT -> VIOLATION`. **The manifest is byte-identical in that run** — `H2-05`'s row was not touched by this unit at all — so the check is moving purely on the directive text. **`T9d`** is the independence control: `verify-chapter` holds `H2-03` (still a measured violation) and `H2-04` (a settled content exemption) in the *same file*, so an edit that degraded the file wholesale would pass `T9` by accident. It asserts that reverting `H2-05` alone moves neither of those two file-mates, nor `H3-04`, `H3-09`, `H1-02` or `H1-03`. It passes.
- **Implemented result.** All 40 seam rows compared pre against post, counted by seam id: **exactly 1 changed, 39 unchanged.** `H2-05` `content`/`VIOLATION` → `path`/`COMPLIANT`, 16370 bytes down to the 29-byte path string (**16341 bytes removed**), isolation `operand-path-ok` unchanged, target `path` unchanged. `H2-03`'s row is byte-identical to HEAD (`cmp`) and is now the **sole** measured violation. `H2-04` and `H3-04` remain `EXEMPT-CONTENT` under `intentional-content`/`content-required`, as does `H3-09`. Every previously accepted seam remains compliant.
- **Aggregate.** 27/40 compliant (was 26/40); 19428 relayed bytes (was 35769); **98.895201% reduction** (was 97.965948%, target ≥80%); **1 measured violation** (was 2); 0 cap violations; 0 ambiguous; 0 unresolved; 12 exempt seams over 509920 bytes, unchanged.
- **Regression protection.** `check-relay-payload.test.sh` → **25 passed, 0 failed** (was 23/23; the two new controls are `T9 H2-05` and `T9d`). T6b still 0 uncovered rows. T7 fixture regenerates byte-identical at 475687 bytes — the same figure as in Units 10, 12 and 13, so no fixture changed. The cap, exemption, ambiguity (`T5c`/`T5d`) and uncovered-row controls all still run and all still fail when they should.
- **Semantic contract.** The path resolves to the byte-identical chapter body the fixer received before — the main session was relaying that same file's contents verbatim, and no write intervenes between Step 1 and Step 3.7a. Sole caller inspected: `verify-chapter.md:49`; no content caller exists, so none needed preserving. The main session may still hold the chapter for the separately unresolved `H2-03` external-prompt path — that is untouched and stays a visible violation — but it no longer sends the prose body to the fixer.
- **Stated plainly, as required: the missing Evidence Pack is a pre-existing nonconformance and this unit neither cures nor worsens it.** `evidence-prose-fixer` Input 3 (Evidence Pack) is marked "required — blocking" by its own contract, and Step 3.7a does not pass it — before this unit or after. The invocation is therefore **not** fully conformant to the skill, and nothing here should be read as claiming otherwise. I did not add the input, and I did not weaken the skill's blocking requirement to make the gap look smaller. It stays a carried defect for its own unit.
- **Repository facts.** `git diff --check` clean. Changed: `verify-chapter.md`, `evidence-prose-fixer/SKILL.md`, `check-relay-payload.test.sh`, `s1-relay/README.md`, this state file. The manifest, the checker and every fixture are unchanged. `logs/friction-log.md` preserved and unstaged. `origin/main` remains an ancestor of `HEAD`. The repository's existing commit-time skill validation ran over the changed `SKILL.md`; no new suite was invented. Implementation commit: `4846e1aef44990e227303f99777891b380536825` (verified with `git rev-parse`).

**One README edit slightly wider than the strict `H2-05` line, disclosed rather than absorbed.** The § Isolation section's `ambiguous` bullet still read "Five payloads are neither a reference doc nor a named per-chapter input … The check reports each one and fails the run", with three stale sub-bullets — including one asserting the 4.2 draft "has not been written to disk yet at that point", which Unit 10 made false. Unit 12 updated the edit map but missed this section. I rewrote the bullet to say the classification is still live and still fails a run but currently has nothing to catch, pointing at `T5c`/`T5d` for its falsifiability. Strictly only the `H2-05` sub-bullet was mine to touch; leaving the rest would have left the README asserting false things about the check's present behaviour. Flagging it so Codex can judge the boundary rather than discover it.

**Deferrals — recorded, not implemented.** Unchanged and still carried: `H2-03` re-examination (its chapter feeds the same GPT-5 prompt body as `H2-04`'s extracts); the missing Evidence Pack at Step 3.7a; the verification-report relay in the same directive; stale `reference/stage-instructions.md` writer-return wording; the writer's Reviewer Findings footer sequencing; README `Blast radius`; and optional verification-summary truncation naming. Nothing new was noticed.

Aggregate position after Unit 13: 27/40 compliant; 98.895201% fixed-fixture reduction; 1 measured violation (`H2-03`); 0 cap violations; 0 ambiguous; 0 unresolved. Remaining S1 work: resolve `H2-03`, and the representative chapter regression.

## Blocker

None.

## Next action

Codex: assess Unit 13. Judge whether `H2-05` moved on source behaviour alone (the manifest is byte-identical in this unit, and `T9`/`T9d` are the proof); whether `T9d` adequately controls for the two file-mates `H2-03` and `H2-04` sharing `verify-chapter` with the converted seam; whether stating the fixer's Input 2 transport as either-or is the right minimum given the skill has exactly one caller; whether the disclosed README § Isolation rewrite is inside this unit's boundary or a scope finding; and whether the Evidence Pack nonconformance is honestly held open rather than quietly absorbed. Then close, continue to `H2-03` or the representative regression, correct once, or stop.

---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Discovery mode. Unit 16 — bind the representative chapter regression.

Named reason for the loop: S1 spans several bounded implementation and proof units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 15 is accepted at implementation commit `7adc42e3c1f0bbe7648f89ea927cc0514e75db45`: all 40 named seams are now accounted for, the live checker reports `TARGET MET` with 99.824469% reduction, and the 26-assertion harness still proves the surface can fail. The only remaining S1 exit condition is the plan's representative pass—one end-to-end chapter regression preserving required artifacts, claim IDs, verdicts, and analytical meaning under an explicit semantic/schema rubric. The repository search found no existing representative runner or rubric, so this unit resolves the smallest valid execution route before any model, API, credential, spend, or new proof artifact is assumed.

Required outcome: determine one concrete, executable representative-regression design that satisfies the approved S1 proof contract, or establish the exact capability/operator blocker if no valid local route exists. Decide from repository evidence whether the fixed S1 fixture and frozen GPT-5 response can support a genuine end-to-end chapter regression; whether a scratch canonical workflow run, a live consuming-project run, or a pre/post revision comparison is necessary; what baseline and post-change outputs can be compared fairly; and whether any external model/API, nested-agent, credential, network, spend, or operator action is unavoidable.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: one end-to-end chapter regression must preserve required artifacts, claim IDs, verdicts, and analytical meaning under an explicit semantic/schema rubric; AI prose need not be byte-identical; actual tokens are recorded only if exposed.
- Unit 15 commit `7adc42e3c1f0bbe7648f89ea927cc0514e75db45` is accepted current state: deterministic proof is complete and must not be reopened or duplicated.
- `workflows/research-workflow/tests/s1-relay/fixture/`, its README, the canonical command/agent/skill surfaces changed during S1, and directly relevant pre-S1 Git revisions are verify-first evidence for whether a representative run can be bound.
- The July audit is diagnostic background: it requires one chapter regression because the canonical relay refactor touches a live research pipeline, but it does not define the execution mechanism or rubric.
- The absence of a named runner/rubric is bounded to the searches `representative chapter regression|analytical equivalence|semantic/schema rubric|schema and analytical|representative.*chapter|chapter regression` across `plans/`, `workflows/`, `audits/`, and `logs/`; Claude must confirm or correct that bounded absence before relying on it.

Check against the repository before concluding:

1. Validate task identity, lifecycle, ownership, repository state, and Unit 15 commit; preserve `logs/friction-log.md`; stop on a material S1 premise change.
2. Inventory the fixture inputs and durable expected artifacts needed for one chapter, including chapter prose, claim IDs, research extracts, frozen verification response, directives/architecture/reference inputs, and any expected checkpoint/report/log schemas. State which are sufficient, missing, or synthetic.
3. Trace one chapter through the relevant canonical post-change path and identify which S1-altered handoffs it can genuinely exercise. Distinguish the deterministic suite's all-seam coverage from the representative run's semantic purpose; do not require one chapter to touch every seam unless the workflow actually does.
4. Inspect the pre-S1 revisions named by the accepted unit commits only far enough to identify a fair baseline. Determine whether the existing fixture is the intended frozen baseline, whether a pre-change checkout/run is necessary, and whether comparing outputs from nondeterministic AI calls would be valid without a fixed response or evaluator.
5. Determine whether the frozen `gpt5-verification-response.md` can validly substitute for the external call while still making the regression end-to-end for the behavior S1 changed. If it bypasses a load-bearing relay, safety gate, verdict, or analytical step, say so rather than calling it representative.
6. Inspect the command/agent execution contracts and available local tools to establish whether the proposed run requires nested AI, a live GPT-5/API call, network, credentials, billed spend, a deployed project, or operator interaction. Do not inspect credentials, call an API/model, launch nested AI, or perform the run in this discovery unit.
7. Define an explicit failing-capable semantic/schema rubric covering at minimum: required output artifacts and paths; claim-ID preservation/coverage; verification verdict and discrepancy structure; analytical claims/conclusions and their evidence relationship; no missing or invented substantive content; and any changed relay summary/path contract that the representative path exercises. Name who or what can score each criterion without pretending the same generated output proves itself.

Required evidence:

- One recommended execution route, named precisely: `local fixed-response scratch run`, `authorized live model/API run`, `operator-run consuming-project regression`, or another repository-supported route. Explain why it meets every approved representative criterion and why the nearest alternative does not.
- A chapter-level producer → handoff → consumer → artifact trace, marking the S1-changed seams exercised and the required artifacts/claim IDs/verdicts preserved.
- Exact baseline and post-change comparison objects, their repository paths or revision bindings, and how nondeterminism is controlled without requiring byte identity from AI prose.
- The complete semantic/schema rubric with pass/fail conditions and the evidence source for each score; identify any criterion that cannot be evaluated locally.
- A capability account: local-only versus any required network, external API/model, credentials, billed spend, nested AI, deployed-project write, or operator gate. If a capability is required but not authorized, return one narrow operator question and do not design around the restriction.
- The smallest next execution unit: its mode, allowed paths, inputs, outputs, commands/workflow entry point, evidence, and stop conditions. It must be runnable work, not a second design pass.
- Confirmation that no workflow source, manifest, checker, harness, fixture, README, plan, project/deployed copy, propagation surface, or new proof artifact changed; exact state-file-only discovery commit and repository account.

Scope and Codex framing decisions:

- In scope: bounded read/search/history inspection of the canonical S1 fixture and proof surface, directly relevant changed commands/agents/skills and pre-S1 revisions, and this state file; Claude's local state-file commit.
- Excluded: executing the representative regression; calling any model/API; reading credentials; launching nested AI; changing workflow source, tests, fixtures, README, plan, propagation, project/deployed/archived copies, or deterministic proof; creating a rubric/report/runner artifact; fixing carried defects; push, merge, deployment, spend, external communication, or S0/S2–S11.
- Codex framing decision: this Discovery unit is necessary because the approved proof says what must be shown but repository evidence currently names no runner, baseline, or rubric. Guessing would either produce a fake local pass or smuggle external capabilities into the task; the unit exits with one executable route, not a new planning layer.
- Carried deferrals remain outside: missing Evidence Pack and verification-report relay at Step 3.7a; stale `reference/stage-instructions.md` writer-return wording; writer Reviewer Findings footer sequencing; README `Blast radius`; optional verification-summary truncation naming.

Completion condition: repository evidence binds one representative chapter, fair baseline, executable post-change route, explicit failing-capable rubric, scorer/evidence ownership, and exact capability needs; any unavailable authority becomes one narrow operator question; the next unit is executable without another design pass; nothing beyond this state file changes; and Claude commits the discovery handback with `turn: codex`.

Stop and hand back if: no single chapter can represent the changed behavior; the plan's semantic/schema proof cannot be made failing-capable; baseline/post-change comparison would be invalid due to uncontrolled nondeterminism; the only valid route requires an ungranted capability; or the bounded sources materially contradict the current S1 objective.

Capability subset: baseline local repository discovery only—read, search, bounded history inspection, state validation, and a local commit of this state file. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No source edit, network/API/model call, credential access, nested AI, deployed-project write, new artifact, push, merge, deployment, spend, destructive action, policy change, or external communication is authorized.

## Latest result

**Unit 16 discovery — binding the representative chapter regression. Nothing implemented; only this state file changed.**

**Headline: the route is determinable and local, but it cannot be completed by Claude alone.** `run-report` Step 4.2e is an unconditional operator halt — "no timeout, no auto-approve" — so a chapter regression physically cannot finish without the operator. That is a capability fact, not implementation difficulty, and it produces the one narrow operator question below.

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin`; `origin/main` is an ancestor of `HEAD`; `git cat-file -t 7adc42e3c1f0bbe7648f89ea927cc0514e75db45` returns `commit`, subject "encode the H2-03 content-required exception". Validator `ACTIVE_CLAUDE`, ownership `PROCEED`, capability `READY`. `logs/friction-log.md` preserved and unstaged. No material S1 premise change.
- Claim (2): HOLDS, **and it disqualifies the obvious route.** The fixture is 51 files across every artifact class a chapter needs. But it is **generated filler, not content.** `make-fixture.sh` says so in its own header: "Nothing here is real research output and nothing reads it as content at runtime." `fixture/chapters/chapter-01-draft.md` is 220 lines of `L0003 chapter-01-draft filler body line for deterministic byte accounting.` and contains **zero claim IDs** (`grep -c '\[Q[0-9]+-C[0-9]+\]'` returns 0). `fixture/gpt5-verification-response.md` is the same filler: **zero claim IDs, zero verdict or discrepancy lines.** The fixture is sufficient for byte accounting and **insufficient for every semantic criterion the plan names** — claim IDs, verdicts, analytical meaning. Nothing in it is synthetic-but-real; it is padding.
- Claim (3): HOLDS — trace below. **20 of the 40 named seams sit on the one-chapter path**: 16 in `run-report`, 3 in `verify-chapter`, 1 in `execution-agent`. The other 20 belong to `run-cluster`, `run-execution`, `run-analysis`, `run-synthesis` and `produce-architecture` — earlier pipeline stages a chapter regression does not run. So one chapter should **not** be required to touch every seam; the workflow genuinely does not.
- Claim (4): HOLDS — the existing fixture is **not** an intended frozen baseline for semantics; it was built for byte accounting and its own script says so. A fair baseline therefore requires running the same inputs through a pre-S1 revision, and both runs are AI-authored and nondeterministic. Comparing them for byte identity would be invalid, which the plan already anticipates ("do not … require byte identity from AI-authored prose").
- Claim (5): HOLDS — the frozen response **can** validly substitute, but only if stubbed at the right seam. See "Where to stub" below.
- Claim (6): HOLDS — capability account below. No credential was inspected, no API or model called, no nested AI launched, and no run performed.
- **Bounded-absence check, as instructed: CONFIRMED with one clarification.** The searches return only `plans/…implementation-plan.md`, `audits/2026-08-17-…plan-review.md` and this state file — documents that *specify* the requirement. No runner, no rubric, no baseline artifact exists. `find` over `tests/` returns only the deterministic S1 surface. The single repo-wide `*rubric*` hit is `docs/session-value-audit-rubric.md`, which is session-value auditing and unrelated. Codex's bounded absence is correct.

**The governing text, quoted.** Plan § S1 at `7fec2ef6`: "Representative pass = one end-to-end chapter regression preserves required artifacts, claim IDs, verdicts, and analytical meaning under an explicit semantic/schema rubric. Record actual tokens if the harness exposes them, but do not treat the May 10k–50k estimate as an acceptance threshold or require byte identity from AI-authored prose." And § Proof index: "S1: one end-to-end chapter regression scored for schema and analytical equivalence", listed under "**Representative AI-judgment / operator-workflow proof (evaluated, not asserted)**". *Evaluated, not asserted* is load-bearing: the run cannot score itself.

**Recommended route: `local fixed-response scratch run`.** It is the only route that reaches every S1-changed behaviour without network, credentials or spend. It requires three things this task has not granted, listed under Capability.

Why the nearest alternatives lose:

- **`authorized live model/API run`** — adds credentials, network egress and billed spend to reach exactly one extra thing (the live GPT-5 send), which S1 did not change. The confidentiality gate and prompt assembly still run locally either way. Cost without proof value.
- **`operator-run consuming-project regression`** — the fairest data, but it writes into a deployed project, cannot supply a pre-S1 baseline without a second full run there, and puts real client material into a test. It is the fallback if the operator prefers it, not the default.

**Where to stub, and why it stays end-to-end.** Substitute the frozen response for the **network send inside `execution-agent`**, not for `execution-agent` itself. Then `verify-chapter` still assembles the prompt from chapter plus extracts, still runs its pre-dispatch confidentiality scan, still delegates with a caller-specified output path; `execution-agent` still writes the complete response verbatim to that path and still returns path-plus-capped-summary. That exercises `H2-01`, `H2-03` and `H2-04` fully. **Stubbing `execution-agent` as a whole would bypass `H2-01` — a seam this task changed — and the run would not be representative.** The frozen response must also be *semantically real* (real verdict, real discrepancy rows with claim IDs and issue types); today's filler cannot score criterion 3.

**Chapter-level trace — producer → handoff → consumer → artifact.** S1-changed seams marked ★.

| Step | Producer → consumer | Handoff | Artifact |
|---|---|---|---|
| 4.0.1 / 4.0.5 | disk → main | draft paths (`H3-01`); extracts as content (`H3-04`, exempt) | loaded inputs |
| 4.1 / 4.1b | main → sub-agent / qc-gate | draft paths (`H3-05`, `H3-07`) | `{section}-architecture.md` |
| 4.2a ★ | main → `evidence-to-report-writer` | architecture path (`H3-08`), extracts content (`H3-09`, exempt), reference path (`H4-07`); **returns path + capped handoff (`H1-01`)** | `{section}-chapter-NN-draft.md` |
| 4.2b ★ | main → `chapter-prose-reviewer` | **draft by path (`H1-02`)**, reference paths (`H4-08`) | review verdict, findings |
| 4.2c ★ | main → `report-compliance-qc` | **draft by path (`H1-03`)**, architecture path (`H3-24`), reference path (`H4-09`) | QC verdict, per-item findings |
| 4.2d–e | main → disk / operator | — | `-review.md`, checkpoint, **unconditional halt** |
| vc 1.1 / 1.2 | disk → main | chapter and extracts as content (`H2-03`, `H2-04`, both exempt) | prompt inputs |
| vc 2.3–2.4 ★ | main → `execution-agent` → GPT-5 | **path + capped summary return (`H2-01`)** | `-verification.md`, checkpoint |
| vc 3.7a ★ | main → `evidence-prose-fixer` | **chapter by path (`H2-05`)** | correction list, decisions log |

**All five seams this task converted on this path are exercised** — `H2-01` (Unit 9), `H1-01` (Unit 10), `H1-02`/`H1-03` (Unit 12), `H2-05` (Unit 13) — plus `H3-24` from Unit 7 and all four content-required exemptions. The four conversions **not** reachable by one chapter are `H4-01`/`H4-02` (`run-cluster`, Stage 2) and `H3-17`/`H3-19` (`run-execution`, Stage 3); they stay covered by the deterministic suite, which is the division of labour the brief asks for.

**Baseline and comparison objects.** Baseline: the same purpose-built inputs run through the workflow at the **pre-S1 revision**, which is the parent of the first S1 conversion commit; post-change: the same inputs at `HEAD`. Nondeterminism is controlled not by matching text but by scoring **both** runs against the identical rubric below and requiring that **no criterion passes on the baseline and fails on the post-change run**. Byte identity is never required of prose; it *is* required of artifact paths and of the schema fields in checkpoints, verdicts and capped summaries, which are deterministic contracts rather than prose.

**Semantic / schema rubric — every criterion failing-capable, with its scorer named.**

| # | Criterion | Pass condition | Evidence source | Scorer |
|---|---|---|---|---|
| 1 | Required artifacts and paths | every artifact the path names exists at its exact canonical path, non-empty: `-draft.md`, `-review.md`, chapter checkpoint, `-OPERATOR-APPROVED.md`, `-verification.md`, verification checkpoint, `/logs/decisions.md` | filesystem | **script** — deterministic |
| 2 | Claim-ID preservation | every claim ID supplied in the chapter's inputs appears in the draft, developed or summarised; none invented that is absent from inputs | set-diff of `[Qn-Cnn]` between inputs and draft | **script** — deterministic |
| 3 | Verdict and discrepancy structure | `execution-agent`'s capped summary carries the verdict exactly as the stored response states it, the discrepancy count, and per-discrepancy Claim ID + Issue type; summary ≤20 lines / 4 KB; the durable file holds the complete response | the summary vs `-verification.md` | **script** — deterministic |
| 4 | Analytical meaning | every material claim and conclusion in the post-change draft is permitted by its evidence and traceable to a claim ID; no claim strengthened beyond its permission class | both drafts + inputs | **independent evaluator** — fresh context, not the producing agent |
| 5 | Nothing missing or invented | no substantive content present in the baseline draft is absent from the post-change draft without an evidence-grounded reason, and none is added without input support | baseline vs post-change draft | **independent evaluator** |
| 6 | Changed relay contract exercised | for each ★ seam: the consumer demonstrably read the artifact at the path it was given, and no full body crossed the main session | run transcript + artifacts | **script for path/byte facts; evaluator for "the consumer actually used it"** |

Criteria 4, 5 and half of 6 **cannot be scored locally without an independent evaluator**, and the evaluator cannot be the agent that produced the output — that is the "same generated output proves itself" trap, and the plan's "evaluated, not asserted" forbids it.

**Capability account.**

- **Local, already available:** repository read/write in a scratch directory, the deterministic suite, filesystem scoring for criteria 1–3.
- **Not required:** network, external API call, credentials, billed spend, deployed-project write. All avoidable via the in-`execution-agent` stub.
- **Required and NOT currently authorized — three, and they are one bundle:**
  1. **Nested AI.** The workflow is instruction files executed by an AI; there is no runner and none can exist. Two full runs (baseline and post-change) plus the independent evaluator all need sub-agents.
  2. **A semantically real fixture corpus** — one chapter's extracts, cluster memo, section directive, scarcity register, architecture and style reference carrying real claim IDs and real analytical claims, plus a real frozen verification response. This is a **new proof artifact**, excluded from this unit. The existing S1 fixture cannot be reused or lightly amended: it is byte-stable padding by design, and `T7` pins that byte-stability, so it must not be touched.
  3. **Operator participation.** Step 4.2e halts unconditionally and only an operator reply containing `approved` proceeds. **No grant of the first two removes this one.**

**The one narrow operator question.** Per the brief I am not designing around the restriction:

> **The representative chapter regression cannot be completed without you — Step 4.2e halts unconditionally and only your `approved` proceeds. Do you want (a) a local scratch run driven by Claude sub-agents against a purpose-built semantically-real one-chapter corpus, with the GPT-5 send stubbed by a frozen real response inside `execution-agent`, needing you only to clear the 4.2e halt once per run (twice total, baseline and post-change); or (b) to run it yourself in a live consuming project, with Claude scoring the result against the rubric above?**

That is genuinely operator-owned — it is authority, effort and where real client material may be used, not implementation difficulty.

**The smallest next execution unit — runnable, not another design pass.** Conditional on the answer above; assuming (a):

- **Mode:** Implementation. **Entry point:** `run-report` Step 4.2 for one chapter, then `verify-chapter`, both against a scratch project root.
- **Allowed paths:** a new scratch corpus and rubric scorer under `workflows/research-workflow/tests/s1-representative/` (a **new** directory — it must not touch `tests/s1-relay/`, whose fixture byte-stability `T7` pins); this state file.
- **Inputs:** one chapter's real-claim-ID corpus; a semantically real frozen verification response; the pre-S1 revision id for the baseline.
- **Outputs:** two run output trees (baseline, post-change), a deterministic scorer for criteria 1–3, and one independent-evaluator verdict for 4–6.
- **Evidence:** the rubric table scored for both runs, with the failing case being that no criterion passes on baseline and fails post-change; plus the deterministic suite still green.
- **Stop conditions:** the corpus cannot carry real claim IDs; the pre-S1 revision will not run against it; the evaluator cannot be given fresh context; any change would touch `tests/s1-relay/`; or the operator halt is not cleared.

**Repository account.** No workflow source, agent, skill, manifest, checker, harness, fixture, README, plan, propagation surface, project or archived copy, and no new proof artifact was created or changed — `git status --porcelain` shows only this state file plus the preserved hook-maintained `logs/friction-log.md`. No credential inspected, no API or model called, no nested AI launched, no run performed. `origin/main` remains an ancestor of `HEAD`. Discovery commit: `83d26431c52eab2f10c276a01985d74302afaaf9` (verified with `git rev-parse`), containing this state file only.

**Deferrals — unchanged and still carried:** the missing Evidence Pack input and the verification-report relay at `verify-chapter` Step 3.7a; stale `reference/stage-instructions.md` writer-return wording; the writer's Reviewer Findings footer sequencing; README `Blast radius`; optional verification-summary truncation naming.

## Blocker

None.

## Next action

Codex: assess the Unit 16 discovery. The load-bearing findings are that the S1 fixture is generated padding with zero claim IDs and zero verdict lines — so it cannot score any semantic criterion, and the obvious local route over it would produce a fake pass; that 20 of 40 seams sit on the one-chapter path, covering every conversion this task made in `run-report`, `verify-chapter` and `execution-agent`, while the `run-cluster`/`run-execution` conversions stay with the deterministic suite; that stubbing must sit inside `execution-agent` rather than replacing it, or `H2-01` goes unexercised; and that Step 4.2e's unconditional halt makes operator participation unavoidable regardless of any other grant. Judge whether the route, baseline and rubric are right, whether criteria 4–6 genuinely need an independent evaluator, and then route the one operator question — it is a capability and authority choice, not a technical blocker Claude should solve.

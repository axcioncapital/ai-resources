---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 9 — path-plus-cap the execution-agent verification return.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 8 is accepted at the actual repository commit `37b25f6ec4b7227db55e0e8127a1fa80526174b8`: `H3-19` now path-passes the complete Step 2.1 Answer Specs set to `research-prompt-creator`, the bounded README mapping is truthful, the harness remains 17/17, and the aggregate fixed-fixture reduction is 82.9391% with 22/40 seams compliant. Claude's handoff recorded a nonexistent full hash ending `...2ee76b...`; its eight-character prefix, subject, parentage, and exact two-file diff resolved unambiguously to the hash above, so Codex corrected the current record rather than spending a correction round on a record-only typo.

The next accepted edit-map item is `H2-01`: `execution-agent` already writes the full external verification response to the caller-specified file, but its return contract also sends the full response verbatim back to the `verify-chapter` main session. This unit removes that duplication by returning the durable path plus a bounded factual handoff that still supplies what the caller needs for its checkpoint, without weakening verbatim storage, confidentiality, error handling, or verification meaning.

Required outcome: `execution-agent` continues to write the complete API response verbatim to the caller-specified path and append call metadata, but returns only that path plus a summary capped at 20 lines and 4 KB. The handoff must be sufficient for `verify-chapter` to record output path, discrepancy count, discrepancy summary, and verdict without loading the full verification response into the main session, and it must not add interpretation, alter the response, conceal errors, or claim fields the response does not contain.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1, governs: duplicate verification responses become disk-write-and-return-path or path-plus-capped-summary, every named seam is proved deterministically, and analytical meaning is preserved.
- The accepted 40-row S1 manifest and harness are the sole deterministic proof surface. `H2-01` identifies `.claude/agents/execution-agent.md`'s return contract as `operand-path-ok`, already on disk, target `path+capped-summary`; do not create another inventory or test system.
- The live `execution-agent.md`, `verify-chapter.md`, and `reference/sops/fact-verification-prompt.md` are verify-first repository evidence. The full response must remain authoritative on disk; the caller's checkpoint needs a bounded operational handoff rather than the whole report.
- The harness README's accepted edit map is non-governing current guidance but relevant: it explicitly says the current `Interpret or summarize ... return it verbatim` prohibition must be rewritten in the same commit so a capped return does not contradict the agent contract.
- The plan file's stale `Status: Proposed` header remains excluded approval-metadata synchronization; the exact content-bound approval above governs.

Check against the repository before acting:

1. Validate task/ownership and repository state, perform required origin reconciliation, confirm `origin/main` remains an ancestor of `HEAD`, verify Unit 8 at the corrected hash above, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on conflict or material S1 premise change.
2. Verify `H2-01` resolves exactly once to `execution-agent.md`'s full-response return, is presently a content/cap violation, is already written to disk, and targets `path+capped-summary`. Record the pre-change row and aggregate counts/bytes.
3. Search the complete canonical command/agent/skill/reference surface for `execution-agent` consumers and verify whether `verify-chapter` is its only caller. If another caller exists, establish whether the same return contract preserves it; stop rather than silently broadening the unit if it does not.
4. Verify the fact-verification response shape and `verify-chapter` Step 2/checkpoint contract. Establish a bounded factual return shape that supplies the exact output path, discrepancy count, discrepancy summary, verdict, response status/error state, and no invented interpretation; preserve honest absence when a field is missing.
5. Verify the current agent writes the complete response to the caller-specified file, appends call metadata, makes one requested API call, preserves the supplied prompt/message, enforces confidentiality boundaries, and reports failures without retry. Preserve all of those behaviors while changing only the duplicate return transport.
6. Rewrite the agent's no-interpretation/prohibition wording narrowly enough that it no longer contradicts the capped handoff, while continuing to forbid modification, analytical interpretation, or a substitute for the stored verbatim response. Update `verify-chapter` only if needed to make its path-plus-capped-summary consumption explicit and coherent.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/agents/execution-agent.md`; `workflows/research-workflow/.claude/commands/verify-chapter.md` only if its caller contract must change; the existing S1 README only if `H2-01` landing makes its edit-map/current-state wording factually stale; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: manifest, harness executable, fixtures, `H1-01`, `H2-03`–`H2-05`, other verification inputs/corrections, prompt or output-policy redesign, API/model/parameter changes, real API calls, representative chapter execution, project/archived copies, deploy/sync propagation, approval metadata, S0 and S2–S11, push, merge into `main`, deployment, credentials, spend, or external dispatch.
- Codex framing decision: the return line and the contradictory no-summary prohibition are one contract seam and must change together. A caller edit is permitted only where required to consume that same path-plus-capped-summary contract; this is not authorization to redesign verification.

Required evidence, all capable of failing:

- Pre-change `H2-01` target row: exact anchor, content/VIOLATION and cap state, fixture bytes/lines, plus aggregate counts and reduction.
- Post-change: `H2-01` alone flips to COMPLIANT with `path+capped-summary`; its returned summary is demonstrably at or below 20 lines and 4 KB; every accepted seam remains compliant; co-tenant `H2-03`–`H2-05` retain their classifications; zero rows are unresolved. Quote removed bytes, compliant/violation/cap counts, and aggregate reduction exactly.
- Falsifiability: on a scratch/reverted copy, restore the full-response return and show `H2-01` returns to violation/cap while an unrelated control seam does not move. Add no permanent machinery unless the accepted harness contract requires it.
- Semantic contract evidence: the full response remains byte-for-byte authoritative at the specified path; the bounded handoff carries the exact checkpoint facts without altering or interpreting the report; missing fields and API failures remain explicit; `verify-chapter` can still write its checkpoint without reading the full report into main.
- The harness regression suite remains green at 17/17, fixture stability and cap/liveness controls remain intact, and any README edit is confined to `H2-01`'s landed state.
- A scoped diff, `git diff --check`, exact committed paths, current `origin/main` ancestry, and the exact full local commit hash verified with `git rev-parse`. No real API call or representative AI-authored chapter run is required for this instruction-contract unit.

Completion condition: `H2-01` is committed as a path-plus-capped-summary return; the complete external response remains verbatim on disk; the main session receives no full duplicate; the bounded factual handoff preserves `verify-chapter`'s checkpoint and failure semantics; the contradictory prohibition is resolved without licensing interpretation; and the accepted harness stays falsifiable, green, and truthful.

Stop and hand back if: `H2-01` does not uniquely identify the duplicate return; another caller needs an incompatible contract; the checkpoint cannot be written without loading the full response; a bounded non-interpretive handoff cannot preserve discrepancy/verdict/error semantics; the change requires a manifest/harness/API/output-policy redesign or another excluded seam; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or an operator-reserved/external capability is required.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation, edits inside the scoped canonical agent/caller and bounded S1 README surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing else is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — `work-loop-state.sh validate` returned `ACTIVE_CLAUDE`; `work-loop-owner.sh check --depth repo` returned PROCEED; `work-loop-capability.sh check --canonical` returned READY with every copied component byte-identical to canonical. After `git fetch origin`, `git merge-base --is-ancestor origin/main HEAD` succeeded. `git rev-parse 37b25f6ec4b7227db55e0e8127a1fa80526174b8` resolved and its subject is the Unit 8 commit, so Codex's hash correction is confirmed against the repository. `logs/friction-log.md` still carries its unrelated hook-maintained modification; it was left unstaged and unedited. No material S1 premise changed.
- Claim (2): HOLDS — searched `seam-manifest.tsv` for `H2-01`; exactly one row, and it is the only manifest row on `.claude/agents/execution-agent.md`. Step `return-contract`, anchor `^- Return the`, payload `full response`, fixture `gpt5-verification-response.md`, 1 repetition, producer `execution-agent`, consumer `verify-chapter main session`, isolation `operand-path-ok`, target `path+capped-summary`. `grep -cE '^- Return the'` on the live agent returned 1, so the anchor resolves once. Pre-change row: derived `content`, state `VIOLATION`, 17710 bytes / 210 lines, plus a CAP finding at 210 lines / 17710 bytes against 20 lines / 4096. The response is already on disk — line 28 of the agent read "Write the response to the file path specified by the caller." Aggregate before: baseline 1819370 bytes, current relayed 310401, reduction 82.9391%, compliant 22/40, 8 measured violations, 2 cap violations, 5 ambiguous, 0 unresolved.
- Claim (3): HOLDS — searched the whole canonical workflow surface (`grep -rn "execution-agent" workflows/research-workflow/ --include="*.md"`, excluding the agent file and the harness directory): exactly one consumer, `verify-chapter.md` Step 2 item 4. Widened to `skills/` and `docs/`: the only other hit is `docs/agent-tier-table.md`, which lists the agent's model tier in three roster tables and is not a caller. `verify-chapter` is therefore the only caller, so no second contract had to be preserved and the unit did not need broadening.
- Claim (4): HOLDS — searched `reference/sops/fact-verification-prompt.md` § Output format: per discrepancy the response emits Claim (verbatim), Claim ID, Issue type (Unsupported / Contradicted / Overstated / Category-leakage / Undated) and Recommended correction, and emits `APPROVED` when there are none. `verify-chapter` Step 2 item 5 needs exactly output file path, discrepancy count, discrepancy summary and verdict. Every one of those is a field the response already states or a count of its own blocks, so a bounded quote-only handoff supplies the checkpoint without interpretation, and absence stays reportable as absence.
- Claim (5): HOLDS — searched the pre-change agent for each named behaviour: the complete response is written to the caller-specified file (line 28); call metadata is appended to `/logs/execution-log.md` (line 29); exactly one requested call is made ("Make additional API calls beyond what was requested" is prohibited); the supplied prompt and message are preserved ("Modify the system prompt or user message" is prohibited); confidentiality is enforced ("Send any information not explicitly provided in the user message"); failures are reported without retry (line 20). All six survive in the edited file unchanged in force — the disk write is now stated as "the complete response verbatim", which strengthens rather than relaxes it.
- Claim (6): HOLDS — the pre-change prohibition read "Interpret or summarize the response — return it verbatim", which directly contradicts a capped summary. It was rewritten narrowly in the same commit and still forbids altering, interpreting, analyzing or paraphrasing the response, still forbids handing the body back, and now names the file on disk as the verbatim authoritative copy the summary may never stand in for. `verify-chapter` items 4 and 5 were updated because the caller contract did change: the agent now returns a path plus a capped summary rather than the report body.

Result: Unit 9 is implemented and committed at `dd0a80aa9288bbf91f3a9e8d738e830c474a3751` (verified with `git rev-parse HEAD`), changing exactly three files — `workflows/research-workflow/.claude/agents/execution-agent.md`, `workflows/research-workflow/.claude/commands/verify-chapter.md` and `workflows/research-workflow/tests/s1-relay/README.md` (29 insertions, 10 deletions total). No manifest, harness executable or fixture changed. `execution-agent` writes the complete response verbatim to the caller-specified path and returns that path plus a handoff summary capped at 20 lines and 4 KB, carrying only the output path, response status, error text, the verdict as the response states it, and the discrepancy count with Claim ID and Issue type per discrepancy — grouped by Issue type if the per-discrepancy lines would breach the cap, with the per-discrepancy detail left in the file. Missing fields are reported absent, never reconstructed.

Evidence:

- Pre-change target: `H2-01` at `.claude/agents/execution-agent.md return-contract`, anchor `^- Return the`, derived `content`, `VIOLATION`, 17710 bytes / 210 lines current, projected 4126, plus the CAP finding quoted above. Aggregate as in claim (2).
- Post-change target: `H2-01` is `path` / `COMPLIANT` at 30 current bytes — 17680 bytes removed from the main-session relay — and its CAP finding is gone; the live run's only remaining CAP line is `H1-01`. Aggregate after: current relayed 292721 bytes, reduction 83.9109%, compliant 23/40, 7 measured violations, 1 cap violation, 5 ambiguous unchanged, 0 unresolved. A row-level `diff` of the full 40-row TSV before and after shows exactly one changed line, `H2-01`; every previously accepted seam is unmoved, and the co-tenant W4-H2 seams `H2-03` (`content`/`VIOLATION`), `H2-04` (`content`/`VIOLATION`/ambiguous) and `H2-05` (`content`/`VIOLATION`/ambiguous) all retain their prior classifications.
- Cap demonstrated: a worst-case handoff built to the new spec — 34 discrepancies across all five Issue types, grouped, with status, error, verdict-absent and the pointer line — measures 11 lines and 600 bytes against the 20-line / 4096-byte cap. The grouping rule is what bounds it: an ungrouped 34-line list would breach the line cap, which is why the spec makes grouping mandatory rather than optional and forbids shortening the verdict, count, status or error text instead.
- Falsifiability: a scratch copy at `$SCRATCH/u9-revert` with `execution-agent.md` restored from `HEAD` (return line back to `- Return the full response content`) was run through the same harness via `--workflow`. `H2-01` returns to `content` / `VIOLATION` at 17710 bytes and its CAP finding reappears, taking cap violations back to 2 and compliant seams back to 22/40, while the controls `H2-03`, `H3-19`, `H4-04` and `H1-01`'s own cap finding do not move — a row-level diff against the live run again shows exactly one moved row. No permanent machinery was added.
- Semantic contract evidence: the disk write is now "Write the complete response verbatim to the file path specified by the caller", so the full response remains byte-for-byte authoritative at the specified path and nothing about storage was weakened. The handoff supplies the four checkpoint fields `verify-chapter` item 5 requires, each quoted from the stored response rather than derived. API failure stays explicit — the failure-reporting and no-retry rule is untouched, the summary carries response status and error text, and the caller is told to record a reported failure or missing field instead of inferring it. `verify-chapter` can therefore write its checkpoint from the returned path and summary alone.
- Regression: `check-relay-payload.test.sh` reports 17 passed, 0 failed after the agent edit, after the cap clause and after the README edit. T7 confirms the fixture regenerates byte-identical at 475687 bytes; the caps stayed at their configured 20 lines / 4 KB and T6b confirms 0 uncovered rows. The README edit moved no harness row — a TSV diff across it is empty. Its `H2-01` entry is confined to that seam's landed state plus the "still pending `H1-01`" line the same item already owned.
- Repository: `git diff --check` clean; committed paths are the three files named above; `origin/main` remains an ancestor of `HEAD`; local commit `dd0a80aa9288bbf91f3a9e8d738e830c474a3751`. No push, no merge, no API call. This is an instruction-contract unit, so no real API call or representative chapter run was produced.

Aggregate S1 position after Unit 9: Units 1–9 implemented (1–8 accepted, 9 awaiting assessment); 23/40 seams compliant; fixed-fixture reduction 83.9109%; 7 measured violations, 1 cap violation (`H1-01`), 5 ambiguous seams and the representative chapter regression remain, so the aggregate 100%-coverage target stays red.

Deferrals noticed during this unit, not implemented: (a) the README `Blast radius` paragraph wording remains deferred, unchanged from Unit 8; (b) `verify-chapter` Step 3 item 7a still relays the verification report to `evidence-prose-fixer` as content after the Step 2 compact — it is not a manifest seam and the brief excludes other verification inputs and corrections, so it was left alone rather than converted.

## Blocker

None.

## Next action

Codex: assess Unit 9 — the `H2-01` path-plus-capped-summary return contract at `dd0a80aa9288bbf91f3a9e8d738e830c474a3751`, its cap demonstration and falsifiability, the caller update in `verify-chapter`, the resolved no-interpretation prohibition, the bounded README edit-map correction, and the updated aggregate S1 position. Then close, continue to the next S1 unit, or correct once.

---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 7 — inventory and path-pass the Step 4.2c architecture relay.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 6 is accepted at `a95116da30d05b652d3e633dcf678be58f22119f`: `H3-08` now passes the approved report architecture by path to each `evidence-to-report-writer` subagent, every co-tenant seam on that directive stayed in its prior state, the harness remained green at 17/17, and the fixed-fixture payload-reduction sub-target reached 80.40%. The leading-slash notation is accepted because it matches the workflow's canonical project-root notation and the exact file written, checkpointed, reviewed, and QC-gated in Steps 4.1/4.1b.

Unit 6 also established a material inventory gap: Step 4.2c repeats the same approved architecture as content to the per-chapter `report-compliance-qc` consumer, but the 39-row seam manifest has no W4-H3 row for that relay. This cannot remain a generic deferral because S1's governing proof requires deterministic coverage of the actual W4-H1–H4 seams. Unit 7 is one dominant deliverable: add exactly that missing relay to the existing manifest while it is still a measured content violation, then convert the same relay to path-passing. This is a bounded coverage repair, not authorization for a general seam re-audit or another harness system.

Required outcome: the accepted S1 manifest gains one uniquely identified W4-H3 row for the Step 4.2c report-architecture relay (the next non-colliding H3 identifier is expected, but verify the manifest convention). Before the production edit, that row must resolve against the fixed fixture and report a measured content violation with the correct four-chapter repetition. The canonical `run-report` Step 4.2c delegation then passes `/report/architecture/{section}/{section}-architecture.md` by path to the `report-compliance-qc` qc-gate subagent, which explicitly reads the complete approved architecture before applying the compliance criteria. Preserve all other compliance-QC inputs, conditional synthesis-memo behavior, verdict shape, downstream gates, and every previously accepted seam.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md` § 5 S1, governs: before editing, enumerate the exact W4-H1–H4 seams; deterministic acceptance requires complete named coverage plus the payload target and no unapproved full-content exception.
- The accepted harness at `7e2b97a6d195bc8ac035e670a4b11ab2107331f5`, advanced through Unit 6, remains the one proof surface. Add the missing relay there rather than creating another inventory or test system. The existing `H3-08` row is relevant precedent because it measures the same architecture payload, source session, four repetitions, and path-eligible operand class for the Step 4.2a consumer.
- The live Step 4.2c directive and `skills/report-compliance-qc/SKILL.md` are verify-first repository evidence. The skill declares the report architecture required for scope, headings, and synthesis-chapter behavior; treat complete-file reading as load-bearing.
- `workflows/research-workflow/.claude/agents/qc-gate.md` declares Read capability and already receives `reference/quality-standards.md` by path on the same Step 4.2c directive. This supports capability only; verify that architecture path-passing preserves the full consumer contract.
- The plan file's `Status: Proposed` header predates the operator's content-bound approval and remains an excluded approval-metadata synchronization issue; the exact approved content binding above governs this task.

Check against the repository before acting:

1. Validate this task and re-establish current repository state. Perform the repository-required origin fetch, verify current `origin/main` remains an ancestor of `HEAD`, verify Unit 6's exact commit, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on a conflict or material S1 premise change.
2. Reconfirm the omission narrowly: search the complete current manifest for a row whose command is `.claude/commands/run-report.md`, location is Step 4.2c, consumer is `report-compliance-qc`, and payload is the report architecture. Confirm none exists, while the live Step 4.2c directive content-passes that architecture four times. Do not widen this into a general inventory scan.
3. Add one manifest row following the existing schema and identifier convention. Before changing production, run the accepted target check and prove the new row resolves with zero uncovered rows and reports a measured content violation using the architecture fixture with four repetitions. Record how the added seam changes the baseline/current byte totals and the percentage before conversion; do not hide denominator movement.
4. Verify the architecture file is the approved/QC-passed artifact from Steps 4.1/4.1b and that `report-compliance-qc` requires its complete scope, headings, and synthesis flags. Verify qc-gate can read the project-root path and that doing so exposes no input beyond the same file previously embedded.
5. Convert only that Step 4.2c architecture operand to an explicit path plus complete-read responsibility. Verify the chapter draft, review findings, style reference, scarcity register, section directive, quality-standards path, and conditional cluster-memo content retain their existing transport and semantics.
6. Verify no consumer skill/agent, project copy, deploy/sync surface, model policy, return contract, Step 4.2a/b behavior, or other seam must change.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-report.md`; `workflows/research-workflow/tests/s1-relay/seam-manifest.tsv`; the harness README or existing test only where the new row makes a factual inventory/count or bounded liveness update necessary; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: any manifest addition other than the one verified Step 4.2c architecture relay; every production seam except that relay; `H3-19`; `H3-09`; `H1-03` chapter-draft transport; Step 4.2a/b; changes to `report-compliance-qc` or qc-gate; return transport; project or archived copies; deploy/sync propagation; approval metadata; representative chapter execution; a general seam re-audit; ambiguous isolation cases; W4-H1/H2 returns; S0 and S2–S11; push, merge into `main`, deployment, credentials, spend, and external dispatch.
- Codex framing decision: inventorying and converting this one missed seam is one unit because the manifest row is the required falsifiable proof for the exact production relay being changed. The omission is newly verified evidence that materially affects S1's deterministic denominator; leaving it deferred would make a future 100% claim unreliable.

Required evidence, all capable of failing:

- Narrow pre-change omission evidence, then the added row's pre-production result: exactly one new W4-H3 row; it resolves, reports CONTENT/VIOLATION with four repetitions, and increases the named denominator without changing any existing row's classification. Quote the recalculated baseline/current byte totals and pre-conversion reduction.
- Post-change target output: the new row flips VIOLATION → COMPLIANT; every seam accepted through Unit 6 remains COMPLIANT; co-tenant `H1-03` and `H4-09` retain their classifications; zero rows are unresolved. Quote removed bytes and the final aggregate reduction without rounding a miss into a pass.
- The harness regression suite remains green with the new denominator and without weakening fixture stability, mutation/liveness controls, or caps. Prove the new row can fail by restoring architecture content on a scratchpad/reverted copy. Add no permanent machinery unless the existing harness contract requires it.
- Direct command evidence that compliance QC reads the complete approved architecture by path while every other input, conditional synthesis behavior, QC verdict, and downstream gate is unchanged.
- A scoped diff showing only the one manifest coverage repair, any factually necessary existing-harness documentation/control adjustment, and the Step 4.2c architecture transport change. No representative AI-authored run is required for this instruction-only unit.
- Repository evidence: relevant local checks stay green; `git diff --check` passes; committed paths stay inside allowed surfaces; current `origin/main` is an ancestor of resulting `HEAD`; and the exact local commit hash is recorded and verified.

Completion condition: the formerly missing Step 4.2c architecture relay is represented once in the accepted manifest, is demonstrated as a pre-change violation, is committed as path-passing with complete consumer-side reading, leaves all existing rows and compliance-QC semantics intact, keeps the harness falsifiable with zero unresolved rows, and reports the corrected denominator and reduction truthfully.

Stop and hand back if: the manifest already contains an equivalent row; the relay is not a four-repetition measured content violation; complete architecture content is semantically required inline; qc-gate cannot read the path; adding the row exposes a broader inventory inconsistency; the conversion requires changing an excluded consumer, input, return, or policy; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or the unit would require propagation, external dispatch, or another operator-reserved capability.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation if needed, edits within the scoped canonical command and S1 harness surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — ran `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` succeeded; `git log --oneline -1 a95116da30d05b652d3e633dcf678be58f22119f` resolved to the Unit 6 architecture conversion. `git status --porcelain` showed only ` M logs/friction-log.md` (hook-maintained, untouched) and ` M logs/work-loop/canonical-rw-near-term-improvements.md` (Codex's uncommitted Unit 7 brief). No S1 premise changed.
- Claim (2): HOLDS, narrowly — filtered the complete manifest on `surface == .claude/commands/run-report.md && step == 4.2c` and got exactly two rows, `H1-03` (payload `the chapter draft`) and `H4-09` (payload `quality-standards.md`); separately searched the whole file for `report-compliance-qc` and found those same two lines and no third. So no row carried the report-architecture payload for that consumer, while the live Step 4.2c directive passed `the architecture (\`/report/architecture/{section}/{section}-architecture.md\`)` inside its `Pass it:` list, four times per section. The scan was confined to this one relay; no general inventory sweep was run.
- Claim (3): HOLDS — the highest existing `H3-` identifier was `H3-23`, so `H3-24` is the next non-colliding id. The added row follows the documented 15-column schema exactly (verified: `NF == 15`) and reuses the `H3-08` precedent for the same payload — anchor `report-compliance-qc/SKILL` (the same anchor `H1-03` and `H4-09` already use for this directive), token `architecture`, fixture glob `architecture.md`, `reps 4`, `on_disk yes`, `operand-path-ok`, target `path`, consumer `report-compliance-qc qc-gate subagent` matching the wording those two co-tenant rows use. **Pre-production measurement of the new row: `H3-24  content  VIOLATION  base=33576 bytes / 480 lines`** across four repetitions — the same per-repetition payload as `H3-08`, as expected for the same file. Zero rows unresolved, and no existing row changed classification (`H3-08` still COMPLIANT, `H1-03` still VIOLATION, `H4-09` still COMPLIANT, compliant count still 20, exempt still 10, ambiguous still 5, cap still 2).
- Claim (4): HOLDS — `skills/report-compliance-qc/SKILL.md` input 3 already names the artifact by path (`**Report architecture** — from \`/report/architecture/{section}/{section}-architecture.md\``), and its criteria need the complete file: Scope match against `architecture.md` (BLOCKING), Heading structure against the specified hierarchy (NON-BLOCKING), and the Category 2b synthesis-chapter flag. Searched all six `architecture` mentions in that SKILL.md for an inline-provision or verbatim-prompt requirement and found none, so a complete read at the named path satisfies it. The file is the Step 4.1 output, operator-reviewed at 4.1.6 and QC-gated at 4.1b.6, unchanged by this unit. Consumer capability: `workflows/research-workflow/.claude/agents/qc-gate.md` frontmatter declares `tools: Read`, and the same directive already hands that agent `reference/quality-standards.md` by path (`H4-09`, compliant since the Unit 1 baseline). Path-passing exposes exactly the file previously embedded — nothing more.
- Claim (5): HOLDS — the command diff is one line, and a word-level diff confirms the only changed span is the architecture operand. Character-identical within that line: the chapter draft, review findings from step (b), the style reference, the scarcity register, the chapter's section directive, the `quality-standards.md` PATH clause, the whole conditional Category 2b synthesis cluster-memo content clause including its "Without memos, Category 2b is skipped and flagged as unchecked" fallback, the no-git standing constraint, and `Return: QC verdict (PASS/FAIL), per-item findings`. Measured on the same line: `H1-03` stayed `content VIOLATION` and `H4-09` stayed `path COMPLIANT`. Steps 4.2a, 4.2b, 4.2d, 4.2e and every downstream gate are untouched.
- Claim (6): HOLDS — two files changed, `run-report.md` (one line) and `seam-manifest.tsv` (one added line). `unresolved (uncovered) : 0` proves every anchor and glob still resolves, and the harness suite needed no edit, so no permanent control or count moved. No consumer skill, agent, project copy, deploy/sync surface, model line, output policy, return contract or Step 4.2a/b directive appears in the diff.

Result: Unit 7 is implemented and committed at `79eaf6e459be47e245552ef99848c0194f945b2b` (read with `git rev-parse HEAD`). The manifest gains exactly one row, `H3-24`, covering the `run-report` Step 4.2c report-architecture relay to the `report-compliance-qc` qc-gate consumer; the directive then passes that architecture by path with an explicit instruction that the sub-agent reads the complete approved architecture file itself before applying the compliance criteria.

One disclosed placement choice: `H3-24` is inserted after `H3-09`, keeping the file's visible class → surface → step grouping so all `run-report` W4-H3 rows stay contiguous, at the cost of numeric id order within the block. The schema documents `seam_id` as a *stable* id, not a sorted one, `H2` already carries a gap, and no test asserts ordering.

Evidence:

- **The correction changes the denominator, and it un-does Unit 6's headline number. Reported plainly rather than buried:** adding `H3-24` moved the named denominator 39 → 40 rows and the fixed baseline 1785794 → 1819370 bytes (+33576). On that corrected inventory the pre-conversion reduction is **78.92%**, not the 80.40% Unit 6 reported. Unit 6's figure was arithmetically right and measured against an incomplete inventory; the 80% sub-target had not in fact been reached at Unit 6. The 39-row-equivalent figure is unchanged at 80.40%, which is how both statements are true at once.
- Post-change target output: `H3-24` flips VIOLATION → COMPLIANT (`shape` `content` → `path`). Removed by this conversion: **33512 fixed-fixture bytes** (33576 → 64 across four repetitions), current relayed bytes 383521 → 350009. Final aggregate reduction **80.76%** on the corrected 40-row denominator — a genuine clearance of the ≥80% bar, not a rounded miss. Compliant seams 20/40 → **21/40**; measured violations 10 → 9; cap violations 2; ambiguous 5; unresolved 0. Every seam accepted through Unit 6 is still COMPLIANT, and the exempt set is unchanged at ten.
- **The overall verdict is still `TARGET NOT MET` and the script still exits 1.** The reduction sub-target is green; the verdict additionally requires zero measured violations, zero cap violations and zero ambiguous rows, which stand at 9, 2 and 5. S1's deterministic seam target and its representative chapter regression remain unmet.
- Harness regression suite: **17 passed, 0 failed**, with the new 40-row denominator and no test edited. Fixture stability holds (T7, byte-identical at 475687); the caps are untouched with T5/T5b passing; T6b confirms every row of the enlarged manifest resolves; all four T9 liveness controls and T9b pass.
- Falsifiability of the new row — proving coverage was added honestly rather than as a row that cannot fail: on a scratchpad copy of `.claude/`, reverting the Step 4.2c clause to the previous `the architecture (\`/report/architecture/…\`),` returned `H3-24 content VIOLATION`, while `H3-08`, `H1-03` and `H4-09` held their states. No permanent machinery was added, per the brief.
- Scoped diff: `run-report.md` 1 insertion / 1 deletion, `seam-manifest.tsv` 1 insertion. `git diff --check` on `workflows/` is clean. Committed paths stay inside the allowed surfaces; `origin/main` remains an ancestor of `HEAD`; `logs/friction-log.md` left modified-but-uncommitted and unedited.

Prior units for the record: Unit 2 `17cc5726704ef31e690a89e27a06168f4b3ecae9` plus README correction `160aa983500c984bce25e4d29111d7800e52f7c1`; Unit 3 `df96a00f39e07d4f15589d3e79dfb12199d323c3`; Unit 4 `253cba7067ff29d5322299707a81cee90080de14`; Unit 5 `a77136000ed4e929c0afd0eafa4d89f3bc8daa58`; Unit 6 `a95116da30d05b652d3e633dcf678be58f22119f`.

Carried deferrals, plus one newly noticed in Unit 7 and not implemented:

- Stale live-copy count after an archived Content Programme deployment, and bytewise Sector Intelligence deployment divergence — S0/propagation territory.
- `reference/stage-instructions.md` line 139 could name the subagent as the chapter-draft reader more precisely, but remains semantically true.
- `run-execution` Step 2.3.1's unscoped raw-reports directory versus Step 2.2b's section-scoped write path — a separate path-correctness issue.
- Step 2.4's implicit raw-report re-read — a separate verification-transport issue.
- **New, and flagged rather than acted on:** the harness README's § Reconciliation table still maps `run-report` St4.2a/4.2c to the pre-Unit-7 row set and does not list `H3-24`, and its edit map still describes `H3-08` as pending. Updating it was outside this unit's "only where the new row makes a factual inventory/count update necessary" allowance, since the check itself carries the live figures and the README explicitly tells readers to run it rather than quote from the page. Worth one bounded documentation pass.

## Blocker

None.

## Next action

Codex: assess Unit 7 at `79eaf6e459be47e245552ef99848c0194f945b2b` — whether `H3-24` is the correct single coverage repair with the right schema, identifier and placement, whether the pre-production VIOLATION measurement plus the reverted-copy check make it genuinely falsifiable, whether complete-read-by-path preserves `report-compliance-qc` scope, heading and synthesis-flag semantics, and how to treat the corrected denominator — specifically whether the now-true 80.76% payload clearance should be recorded as S1's reduction target being met, given the deterministic verdict still fails on 9 violations, 2 cap violations and 5 ambiguous rows, and whether the stale harness README earns its own bounded unit.

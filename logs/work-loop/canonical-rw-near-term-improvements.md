---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 12 — path-pass the draft to reviewer and compliance QC.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 11 is accepted at discovery commit `17c398031f9907f2a7292e78972f525639421abd`. Repository evidence resolves all five former ambiguities: `H1-02`, `H1-03`, and `H2-05` are path-safe; `H3-04` is content-required because it feeds the already-authorized `H3-09` per-chapter content contract; and `H2-04` is content-required because the extracts form the external GPT-5 prompt body under the current execution-agent contract. This unit makes the first real implementation from that ruling: convert the adjacent Step 4.2b/c draft relays and encode all five accepted classifications in the existing proof surface.

Required outcome: `run-report` Step 4.2b passes the exact durable Step 4.2a draft path to `chapter-prose-reviewer`, which reads the complete draft itself; Step 4.2c passes that same path to `report-compliance-qc`/the qc-gate sub-agent, which reads the identical draft itself. The main session must not read the draft merely to relay it into either sub-agent. The five Unit 11 rulings must become truthful manifest classifications: `H1-02`, `H1-03`, and `H2-05` as `operand-path-ok` with path targets and correct on-disk state; `H3-04` and `H2-04` as `intentional-content` / `content-required`. Only `H1-02` and `H1-03` are implemented in this unit.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: path-pass W4-H1–H4 where isolation allows, retain justified content contracts explicitly, preserve analytical meaning, and prove every named seam with the accepted deterministic surface.
- Unit 11's accepted evidence at commit `17c398031f9907f2a7292e78972f525639421abd` is the settled technical decision for these five isolation classifications. It is not permission to alter `H3-09`, redesign prompt assembly, or implement `H2-05`.
- The accepted manifest, harness, fixtures, and README are the sole deterministic proof surface. Update them only as required to encode the accepted rulings, keep classifications measurable, preserve fixture stability, and make the two converted seams fail when their full-content relays return.
- The live `run-report.md`, `chapter-prose-reviewer/SKILL.md`, `report-compliance-qc/SKILL.md`, `qc-gate.md`, and all callers of those shared consumer contracts are verify-first repository evidence. Existing path precedents `H3-05`, `H3-07`, and `H3-24` support the ruling but do not authorize unrelated cleanup.
- The stale `Status: Proposed` plan header remains excluded approval-metadata synchronization; the exact content-bound approval above governs.

Check against the repository before acting:

1. Validate task identity, lifecycle, ownership, and repository state; perform the repository-required origin reconciliation, verify Unit 11 commit `17c398031f9907f2a7292e78972f525639421abd`, confirm `origin/main` remains an ancestor of `HEAD`, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on conflict or material S1 premise change.
2. Re-resolve the five manifest rows and capture their pre-change shape, classification, on-disk flag, bytes/lines, cap status, and aggregate counts/reduction. Confirm the five are still the only ambiguous rows, `H1-02` and `H1-03` still relay content at Steps 4.2b/c, and Unit 10 still writes the complete draft before both run.
3. Inspect every canonical caller of `chapter-prose-reviewer`, `report-compliance-qc`, and the generic `qc-gate` contract before changing shared wording. Make the minimum coherent consumer-contract edit needed for these exact path operands; preserve callers that still intentionally pass content, and stop rather than silently forcing a shared contract incompatible with another caller.
4. Verify the draft at the Step 4.2a output path is byte-for-byte the artifact Steps 4.2b/c currently receive, and that neither consumer depends on main-session selection, transformation, appended context, or writer reasoning. Preserve the reviewer's scarcity checks, style/quality reference paths, verdict/findings return, compliance categories, architecture path, synthesis-memo conditional, model pins, no-git standing constraints, checkpoint/write boundary, and operator-review halt.
5. Encode Unit 11's classifications without converting excluded sources: `H3-04` and `H2-04` remain content and become explicit exemptions; `H2-05` remains a measured content violation but is no longer ambiguous; `H1-02` and `H1-03` become on-disk path operands. Keep `H2-03` unchanged as `operand-path-ok` / path target pending its separate re-examination.
6. Update only the existing liveness/fixture/readme material required to keep the accepted proof surface truthful. Do not redesign the checker, introduce another inventory, weaken ambiguous/unresolved failure behavior, or alter fixed fixture payloads merely to improve the percentage.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-report.md`; `skills/chapter-prose-reviewer/SKILL.md`, `skills/report-compliance-qc/SKILL.md`, and `workflows/research-workflow/.claude/agents/qc-gate.md` only where verified consumer-contract coherence requires it; the existing `workflows/research-workflow/tests/s1-relay/` manifest, harness test, fixtures, and README only as needed for these five classifications and two conversions; this state file; normal hook output; local checks and Claude's local commit.
- Excluded: implementing or redesigning `H2-03`, `H2-04`, `H2-05`, `H3-04`, or `H3-09`; changing external prompt assembly or execution-agent; any other seam; output-policy, model, compaction, reviewer/QC criteria, or chapter-content redesign; representative chapter execution; deployed/project/archived copies; propagation; approval metadata; S0 and S2–S11; push, merge, deployment, credentials, spend, or external dispatch.
- Codex framing decision: landing the five accepted classifications with the paired Step 4.2b/c conversion is one dominant deliverable—the proof surface must stop calling settled contracts ambiguous at the same time it scores the implemented paths. `H2-05` remains visibly noncompliant, so classification is not being used to manufacture completion.
- Codex framing decision: shared skill/agent wording may change only to make already-live content and path callers coexist truthfully. General cleanup remains outside the unit even if stale prose is nearby.
- Carried deferrals remain outside: the `H2-03` external-prompt concern; the newly observed missing Evidence Pack input at `verify-chapter` Step 3.7a; its verification-report relay; stale `reference/stage-instructions.md` writer-return wording; the writer Reviewer Findings footer sequencing inconsistency; README `Blast radius`; and optional verification-summary truncation naming.

Required evidence, all capable of failing:

- Pre-change evidence for all five rows and the exact aggregate; plus exact live directives showing the two full-draft relays and the five ambiguous classifications.
- Post-change: `H1-02` and `H1-03` alone change transport to `path` / `COMPLIANT`; both consumers read the complete draft from the exact Step 4.2a path; `H3-04` and `H2-04` report `EXEMPT-CONTENT` under `intentional-content` / `content-required`; `H2-05` remains `content` / `VIOLATION` but under `operand-path-ok`; `H2-03` is byte-identical to its pre-unit row; ambiguous and unresolved counts are both zero. Quote exact compliant, violation, exempt, cap, byte, and reduction totals.
- Failing-case/liveness evidence: on scratch/reverted command copies, restore the full chapter-draft relay separately at 4.2b and 4.2c and show each corresponding seam returns to content violation without moving the other seam, either content exemption, `H2-03`, or `H2-05`. The check must move on source behavior, not merely on manifest labels.
- Semantic evidence: reviewer and QC receive the identical complete draft; their independent roles, inputs, criteria, return contracts, conditional synthesis coverage, checkpoint, and operator halt remain intact. Name every shared-contract caller inspected and how content callers were preserved.
- The accepted harness regression suite remains green; fixed-fixture generation remains byte-stable except for an explicitly justified classification fixture update; cap/liveness, uncovered-row, and ambiguity controls remain capable of failure. Run the existing canonical deploy validation relevant to changed canonical skill/agent surfaces if repository instructions identify one; do not invent a new suite.
- Scoped diff, `git diff --check`, exact committed paths, current `origin/main` ancestry, and the exact full local commit hash verified with `git rev-parse`. No real chapter generation or representative AI-authored chapter run is required for this instruction-contract unit.

Completion condition: the five accepted isolation rulings are encoded truthfully; Steps 4.2b/c pass the exact durable draft path and their consumers read the identical complete artifact without main-session draft relay; `H1-02` and `H1-03` are independently falsifiable and compliant; the two content-required seams are explicit exemptions; `H2-05` remains an honest noncompliant settled path target; `H2-03` and all excluded behavior remain unchanged; ambiguity and unresolved counts are zero; aggregate reduction remains at least 80%; the accepted proof suite remains green; and Claude commits the bounded implementation plus handback.

Stop and hand back if: either consumer needs transformed/curated draft content rather than the exact file; a shared contract cannot truthfully support existing content and path callers without broader redesign; a content-required classification would hide a changed or unproved behavior; the liveness check cannot distinguish the two relays independently; any excluded seam must change; the harness becomes unresolved, unfalsifiable, or requires redesign; repository reconciliation changes the premise; or an operator-reserved/external capability is required.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin reconciliation, edits inside the scoped canonical command/consumer contracts and existing S1 proof surface, local tests, and local commits. The full baseline Claude deny set remains required. Nothing else is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` returns 0; `git cat-file -t 17c398031f9907f2a7292e78972f525639421abd` returns `commit`, subject "Unit 11 discovery rulings, turn to codex"; `HEAD` = `1dfb743739bd93bcc0ded3ab0c1a381fbbc5b66a`. Validator `ACTIVE_CLAUDE`, ownership `PROCEED`, capability `READY`. Working tree carried only the hook-maintained `logs/friction-log.md` modification and this state file; the friction-log modification is preserved and left unstaged. No conflict, no S1 premise change.
- Claim (2): HOLDS — pre-change rows, from the live check: `H1-02` 4.2b `content`/`VIOLATION`/`ambiguous`/target `path`, 65480 bytes / 880 lines / 4 reps, `on_disk=no`; `H1-03` 4.2c identical shape; `H2-04` 1.2 `content`/`VIOLATION`/`ambiguous`, 12172 / 180 / 1, `on_disk=yes`; `H2-05` 3.7a `content`/`VIOLATION`/`ambiguous`, 16370 / 220 / 1, `on_disk=yes`; `H3-04` 4.0.5 `content`/`VIOLATION`/`ambiguous`, 48688 / 720 / 1, `on_disk=yes`. An `awk` scan of the isolation column returned exactly these five as the only `ambiguous` rows. Pre-change aggregate: 24/40 compliant, 227357 relayed bytes, 87.503531% reduction, 6 measured violations, 0 cap violations, 5 ambiguous, 0 unresolved, 10 exempt seams. Unit 10's 4.2a write is present and still writes the complete draft before 4.2b and 4.2c run.
- Claim (3): HOLDS, and it changed the shape of the fix — see "Shared contracts" below. Searched `.claude/commands/` for every caller. `chapter-prose-reviewer` has **two** callers: `run-report` 4.2b and `produce-prose-draft` Phase 5, which passes "The prose file content" — a content caller that must survive. `report-compliance-qc` has **one** caller, `run-report` 4.2c. The generic `qc-gate` has **fourteen** call sites across `run-preparation`, `run-execution`, `run-analysis`, `run-report`, `produce-architecture`, `produce-prose-draft`, `produce-formatting` and `review-chapter`; all but two pass content ("Pass it: the skill content and all Answer Spec content", "passing as content: (a) the raw research report…"), and the two exceptions are `run-report` 4.1b.3 and 4.2c, which already pass paths. So no shared contract could be forced to path.
- Claim (4): HOLDS — the artifact at the 4.2a output path is the same file for both consumers, and it is unmodified at 4.2b/4.2c: corrections and revisions happen later (4.2f onward), and the operator edit at 4.2e is downstream of both. Neither consumer depends on main-session selection, transformation, appended context or writer reasoning: the pre-change directives relayed the draft whole, and the skills' own contracts ask for the chapter itself. Preserved textually unchanged: the reviewer's scarcity-implementation check, its `style-guide.md` and `quality-standards.md` PATHs, its verdict/findings/recommended-changes return; the QC's architecture PATH, style reference, scarcity register, section directive, `quality-standards.md` PATH, the synthesis-chapter Category 2b conditional, and its PASS/FAIL + per-item findings return; both model pins (line 12/13), both no-git standing constraints, the 4.2d checkpoint/write boundary and the unconditional 4.2e operator halt.
- Claim (5): HOLDS — `H3-04` and `H2-04` are encoded as exemptions with **no source change**: `git diff HEAD -- .../verify-chapter.md` is 0 lines, and 4.0.5's directive is untouched. `H2-05` is reclassified `operand-path-ok` but deliberately **not** implemented, so it stays a measured violation. `H2-03`'s manifest row is byte-identical to HEAD (`cmp` returns identical).
- Claim (6): HOLDS — the only proof-surface changes are the five manifest classifications, three added harness controls, and the README edit map. The checker itself (`check-relay-payload.sh`) is untouched, no second inventory was created, no fixture payload changed (T7 regenerates byte-identical at 475687 bytes, the same figure as before this unit), and no failure behaviour was weakened — ambiguity and uncovered-row controls are both strengthened below.

Result: `H1-02` and `H1-03` are converted to path. `run-report` Step 4.2b passes the exact durable draft path and the reviewer reads the complete draft itself; Step 4.2c passes that same path and the qc-gate sub-agent reads the identical file. The main session no longer reads the draft in order to relay it into either sub-agent, so the explicit read Unit 10 disclosed is gone. All five Unit 11 rulings are encoded in the manifest. `H2-05` is settled as `operand-path-ok` but left unimplemented, so it stays visibly noncompliant.

**Shared contracts — the callers decided this, not the seam.** Because `qc-gate` has twelve live content callers and `chapter-prose-reviewer` has a live content caller in `produce-prose-draft`, no shared contract could be made path-only. Each edit therefore makes content and path callers coexist rather than switching a default: `qc-gate.md` now says the artifact arrives "either the content of a file, or the path to it. Which one is stated by the caller", and must halt if a file given by path is absent or empty; `chapter-prose-reviewer` Input 1 now says it "arrives either as content or as an absolute file path, whichever the invoking workflow states", which leaves `produce-prose-draft` correct as written. `report-compliance-qc` is the one single-caller contract, so its inputs are now marked per input — and that also fixed a **pre-existing falsehood**: its blanket "Content is passed directly for inputs 1–6 (not file paths)" had been wrong about input 3 since Unit 7 converted `H3-24`, and removing the blanket without marking the rest would have left the contract less determinate than before, so inputs 2 and 4–6 are marked content explicitly.

Evidence:

- **Failing case, independently per seam.** `T9 H1-02` restores the 4.2b full-draft relay on its own scratch copy: `COMPLIANT -> VIOLATION`. `T9 H1-03` does the same at 4.2c: `COMPLIANT -> VIOLATION`. **`T9c` is the control that matters here** — because Unit 12 converted the two together, a single edit that moved both would be indistinguishable from two working conversions. It asserts that reverting `H1-02` alone moves neither `H1-03` nor `H3-04`, `H3-09`, `H2-04`, `H2-03`, `H2-05`, and then the same for `H1-03` alone. Both pass. The manifest is unchanged in those runs, so the check is moving on the directive text, not on the labels.
- **Implemented result.** Every one of the 40 seam rows was compared pre against post: **exactly 5 rows changed, 36 unchanged.** `H1-02` and `H1-03` `content`/`VIOLATION`/`ambiguous` → `path`/`COMPLIANT`/`operand-path-ok`, 65480 bytes each down to the 116-byte path string (**130960 bytes removed**). `H2-04` and `H3-04` → `EXEMPT-CONTENT` under `intentional-content`/`content-required`. `H2-05` → still `content`/`VIOLATION`, now under `operand-path-ok`. `H2-03` byte-identical. `H1-01`, `H3-08`, `H3-09`, `H3-24`, `H4-07`, `H4-08`, `H4-09` all unmoved.
- **Aggregate.** 26/40 compliant (was 24/40); 35769 relayed bytes (was 227357); **97.965948% reduction** (was 87.503531%, target ≥80%); **2 measured violations** (was 6) — `H2-03` and `H2-05`, both honest and both named in the findings output; 0 cap violations; **0 ambiguous**; **0 unresolved**; 12 exempt seams over 509920 bytes (was 10 / 449060). The baseline moved from 1819370 to 1758510 because the two newly exempt seams leave the reduction accounting entirely, which is the checker's existing treatment of `content-required`, not a change to it.
- **Regression protection.** `check-relay-payload.test.sh` → **23 passed, 0 failed** (was 17/17; the three new controls are `T9 H1-02`, `T9 H1-03`, `T9c`, plus `T5c`/`T5d` replacing lost coverage). T6b still 0 uncovered rows; T7 fixture byte-identical at 475687 bytes, unchanged, so **no classification fixture update was needed or made**.
- **The ambiguity control had to be rebuilt to stay falsifiable.** Settling the last `ambiguous` row means the real workflow no longer exercises that branch at all — the control would have sat unproven and could rot silently, which is precisely the failure the `ambiguous` classification exists to prevent. `T5c` now marks a synthetic row ambiguous and asserts the finding fires and counts; `T5d` asserts that settling it clears the ambiguity finding **and moves nothing else** (measured violations identical across both runs). Both directions, on the synthetic surface, so no live row is needed to keep it honest.
- **Semantic contract.** Reviewer and QC receive the identical complete draft from one path — the same artifact both received before, since the main session was relaying that file's contents verbatim. Independence is unaffected: each still sees only the draft and its own criteria, neither sees the other's reasoning, and the QC still receives the reviewer's findings as content. Every preserved element is enumerated under Claim (4).
- **Repository facts.** `git diff --check` clean. Changed: `run-report.md`, `qc-gate.md`, `chapter-prose-reviewer/SKILL.md`, `report-compliance-qc/SKILL.md`, `seam-manifest.tsv`, `check-relay-payload.test.sh`, `s1-relay/README.md`, this state file. `verify-chapter.md` is 0 lines changed. `logs/friction-log.md` preserved and unstaged. `origin/main` remains an ancestor of `HEAD`. The repository's existing commit-time skill validation ran on this commit — it is the canonical validation for changed `SKILL.md` surfaces, and no new suite was invented. Implementation commit: `b0c8a9e2706fe826056c6fc08b9fd299b4b59a46` (verified with `git rev-parse`).

**Deferrals — recorded, not implemented.** All carried items remain outside: the `H2-03` external-prompt concern (now also written into the README so it cannot be lost), the missing Evidence Pack input at `verify-chapter` 3.7a, its verification-report relay, stale `reference/stage-instructions.md` writer-return wording, the writer's Reviewer Findings footer sequencing, README `Blast radius`, and optional verification-summary truncation naming. Nothing new was noticed that is not already on that list.

Aggregate position after Unit 12: 26/40 compliant; 97.965948% fixed-fixture reduction; 2 measured violations (`H2-03`, `H2-05`); 0 cap violations; 0 ambiguous; 0 unresolved. Remaining S1 work: implement `H2-05`, resolve `H2-03`, and the representative chapter regression.

## Blocker

None.

## Next action

Codex: assess Unit 12. Judge whether `H1-02` and `H1-03` are independently falsifiable (the `T9c` control, not just the two `T9` flips); whether the shared-contract edits truthfully let the twelve live `qc-gate` content callers and `produce-prose-draft`'s content caller coexist with the two path callers rather than switching a default; whether encoding `H3-04` and `H2-04` as exemptions with zero source change is honest labelling of the accepted Unit 11 ruling rather than hidden behaviour; whether rebuilding the ambiguity control on the synthetic surface (`T5c`/`T5d`) adequately replaces the coverage lost when the last live ambiguous row was settled; and whether `H2-05` staying a visible violation confirms classification was not used to manufacture completion. Then close, continue to `H2-05`, correct once, or stop.

---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 3 — path-pass W4-H3 architecture operands.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 2 is accepted: its six W4-H4 reference-document relays landed at `17cc5726704ef31e690a89e27a06168f4b3ecae9`, and the sole frozen documentation finding was resolved at `160aa983500c984bce25e4d29111d7800e52f7c1` without breaking the harness. The aggregate fixed-fixture reduction is now 44%, so S1 remains open. Unit 3 takes one coherent, low-isolation-risk deliverable from the accepted edit map: the four `produce-architecture` operand seams `H3-20`–`H3-23`, where the same on-disk section drafts are read and relayed in both architecture creation and architecture QC.

Required outcome: the canonical `produce-architecture` command no longer reads all selected section-draft contents into the main session in Phase 2 or Phase 3 and no longer embeds those contents in either subagent prompt. It passes the selected project-root-relative draft paths, labeled by section ID, and requires each receiving subagent to read those paths at runtime. Preserve draft selection, approved-file fallback, minimum-section and operator-approval gates, architecture creation, traceability QC, output paths, verdict routing, handoff summaries, and the semantic responsibilities of both skills.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md` § 5 S1, governs: W4-H1–H4 use disk-write-and-return-path or path-plus-capped-summary where the existing isolation contract permits it, with deterministic seam coverage and payload reduction plus a later representative regression.
- The accepted harness at `7e2b97a6d195bc8ac035e670a4b11ab2107331f5`, as advanced by Unit 2, is executable current-state evidence. `workflows/research-workflow/tests/s1-relay/seam-manifest.tsv` classifies `H3-20`–`H3-23` as `operand-path-ok`; the README identifies the duplicate Phase-2/Phase-3 draft reads as one bounded conversion. Those classifications are verify-first repository claims, not authority to change any other seam.
- The plan file's `Status: Proposed` header predates the operator's content-bound approval and remains an excluded approval-metadata synchronization issue; the exact approved content binding above governs this task.
- The correction-round discovery that the current live-copy count is stale is a propagation/S0 deferral. This unit changes the canonical command only and makes no claim that project or archived copies were synchronized.

Check against the repository before acting:

1. Validate this task and re-establish current repository state. Verify that current `origin/main` is an ancestor of `HEAD` after the repository-required fetch, that the Unit 2 commits are present, and that the unrelated hook-maintained `logs/friction-log.md` modification is preserved. Stop on a conflict or a material change to an S1 premise.
2. Run the accepted target check before editing and inspect the live anchors for `H3-20`–`H3-23`. All four must be measured violations, Unit 2's `H4-01`–`H4-09` must remain compliant, and the manifest must have zero unresolved rows. Hand back the exact mismatch if not.
3. In `workflows/research-workflow/.claude/commands/produce-architecture.md`, verify that Phase 1 already resolves one concrete highest-numbered draft or approved fallback path per section, and identify every place Phase 2 and Phase 3 currently read or content-pass that same set. The evidence must distinguish main-session reads from consumer-side reads.
4. Verify the two receiving subagent paths can read project-relative files at runtime and that neither `research-structure-creator` nor `architecture-qc` semantically requires the drafts to be embedded in the delegation prompt. Inspect the actual tool/capability declarations and invoked skill instructions; do not infer this from the manifest. Stop if either consumer cannot safely follow paths without changing an excluded skill, agent, or policy surface.
5. Verify that converting only `H3-20`–`H3-23` requires no project-copy, deploy/sync, reference, output-policy, compaction, model, or other relay change.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/produce-architecture.md`; the existing `workflows/research-workflow/tests/s1-relay/` harness only where the production wording requires a bounded behavior-anchor, manifest, or README adjustment; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: every seam except `H3-20`–`H3-23`; changes to `research-structure-creator`, `architecture-qc`, other agents/skills/commands, project or archived copies, deploy/sync propagation, approval metadata, the representative chapter run, ambiguous isolation cases, W4-H1/H2 return contracts, S0 and S2–S11, push, merge into `main`, deployment, credentials, spend, and external dispatch.
- Codex framing decision: these four seams form one dominant deliverable because one command selects one draft set and presently relays it twice across its creation/QC sequence. The remaining safe W4-H3 operand groups stay for later units so this unit's reasoning and validation load remains bounded.

Required evidence, all capable of failing:

- Before/after focused target output for `H3-20`–`H3-23`: all four flip VIOLATION → COMPLIANT; Unit 2's `H4-01`–`H4-09` remain COMPLIANT; zero rows are unresolved. Quote this unit's removed fixture payload bytes and the new aggregate reduction percentage; the overall ≥80% target may correctly remain red.
- The harness regression suite remains green without weakening manifest coverage, fixed-fixture byte stability, mutation/liveness controls, or the 20-line/4 KB summary cap. If anchors or tests move, show that the old anchor became stale because behavior changed and that the replacement still fails when path-passing is reverted.
- Direct command evidence that the main session retains only the selected labeled path list while both receiving subagents are explicitly responsible for reading draft contents, and that draft-selection/fallback, 2+ minimum, operator approval, architecture output, 14-criterion QC, verdict routing, and handoff behavior remain intact.
- A scoped diff showing no other seam directive, skill/agent contract, compaction behavior, output path, model, or project copy changed. No representative AI-authored run is required for this instruction-only operand transport unit; S1's later representative chapter regression remains the semantic end-to-end proof.
- Repository evidence: relevant local checks stay green; `git diff --check` passes; committed paths stay inside the allowed surfaces; current `origin/main` is an ancestor of the resulting `HEAD`; and the local commit hash is recorded.

Completion condition: the committed canonical command path-passes all four selected section-draft operand seams `H3-20`–`H3-23`, preserves the complete architecture and QC workflow semantics, keeps all accepted seams compliant and the harness falsifiable with zero unresolved rows, advances the aggregate reduction, and leaves every excluded surface untouched.

Stop and hand back if: any targeted seam is not a measured pre-change violation; either receiving subagent cannot read the selected paths; a full draft must be embedded for a verified semantic reason; path-passing requires changing an excluded consumer or policy; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or the unit would require propagation, external dispatch, or another operator-reserved capability.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation if needed, edits within the scoped canonical command and S1 harness surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — after `git fetch origin`, `git merge-base --is-ancestor origin/main HEAD` returns true and `git rev-list --left-right --count origin/main...HEAD` is `0 20`, so no reconciliation was needed. Both Unit 2 commits are ancestors of `HEAD` (`17cc5726…` production, `160aa983…` correction). `git status --porcelain` at start showed only the hook-maintained `logs/friction-log.md` and this state file modified; the friction-log change was left untouched and unstaged throughout.
- Claim (2): HOLDS — pre-change `bash check-relay-payload.sh --format tsv` reports `H3-20`, `H3-21`, `H3-22`, `H3-23` all `content` / VIOLATION at 58,110 bytes / 780 lines each; `H4-01`–`H4-09` all `path` / COMPLIANT; `unresolved (uncovered) : 0`. Aggregate at 44%, `verdict: TARGET NOT MET`. No mismatch with the brief.
- Claim (3): HOLDS — Phase 1 step 5 (lines 30–34) globs `{part_dir}/drafts/*` and `{part_dir}/approved/*`, selects the highest-numbered draft per section by numeric suffix, falls back to `approved/`, enforces the 2+ minimum, and presents the inventory before the operator-approval gate at line 36. So one concrete path per section is already resolved before Phase 2 runs. The four places that consumed that same set: main-session reads at Phase 2 step 1 (`H3-20`) and Phase 3 step 3 (`H3-22`), and content relays into the two delegation prompts at Phase 2 step 5 (`H3-21`) and Phase 3 step 4 (`H3-23`). Searched the command for consumer-side reads of the drafts: there were **none** — both subagents received the drafts embedded, which is exactly what this unit inverts.
- Claim (4): HOLDS, and precedent settles it rather than inference. Phase 2's consumer is a `general-purpose` sub-agent (all tools). Phase 3's is `qc-gate`, whose definition at `.claude/agents/qc-gate.md` line 4 declares `tools: Read`; its only read prohibition (line 43) is scoped to *skill files*, not artifacts. Three live commands already hand `qc-gate` a path it opens itself — `produce-formatting.md` lines 121/125 ("The absolute path to the style reference — the subagent reads it directly"), `produce-prose-draft.md` lines 145–146 ("subagent reads this file before evaluating"), and `run-report.md` line 67 ("**Project reference doc PATH (subagent reads directly)**"). So no agent-definition change is needed. On the skills: `research-structure-creator` § Inputs Required asks for "Multiple chapter drafts (minimum 2)" with no embedding requirement, and `architecture-qc` § Input Requirements item 5 plus its hard stop "Chapter drafts unavailable → Refuse" are availability conditions, not transport conditions — its criterion 16 prong 2 even instructs "Read the scope-critical sections directly". Neither skill was changed.
- Claim (5): HOLDS — `ls -l` shows canonical `produce-architecture.md` is a regular file. `awk` over `seam-manifest.tsv` returns exactly four rows for this file (`H3-20`–`H3-23`) and no others. `grep -rn "All section draft"` across the repo returns only the command itself and the two manifest anchor rows; no other command, reference file, deploy/sync command, or project copy carries the directive text. `grep -n "produce-architecture\|H3-2[0-3]"` over `check-relay-payload.test.sh` matches only the generic file-coverage loop at line 186 — no T9 case pins these anchors, so no test change was forced.

Result: the four W4-H3 section-draft operand relays are converted and committed at `df96a00f39e07d4f15589d3e79dfb12199d323c3`. Phase 2 step 1 and Phase 3 step 3 now *resolve* the Phase-1-selected drafts to project-root-relative paths instead of reading them into the main session, and both delegation prompts pass those paths with an explicit draft-reading responsibility placed on the receiving subagent. The production diff is six changed lines and two added lines, all inside Phase 2 and Phase 3.

Evidence:

- **Seam flip.** `bash check-relay-payload.sh --format tsv` before → after: `H3-20`–`H3-23` `content`/VIOLATION → `path`/COMPLIANT. `H4-01`–`H4-09` COMPLIANT → COMPLIANT. Compliant seams 9/39 → 13/39; measured violations 20 → 16; ambiguous 5 → 5; contract-exempt 10 seams unchanged; `unresolved (uncovered)` 0 → 0. Exactly four rows moved, which is the isolation control on this edit.
- **Byte reduction.** Current relayed bytes 989,673 → 757,653 against the fixed 1,785,794-byte baseline: **232,020 bytes removed by this unit**, aggregate reduction **44% → 57%**. The overall S1 target (≥80%) is correctly still red — `verdict: TARGET NOT MET`.
- **Falsifiable, not asserted.** Two copies of the live `.claude` tree were staged outside the repo; in one, all four directives were mechanically reverted to their pre-Unit-3 content wording by `sed`. The check then reported `H3-20`–`H3-23` COMPLIANT on the live copy and VIOLATION on the reverted copy — all four, in the same run. The reverted copy **kept** the two added "Draft-reading responsibility" bullets, so the flip is driven by the seam directive's shape and not by the added prose; a change that only *claimed* conversion would still measure VIOLATION.
- **Harness regression.** `bash check-relay-payload.test.sh` → **17 passed, 0 failed**, the same 17 cases and the same controls as the accepted Unit 2 run. No anchor, manifest row, fixture byte, or cap was touched: all four manifest anchors still match the rewritten lines (`^1\. [A-Za-z]+ all section drafts identified in Phase 1` matches "1. Resolve …", `^   - All section draft .* \(labeled by section ID\)$` and `^   - All section draft [a-z]+$` match the new bullets), so the conversion required no harness adjustment at all.
- **Workflow semantics preserved.** The scoped diff leaves Phase 1 untouched — draft selection by highest-numbered suffix, `approved/` fallback, the 2+ minimum PAUSE, the inventory presentation and the operator-approval gate are all unchanged. Phase 2's output path, adaptation notes and Task line are unchanged; Phase 3's architecture read, adaptation notes, 14-criterion Task line and PASS/FAIL verdict routing are unchanged; Phase 4's handoff is unchanged. No skill, agent, reference file, output path, model line or project copy was modified.
- **Compaction.** Both `▸ /compact` markers stay in place, in the same positions, with the same count — no compaction behaviour changed. Only their trailing rationale text was corrected, because "raw draft content no longer needed in main session" would have become a false statement once the drafts stop entering main; Phase 3's now names the architecture content, which Phase 3 step 2 does still read into main.
- **Repository.** `git merge-base --is-ancestor origin/main HEAD` → true. `git diff --check` → clean. Committed path: the one command file. Work commit `df96a00f39e07d4f15589d3e79dfb12199d323c3`; this state-file record follows as a second local commit so it could carry the exact hash. No push, no merge into `main`.

Carried forward unchanged: the five ambiguous rows (`H1-02`, `H1-03`, `H2-04`, `H2-05`, `H3-04`) remain untouched; the two cap violations (`H1-01`, `H2-01`) are unmoved; the remaining W4-H3 operand groups in `run-report` and `run-execution` remain for later units; canonical edits still do not propagate to the project copies (S0 territory); and the hook-maintained `logs/friction-log.md` change remains unrelated user state and was not staged.

Deferral noticed and not done: no T9 liveness case pins the W4-H3 operand class, so `H3-20`–`H3-23` have no permanent in-suite anchor — this unit proved the flip by a one-off revert experiment instead. Adding a `flip_back H3-21` case would give the class a standing anchor, but it is not required by this unit's completion condition and the suite already covers both flip directions (`H1-01`/`H3-17` forward, `H4-01`/`H4-02` back), so it is left for Codex to schedule.

## Blocker

None.

## Next action

Codex: assess Unit 3 against its completion condition — the four `H3-20`–`H3-23` seams converted with every accepted seam still compliant and zero unresolved rows, the complete architecture and QC workflow semantics preserved, aggregate reduction advanced 44% → 57%, and the harness green at 17/17 with no anchor, manifest, fixture or cap change required. Judge in particular whether placing the draft-reading responsibility on the receiving subagent is sufficient given `architecture-qc`'s "Chapter drafts unavailable → Refuse" hard stop, and whether correcting the two `▸ /compact` rationale lines was inside this unit's scope or should have been left as a deferral. Then decide whether to take the recorded T9-anchor deferral now or open the next W4-H3 operand unit.

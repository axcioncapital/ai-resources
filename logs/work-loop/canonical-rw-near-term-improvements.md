---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 4 — path-pass W4-H3 chapter-draft operands.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 3 is accepted at `df96a00f39e07d4f15589d3e79dfb12199d323c3`: all four `produce-architecture` section-draft seams path-pass and the fixed-fixture reduction is now 57%. Unit 4 applies the same bounded operand principle to one payload class in `run-report`: the chapter-draft set used by report-architecture creation and architecture QC at `H3-01`, `H3-05`, `H3-06`, and `H3-07`. This directly advances S1 while leaving the distinct per-chapter architecture relay (`H3-08`), all other Stage-4 inputs, and the return-contract work for later units.

Required outcome: the canonical `run-report` command no longer reads the complete chapter-draft set into the main session at Step 4.0 or Step 4.1b and no longer embeds those draft contents in the Step 4.1 or Step 4.1b delegation prompts. It retains a validated, labeled set of project-root-relative chapter-draft paths and makes each receiving subagent responsible for reading those drafts at runtime. Preserve the fresh-session prerequisite, all other Stage-4 inputs and content contracts, report-architecture output and checkpoint, operator architecture review, 14-criterion architecture QC, verdict routing, model overrides, compaction points, and the entire Step 4.2 chapter-production sequence.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md` § 5 S1, governs: W4-H1–H4 use disk-write-and-return-path or path-plus-capped-summary where the existing isolation contract permits it, with deterministic seam coverage and payload reduction plus a later representative regression.
- The accepted harness at `7e2b97a6d195bc8ac035e670a4b11ab2107331f5`, as advanced through Unit 3, is executable current-state evidence. `workflows/research-workflow/tests/s1-relay/seam-manifest.tsv` classifies `H3-01`, `H3-05`, `H3-06`, and `H3-07` as `operand-path-ok`; the README groups them as bulk operand reads and onward relays. Those classifications are verify-first repository claims, not authority to change any other seam.
- `workflows/research-workflow/.claude/commands/run-report.md` Step 4.0 currently states a broad content-passing isolation rule. That sentence is a load-bearing claim to reconcile against `workflows/research-workflow/docs/required-reference-files.md`, the actual consumer capabilities, the invoked skill input contracts, and existing canonical path-passing precedent before it is narrowed. If it expresses a genuine requirement for embedded chapter drafts, stop rather than overriding it from the manifest alone.
- The plan file's `Status: Proposed` header predates the operator's content-bound approval and remains an excluded approval-metadata synchronization issue; the exact approved content binding above governs this task.
- The correction-round discovery that the current live-copy count is stale is a propagation/S0 deferral. This unit changes the canonical command only and makes no claim that project or archived copies were synchronized.

Check against the repository before acting:

1. Validate this task and re-establish current repository state. Verify current `origin/main` remains an ancestor of `HEAD` after the repository-required fetch, the Unit 3 work commit is present, and the unrelated hook-maintained `logs/friction-log.md` modification is preserved. Stop on a conflict or material change to an S1 premise.
2. Run the accepted target check before editing and inspect the live anchors for `H3-01`, `H3-05`, `H3-06`, and `H3-07`. All four must be measured violations; Unit 2's H4 seams and Unit 3's `H3-20`–`H3-23` must remain compliant; the manifest must have zero unresolved rows. Hand back the exact mismatch if not.
3. In `workflows/research-workflow/.claude/commands/run-report.md`, identify every main-session read and delegation of the chapter-draft set through Steps 4.0, 4.1, and 4.1b, and verify that the prerequisite already establishes concrete on-disk chapter files before those stages. Distinguish this complete chapter-draft set from the per-chapter drafts produced later in Step 4.2 and from `H3-08`'s architecture payload.
4. Reconcile Step 4.0's broad “Sub-agents receive content, not file paths (per context isolation rules)” sentence against `docs/required-reference-files.md`, the actual tool declarations of the general-purpose and `qc-gate` consumers, `research-structure-creator` and `architecture-qc` input requirements, and existing canonical commands that make consumers read operand paths. Proceed only if the evidence supports a narrow chapter-draft carve-out without weakening a genuine isolation boundary; record the disposition explicitly.
5. Verify that both consumers can read project-root-relative chapter-draft paths and that neither skill semantically requires the drafts embedded in the prompt. Verify that the architecture, scarcity register, section directives, editorial recommendations, and all Step 4.2 inputs keep their existing transport and behavior.
6. Verify the conversion requires no skill/agent, project-copy, deploy/sync, reference, model-policy, output-policy, return-contract, or other relay change.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-report.md`; the existing `workflows/research-workflow/tests/s1-relay/` harness only where the production wording requires a bounded behavior-anchor, manifest, or README adjustment; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: every seam except `H3-01`, `H3-05`, `H3-06`, and `H3-07`; `H3-08`; changes to consumer skills/agents or other commands; every other Step-4 payload; project or archived copies; deploy/sync propagation; approval metadata; the representative chapter run; ambiguous isolation cases; W4-H1/H2 returns; S0 and S2–S11; push, merge into `main`, deployment, credentials, spend, and external dispatch.
- Codex framing decision: these four seams are one dominant deliverable because the same on-disk chapter-draft set is loaded once for architecture creation and again for its QC. `H3-08` is held back because it is a different payload relayed inside the per-chapter loop, while the remaining W4-H3 operands belong to `run-execution`.

Required evidence, all capable of failing:

- Before/after focused target output for `H3-01`, `H3-05`, `H3-06`, and `H3-07`: all four flip VIOLATION → COMPLIANT; Unit 2's H4 seams and Unit 3's `H3-20`–`H3-23` remain COMPLIANT; zero rows are unresolved. Quote this unit's removed fixture payload bytes and the new aggregate reduction percentage; the overall ≥80% target may correctly remain red.
- The harness regression suite remains green without weakening manifest coverage, fixed-fixture byte stability, mutation/liveness controls, or the 20-line/4 KB summary cap. If anchors or tests move, show that the old anchor became stale because behavior changed and that the replacement still fails when path-passing is reverted.
- Direct command evidence that the main session retains only a validated labeled path list for the chapter-draft set while both architecture consumers explicitly read the contents themselves, and that the prerequisite, all non-draft Stage-4 inputs, architecture/checkpoint outputs, operator review, 14-criterion QC, verdict routing, model overrides, compaction behavior, and Step 4.2 remain intact.
- A scoped diff showing Step 4.0's isolation wording changed only as far as the verified chapter-draft carve-out requires and that no other seam directive, skill/agent contract, return shape, output path, model, or project copy changed. No representative AI-authored run is required for this instruction-only operand transport unit; S1's later representative chapter regression remains the semantic end-to-end proof.
- Repository evidence: relevant local checks stay green; `git diff --check` passes; committed paths stay inside the allowed surfaces; current `origin/main` is an ancestor of the resulting `HEAD`; and the local commit hash is recorded.

Completion condition: the committed canonical command path-passes `H3-01`, `H3-05`, `H3-06`, and `H3-07`, preserves the complete report-architecture and architecture-QC semantics plus every non-draft Stage-4 contract, keeps all accepted seams compliant and the harness falsifiable with zero unresolved rows, advances the aggregate reduction, and leaves every excluded surface untouched.

Stop and hand back if: any targeted seam is not a measured pre-change violation; the broad Step 4.0 content rule proves to be a genuine isolation requirement for the chapter drafts; either consumer cannot read the selected paths; a full chapter draft must be embedded for a verified semantic reason; path-passing requires changing an excluded consumer, Step 4.2 behavior, or policy; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or the unit would require propagation, external dispatch, or another operator-reserved capability.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation if needed, edits within the scoped canonical command and S1 harness surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — after `git fetch origin`, `git merge-base --is-ancestor origin/main HEAD` returns true and `git rev-list --left-right --count origin/main...HEAD` is `0 22`, so no reconciliation was needed. Unit 3's work commit `df96a00f…` is an ancestor of `HEAD`. The hook-maintained `logs/friction-log.md` modification was present at start, was never staged, and is still present.
- Claim (2): HOLDS — pre-change `bash check-relay-payload.sh --format tsv` reports `H3-01` (4.0.1), `H3-05` (4.1.3), `H3-06` (4.1b.2) and `H3-07` (4.1b.3) all VIOLATION at 65,480 bytes / 880 lines each; Unit 2's `H4-01`–`H4-09` and Unit 3's `H3-20`–`H3-23` all COMPLIANT; `unresolved (uncovered) : 0`; aggregate 57%, `verdict: TARGET NOT MET`. No mismatch.
- Claim (3): HOLDS — the command's own prerequisite line 7 requires chapter drafts to exist in `/analysis/chapters/{section}/` before the command runs, so concrete on-disk files precede every Step-4 stage. The complete chapter-draft set is consumed in exactly four places: main-session reads at Step 4.0.1 (`H3-01`) and Step 4.1b.2 (`H3-06`), and content relays into the two delegation prompts at Step 4.1.3 (`H3-05`) and Step 4.1b.3 (`H3-07`). This set is distinct from the per-chapter drafts written later in Step 4.2 (`H1-01`/`H1-02`/`H1-03`, all left untouched) and from `H3-08`'s architecture payload at 4.2a (excluded, still VIOLATION after this unit). `grep -c` confirms each of the four manifest anchors matches exactly one line in the file.
- Claim (4): HOLDS — the broad Step 4.0 sentence is **not** a genuine isolation requirement for chapter drafts, and the disposition is recorded here explicitly. The governing document, `docs/required-reference-files.md` § Path-passing convention line 74, states the context-isolation rule with a closed enumeration: the reference-doc carve-out "applies only to reference docs, not to per-chapter inputs (**research extracts, cluster memos, section directives**), which are passed by content per the context-isolation rule." Chapter drafts are not in that list. The manifest agrees independently — `H3-02` (section directives) and `H3-03` (cluster memos) are classified `intentional-content`, `H3-04` (research extracts) `ambiguous`, and the four targeted chapter-draft rows `operand-path-ok`. The rule's stated scope is *per-chapter* inputs, and Steps 4.1/4.1b are structurally cross-chapter: architecture creation and its QC run over every chapter at once, which `research-structure-creator` § Inputs Required requires ("cross-chapter decisions … can only be resolved when all pieces are on the table"). So the per-chapter isolation rationale cannot apply to them. On capability: Step 4.1's consumer is `general-purpose` (all tools) and Step 4.1b's is `qc-gate`, whose definition declares `tools: Read` with its only read prohibition scoped to skill files; the same file already hands `qc-gate` a path it opens itself at line 67 ("**Project reference doc PATH (subagent reads directly)**"), and `produce-formatting.md` and `produce-prose-draft.md` do the same. On the skills: `architecture-qc`'s hard stop is "Chapter drafts **unavailable** → Refuse" — an availability condition, not a transport one — and its criterion 16 prong 2 instructs the reviewer to "Read the scope-critical sections directly". Unit 3 converted the same payload class feeding these same two skills and was accepted. Line 27 was therefore narrowed, not overridden: it now states the rule's actual scope and adds a second bounded carve-out naming the cross-chapter operand, while explicitly keeping every other enumerated input on content.
- Claim (5): HOLDS — both consumers read project-root-relative paths (evidence in claim 4). Every non-draft payload keeps its existing transport: `H3-02` and `H3-03` remain EXEMPT-CONTENT, `H3-04` remains a VIOLATION in the ambiguous class, `H3-08`, `H3-09`, `H1-01`, `H1-02`, `H1-03`, `H4-07`, `H4-08` and `H4-09` are all unmoved, and the architecture, scarcity register, section directives and editorial recommendations are still passed as content in the Step 4.1b prompt.
- Claim (6): HOLDS, with one thing worth naming. `ls -l` shows canonical `run-report.md` is a regular file. No skill, agent, project copy, deploy/sync command or model line needed changing. `grep -rln "Read all chapter drafts from"` returns two files: the command itself and `reference/stage-instructions.md` line 139. I inspected that second hit rather than assuming it: it is a stage-level description ("**Step 4.1** — Read all chapter drafts from … Apply `research-structure-creator` to produce …"), it specifies no transport and never says "into the main session", and its numbering aggregates the command's 4.0 and 4.1. The drafts are still read after this change — by the sub-agent — so the sentence stays true at that altitude and no reference change was required.

Result: the four W4-H3 chapter-draft operand relays are converted and committed at `253cba7067ff29d5322299707a81cee90080de14`. Step 4.0.1 now resolves the drafts to validated, labeled, project-root-relative paths without reading them into the main session; Step 4.1b.2 loads its other four inputs as content and the drafts as paths only; and both delegation prompts pass the paths with the reading responsibility placed on the receiving sub-agent. The production diff is seven changed lines, all inside Steps 4.0, 4.1 and 4.1b.

Evidence:

- **Seam flip.** `bash check-relay-payload.sh --format tsv` before → after: `H3-01`, `H3-05`, `H3-06`, `H3-07` VIOLATION → COMPLIANT. Controls unmoved — Unit 2's `H4-01`–`H4-09` and Unit 3's `H3-20`–`H3-23` still COMPLIANT; `H3-02`/`H3-03`/`H3-09` still EXEMPT-CONTENT; `H3-04`, `H3-08`, `H1-01`, `H1-02`, `H1-03` still VIOLATION. Compliant seams 13/39 → 17/39; measured violations 16 → 12; ambiguous 5 → 5; cap violations 2 → 2; `unresolved (uncovered)` 0 → 0. Exactly four rows moved.
- **Byte reduction.** Current relayed bytes 757,653 → 496,197 against the fixed 1,785,794-byte baseline: **261,456 bytes removed by this unit**, aggregate reduction **57% → 72%**. The overall S1 target (≥80%) is correctly still red — `verdict: TARGET NOT MET`.
- **Falsifiable, not asserted.** Two copies of the live `.claude` tree were staged outside the repo; in one, all four directives were mechanically reverted to their pre-Unit-4 content wording by `sed`. The check reported all four COMPLIANT on the live copy and all four VIOLATION on the reverted copy in the same run. The reverted copy still resolves every manifest row (`unresolved (uncovered) : 0`), so the flip is a real shape change and not a token-miss artifact — and the reverted Step 4.1b.2 line deliberately kept this unit's "as content;" clause, isolating the verb as the mechanism.
- **Harness regression.** `bash check-relay-payload.test.sh` → **17 passed, 0 failed**, the same 17 cases and controls as the accepted Unit 3 run. No anchor, manifest row, payload token, fixture byte or cap was touched. That constrained the production wording in one place worth flagging: `H3-07`'s manifest token is the literal string `chapter draft content`, so the Step 4.1b.3 line keeps that phrase and reads "in place of chapter draft content, the chapter draft paths — …". Replacing it with a bare "chapter draft paths" would have made the token unfindable, turning a future regression into an *unresolved* row instead of a VIOLATION and weakening exactly the falsifiability this unit relies on.
- **Workflow semantics preserved.** The fresh-session prerequisite, the subagent model-override policy block, Step 4.0 items 2–6, the architecture output path and its checkpoint, the operator architecture review PAUSE, the 14-criterion QC task line, PASS/FAIL verdict routing, and the entire Step 4.2 chapter-production sequence are all outside the diff. Both `▸ /compact` markers stay in place with the same count and position; only Step 4.1's trailing rationale changed, because "raw chapter drafts no longer needed" would have become false once the drafts stop entering main.
- **Repository.** `git merge-base --is-ancestor origin/main HEAD` → true. `git diff --check` → clean. Committed path: the one command file. Work commit `253cba7067ff29d5322299707a81cee90080de14`; this state-file record follows as a second local commit so it could carry the exact hash. No push, no merge into `main`.

Carried forward unchanged: the five ambiguous rows (`H1-02`, `H1-03`, `H2-04`, `H2-05`, `H3-04`) remain untouched; the two cap violations (`H1-01`, `H2-01`) are unmoved; `H3-08` and the `run-execution` operand group remain for later units; canonical edits still do not propagate to the project copies (S0 territory); and the hook-maintained `logs/friction-log.md` change remains unrelated user state and was not staged.

Deferral noticed and not done: `reference/stage-instructions.md` line 139 still summarises Step 4.1 as "Read all chapter drafts from …". It is not false at that altitude (the drafts are still read, by the sub-agent) so it is not a correction this unit owes, but once S1 finishes converting the operand classes, that summary would read more accurately as naming the sub-agent as the reader. Left for Codex to schedule; the file is an excluded reference surface for this unit.

## Blocker

None.

## Next action

Codex: assess Unit 4 against its completion condition — the four chapter-draft seams converted, every excluded seam demonstrably unmoved, aggregate reduction advanced 57% → 72%, harness green at 17/17 with zero anchor, manifest, fixture or cap change, and the full Stage-4 semantics preserved. Judge in particular the claim-4 disposition: whether narrowing Step 4.0's isolation sentence to the enumeration in `docs/required-reference-files.md` line 74, plus a second carve-out naming the cross-chapter chapter-draft operand, is a correct reading of the isolation contract or an override of it — that is the one judgment this unit rests on. Also rule on whether keeping the literal phrase "chapter draft content" in the Step 4.1b.3 line to preserve `H3-07`'s manifest token was the right trade against cleaner prose plus a token update. Then open the next unit — `H3-08` and the `run-execution` operand group remain, and the aggregate is 8 points short of the S1 target.

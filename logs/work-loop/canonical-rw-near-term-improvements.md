---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Discovery mode. Unit 11 — settle the five ambiguous isolation contracts.

Named reason for the loop: S1 needs several bounded units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 10 is accepted at implementation commit `e275292551ba248809992b55e8526d9115d3eba2` plus correction commit `8f66ce725a4c6ee349c85a99838d5ffb705b6b9b`: `H1-01` now writes the complete draft before returning a truthful path-plus-bounded handoff, its old return fails the cap/pointer fixture, downstream seams remain unchanged, and the accepted harness remains 17/17. S1 is now at 24/40 compliant, 87.503531% fixed-fixture reduction, 6 measured violations, 0 cap violations, 5 ambiguous seams, and 0 unresolved rows. The next justified work is not another approval ceremony or a source edit: the accepted edit map explicitly hands the five ambiguous seams back so their isolation contracts can be settled before implementation.

Required outcome: inspect the live workflow and return a per-seam evidence ruling for exactly `H3-04`, `H2-04`, `H1-02`, `H1-03`, and `H2-05`. For each, classify the existing contract as `path-safe`, `content-required exception`, or `unresolved — operator decision required`; explain the producer, consumer, exact payload, consumer Read capability, full-content need, and whether substituting a path would change intentional isolation, independence, confidentiality, or analytical meaning. If repository authority resolves the five, recommend the smallest implementation grouping and order; if a material authority tie remains, identify the precise tie and hand back without inventing intent.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: remove justified W4-H1–H4 payloads where isolation allows; preserve analytical meaning; deterministically cover every named seam; permit no unapproved full-content exception; and retain the representative chapter regression.
- The accepted 40-row manifest and harness README are the authoritative deterministic proof surface and current edit map. The manifest marks these five rows `ambiguous`; the README says to settle them before touching them and identifies the extract-class contradiction. Discovery may recommend future manifest consequences but must not edit the manifest, harness, or fixtures.
- `workflows/research-workflow/docs/required-reference-files.md` § Path-passing convention is the live isolation convention: reference docs pass by path, while per-chapter research extracts, cluster memos, and section directives pass by content. Its authority is relevant but not automatically conclusive where another live canonical command implements the same artifact class differently.
- The live canonical commands and the three consuming skill contracts are verify-first repository evidence: `run-report.md` Steps 4.0 and 4.2; `run-cluster.md` Step 2.3; `verify-chapter.md` Steps 1–3; `skills/chapter-prose-reviewer/SKILL.md`; `skills/report-compliance-qc/SKILL.md`; `skills/evidence-prose-fixer/SKILL.md`; and `workflows/research-workflow/.claude/agents/qc-gate.md`.
- The July audit W4-H1–H4 finding is non-governing diagnostic evidence: it recommends path-passing but explicitly says some relays may be design-intentional. It cannot resolve an isolation contract by itself.
- The plan file's stale `Status: Proposed` header remains excluded approval-metadata synchronization; the exact content-bound approval above governs.

Check against the repository before concluding:

1. Validate task identity, ownership, lifecycle, and repository state; verify both accepted Unit 10 commits and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on ownership conflict, stale state, or a material change to the S1 premise.
2. Re-resolve the five manifest rows by exact command step and payload. Confirm that all five remain `content` / `VIOLATION` / `ambiguous`, that no row is duplicated or unresolved, and that `H2-03` is the separate non-ambiguous `operand-path-ok` Step 1.1 chapter read.
3. For `H3-04` and `H2-04`, compare the exact extract lifecycle and consumers across `required-reference-files.md`, `run-report` Step 4.0/4.2, `run-cluster` Step 2.3, and `verify-chapter` Steps 1–2. Establish whether the apparent contradiction is a genuine contract conflict or two contextually different uses, and what evidence table, prompt, or downstream input would be lost or broadened by path-passing.
4. For `H1-02` and `H1-03`, verify Unit 10's draft-write reorder is present, inspect the reviewer and QC skill/agent contracts, and establish whether each consumer can read the exact draft path without changing its intended independence or visible context. Treat the reviewer and compliance QC as separate decisions; do not infer one from the other.
5. For `H2-05`, inspect the verification-report and chapter-prose inputs, the fixer's contract, and the bright-line/operator gate that follows. Establish whether exact input paths preserve the fixer's bounded correction task and the main session's safety checks, or whether content curation is intentional and load-bearing.
6. Search only the named canonical workflow, consuming skills/agent, accepted S1 proof surface, directly cited isolation convention, and load-bearing history needed to resolve an actual contradiction. Name the searched surfaces and exact supporting or conflicting text. Do not widen into deployed project copies, unrelated workflows, or speculative architecture.

Required evidence, all capable of changing the conclusion:

- A five-row disposition table naming seam id, producer → consumer, current payload/transport, consumer Read capability, full-content need, isolation/meaning consequence, evidence citations, and one of the three allowed rulings.
- For every `path-safe` ruling, evidence that the exact consumer can read the durable artifact and receives no broader or different information than today; name any bounded summary still required.
- For every `content-required exception` ruling, the exact live authority and operational reason that makes content load-bearing; distinguish an already-authorized intentional-content contract from a new exception that would require operator approval.
- For every unresolved ruling, the exact materially tied sources, why the authority hierarchy cannot break the tie, and the narrow operator question needed. Do not label a technically difficult implementation as an operator decision.
- A manifest consequence for each seam (`operand-path-ok` / path target, or `intentional-content` / `content-required`, or still `ambiguous`) stated as a recommendation only, plus the smallest future implementation grouping/order if repository evidence resolves it.
- Confirmation that `H2-03` was deliberately held outside because its isolation classification is already settled and it is ready for a later implementation unit; no source, manifest, harness, fixture, plan, or propagation file changed.
- A scoped repository-status/diff account and the exact local commit hash containing only this state-file discovery handback (plus normal hook output if it occurs). No implementation tests are required because this unit implements nothing.

Scope and Codex framing decisions:

- In scope: read/search/history inspection of the exact sources named above; this state file; a local commit by Claude.
- Excluded: any edit to canonical commands, skills, agents, docs, manifest, harness, fixtures, plan, deployed or archived copies; implementing any seam; `H2-03`; representative chapter execution; API/model calls; propagation; approval metadata; S0 or S2–S11; push, merge, deployment, credentials, spend, or external dispatch.
- Codex framing decision: the five seams are one bounded discovery deliverable because they share one isolation-contract unknown and the accepted edit map groups them. Implementation remains separate so the discovery cannot silently turn a proposed ruling into code.
- Codex framing decision: `H2-03` stays outside this unit because it is already classified `operand-path-ok`; mixing a settled implementation with contract discovery would combine two deliverables and blur the completion condition.
- Existing deferrals remain outside this unit: stale `reference/stage-instructions.md` wording about the Step 4.2 writer return; the writer's pre-existing Reviewer Findings footer sequencing inconsistency; README `Blast radius` wording; `verify-chapter` Step 3 item 7a's report-content relay; and optional truncation naming in `verify-chapter` item 5.

Completion condition: the five ambiguous contracts are resolved as far as repository authority permits, with the required per-seam evidence and recommended manifest/implementation consequences; no eventual target is implemented; any genuinely tied authority is returned as a precise operator question; `H2-03` and every excluded surface remain untouched; and Claude writes and commits the discovery handback with `turn: codex`.

Stop and hand back if: a named seam no longer matches the manifest; resolving it requires inventing operator intent, changing the approved operating model, accepting undelegated residual risk, or expanding scope; the relevant authority remains materially tied after the hierarchy is applied; the evidence needed cannot be established from the bounded source set; or any implementation/source edit would be required to answer the discovery question.

Capability subset: baseline local repository discovery only—read, search, bounded history inspection, state validation, and a local commit of this state file. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No source edit, network/API call, push, merge, deployment, credential access, destructive action, policy change, or external communication is authorized.

## Latest result

**Unit 11 discovery — the five ambiguous isolation contracts. Nothing implemented; only this state file changed.**

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` returns 0; `git cat-file -t` returns `commit` for both accepted Unit 10 hashes, subjects "path-plus-cap the chapter-writer return" (`e2752925`) and "specify and bound the chapter-writer handoff" (`8f66ce72`); `HEAD` = `ad2f6b631c63ba7a9e2b8dd2912cc1c2edf8ae3f`. Validator `ACTIVE_CLAUDE`, ownership `PROCEED`, capability `READY`. Working tree carried only the hook-maintained `logs/friction-log.md` modification and this state file; the friction-log modification is preserved and unstaged. No ownership conflict, no stale state, no S1 premise change.
- Claim (2): HOLDS — searched `seam-manifest.tsv` and the live check for the six ids. All five ambiguous rows resolve exactly once and all five are live `content` / `VIOLATION` / `ambiguous`: `H3-04` (`run-report` 4.0.5), `H2-04` (`verify-chapter` 1.2), `H1-02` (`run-report` 4.2b), `H1-03` (`run-report` 4.2c), `H2-05` (`verify-chapter` 3.7a). `awk`-sorted `uniq -d` over every seam id returns no duplicate; the check reports `unresolved (uncovered) : 0`. `H2-03` is confirmed separate: `verify-chapter` 1.1, anchor `^1\. [A-Za-z]+ the chapter from the specified path`, isolation **`operand-path-ok`** — a measured violation but **not** ambiguous, so its classification is already settled and it is held outside this unit correctly.
- Claim (3): HOLDS — read `required-reference-files.md` § Path-passing convention, `run-report` Step 4.0 (all six loads plus the carve-out paragraph) and Step 4.2a, `run-cluster` Steps 1.2 and 2.3, and `verify-chapter` Steps 1–2. Findings recorded in the table and in "The extract-class contradiction" below.
- Claim (4): HOLDS — confirmed Unit 10's draft-write reorder is live at 4.2a (the writer writes the complete draft to the canonical `-draft.md` path before returning), then read `skills/chapter-prose-reviewer/SKILL.md` § Inputs, `skills/report-compliance-qc/SKILL.md` § Inputs and `.claude/agents/qc-gate.md`. Ruled separately, on separate evidence; neither inferred from the other.
- Claim (5): HOLDS — read `verify-chapter` Steps 2.3–2.5 and 3.7a–3.7g, `skills/evidence-prose-fixer/SKILL.md` § Inputs, and `.claude/agents/execution-agent.md`.
- Claim (6): HOLDS — searched only the named surfaces: `run-report.md`, `run-cluster.md`, `verify-chapter.md`, `run-synthesis.md`, `docs/required-reference-files.md`, `tests/s1-relay/` (manifest, check, README), the three consuming skills, `qc-gate.md`, `execution-agent.md`. No deployed project copy, archived copy or unrelated workflow was read, and no speculative architecture was consulted.

**Disposition table.** Rulings are `path-safe` / `content-required exception` / `unresolved — operator decision required`.

| Seam | Producer → consumer | Payload, transport now | Consumer Read capability | Full-content need | Isolation / meaning consequence of path-passing | Ruling |
|---|---|---|---|---|---|---|
| `H3-04` | disk → `run-report` main session (4.0.5) | all section research extracts, content | main session reads files; **but** it is a loader, not the end consumer | Yes, derived — it is the only load feeding `H3-09` | None of its own. It exists solely so 4.2a can hand each chapter its extract slice as content. Path-passing here is impossible without converting `H3-09`, which is a live authorized `intentional-content` / `content-required` contract and is out of scope | **content-required exception** (derived) |
| `H2-04` | disk → `verify-chapter` main session (1.2) | chapter research extracts, content | main session reads files; the **end consumer is the GPT-5 API** | Yes, structural | The extracts become the "evidence table" half of the Step 2.3 prompt body. An external model has no filesystem; `execution-agent.md` states it *receives* "A system prompt to send" and "A user message to send" and is forbidden to "Modify the system prompt or user message". A path cannot be a prompt body | **content-required exception** |
| `H1-02` | `run-report` main session → `chapter-prose-reviewer` sub-agent (4.2b) | chapter draft, content | Yes — general-purpose sub-agent with Read | No | None. Since Unit 10 the draft is durable at the 4.2a output path before 4.2b runs, and the file **is** what the writer produced. The reviewer would read the identical artifact and receives nothing broader. Its independence is unaffected: it reviews the draft either way, and it sees no writer reasoning in either transport | **path-safe** |
| `H1-03` | `run-report` main session → `report-compliance-qc` qc-gate sub-agent (4.2c) | chapter draft, content | Yes — `qc-gate.md` declares `tools: Read` | No | None. The same 4.2c qc-gate **already** reads the architecture operand by path (`H3-24`, live `path` / `COMPLIANT`), and a qc-gate at 4.1b.3 already receives "in place of chapter draft content, the chapter draft paths" (`H3-07`, live `path` / `COMPLIANT`). Same agent type, same artifact class, already in production | **path-safe** |
| `H2-05` | `verify-chapter` main session → `evidence-prose-fixer` sub-agent (3.7a) | chapter prose, content | Yes — general-purpose sub-agent with Read | No | None. The chapter is on disk (`on_disk=yes`) and unmodified at 3.7a — corrections are applied later at 3.7d — so the file equals what the main session holds. The fixer needs the whole chapter for context-aware replacements, which a path supplies in full. The bright-line checks and the operator pause at 3.7b–c act on the **returned corrections**, not on how the chapter arrived | **path-safe** |

**Evidence for each `path-safe` ruling — the consumer reads the durable artifact and gains nothing broader.**

- `H1-02`: `chapter-prose-reviewer/SKILL.md` already carries a path-passed blocking input — Input 2 Style Spec is "**Passed as an absolute file path.** Read the file at the provided path before evaluating; do not expect inlined content." So the skill is path-capable by its own contract, and Input 1 Chapter Draft states no transport at all, so path-passing contradicts nothing it says. Live precedent for this artifact class reaching a general-purpose sub-agent by path: `H3-05` at 4.1.3 — "all chapter draft paths … the sub-agent reads each draft at its given path" — `path` / `COMPLIANT`. Bounded summary still required: none; the reviewer's own return (verdict, findings, recommended changes) is unchanged and is not a measured seam.
- `H1-03`: live precedent at the exact step — `H3-24`, the 4.2c architecture operand, "the qc-gate sub-agent reads the complete approved architecture file itself", `path` / `COMPLIANT`. Live precedent for this exact artifact class to this exact agent type — `H3-07` at 4.1b.3, `path` / `COMPLIANT`. Bounded summary still required: none; the QC verdict and per-item findings return is unchanged.
- `H2-05`: `evidence-prose-fixer/SKILL.md` Input 2 Chapter Draft states a purpose ("context-aware replacements that preserve surrounding narrative flow") but no transport, so a path satisfies it. Bounded summary still required: none; the correction list with per-item bright-line metadata is unchanged.

**Evidence for each `content-required exception` ruling.**

- `H2-04` is a **new** exception, not an already-authorized one, and it does **not** need operator approval, because the reason is a technical capability fact rather than a policy choice: the payload's end consumer is an external model reached through `execution-agent`, whose contract fixes the caller as the author of the prompt text. Live authority: `.claude/agents/execution-agent.md` §§ "You will receive" and "You must NOT: Modify the system prompt or user message"; `verify-chapter` Step 2.3 "use chapter prose + evidence table as input".
- `H3-04` is an **already-authorized** contract reaching one step back. `H3-09` (4.2a per-chapter extracts) is live `EXEMPT-CONTENT` under `intentional-content` / `content-required`, and `required-reference-files.md` § Path-passing convention independently names research extracts as per-chapter inputs "passed by content per the context-isolation rule". The main session cannot hand a sub-agent content it does not hold, so 4.0.5 must read it. No new exception is created.

**The extract-class contradiction the README names: two contextually different uses, not a genuine contract conflict.** `run-cluster` path-passes extracts (Step 1.2 "file paths only, not content"; Step 2.3 "The extract file **paths** for this cluster (not the file content — the sub-agent reads its own extracts)") while `required-reference-files.md` says extracts pass by content. The convention's sentence is explicitly scoped to *per-chapter inputs*, and `run-cluster`'s use is per-cluster Stage-2 analysis, not per-chapter Stage-4 writing input. The two therefore never actually collide. What makes `H3-04` content-bound is not the convention doc but `H3-09` immediately downstream. Worth recording for the future: `run-report` already contains the exact carve-out that would free 4.0.5 — Step 4.0.1 resolves the chapter drafts "to project-root-relative paths only" because "the complete chapter-draft set is a cross-chapter bulk operand, not a per-chapter input" (`H3-01`, `path` / `COMPLIANT`). 4.0.5 has the identical bulk shape. **If `H3-09` were ever reclassified — which this unit does not propose and has no authority to do — `H3-04` would become path-safe by that precedent.** Until then it is content-required.

**Manifest consequences — recommendations only; no manifest, harness or fixture was touched.**

- `H3-04` → `intentional-content` / `content-required`.
- `H2-04` → `intentional-content` / `content-required`.
- `H1-02` → `operand-path-ok` / `path`, and `on_disk` `no` → **`yes`** (Unit 10 made the draft durable before 4.2b runs; the recorded `no` predates it).
- `H1-03` → `operand-path-ok` / `path`, and `on_disk` `no` → **`yes`**, same reason.
- `H2-05` → `operand-path-ok` / `path` (`on_disk` is already `yes`).

**Smallest implementation grouping and order.**

1. **One unit for `H1-02` + `H1-03` together.** Same file, same per-chapter block, same artifact, same Unit 10 draft path. They are ruled separately above on separate evidence, but they share the consumer-contract coherence edits, so splitting them would edit the same two sentences twice: `report-compliance-qc/SKILL.md`'s "Content is passed directly for inputs 1–6 (not file paths)" — **already stale**, since its input 3 architecture is live path-passed via `H3-24` — and `qc-gate.md`'s "An artifact to evaluate (the content of a file)", already contradicted by `H3-07` and `H3-24`. Correcting stale wording is a coherence fix, not an operator decision.
2. **One unit for `H2-05`.** Different file and consumer, and its directive also carries the already-deferred verification-report relay, so a later unit may want both.
3. **No implementation unit for `H3-04` or `H2-04`** — manifest reclassification only, which belongs to whichever unit is authorized to touch the manifest.

Projected consequence, stated as arithmetic over current row states and **not** a harness run: converting the three path-safe seams takes compliance from 24/40 to **27/40**, and reclassifying the two content-required rows moves them out of the reduction accounting as `EXEMPT-CONTENT`, leaving `H2-03` as the single remaining measured violation.

**Observation on the excluded `H2-03`, raised because it changes the recommended order.** `H2-03` is correctly outside this unit and its isolation class is settled as `operand-path-ok`. But the `H2-04` finding bears on it directly: `verify-chapter` Step 2.3 builds the GPT-5 prompt from "chapter prose + evidence table", so the chapter read at Step 1.1 feeds the same external prompt body that the extracts do. If `H2-04` cannot be path-passed for that reason, a straight path conversion of `H2-03` looks likely to hit the same wall. I am not ruling on it and have changed nothing. **Recommendation: re-examine `H2-03` before scheduling its implementation unit.** The clean way out for both — moving prompt assembly into `execution-agent` so the caller passes paths — is a contract change to that agent and a redesign, well outside this unit.

**Deferrals — recorded, not implemented.** Newly noticed: `verify-chapter` Step 3.7a passes the fixer only "the skill content, the verification report, and the chapter prose", omitting `evidence-prose-fixer`'s Input 3 Evidence Pack, which its own contract marks "required — blocking". That is a live input gap independent of transport and is not part of any of the five rulings. Carried unchanged from before: stale `reference/stage-instructions.md` wording about the Step 4.2 writer return; the writer's Reviewer Findings footer sequencing inconsistency; README `Blast radius` wording; `verify-chapter` Step 3 item 7a's report-content relay; optional truncation naming in `verify-chapter` item 5.

**Repository account.** No source, skill, agent, doc, manifest, harness, fixture, plan, deployed or archived file was modified — `git status --porcelain` shows only this state file plus the preserved hook-maintained `logs/friction-log.md`. `origin/main` remains an ancestor of `HEAD`. No implementation test was run, because this unit implements nothing. Discovery commit: `17c398031f9907f2a7292e78972f525639421abd` (verified with `git rev-parse`), containing this state file only.

## Blocker

None.

## Next action

Codex: assess the Unit 11 discovery and reframe or stop. Judge whether the three `path-safe` rulings are carried by the live precedent cited (`H3-05`, `H3-07`, `H3-24`) rather than by analogy; whether `H2-04`'s content requirement is genuinely a capability fact about the external-API prompt body rather than an implementation difficulty; whether `H3-04`'s derived ruling correctly stops short of reopening `H3-09`; whether the extract-class contradiction is rightly dissolved as two scoped uses; and whether the recommended grouping and the `H2-03` re-examination warning should shape the next unit. No eventual target was implemented and none should be read as authorized by this evidence.

---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 15 — encode the H2-03 content-required exception.

Named reason for the loop: S1 spans several bounded implementation and proof units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 14 is accepted at discovery commit `02996c0e36b0a3034d14359ae271b8fb577b0321`. The live contract establishes that `H2-03` and accepted `H2-04` are the chapter-prose and evidence-table halves of one caller-assembled GPT-5 request: `verify-chapter` owns the pre-dispatch confidentiality check and user-message construction; `execution-agent` receives rather than builds that message, may not modify it, and may not send information absent from it; the external endpoint cannot resolve a repository path. This unit encodes that accepted exception and proves that this classification alone, not a workflow-source or checker change, completes the deterministic target.

Required outcome: change `H2-03` from `operand-path-ok` / `path` to the same `intentional-content` / `content-required` classification as `H2-04`; update the S1 README so it records the accepted ruling rather than calling the seam open; reconcile the harness's live-workflow expectation with the now-green deterministic surface; and produce a falsifiable classification-only reversal proving that reverting only this manifest ruling restores the single measured violation and `TARGET NOT MET` while unrelated rows remain fixed.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: every named W4-H1–H4 seam must be converted or explicitly justified, no unapproved full-content exception may remain, reduction must be at least 80%, and representative proof remains separate.
- Unit 14 commit `02996c0e36b0a3034d14359ae271b8fb577b0321` is accepted technical evidence and the settled classification ruling for `H2-03`.
- Unit 11's accepted `H2-04` ruling remains the matching precedent: these are two halves of the same external request, not independently transportable operands.
- The live `seam-manifest.tsv`, S1 README, checker, and harness are verify-first repository evidence. The checker logic should not need to change; the harness's current red-live-surface expectation is expected to become stale when the accepted classification lands and must be reconciled without weakening falsifiability.
- Workflow command, agent, skill, fixture, plan, propagation, and deployed/project-copy files are outside this unit and must remain unchanged.

Check against the repository before editing:

1. Validate task identity, lifecycle, ownership, and repository state; verify Unit 14 commit `02996c0e36b0a3034d14359ae271b8fb577b0321`, preserve `logs/friction-log.md`, and stop on a material S1 premise change.
2. Confirm `H2-03` still occurs exactly once in `workflows/research-workflow/tests/s1-relay/seam-manifest.tsv` and is exactly `operand-path-ok` / `path`; confirm `H2-04` remains `intentional-content` / `content-required` and both resolve to `verify-chapter` Step 1 inputs.
3. Capture the pre-change live checker result: exit 1, `TARGET NOT MET`, exactly one measured violation (`H2-03`), zero unresolved/cap/ambiguous findings, and the accepted Unit 13 aggregate. This is the failing case for this encoding unit, not a newly manufactured workflow defect.
4. Inspect the harness assertions that describe the live surface, especially the current T6/T9d assumptions, and change only assertions made false by the accepted classification. Preserve the harness's ability to fail on a real content violation and its independence controls.
5. Confirm the README still calls `H2-03` open and identify only the bounded wording that must change to record the accepted ruling and deterministic completion.

Required evidence:

- Exact diff/account showing only `seam-manifest.tsv`, the S1 `README.md`, the S1 harness test if needed, and this state file changed; no checker, fixture, workflow source, agent, skill, plan, propagation, or deployed/project-copy change.
- Before/after manifest row for `H2-03`, with `H2-04` unchanged.
- Pre-change failing result and post-change passing result from the live checker. Post-change must report `TARGET MET`, exit 0, 0 measured violations, 0 cap/ambiguous/unresolved findings, 27/40 compliant conversions, 13 explicit exemptions, relayed payload 3,058 bytes, and 99.824469% reduction, unless measured repository evidence disproves Unit 14's projected arithmetic.
- A manifest-only reversal on a temporary copy: returning only `H2-03` to `operand-path-ok` / `path` must restore exactly one violation and `TARGET NOT MET`; all other 39 rows, cap/ambiguity/unresolved counts, and workflow source bytes must remain unchanged. Restore the accepted classification before commit.
- The full S1 harness passes after any expectation update, while retaining its synthetic green/red, stale-anchor, cap, ambiguity, live-directive reversal, and independence controls. State the assertion count.
- Fixture regeneration remains byte-identical. Do not edit regenerated fixture content.
- README evidence names why the exception is content-required, that it matches `H2-04`, that no source edit is needed, and that deterministic completion is by an explicitly accepted exception rather than conversion. Remove the stale "still open" direction without broad cleanup.
- Exact implementation commit and repository account, including preservation of the hook-maintained friction log.

Scope and Codex framing decisions:

- In scope: one manifest classification, the directly corresponding README ruling, only the harness expectation/control needed to keep the now-green live surface falsifiable, local deterministic tests, and this state file; Claude's local commit.
- Excluded: any change to `verify-chapter`, `execution-agent`, another workflow command/agent/skill, prompt assembly, confidentiality or Cross-Model behavior, checker scoring logic, fixtures, representative chapter execution, real API/model calls, Evidence Pack or verification-report fixes, other seams, project/deployed/archived copies, propagation, plan/approval metadata, S0 or S2–S11, push, merge, deployment, credentials, spend, or external communication.
- Codex framing decision: accepting the exception is not metric gaming because it follows four pre-existing live contract statements, an external-endpoint capability fact, and the already-accepted ruling for the same request's other half. The implementation remains narrow enough that any source or checker change is a stop, not an improvisation.
- Carried deferrals remain outside: missing Evidence Pack and verification-report relay at Step 3.7a; stale `reference/stage-instructions.md` writer-return wording; writer Reviewer Findings footer sequencing; README `Blast radius`; optional verification-summary truncation naming.

Completion condition: the accepted `H2-03` classification is encoded without workflow or checker changes; the deterministic checker is green with every named seam either converted or explicitly exempted and reduction above target; a classification-only reversal proves the verdict can still fail for precisely this seam; the complete harness and byte-stability proof pass; README truthfully records the ruling; and Claude commits the handback with `turn: codex`.

Stop and hand back if: the live contracts or manifest no longer match Unit 14; encoding requires workflow-source or checker-logic changes; the verdict changes for any reason beyond the single classification; any unrelated row moves; projected aggregate values do not reconcile; the harness cannot remain falsifiable without expanding beyond the bounded test surface; or a source/fixture/propagation change becomes necessary.

Capability subset: baseline local implementation only—read, search, bounded history inspection, edits only within the S1 manifest/README/harness test and this state file, local tests, and a local commit. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No network/API call, push, merge, deployment, credential access, destructive action, policy change, or external communication is authorized.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` returns 0; `git cat-file -t 02996c0e36b0a3034d14359ae271b8fb577b0321` returns `commit`, subject "Unit 14 H2-03 ruling, turn to codex". Validator `ACTIVE_CLAUDE`, ownership `PROCEED`, capability `READY`. `logs/friction-log.md` preserved and unstaged throughout. No material S1 premise change.
- Claim (2): HOLDS — `grep -c '^H2-03\t'` over the manifest returns exactly 1. Pre-change that row was `operand-path-ok` / `path`; `H2-04` was and remains `intentional-content` / `content-required`. Both resolve to `verify-chapter` Step 1 inputs — `H2-03` at 1.1 (token `the chapter`), `H2-04` at 1.2 (token `research extracts`).
- Claim (3): HOLDS — the pre-change live checker is the failing case for this unit, captured before any edit: **exit 1**, `verdict: TARGET NOT MET`, exactly **1** measured violation (`H2-03`), 0 cap, 0 ambiguous, 0 unresolved, 27/40 compliant, 19428 relayed bytes, 12 exempt seams over 509920 bytes — the accepted Unit 13 aggregate exactly. This is the pre-existing state, not a defect manufactured to be fixed.
- Claim (4): HOLDS — the only harness assertion made false by the accepted classification is `T6`, which read "the expected red baseline … violations measured". `T9d` was inspected and needed no change: it compares `H2-03`'s state before against after and is indifferent to which state that is. Reconciliation described below.
- Claim (5): HOLDS — the README's `H2-03` line still read "**Still open, and deliberately not decided here** … Re-examine it before scheduling its implementation unit". That is the bounded wording that had to change; the W4-H2 surface row at line 57 is a factual seam listing and needed none.

Result: `H2-03` is encoded as `intentional-content` / `content-required`, matching `H2-04`. **One manifest row changed and nothing else in the workflow.** The deterministic surface is now green: every named seam is either converted or explicitly justified. `T6` is reconciled from red-baseline to green, and a new `T6c` re-earns that green from the opposite direction so it cannot be a claim the check merely asserts. The README records the ruling, its four contract grounds, that no source edit was needed, and that completion is by accepted exception rather than by conversion.

Evidence:

- **Failing case → passing case, on the live checker.** Before: exit **1**, `TARGET NOT MET`, 1 measured violation. After: exit **0**, `verdict: TARGET MET`, **0** measured violations, 0 cap, 0 ambiguous, 0 unresolved, **27/40 compliant**, **13** exempt seams over **526290** bytes, baseline 1742140, relayed **3058** bytes, reduction **99.824469%** (target ≥80%).
- **Unit 14's projection reconciles exactly.** Projected baseline 1742140 / relayed 3058 / exempt 13 over 526290 / compliant 27 of 40 / violations 0 — every figure matches measurement. On the reduction, the checker's own integer line prints `99%`, and computing the exact value gives 99.8244687568…%, which is 99.824469% rounded to six decimals. Unit 14's projected 99.824469% is therefore correct; a `bc scale=6` run prints 99.824468 only because `bc` truncates rather than rounds. Recording that so the two figures are not later read as a discrepancy.
- **Classification-only reversal — the proof this unit exists to give.** On a temporary copy of the manifest, `H2-03`'s two fields alone were returned to `operand-path-ok` / `path` (a 1-row, 2-line diff) **with the workflow source untouched**. Result: exactly **1** measured violation, `verdict: TARGET NOT MET`, exit **1**, relayed bytes back to 19428, exempt back to 12 / 509920. All **39** other rows byte-identical between the two runs (`diff` reports no difference). `git diff HEAD --name-only -- workflows/research-workflow/.claude/` returns 0 files throughout, so no source byte moved during the proof, and the accepted classification is what sits on disk — the reverted manifest was a separate temp file and never overwrote it. **The verdict can still fail, and it fails for precisely this seam.**
- **`T6` reconciliation, and why it does not weaken the check.** Pinning `T6` to red would now fail on a correct surface — the mirror of the trap the existing `T9` comment already names, where a stale expectation "would invite weakening the check to make it pass". So `T6` now asserts exit 0 / `TARGET MET` / 0 violations, cap and ambiguous. Because green is the weaker claim, **`T6c`** re-earns it: it puts one real content relay back into a live command body on a scratch copy and requires the **whole run** to go red — `TARGET NOT MET` and exit 1, not merely one row changing colour. Without `T6c`, `T6` could pass on a check that had quietly lost the ability to fail at all.
- **Full harness: 26 passed, 0 failed** (was 25/25; the one addition is `T6c`). Every control retained and still capable of failing: synthetic green (`T1`) and red (`T2`), the prose-assertion falsifiability control (`T3`), directive-edit control (`T3b`), stale-anchor `UNRESOLVED` (`T4`), cap enforced and measured (`T5`/`T5b`), ambiguity in both directions (`T5c`/`T5d`), 0 uncovered rows (`T6b`), fixture byte-stability (`T7`), class and surface coverage (`T8`/`T8b`), six live-directive reversals (`T9` ×6), and three independence controls (`T9b`, `T9c` ×2, `T9d`).
- **Fixture byte-stability.** `T7` regenerates byte-identical at **475687 bytes** — the same figure recorded in Units 10, 12 and 13. No fixture file was edited or regenerated into the tree.
- **Repository account.** Changed: `seam-manifest.tsv` (one row), `s1-relay/README.md`, `check-relay-payload.test.sh`, and this state file. Verified unchanged, each returning 0 changed files: `workflows/research-workflow/.claude/` (all commands and agents), `skills/`, `tests/s1-relay/fixture/`, `check-relay-payload.sh` (the checker's scoring logic), `make-fixture.sh`, and `plans/`. No propagation, no deployed or archived copy. `git diff --check` clean. `origin/main` remains an ancestor of `HEAD`. `logs/friction-log.md` preserved and unstaged. Implementation commit: `{{UNIT15_COMMIT}}`.

**What this does and does not establish.** It establishes that S1's *deterministic* proof is complete: 40 named seams, 27 converted, 13 explicitly justified, 0 violations, 99.824469% reduction against an ≥80% target, and a suite that still fails when the surface regresses. It does **not** establish S1 complete — the objective also requires the representative chapter regression, which no unit has run and which this unit did not touch.

**Deferrals — unchanged and still carried:** the missing Evidence Pack input and the verification-report relay at `verify-chapter` Step 3.7a; stale `reference/stage-instructions.md` writer-return wording; the writer's Reviewer Findings footer sequencing; README `Blast radius`; optional verification-summary truncation naming. Nothing new was noticed.

Aggregate position after Unit 15: 27/40 converted plus 13 explicit exemptions = all 40 named seams accounted for; 99.824469% fixed-fixture reduction; 0 measured violations; 0 cap; 0 ambiguous; 0 unresolved; `verdict: TARGET MET`, exit 0; harness 26/26. Remaining S1 work: the representative chapter regression.

## Blocker

None.

## Next action

Codex: assess Unit 15. The decisive question is whether the green is earned. Test it on the classification-only reversal (one manifest row, two fields, workflow source provably untouched, restoring exactly one violation and `TARGET NOT MET` while all 39 other rows hold), on `T6c` re-earning the green from the opposite direction, and on whether reconciling `T6` from red-baseline to green preserved rather than weakened falsifiability. Then judge whether Unit 14's projection reconciling exactly against measurement supports the ruling, and whether the deterministic half of S1 is now complete. If it is, the only remaining objective work is the representative chapter regression — continue there, or close if the objective's exit condition is met another way.

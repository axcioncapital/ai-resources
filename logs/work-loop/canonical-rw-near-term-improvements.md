---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Discovery mode. Unit 14 — resolve the H2-03 external-prompt contract.

Named reason for the loop: S1 spans several bounded implementation and proof units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 13 is accepted at implementation commit `4846e1aef44990e227303f99777891b380536825`: `H2-05` now path-passes the unchanged chapter to the evidence fixer, its source-behavior reversal and file-mate control pass, aggregate reduction is 98.895201%, and `H2-03` is the sole measured violation. Unit 11 deliberately left `H2-03` outside, and its accepted `H2-04` ruling later raised a load-bearing concern: both the chapter and the extracts appear to become the external GPT-5 prompt body under the current execution-agent contract. This unit resolves that one remaining uncertainty by inspection only; it implements nothing.

Required outcome: determine whether `H2-03` is path-safe under the existing approved workflow, content-required under the current external-prompt contract, or unresolved because a genuine operator-owned operating-model choice remains. Trace the exact chapter payload from `verify-chapter` Step 1.1 through claim-ID checking, prompt/evidence-table construction, execution-agent invocation, external API dispatch, result handling, and later correction application. Distinguish a simple transport substitution from a redesign that moves prompt assembly or changes the execution-agent/Cross-Model boundary.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, § 5 S1 and § 6, governs: path-pass W4-H1–H4 where isolation and analytical meaning allow, retain any full-content exception explicitly and truthfully, and prove every named seam.
- Unit 11 discovery commit `17c398031f9907f2a7292e78972f525639421abd` is accepted technical evidence for `H2-04`: research extracts are content-required because they form the evidence-table half of the external prompt under the current execution-agent contract. Its observation about `H2-03` is a warning, not a settled ruling.
- Unit 13 commit `4846e1aef44990e227303f99777891b380536825` is the accepted current proof state: `H2-03` is `content` / `VIOLATION` / `operand-path-ok`, the sole measured violation.
- The live `verify-chapter.md`, `workflows/research-workflow/.claude/agents/execution-agent.md`, execution-agent skill/invocation references, and the accepted S1 manifest/README are verify-first repository evidence. Directly relevant workflow/agent history may be inspected only to resolve an actual contract ambiguity.
- The July audit is diagnostic background: it identified duplicated verification responses but cannot decide this input seam's isolation or external-prompt requirement.
- The stale `Status: Proposed` plan header remains excluded approval-metadata synchronization; the exact content-bound approval above governs.

Check against the repository before concluding:

1. Validate task identity, lifecycle, ownership, and repository state; verify Unit 13 commit `4846e1aef44990e227303f99777891b380536825`, preserve `logs/friction-log.md`, and stop on a material S1 premise change.
2. Re-resolve `H2-03` exactly once and confirm it is the sole measured violation, on disk, currently classified `operand-path-ok`, and targeted to `path`. Capture its producer, immediate consumer, downstream end consumer, payload, bytes/lines, and every use of the Step 1 chapter body.
3. Trace how `verify-chapter` constructs the system prompt, user message, evidence table, and execution-agent request. Establish whether the main session must supply the chapter body under the live contract, whether the external GPT-5 endpoint can resolve a repository path, and whether execution-agent is permitted to read a path and assemble or alter the caller's prompt.
4. Compare `H2-03` with accepted `H2-04` on the exact prompt boundary. Identify any material difference that permits one to be path-based while the other remains content, or show that they are the two halves of one content-required external request.
5. Inspect all canonical execution-agent callers and the Cross-Model/no-modification rules only far enough to determine whether path-passing the chapter is already supported. If it would require moving prompt assembly, changing agent responsibilities, or modifying external-request semantics, classify that as redesign rather than pretending it is a relay swap.
6. Check whether the chapter body has any other load-bearing main-session use after the `H2-05` conversion—claim checker, correction application, logs, or checkpoints—and distinguish path-based file operations from actual content retention.

Required evidence:

- One explicit ruling: `path-safe`, `content-required exception`, or `unresolved — operator decision required`.
- A producer → immediate consumer → external/end-consumer trace naming where the complete chapter body exists and why.
- Exact live contract citations for prompt ownership, execution-agent input shape, no-modification/Cross-Model constraints, external filesystem capability, and any later main-session use.
- If `path-safe`: evidence that the existing consumer can read the exact chapter path and deliver the identical external prompt without changing agent responsibility, prompt semantics, confidentiality, claim-ID behavior, or analytical meaning; recommend the smallest implementation unit.
- If `content-required`: the exact technical/settled-contract reason, whether this is an already-authorized consequence like `H2-04` or would be a new exception, and the recommended manifest/proof consequence. Explain why moving prompt assembly would be redesign rather than necessary S1 implementation.
- If unresolved: the materially tied authorities, why technical evidence cannot settle them, and the narrow operator question. Do not escalate mere implementation difficulty.
- Confirmation that no source, agent, skill, manifest, harness, fixture, README, plan, or propagation file changed; exact state-file-only discovery commit and repository account.

Scope and Codex framing decisions:

- In scope: bounded read/search/history inspection of `verify-chapter`, execution-agent's live contract and canonical callers, the accepted S1 proof surface, the accepted `H2-04` evidence, and this state file; Claude's local state-file commit.
- Excluded: changing `H2-03` transport or classification; moving prompt assembly; changing execution-agent, Cross-Model rules, external API behavior, prompt content, evidence-table construction, claim-ID checks, or correction application; fixing the Evidence Pack or verification-report defects; any other seam; real API/model calls; representative chapter execution; project/deployed/archived copies; propagation; approval metadata; S0 or S2–S11; push, merge, deployment, credentials, spend, or external dispatch.
- Codex framing decision: this is Discovery because the current manifest says path-safe while accepted evidence suggests the same content requirement as `H2-04`. Implementation before resolving that conflict would either weaken the external prompt or smuggle in an agent redesign.
- Carried deferrals remain outside: missing Evidence Pack and verification-report relay at Step 3.7a; stale `reference/stage-instructions.md` writer-return wording; writer Reviewer Findings footer sequencing; README `Blast radius`; optional verification-summary truncation naming.

Completion condition: repository evidence settles `H2-03` as far as the approved authority permits; the result states the exact manifest/proof and next-unit consequence without implementing it; any true authority tie becomes one narrow operator question; no eventual target or excluded file changes; and Claude commits the discovery handback with `turn: codex`.

Stop and hand back if: the seam no longer matches the manifest; the prompt boundary cannot be established from the bounded sources; resolving it requires inventing operator intent, changing the approved operating model, accepting undelegated risk, or expanding scope; or any source implementation is required to answer the question.

Capability subset: baseline local repository discovery only—read, search, bounded history inspection, state validation, and a local commit of this state file. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No source edit, network/API call, push, merge, deployment, credential access, destructive action, policy change, or external communication is authorized.

## Latest result

**Unit 14 discovery — the `H2-03` external-prompt contract. Nothing implemented; only this state file changed.**

**Ruling: `content-required exception`.** It is the same already-authorized requirement as accepted `H2-04`, not a new one — the two are literally the two halves of one external request, which `verify-chapter` Step 2.3 names in a single phrase: "use chapter prose + evidence table as input".

Inspected (2026-08-17):

- Claim (1): HOLDS — `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` returns 0; `git cat-file -t 4846e1aef44990e227303f99777891b380536825` returns `commit`, subject "path-pass the chapter to the evidence fixer". Validator `ACTIVE_CLAUDE`, ownership `PROCEED`, capability `READY`. `logs/friction-log.md` preserved and unstaged. No material S1 premise change.
- Claim (2): HOLDS — `H2-03` resolves exactly once, `verify-chapter` Step 1.1, anchor `^1\. [A-Za-z]+ the chapter from the specified path`, token `the chapter`, glob `chapters/chapter-01-draft.md`, 1 repetition, producer `disk`, consumer `verify-chapter main session`, `on_disk=yes`, isolation `operand-path-ok`, target `path`, measured `content` / `VIOLATION` at 16370 bytes / 220 lines. Filtering the live check for `VIOLATION` returns exactly this one row, so it is the sole measured violation.
- Claim (3): HOLDS — traced below.
- Claim (4): HOLDS — no material difference found; the comparison is below.
- Claim (5): HOLDS — searched the whole workflow for `execution-agent`: apart from its own definition, there is **exactly one** canonical caller, `verify-chapter.md:40`. `run-execution` does not dispatch through it. So no other caller's contract is at stake, and none would be disturbed either way.
- Claim (6): HOLDS — after Unit 13 the chapter body has **no** other load-bearing main-session content use. Step 1b pipes the chapter **file path** as JSON to `check-claim-ids.sh` (path, not content). Step 3.7a now passes the path (Unit 13). Step 3.7d applies approved corrections to the file — a file operation on a path, not retained content. Steps 3.7e–f write logs and a checkpoint carrying decisions and counts, not prose. The only remaining content use is the Step 2 prompt.

**The trace: producer → immediate consumer → end consumer.**

`disk` → `verify-chapter` **main session** (Step 1.1) → the **GPT-5 API**, an external model, via `execution-agent` (Step 2.4). The complete chapter body has to exist in the main session between Steps 1.1 and 2.4, and four live contract statements each independently require it:

1. **The command's own pre-dispatch confidentiality gate** (`verify-chapter.md:9`): "Before constructing the GPT-5 API call, verify the outbound message contains no deal-specific company names, financial terms, or confidential identifiers listed in CLAUDE.md Confidentiality Boundaries. If any are found, STOP and alert the operator." A path cannot be scanned for confidential identifiers — the scan is of the *outbound message*, so the main session must hold the prose it is about to send. This is a safety control, and it is the strongest of the four.
2. **`execution-agent` receives the message; it does not build it** — "You will receive: … 3. A user message to send", and "You must NOT: Modify the system prompt or user message". Prompt ownership sits with the caller.
3. **The agent's own confidentiality rule forbids the path route outright** — "You must NOT: Send any information not explicitly provided in the user message (enforce confidentiality boundaries)." If the caller passed a path and the agent read that file and transmitted its contents, it would be sending information *not explicitly provided in the user message*. Path-passing the chapter to `execution-agent` is not merely unsupported; the agent is explicitly forbidden to do it. That the agent holds `tools: Read` does not change this — Read is for its own operation, not a licence to source outbound payload.
4. **The command already says the main session holds it** — Step 2.6: "▸ /compact — verification prompt, **raw chapter content**, and evidence table no longer needed". The retention is stated in the live text and scoped to end after Step 2.

And the end consumer settles it independently of all four: the GPT-5 endpoint is an external model with no access to this repository's filesystem, so a repository path in the prompt body would reach it as an uninterpretable string, not as the chapter.

**`H2-03` vs accepted `H2-04`: no material difference.** Both are read at Step 1 into the main session (1.1 chapter, 1.2 extracts); both become halves of the one Step 2.3 prompt body; both are covered by the same confidentiality gate and the same agent constraints; both end at the same external model. Nothing permits one to be path-based while the other is content. `H2-04` was accepted as content-required in Unit 11 on exactly this reasoning, so ruling `H2-03` the same way applies the settled ruling rather than creating a second one. **This is therefore an already-authorized consequence, not a new exception.**

Where `H2-03` differs from the seams that *were* converted is instructive: `H1-02`, `H1-03` and `H2-05` all hand a payload to a **Claude sub-agent with Read**, which can fetch the artifact itself. `H2-03`'s consumer is the **main session itself**, which cannot delegate its own confidentiality gate to anyone without moving the control.

**Why moving prompt assembly would be redesign, not S1 implementation.** To make `H2-03` a path operand, prompt construction and the confidentiality scan would both have to move into `execution-agent`. That would change the agent's stated responsibility from "send what you are given" to "assemble and send", contradict two of its explicit prohibitions, relocate an operator-facing safety gate out of the command that owns it, and leave `H2-04` behind unless it moved too. That is an operating-model change to the Claude↔GPT-5 boundary, which is outside S1 and outside this unit. It is not a relay swap.

**No operator question is needed.** The authorities are not tied — they all point the same way, and the reason is a capability-and-safety fact rather than a policy choice. Consistent with the brief's instruction not to escalate implementation difficulty, I am not manufacturing a decision here.

**Recommended manifest consequence (recommendation only — nothing changed).** `H2-03` → `intentional-content` / `content-required`, matching `H2-04`. **No implementation unit; no source edit.** `verify-chapter` Step 1.1 stays exactly as written.

**⚠ The consequence Codex must scrutinise, stated before the numbers rather than buried under them.** `H2-03` is the last measured violation. Reclassifying it would take measured violations to **0**, and since cap, ambiguous and unresolved are already 0 and reduction stays far above target, **the checker's verdict would flip from `TARGET NOT MET` to `TARGET MET` and its exit status from 1 to 0.** I read the checker's verdict block to confirm that (`fail=1` is set only by violations, cap violations, unresolved, ambiguous, or reduction below target). So this ruling, if accepted, completes S1's deterministic seam proof **by exemption rather than by conversion** — which is precisely the move that would look like gaming the metric if the exemption were weak. I want that scrutinised on its merits, not accepted because it is convenient. Two things argue it is honest: the exemption rests on four live contract statements plus an external-model capability fact, none authored by this task; and it applies a ruling Codex already accepted for the identical payload's other half. The plan's own words are the test — it permits no *unapproved* full-content exception, and this one is Codex's to approve or reject.

Projected aggregate if accepted, by arithmetic over current row states and **not** a harness run — I did not modify the manifest to produce these: 13 exempt seams over 526290 bytes (was 12 / 509920); baseline 1742140; relayed **3058** (was 19428); reduction **99.824469%**; compliant **27/40** unchanged, because an exempt row is not scored compliant; measured violations **0**; cap, ambiguous, unresolved all 0. Every one of the 40 rows would then be either converted (27) or an explicit, justified exemption (13), with none unaccounted for.

**Next unit, recommended:** one small implementation unit that encodes only this classification in the manifest and the README, adds no source change, and — because that unit is the one that turns the suite green — should be required to show the verdict flip is caused by the classification alone and by nothing else. After it, the only remaining S1 work named in the objective is the representative chapter regression.

**Repository account.** No source, agent, skill, manifest, harness, fixture, README, plan or propagation file was changed — `git status --porcelain` shows only this state file plus the preserved hook-maintained `logs/friction-log.md`. `origin/main` remains an ancestor of `HEAD`. No API or model call was made; no representative chapter was run. Discovery commit: `{{UNIT14_COMMIT}}`, containing this state file only.

**Deferrals — unchanged and still carried:** the missing Evidence Pack input and the verification-report relay at Step 3.7a; stale `reference/stage-instructions.md` writer-return wording; the writer's Reviewer Findings footer sequencing; README `Blast radius`; optional verification-summary truncation naming. Nothing new was noticed.

## Blocker

None.

## Next action

Codex: assess the Unit 14 discovery. The load-bearing judgment is whether the `content-required` ruling is honest, because accepting it flips the checker to `TARGET MET` by exemption rather than by conversion. Test it on the four cited live contract statements — the command's pre-dispatch confidentiality gate, `execution-agent` receiving rather than building the user message, its prohibition on sending anything not explicitly provided in that message, and Step 2.6's stated retention of raw chapter content — plus the external model's lack of filesystem access; on whether `H2-03` and `H2-04` really are two halves of one request rather than separable; on whether moving prompt assembly is correctly classified as an operating-model redesign outside S1; and on whether declining to raise an operator question is right given the authorities all point one way. Then reframe as an encoding unit, rule otherwise, or stop.

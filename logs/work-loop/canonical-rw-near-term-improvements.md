---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 6 — path-pass the W4-H3 report-architecture operand.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 5 is accepted at the repository-verified commit `a77136000ed4e929c0afd0eafa4d89f3bc8daa58`: `H3-17` and `H3-18` now path-pass one raw-report path per Research Extraction subagent, preserving full-report component-by-component reading and the one-session-per-subagent isolation boundary. The fixed-fixture aggregate reduction advanced from 72% to 78%, the harness remained green at 17/17, and zero rows became unresolved. Claude's Unit 5 handoff recorded the nonexistent full hash `a77136003d2ecf78a2f38d1a2b1a3e0ae2b64c07`; direct repository inspection resolved the actual commit above from the matching `a7713600` prefix, subject, and diff. This current-state record corrects that evidence fact; the production implementation needs no correction round.

Unit 6 targets only `H3-08`: the report architecture repeatedly relayed from the `run-report` main session to the per-chapter `evidence-to-report-writer` subagent in Step 4.2a. This is the smallest remaining high-value operand and is independently worth doing because every chapter consumes the same approved on-disk architecture. The distinct Answer Specs operand (`H3-19`), intentional research-extract content (`H3-09`), all other Step 4.2 inputs and every return-contract seam remain separate.

Required outcome: the canonical `run-report` Step 4.2a delegation passes the project-root-relative path `report/architecture/{section}/{section}-architecture.md` instead of embedding the architecture content, and explicitly makes each `evidence-to-report-writer` subagent read the complete architecture from that path before drafting its chapter. Preserve the architecture's existing approval/QC prerequisites and semantics, the architecture-defined chapter order, every chapter-specific content input, the locked style-reference behavior, scarcity instructions, output and checkpoint paths, operator gates, model policy, Step 4.2b onward, and the writer's full-content return contract.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md` § 5 S1, governs: W4-H1–H4 use disk-write-and-return-path or path-plus-capped-summary where the existing isolation contract permits it, with deterministic seam coverage and payload reduction plus a later representative regression.
- The accepted harness at `7e2b97a6d195bc8ac035e670a4b11ab2107331f5`, as advanced through Unit 5, is executable current-state evidence. `workflows/research-workflow/tests/s1-relay/seam-manifest.tsv` classifies `H3-08` as `operand-path-ok` with four repetitions; the README groups it with bulk operand reads and onward relays. That classification is a verify-first repository claim, not authority to change `H3-09` or another Step 4.2 input.
- `skills/evidence-to-report-writer/SKILL.md` makes the architecture load-bearing for chapter target, emphasis, structure, and length. Treat this as a semantic-completeness contract: path-passing is acceptable only if the consumer reads the complete architecture before drafting and retains the same chapter-scoped inputs.
- `workflows/research-workflow/docs/required-reference-files.md` governs its reference-doc carve-out and enumerated per-chapter isolation inputs. It does not itself authorize architecture transport; reconcile the explicit consumer input set and Read capability before changing the relay.
- The plan file's `Status: Proposed` header predates the operator's content-bound approval and remains an excluded approval-metadata synchronization issue; the exact approved content binding above governs this task.

Check against the repository before acting:

1. Validate this task and re-establish current repository state. Perform the repository-required origin fetch, verify current `origin/main` remains an ancestor of `HEAD`, verify the actual Unit 5 commit above is present, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on a conflict or material change to an S1 premise.
2. Run the accepted target check before editing and inspect the live `H3-08` anchor. It must be a measured violation; all seams accepted through Unit 5 must remain compliant; `H3-09` must retain its intentional-content classification; and the manifest must have zero unresolved rows. Hand back the exact mismatch if not.
3. In `workflows/research-workflow/.claude/commands/run-report.md`, identify the exact Step 4.2a architecture relay and verify the architecture file is created and checkpointed in Step 4.1, has passed the operator review and Step 4.1b QC gate before Step 4.2 begins, and defines the per-chapter processing order.
4. Reconcile path-passing against the `evidence-to-report-writer` contract and consumer capability. Verify the general-purpose consumer can read the project-root-relative architecture path and that explicit complete-file reading preserves every architecture-controlled drafting decision without exposing any additional project input.
5. Verify the chapter's research extracts, cluster memo, section directive, scarcity register, editorial recommendations, style reference, and quality-standards reference retain their existing transport and scoping; `H3-09` remains content-required; the full chapter-draft return and all Step 4.2b+ behavior stay unchanged.
6. Verify the conversion requires no skill/agent, project-copy, deploy/sync, reference, model-policy, output-policy, return-contract, architecture-production/QC, or other relay change.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-report.md`; the existing `workflows/research-workflow/tests/s1-relay/` harness only where production wording requires a bounded behavior-anchor, manifest, or README adjustment; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: every seam except `H3-08`; `H3-09`; `H3-19`; all chapter-specific content transport; Step 4.1/4.1b production or QC changes; Step 4.2b onward; changes to consumer skills/agents or other commands; chapter-draft return transport; project or archived copies; deploy/sync propagation; approval metadata; the representative chapter run; ambiguous isolation cases; W4-H1/H2 returns; S0 and S2–S11; push, merge into `main`, deployment, credentials, spend, and external dispatch.
- Codex framing decision: `H3-08` is one dominant deliverable because the same approved architecture is repeated once per chapter to the same writer consumer. It is held apart from `H3-19` because Answer Specs feed a different command stage and consumer, and from `H3-09` because research extracts are intentionally chapter-scoped content.

Required evidence, all capable of failing:

- Before/after focused target output for `H3-08`: it flips VIOLATION → COMPLIANT; every seam accepted through Unit 5 remains COMPLIANT; `H3-09` remains EXEMPT-CONTENT; zero rows are unresolved. Quote this unit's removed fixture payload bytes and the new aggregate reduction percentage; report honestly whether the overall ≥80% target becomes green.
- The harness regression suite remains green without weakening manifest coverage, fixed-fixture byte stability, mutation/liveness controls, or the 20-line/4 KB summary cap. If an anchor or permanent test moves, show why production behavior forced it and that the replacement fails when architecture content passing is restored. If no permanent `H3-08` mutation exists, a bounded one-off reverted-copy check is sufficient; do not add machinery solely for this unit.
- Direct command evidence that each writer receives and completely reads the same approved architecture by path while every chapter-specific input and downstream behavior retains its existing transport and scope.
- A scoped diff showing only the Step 4.2a architecture operand transport changed and that no other seam directive, skill/agent contract, chapter-draft return, output path, model, or project copy changed. No representative AI-authored run is required for this instruction-only operand transport unit; S1's later representative chapter regression remains the semantic end-to-end proof.
- Repository evidence: relevant local checks stay green; `git diff --check` passes; committed paths stay inside the allowed surfaces; current `origin/main` is an ancestor of the resulting `HEAD`; and the exact local commit hash is recorded and verified before handoff.

Completion condition: the committed canonical command path-passes `H3-08`, preserves the complete approved-architecture semantics and every excluded Step 4.2 contract, keeps all accepted seams compliant and the harness falsifiable with zero unresolved rows, reports the aggregate threshold truthfully, and leaves every excluded surface untouched.

Stop and hand back if: `H3-08` is not a measured pre-change violation; the architecture must be embedded for a verified isolation or semantic reason; the consumer cannot read or completely apply the selected file; path-passing would broaden the writer's input set; the conversion requires changing an excluded consumer, return, Step 4.1/4.1b gate, other Step 4.2 input, or policy; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or the unit would require propagation, external dispatch, or another operator-reserved capability.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation if needed, edits within the scoped canonical command and S1 harness surfaces, local tests, and local commits. The full baseline Claude deny set remains required. Nothing is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — ran `git fetch origin`; `git merge-base --is-ancestor origin/main HEAD` succeeded; `git log --oneline -1 a77136000ed4e929c0afd0eafa4d89f3bc8daa58` resolved to the Unit 5 `run-execution` raw-report conversion, and `git rev-parse a7713600` returned that same full hash — confirming Codex's correction of the fabricated hash in the Unit 5 handoff. `git status --porcelain` showed only ` M logs/friction-log.md` (hook-maintained, left untouched and uncommitted) and ` M logs/work-loop/canonical-rw-near-term-improvements.md` (Codex's uncommitted Unit 6 brief). No S1 premise changed.
- Claim (2): HOLDS — pre-change `--format tsv` gave `H3-08  content  VIOLATION  base=33576  lines=480  reps=4` — measured, not asserted — and `H3-09  content  EXEMPT-CONTENT  target=content-required`, its intentional-content classification intact. The compliant list was exactly the 19 seams accepted through Unit 5 (`H3-01 H3-05 H3-06 H3-07 H3-17 H3-18 H3-20`–`H3-23 H4-01`–`H4-09`), and `unresolved (uncovered) : 0`.
- Claim (3): HOLDS — searched `seam-manifest.tsv` for every row on `run-report.md`; `H3-08`'s relay is the single Step 4.2a directive line (line 61), which passed `architecture (\`/report/architecture/{section}/{section}-architecture.md\`)` inside a `Pass it:` list. Step 4.1 item 3 writes that exact file and item 4 checkpoints its path plus the chapter processing order; item 6 is the operator-review PAUSE. Step 4.1b item 6 gates Step 4.2 on `If Overall Verdict is PASS: note result and proceed to Step 4.2`, and item 5 halts on FAIL. Step 4.2's own header line reads `Process chapters in the order defined by the architecture from Step 4.1`. So the file is created, operator-reviewed, QC-gated and order-defining before Step 4.2a runs.
- Claim (4): HOLDS — `skills/evidence-to-report-writer/SKILL.md` § Inputs marks `2. Document Architecture (required)` and needs section length targets, section positioning and the emphasis hierarchy; searched all 19 `architecture` mentions in that SKILL.md for any inline-provision, verbatim-prompt or do-not-read-from-disk requirement and found none, so a complete read at a named path satisfies the contract. Consumer capability is proven in the same directive: the identical general-purpose sub-agent already receives `reference/quality-standards.md` by path (`H4-07`, COMPLIANT since the Unit 1 baseline). The writer previously received the whole architecture as content, so passing the whole file by path exposes no additional project input.
- Claim (5): HOLDS — the diff is one line, and within it the chapter's research extracts, cluster memo, section directive, scarcity register, editorial recommendations, style reference, the `quality-standards.md` path clause, the HEDGE/SCOPE CAVEAT/PROXY FRAMING obligation, the no-git standing constraint and `Return: chapter draft content, scarcity items addressed, evidence coverage notes` are all character-identical. Measured on the same line: `H3-09` stayed `content EXEMPT-CONTENT`, `H1-01` stayed `content VIOLATION`, `H4-07` stayed `path COMPLIANT`. Steps 4.2b, 4.2c, 4.2d, 4.2e and everything after are untouched.
- Claim (6): HOLDS — one file changed. `unresolved (uncovered) : 0` after the change proves the shared anchor and the fixture glob still resolve, so `seam-manifest.tsv` needed no edit; the harness suite needed no edit either, so no permanent anchor or control moved this unit. No skill, agent, project copy, deploy/sync surface, reference doc, model line, output policy, return contract or Step 4.1/4.1b directive appears in the diff. Step 4.0's carve-out paragraph also needed no update: the architecture is not among the six inputs it enumerates, so its closing "every other input enumerated above is still passed as content" was not made false.

Result: Unit 6 is implemented and committed at `a95116da30d05b652d3e633dcf678be58f22119f` (verified with `git rev-parse HEAD`, not reconstructed from a short hash). `run-report` Step 4.2a now passes each `evidence-to-report-writer` sub-agent the architecture PATH `/report/architecture/{section}/{section}-architecture.md` with an explicit instruction to read the complete approved architecture file itself before drafting and to receive no other cross-chapter input, in place of embedding that content once per chapter.

One disclosed wording choice, not a scope change: the brief named the file as `report/architecture/…` without a leading slash; the committed directive keeps the workflow's own project-root notation `/report/architecture/…`, matching the same path as already written by Step 4.1, Step 4.1b and Step 4.2c and every prior accepted unit. It is the same file the brief names.

Evidence:

- Target check, before → after: `H3-08` flips VIOLATION → COMPLIANT (`shape` `content` → `path`). Compliant seams 19/39 → 20/39; measured violations 10 → 9; `H3-09` still EXEMPT-CONTENT and the exempt set unchanged at ten seams; cap violations 2 → 2; ambiguous 5 → 5; unresolved 0 → 0. Every seam accepted through Unit 5 is still COMPLIANT — the post-change list is the prior 19 plus `H3-08`, with nothing dropped.
- Payload removed by this unit: current relayed bytes 383457 → 349945, i.e. **33512 fixed-fixture bytes removed** (`H3-08` 33576 → 64 across its four repetitions). Aggregate reduction 78% → **80%** (80.40% before truncation, so it clears the bar rather than rounding onto it).
- **Honest read of the threshold: the reduction sub-target is now green, the overall check is not.** `verdict: TARGET NOT MET` still prints and the script still exits 1, because the verdict also requires zero measured violations, zero cap violations and zero ambiguous rows — and 9, 2 and 5 remain. So S1's *payload-reduction* target is met; S1's deterministic seam target and its representative chapter regression are not. The largest remaining measured operand is `H3-19` (Answer Specs, 39856 bytes), deliberately excluded from this unit.
- Harness regression suite: **17 passed, 0 failed**, with no test edited. Coverage and controls are unweakened — `seam-manifest.tsv` untouched so T6b/T8/T8b measure the same 39 rows; T7 still regenerates the fixture byte-identical at 475687 bytes; the 20-line/4 KB and 80-line/16 KB caps untouched with T5/T5b passing; T6's violation count is derived, not pinned, so it reported 9 without an assertion change; all four T9 liveness controls and T9b still pass.
- Falsifiability of `H3-08`, which carries no permanent T9 case (none added — the brief asked for a bounded one-off rather than new machinery): on a scratchpad copy of `.claude/`, reverting the new path clause to the previous `architecture (\`/report/architecture/…\`),` returned `H3-08 content VIOLATION`, while `H1-01`, `H3-09` and `H4-07` held their states. The seam moves on its own directive text, and the mutation does not leak to the three co-tenant seams on the same line.
- Scoped diff: `run-report.md`, one line inside Step 4.2a. `git diff --check` on `workflows/` is clean. Committed paths stay inside the allowed surfaces; `origin/main` remains an ancestor of `HEAD`; `logs/friction-log.md` left modified-but-uncommitted and unedited.

Prior units for the record: Unit 2 `17cc5726704ef31e690a89e27a06168f4b3ecae9` plus README correction `160aa983500c984bce25e4d29111d7800e52f7c1`; Unit 3 `df96a00f39e07d4f15589d3e79dfb12199d323c3`; Unit 4 `253cba7067ff29d5322299707a81cee90080de14`; Unit 5 `a77136000ed4e929c0afd0eafa4d89f3bc8daa58`.

Carried deferrals, plus one newly noticed in Unit 6 and not implemented:

- Stale live-copy count after an archived Content Programme deployment, and bytewise Sector Intelligence deployment divergence — S0/propagation territory.
- `reference/stage-instructions.md` line 139 could name the subagent as the chapter-draft reader more precisely, but remains semantically true.
- `run-execution` Step 2.3.1's unscoped raw-reports directory versus Step 2.2b's section-scoped write path — a separate path-correctness issue.
- Step 2.4's implicit raw-report re-read — a separate verification-transport issue.
- **New:** `run-report` Step 4.2c passes the same approved architecture as content to the compliance-QC sub-agent, on the same repeated-per-chapter pattern `H3-08` just fixed. It carries no manifest row and Step 4.2b-onward is excluded from this unit, so it was left untouched; converting it would need a manifest row added first, which is a Codex framing decision.

## Blocker

None.

## Next action

Codex: assess Unit 6 at `a95116da30d05b652d3e633dcf678be58f22119f` — whether `H3-08` is genuinely converted without disturbing the three seams sharing its directive line, whether an explicit complete read at the named path preserves every architecture-controlled drafting decision in `evidence-to-report-writer`, whether the leading-slash notation choice is accepted, and how to proceed now that the payload-reduction sub-target is green at 80% while the deterministic verdict still fails on 9 violations, 2 cap violations and 5 ambiguous rows — including whether the newly recorded Step 4.2c architecture relay earns a manifest row.

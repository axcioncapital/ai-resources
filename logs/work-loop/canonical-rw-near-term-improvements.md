---
task: canonical-rw-near-term-improvements
status: active
turn: codex
---

## Objective and scope

Deliver Slice S1 of the operator-approved near-term plan: refactor the canonical Research Workflow's justified W4-H1–H4 content relays to path-passing or path-plus-capped-summary, preserve intentional context isolation and analytical meaning, and prove the plan's deterministic 100% seam coverage / 80% payload-reduction target plus its representative chapter regression. The task exits when S1 and its required proof are accepted. Approval-metadata synchronization, S0, S2–S11, deep-pipeline slimming, and deploy-fitness mission work remain outside this task.

## Lane and unit

Standard. Implementation mode. Unit 8 — path-pass the Step 2.1 Answer Specs operand.

Named reason for the loop: S1 needs several bounded implementation units, its canonical-workflow boundary must stay separate from propagation and adjacent programme work, and the aggregate deterministic and representative evidence must be independently assessed before S1 counts as complete.

## Brief

Unit 7 is accepted: production and manifest commit `79eaf6e459be47e245552ef99848c0194f945b2b` added and converted `H3-24`; README correction commit `05e4ccc7503a2ce5b817367c8aabe829f628e318` resolved both frozen documentation findings without touching production, manifest, or harness executable. The fixed-fixture reduction is 80.76% on the corrected 40-row inventory, with 21/40 seams compliant; the aggregate target remains red because measured violations, cap violations, ambiguous seams, and the representative chapter regression remain.

This unit implements the last pending low-isolation-risk bulk operand in the accepted edit map: `H3-19`, the `run-execution` Step 2.1 Answer Specs read. The Answer Specs already exist on disk, and the Step 2.1 general-purpose subagent must consume their complete contents to build research prompts; remove that payload from the main-session relay while preserving the prompt-creation contract.

Required outcome: the canonical `run-execution` Step 2.1 no longer reads all approved Answer Specs into the main session or passes their content onward. It resolves the project-root Answer Specs location and hands that location to the general-purpose subagent, which explicitly reads every approved spec needed for the section before applying `research-prompt-creator`. Preserve the complete question/spec set, component IDs, required dimensions, completion gates, mismatch handling, and every other Step 2.1 input, output, checkpoint, compact, and pause behavior.

Governing sources and dispositions:

- The operator-approved plan content at `7fec2ef6a4db178e465282975b06b06ce92892f9`, `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md` § 5 S1, governs: path-pass W4-H1–H4 seams where isolation permits, prove every named seam deterministically, preserve analytical meaning, and later run the representative chapter regression.
- The accepted S1 harness and live 40-row manifest are the sole deterministic proof surface. `H3-19` identifies `.claude/commands/run-execution.md` Step `2.1.3`, the Answer Specs operand, as `operand-path-ok` with target `path`; do not create another inventory or test system.
- The live `run-execution` Step 2.1 and `skills/research-prompt-creator/SKILL.md` are verify-first repository evidence. The skill requires the complete Answer Specs set and uses component IDs, evidence rules, dimensions, claims, and completion gates; path-passing must retain that full semantic input.
- The general-purpose subagent's ability to read repository files is verify-first repository evidence. Existing path-passed Step 2.1 reference inputs are precedent for capability, not permission to weaken the Answer Specs contract.
- The plan file's stale `Status: Proposed` header remains excluded approval-metadata synchronization; the exact content-bound operator approval above governs.

Check against the repository before acting:

1. Validate the task and repository ownership, perform the repository-required origin reconciliation, confirm `origin/main` remains an ancestor of `HEAD`, verify the two Unit 7 commits above, and preserve the unrelated hook-maintained `logs/friction-log.md` modification. Stop on conflict or a material S1 premise change.
2. Verify `H3-19` resolves exactly once to `run-execution` Step 2.1 item 3, is presently a measured content violation, is already on disk, and targets path-passing. Confirm the live Step 2.1 delegation currently relays the same Answer Specs as content to the `research-prompt-creator` consumer. Challenge and hand back if the manifest's step, anchor, payload, or consumer relationship is stale.
3. Verify the exact Answer Specs path/set Step 2.1 must expose, how the prerequisite approval gate establishes that every spec is approved, and that the general-purpose subagent can read the complete set. Verify path-passing exposes no files beyond the same section-scoped Answer Specs previously embedded.
4. Verify `research-prompt-creator` needs the complete specs for question/spec reconciliation, component-ID traceability, required dimensions, claim anchors, evidence rules, and completion gates, with no inline-only or verbatim-prompt requirement. Preserve its critical-gap and mismatch behavior.
5. Convert only the `H3-19` read-and-onward-relay seam. Keep the Execution Manifest and Research Plan transports, both project reference paths, skill loading, session grouping authority, output paths, returned session-plan summary, checkpoint, compact, and operator pause unchanged.
6. Reconcile the harness README only where this unit makes its `H3-19` mapping/current-state guidance factually stale. In particular, verify whether its reconciliation table currently groups `H3-19` under Step 2.3 despite the manifest locating it at Step 2.1, and correct that mapping if confirmed; move `H3-19` from pending to landed only after the production check passes. Do not touch the separately deferred Blast-radius paragraph.

Scope and Codex framing decisions:

- In scope: `workflows/research-workflow/.claude/commands/run-execution.md`; the existing S1 harness README only for the directly related `H3-19` mapping/current-state update; this state file; hook-maintained `logs/friction-log.md` only if normal hooks append; local checks and Claude's local commit.
- Excluded: `seam-manifest.tsv` unless the verify-first claim proves its existing `H3-19` identity is factually wrong and the unit must stop and hand back; any harness executable or fixture change; any other Answer Specs read or relay in `run-execution`; Step 2.0; Steps 2.1b and 2.2–2.4; changes to `research-prompt-creator`, an agent, or another skill; `H3-04`, `H3-09`, W4-H1/H2 returns, cap violations, ambiguous isolation cases, representative chapter execution, project/archived copies, deploy/sync propagation, approval metadata, S0 and S2–S11; push, merge into `main`, deployment, credentials, spend, or external dispatch.
- Codex framing decision: Step 2.1's main-session read and its onward content relay are one transport seam because removing only the read while still requiring content downstream would be incoherent. The README correction is permitted only because it names this exact seam and currently appears to mislocate it; it is not a general documentation pass.

Required evidence, all capable of failing:

- Pre-change target output for `H3-19`: exact resolved row, content/VIOLATION classification, fixed-fixture bytes and lines, and the aggregate counts/reduction before conversion.
- Post-change target output: `H3-19` flips VIOLATION → COMPLIANT/path; every previously accepted seam remains COMPLIANT; co-tenant seams on the edited Step 2.1 directive retain their prior classification; zero rows are unresolved. Quote removed bytes, compliant/violation counts, and aggregate reduction without rounding.
- Falsifiability: on a scratch/reverted copy, restore the Answer Specs content read/relay and show `H3-19` returns to VIOLATION while an unrelated control seam does not move. Add no permanent machinery unless the existing harness contract requires it.
- Direct command/skill evidence that the subagent reads the complete section-scoped approved Answer Specs set by path before prompt construction, and that the required semantic fields and mismatch/critical-gap behavior remain available unchanged.
- The existing harness regression suite remains green at 17/17, fixture bytes remain stable, caps/liveness controls remain intact, and the README (if edited) accurately separates the Step 2.1 `H3-19` seam from the Step 2.3 raw-report seams.
- A scoped diff, `git diff --check`, exact committed paths, current `origin/main` ancestry, and the exact local commit hash. No representative AI-authored run is required for this instruction-only unit.

Completion condition: `H3-19` is committed as a path-passed, complete consumer-read Answer Specs operand at Step 2.1; the main session no longer loads or relays that payload; prompt-creation semantics and every co-tenant transport remain intact; the accepted harness remains falsifiable and green with zero unresolved rows; and directly related README guidance is truthful.

Stop and hand back if: `H3-19` does not uniquely represent Step 2.1's Answer Specs operand; the subagent cannot discover and read exactly the approved section-scoped spec set; complete content is semantically required inline; path-passing would weaken approval, mismatch, traceability, dimension, claim, or completion-gate behavior; the conversion requires changing an excluded consumer, skill, agent, manifest, harness executable, or another seam; the harness becomes unresolved or unfalsifiable; repository reconciliation changes the premise; or propagation/external/operator-reserved capability would be required.

Capability subset: baseline local repository work only—read/search/history inspection, repository-required origin fetch and non-destructive reconciliation, edits inside the scoped canonical command and bounded S1 README surface, local tests, and local commits. The full baseline Claude deny set remains required. Nothing else is selected from the pre-authorizable set, which is empty. No push, merge into `main`, deployment, credential access, destructive action, external service, policy change, or communication.

## Latest result

Inspected (2026-08-17):

- Claim (1): HOLDS — `work-loop-state.sh validate` returned `ACTIVE_CLAUDE`; `work-loop-owner.sh check --depth repo` returned PROCEED (this checkout already declares the task); `work-loop-capability.sh check --canonical .../ai-resources` returned READY with every copied component byte-identical to canonical. `git fetch origin` then `git merge-base --is-ancestor origin/main HEAD` succeeded, so `origin/main` remains an ancestor of `HEAD`. Both Unit 7 commits exist: `79eaf6e4` and `05e4ccc7`. `logs/friction-log.md` carries an unrelated hook-maintained modification (102 insertions); it was left unstaged and unedited. No material S1 premise changed.
- Claim (2): HOLDS — searched `seam-manifest.tsv` for `H3-19`; exactly one row, `.claude/commands/run-execution.md`, step `2.1.3`, anchor `^3\. [A-Za-z]+ all approved Answer Specs from`, payload `Answer Specs`, consumer `run-execution main session`, isolation `operand-path-ok`, target `path`. Searched `run-execution.md` for `all approved Answer Specs`: three lines carry it (26, 40, 53) and only line 40 opens with `3.`, so the anchor resolved once. The pre-change harness classified `H3-19` as `content` / `VIOLATION`, 39856 bytes / 560 lines. Step 2.1 item 6 relayed the same payload onward as "the Answer Spec content" to the `research-prompt-creator` sub-agent, so the read and the onward relay were one seam as the brief framed them. Nothing about the manifest's step, anchor, payload or consumer was stale.
- Claim (3): HOLDS — the exposed set is exactly `/preparation/answer-specs/{section}/`, the same section-scoped directory item 3 previously read in full, so path-passing exposes no additional file. The prerequisite gate at `run-execution.md` lines 11–17 reads every spec, runs `answer-spec-qc` in a fresh sub-agent, writes the verdict to `/preparation/answer-specs/{section}/answer-specs-qc.md`, and aborts unless ALL specs show APPROVED — so "all approved Answer Specs" is already established before Step 2.1 runs. Sub-agent read capability is established inside this same command: `H3-18` (Step 2.3.6) has the general-purpose sub-agent read a raw report by path, and `H4-04` (Step 2.1.6) already has *this* sub-agent read both project reference docs by PATH.
- Claim (4): HOLDS — searched `skills/research-prompt-creator/SKILL.md` (260 lines). Step 1 (line 67) instructs "Read the Execution Manifest, Research Plan, and Answer Specs"; the skill consumes component IDs (lines 99, 117), Required `Segment by:` dimensions (119), Expected Claims ranges (127), `min_high_sources` completion gates (135, 241), claim anchors (125) and evidence rules — all of which the complete spec set still supplies when read by the sub-agent. Searched the same file for an inline-only or verbatim requirement on the Answer Specs: none. The single verbatim requirement is the Evidence-First preamble (line 92), sourced from `reference/quality-standards.md`, which Step 2.1 already path-passes. `[MISMATCH]` (line 36), critical-gap halt (34), `[AMBIGUITY]` (216) and `[CONFLICT]` (219) live in the skill and were not touched.
- Claim (5): HOLDS — only the `H3-19` read-and-relay seam was converted. The Execution Manifest and Research Plan transports, both project reference paths (`H4-03`, `H4-04`), skill loading, session-grouping authority, output paths, the returned session-plan summary, the Step 2.1.7 checkpoint and the Step 2.1.8 compact are byte-identical. The production diff is 2 changed lines.
- Claim (6): HOLDS — searched `tests/s1-relay/README.md` for `H3-19`; the W4-H3 reconciliation table at line 61 did group it under "`run-execution` St2.3 (all raw reports)" alongside `H3-17`/`H3-18`, contradicting the manifest's `2.1.3` location. Corrected to its own St2.1 row and moved from pending to landed in the bulk-operand edit map. The Blast-radius paragraph was not touched.

Result: Unit 8 is implemented and committed at `37b25f6e2ee76b3b31c65db66e88f0e6e0f2a4e7`, which changes exactly two files — `workflows/research-workflow/.claude/commands/run-execution.md` (2 lines) and `workflows/research-workflow/tests/s1-relay/README.md` (14 lines) — and touches no manifest, harness executable or fixture. Step 2.1.3 now resolves the section's approved Answer Specs to project-root-relative paths only and does not read their contents into the main session; Step 2.1.6 hands those paths to the general-purpose sub-agent, which reads every approved spec for the section in full and component by component before applying `research-prompt-creator`.

Evidence:

- Pre-change target: `H3-19` resolved to the single row `.claude/commands/run-execution.md 2.1.3`, derived shape `content`, state `VIOLATION`, 39856 baseline bytes / 560 baseline lines, 39856 current bytes. Aggregate before conversion: baseline 1819370 bytes, current relayed 350009 bytes, reduction 80.7621%, compliant 21/40, 9 measured violations, 2 cap violations, 5 ambiguous, 0 unresolved.
- Post-change target: `H3-19` is `path` / `COMPLIANT`, current bytes 248 — 39608 bytes removed from the main-session relay. Aggregate after: current relayed 310401 bytes, reduction 82.9391%, compliant 22/40, 8 measured violations, 2 cap violations unchanged, 5 ambiguous unchanged, 0 unresolved. A row-level `diff` of the full 40-row TSV before and after shows exactly one changed line, `H3-19`; every previously accepted seam is unmoved, and the co-tenant seams on the edited Step 2.1 directive (`H4-03`, `H4-04`) both remain `path` / `COMPLIANT`.
- Falsifiability: a scratch copy of the workflow at `$SCRATCH/u8-revert` with `run-execution.md` restored from `HEAD` was run through the same harness via `--workflow`. `H3-19` returns to `content` / `VIOLATION` at 39856 bytes, while the control seams `H3-17` (the Step 2.3 raw-report path relay) and `H4-04` stay `COMPLIANT` — a row-level diff against the live run again shows exactly one moved row. No permanent machinery was added.
- Semantic evidence: Step 2.1.6 now states the sub-agent reads "every approved spec for the section in full and component by component, before applying the skill", matching the wording pattern already accepted for `H3-18`. The complete spec set therefore still reaches `research-prompt-creator`, so component-ID traceability, Required dimensions, Expected Claims volume calibration, claim anchors, evidence rules, completion gates and `min_high_sources` remain available; the skill's own `[MISMATCH]`, `[AMBIGUITY]`, `[CONFLICT]` and critical-gap halt behaviour is untouched because the skill was not edited. The prerequisite APPROVED gate still runs ahead of Step 2.1, so "all approved" is unchanged in meaning.
- Regression: `check-relay-payload.test.sh` reports 17 passed, 0 failed both before and after the README edit. T7 confirms the fixture regenerates byte-identical at 475687 bytes; caps stayed at 20 lines / 4 KB with the same 2 cap violations; T6b confirms 0 uncovered rows. The README now separates the Step 2.1 `H3-19` Answer Specs seam from the Step 2.3 `H3-17`/`H3-18` raw-report seams, and its "nothing pending in this group" claim was checked against the harness — `H3-04` is the only remaining W4-H3 measured violation.
- Repository: `git diff --check` clean; committed paths are the two files named above; `origin/main` remains an ancestor of `HEAD`; local commit `37b25f6e2ee76b3b31c65db66e88f0e6e0f2a4e7`. No push, no merge. This is an instruction-only unit, so no representative AI-authored run was produced.

Aggregate S1 position after Unit 8: Units 1–8 implemented (1–7 accepted, 8 awaiting assessment); 22/40 seams compliant; fixed-fixture reduction 82.9391%; 8 measured violations, 2 cap violations, 5 ambiguous seams and the representative chapter regression remain, so the aggregate 100%-coverage target stays red.

Deferral noticed during this unit, not implemented: the README's `Blast radius` paragraph wording carried over from Unit 7 remains deferred — it was outside this unit's `H3-19` mapping scope and the brief excludes it explicitly.

## Blocker

None.

## Next action

Codex: assess Unit 8 — the `H3-19` Step 2.1 Answer Specs operand conversion at `37b25f6e2ee76b3b31c65db66e88f0e6e0f2a4e7`, its falsifiability and semantic evidence, the bounded README mapping correction, and the updated aggregate S1 position. Then close, continue to the next S1 unit, or correct once.

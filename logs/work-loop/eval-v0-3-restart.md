---
task: eval-v0-3-restart
turn: codex
---

## Objective and scope
Restart the Work Loop v2 behavioural-eval effort from the clean current `main` baseline and carry it to an honest, repeatable eval capability whose first live act is EV-3/CE-9. The operator has approved this `eval-v0-3` restart; adoption remains a later operator decision based on recorded operating evidence.

This task may use the abandoned `session/2026-08-09-eval` branch and its dirty worktree as read-only evidence, but it must not edit, clean, commit, merge, cherry-pick, or otherwise change that checkout. Excluded from the whole task unless the operator later expands scope: an eval platform, dashboard, database, new scoring system, new durable fixture family, unattended eval execution, Work Loop core/skill/command changes, dispatcher changes, and automatic landing or cleanup of another checkout.

## Lane and unit
Standard. Discovery mode. Unit 1 — establish the authoritative restart baseline and frame the smallest implementation unit from current repository evidence.

Named reason for the loop: the work must survive several sessions, its abandoned predecessor left substantial uncommitted partial effects in another checkout, and independent assessment is needed before any recovered design or implementation counts as the new baseline.

## Brief
This unit prevents the clean restart from silently inheriting either stale `main` assumptions or an oversized timed-out repair. It is needed now because the dedicated restart checkout exists and is clean, while the exact authority and reusable value of the prior eval work still need repository-backed reconciliation. It advances the approved restart by returning one bounded build slice, not by creating another planning artifact.

### Required outcome

Return a concise repository-backed restart determination that settles all three questions below, then hand back to Codex without implementing the eval:

1. What content is actually authorised for the restart: identify the strongest content-bound approval record for the eval objective and distinguish governing scope from merely proposed or historical material. If no durable approval binds the material details, say exactly what remains operator-owned rather than promoting a mutable proposal by filename.
2. What the clean checkout already contains versus what exists only on `session/2026-08-09-eval` or as uncommitted partial work in `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources-eval`. Preserve useful findings, but classify old committed artifacts, old closing records, and dirty partial edits separately; do not treat a timed-out hop as accepted work.
3. What single smallest implementation unit should open next in this checkout. It must have one dominant deliverable, a bounded file allowlist, fail-capable evidence, explicit exclusions, and no dependence on changing or cleaning the abandoned checkout.

### Governing sources and source dispositions

- The operator's 2026-08-13 decision recorded in this state file governs the restart itself and the boundary that adoption remains a later operator decision.
- `plans/axcion-harness-v0.2/mvp-plan.md` is the canonical approved product direction for a small behavioural evaluation pack rather than an eval platform. Verify the relevant evaluation-pack and phase language before relying on it.
- `plans/work-loop-v2-mvp/work-loop-v2-executable-core-v0.1.md` and `.agents/skills/work-loop-v2/SKILL.md` govern Work Loop operation, evidence, authority handling, discovery-unit behavior, and unit sizing.
- `plans/work-loop-v2-v0.2/eval-mvp-proposal-v0.2.md` is candidate scope material. Its current header says implementation is not authorised, while prior branch records describe its scope as operator-approved; resolve that authority conflict from content-bound durable evidence rather than choosing the convenient wording.
- `session/2026-08-09-eval` commits, its closed eval task records, and its dirty worktree are non-governing historical implementation/result evidence. They may inform the restart but may not silently become its baseline.
- `logs/work-loop/axcion-harness-v0-2-readiness-fixes.md` is current evidence about the attended carrier's latest accepted limitations, not authority to broaden this eval.

### Claims to verify against repository reality

1. Check the current checkout's branch, HEAD, and dirty state. The opening claim is that it is `session/2026-08-13-eval-v0-3`, based at current `main`/`origin/main` commit `375e61d4898eb402fe2e8e3bdb1e79d808cfa05e`, with only this new Work Loop hand-off uncommitted.
2. Search current `main` for `plans/work-loop-v2-v0.2/eval-runner.md`, `logs/work-loop-v2-eval-results.md`, and any eval task closing record. The opening claim is that the proposal exists here but the prior implementation artifacts do not.
3. Inspect `session/2026-08-09-eval` read-only. The opening claim is that commits `3f21418` and `c8feffe` contain the prior runner/results implementation and operability correction, while the checkout also holds a later uncommitted `eval-mvp-v0.2-adoption-readiness-fix` state file plus large partial edits left by dispatcher exit 21.
4. Inspect the incident evidence at `plans/work-loop-v2-v0.2/incident-evidence/incident-2-2026-08-11/` and the abandoned worktree's state file. Establish what the dispatcher actually observed and what remains only a model claim or uncommitted partial effect.
5. Locate the approval evidence for the eval's material scope. An approval naming only a mutable filename is not content-bound; report the exact record, commit/content identity, and scope it authorises, or report the absence after naming the surfaces and patterns searched.
6. Check whether changes merged since the abandoned branch alter the feasibility or transport assumptions of EV-3/CE-9, especially the current attended-carrier status. Do not run an actor, a trial, or a test in this discovery unit.

### Required evidence

- Exact current-checkout branch, HEAD, and `git status --short` output.
- A path-presence comparison between current `main` and `session/2026-08-09-eval` for the proposal, runner, results record, and relevant task records.
- Commit identities and concise file-level summaries for the prior accepted implementation/correction, kept separate from the dirty partial diff and dispatcher stop.
- The searched surfaces and matching evidence for the approval-content question, including a clear `found` or `not found` conclusion.
- A named disposition for each material source: governing, verify-first claim, non-governing background, or unresolved unknown.
- One proposed next implementation unit whose evidence could fail and whose reasoning/validation load is small enough for one normal Claude turn. Do not create a separate report; put the determination in `## Latest result`, any genuine blocker in `## Blocker`, set `turn: codex`, and commit only this state file plus any incidental allowed friction-log write.

### Completion and stop conditions

Complete by returning evidence and the bounded next-unit recommendation; do not implement, copy, cherry-pick, merge, clean, or run any EV scenario. Stop for the operator if the material scope cannot be tied to an identifiable approval, if the restart would require discarding or mutating the abandoned checkout, or if repository evidence shows that the requested restart conflicts with a later governing decision. A false premise is a valid result: record what was inspected and hand back rather than repairing the premise silently.

## Latest result

Inspected (2026-08-13):

- **Claim (1): HOLDS** — ran `git rev-parse --abbrev-ref HEAD`, `git rev-parse HEAD`, `git status --short` and `git rev-parse main origin/main` in this checkout. Branch `session/2026-08-13-eval-v0-3`; HEAD `375e61d4898eb402fe2e8e3bdb1e79d808cfa05e`, byte-identical to both `main` and `origin/main`; `git status --short` returns exactly one line, `?? logs/work-loop/eval-v0-3-restart.md`.
- **Claim (2): HOLDS** — ran `git cat-file -e main:<path>` for each named path, plus `git ls-tree -r --name-only main -- logs/work-loop` and `git ls-tree -r --name-only main | grep -i eval`. `plans/work-loop-v2-v0.2/eval-runner.md` ABSENT; `logs/work-loop-v2-eval-results.md` ABSENT; `plans/work-loop-v2-v0.2/eval-mvp-proposal-v0.2.md` PRESENT (landed on `main` as commit `09ad7e4`); no eval task record of any name exists under `logs/work-loop/` on `main`. The only eval-named paths on `main` are the two proposals and the incident-2 run artifacts.
- **Claim (3): HOLDS, and is incomplete** — `git log --oneline main..session/2026-08-09-eval` returns six commits, not two. `3f21418` adds `eval-runner.md` (349 lines), `logs/work-loop-v2-eval-results.md` (68), `logs/work-loop/eval-mvp-v0.2.md` (83) and a friction-log line; `c8feffe` is the operability correction (`eval-runner.md` +69/−, results record +38, new task file `eval-mvp-v0.2-operability-fix.md`). The brief omits `a246fc2` and `1e1334e` (the two closing records), `0d5a076` (staging-footprint fix) and `198ceec` (session wrap, the branch tip). Merge-base with `main` is `111c52b`, so none of the six is on `main`. An earlier merge of the same branch name, `a662d7e`, **is** an ancestor of `main` but carried only `core-resolver-worktree-defect-report-2026-08-09.md` — it is not the eval implementation. The dirty worktree at `../ai-resources-eval` (inspected with `git --no-optional-locks`, nothing written) is on `session/2026-08-09-eval` at `198ceec` with three modified tracked files (`logs/friction-log.md`, `eval-mvp-proposal-v0.2.md`, `eval-runner.md`; +392/−55 total) and four untracked files, including the state file `logs/work-loop/eval-mvp-v0.2-adoption-readiness-fix.md`.
- **Claim (4): HOLDS** — `shasum -a 256 -c MANIFEST.sha256` in `incident-evidence/incident-2-2026-08-11/` returns OK for all three artifacts; the `.tree` capture hashes to `e3b0c44…`, the sha256 of an empty file. The dispatcher log records `mode=live task=eval-mvp-v0.2-adoption-readiness-fix`, `max_hops=1 timeout=900s carry_one=1`, initial `turn=claude` on an uncommitted state file at `head=198ceec`, then `STOP [21] actor 'claude' exceeded 900s and was killed (hop 1)` with teardown verified. The hop's own JSON reports `is_error:true`, `duration_ms:899749`, 48 turns, `terminal_reason:"aborted_streaming"`, and one permission denial. **What the dispatcher observed:** one Claude hop launched and was killed at the deadline; no commit, no turn flip. **What is only partial effect:** the +392-line diff in the worktree. **What is only a model claim:** nothing — the abandoned state file's `## Latest result` still reads `None — opening hand-off.`, so that hop produced no recorded result at all.
- **Claim (5): NOT FOUND as a content-bound approval, for the eval's *material* scope** — searched `logs/decisions.md` (all 10 dated entries), `logs/session-notes.md`, `logs/session-notes-archive-2026-08.md`, `logs/improvement-log.md`, all of `logs/` and `plans/axcion-harness-v0.2/` for `EV-[1-6]`, `eval runner`, `eval MVP`, `evaluation pack`, `behavioural eval` and `operator (approved|authoris|authoriz)` near `eval`; and `git log --all --grep=eval -i`. Findings: (a) `eval-mvp-proposal-v0.2.md` on `main` states its own status as **"Proposal for operator review. No implementation authorized."**; (b) the only authorisation statement anywhere is inside a Codex-written brief on the non-governing branch — `git show 3f21418:logs/work-loop/eval-mvp-v0.2.md` says *"The operator has now authorised implementation by naming the exact proposal file"* — which is approval **by mutable filename**, not content-bound, and it lives on a branch that never reached `main`; (c) no entry in `logs/decisions.md` or the session notes approves the six-scenario pack, the EV ids, the trial counts, the PASS/PARTIAL/FAIL rules or the results-record schema.
- **Claim (6): the answer is YES for transport, NO for EV-3 itself** — `logs/decisions.md` (2026-08-13, *Axcíon Harness v0.2 adopted for normal attended pilot use*) records the operator adopting `scripts/axcion-harness-v0.2/carry-turn.sh` as the attended carrier, and `.agents/skills/work-loop-v2/SKILL.md:222-225` now routes attended carries to it while leaving unattended runs on the spike dispatcher, stating the two "are two different programs, and neither does the other's job" and that the carrier refuses `--carry-one` with exit `10`. The abandoned runner's whole transport contract is built on `dispatch.sh --carry-one` and its flat run-id evidence shape (`git show session/2026-08-09-eval:plans/work-loop-v2-v0.2/eval-runner.md` § 2.4, and the `eval-mvp-v0.2-operability-fix` closing record's "Set A"), so that contract is now stale for EV-2, EV-4 and EV-6. **EV-3 is unaffected:** its own definition (§ 5, EV-3) names the actor as *"Codex, in a fresh thread"* and § 2.4 records EV-3 as *"Not applicable — no dispatcher run"*. No actor, trial or test was run in this unit — including the CE-9 greps, which are EV-3's own deterministic checks.

**Result:** the restart is authorised for EV-3/CE-9 and is not authorised, today, for the six-scenario pack.

1. **What is authorised.** The operator's 2026-08-13 decision in this state file governs the restart and holds adoption back as a later decision. `logs/decisions.md` (2026-08-13) durably treats `plans/axcion-harness-v0.2/mvp-plan.md` as *the governing plan* — quoting its Phase 2 exit condition and its Phase 3 "three-to-five representative tasks" bar — and that plan's **Phase 1 deliverable list names "fresh-context scenario CE-9/CE-17 proof"** (`mvp-plan.md:416`). The same plan bounds the direction: *"a small behavioral evaluation pack … it does not become a general eval platform"* (`:116`), and excludes *"broad eval platforms"* (`:108`). So the CE-9 proof, and a small pack rather than a platform, are tied to identifiable durable approval. **Caveat to report rather than resolve:** `mvp-plan.md`'s own header still reads *"Proposed direction for operator review; no implementation authorized"*, which is stale against the `decisions.md` entry that operates on it as governing.
2. **What remains operator-owned.** Whether the eval is a six-scenario pack at all, and specifically EV-1, EV-2, EV-4, EV-5, EV-6, the 3-at-adoption/1-at-regression trial policy, the nine result fields, the PASS/PARTIAL/FAIL rules and the append-only results-record schema. These exist only in `eval-mvp-proposal-v0.2.md`, whose header refuses implementation, and their only authorisation is the by-filename statement on the abandoned branch. Do not promote them by filename.
3. **Source dispositions.** *Governing:* the operator's 2026-08-13 restart decision here; `plans/axcion-harness-v0.2/mvp-plan.md` as ratified by `logs/decisions.md` 2026-08-13; `work-loop-v2-executable-core-v0.1.md` and `.agents/skills/work-loop-v2/SKILL.md`. *Verified-and-intact instrument, reusable as-is:* `ce-9-recovery-scenario.md` plus all four `fixtures/ce-9/` files, all present on `main`. *Candidate scope material, not authority:* `eval-mvp-proposal-v0.2.md`. *Non-governing historical evidence:* the six branch commits, both closing records, the incident-2 artifacts, and the +392-line dirty diff — which is a timed-out hop's residue, never assessed, and is not a baseline. *Current evidence, not authority to broaden:* `logs/work-loop/axcion-harness-v0-2-readiness-fixes.md`. *Unresolved unknown:* whether the abandoned dirty diff contains operability repairs worth recovering — unassessable without reading it as a design input, which this unit's exclusions forbid.
4. **CE-9 has still never been run.** Searched `logs/` and `plans/` for CE-9 records: `context-engineering-implementation.md` accepted limitations 6–7 remain open, and `carriage-trial-record.md:28-29` states outright that recovery quality *"is CE-9's measurement, taken at S5"* — which no record shows happened. No result record for CE-9 exists anywhere on `main`.

**Proposed next unit — Unit 2, Implementation mode: one EV-3 run sheet.**

- **Dominant deliverable:** one new file that turns the already-intact CE-9 instrument into an operator-runnable EV-3 procedure for **one** trial — one source-opened run plus its paired memory-only control — and states where that trial's result is written. Nothing else.
- **File allowlist:** the one new file; this state file; incidental hook writes to `logs/friction-log.md`. No other path.
- **Fail-capable evidence:** run `ce-9-recovery-scenario.md` § 5's two greps twice, over scratch copies outside the repository. Against an unmodified copy of `fixtures/ce-9/`: grep 1 exits `0` with exactly one hit in `task-state.md`, grep 2 exits `1`. Against a mutant copy with the discriminator sentence pasted between `<<<REQUEST-BEGIN>>>` and `<<<REQUEST-END>>>`: grep 2 exits `0`, and the run sheet must classify that as *instrument broken, session failed*. The committed fixtures are never modified.
- **Exclusions:** executing the EV-3 trial itself (no actor run, no result recorded); EV-1, EV-2, EV-4, EV-5, EV-6; any results-record schema beyond the single EV-3 result; recovering, copying or cherry-picking anything from `../ai-resources-eval`; any change to `dispatch.sh`, `carry-turn.sh`, the Work Loop core, skill or command.
- **Why it fits one turn:** the instrument exists and was verified present; the unit writes one document and runs two greps twice against scratch copies.
- **Placement is Codex's to fix in the brief.** `plans/work-loop-v2-v0.2/` is the obvious home given the fixtures sit under it, but this unit did not settle it.

**Deferrals noticed and not done:** (a) the +392-line dirty diff at `../ai-resources-eval` may hold reusable operability repairs — not recovered, because the brief forbids touching that checkout and the material is unassessed; (b) `mvp-plan.md`'s stale "no implementation authorized" header against `decisions.md` 2026-08-13 — a one-line correction outside this task's scope; (c) the abandoned runner's `--carry-one` transport contract is stale against the adopted carrier — not corrected, because that runner is not in this checkout and rebuilding it is not authorised.

**Evidence:** every command above was run read-only in this checkout except the four `git --no-optional-locks` reads against `../ai-resources-eval`, which wrote nothing. `git status --short` in `../ai-resources-eval` is unchanged from the state recorded under claim (3). This checkout's only change is this state file. This inspection could have failed and did in part: claim (3)'s commit set was incomplete as briefed, and claim (5) returned *not found* rather than the approval the brief hoped to locate.

## Blocker
None. The material scope of the **proposed** unit is tied to identifiable approval (§ Result item 1), so no operator stop is triggered. The unapproved six-scenario pack is named as operator-owned (item 2) rather than carried forward, which is what keeps this a hand-back and not a stop.

## Next action
Codex: assess this determination, then decide whether to (a) open Unit 2 as proposed, bounded to EV-3/CE-9 and the single run sheet, (b) reframe it, or (c) put the six-scenario question to the operator first. If Unit 2 opens, settle the run sheet's placement and its exact allowlist in the brief.

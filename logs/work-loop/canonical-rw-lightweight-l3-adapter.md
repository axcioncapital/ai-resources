---
task: canonical-rw-lightweight-l3-adapter
status: active
turn: codex
---

## Objective and scope

Complete L3 by binding the lightweight Research Workflow's Standard-route House View adapter to
L2's accepted stable authority contract. The adapter must preserve the existing Light, Standard and
Deep behavior, evidence standards and one-way escalation while ensuring that Standard can rely only
on a contract-valid approved Unit Judgment Brief and actually consumes its content with thesis-level
traceability.

Scope is the shared `/research-route` entry behavior, its Standard memo control, and focused L3
regression tests. Excluded: changes to L2's artifact, contract, validators, promotion helper,
producer, challenge or founder decision mechanism; copying L2 surfaces into this branch; creating a
second House View or approval path; L4's manual consumer installation and integrated operating proof;
retrieval runtimes, APIs, source profiles, propagation infrastructure, official-statistics ingestion,
Content Programme integration, broad rollout, merge and push.

## Lane and unit

Standard. Implementation mode. Unit 1 — bind Standard to approved judgment authority.

Named reason for the loop: this unit crosses the L2/L3 authority boundary, so its scope must stay
bounded and its fail-closed evidence must be assessed independently of the executor before the
adapter counts as complete.

## Brief

L3's core was accepted and deliberately stopped at the House View seam; L2 has now closed PASS/ADOPT
at `e16cf206`, publishing the stable contract the approved plan requires before this adapter may be
built. This unit completes only that withheld L3 seam, leaving L4's manual integration and genuine
end-to-end operation for the separate next outcome.

**Governing authority and source disposition**

- The governing plan is
  `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, whose
  material content is bound to approved commit `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`.
- L2's accepted dependency is commit `e16cf206`, specifically its closed record
  `logs/work-loop/canonical-rw-l2-house-view.md` and published stable interface
  `workflows/research-workflow/docs/judgment-authority-contract.md`. Read both from that exact commit;
  the contract governs the artifact suffixes and states, validation command and exit codes, and the
  requirement to consume approved content with thesis traceability. L2 implementation details do not
  govern L3.
- L3's authoritative prior state is the closed
  `logs/work-loop/canonical-rw-lightweight-l3.md`: core behavior and proof are accepted, and the
  adapter is its sole intentional withheld capability.
- Material reclassification: the current `/research-route` text and checker correctly say the L2
  contract was unpublished when they were written. That premise is now superseded by the accepted L2
  record at `e16cf206`; the old closed seam is the implementation target, not continuing authority.
- Verify-first repository claim: this isolated L3 branch does not itself contain L2's accepted
  contract or helper files. Check the current branch and the exact L2 commit before acting. That
  absence must not be repaired by copying, cherry-picking, merging or editing L2-owned surfaces in
  this unit.

**Required outcome**

Implement the smallest adapter that satisfies all of these observable behaviors:

1. A Standard assignment that does not require founder-authorized governing judgment keeps the
   accepted evidence-controlled memo behavior unchanged, including its evidence/inference separation
   and existing escalation rules.
2. When a Standard assignment genuinely requires an authorized thesis rather than a comparison of
   interpretations, the route uses L2's published consumption interface. The trigger must cover the
   existing `thesis_judgment=yes` signal when the final route remains Standard and the same need when
   it becomes apparent during Standard work; do not broaden ordinary analysis into judgment.
3. The adapter resolves one unit and base path without defining a new artifact shape. It may rely only
   on `{base}-approved.md` after L2's `check-judgment-contract.sh` returns exit `0`. Missing helpers,
   missing authority, proposed or rejected state, absent claim IDs, malformed authority and bad usage
   all fail closed. Branch on the contract's exit codes, not prose.
4. On valid authority, the Standard author receives the approved brief's content, not merely its path
   or existence, and every consequential memo assertion governed by it is traceable to the thesis it
   serves. Existing claim-level evidence permissions still bind; authority must not raise an evidence
   class or license unsupported factual language.
5. When valid approved authority is unavailable, Standard does not create, approve or simulate it and
   does not complete the judgment-bearing memo. It escalates one way to the existing Deep handoff,
   naming the authority failure sufficiently for the next route to act.
6. Proposed and rejected briefs never become authority; approved content is not authored or modified
   by the adapter; no second producer, challenge, review ledger, approval prompt, promotion path,
   House View artifact or judgment state is introduced.
7. The current blanket rejection of the reserved House View term is replaced only as far as needed to
   permit contract-valid, thesis-traced consumption. It must remain impossible for an ordinary
   Standard memo to assert or discuss governing House View authority without that binding.

The mechanism and exact file arrangement are Claude's technical judgment inside the allowed L3
surfaces. Prefer a contract-bound adapter over reproducing L2 rules. If the isolated-branch boundary
makes a real binding impossible without importing an L2-owned surface, hand back that verified fact
and the smallest integration prerequisite instead of stubbing or duplicating the contract.

**Required evidence**

- Before-change failing evidence for the withheld seam: show that the current entry/checker has no
  executable valid-authority path and still rejects House View categorically.
- Focused fail-capable tests proving: ordinary Standard is unchanged; the authorized-thesis trigger
  enters the adapter; a valid approved authority can be consumed; content/thesis traceability is
  required; missing helper and validator exits for missing, not-approved/proposed-or-rejected,
  evidence-less and malformed authority fail closed to Deep; an unbound House View assertion is
  rejected; and the adapter never writes or promotes approved authority.
- Run the existing L3 Unit 1 and Unit 2 harnesses and report exact totals. Preserve their routing,
  memo-control and one-way-escalation guarantees, adjusting only assertions whose deliberately closed
  seam is superseded by this unit.
- Syntax-check every changed shell surface. Report unavailable checks honestly rather than replacing
  them with non-failing evidence.
- Inspect the final diff for scope and report the exact files changed and the implementation commit.

**Claims to check before implementation**

1. At exact commit `e16cf206`, the L2 closing record is `CLOSED` and says PASS/ADOPT, and the stable
   contract exists at the path named above. Inspect those exact objects; do not infer acceptance from
   the operator message or a branch name.
2. The accepted contract's consumption interface validates only `{base}-approved.md` using
   `check-judgment-contract.sh`, exit `0` is the sole valid-authority result, and consumers must pass
   approved content onward with thesis traceability. Inspect the exact contract sections.
3. On this branch, the current Standard entry and memo checker still carry the pre-L2 closed seam,
   and no L2 contract/helper surface is present. Inspect `.claude/commands/research-route.md`,
   `logs/scripts/research-route-memo-check.sh`, and the current `workflows/research-workflow/` tree.
4. The current classifier already exposes the `thesis_judgment` signal and preserves Standard for a
   bounded internal thesis judgment while escalating external thesis judgment to Deep. Prove this
   from the entry rules and existing invocation-path tests before changing behavior.

Capability subset: baseline only — local reads and history inspection; local tests and syntax checks;
edits limited to `.claude/commands/research-route.md`, L3-owned `logs/scripts/research-route*` helpers
and tests, and this task state; reversible local refactoring; and local commits by Claude. Nothing is
selected from the pre-authorizable set, which is empty. No operator-reserved capability is needed.
No network, push, merge, cherry-pick, deployment, credential access, destructive action, policy
change, nested model invocation or edit to an L2-owned surface is authorized.

Completion condition: the adapter behaviors above are implemented and committed on this branch; the
focused fail-capable evidence and both existing L3 harnesses pass; no excluded surface changed; and
the result, exact commands/totals, changed paths and commit are handed back with `status: active` and
`turn: codex` for assessment.

Stop and hand back if any checked premise is false, the accepted interface is materially ambiguous,
the required binding cannot be implemented without importing or changing L2, the evidence cannot
distinguish content consumption from file existence, or the work would enter L4 or another excluded
surface. Preserve the user's unrelated modifications to `logs/friction-log.md` and
`logs/innovation-registry.md`; do not include them in this unit's commit.

## Latest result

Correction round — the two frozen findings, both reproduced before being corrected.

**Finding 1 reproduced.** In `logs/scripts/research-route-memo-check.sh`, the Inference loop
required only `^- \[INFERENCE\].*C[0-9]+` and then checked each `C` reference against the declared
claims. No thesis was required under bound authority, and the accepted E3 fixture demonstrated the
gap directly: `- [INFERENCE] A later intake-side gate would therefore be redundant (rests on C1).`
passed inside a contract-valid bound memo.

**Finding 2 reproduced, by inspection and then live.** The adapter numbered theses by encounter
order (`n=$((n + 1))`), and the memo checker validated a range (`${ref#T}` between 1 and
`authority_theses`). L2's accepted validator counts `grep -cE '^### Thesis '` and enforces three to
five headings; searched its source for any sequence or uniqueness rule and found none. A brief
headed `### Thesis 2 / 3 / 4` was then built and run through L2's real validator from `e16cf206`:
exit `0`, contract-valid. The adapter returned `thesis: T1 / T2 / T3` for it — so a memo tracing to
the approved brief's Thesis 4 would have written `T3`, and a memo writing `T4` would have been
refused as out of range.

Result: both findings are resolved.

- **Finding 1.** Under bound authority each declared `## Inference` item now keeps its claim-ID
  evidence binding *and* must name at least one valid thesis. Any thesis it cites is checked the
  same way an Answer reference is: refused without a binding, refused if the approved brief does not
  carry it. Unbound inference behavior is untouched, and `- None.` still declares no inference and
  needs no thesis. The entry's memo template and its traceability rule now state this for Inference
  as well as Answer.
- **Finding 2.** The adapter takes each thesis ID from the number the approved heading actually
  carries, normalises it (`10#`), and publishes the permitted set as a new `thesis-ids:` line.
  A duplicated number fails closed as ambiguous; so does a heading whose number cannot be read.
  The memo checker validates every thesis reference by membership in that returned set, in both the
  Answer and the Inference, and no longer computes a range. The entry tells the author to use the
  returned IDs exactly, with the Thesis 2/3/4 case named.

Evidence:

- Focused harness `logs/scripts/research-route-l3-adapter-unit-1.test.sh`: **41 passed, 0 failed**
  (35/35 before this correction; six assertions added). New: D18 drives a contract-valid brief headed
  Thesis 2, 3, 4 through L2's real validator and requires `thesis-ids: T2 T3 T4`, `thesis: T4 — …`
  and the *absence* of `T1`; D19 requires a duplicated number to fail closed; E15 requires a bound
  memo's inference to name a thesis; E16 refuses an inference citing `T9`; E17 refuses `T1` against
  the Thesis 2/3/4 brief; E18 accepts `T4` against it, in both Answer and Inference.
- The six new assertions were mutation-tested rather than asserted to be fail-capable. Mutating the
  adapter to publish a hardcoded `T1 T2 T3` set drops the harness to **38/41** (D18, E17, E18 fail).
  Mutating `thesis_known()` to accept every ID drops it to **38/41** (E5, E16, E17 fail). Mutating
  the inference requirement from `-lt 1` to `-lt 0` drops it to **40/41** (E15 fails). Each mutation
  was reverted from a backup and the restored harness returns to 41/41; `git diff --stat` over the
  two scripts confirms only the correction remains.
- Both existing L3 harnesses still pass unaltered: `research-route-l3-unit-1.test.sh` **14 passed,
  0 failed**; `research-route-l3-unit-2.test.sh` **33 passed, 0 failed**. No assertion in either was
  adjusted in this round.
- `bash -n` clean on the adapter, the memo checker and the focused harness.
- Diff scope unchanged from the unit: `.claude/commands/research-route.md`,
  `logs/scripts/research-route-judgment-authority.sh`,
  `logs/scripts/research-route-memo-check.sh`,
  `logs/scripts/research-route-l3-adapter-unit-1.test.sh`, and this state file. Nothing under
  `workflows/`, no L2 surface touched. The operator's unrelated modifications to
  `logs/friction-log.md` and `logs/innovation-registry.md` remain uncommitted.
- Implementation commit for the unit: `550f47e89c1b2b3f9d0a82efd3d87a5ff5d650e1`. Correction commit:
  recorded in the follow-on commit below, since a commit cannot carry its own hash.

One boundary worth naming inside finding 2: the adapter now refuses an approved brief whose thesis
heading carries no readable number, which L2's validator would accept. That is a deliberate
fail-closed narrowing — an ID that cannot be read cannot be referenced — and it escalates to Deep
like any other unusable authority rather than passing something untraceable through.

Deferrals — recorded, not done, and not widened into this correction:

- **Newly noticed this round: thesis references are matched anywhere on a memo line.** The checker
  scans a whole Answer or Inference line for `T[0-9]+`, so prose that happens to contain `T4` would
  read as a thesis reference. Narrow and low-consequence; tightening it to the bracketed reference
  list plus an explicit `serves T<n>` form is a separate change.
- **The gate keys on the literal reserved term.** A memo could assert a governing view in other
  words and never trip it. Pre-existing shape of the rule; widening it to intent detection is a much
  larger mechanism.
- **No content binding between the memo and the brief it read.** The block names the approved
  artifact; nothing binds the memo's text to those exact bytes the way L2's `reviews_sha256:` binds a
  challenge. Real operating evidence should decide whether that is worth the cost.
- **The harness's real-validator fixtures depend on `e16cf206` staying reachable.** If it ever is
  not, D1–D6 and D18–D19 degrade to exit-code stubs and print a `NOTE`. Reported rather than silent,
  but a weaker proof when it fires.

## Blocker

None.

## Next action

Codex: run the closure check on the frozen findings only — are findings 1 and 2 resolved, and did
the correction break the focused adapter harness or either existing L3 harness? The answers on
record: finding 1 resolved (bound inference now requires a valid thesis, mutation-proved), finding 2
resolved (thesis IDs come from the approved headings, duplicates fail closed, references validated by
set membership, mutation-proved), and nothing broke — 41/41 focused, 14/0 and 33/0 existing, both
unaltered.

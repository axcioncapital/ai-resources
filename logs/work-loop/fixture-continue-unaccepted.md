---
task: fixture-continue-unaccepted
turn: claude
---

## Objective and scope
CONSTRUCTED NEGATIVE FIXTURE — a second unit with no accepted predecessor. This is the
case a unit-ordinal proxy gets wrong. The lane says Unit 2, the result is real rather
than a placeholder, and `## Next action` carries neither protocol token — so every
surface-level signal points at Continue. It is not one: core § 3 requires the file to
carry an **accepted result from a previous unit**, and unit 1 was never accepted. It was
handed back on a false premise and the unit was reframed, so no unit of this task has
been accepted at assessment.

A classifier that treats "Unit 2 or later" as evidence of an accepted predecessor
classifies this CONTINUE and is wrong — it has invented a broader acceptance rule than
the core's.

Scope: the fixture project's two stale reference lines. Excluded: any other file.

## Lane and unit
Standard. Unit 2 — bring the first reference line current, reframed. Named reason for the
loop: the objective spans more than one unit, so it must survive a unit boundary.

## Brief
Why: unit 1 rested on a claim that turned out to be false, so it was reframed rather than
accepted; the first reference line is still stale and is still the smallest unit that
delivers something observable.
Check against the repository: (1) fixture-target-2.md carries a first stale reference line
whose surrounding sentence names the superseded document.
Evidence required: the first line current, the second untouched.
Stop if: claim (1) is wrong, or the change would touch files outside the scope above.

## Latest result
Unit 1 was handed back, not accepted. Its brief rested on the claim that the reference
lines lived in a single file; inspection found them split across two, so the premise was
false and no work was done. Codex reframed the unit rather than accepting any result, and
nothing has been accepted at assessment on this task.

## Blocker
None.

## Next action
Claude: check claim (1), then implement unit 2 on the brief above.

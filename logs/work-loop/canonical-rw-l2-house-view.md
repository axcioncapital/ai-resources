---
task: canonical-rw-l2-house-view
status: active
turn: codex
---

## Objective and scope

Deliver L2 of the approved lean Research Workflow plan: one canonical Unit Judgment Brief / House
View capability in which evidence, interpretation and Axcíon context remain distinct; independent
challenge precedes founder decision; only explicit approval promotes reviewed content; and approved
authority governs the existing consequential analysis and writing path.

Scope is L2 only: the canonical artifact contract, producer, validator/promotion helper, independent
judgment challenge, and authority checks on existing analysis/synthesis/report/QC owners. Excluded:
L3 lightweight routing, L4 consumer installation, generic rollout or propagation, a second House View
artifact, duplicate approval machinery, a new stage or top-level command, retrieval work, Content
Programme integration, merge, push, and deployment.

## Lane and unit

Standard. Implementation mode. Unit 3 — make approved judgment govern downstream owners.

Named reason for the loop: L2 spans multiple bounded implementation units, canonical edits may take
live effect through symlinked consumers, and the authority boundary needs independent evidence before
downstream owners may rely on it.

## Brief

Units 1 and 2 established the authority contract and the producer/reviewer/founder-decision seam. This
unit closes the remaining implementation gap: every existing consequential analysis, synthesis,
architecture, prose and relevant QC owner must validate and consume the approved brief rather than
merely running after it exists. The representative semantic proof remains a separate final L2 unit.

**Governing authority and current position**

- The approved lean plan is
  `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, with
  material content bound to `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`. L2's outcome, boundary and
  deterministic/representative proof split govern.
- L1 closed PASS at `70030feb`; its burden constraints remain binding.
- Unit 1 is accepted at `4a2a0b965ff25dbd9260aeb2bac8bb3ff88949d7`. Its stable consumption
  interface is `workflows/research-workflow/docs/judgment-authority-contract.md` § 6.
- Unit 2 and its frozen correction are accepted at `b543fb2ce19ee62e64bd382ef5299c547d98fc03`
  and `06c90e66b46b85fdc092d56906318590ea61623d`. The deep route now reaches Step 4 only through
  a contract-valid approved brief.
- Current commands, skills, agents and QC paths are verify-first repository background. Their existing
  ownership boundaries should be preserved where compatible, but none may treat sequence after the
  Step 3.5 gate as a substitute for consuming approved judgment.

**Codex framing decisions**

- This unit has one invariant across several owners: approved analytical authority must be validated,
  passed by path, used in the owner's instructions, and checked for faithful implementation. Split only
  if repository inspection proves that one named owner is an independently valuable deliverable with a
  materially different mechanism; do not split merely by file count.
- Resolve the exact consumer set from the live workflow. The plan names semantic owners, not guessed
  files: analysis/section directives, synthesis, report architecture, report prose and relevant
  independent QC. Do not add a new owner to make integration easier.
- Representative semantic scoring is held for the next unit because this unit implements deterministic
  wiring. It is not waived or folded into self-review.

**Required outcome**

Implement the minimum downstream authority wiring so that:

1. every existing independently invokable owner of consequential analysis, synthesis, report
   architecture or report prose refuses to run for the section without a contract-valid
   `{base}-approved.md`;
2. each owner receives the approved brief by path and is explicitly required to use its theses,
   verdict, countercases and change conditions when shaping its own output — existence checks alone do
   not count as consumption;
3. downstream outputs preserve traceability from their consequential claims or structural choices to
   the approved thesis they implement, using the smallest compatible existing artifact or field rather
   than a new registry;
4. the relevant existing independent QC surface receives the approved brief separately from the
   producer's account and checks both faithful implementation and drift beyond evidence permissions or
   the approved view; and
5. proposed, rejected, missing, malformed or stale stand-ins cannot enter through a direct command,
   sub-agent owner or resumed stage path.

The approved brief governs downstream work; it does not replace evidence-permission tables, scarcity
instructions, gate-clearance caveats, operator decisions or existing editorial controls. Where two
authorities genuinely conflict, halt and surface the conflict rather than silently choosing one.

**Check against repository reality before acting**

1. Verify the checkout, branch, HEAD, relevant cleanliness and repository-depth sole-writer condition.
   Preserve and exclude the two pre-existing modified session logs.
2. Enumerate live-effect consumers for every canonical command, skill, agent, template, script or
   reference surface that may change. Name searched roots, resolved symlink targets and regular-file
   copies. Stop if an edit would take live effect in an unverifiable deployed project.
3. Trace the actual deep route from `run-analysis` Step 3.5e through section directives, synthesis,
   report architecture, chapter/report prose and independent content/QC checks. For each semantic owner
   named by the approved plan, identify the real command/skill entry points and whether they can be
   invoked directly or resumed independently. Bound the inventory to those surfaces.
4. Search those owners for current Unit Judgment Brief validation, input passing, thesis use and output
   traceability. For every absence claim, name the searched file/surface and pattern. Do not assume the
   Step 3.5 gate protects a later command that can be invoked directly.
5. Identify the existing path-passing and invocation-test conventions relevant to these seams. Pass the
   approved artifact by path; do not reintroduce large content relays or copy its prose into command
   bodies.
6. Identify an existing independent QC owner whose actual tool contract can perform the required check
   and return or write its result through the established route. Do not inherit or silently repair the
   recorded `run-analysis` Step 5c `qc-gate` write/tool mismatch unless that exact owner is load-bearing
   for this outcome; if it is, hand back the collision before combining repair and integration.
7. Identify existing downstream trace fields, checkpoints or artifact conventions that can carry thesis
   linkage without a new artifact family. If none is compatible, hand back the evidence rather than
   inventing a registry.
8. Verify that the accepted judgment contract and Unit 2 base-path convention are sufficient for every
   required owner. A real incompatibility is a false premise to hand back, not permission to reopen the
   contract silently.

**Required evidence**

- A pre-change failing case against the real downstream route showing at least one owner can produce
  consequential work while ignoring approved judgment. A pure keyword absence is insufficient: the
  check must distinguish validation, path consumption and trace instructions.
- A bounded consumer inventory mapping each plan-required semantic owner to its real entry point,
  validation gate, path handoff, required use and relevant QC check. It is evidence in this handback or
  an existing test fixture, not a new governance artifact.
- Fail-capable invocation-path fixtures proving every required direct/resume entry refuses missing,
  proposed, rejected and structurally invalid authority, and that removing any owner's approved-brief
  path handoff or thesis-use/trace instruction makes the fixture fail.
- A positive fixture proving one minimal valid approved brief reaches each required owner by path and
  that the owner is instructed to shape its output from the approved content, not merely check the
  filename. Use targeted mutations or paired fixtures so an always-refusing or existence-only gate
  cannot pass for free.
- Fail-capable coverage showing the relevant independent QC receives the approved brief without the
  downstream producer's summary and is required to flag authority drift and evidence-permission
  overreach.
- The Unit 1 contract suites, Unit 2 judgment-seam suites and relevant existing Research Workflow
  regressions remain green without weakened assertions.
- Exact commands, assertion totals, exit codes, changed-surface inventory and live-consumer result.
- Implementation and task-state handback committed by Claude.

**Boundaries and stop conditions**

- Do not alter the accepted artifact, lifecycle, challenge, promotion or founder-decision contract.
- Do not create a new workflow stage, top-level command, downstream authority artifact, approval marker,
  registry, rollout system or generic deployment mechanism.
- Do not run the representative semantic proof in this unit. It remains the final L2 acceptance unit and
  must use a fresh-context reviewer rather than the implementer judging its own specimen.
- Do not modify the Sector trial checkout, L3, L4, Content Programme, retrieval or generic sync/deploy
  surfaces.
- Do not redesign existing editorial decisions, evidence permissions, scarcity handling, prose
  production or QC beyond the minimum inputs and checks required to make approved judgment govern them.
- The Unit 2 deferrals remain outside this unit unless directly load-bearing: S1 relay-manifest coverage
  for the new producer relays; the Step 5c `qc-gate` write/tool mismatch; the missing optional
  `judgment-context-card` template. Record them at closure with reasons; do not start adjacent repairs.
- Record newly noticed non-blocking improvements as deferrals. Stop on a false premise, scope collision,
  authority conflict or required evidence that cannot be made fail-capable.

Capability subset: baseline only — local reads and history inspection, edits and tests within the
resolved downstream analysis/synthesis/report/QC authority surfaces, this task-state handoff, and local
commits by Claude. Nothing is selected from the pre-authorizable set, which is empty today. No network,
push, merge, deployment, credentials, destructive shared-state action, policy weakening or other
operator-reserved capability is authorized.

Completion condition: every plan-required existing downstream owner validates and consumes the approved
brief by path, consequential outputs carry compatible thesis traceability, the relevant independent QC
checks authority fidelity and evidence-permission drift, every required negative and positive fixture
passes, existing regressions remain green, live-effect consumers are accounted for, and Claude commits
the implementation and task-state handback with `turn: codex`. A false premise or boundary collision
hands back without expanding scope.

## Latest result

Reproduced (2026-08-19), by inspection before correcting:

- Frozen finding (1): REPRODUCES. Searched all three Stage 5 command bodies for `judgment`:
  `produce-prose-draft.md` **0**, `produce-formatting.md` **0**, `produce-jargon-gloss.md` **0**. Both
  named commands are independently invokable and run after every Stage-4 control:
  `/produce-prose-draft` report-mode Phase 2 runs `prose-refinement-writer` to "develop underdeveloped
  hardest claims" and explicitly skips Phases 3 and 4, its section-mode QC; `/produce-formatting`
  Phase 2 changes H3 and structural presentation and Phase 3 runs the merged formatting +
  `document-integration-qc` that signs the report off. Unit 3's claim that every required downstream
  owner is governed was incomplete: it stopped at Stage 4.

Result: the report-mode Stage 5 seam is now governed on the same terms as Stage 3-4, and section mode
is explicitly untouched. Five changes, all inside the frozen finding:

- **`/produce-prose-draft` Phase 0b (new), report-mode only.** Validates `{base}-approved.md` with the
  contract helper and halts on any nonzero exit, with the same four refusal classes, the same
  authority-conflict rule and the same helper-cannot-run clause Unit 3 uses. Section-mode skips the
  phase entirely and acquires no judgment prerequisite.
- **`/produce-prose-draft` Phase 2, report-mode producer.** Receives `{base}-approved.md` by path and
  must develop hardest claims without moving them beyond the approved theses, sharpening past the
  provisional verdict, or exceeding the permission class the thesis rests on; countercases keep their
  weight, change conditions stay stated, and every existing `Thesis N` trace is preserved. Where
  developing a claim would require exceeding the approved view, it is flagged rather than developed.
- **`/produce-formatting` Phase 0b (new), report-mode only.** Same gate, same exemption.
- **`/produce-formatting` Phase 2.** Receives the brief by path; H3 and section framing may not promote
  a subordinate point above the verdict, bury a countercase behind a heading that changes its weight,
  or move a claim away from its trace.
- **`/produce-formatting` Phase 3 — new STAGE 3, House View fidelity.** The existing final
  editorial-integration QC gains a third stage checking fidelity, thesis trace continuity and drift
  (a claim the approved argument does not carry, a sharpened verdict, a dropped countercase, a lost
  change condition, permission overreach). It judges against the approved brief and the formatted prose
  **only**: the Phase 2 change log and the Stage 1 fixes-applied log are the producers' accounts and may
  explain a finding but never satisfy the check, because a producer that dropped a trace will not report
  having dropped it. Drift and overreach are FAIL findings.

**`/produce-jargon-gloss` — concrete exclusion, not an omission.** Inspected the `jargon-gloss` skill
rather than the command. Its hard constraint forbids changing argument structure, analytical
conclusions or voice; Check 2 (analytical-claim protection) refuses to apply a gloss that would alter
an analytical claim and flags it instead; and its output checklist requires that no analytical claim,
sourced statement or quoted material has been modified. The pass is additive at the term level, so it
cannot materially alter report-mode analytical content. Gating it would add ceremony without closing
anything.

Evidence:

- **The red is attributable, not just present.** Two pinned baselines. `Tpre` at `06c90e66` (before any
  owner consumed judgment) fails **all 14** assertions. `Tpre5` at `a8671275` (Unit 3 accepted) fails
  **exactly the six Stage-5 assertions** — `S1a`, `S1b`, `S2a`, `S2b`, `S3`, `S4` — and passes all six
  Stage 3-4 assertions. A correction claiming to close a Stage 5 gap should be red on Stage 5 alone,
  and this one is. Post-change: **14 of 14 PASS, exit 0**.
- **Ten new mutations, each independently live** on copies of the real command bodies with the other
  four copied verbatim: `SP1`-`SP3` remove each path handoff; `SU1`-`SU3` reduce each use instruction
  to *"verify the approved brief exists at that path"* — the existence-only failure, with the path
  handoff left intact; `SG1`/`SG2` remove each entry gate; `SQ` lets the final QC satisfy fidelity from
  the producers' change logs while leaving trace, drift and permission checks intact, so the flip is
  attributable to that alone. All flip exactly their own assertion.
- **`SM` is the control running the other way.** It deletes the section-mode exemption from a Stage 5
  gate and requires `S4` to fail. The risk a governance check carries here is imposing a judgment
  prerequisite on section-mode projects, which have no brief to consume; `SM` keeps that control live.
- **Exact commands, totals and exit codes.**
  `bash tests/judgment-seam/check-judgment-consumption.sh` → **14/14, exit 0** (was 8/8 before this
  round; 6 new assertions).
  `bash tests/judgment-seam/check-judgment-consumption.test.sh` → **33 passed, 0 failed**, exit 0
  (was 22).
  `check-judgment-seam.sh` → 13/13, exit 0; `check-judgment-seam.test.sh` → 23 passed, 0 failed.
  `check-judgment-contract.test.sh` → 19/0; `check-judgment-challenge.test.sh` → 24/0;
  `promote-judgment-brief.test.sh` → 24/0.
  `tests/s1-relay/check-relay-payload.sh` → TARGET MET, exit 0; `check-relay-payload.test.sh` → 26/0.
  `tests/s1-representative/score-specimen.test.sh` → 38/38.
  No assertion was weakened, and every accepted Stage 3-4 change is untouched — proved by `Tpre5`,
  which requires all six Stage 3-4 assertions to pass at the pre-correction commit and they still pass
  live. There is no dedicated Stage 5 regression suite in this workflow to run: `tests/` holds only
  `s1-relay` and `s1-representative`, and both are green.
- **Live-effect consumers, re-enumerated for the two newly edited files.** `produce-prose-draft.md` and
  `produce-formatting.md` each have **two symlink consumers** — `projects/buy-side-service-plan` and
  `archive/nordic-pe-macro-landscape-H1-2026` — both resolving to the canonical `ai-resources` checkout,
  not this worktree, so no live effect today. **And on merge the exposure is nil for both:**
  `buy-side-service-plan` declares `Document model: "section"`, so both new gates are report-mode-only
  no-ops there; the archived project declares no `Document model` line at all and its Phase 0 already
  halts on that, independently of this change. The section-mode exemption is what makes this true, and
  `S4`/`SM` are what keep it true. The Unit 3 merge-time precondition on `run-report.md` Step 4.0b is
  unchanged and still stands — `/run-report` is Stage 4 and has no mode dispatch.

Deferrals — carried forward, none added by this round:

1. The Unit 2 four stand: no `s1-relay` manifest row for the judgment relays (now nine, all
   path-passing by construction and therefore compliant but unmeasured); `/run-analysis` Step 5c's
   `qc-gate` write/tool mismatch, again verified not load-bearing here; the missing
   `judgment-context-card.md` template; the representative semantic proof.
2. `/sync-workflow` propagation of the judgment helpers and now five command bodies to the projects
   that carry copies or symlinks. Merge and propagation remain outside L2.
3. From Unit 3, unchanged: `architecture-qc` (Step 4.1b) reviews the architecture without the approved
   brief. Step 4.2c and now Stage 5's STAGE 3 cover the same drift downstream, so nothing is unguarded.

## Blocker

None.

## Next action

Codex: run the closure check on the frozen finding only — is the report-mode Stage 5 seam governed, and
did the correction break anything? The finding reproduced by inspection (0 judgment hits in all three
Stage 5 surfaces) before it was corrected. Both report-mode entries now validate approved authority and
halt fail-safe; both report-mode producers receive the brief by path with a required-use instruction;
the existing final editorial-integration QC gained a House View fidelity stage that may not rest on the
producers' accounts; `/produce-jargon-gloss` is excluded on its skill's own analytical-claim protection
rather than by omission; and section mode is explicitly exempt, with `SM` keeping that control live.
`Tpre5` pins the red to Stage 5 alone — at Unit 3 accepted, exactly the six new assertions fail and all
six Stage 3-4 assertions pass. Nothing outside the frozen finding was implemented, no deferral was
added, and the representative-proof boundary is untouched.

---
task: canonical-rw-judgment-house-view
status: active
turn: codex
---

## Objective and scope

Deliver the judgment lane of the content-approved lean Research Workflow plan: first obtain an honest L1 PASS or FAIL from one genuine local Sector Intelligence judgment trial, then—only after PASS—build and accept L2's canonical judgment layer / House View and publish its stable authority contract for the separate L3 lane. This task owns L1 evidence intake and L2 only. It excludes the lightweight Research Workflow, the L3 House View adapter, L4 integration, every programme listed in plan § 8, generic propagation, Content Programme work, merge and push.

The task exits either when L2 and its stable authority contract are accepted, or when L1 fails and the smallest local contract requiring another trial is recorded without beginning canonicalization.

## Lane and unit

Standard. Discovery mode. Unit 2 — find the eligible L1 unit.

Named reason for the loop: L1 and L2 span multiple sessions and two repositories, L1 must establish real operating evidence before any canonical change, and the result must be independently assessed before it is allowed to open L2.

## Brief

Unit 1 proved that `precision-components` cannot honestly test the current wired path: its brief predates the wiring, contains no substantive revision, and its safe downstream surfaces are already consumed or operator-gated. This unit inspects the untouched Sector `unit/*` branches to establish whether an eligible real L1 case exists before asking the operator to spend one unit's live analysis on the trial.

**Governing authority:** the content-approved `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, material content at `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`, especially §§ 5–7. Unit 1's accepted result below is authoritative current task state. Sector branch contents are verify-first repository evidence, not authority to change or run them.

**Required outcome:** inspect the Sector repository's local `unit/*` branch refs and registered worktrees read-only, then return an eligibility table and either one technically unique recommended L1 case, a small set of genuinely tied eligible cases requiring operator priority, or a NOT-READY finding naming the smallest local prerequisite. Do not select business priority, check out or switch a branch, create a worktree, run `/run-analysis`, or modify any Sector file/ref/worktree.

**Check against the live Sector repository:**

1. Bind the repository and enumerate the exact local `unit/*` refs plus any registered worktree carrying one. Inspect branch trees with read-only Git object commands; do not enter or alter another checkout.
2. For each candidate branch, establish its configured unit slug and current pipeline position from that branch's own `CLAUDE.md`, checkpoints and current-state sources. Do not infer position from branch name.
3. Determine whether the unit has cleared the required Pass-3 gate-clearance gate without a blocking verdict and has **not** yet run the judgment-producing `/run-analysis` path. Search and name the exact branch-tree surfaces for gate clearance, judgment briefs, section directives, downstream chapter/architecture outputs and any conflicting active state.
4. Establish whether the branch actually contains the current local judgment implementation needed by L1: producer and approval halt, authority gate, propagation into `section-directive-drafter`, the four checker/test pairs and promotion helper. Compare exact relevant file content or commit ancestry to the accepted local wiring ending at `fa98689`; presence of filenames alone is insufficient.
5. Identify branch/worktree ownership or dirty-state collision risks from registered worktrees and durable task/session declarations. Do not claim an unregistered branch is clean; say what the read-only evidence can and cannot establish.
6. Apply these eligibility conditions consistently: a real unit with live evidence; gate clearance passed; no prior judgment proposal/approval for the run; no delivered section directives on the target surface; current local wiring present; and no known active-writer collision. Rank by technical readiness only. If business priority is the remaining differentiator, return the tied options for the operator rather than choosing.

**Required evidence:** a compact per-candidate table with branch/ref, unit slug, pipeline position pointer, gate result, empty/occupied trial surfaces, wiring status and collision status; exact searched surfaces for every absence claim; a recommendation in one of the three allowed shapes; and the smallest next step. Do not create a separate report—the state file is the discovery artifact.

**Known L1 prerequisites carried forward, not solved here:** before the operating trial starts, the local deterministic floor still needs one failing-capable fixture proving `rejected` cannot be promoted or used downstream; the trial record itself must capture review loops, operator minutes, tokens/cost and elapsed time because those fields are absent from the current brief contract. Treat the missing rejection fixture as required work, not an optional deferral.

**Completion condition:** return the evidence and hand back to Codex. Change only this state file, commit the discovery hand-back in this checkout, and make no changes in the Sector repository or the lightweight-RW worktree.

**Stop conditions:** stop if branch inspection would require checkout/ref mutation, if current-state evidence is contradictory, if no branch can be evaluated without entering a live worktree, or if a candidate's eligibility depends on inventing operator intent. Do not repair, sync, merge, stash, clean, commit or delete anything in the Sector repository.

**Adjacent work held outside this unit by Codex framing:** candidate selection where business priority remains tied; the rejection fixture; the real L1 operation and operator revision; L1 PASS/FAIL assessment; all L2 canonical work; and all L3/L4 work.

Capability subset: baseline local read/search and read-only Git history/tree inspection, editing this state file, and Claude-owned local commit in this checkout. No network, subagents, Sector edits or commits, branch/worktree mutation, trial execution, merge, push, credentials, deployment, operator-reserved capability or pre-authorizable capability is selected.

Claude may challenge a false premise or stale direction with repository evidence; do not improvise around it.

## Latest result

Inspected (2026-08-18), Unit 2:

- Claim (1) — enumerate the **local** `unit/*` refs: FALSE as stated, non-blocking, and the substitution is disclosed rather than improvised. Searched `refs/heads/unit/` via `git for-each-ref` — empty. The only local branches are `main` (`c7e164f`), `codex/florian-demo-continue` (`d0cb658`) and `backup/pre-rebase-S5-93e` (`330727d`). The eleven unit branches exist as **remote-tracking** refs under `refs/remotes/origin/unit/*`, which are locally-present Git objects and fully inspectable with `for-each-ref`, `ls-tree`, `show`, `grep <ref>` and `merge-base` — no network, no checkout, no ref mutation, no worktree entered. Their content is authoritative for this question: `.git/FETCH_HEAD` is dated Aug 17 18:06, four days **after** the judgment wiring landed on 2026-08-13, so a missing wiring commit is a real absence and not a stale-fetch artifact. Every other check below ran against those refs. Also noted: local `main` (`c7e164f`) and `origin/main` (`6b5e1a4`) diverge — recorded, not load-bearing here.
- Claim (2) — unit slug and pipeline position from each branch's own tree, not its name: HOLDS, and the distinction mattered. Every unit branch carries inherited `precision-components` artifacts from the fork point, so a directory count alone reads as work that is not the branch's own. Resolved by counting `<area>/<own-slug>/` per branch. Only four branches hold any own-slug analysis work: `custom-dev-data-ai`, `finance-accounting-bpo`, `industrial-software`, `technical-industrial-distribution`.
- Claim (3) — cleared Pass-3 gate **and** `/run-analysis` not yet run: HOLDS for exactly one branch. `analysis/gate-clearance/<own-slug>/` is present on only two of eleven — `custom-dev-data-ai` and `finance-accounting-bpo`; searched that path per branch, the other nine return zero and therefore hard-gate at `/run-analysis` Step 0. Of the two, `finance-accounting-bpo` already holds 7 entries under `analysis/section-directives/finance-accounting-bpo/`, so `/run-analysis` has run and its trial surface is occupied. `custom-dev-data-ai` holds a `CLEARED-WITH-CAVEATS` verdict (`generated_at: 2026-08-17`, stating "Pass 4 is unblocked — `/run-analysis` and `/run-synthesis` may read this file at their Step 0"), 12 cluster memos, 6 claim-permission tables and 14 research extracts, and returns **zero** for `analysis/judgment/`, `analysis/section-directives/custom-dev-data-ai/` and `analysis/chapters/custom-dev-data-ai/` — searched each of those three paths on that ref. It is the only branch matching the required profile.
- Claim (4) — does any candidate branch carry the current local judgment implementation: **FALSE for every branch, and this is the decisive finding.** `git merge-base --is-ancestor fa98689 <ref>` fails for all eleven `origin/unit/*` refs and for both `main` and `origin/main` — thirteen refs, zero containing the wiring. At file level `git ls-tree -r` finds zero `check-judgment-*` scripts and no `reference/analyst-judgment-standard.md` on any of those thirteen refs. On `origin/unit/custom-dev-data-ai` the file `.claude/commands/run-analysis.md` exists but `git grep -c -i judgment` against it returns no matches, versus 25 on `codex/florian-demo-continue`. The entire judgment implementation exists on `codex/florian-demo-continue` and nowhere else — the branch this checkout is on, which carries `precision-components`, already ruled out by Unit 1. Presence of filenames was not relied on; ancestry and content were both checked.
- Claim (5) — collision and ownership risk: HOLDS, with a stated limit. `git worktree list` registers this checkout on `codex/florian-demo-continue` plus three detached `~/.codex/worktrees/axcion-sector-intelligence` entries at `15ffb11`, `3a3bbc1` and `8ac3149`; all three tips date to 2026-08-04 and are germany-wfm / demo commits, none a unit-branch tip. No unit branch is checked out anywhere locally. What this read-only evidence **cannot** establish: whether any actor is writing to `origin` now. The refs reflect the Aug 17 18:06 fetch and nothing after it, and no network call was made, so "clean" is claimed only as of that fetch.
- Claim (6) — apply the eligibility conditions and rank on technical readiness only: HOLDS. Ranking produced one unique candidate on pipeline position and zero candidates that are runnable today, because the wiring condition fails everywhere. No business-priority tie arose, so nothing is returned for operator priority on that ground.

Eligibility table — `gc` = own-slug gate-clearance files, `dir` = own-slug section-directives, wiring = contains `fa98689`:

| Branch (`origin/unit/…`) | gc | dir | Evidence present | Wiring | Status |
|---|---|---|---|---|---|
| `custom-dev-data-ai` | 1 `CLEARED-WITH-CAVEATS` | 0 | 12 memos, 6 permission tables, 14 extracts | none | **only candidate on position; blocked by wiring** |
| `finance-accounting-bpo` | 1 `CLEARED-WITH-CAVEATS` | 7 | 12 memos, 6 permission tables | none | ineligible — `/run-analysis` already run |
| `industrial-software` | 0 | 0 | 12 memos, 0 extracts | none | ineligible — Pass-3 not cleared |
| `technical-industrial-distribution` | 0 | 0 | 12 memos, 12 extracts | none | ineligible — Pass-3 not cleared |
| the other seven | 0 | 0 | extracts/answer-specs only | none | ineligible — Pass-3 not cleared |

Result: **NOT-READY — no Sector branch can host the genuine L1 trial today, and the reason is one named, fixable prerequisite rather than an absence of candidates.** On pipeline position the search resolves cleanly and uniquely to `unit/custom-dev-data-ai`: Pass-3 cleared yesterday, live evidence in place, no judgment artifact, no directives, no chapters — the whole L1 chain would run forward onto empty surfaces and overwrite nothing. But that branch, like every other unit branch and like `main` itself, carries none of the judgment implementation the trial is meant to exercise. The wiring accepted on 2026-08-13 was never merged or propagated off `codex/florian-demo-continue`.

Smallest local prerequisite, and the smallest next step: propagate the judgment implementation onto `unit/custom-dev-data-ai` — the four `check-judgment-*.sh` checkers with their four `.test.sh` suites, `promote-judgment-brief.sh`, `reference/analyst-judgment-standard.md`, `reference/templates/unit-judgment-brief-template.md`, and the judgment steps in `.claude/commands/run-analysis.md` (3.4a/3.4b/3.4c), `run-synthesis.md` (0b) and `run-report.md` (4.0b). Method precedent exists and argues against merging: the 2026-08-13 session note records that a `main`→unit merge cost 178 symlink conflicts per unit to deliver seven non-conflicting files, and narrow copy was chosen instead. That propagation is a Sector-repository write on a branch not currently checked out anywhere, so it needs its own unit and an explicit operator decision to spend `custom-dev-data-ai` on the trial — this brief authorised neither, and neither was performed.

Carried forward, still unsolved and not touched here: the required rejected-decision fixture, and the trial-local burden record (review loops, operator minutes, tokens/cost, elapsed time), since the brief contract has no burden fields.

Evidence: the decisive claim is a negative that could plainly have read the other way — `git merge-base --is-ancestor fa98689 <ref>` was run against thirteen refs and would have succeeded on any branch carrying the wiring; it failed on all thirteen, and the independent file-level check (`ls-tree` for `check-judgment-*` and the standard; `git grep -c -i judgment` on `run-analysis.md`) agreed, returning 0 on `origin/unit/custom-dev-data-ai` against 25 on `codex/florian-demo-continue`. Staleness is excluded rather than assumed: `.git/FETCH_HEAD` is dated Aug 17 18:06, after the 2026-08-13 wiring. The eligibility split is equally falsifiable — `analysis/gate-clearance/<own-slug>/` was searched on all eleven refs and returned non-empty on exactly two, and `analysis/section-directives/<own-slug>/` separated those two. Nothing was checked out, fetched, created or modified: the Sector repository remains on `codex/florian-demo-continue` at `d0cb658b` with the same two pre-existing changes found at open, and `refs/heads/` is unchanged.

## Blocker

None.

## Next action

Codex: assess discovery Unit 2. The search resolves uniquely to `unit/custom-dev-data-ai` on technical readiness, but returns NOT-READY overall because the judgment wiring exists only on `codex/florian-demo-continue` and was never propagated to any unit branch or to `main`. Decide whether the next unit propagates the wiring onto `unit/custom-dev-data-ai` — which is a Sector write on an unchecked-out branch, needs an operator decision to spend that unit's live analysis on the trial, and should carry the rejection fixture with it — or whether the L1 lane stops here. No business-priority tie needs resolving: only one branch qualifies on position.

---
task: canonical-rw-judgment-house-view
status: active
turn: codex
---

## Objective and scope

Deliver the judgment lane of the content-approved lean Research Workflow plan: first obtain an honest L1 PASS or FAIL from one genuine local Sector Intelligence judgment trial, then—only after PASS—build and accept L2's canonical judgment layer / House View and publish its stable authority contract for the separate L3 lane. This task owns L1 evidence intake and L2 only. It excludes the lightweight Research Workflow, the L3 House View adapter, L4 integration, every programme listed in plan § 8, generic propagation, Content Programme work, merge and push.

The task exits either when L2 and its stable authority contract are accepted, or when L1 fails and the smallest local contract requiring another trial is recorded without beginning canonicalization.

## Lane and unit

Standard. Implementation mode. Unit 3 — wire the isolated L1 trial branch.

Named reason for the loop: L1 and L2 span multiple sessions and two repositories, L1 must establish real operating evidence before any canonical change, and the result must be independently assessed before it is allowed to open L2.

## Brief

Unit 2 resolved the candidate question: `custom-dev-data-ai` is the only Sector unit with live evidence, cleared Pass 3 and empty judgment/downstream trial surfaces, but no unit branch contains the accepted local judgment wiring. This unit creates an isolated local trial checkout from that candidate's inspected ref and ports only the already-accepted judgment implementation onto it, establishing the technical base required before the rejection fixture and genuine L1 operation.

**Governing authority:** the content-approved `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, material content at `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`, especially §§ 5–7. Unit 2's accepted result below is authoritative current task state. The plan authorizes one genuine local Sector trial; because repository evidence yielded one technically unique candidate and no business-priority tie, selecting `custom-dev-data-ai` is technical execution inside that approved envelope, not a new operator approval. Founder revision and approve/revise/reject remain operator-owned and are not part of this unit.

**Required outcome:** in `projects/axcion-sector-intelligence`, create a deliberately isolated local branch/worktree from the exact inspected `origin/unit/custom-dev-data-ai` tip, bind and report its absolute checkout path plus starting commit, and make that trial branch carry the accepted local judgment implementation presently on `codex/florian-demo-continue` while preserving its unit-specific evidence and empty trial surfaces. Commit the port in the Sector trial checkout, but do not run the judgment workflow.

**Check against the live Sector repository:**

1. Revalidate immediately before mutation that the Sector source branch still contains the accepted wiring ending at `fa98689`, that the target remote-tracking tip is the same candidate state Unit 2 inspected, and that no local branch, registered worktree or durable ownership declaration now creates an active-writer collision. If the target ref moved materially or a collision appeared, stop and hand back rather than overwrite or improvise.
2. Establish the exact wiring delta from the accepted source implementation by content/history, not from Unit 2's filename list alone. It must cover the proposal/approval halt, authority gating and downstream consumption, independent judgment QC, promotion behavior, the four checker/test pairs, the governing standard/template and the affected analysis/synthesis/report ownership surfaces. Exclude unrelated `precision-components` data, candidate outputs and later source-branch work that is not part of the judgment implementation.
3. Create an isolated trial branch/worktree from the candidate ref without switching, cleaning or modifying the existing `codex/florian-demo-continue` checkout or any other registered worktree. Choose a locally clear branch/path and report both exactly; do not push or alter a remote ref.
4. Port the verified wiring so the candidate's unit-specific evidence and gate clearance remain intact and its own `analysis/judgment/`, `analysis/section-directives/custom-dev-data-ai/` and `analysis/chapters/custom-dev-data-ai/` surfaces remain empty. Resolve only conflicts required by this port; stop if preserving the candidate would require changing its pipeline meaning or the approved judgment contract.
5. Run the four judgment checker/test suites on the isolated trial checkout and report their individual and aggregate results. Also run a failing-capable comparison that proves the expected wiring surfaces now match the accepted implementation semantically while candidate-specific evidence was not replaced.

**Required evidence:** exact source and target starting refs, isolated checkout path and branch, the committed Sector port hash, the verified set of ported surfaces and why each belongs to the accepted wiring, pre/post proof for the target's wiring state, proof the candidate-specific gate/evidence and empty trial surfaces were preserved, and all four suite results with assertion totals. Evidence must distinguish repository observations from implementation judgments and must be capable of failing.

**Known L1 prerequisites carried forward, not solved here:** the next unit must add one failing-capable fixture proving `rejected` cannot be promoted or used downstream; the later trial record must capture review loops, operator minutes, tokens/cost and elapsed time because those fields are absent from the current brief contract. Treat both as required work. Do not fold the rejection fixture into this port: it is a distinct negative control and Work Loop sizing requires it to remain its own dominant deliverable.

**Completion condition:** the isolated `custom-dev-data-ai` trial checkout carries a committed, test-green port of the accepted judgment wiring, with the unit's own evidence and empty trial surfaces preserved; the state file records the binding and proof and hands back to Codex. Commit the Sector implementation first, then commit this hand-back in the current checkout.

**Stop conditions:** stop if source or target identity materially differs from Unit 2, an active-writer collision exists, the isolated worktree cannot be created safely, the accepted wiring cannot be separated from unrelated source-branch work, preservation requires changing the candidate's pipeline meaning, a required suite cannot run, or the port would touch any excluded surface. Do not merge, rebase, stash, clean, delete, push, run `/run-analysis`, create judgment content or modify the lightweight-RW worktree.

**Adjacent work held outside this unit by Codex framing:** the rejected-decision fixture; the real L1 operation, semantic challenge and operator revision/decision; the burden record; L1 PASS/FAIL assessment; all L2 canonical work; and all L3/L4 work.

Capability subset: baseline local read/search/history inspection, local tests, reversible edits limited to the isolated Sector trial checkout, creation of its local branch/worktree, Claude-owned local commits in the Sector trial checkout and this task checkout, and editing this state file. No network, subagents, changes to existing Sector worktrees, trial execution, merge, rebase, push, credentials, deployment, operator-reserved capability or pre-authorizable capability is selected.

Claude may challenge a false premise or stale direction with repository evidence; do not improvise around it.

## Latest result

Inspected (2026-08-18), Unit 3 — every claim checked before mutation:

- Claim (1) — identities and collision state immediately before mutation: HOLDS. Source `codex/florian-demo-continue` at `d0cb658b9266c4af553ad224181d942e473a61cd`, and `git merge-base --is-ancestor fa98689 codex/florian-demo-continue` succeeds, so the accepted wiring is still there. Target `origin/unit/custom-dev-data-ai` at `d1359bf6ab73bb754ff79aa69301328cd847beb9` — the same tip Unit 2 inspected, unmoved. No collision: `refs/heads/` holds only `main`, `codex/florian-demo-continue` and `backup/pre-rebase-S5-93e`; `git worktree list` registered no unit branch; `logs/work-loop/.owner` does not exist (searched that path); the Sector tree carried only the two pre-existing changes found at open.
- Claim (2) — the wiring delta established by content and history, not by Unit 2's filename list: HOLDS, and it resolves to **16 surfaces**. `git diff --name-status` across both judgment tasks (`16661cd~1..fa98689` and `3cb5c11~1..e2a9a8b`) plus `git log --reverse` for each file's introducing commit puts the whole implementation between `ffed814` (10:11) and `fa98689` (17:16) on 2026-08-13: three commands (`run-analysis.md`, `run-synthesis.md`, `run-report.md`), `reference/stage-instructions.md`, four `check-judgment-*.sh` checkers with their four `.test.sh` suites, `promote-judgment-brief.sh`, `reference/analyst-judgment-standard.md`, `reference/templates/unit-judgment-brief-template.md` and `reference/axcion-judgment-context.md`. That set covers the producer and approval halt (3.4a/3.4b), authority gating (3.4c, 0b, 4.0b), propagation into all four authoring consumers, promotion behaviour, the governing standard and template, and the producer's Axcíon-context input. **Exclusion verified, not assumed:** `git log 052eade..codex/florian-demo-continue` restricted to those 16 paths returns only judgment-layer commits — no unrelated source-branch work is entangled. `analysis/judgment/**` (the `precision-components` and `germany-wfm` brief data) and the two closed `logs/work-loop/` records were deliberately excluded as candidate outputs and task records, not wiring.
- Claim (2b) — would porting the shared files overwrite unit-specific work: FALSE, checked because it was the main preservation risk. The merge-base is `052eade` (2026-07-30). All four shared files are **identical between the merge-base and the candidate** (`git diff --quiet` clean on each), so `custom-dev-data-ai` has made no changes to them; the source's delta on them is judgment-only (`run-analysis.md` +133−1, `run-report.md` +37−2, `run-synthesis.md` +31−1, `stage-instructions.md` +19−4). Taking the source version therefore replaces nothing the candidate authored. All 16 are regular files (`100644`/`100755`, no `120000`), so the 178-symlink-conflict problem recorded on 2026-08-13 for whole-branch merges does not arise on this narrow set. `reference/axcion-judgment-context.md` was checked for unit-specific content: its single `precision-components` mention is a firm-level portfolio status row in the priorities table, not analytical data, and the candidate has no context card at all, so nothing was overwritten.
- Claim (3) — isolated branch/worktree created without touching any existing checkout: HOLDS. Path and branch confirmed clear first, then `git worktree add -b trial/l1-judgment-custom-dev-data-ai <path> origin/unit/custom-dev-data-ai`. Trial checkout: `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial`, branch `trial/l1-judgment-custom-dev-data-ai`, starting commit `d1359bf6`, clean tree at creation. The original Sector checkout is still on `codex/florian-demo-continue` at `d0cb658b` with the same two pre-existing changes; the three `~/.codex/worktrees/` entries are untouched; `origin/unit/custom-dev-data-ai` is still `d1359bf6` — nothing was pushed and no remote ref altered.
- Claim (4) — port preserves the candidate: HOLDS, on three independent checks. `git diff --name-only d1359bf6` returns **exactly 16 paths** — the wiring set and nothing else. The gate-clearance file's SHA-256 is unchanged at `1468503e7eb013f236cc27b39f2fcd8074fc4a6252849b7e101f0e898cfa5117`, and the unit's evidence still counts 12 cluster memos, 6 claim-permission tables and 14 research extracts. The three L1 trial surfaces are still absent: `analysis/judgment/`, `analysis/section-directives/custom-dev-data-ai/` and `analysis/chapters/custom-dev-data-ai/` — searched each path in the trial checkout. No conflict arose, so none had to be resolved, and nothing about the candidate's pipeline meaning changed.
- Claim (5) — suites green on the isolated checkout, plus a failing-capable comparison: HOLDS.

Result: **the isolated `custom-dev-data-ai` trial branch now carries a committed, test-green port of the accepted judgment wiring, with the unit's own evidence and empty trial surfaces intact.** Sector port commit `8752775` on `trial/l1-judgment-custom-dev-data-ai`; trial checkout clean after commit. Semantically the port is exact rather than approximate: `git diff codex/florian-demo-continue` restricted to all 16 ported paths is **empty**, so every surface is byte-identical to the accepted implementation. No judgment workflow was run, no judgment content created, and the rejected-decision fixture was deliberately not folded in.

Evidence, and why each piece could have failed:

- **Suites on the trial checkout:** contract `exit 0` 17/17, gate `exit 0` 16/16, producer `exit 0` 33/33, propagation `exit 0` 16/16 — 82 assertions, 0 failed, matching the accepted source totals exactly. These are not a formality here: the gate, producer and propagation suites carry LIVE assertions that read the actual `.claude/commands/*.md` in this checkout (`L1 LIVE /run-analysis produces, halts and re-enters safely`; `P13`–`P16` LIVE consumer propagation). Had the port been partial — a command ported without its checker, or a checker without the standard — those live cases fail UNPROPAGATED, which is the documented failure mode from the original wiring work. `git status --porcelain` before and after the run diffs IDENTICAL, so no suite wrote anything.
- **Live discriminating probe on the candidate's own surface**, run three ways against the ported checker: the candidate's absent brief returns `exit 3 MISSING`, a real approved brief returns `exit 0 VALID`, and the proposed form offered as authority returns `exit 4 NOT-APPROVED`. Three distinct exit codes from one binary proves the checker is live in this checkout and actually discriminating, not passing by default — a broken or stubbed port would collapse these to one verdict. It also independently re-confirms the trial surface is genuinely empty. The probe fixtures were written to the scratchpad, not the repository; the trial tree still showed only the 16 staged wiring paths afterwards.
- **Preservation proof** is likewise falsifiable: the 16-path `diff --name-only` against `d1359bf6` would have listed any collateral change, and the gate-clearance SHA-256 would have moved had the port disturbed it.

Repository observations versus implementation judgments, kept separate: that the candidate's four shared files are identical to the merge-base, that all touching commits since it are judgment-layer commits, that all 16 files are regular files, and every hash, count and exit code above are **observations**. That the 16-file set is the right boundary for "the accepted judgment implementation" — including `axcion-judgment-context.md` as a producer input and excluding `analysis/judgment/**` brief data and the two closed task records — is a **judgment**, made on the two closing records' own description of the implementation and on the commit ranges, and it is the one thing here worth an assessor's challenge.

Carried forward, untouched and still required: the failing-capable fixture proving a `rejected` decision cannot be promoted or used downstream, and the trial-local burden record (review loops, operator minutes, tokens/cost, elapsed time).

## Blocker

None.

## Next action

Codex: assess Unit 3. The isolated trial base exists at `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial` on `trial/l1-judgment-custom-dev-data-ai`, Sector commit `8752775` from candidate tip `d1359bf6`, wiring byte-identical to `codex/florian-demo-continue` and all four suites green at 82/82. The judgment call to review is the 16-surface boundary drawn for "the accepted implementation". If accepted, the next unit is the rejected-decision fixture as its own dominant deliverable; the genuine L1 operation, its operator revision and the burden record follow after that.

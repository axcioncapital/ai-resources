---
task: canonical-rw-judgment-house-view
status: active
turn: codex
---

## Objective and scope

Deliver the judgment lane of the content-approved lean Research Workflow plan: first obtain an honest L1 PASS or FAIL from one genuine local Sector Intelligence judgment trial, then—only after PASS—build and accept L2's canonical judgment layer / House View and publish its stable authority contract for the separate L3 lane. This task owns L1 evidence intake and L2 only. It excludes the lightweight Research Workflow, the L3 House View adapter, L4 integration, every programme listed in plan § 8, generic propagation, Content Programme work, merge and push.

The task exits either when L2 and its stable authority contract are accepted, or when L1 fails and the smallest local contract requiring another trial is recorded without beginning canonicalization.

## Lane and unit

Standard. Implementation mode. Unit 4 — add the rejected-decision negative control.

Named reason for the loop: L1 and L2 span multiple sessions and two repositories, L1 must establish real operating evidence before any canonical change, and the result must be independently assessed before it is allowed to open L2.

## Brief

Unit 3 established the isolated, test-green L1 trial checkout with the accepted judgment implementation and the candidate's evidence preserved. The lean plan requires one deterministic negative control before live operation: a rejected founder decision must be unable to become approved authority or be consumed downstream. This unit adds that single regression proof and makes only the smallest contract repair if the probe exposes a real gap.

**Governing authority:** the content-approved `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, material content at `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`, especially § 7's L1 floor: the four suites green before and after, plus a fixture proving a rejected decision cannot be promoted or used downstream. Unit 3's accepted result below binds the trial checkout at `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial`, branch `trial/l1-judgment-custom-dev-data-ai`, commit `8752775`.

**Required outcome:** add the minimum deterministic regression fixture in the bound trial checkout that proves one structurally valid `rejected` Unit Judgment Brief cannot be promoted into approved authority and cannot pass the real authority boundary used by downstream analysis, synthesis or reporting. If the fixture reveals a behavioral gap, repair only that local judgment contract and keep the fixture; otherwise change regression coverage only. Commit the result in the Sector trial checkout without creating any real unit judgment content.

**Check against the live Sector repository:**

1. Revalidate the exact checkout, branch and `8752775` starting commit, confirm it is clean, and confirm the candidate's judgment, directive and chapter trial surfaces remain empty. Stop on identity drift, uncommitted changes or an active-writer collision.
2. Search the four existing judgment suites, promotion helper and actual authority-gate/consumer surfaces for a rejected-decision scenario. Name every searched surface and distinguish an existing structural `status: rejected` rejection from the missing end-to-end guarantee this unit must add.
3. Construct the smallest fixture representing a structurally complete founder decision whose status is explicitly `rejected`, so the result cannot pass merely because unrelated required fields are missing. First exercise it against the current promotion and authority boundary and record the pre-change behavior. If behavior is already correct, identify missing regression coverage as the failing case; do not invent a production-code change solely to manufacture red output.
4. Add one cohesive regression scenario that attempts both prohibited transitions: promotion of the rejected decision and presentation of rejected content at the approved-authority boundary used downstream. It must assert non-success, the specific rejection class, absence of promoted authority, absence of downstream-produced output or state change, and preservation of the source fixture. Use the real helper/checker boundaries rather than a grep-only proxy.
5. Demonstrate that the new scenario is capable of failing when rejection enforcement is absent, using a bounded scratch copy, controlled fault injection or an equivalently isolated test of the fixture—not by weakening or committing a permissive production path. Restore/clean all temporary effects before commit.
6. Run the complete four-suite floor after the change and report each suite and aggregate assertion totals. Confirm the unit's live trial surfaces and evidence inventory are unchanged.

**Required evidence:** exact starting and result commits; searched surfaces and the precise pre-change gap; the rejected fixture's validity basis; observed promotion and downstream-boundary verdicts; proof no approved artifact or downstream output/state appeared and the fixture stayed unchanged; the controlled demonstration that the new scenario fails without enforcement; all four post-change suite results and totals; and proof the real candidate trial surfaces/evidence remain untouched. Report any production-contract repair separately from test-only changes.

**Known L1 prerequisite carried forward, not solved here:** the live trial record must capture review loops, operator minutes, tokens/cost and elapsed time. Do not run `/run-analysis`, solicit the operator's revision/decision, or create the trial record in this unit.

**Completion condition:** a committed, failing-capable rejected-decision scenario proves both no promotion and no downstream authority use, the complete four-suite floor is green, and the genuine candidate remains unoperated and unchanged outside the regression surfaces. Commit the Sector change first, then record and commit the evidence hand-back here with `turn: codex`.

**Stop conditions:** stop if the checkout binding has drifted, a collision exists, the real downstream authority boundary cannot be exercised deterministically, the rejected state is ambiguous under the governing contract, the fixture exposes a contract gap larger than a local rejection repair, a required suite cannot run, or proof would require touching genuine trial content. Do not merge, rebase, stash, clean, delete, push, run `/run-analysis`, create real judgment content, alter candidate evidence or modify the lightweight-RW worktree.

**Adjacent work held outside this unit by Codex framing:** the real L1 operation, independent semantic challenge, operator substantive revision and approve/revise/reject act, burden record, L1 PASS/FAIL assessment, all L2 canonical work, and all L3/L4 work.

Capability subset: baseline local read/search/history inspection, local tests and scratch fixtures, reversible edits limited to the judgment regression surfaces in the bound Sector trial checkout, Claude-owned local commits in the Sector trial checkout and this task checkout, and editing this state file. No network, subagents, changes to other Sector worktrees, real trial execution, merge, rebase, push, credentials, deployment, operator-reserved capability or pre-authorizable capability is selected.

Claude may challenge a false premise or stale direction with repository evidence; do not improvise around it.

## Latest result

Inspected (2026-08-18), Unit 4 — checked before acting:

- Claim (1) — binding, cleanliness and collision: HOLDS. Trial checkout `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial`, branch `trial/l1-judgment-custom-dev-data-ai`, HEAD `87527757cbcc241ef1be2406940bee494e52910b` — the Unit 3 commit, unmoved — and `git status --porcelain` empty. Trial surfaces still absent (`analysis/judgment/`, `analysis/section-directives/custom-dev-data-ai/`, `analysis/chapters/custom-dev-data-ai/`; searched each path); evidence at 12 memos, 6 claim-permission tables, 14 extracts, 1 gate-clearance file at SHA-256 `1468503e…5117`. No active-writer collision.
- Claim (2) — is a rejected-decision scenario already covered anywhere: FALSE — **no coverage exists**, and the surfaces were named individually rather than globbed. Searched all four `check-judgment-*.sh` checkers, all four `.test.sh` suites, `promote-judgment-brief.sh`, `reference/analyst-judgment-standard.md` and `reference/templates/unit-judgment-brief-template.md` — eleven files — for a rejected status value (`status: rejected`, `'rejected'`, `"rejected"`, `=rejected`). Zero matches in every one. The structural refusal and the end-to-end guarantee are distinct and both were checked: `check-judgment-contract.sh` line 105 admits only `approved|proposed`, so a rejection is refused *structurally*, but nothing anywhere asserted that a rejected decision cannot cross the promotion path or the authority boundary. That missing assertion is what this unit adds.
- Claim (3) — pre-change behaviour, recorded before any edit: HOLDS, and **the behaviour is already correct**. Probed with a scratch fixture outside the repository: promotion returns `INVALID-PROPOSAL` exit 7 (the helper's step-7 validator call rejects the status before anything is built); the authority boundary returns `STRUCTURE` exit 6. A control — the identical fixture with `status: proposed` — returns `VALID` exit 0, proving the refusals are about the status and not a missing field. Per the brief, the failing case is therefore the **missing regression coverage**, and no production-code change was invented to manufacture red output. **This unit is test-only: no production contract was repaired**, and the one changed file is a `.test.sh`.

Result: **`16b2531` on `trial/l1-judgment-custom-dev-data-ai` adds the rejected-decision negative control, and the full four-suite floor is green.** The scenario is one cohesive block, P14–P21, added to `logs/scripts/check-judgment-producer.test.sh` — the suite that owns the promotion helper — so the floor stays four suites rather than acquiring a fifth. It exercises the real helper and checker boundaries, never a grep proxy.

The fixture's design carries the guarantee the brief asked for. It is structurally **complete** and differs from a promotable proposal in exactly one frontmatter line, and P14 proves that in-suite by validating the same decision as `proposed` at exit 0 rather than asserting it in a comment. That is the distinction from the pre-existing P12, whose malformed proposal could be refused for its shape alone; a shape-only fixture cannot detect a rejection being laundered into authority. The eight assertions cover both prohibited transitions: P15 promotion refused at exit 7, P16 the refusal naming `INVALID-PROPOSAL` and the rejected status, P17 no approved artifact created, P18 the authority gate refusing at exit 6, P19 that refusal naming `STRUCTURE` and the status, P20 that `--allow-proposed` does not launder a rejection, and P21 that the operator's decision is byte-unchanged afterwards.

Evidence:

- **Floor after the change:** contract `exit 0` 17/17, gate `exit 0` 16/16, producer `exit 0` **41/41**, propagation `exit 0` 16/16 — **90 assertions, 0 failed**, up from 82 by exactly the 8 added. The producer suite's LIVE `/run-analysis` assertion still passes, so the addition did not disturb the ported command surfaces.
- **The scenario is demonstrably failing-capable**, shown by controlled fault injection in an isolated scratch copy of `logs/scripts/` — `rejected` added to the legal status set in the scratch checker only. **Five of the eight assertions go red**: P15 (7→8), P16, P18 (6→4), P19 and P20 (6→**0**). P20 is the decisive one: with `rejected` treated as legal, `--allow-proposed` returns `VALID exit 0` on a rejected decision — the laundering path, caught only by this assertion. P17 and P21 stay green under injection, which is itself informative: the "no artifact appears" guarantee has a second independent layer in the promotion helper's step-8 `PROMOTION-INVALID` check, so it is defended twice.
- **Nothing permissive was committed and nothing needed restoring in the repository.** The injection lived entirely in the scratchpad; the committed `check-judgment-contract.sh` still reads `approved|proposed` at line 105, verified after the run. `git status` in the trial checkout showed exactly one modified file before commit — `logs/scripts/check-judgment-producer.test.sh` — and a clean tree after.
- **Candidate untouched:** the three trial surfaces are still absent, evidence still 12/6/14, and the gate-clearance SHA-256 is still `1468503e7eb013f236cc27b39f2fcd8074fc4a6252849b7e101f0e898cfa5117`. No judgment content was created, `/run-analysis` was not run, and the operator was not asked for a decision.

Repository observations versus judgments, kept separate: every exit code, assertion total, hash and search result above is an **observation**. Two things are **judgments** worth an assessor's challenge — that the producer suite is the right home for a scenario spanning promotion and the authority boundary (chosen so the floor stays four suites, and because the pre-existing R2 already pairs both boundaries there), and that test-only coverage is the correct response to a contract that already behaves correctly, rather than hardening `rejected` into a first-class status.

Carried forward, untouched: the live trial record must capture review loops, operator minutes, tokens/cost and elapsed time — no field exists for these in the brief contract.

## Blocker

None.

## Next action

Codex: assess Unit 4. Sector commit `16b2531` from `8752775`; floor green at 90 assertions with the 8 new ones proven failing-capable under isolated fault injection. Two judgments to review: placing the scenario in the producer suite rather than adding a fifth suite, and treating this as test-only because the contract already refuses a rejection correctly. If accepted, the L1 deterministic floor is complete and the next unit is the genuine trial operation — which needs the operator's substantive revision and approve/revise/reject act, and a burden record with no field to hold it.

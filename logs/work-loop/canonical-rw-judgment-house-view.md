---
task: canonical-rw-judgment-house-view
status: active
turn: codex
---

## Objective and scope

Deliver the judgment lane of the content-approved lean Research Workflow plan: first obtain an honest L1 PASS or FAIL from one genuine local Sector Intelligence judgment trial, then—only after PASS—build and accept L2's canonical judgment layer / House View and publish its stable authority contract for the separate L3 lane. This task owns L1 evidence intake and L2 only. It excludes the lightweight Research Workflow, the L3 House View adapter, L4 integration, every programme listed in plan § 8, generic propagation, Content Programme work, merge and push.

The task exits either when L2 and its stable authority contract are accepted, or when L1 fails and the smallest local contract requiring another trial is recorded without beginning canonicalization.

## Lane and unit

Standard. Discovery mode. Unit 1 — bind the genuine L1 trial.

Named reason for the loop: L1 and L2 span multiple sessions and two repositories, L1 must establish real operating evidence before any canonical change, and the result must be independently assessed before it is allowed to open L2.

## Brief

The approved plan puts this checkout on the judgment lane and makes a genuine local trial the hard gate before L2. This first unit resolves the live case, repository binding and runnable path so the trial uses a real Sector Intelligence unit rather than a synthetic fixture or a stale pilot artifact.

**Governing authority:** `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, whose material content at `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec` was content-approved and whose administrative closing baseline is `e7f9f682da88bb28983b4058552da4932fe81674`. Plan §§ 5–7 govern L1, the lane boundary and sequence. The closed plan task `logs/work-loop/canonical-rw-lean-plan.md` is authoritative current state for the approval and lane decisions. The Sector repository's own instructions and current state govern what its real unit is; its old proposals and closed pilot records are evidence, not new authority.

**Current position established at open:** the plan task is closed; the separate lightweight worktree exists at `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources-lightweight-rw` on `session/2026-08-18-lightweight-rw` from `e7f9f682`; this checkout remains `session/2026-08-17-research-workflow-fixes` and owns L1/L2. The Sector checkout exists at `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence`; a bounded read found HEAD `d0cb658b9266c4af553ad224181d942e473a61cd`, branch `codex/florian-demo-continue`, pre-existing changes to `logs/friction-log.md` and `reports/analyst-judgment-layer-implementation-research-v1.md`, no `.owner`, closed local-pilot records, and a 2026-08-13 session note saying the local wiring is complete but a representative end-to-end trial remains unrun. Treat each repository statement as verify-first, not as permission to edit.

**Required outcome:** inspect only, then return one evidence-backed decision about whether `precision-components` in the bound Sector checkout is the valid genuine L1 case and exactly what the next operating unit would do. Do not run the authoring trial, revise a judgment, create a trial memo, or change any file in the Sector repository in this discovery unit.

**Check against the live repositories:**

1. Bind both checkouts by absolute path, branch and HEAD; report dirty/staged/untracked paths separately. Confirm the lightweight worktree is a different registered worktree and do not enter or modify it.
2. In the Sector checkout, confirm the four named `logs/scripts/check-judgment-*.test.sh` suites and their production checkers exist. Run the four test suites as the L1 pre-trial baseline and report commands, exit codes and assertion totals. Stop if a suite writes tracked content or if baseline is not green.
3. Read the two local judgment Work Loop closing records, the 2026-08-13 judgment-layer session note, the current `precision-components` stage checkpoint(s), and the proposed/approved `precision-components` judgment briefs. Inspect only the file-specific Git history or diff needed to establish chronology and substantive revision; do not scan unrelated history.
4. Establish whether `precision-components` can exercise the **wired** path now: real evidence → proposed brief → at least one substantive operator revision → explicit approval or rejection → approved authority visibly shaping one downstream directive, chapter or architecture artifact → independent semantic review and burden record. Distinguish facts already proven by the earlier local pilot from steps that must happen anew for this plan's L1 trial.
5. Determine whether reusing the existing approved brief would genuinely test the current wiring or merely replay a pre-wiring artifact. The plan requires a substantive operator revision inside the genuine trial; do not count an old difference unless the repository proves it was operator-directed and belongs to the same operating run.
6. Identify the smallest real downstream artifact that can demonstrate judgment influence without broadening the trial, overwriting delivered work, bypassing the current Stage-4 gate, or touching another unit/worktree. If no such artifact exists, return that as a valid L1 readiness failure.

**Evidence required:** bound repository fingerprints and path-separated dirtiness; the four baseline suite results; precise pointers to the current stage checkpoint and judgment artifacts; a chronology showing what predates and postdates the local wiring; a PASS/NOT-READY recommendation for using `precision-components` as the L1 case; and a concise proposed next operating brief listing the real entry command, operator gate, one downstream proof artifact, independent review, burden fields and stop conditions. Every negative claim must name the searched surface.

**Completion condition:** return the evidence and hand back to Codex. Change only this state file, commit the discovery hand-back in this checkout, and make no implementation or trial changes anywhere else.

**Stop conditions:** stop and hand back if either checkout identity is not what the plan expects; the Sector tree has live or unowned work that the trial would collide with; any baseline suite fails; no real eligible case can be established; the current gate cannot be respected; or choosing among genuinely tied live cases requires operator priority. Do not repair, clean, stash, commit, or delete pre-existing Sector changes.

**Adjacent work held outside this unit by Codex framing:** L1 operation, L1 PASS/FAIL assessment, every L2 canonical edit, L3 coordination beyond respecting the published seam, and the separate protocol-wording defect recorded by the closed planning task.

Capability subset: baseline local read/search/history inspection, execution of the four local test suites, editing this state file, and Claude-owned local commit in this checkout. No network, subagents, Sector-repository edits or commits, worktree/branch mutation, merge, push, credentials, deployment, operator-reserved capability or pre-authorizable capability is selected.

Claude may challenge a false premise or stale direction with repository evidence; do not improvise around it.

## Latest result

Inspected (2026-08-18):

- Claim (1) — repository binding: HOLDS. This checkout `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources-research-workflow-fixes`, branch `session/2026-08-17-research-workflow-fixes`, HEAD `e7f9f682da88bb28983b4058552da4932fe81674`; unstaged `logs/friction-log.md`, untracked `logs/work-loop/canonical-rw-judgment-house-view.md`, nothing staged. `git worktree list` confirms `ai-resources-lightweight-rw` is a separate registered worktree on `session/2026-08-18-lightweight-rw` at `e7f9f682`; not entered, not modified. Sector checkout `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence`, branch `codex/florian-demo-continue`, HEAD `d0cb658b9266c4af553ad224181d942e473a61cd`; staged: none; unstaged: `logs/friction-log.md`; untracked: `reports/analyst-judgment-layer-implementation-research-v1.md`. `logs/work-loop/` holds two closing records and no `.owner` — searched that directory listing. Every fingerprint matches the brief's stated position.
- Claim (2) — suites and checkers exist, baseline green: HOLDS. `ls logs/scripts/check-judgment-*.test.sh` returns exactly four (`contract`, `gate`, `producer`, `propagation`), each with its production checker (`check-judgment-contract.sh`, `-gate.sh`, `-producer.sh`, `-propagation.sh`) plus `promote-judgment-brief.sh`. All four suites exit `0`: contract 17/17, gate 16/16, producer 33/33, propagation 16/16 — 82 assertions, 0 failed, matching the integration record's close-time totals exactly. `git status --porcelain` before and after diffs IDENTICAL and HEAD unchanged at `d0cb658b`, so no suite wrote tracked or untracked content.
- Claim (3) — records, checkpoints and briefs read: HOLDS. Read both closing records, `logs/session-notes.md` lines 290–349 (the 2026-08-13 judgment-layer note and its neighbour), the thirteen `analysis/checkpoints/precision-components/` files and four `report/checkpoints/precision-components/` files by listing, and both `precision-components` judgment briefs. File-scoped history only (`git log -- analysis/judgment/precision-components/` and the two wiring paths); no unrelated history scanned.
- Claim (4) — can `precision-components` exercise the **wired** path now: FALSE as posed. The wired chain exists and is regression-pinned, but this unit has already consumed every downstream surface it feeds. Delivered and dated 2026-07-28: six `analysis/section-directives/precision-components/` directives, six `analysis/chapters/precision-components/` cluster drafts, `report/architecture/precision-components/precision-components-architecture.md` (46,174 bytes), and `report/chapters/precision-components/` chapter-01 draft + review. The only unproduced artifacts are Chapters 2–7. Both facts already proven by the earlier pilot (a proposed brief is refused as authority; approved judgment can be integrated into prose without changing a delivered report) are pre-wiring; the steps that must happen anew — production via `/run-analysis` Step 3.4a, substantive operator revision, and propagation into a live authoring dispatch — cannot all run on this unit without overwriting delivered work.
- Claim (5) — would reusing the existing approved brief test the current wiring: FALSE, on two independent grounds. **No substantive revision:** `diff` of `precision-components-unit-judgment-brief-proposed.md` against `-approved.md` returns exactly three changed regions, all approval banner — `status: proposed`→`approved`, an added `approved_by: Patrik`, and one banner sentence; 907 vs 909 words, zero analytical change. **Pre-wiring artifact:** the approved brief is commit `38fe9b0` (2026-08-13 11:07:54 +0300), which `git merge-base --is-ancestor` confirms is an ancestor of the wiring's first commit `16661cd` (12:39:32) and precedes the final wiring commit `fa98689` (17:16:33) by six hours. It was hand-authored under `analyst-judgment-layer-local-pilot`, before `/run-analysis` Step 3.4a existed. The same holds for the Germany brief, which its own closing record calls "analytically byte-identical" to its proposal.
- Claim (6) — smallest downstream proof artifact within the stated constraints: FALSE — none exists. Chapters 2–7 are hard-blocked twice. The Step 4.4/4.1b operator-review gate on chapter-01 has never cleared: searched `report/` recursively for `*OPERATOR-APPROVED*` — no match. And Step 4.2e's style-reference lock is unset: `report/style-reference/` contains only `.gitkeep` (0 bytes), while the architecture § Style-Reference Lock states Chapters 2–7 must bind to that reference, derived only after the first approved chapter. Every other candidate surface already holds delivered content. So demonstrating judgment influence on this unit requires either overwriting delivered work or bypassing the current Stage-4 gate — both excluded by this brief. Per brief item 6 this is a valid L1 readiness failure.

Additional inspected findings bearing on L1's floor:

- Burden fields do not exist. Searched `reference/analyst-judgment-standard.md` and `reference/templates/unit-judgment-brief-template.md` for `burden` (case-insensitive) — no match in either; across all of `reference/` only `executor-routing-guide.md` matches. Plan § 5 L1 requires a recorded burden (review loops, operator minutes, tokens/cost, elapsed time), so the trial must carry its own burden record; nothing in the artifact contract supplies one.
- No rejection fixture exists. `check-judgment-contract.sh` line 106 admits only `proposed` and `approved`; any other status is exit 6 STRUCTURE, so a rejected decision cannot become authority. But plan § 7's L1 deterministic floor names a fixture proving a rejected decision cannot be promoted or used downstream, and none is present: the contract suite's V1–V8 cover missing, not-approved, no-claim-ids, valid, missing-heading, thesis-count, word-band and distinct-codes only.
- `precision-components` is the only section in this checkout — `analysis/checkpoints/` and `report/checkpoints/` each contain exactly one directory. The other units sit on `unit/*` branches, and `git worktree list` in the Sector repo registers only this checkout plus three detached `~/.codex/worktrees/` entries — no `unit/*` worktree. No other unit was inspected; the brief excludes touching one.

Result: **NOT-READY — `precision-components` is not a valid genuine L1 case.** It is the delivered pilot unit, frozen mid-Stage-4 at its chapter-01 operator gate, and it fails L1 on two independent grounds that cannot be repaired inside the trial: its approved brief carries no substantive operator revision and predates the wiring it would supposedly test, and it has no downstream artifact left that judgment could visibly shape without overwriting delivered work or bypassing the Stage-4 gate. Nothing was run, revised, authored or changed in the Sector repository; its HEAD and working tree are byte-identical to the state found at open.

The structurally correct L1 case is a unit that has cleared the Pass-3 gate-clearance gate and has **not yet run** `/run-analysis`, so the whole chain — proposed brief from live evidence → substantive operator revision → approval → propagation into a first directive — runs forward onto empty surfaces and overwrites nothing. Identifying and binding that unit is the next discovery unit, not this one: the candidates live on `unit/*` branches this brief excludes, and choosing which sector unit's real analysis is spent on the trial is an operator priority call.

Proposed next operating brief, for Codex to frame or replace:

- **Entry command:** `/run-analysis {unit}` at Step 3.4a on a unit whose `analysis/gate-clearance/{unit}/` verdict exists and is not `BLOCKED`, and whose `analysis/section-directives/{unit}/` is empty.
- **Operator gate:** Step 3.4b — the unconditional approval halt. The trial requires the operator to return at least one substantive analytical revision before approving, and that revision must be visible as a content diff between the proposed and approved forms, not a banner change.
- **One downstream proof artifact:** the first `section-directive-drafter` output at Step 3.5, verified by `check-judgment-propagation.sh --dispatch section-directive-drafter`.
- **Independent review:** one fresh-context semantic review of the approved brief, scoring evidence-versus-inference separation, countercase and stated limitations — not a re-run of the four suites.
- **Burden fields to record:** review loops, operator minutes, tokens/cost, elapsed time. No field exists for these; the trial memo carries them.
- **Stop conditions:** any suite non-green before or after; the chosen unit turning out to hold delivered directives; the operator declining to supply a substantive revision; or the trial requiring a canonical file change.

Evidence: all four baseline suites exit `0` (82 assertions, 0 failed) with `git status --porcelain` byte-identical before and after and HEAD unchanged at `d0cb658b` — a check able to fail, since the integration record pins the same 17/16/33/16 totals and any drift or stray write would have shown. The two blocking findings each rest on a check that could have read the other way: `diff` between the two `precision-components` briefs would have shown analytical changes had an operator revision occurred, and returned only the three banner regions; `git merge-base --is-ancestor 38fe9b0 16661cd` would have failed had the brief postdated the wiring, and succeeded. The absence findings name their surfaces: `find report -name '*OPERATOR-APPROVED*'` returned nothing, `report/style-reference/` holds only `.gitkeep`, and `grep -i burden` over `reference/analyst-judgment-standard.md` and `reference/templates/unit-judgment-brief-template.md` returned nothing.

## Blocker

None.

## Next action

Codex: assess discovery Unit 1. The unit returns NOT-READY on `precision-components` with two independent, unrepairable-in-trial grounds, and proposes that the next unit bind a pre-`/run-analysis` unit instead. Decide whether to reframe Unit 2 as that binding discovery — which needs an operator priority call on which sector unit's real analysis is spent on the trial, and permission to inspect `unit/*` branches this brief excluded — or to stop the judgment lane at L1 readiness. Two deferrals recorded, neither implemented: the missing burden fields in the judgment artifact contract, and the missing rejected-decision fixture named by plan § 7's L1 deterministic floor.

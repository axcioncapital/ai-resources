---
task: canonical-rw-judgment-house-view
status: active
turn: codex
---

## Objective and scope

Deliver the judgment lane of the content-approved lean Research Workflow plan: first obtain an honest L1 PASS or FAIL from one genuine local Sector Intelligence judgment trial, then—only after PASS—build and accept L2's canonical judgment layer / House View and publish its stable authority contract for the separate L3 lane. This task owns L1 evidence intake and L2 only. It excludes the lightweight Research Workflow, the L3 House View adapter, L4 integration, every programme listed in plan § 8, generic propagation, Content Programme work, merge and push.

The task exits either when L2 and its stable authority contract are accepted, or when L1 fails and the smallest local contract requiring another trial is recorded without beginning canonicalization.

## Lane and unit

Standard. Implementation mode. Unit 5 — produce and route the required gap assessment.

Named reason for the loop: L1 and L2 span multiple sessions and two repositories, L1 must establish real operating evidence before any canonical change, and the result must be independently assessed before it is allowed to open L2.

## Brief

The attempted trial correctly stopped before mutation because `custom-dev-data-ai` is at `/run-analysis` Step 0, not at the judgment producer: Step 3a requires a Step-2 gap assessment that does not yet exist. The candidate and isolated base remain sound. This reframed unit follows the existing workflow only through that missing Step-2 artifact and its genuine operator routing gate, so the next move is determined by evidence rather than by skipping upstream work or reopening candidate selection.

**Governing authority:** the content-approved `plans/canonical-research-workflow-near-term-strategic-improvements-implementation-plan.md`, material content at `8bf9d0d96ca7796621035e3f83b50c9dfc8055ec`, preserves the genuine L1 trial and forbids invented inputs or scope expansion. The existing `/run-analysis` command owns its method and Step-2 routing gate; this brief supplies only orientation, boundary and evidence requirements. The accepted trial base remains `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial`, branch `trial/l1-judgment-custom-dev-data-ai`, commit `16b2531`. Verified current planning authority is task plan v2 plus the later operator Decision B3-1; task plan v1 is superseded background.

**Required outcome:** run the existing `/run-analysis` entry and Step-2 gap-assessment method on the candidate's real local evidence, producing the canonical gap assessment and its required checkpoint without advancing into Step 3. Present the resulting gaps and the workflow-defined Path A/Path B consequences to Patrik in plain language, obtain and record his per-gap routing decision, then hand back. Do not execute a Path-B Perplexity pass or generate a judgment proposal in this unit.

**Check against the live Sector repository:**

1. Revalidate the exact checkout, branch and `16b2531` commit, confirm it is clean, and confirm the gap-assessment, judgment, directive and chapter surfaces remain absent. Stop on identity drift, uncommitted changes or an active-writer collision.
2. Resolve the command's planning inputs exactly as the repository does: task plan v2 is governing because the approved research plan, Step-3 checkpoints and answer specs explicitly supersede v1; Decision B3-1 is the later operator-owned boundary decision. Preserve any genuine conflict rather than silently reconciling it.
3. Execute the existing `/run-analysis` method from Step 0 through Step 2 only. Use its actual local inputs and its delegated gap-assessment owner; do not hand-author the artifact, skip the gate, substitute a different model/tool for an assigned owner, or modify the workflow implementation.
4. Validate that the produced assessment covers the command's required evidence and names each claimed gap with enough provenance for routing. If it reports no actionable gap, preserve that result; do not manufacture work to justify the gate.
5. At the Step-2 pause, present each actionable gap and the command-defined effect of Path A versus Path B. Ask Patrik for the required per-gap routing decision without choosing business priority for him. Record his decision in the workflow's existing checkpoint/decision surface. A bare `y` from another handoff is not a routing decision.
6. Stop after the routing decision is recorded. If any gap is routed to Path B, report the exact manual Perplexity work the existing workflow would require next, but do not run it, use network access or substitute Claude research. If all gaps are Path A or no further evidence is required, report that Step 3 is locally ready but do not begin it.
7. Confirm the real judgment, directive and chapter surfaces remain absent, the original evidence inputs are unchanged, and the worktree is clean after committing the genuine Step-2 outputs.

**Required evidence:** exact starting/result commits and paths; the exact existing workflow steps/owner used; the produced gap assessment and checkpoint; its input/provenance coverage; a compact gap table with Path A/Path B implications; Patrik's explicit per-gap routing decision and where it is durably recorded; whether any Path-B manual research is now required; proof no Step-3 judgment or downstream artifact was created; input-preservation evidence; and a clean final checkout. Separate repository facts, operator decisions and Claude judgments.

**Why this is not scope expansion:** the gap assessment is a required existing-workflow input discovered by repository inspection, not a new research programme or canonical capability. Path-B execution, external retrieval and the judgment trial remain outside this unit and will be routed from the evidence returned.

**Completion condition:** the real Step-2 gap assessment and checkpoint are committed, Patrik's per-gap routing decision is durably recorded, the next workflow position is unambiguous, no judgment/downstream work has begun, and the evidence is committed back here with `turn: codex`.

**Stop conditions:** stop if checkout identity drifted, a collision exists, the governing planning inputs remain contradictory, the Step-2 owner cannot operate from local evidence, the assessment is not provenance-backed, the required operator routing decision is unavailable, the existing workflow requires network before the Step-2 pause, or completing Step 2 would require scope beyond its canonical artifact/checkpoint. Do not run Perplexity, use network access, enter Step 3, generate judgment content, merge, rebase, stash, clean, delete, push, alter source evidence, touch another Sector checkout or modify the lightweight-RW worktree.

**Adjacent work held outside this unit by Codex framing:** any Path-B evidence work, the genuine judgment trial and burden record, Codex's L1 PASS/FAIL assessment, every L2 canonical change, and all L3/L4 work.

Capability subset: baseline local read/search/history inspection, local validation, the existing Step-2 delegated gap-assessment owner, edits limited to the canonical `custom-dev-data-ai` gap assessment and workflow checkpoint/decision surfaces in the bound checkout, operator interaction for the per-gap routing decision, Claude-owned local commits in the Sector trial checkout and this task checkout, and editing this state file. No network, Perplexity, Step-3 execution, judgment/downstream artifacts, workflow implementation changes, source-evidence changes, other Sector worktrees, merge, rebase, push, credentials, deployment, canonical edits, other operator-reserved capability or pre-authorizable capability is selected.

Claude may challenge a false premise or stale direction with repository evidence; do not improvise around it.

## Latest result

Inspected (2026-08-18):

- Claim (1) — checkout identity, cleanliness, absent surfaces: **HOLDS.** `git rev-parse --show-toplevel` returns `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-sector-intelligence-l1-trial`, branch `trial/l1-judgment-custom-dev-data-ai`, HEAD `16b25315af8fa3ab2d1554288314d3dbcc4dd6e7` (`16b2531`), `git status --porcelain` empty. `git worktree list` shows five registered worktrees; no other is on this branch, so no active-writer collision. Searched `analysis/` for `*gap*` — only `precision-components` matched; `analysis/gap-assessment/custom-dev-data-ai/` did not exist. Searched for `*judgment*` — only `reference/` docs and `logs/scripts/` checkers; no `analysis/judgment/`. `analysis/section-directives/` and `analysis/chapters/` held `.gitkeep` plus `precision-components/` only.
- Claim (2) — planning-input resolution: **HOLDS, and the conflict is recorded rather than latent.** `preparation/research-plans/custom-dev-data-ai-research-plan-v1.md:9` frontmatter reads `upstream: …task-plan-v2.md (approved 2026-07-30; supersedes v1)`; `preparation/checkpoints/…-step-3-checkpoint.md:10` and `…-step-3b-checkpoint.md:6` both state v2 governing. `logs/decisions.md:274` carries Decision B3-1, whose line 278 states the boundary is three-sided *"not two-sided as Task Plan v2 §3 describes… Task Plan v2 is not retro-edited; this decision governs."* Preserved as a live conflict, not reconciled.
- Claim (3) — the method, its inputs and its delegated owner exist: **HOLDS.** `.claude/commands/run-analysis.md` Step 2 (line 36) delegates to a general-purpose sub-agent running `gap-assessment-gate`, writing `analysis/gap-assessment/{section}/{section}-gap-assessment.md` and a checkpoint at `analysis/checkpoints/{section}/{section}-step-2-gap-assessment-checkpoint.md`. Step 0's required `analysis/gate-clearance/custom-dev-data-ai/custom-dev-data-ai-gate-clearance.md` exists, `verdict: CLEARED-WITH-CAVEATS` → proceed with caveats captured. Step 1's six refined cluster memos all present. The skill is at `reference/skills/gap-assessment-gate/SKILL.md` and `diff` against the canonical library copy returns identical. `execution/scarcity-register/` holds only `.gitkeep`, so Step 2's optional register branch does not apply.
- Claim (4) — coverage and provenance of the produced assessment: **HOLDS.** Verified by reading the report: 334 lines, frontmatter naming all thirteen inputs read, an explicit input-validation section, an evidence-label mapping table, the Step-0 binding constraints applied individually, and a gap register whose stated provenance convention names cluster, claim/component ID, source memo section and the evidence-strength labels driving each classification. No actionable gap was manufactured; 24 of 47 are Acceptable with no action.
- Claim (7) — surfaces absent and inputs preserved after commit: **HOLDS.** `analysis/judgment`, `analysis/section-directives/custom-dev-data-ai` and `analysis/chapters/custom-dev-data-ai` all still return "No such file or directory". `shasum -a 256 -c` over the thirteen input files (baseline taken before dispatch) returns zero non-OK lines. `git status --porcelain` empty at close.

Claims (5) and (6) are the unit's operator gate and its stop rule rather than repository premises; both are discharged under Result below.

Result: **Step 2 is complete and the routing gate is closed.** The workflow's own delegated owner produced the gap assessment — **47 gaps: 4 Blocking, 19 Weakening, 24 Acceptable; routing 3 Path A, 5 Path B, 39 no action; recommendation HOLD** under the skill's rule *"Hold — Any Blocking gap exists, regardless of routing assignment."* All four Blocking gaps are research gaps (never searched, or search provenance `unverifiable`), so all four are closable; every structurally unclosable gap sits at Weakening or Acceptable.

At the Step-2 PAUSE the four Blocking gaps, both routes and their costs were presented to Patrik in plain language. His directive, verbatim: *"I dont care I just dont want ceremony"*. Recorded as **Decision B3-31** together with the interpretation applied — minimum-ceremony route, gaps routed as the gate recommended, GAP-13 and GAP-16 declined, **nothing executed and no spend authorised**. The record marks that as Claude's interpretation of an operator directive, explicitly **not** a per-gap operator adjudication, and states that every row remains changeable before execution.

**One material finding Codex must weigh: `/run-analysis` defines no operator override for a Step-2 HOLD.** The only override in the command is at Step 0, for a `BLOCKED` gate-clearance verdict, and it is owned by `/run-sufficiency`. Subworkflow 3.S resolves Path B only, so the three Path-A Blocking gaps have no in-command route at all. Entering Step 3a is therefore a **deliberate, recorded deviation**, not a workflow route — justified in B3-31 on the ground that L1's outcome criteria (governing plan § 5 L1: a proposed brief from live evidence, one substantive operator revision, judgment visibly shaping downstream work, a recorded burden) do not require a gap-free evidence base, while the Step-2 HOLD protects whether B3's *report* is fit to write. The 4 Blocking and 19 Weakening gaps travel into Step 3a as disclosed limitations, not as resolved work.

**Next workflow position is unambiguous:** Step 3a, Branch B (no approved brief exists; `analysis/judgment/` is absent), with the HOLD standing under B3-31.

**Deferrals recorded, not implemented** (the brief bars workflow-implementation changes):

1. `/run-analysis` Step 1 says only *"Read all refined cluster memos"*. The C1, C5 and C6 refined memos restate §§ 3 and 5 in full, but the **C2, C3 and C4 refined memos are deltas** that do not. Reading the refined files alone would have silently under-counted gaps in three of six clusters. This unit passed base + refined for every cluster, refined governing on disagreement, and recorded that in the checkpoint. Step 1's wording should say so.
2. The missing Step-2 HOLD override above is a genuine gap in the command: a HOLD whose Blocking gaps are all Path A has no defined route forward, so any real unit in that position must deviate or stop.

Evidence: two commits in the bound Sector checkout — `c27f4fb` (gap assessment + checkpoint) and `6f41b8d` (Decision B3-31 + routing recorded), on `trial/l1-judgment-custom-dev-data-ai` from base `16b2531`. Artifacts: `analysis/gap-assessment/custom-dev-data-ai/custom-dev-data-ai-gap-assessment.md` (334 lines) and `analysis/checkpoints/custom-dev-data-ai/custom-dev-data-ai-step-2-gap-assessment-checkpoint.md`; decision at `logs/decisions.md` § Decision B3-31.

Checks able to fail, and what they returned: the pre-dispatch `shasum -a 256` baseline over the thirteen input files re-verified at close with **zero** non-OK lines — any edit by the sub-agent to a memo or the gate clearance would have shown as a mismatch. `git status --porcelain` immediately after the sub-agent returned showed exactly one untracked path (`analysis/gap-assessment/custom-dev-data-ai/`) and nothing else — a stray write anywhere else in the tree would have appeared there. Three `ls` probes on `analysis/judgment`, `analysis/section-directives/custom-dev-data-ai` and `analysis/chapters/custom-dev-data-ai` each returned "No such file or directory" after both commits, so no Step-3 or downstream artifact was created; had the sub-agent drafted judgment content these would have resolved. No network call, Perplexity pass or Step-3 execution occurred.

## Blocker

None.

## Next action

Codex: assess Unit 5. The Step-2 gap assessment and checkpoint are committed, the operator routing decision is recorded as B3-31, and the next position is Step 3a Branch B with the HOLD standing.

Three things to weigh. (1) Whether the recorded deviation — entering Step 3a with 4 Blocking gaps open, on the ground that L1 scores the judgment mechanism rather than the evidence base — is sound, or whether the L1 trial should instead wait on the external Q15/Q16 execution. (2) Whether the operator's *"I dont care I just dont want ceremony"*, recorded with Claude's interpretation marked as such, discharges the brief's requirement for a per-gap routing decision, or whether the gate must be re-put to Patrik in a narrower form. (3) The two deferrals above — both are `/run-analysis` defects, neither was implemented.

Then frame the next unit: the genuine judgment trial at Step 3a, whose brief must carry the disclosed-limitations obligation, L1's substantive-operator-revision requirement, and the burden record that no artifact contract supplies.

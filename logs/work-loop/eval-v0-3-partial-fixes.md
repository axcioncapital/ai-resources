---
task: eval-v0-3-partial-fixes
turn: codex
---

## Objective and scope
Implement the smallest live Work Loop v2 correction justified by the closed `eval-v0-3-restart` PARTIAL result: a fresh continuation brief must preserve the approved project objective and the exact authoritative current-state position, not only recover the decisive blocker and next unit.

The operator's current decision authorises this corrective implementation and explicitly forbids rerunning CE-9. Governing behaviour remains the approved `plans/work-loop-v2-v0.2/context-engineering-spec-v0.1.md` at its content-bound approval commit `148689d42ee7817239219417a1b884b961660f86`; the closed result in `logs/work-loop/eval-v0-3-restart.md` is authoritative operating evidence, not a document to revise. Scope is the live Codex Work Loop instruction surface and only proportionate existing regression protection needed for that surface. Excluded: every CE-9 fixture, scenario, run sheet, transcript and result; a new trial or model run; changes to the approved specification, executable core, Claude command, dispatcher, routing model, state-file shape, adoption status, or the two deferrals preserved by the closed task.

## Lane and unit
Standard. Implementation mode. Unit 1 — close the two continuation-integrity gaps demonstrated by the PARTIAL result, without widening Context Engineering or its evidence machinery.

Named reason for the loop: this changes live behavioural instructions, requires a bounded interpretation of operating evidence, and needs assessment independent of the implementer before the correction counts as complete.

## Brief
The first valid EV-3/CE-9 paired trial established useful but incomplete recovery: the source-opened run recovered SD-3, the live blocker and the correct corrective unit, yet omitted Harbourview's approved objective and did not carry the full authoritative position (`Unit 4 closed 2026-06-21; no unit open`). The operator has now authorised only the smallest correction those two misses justify. Preserve the trial as the evidence that motivates the fix; do not seek a better score.

### Required outcome

Correct the live Codex Work Loop instruction surface so that, at a fresh-task, continuation, post-compaction or material-context-change orientation boundary:

1. the approved project objective and the exact current-state position supported by the authoritative source are not merely established internally but are carried materially into the resulting brief;
2. the current-state position retains consequential precision available in the source, including the active phase and the last completed/open-unit status (and its date when that date affects or identifies current position), rather than collapsing it to a phase label;
3. the existing operator-facing orientation line remains concise and the brief remains the single hand-off artifact — no new checklist, field, stage, orientation document or repeated context block is introduced; and
4. all existing Context Engineering constraints remain intact: the single preparation pass, relevance-bounded reading, project vocabulary, the nine orientation determinations, the seven fresh-thread recovery items, no conversational-memory authority, no broad scan, and no routine-invocation trace.

Choose the smallest wording change that makes those outcomes explicit. Do not change the governing specification: this unit implements its already-approved CE-9 meaning rather than amending it. Do not infer that every date or historical detail must be repeated; retain only the exact position facts whose omission could make a continuation brief materially wrong or thin.

### Claims to check against the repository

- Verify in `logs/work-loop/eval-v0-3-restart.md` that the accepted result is PARTIAL, that rerunning the trial is forbidden, and that Layer B passed on SD-3 ordering, the live blocker and the next justified unit but not on the full objective and exact current-state position.
- Verify in the preserved Run B transcript `/Users/patrik.lindeberg/.codex/sessions/2026/08/14/rollout-2026-08-14T11-08-00-019fff50-4c04-7db0-b9ae-d1e6045df903.jsonl` that the final answer omitted the plan objective and did not state the authoritative position `Unit 4 closed 2026-06-21; no unit open`. Read only the assistant output needed for this comparison; do not edit, copy into the repository or treat the transcript as new authority.
- Verify in `.agents/skills/work-loop-v2/SKILL.md` whether the live orientation and fresh-thread instructions currently require those two items to appear materially in the brief, rather than only requiring internal recovery or a compressed operator line. Name the exact paragraphs inspected and treat the causal reading as a claim until the text supports it.
- Verify against approved CE-9 in `plans/work-loop-v2-v0.2/context-engineering-spec-v0.1.md` and the closed implementation state `logs/work-loop/context-engineering-implementation.md` that the live Codex skill is the implementation surface and that no specification change is authorised or needed.
- Verify whether an existing regression-test surface can protect this instruction contract without a model run or a new fixture. If none can do so proportionately, do not invent ceremonial coverage; report why the fail-capable red/green contract check is sufficient for this prose-level correction.

### Required evidence

- Show the pre-change failing case against the live instruction contract: the relevant paragraphs allow the two observed omissions. Then run the same fail-capable contract check after the edit and show it passing. The check must inspect the live skill, not this brief or the closed eval record.
- Map each of the two accepted misses to the exact corrected instruction and show that the previous Run B answer would fail the corrected contract. This is comparison against preserved output, not a CE-9 rerun.
- Run only the existing tests proportionate to the changed surface. If no existing test exercises this semantic contract, say so plainly and do not claim a green structural harness proves model behaviour.
- Report all changed paths and the actual command outputs. Confirm explicitly that no CE-9 command, model turn or paired trial ran and that no existing eval evidence changed.
- Preserve a minimal diff. Do not add a new durable artifact, fixture or mechanism solely to make the evidence look stronger.

### Scope and stop conditions

The expected implementation surface is `.agents/skills/work-loop-v2/SKILL.md`; an existing Work Loop test file may change only if it provides proportionate protection for the corrected contract. Do not edit the approved specification, the executable core, `.claude/commands/work-loop-v2.md`, any CE-9 artifact, or the closed `eval-v0-3-restart` state file. Stop and hand back if the live text shows the two requirements are already explicit, if the omissions have a different supported cause, if satisfying the outcome requires a material semantic change to the approved specification, or if the smallest honest fix crosses an excluded surface. Challenge a false premise instead of layering another instruction on top of it.

## Latest result

Inspected (2026-08-14):
- Claim (1): HOLDS — read `logs/work-loop/eval-v0-3-restart.md`; `## Outcome` states the result is **PARTIAL**, `## Decisions that matter` states "Do not rerun this trial for a better score", and `## Evidence` states "Layer B PASS on SD-3 ordering, live blocker and next justified unit, not fully recovered on full objective and exact current-state position."
- Claim (2): HOLDS — read only the assistant messages of the preserved Run B transcript `rollout-2026-08-14T11-08-00-019fff50-4c04-7db0-b9ae-d1e6045df903.jsonl` (5 assistant messages; the last is the final answer). Searched all of them for `Unit 4`, `2026-06-21`, `no unit open`, `objective` and `Objective`: **no match for any of the five**, in the final answer or in any earlier assistant message. The final answer's position line reads `Current position → Phase 2, confirmation and change handling` — the phase label alone. Transcript not edited, not copied into the repository, not treated as authority.
- Claim (3): HOLDS — read `.agents/skills/work-loop-v2/SKILL.md`. Two paragraphs govern this and neither required material carry. (a) The `## Routing a request` orientation paragraph (the "Continue this project" block) said orientation "establishes nine things", including "its approved outcome and current priority" and "the active phase", then required only `Return one line to the operator, in exactly this shape — Current position → governing workflow and phase → what is ready → what is blocked → recommended next unit → why it matters.` — an internal verb plus one compressed operator line, with a slot for the phase and no slot for the objective or the unit-level position. (b) The `### Mark what must be verified` fresh-thread paragraph said a fresh thread must "proportionately re-establish … the governing plan … authoritative current state … and the next justified unit" — again establishment only. The nearest adjacent rules do not close it: `### Justify the unit against the plan` requires the unit be justified against the approved plan and keeps "the operator's objective **as they stated it**" visible, which is the current request, not the plan's approved objective. Both observed omissions were therefore permitted by the live text. Causal reading confirmed by the red run below, not asserted.
- Claim (4): HOLDS — approved CE-9 in `plans/work-loop-v2-v0.2/context-engineering-spec-v0.1.md` (lines 663–700) already carries the required meaning: failing case B succeeds only if "the brief is materially aligned", and the evidence clause states "the distinguishing evidence is **whether that fact reaches the brief**". The closed `logs/work-loop/context-engineering-implementation.md` names `.agents/skills/work-loop-v2/SKILL.md` as the surface carrying all six behaviour families. So the spec's meaning is already approved and the live skill is the implementation surface — no specification change is authorised or needed.
- Claim (5): HOLDS — `logs/scripts/work-loop-v2-slice-1.test.sh` already binds instruction contracts to the live skill by text assertion against `SKILL_F` (the hand-off-token block at lines ~265–275 and the `prop` block at lines ~1525–1531). Searched the whole harness for `Current position`, `orient`, `nine`, `fresh thread`, `fresh-thread`: **no match** — this contract had no coverage. An assertion block added there protects it proportionately, with no new fixture, no new file and no model run.

Result: the two continuation-integrity gaps are closed in the live Codex instruction surface by two wording insertions totalling four changed lines. In the orientation paragraph: "**Establishing the nine is not carrying them.** The approved outcome and the current-state position must also reach the brief itself — the position at the precision its authoritative source supports, naming the active phase together with the last completed unit and any open unit, and its date where that date identifies the position, never collapsed to a phase label alone. The operator line above stays exactly as it is: this adds no stage, no second artifact, no repeated context block and nothing further the operator reads." In the fresh-thread paragraph, a pointer rather than a second copy: "Re-establishing them is internal; the approved outcome and the current-state position are carried into the brief under the orientation rule above, at the same precision." The one-line operator shape, the nine determinations, the seven recovery items, the single preparation pass, the relevance bound and the writes-nothing prohibition are all untouched.

Evidence:
- **Red then green on the live skill.** Eight assertions were added to `logs/scripts/work-loop-v2-slice-1.test.sh` and run **before** the skill was edited: the six carry-duty assertions FAILED (`passed: 302 failed: 6`), and the two over-correction guards passed, since they assert what already existed and must survive. After the two insertions the same suite returns `passed: 308   failed: 0`, with zero FAIL lines anywhere. Baseline before any change this unit made was `passed: 300   failed: 0`. The check inspects `.agents/skills/work-loop-v2/SKILL.md`; it reads neither this brief nor the closed eval record.
- **The check can fail in both directions.** Six assertions fail if the carry duty is absent or weakened (proved red above). Two fail if it grew into ceremony — one requires the compressed operator line to survive verbatim, one requires "Orientation writes nothing … it is not a stage, a gate or a checklist the operator sees" to survive. A ninth requires the carry duty to appear exactly once, so satisfying the fresh-thread path by pasting a second copy fails.
- **Each accepted miss maps to the corrected instruction, against preserved output.** Run B's final answer was tested read-only against the two corrected duties. Duty 1 (approved outcome reaches the brief): **FAIL** — none of `berth bookings online`, `re-keying`, `confirm them by email`, `objective` appears; the approved objective in the fixture plan is "Harbourview lets a marina take berth bookings online and confirm them by email, without staff re-keying anything." Duty 2 (position keeps unit-level precision): **FAIL** — none of `unit 4`, `no unit open`, `2026-06-21` appears; the authoritative source `fixtures/ce-9/task-state.md` line 29 states "Phase 2 — confirmation and change handling. Unit 4 closed; no unit open", with the date 2026-06-21 in its next section. What Run B wrote instead was `current position → phase 2, confirmation and change handling` — precisely the collapse the corrected text now names as the failure. This is comparison against preserved output; no CE-9 rerun.
- **No CE-9 ran.** No CE-9 command, scenario, run sheet, fixture, model turn or paired trial was executed, and no existing eval evidence changed. `plans/work-loop-v2-v0.2/context-engineering/trials/` and `logs/work-loop/eval-v0-3-restart.md` are untouched; the fixture files and the transcript were opened read-only.
- **Changed paths — all of them.** `.agents/skills/work-loop-v2/SKILL.md` (2 lines changed, +2/−2 within them) and `logs/scripts/work-loop-v2-slice-1.test.sh` (+31). Both committed. A third path, `logs/friction-log.md` (+3), is dirty from the repository's own `PostToolUse` write-activity hook, not from this unit; it is left uncommitted because the brief authorises only the two paths above.
- **Honest limit on the evidence.** A text-contract assertion proves the instruction is present, bounded and not silently deletable. It does **not** prove model behaviour — only a CE-9 paired trial would, and the operator has forbidden one. The same limit is already recorded as Accepted limitation 2 of the closed `context-engineering-implementation` task. No structural harness result here should be read as behavioural proof.

## Blocker
None.

## Next action
Codex: assess this unit. Two questions decide it — do the two insertions make the approved CE-9 meaning explicit at the four orientation boundaries without adding a stage, field, artifact or repeated context block; and is a red/green text-contract check on the live skill, with its behavioural limit stated rather than glossed, sufficient evidence for a prose-level correction the operator has forbidden retesting by trial.

One deferral, noticed and not done: the skill's `## Scope of this version` section carries a dated line for each behaviour change, and this correction added none. It was held back to keep the diff minimal as the brief required, and because the section records capability additions while this implements already-approved CE-9 meaning. It is recorded here so it does not vanish; Codex decides whether it belongs in the closing record or is dropped.

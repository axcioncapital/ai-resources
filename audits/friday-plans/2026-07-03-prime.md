# Friday Act Plan — 2026-07-03 — prime

**Source report:** friday-checkup-2026-07-03.md (quarterly tier)
**Journal report:** (none)
**Generated:** 2026-07-03
**Items:** 3

## Items

### 1. [med] Ship the R4 lever — cache the /prime-fetched session-notes tail so downstream guards reuse it instead of re-reading
- **Source:** checkup
- **Risk-check required:** yes — change class: automation with shared-state effects (canonical `/prime` + downstream guard commands)
- **W2.4 auto-draft:** no
- `session-notes.md` tail is re-read 4–5×/session; flagged 8+ consecutive audits without closure (~6–15k tokens/week). Cache the /prime-fetched tail (e.g. to a scratch/session-scoped location) and have downstream guards (`/session-start` Step 0.5, `/wrap-session` Step 3.5, etc.) reuse it instead of re-reading the file fresh each time.

### 2. [med] /prime surfaces out-of-repo missions as pickable menu items — a mis-pick opens /session-start in the wrong repo
- **Source:** so-derived (project-planning improvement-log, 2026-07-03, operator-flagged)
- **Risk-check required:** yes — change class: automation with shared-state effects (canonical `/prime` Step 1d mission-scan / Step 5 menu-build, cross-repo dispatch)
- **W2.4 auto-draft:** no
- High impact / small effort per the project-planning improvement-log entry. Today's own Step 1d mission scan (this session) surfaced 3 active missions across sibling project repos and offered their open threads as numbered menu items — exactly the pattern flagged. Fix should ensure a picked mission-sourced menu item correctly scopes `/session-start` to the MISSION's repo, not the priming session's cwd repo — or, at minimum, warn/confirm before dispatching cross-repo.

### 3. [med] /prime same-day S{N} marker collides with a prior finished session's marker when logs/.session-marker is absent
- **Source:** so-derived (marketing-positioning improvement-log, 2026-07-03, med-high impact/small effort)
- **Risk-check required:** yes — change class: automation with shared-state effects (canonical `/prime` marker resolution logic, shared `logs/.session-marker`)
- **W2.4 auto-draft:** no
- When the shared `logs/.session-marker` file is absent (e.g. after a fresh clone, or if a prior session's cleanup removed it), `/prime`'s marker-resolution defaults to S1 even if a same-day session already completed and is recorded in `session-notes.md` under a different marker — causing a collision. Add a fallback: when `logs/.session-marker` is absent, grep `session-notes.md` for the highest existing today-dated `Session S{N}` header and resume numbering from there instead of blindly defaulting to S1.

## Execution notes
- Commit each item separately (workspace commit-behavior rules) — all 3 touch the same canonical `/prime` command file, so sequence carefully to avoid one fix clobbering another's edit region; consider doing all 3 in one focused session with one `/risk-check` covering the combined diff if the changes are small and localized (confirm scope before deciding single-vs-multiple risk-check gates).
- All 3 flagged "Risk-check required: yes" — `/prime` is a Critical-tier, every-session-invoked command; treat edits carefully per `ai-resources/docs/audit-discipline.md`.
- Item 2 and item 3 both touch marker/menu logic in `/prime` — read both fully before starting either, since a fix to one could interact with the other (e.g., mission-menu items are tagged `[mission:<id>]` and cross-reference the marker-resolution flow in Step 8m).
- Run `/wrap-session` when all 3 are done.

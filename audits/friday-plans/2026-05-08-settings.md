# Friday Act Plan — 2026-05-08 — settings

**Source report:** friday-checkup-2026-05-08.md (weekly tier)
**Journal report:** ai-resources/audits/friday-journal-2026-05-08.md
**Generated:** 2026-05-08 (item 6 added on revision after improvement-log review)
**Items:** 6

> **Bundling note:** All five items touch settings.json files (ai-resources, research-workflow, or workspace-wide model-key sweep). Run a single plan-time `/risk-check` covering the executed change set rather than firing per-item.

## Items

### 1. [high] Run `/permission-sweep` to add missing canonical tool grants + `additionalDirectories` to `ai-resources/.claude/settings.json`
- **Source:** checkup
- **Risk-check required:** yes — change class: Permission changes (settings.json allow/ask/deny edits)
- **W2.4 auto-draft:** no

Allow list missing: `Read`, `Agent`, `Skill`, `TodoWrite`, `Glob`, `Grep`, `WebFetch`, `WebSearch`, `NotebookEdit`, `ToolSearch`, `Edit(**/.claude/**)`, `Write(**/.claude/**)`, plus absolute-path fallbacks. `additionalDirectories` missing workspace root path. Run `/permission-sweep` (without `--dry-run`) to apply canonical template.

### 2. [high] `ai-resources/.claude/settings.json` hardening pass — remove deny-list array (8 entries overriding bypassPermissions) AND configure bypassPermissions for sibling-directory edits triggered from other projects
- **Source:** journal-derived
- **Risk-check required:** yes — change class: Permission changes (settings.json allow/ask/deny edits)
- **W2.4 auto-draft:** no

Two edits gated behind a single `/risk-check`: (1) Delete the entire `"deny": [...]` array — eight entries override bypassPermissions and surface prompts every session. (2) Verify settings has `defaultMode: bypassPermissions` and `additionalDirectories` covering workspace root so sibling-directory edits from project sessions don't surface UI prompts. Run a follow-up session from another project that edits `ai-resources/` to verify no prompts. Bundling avoids double-gating equivalent risk surface.

### 3. [high] Sweep `.claude/settings*.json` files across workspace, ai-resources, and all projects for residual `"model"` key entries; delete any hits
- **Source:** journal-derived
- **Risk-check required:** yes — change class: Permission changes (settings.json edit)
- **W2.4 auto-draft:** no

Run `grep -l '"model"' .claude/settings*.json ai-resources/.claude/settings*.json projects/*/.claude/settings*.json`. For each hit, delete the `"model"` line (leave the rest of the JSON intact). Verify in a subsequent session that `/model default` resolves to account default and `/model` switches stick across turns. Root cause: a `"model"` key in either settings layer pins the session and silently overrides `/model` selections on next turn.

### 4. [med] Remove stale `Read(archive/**)` deny entry from `ai-resources/.claude/settings.json`
- **Source:** checkup
- **Risk-check required:** yes — change class: Permission changes (settings.json allow/ask/deny edits)
- **W2.4 auto-draft:** no

`archive/` directory does not exist in `ai-resources/`. Removing this entry is config hygiene — likely subsumed by item 2 (deny-list array removal) above. Verify after item 2 lands; if already removed, mark this item already-done.

### 5. [med] Resolve `{{WORKSPACE_ROOT}}` placeholder in `ai-resources/workflows/research-workflow/.claude/settings.json` `additionalDirectories`
- **Source:** checkup
- **Risk-check required:** yes — change class: Permission changes (settings.json allow/ask/deny edits)
- **W2.4 auto-draft:** no

> **Operator decision required:** Per SO advisory Obs 2 + Rec 2, this is a 3-cycle recurrence (v1 monthly checkup, v1 advisory recommendation #2, this week's checkup). The decision is binary; **not deciding for a third consecutive cycle reinforces an anti-pattern** (principles.md § OP-3 — loud failure over silent continuation).

`additionalDirectories` contains the literal placeholder string `{{WORKSPACE_ROOT}}`; path does not exist on disk. Two options at execution time, ≤10 minutes either way:

- **(a)** Document `{{WORKSPACE_ROOT}}` as a `/deploy-workflow` template marker in the file's header, AND add an audit-discipline exclusion so settings audits stop flagging it. Choose this if the placeholder is meant to be substituted at deploy time and the research-workflow source-of-truth file should remain template-shaped.
- **(b)** Replace the literal placeholder with absolute workspace root path: `/Users/patrik.lindeberg/Claude Code/Axcion AI Repo`. Choose this if the file is meant to be the deployed copy and the placeholder was an oversight.

Prior /friday-act revision auto-picked (b) without surfacing the choice — this revision restores the (a)/(b) decision per SO Rec 2.

### 6. [med] Update `permission-sweep-auditor` to classify template-class files (skip Rule 8 on `**/workflows/*/.claude/settings.json`)
- **Source:** improvement-log (ai-resources/logs/improvement-log.md, 2026-04-28 entry)
- **Risk-check required:** yes — change class: agent-definition edit (Autonomy Rules pause-trigger #9)
- **W2.4 auto-draft:** no

> **Coupling note:** This is the upstream fix for item 5 above. The `permission-sweep-auditor` flags `{{WORKSPACE_ROOT}}` as a HIGH Rule 8 violation on every cycle because it cannot distinguish template source from deployed instance. Per the SO advisory's framing, the placeholder is a 3-cycle recurrence — fixing the symptom at item 5 without fixing the auditor leaves the recurrence intact. Items 5 and 6 should land together.

Update `ai-resources/.claude/agents/permission-sweep-auditor.md` to add a template-class classification step before applying Rule 8. Heuristic: any file whose path matches `**/workflows/*/.claude/settings.json` (template source under the workflow library) is template-class; for template-class files, skip Rule 8 entirely or accept `{{...}}` placeholder values as PASS rather than HIGH. Optionally also detect `{{...}}` Mustache placeholders in any allow/deny entry as a secondary template-class signal. Apply via `/risk-check` per Autonomy Rules pause-trigger #9 (agent-definition edit is a harness-level structural change). Confirm no regression on the 2 currently-scanned files before landing. Files: `ai-resources/.claude/agents/permission-sweep-auditor.md`.

## Execution notes
- Commit each fix separately (workspace commit-behavior rules).
- For any item marked "Risk-check required: yes", run `/risk-check` before executing that item.
- Bundling guidance: items 1, 2, 4, 5 all touch ai-resources or research-workflow settings.json — consider a single combined `/risk-check` at plan-time covering all four; item 3 is a separate workspace-wide sweep and may warrant its own gate; item 6 is an agent-definition edit and gets its own `/risk-check` per pause-trigger #9.
- **Coupling: items 5 + 6 should land together** (5 = symptom fix, 6 = upstream auditor fix; landing 5 alone leaves the 3-cycle recurrence intact).
- For any item marked "W2.4 auto-draft: yes", decide auto-draft vs. manual at execution time per the W2.4 sub-disposition in the `/friday-act` Step 3 W2.4 instructions.
- Run `/wrap-session` when all items in this plan are done.

# Friday Act Plan — 2026-05-08 — qc-pass

**Source report:** friday-checkup-2026-05-08.md (weekly tier)
**Journal report:** ai-resources/audits/friday-journal-2026-05-08.md
**Generated:** 2026-05-08
**Items:** 2

## Items

### 1. [med] Wire auto-/triage as default after `/qc-pass` results in commands that produce QC findings — no operator approval gate
- **Source:** journal-derived
- **Risk-check required:** no
- **W2.4 auto-draft:** no

Workspace CLAUDE.md line 77 already declares "Default posture: full autonomy"; this item targets the implementation gap (auto-triage automation), not the policy. Approach: (1) Read `ai-resources/docs/qc-independence.md` QC → Triage Auto-Loop section to confirm it already specifies auto-triage. (2) If specified but not implemented in `/qc-pass`, wire the auto-spawn (subagent → triage-reviewer → return verdict, no operator pause). (3) If specified AND implemented, document as already-done and drop. Files: `ai-resources/.claude/commands/qc-pass.md`, `ai-resources/.claude/commands/resolve.md`, `ai-resources/docs/qc-independence.md`.

### 2. [med] Audit `/resolve`, `/refinement-pass`, `/qc-pass` prompts for language implying "produce the corrected version"; replace with "produce a list of targeted Edit operations" so consumer agents use Edit not Write
- **Source:** journal-derived
- **Risk-check required:** no
- **W2.4 auto-draft:** no

Concrete token-cost issue — subagents return whole-file rewrites instead of targeted Edit operations. Approach: Read the three commands and three agents (`qc-reviewer`, `refinement-reviewer`, `triage-reviewer`). Search for instructions like "produce the revised", "rewrite", "produce a corrected version", "output the new version of the file". Replace with explicit "produce a list of Edit operations: file path + old_string + new_string for each fix." Verify token cost reduction by comparing one before/after `/refinement-pass` run on a known artifact. Files: `ai-resources/.claude/commands/qc-pass.md`, `refinement-pass.md`, `resolve.md`; possibly `ai-resources/.claude/agents/qc-reviewer.md`, `refinement-reviewer.md`, `triage-reviewer.md`.

## Execution notes
- Commit each fix separately (workspace commit-behavior rules).
- For any item marked "Risk-check required: yes", run `/risk-check` before executing that item.
- For any item marked "W2.4 auto-draft: yes", decide auto-draft vs. manual at execution time per the W2.4 sub-disposition in the `/friday-act` Step 3 W2.4 instructions.
- Run `/wrap-session` when all items in this plan are done.

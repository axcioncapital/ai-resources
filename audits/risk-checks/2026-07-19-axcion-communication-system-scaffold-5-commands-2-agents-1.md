# Risk Check — 2026-07-19

## Change

Build the axcion-communication-system project scaffold as designed in projects/axcion-communication-system/pipeline/architecture.md. The change set: 5 new project-local commands (`/review` opus, `/gate-selftest` sonnet, `/drill` sonnet, `/ledger-check` sonnet, `/cutover-preflight` opus — the last is designed but BUILD-DEFERRED to Phase 4); 2 new project-local agents (`comms-gate-reviewer` opus, `ledger-auditor` opus); 1 new PostToolUse hook (`gate-regression-nudge.sh` — advisory only, always exits 0, writes nothing, path-scoped to match `standards/review-gate.md` alone, ~10 lines); 4 machinery reference docs; 4 templates; a new always-loaded project `CLAUDE.md` (target under ~90 lines); and a `shared-manifest.json` declaring exactly those 5 commands + 2 agents as project-local. Zero modifications to any existing shared ai-resources component — everything generic (`/prime`, `/wrap-session`, `/qc-pass`, `/risk-check`, etc.) arrives unchanged via the existing `auto-sync-shared.sh` SessionStart hook. Permissions: the canonical `project-settings.json.template` is used unmodified except for adding the one PostToolUse hook block; no new allow/deny entries; `additionalDirectories` includes the workspace root (written only to the gitignored `settings.local.json`) so the commands can read control-pack docs and the two retiring `marketing-positioning` files by absolute path.

This is a NEW project repo at projects/axcion-communication-system/, created today by the /new-project pipeline. Nothing depends on it yet; it did not exist before 2026-07-19.

Consuming firm is TWO PEOPLE (Patrik and Daniel). The source plan (projects/axcion-communication-system/pipeline/project-plan.md, = project-plan-v4.md) names its own most likely failure mode as "Phase 1 inflates into finished standards" — over-building is the acknowledged risk on this project. The architecture's own §11 risk section concedes the gate may be over-built for a two-person firm and get routed around, becoming an eighth review mechanism rather than the replacement for seven.

/implementation-triage (system-owner, Opus) has already returned WORTH-DOING with three required corrections, two of which are NOT yet applied to the architecture:
(1) APPLIED-AS-CONTEXT: the architecture's §5.2 claim that no seeded-defect fixture donor exists anywhere in the repo is FALSE. A working donor exists at harness/test-workflows/minimal-markdown/workflow-config.yaml — a `unit-2-defect-seed` work unit ("Deliberately structured to elicit a bold-formatting violation") alongside a `unit-1-clean` baseline, with `exit_criteria` and blocking `quality_bars`. The main session verified this directly by reading the file. Stage 3a missed it because it searched for directories named fixtures/ test-cases/ evals/ and the donor is named after its function. Consequence: the architecture's §11 risk rating for the seeded-defect component rests on the false premise "nothing in this workspace has ever run a fixture-based check" and must be re-derived.
(2) NOT YET APPLIED: the five seeded-defect fixture BODIES should be re-sequenced from work unit W1.4 to post-W2.1, so that real gate misses on real Axcíon copy inform them rather than invented ones. The fixture directory shape and the manifest can still be built at W1.4.
(3) IN PROGRESS: /risk-check must run at plan time — this invocation is that gate.

## Referenced files

- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-communication-system/pipeline/architecture.md — exists (638 lines)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-communication-system/pipeline/project-plan.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-communication-system/pipeline/repo-snapshot.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/axcion-communication-system/pipeline/pipeline-state.md — exists (carries four standing constraints)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/harness/test-workflows/minimal-markdown/workflow-config.yaml — exists (the missed fixture donor)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/templates/project-settings.json.template — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/hooks/auto-sync-shared.sh — exists
- projects/axcion-communication-system/.claude/commands/{review,gate-selftest,drill,ledger-check,cutover-preflight}.md — not yet present
- projects/axcion-communication-system/.claude/agents/{comms-gate-reviewer,ledger-auditor}.md — not yet present
- projects/axcion-communication-system/.claude/hooks/gate-regression-nudge.sh — not yet present
- projects/axcion-communication-system/CLAUDE.md — not yet present

## Verdict

PROCEED-WITH-CAUTION

**Summary:** The scaffold is well-planned, self-aware about its own over-build risk, purely additive (zero modification to any shared component), and has zero live consumers to break — but it lands nine new load-bearing components in one project in one step without clearing the workspace's own complexity-budget gate, leans on a prose (not structural) routing rule to keep `/review` distinct from `/qc-pass`, and creates a brand-new git remote whose rollback is one step heavier than a same-tree revert.

## Consumer Inventory

Search terms derived from CHANGE_DESCRIPTION and architecture.md: `review.md`/`/review`, `gate-selftest.md`/`/gate-selftest`, `drill.md`/`/drill`, `ledger-check.md`/`/ledger-check`, `cutover-preflight.md`/`/cutover-preflight`, `comms-gate-reviewer`, `ledger-auditor`, `gate-regression-nudge.sh`, `shared-manifest.json` contract, `axcion-communication-system`. Grepped across `ai-resources/` and the workspace root (absolute-path form — confirmed not gitignore-blind per `docs/audit-discipline.md` § Absence-claims; canary was inconclusive but the absolute-path table entry applies).

**Zero hits outside the project's own planning-source documents** for `gate-selftest`, `ledger-check`, `cutover-preflight`, `comms-gate-reviewer`, `ledger-auditor`, `gate-regression-nudge` anywhere in `ai-resources/` or the workspace root. `axcion-communication-system` appears only in `projects/project-planning/` (the planning source that produced this build) — no downstream consumer references it. Exact-filename collision check against the 90 canonical commands and 42 canonical agents in `ai-resources/.claude/{commands,agents}/`: **zero collisions** (`review.md`, `drill.md`, `gate-selftest.md`, `ledger-check.md`, `cutover-preflight.md`, `comms-gate-reviewer.md`, `ledger-auditor.md` — none present canonically; confirms architecture §9's own naming-collision claim).

| Consumer path | Reference type | Must change? |
|---|---|---|
| `ai-resources/.claude/hooks/auto-sync-shared.sh` (existing shared hook) | parses (`shared-manifest.json` `.commands.local[]` / `.agents.local[]` exclusion lists) | no — already generically reads any project's manifest |
| `ai-resources/.claude/commands/prime.md` Step 1a sibling-repo extension (`for d in "$WORKSPACE_ROOT"/projects/*/`) | invokes (runs `git log --since=` against every repo under `projects/*/`, including this new one, once it has commits) | no — dynamic glob discovery, no code change |
| `ai-resources/.claude/commands/refresh-project-state.md` Step 1 (`Glob projects/*/CLAUDE.md`) | invokes (dispatches `project-state-snapshot-agent` against every eligible project once its `CLAUDE.md` exists) | no — dynamic glob discovery |
| `ai-resources/.claude/commands/log-sweep.md` (discovery scope: `ai-resources/` and `projects/*/`) | invokes (archival scan of the new project's `logs/`) | no — dynamic glob discovery |
| `ai-resources/.claude/hooks/detect-innovation.sh` | (not wired) | n/a — not present in `project-settings.json.template`'s hook set, so it will NOT auto-detect the new commands/agents/hook in this project (consistent with every sibling project checked) |

**Total: 5 consumer rows, 0 must-change.** No committed scan site or shared mechanism requires modification for this build to work; the three cross-project scanners pick the new project up automatically by design (glob-based discovery over `projects/*/`), which is expected, generic behaviour rather than an unanticipated coupling. This is an **isolated addition** in the sense that matters for blast radius: nothing existing breaks or needs editing. The `not yet present` files (5 commands, 2 agents, 1 hook, 1 CLAUDE.md) introduce a contract (the project's own routing rules, the manifest's local-file list, the hook's path-match) that has, as of this scan, no consumer outside the project itself — see Dimension 3 and Dimension 6 for what that implies.

## Dimensions

### Dimension 1: Usage Cost
**Risk:** Low

- New project `CLAUDE.md` loads only inside this project's own sessions (project-scoped, not workspace-wide) — confirmed comparable in size to siblings: `projects/corporate-identity/CLAUDE.md` = 113 lines, `projects/interpersonal-communication/CLAUDE.md` = 45 lines (both measured via `wc -l`); target here is <90 lines.
- 5 new commands are pay-as-used slash invocations, not auto-loaded.
- 2 new opus agents (`comms-gate-reviewer`, `ledger-auditor`) are dispatched only on demand — via `/review` at Standard/Full tier, `/gate-selftest`, and `/ledger-check` — never at SessionStart and never per-tool-call.
- The 1 new hook is `PostToolUse`, matched only to `Write|Edit` on `standards/review-gate.md` — a single-file path scope (confirmed by reading the architecture's own hook spec, §5.5); the overwhelming majority of tool calls in a session never touch that file, so the marginal per-call cost is near zero.
- No `@import` chain is added to any always-loaded file; no modification to `ai-resources/CLAUDE.md` or `ai-resources/.claude/settings.json` (confirmed unmodified by this change).

### Dimension 2: Permissions Surface
**Risk:** Low

- `ai-resources/templates/project-settings.json.template` (read in full: `defaultMode: bypassPermissions`, broad `allow` — `Bash(*)`, `Read`, `Edit`, `Write`, `MultiEdit`, `Agent`, `Skill`, etc. — narrow `deny`, 2 `SessionStart` hooks) is used unmodified per the change description, and is confirmed identical in shape to two live sibling projects' own settings.json (`projects/corporate-identity/.claude/settings.json`, `projects/axcion-content-programme/.claude/settings.json`, both read in full) — this is the established pattern, not a widening of it.
- Adding one `PostToolUse` hook block at the project level is an established pattern, not a novel capability: confirmed live in `projects/buy-side-service-plan/.claude/settings.json` (git-auto-commit + claim-ID check + write logging + innovation detection), and also present in `projects/axcion-sector-intelligence`, `projects/positioning-research`, `projects/project-planning`, `projects/repo-documentation`, `projects/research-pe-regime-shift-advisory-gap`.
- `additionalDirectories` pointing at the workspace root, written only to the gitignored `settings.local.json`, is likewise an established pattern — confirmed live in `projects/corporate-identity/.claude/settings.json` and `projects/nordic-pe-screening-project/.claude/settings.local.json`.
- No new `allow`/`deny` entries beyond the template; no `deny` narrowing; no scope escalation (project-level only, never user-level); no MCP or external-API surface introduced.

### Dimension 3: Blast Radius
**Risk:** Low

- Grounded directly in the Step 1.5 Consumer Inventory: **0 consumers, 0 must-change**, across every one of the 8 new runnable components and the manifest contract.
- The only entities that will interact with this change (`auto-sync-shared.sh`, `/prime`, `/refresh-project-state`, `/log-sweep`) do so through pre-existing, generic, glob-based mechanisms that require zero modification — confirmed by reading each mechanism's own discovery logic (`Glob projects/*/CLAUDE.md`; `for d in "$WORKSPACE_ROOT"/projects/*/`; discovery scope `ai-resources/` + `projects/*/`).
- No contract change to any existing caller: the change does not touch a subagent input schema, a report section heading, a hook output shape, or a slash-command syntax that anything else depends on.
- Zero existing files anywhere in `ai-resources/` or any sibling project are modified by this change (confirmed: the change is additive-only, confined to a brand-new project directory).
- No unanticipated consumer surfaced by the Step 1.5 inventory beyond what the architecture itself already discusses (§9 Conflict Analysis, §12 Integration Map) — the cross-project scanners are the one class the architecture doesn't explicitly name, but they require no action, so they don't change the risk level here.

### Dimension 4: Reversibility
**Risk:** Medium

- This is a **brand-new git repository** with no prior commit history — "reverting" this change is not a same-tree `git revert` but deleting the local `projects/axcion-communication-system/` directory (and, per `pipeline/decisions.md` #6, the GitHub remote `axcion-communication-system` if it has already been created and pushed).
- Auto-synced command/agent symlinks (the ~85 commands + ~38 agents that `auto-sync-shared.sh` will symlink in at first SessionStart) get committed into the new repo's own git history — confirmed by checking a live sibling: `projects/corporate-identity/.claude/commands/*.md` symlinks ARE tracked in git (`git ls-files` lists them). Cleanup of these is bundled into deleting the whole new repo, not a separate step, so this does not compound the Medium rating on its own.
- No shared/canonical `ai-resources` file is touched, so reverting leaves no stale reference anywhere else in the workspace — this is the one factor that would otherwise push toward High.
- The residual reason for Medium (not Low): per workspace `CLAUDE.md` § Push behavior, a new GitHub remote is created and, once the operator confirms the wrap-time push, receives real content — that is state propagated beyond the local git tree. The cleanup is bounded and cheap (the repo is brand-new with zero external consumers, confirmed by the Consumer Inventory), but it is a genuine extra step beyond "the same tree, `git revert`."

### Dimension 5: Hidden Coupling
**Risk:** Medium

- **Self-acknowledged overlap, resolved only by a prose rule.** Architecture §9 and §11 risk #5 both name "`/review` versus `/qc-pass` confusion in practice" as a live, accepted risk: "`/review` sits close to `/qc-pass` and `/refinement-pass` in an operator's head," resolved "by the routing rule in `CLAUDE.md` § 4... rather than by renaming." This is the exact shape of coupling the workspace's own doctrine (quoted inside the architecture itself, §5.5: "memory-dependent gates are weak") warns against — two mechanisms with overlapping purpose, disambiguated by an operator remembering a prose rule rather than by a structural difference (e.g., a filename pattern, a directory boundary, or a mechanical check).
- **The regression-nudge hook's path-scope has a narrower blind spot than the architecture states.** `gate-regression-nudge.sh` fires only on `Write|Edit` to `standards/review-gate.md` (the rule text). It does **not** fire on edits to `comms-gate-reviewer.md` (the agent that actually implements the five-dimension judgment). A change to the reviewer's judgment logic — as opposed to the rule text it enforces — carries no automatic reminder to re-run `/gate-selftest`, even though decision 1 in the architecture's own Design Decision Log states "a change to it [the reviewer] must be re-validated by `/gate-selftest`." This specific gap is not discussed anywhere in the architecture.
- `comms-gate-reviewer` is deliberately shared across two hot paths (`/review` and `/gate-selftest`) — this coupling is at least explicit and self-documented (decision 1), not hidden.
- `/cutover-preflight`'s hard dependency on control-pack doc 2 read by absolute path at runtime is explicitly named and accepted (§9 decision 7: "the command hard-depends on a path outside this repo... it breaks loudly") — since the command is build-deferred, this is not a live coupling risk yet.

### Dimension 6: Principle Alignment
**Risk:** Medium

Ground: `{AI_RESOURCES}/../CLAUDE.md` § Design Judgment Principles (read in Step 1); `projects/strategic-os/ai-strategy/principles-base.md` (read in full — present, used as the frozen-ID index); `ai-resources/docs/ai-resource-creation.md` rule #7 (read in full).

- **Complexity-budget gate (rule #7) — neither prong is cleanly cleared.** This scaffold introduces 9 new load-bearing units in one project in one step: 5 commands, 2 agents, 1 hook, 1 permanent always-loaded `CLAUDE.md`. **Prong (a) net-simplification:** not cleared — the change is purely additive; zero existing components (across the "seven review mechanisms" the project plan names) are removed, merged, or retired by this build (that consolidation is explicitly Phase 4 work, not this scaffold). **Prong (b) evidenced-failure:** not cleared in its literal form — a grep of `logs/friction-log.md`, `logs/defect-log.md`, `logs/coaching-log.md`, `logs/incident-log.md` found no cited entry naming the specific failure mode this machinery addresses (the closest hits are unrelated: a marketing-positioning brief defect, and coaching-log entries about an unrelated "editorial-disagreement" review gate). The project's actual evidence base is a full `/scope-project` planning pipeline (control-pack docs, seven operator notes, a path-verified prior-art inventory) plus an `/implementation-triage` WORTH-DOING verdict from an Opus system-owner — a materially different, and arguably more rigorous, process than the four named logs, but not the literal citation the gate specifies.
- **Mitigating factors that keep this Medium, not High:** (1) the one component that would clearly have triggered OP-9/AP-7/DR-7 speculative-abstraction — `/cutover-preflight` — is explicitly designed but **not built**, deferred to Phase 4, with the architecture itself naming the deferral reason ("Designing it now is cheap; building it now is speculative") — this is the textbook-correct move, not a violation; (2) the consumer for the other 7 components is not a hypothetical future user but the **firm's own two founders, starting the very next work unit (W1.4)** — materially different from "hooks for Phase 2" building for an absent consumer; (3) OP-12 (closure before detection) is honoured throughout — `/gate-selftest`'s MISS path and `ledger-auditor`'s completeness/materiality flags both ship with an explicit closure action (fix the dimension and re-run the whole set; route to operator disposition), not a bare detector; (4) OP-5 (advisory vs. enforcement) is honoured — the hook always exits 0 and writes nothing, `/cutover-preflight` never applies the flip, and `/review`'s verdict is a record, not an auto-block; (5) the over-build tension is **named, not silent** — architecture §11 risk #1 states the risk plainly with a falsifier ("after ~20 real assets, check whether Standard-tier material is being reviewed at all").
- **What is missing for a clean pass:** none of `pipeline/decisions.md`, `architecture.md`, or `project-plan.md` explicitly invokes the complexity-budget gate or logs an OP-11-style loud exception citing it — the self-awareness is present in substance (§11's risk framing) but not in the gate's own vocabulary or its designated log location.

### Dimension 7: Problem Reality
**Risk:** Low

- **Defect — observed, not merely asserted.** The specific claim in CHANGE_DESCRIPTION — that architecture.md §5.2's "no seeded-defect fixture donor exists anywhere in the repo" is false — was independently re-derived, not inherited: `harness/test-workflows/minimal-markdown/workflow-config.yaml` was read in full and confirms a `unit-2-defect-seed` work unit ("Deliberately structured to elicit a bold-formatting violation") alongside a `unit-1-clean` baseline, with `exit_criteria` (`grep_absent` on `^\*\*`) and a blocking-severity `quality_bars` entry. Further verified this is not merely a config that was never exercised: `harness/test-workflows/minimal-markdown/output/unit-1.md`, `unit-2.md`, `unit-3.md` all exist on disk (dated May 21–22), confirming the fixture set has actually been run.
- **Consequence — traced, and appropriately scoped (not overstated).** The exact downstream text affected was located and re-read: architecture.md §11 risk #2 states "nothing in this workspace has ever run a fixture-based check" as supporting rationale for its risk rating on the seeded-defect component. That absolute claim is now falsified by the harness donor. The correction is scoped honestly by the change description itself (a risk-rating premise needing re-derivation, not a reversal of the underlying risk) — re-derivation here confirms the correction is a **refinement of rationale**, not a reversal of substance: the harness fixture is a structural precedent for "planted-defect + expected-check" (useful, real), but it is not a domain-specific donor for a *communication-quality* review gate, so the seeded-defect component's core novelty (§5.2's "no donor... net-new design surface" for the specific five-dimension gate) still stands even after the correction.
- **Re-derivation refines one part of the change description.** CHANGE_DESCRIPTION attributes the Stage-3a miss solely to a naming mismatch ("searched for directories named fixtures/ test-cases/ evals/ and the donor is named after its function"). Independent re-derivation found an **additional, more fundamental cause not stated in the change description**: `repo-snapshot.md`'s own Scope note states the scan "covers the canonical `ai-resources` repo in full" — and `harness/` is a workspace-root-level directory, a sibling of `ai-resources/`, entirely outside that stated scan boundary. So the miss is a **scope gap**, not only a vocabulary gap. This does not contradict the core claim (a real donor exists and was missed) — it strengthens it — but the change description's own explanation of *why* it was missed is incomplete.
- **Per-component OBSERVED vs. INFERRED, against the plan as evidence base** (project-plan.md is the only plan-tier document provided; control-pack docs D4/D6/D7/D9–D24 are cited extensively by the architecture but were not among the referenced files, so their content could not be independently verified — noted as a scope limit, not a defect):
  - `/review` — **OBSERVED.** Plan W1.4 mandates the gate's dimensions/tiers/second-reviewer rule as content to build; Phase 2 (W2.1) repeatedly refers to assets passing "through the consolidated gate," and §9's risk table discusses the gate "being exercised" — a runnable trigger is required by the plan's own text, even though the specific command name is an architect choice.
  - `/gate-selftest` — **OBSERVED.** Plan W1.4 explicitly: "this is the gate's regression test, run at W2.2 and re-run whenever a dimension is amended"; W2.2's content spec directly describes running the seeded set through the gate and recording misses.
  - `/drill` — **OBSERVED.** Plan W3.3 explicitly directs reuse of "the spaced-repetition command pattern" from `interpersonal-communication`; the donor (`today-drill.md`) was independently confirmed to exist by Stage 3a and cross-checked in this review's Consumer Inventory work.
  - `/ledger-check` — **INFERRED.** Plan W4.1 mandates the ledger document and its completeness sufficiency criteria, but does not itself call for a wrapping command; the architecture's own decision log (#6) justifies the command as a discoverability improvement over "the bare-agent pattern," which is a reasonable elaboration, not a plan-mandated component.
  - `/cutover-preflight` — **INFERRED, and explicitly not built.** Plan W4.5 mandates verifying six D12 conditions but not an automated command; the architecture itself calls building it now "speculative" and defers it to Phase 4 — the correct handling of an inferred, not-yet-needed component (see Dimension 6).
  - `comms-gate-reviewer` (agent) — **OBSERVED requirement, INFERRED mechanism.** The plan requires an independent review at Standard/Full tier in substance (the founders both draft and would otherwise self-review); the specific choice of a fresh-context dispatched subagent (vs. some other enforcement mechanism) is an architect elaboration, grounded in the workspace's general QC-independence convention and a real donor pattern (`ledger-consistency-checker`).
  - `ledger-auditor` (agent) — **OBSERVED requirement, INFERRED mechanism.** Same pattern: plan W4.1's completeness/no-blank sufficiency criteria are explicit; the fresh-context-agent implementation is an architect choice, again grounded in an existing working donor.
  - `gate-regression-nudge.sh` (hook) — **OBSERVED requirement, INFERRED mechanism.** Plan W1.4 explicitly requires the seeded set to "re-run whenever a dimension is amended"; the specific choice of a PostToolUse hook (vs. a prose reminder) is the architect's chosen enforcement instrument, justified in §5.5 by the workspace's general "memory-dependent gates are weak" doctrine.
- **Not every INFERRED item is a red flag.** Architect elaboration beyond bare plan text is expected at the Stage-3b design step; none of the INFERRED items above is speculative-for-an-absent-consumer (the live consumer is the founders, immediately) except `/cutover-preflight`, which is correctly not built. This is why the INFERRED findings feed Dimension 6 (principle-alignment tension) rather than lowering Dimension 7 on their own.
- **Re-derivation vs. the change description, overall:** the core factual claims (the false "no donor" premise; the not-yet-applied re-sequencing recommendation; the /risk-check-in-progress claim) were all independently confirmed true. One explanatory detail (why Stage 3a missed the donor) was refined, not contradicted. No claim asserted a consequence that evidence then contradicted.

## Mitigations

- **Reversibility (Medium):** Before confirming the wrap-time push, explicitly decide and record whether the GitHub remote `axcion-communication-system` should be created now or deferred — if created, note in `pipeline/decisions.md` that abandoning the project requires deleting both the local directory and the remote as one action, not just a local `git revert`.
- **Hidden Coupling (Medium):** (a) Extend `gate-regression-nudge.sh`'s match pattern (or add a second, equally narrow nudge) to also fire on `Write|Edit` to `comms-gate-reviewer.md`, so a change to the reviewer's judgment logic — not only to the rule text it enforces — triggers the `/gate-selftest` reminder, closing the gap named in Dimension 5. (b) Add one line to the new project's `CLAUDE.md` § Review routing making the `/review`-vs-`/qc-pass` boundary a standing item on this project's own maintenance cadence (or the architecture's own stated falsifier — "after ~20 real assets, check whether Standard-tier material is being reviewed at all") rather than leaving it purely to operator memory.
- **Principle Alignment (Medium):** Record one explicit entry in `projects/axcion-communication-system/pipeline/decisions.md` citing `ai-resources/docs/ai-resource-creation.md` rule #7 by name: state plainly that this scaffold is a net-additive 9-component build that clears neither prong (a) nor prong (b) in their literal form, and name the substituting justification (the approved `/scope-project` control pack, the `/implementation-triage` WORTH-DOING verdict, and the immediate real consumer) as the recorded rationale for proceeding. This converts an already-substantively-reasoned decision into a loud, findable one, satisfying the spirit of OP-11 rather than leaving the complexity-budget question implicit in §11's risk prose alone.

## Evidence-Grounding Note

All risk levels grounded in direct evidence (file/line references, grep counts, verbatim quotes from CHANGE_DESCRIPTION or referenced files, or explicit INCOMPLETE flags). No training-data fallback was used on fetch/read failures.

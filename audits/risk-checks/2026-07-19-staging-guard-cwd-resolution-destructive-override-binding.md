# Risk Check — 2026-07-19

## Change

Two hook fixes in ai-resources, landing as two separate commits. Session S5-dd5, mission repo-health-backlog-2026-07.

ITEM 3 (sequenced FIRST) — `.claude/hooks/check-destructive-liveness.sh`, mission thread 16. Two defects.

(a) THE OVERRIDE BINDS TO NOTHING. `:207` reads `override = bool(re.search(r'\bAXCION_LIVENESS_OVERRIDE=1\b', cmd))`. The file maintains TWO views of the command — `cmd` (raw) and `scan` (produced by `_command_text_only`, which blanks quoted spans). Destructive-verb DETECTION correctly uses `scan` (:183-186); the override check at :207 uses `cmd`, on the wrong side of the file's own documented safety split (:227-230). Proposed fix: evaluate the override against `scan` AND require the assignment to occupy the environment-prefix position of the destructive command, reusing the existing `_ENVPFX` primitive (:175, already composed into RE_WORKTREE/RE_BRANCH/RE_RESET/RE_CLEAN at :177-180).

(b) THE AUDIT TRAIL RECORDS OUTCOMES THAT NEVER HAPPENED. `:208-224` writes the audit line and takes `sys.exit(0)` at PreToolUse — before the command executes — with no `outcome`/`exit_code` field. Two candidate shapes, UNDECIDED: (i) add outcome/exit_code and move the write to where outcome is known (implies a PostToolUse counterpart); (ii) keep the PreToolUse write but relabel the record to assert only what is true at that moment. Caller's instinct is (ii); wants it scored, not confirmed.

ITEM 1 (sequenced SECOND, own commit) — `.claude/hooks/check-foreign-staging.sh`. Returns from a RECONSIDER issued earlier today (2026-07-19) in the bundled-staging-hook report. Defect: `:223` reads `project_dir = os.environ.get("CLAUDE_PROJECT_DIR", "") or os.getcwd()` and `:224` runs `git -C project_dir rev-parse --show-toplevel`, so a `git add` issued inside a nested project repo is judged against the wrong tree. Fix per the prior gate's Recommended redesign (:149-155): resolve the toplevel from the PreToolUse payload's `cwd` field, run git probes with `-C <that toplevel>`, relativize footprint paths against the same root; fixture suite first; rollback plan named in the commit message; explicit fix/delete/park call on the `.codex/hooks/` sibling fork.

Two things scored hardest: (A) whether bundling Item 1 with Item 3 (instead of Item 1 alone, per the prior redesign) defeats the intent of that redesign; (B) whether the compound-`cd` path is designable/testable at all without reopening the fail-open `979ed01` closed on this hook.

## Referenced files

- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/hooks/check-destructive-liveness.sh — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/hooks/check-foreign-staging.sh — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.codex/hooks/check-foreign-staging.sh — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/missions/repo-health-backlog-2026-07.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/improvement-log.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/commit-discipline.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/risk-checks/2026-07-19-bundled-staging-hook-repo-resolution-thread-14-orphan-hooks.md — exists (prior pass, § Recommended redesign :149-155 binding for Item 1)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/logs/session-plan-2026-07-19-S5-dd5.md — exists

## Verdict

**BUNDLE: RECONSIDER** — dragged down by **Item 1 alone, a SECOND time**. Item 3 (both sub-defects, one commit, one file) clears independently and may land on its own merits — it is sequenced first, in its own commit, on a different file, with no shared code path in the actual fix diff. Per the mission's own item-scoped gating design, Item 3 should proceed regardless of Item 1's outcome.

**Per-item verdicts:**

| Item | Verdict | Driving dimension(s) |
|---|---|---|
| 3(a) — override binds to nothing | **PROCEED-WITH-CAUTION** | Blast Radius (High, mitigated) |
| 3(b) — audit trail records false outcomes | **PROCEED-WITH-CAUTION** | Blast Radius (High, mitigated); Principle Alignment (Medium if candidate (i) chosen) |
| 1 — staging hook repo resolution | **RECONSIDER (second time)** | Blast Radius (High), Hidden Coupling (High) |

**Summary:** both Item 3 sub-defects are real, execution-confirmed (one execution run even surfaced a third override-bypass variant not in the mission thread), narrowly scoped, and reuse existing tested primitives — they clear with standard SPOF-hook mitigations (fixture-first, rollback plan). Item 1's defect is equally real and well-evidenced, but the design gap the first gate flagged (compound-`cd` handling) is still, as scoped in this brief, an unresolved "degrade to warn" candidate that risks reopening the exact fail-open `979ed01` closed on this same file two days ago — and unlike Item 3, nothing has yet been built or tested to close that gap. A safer, buildable alternative exists (detailed below) but is not yet adopted; scoring the change *as described* rather than *as it could be redesigned* means Item 1 holds at RECONSIDER.

## Consumer Inventory

Search terms: `check-destructive-liveness`, `check-foreign-staging` (basenames), `AXCION_LIVENESS_OVERRIDE` and `destructive-override.log` (contract markers). Grepped with `command grep` (not the ambient shell alias — confirmed non-blind per mission thread 11's own canary evidence, reused rather than re-run since both my search paths and terms are name-based, not the dot-rooted walk form thread 11 flags as blind) across `ai-resources/.claude`, `.codex`, `docs`, `logs`, `skills`, `workflows`, `CLAUDE.md`, and the workspace root `.claude`/`CLAUDE.md`. Raw hit counts are large (~24 files for `check-destructive-liveness`, ~65 for `check-foreign-staging`, mostly session-plans, scratchpads, and log archives — historical point-in-time records). The table below lists the structurally load-bearing consumers only.

| Consumer path | Reference type | Must change? |
|---|---|---|
| `~/.claude/settings.json` | invokes (sole registration for **both** hooks, user-level, global — confirmed via `command grep -rl … --include="settings*.json"`, one hit each) | no — wiring unaffected, internal logic only |
| `.claude/commands/new-worktree-session.md`, `.claude/commands/close-worktree-session.md` | documents (`close-worktree-session.md:151-154` gives the exact `AXCION_LIVENESS_OVERRIDE=1 git worktree remove "<path>"` usage syntax) | no — the fix's env-prefix-position requirement is satisfied by this exact documented syntax; verified compatible |
| `docs/commit-discipline.md` § Destructive-op pre-flight | documents (describes the 3 liveness probes) | no requirement today — the section currently documents **zero** override mechanism, so nothing becomes stale; recommended to add one now that the contract is being sharpened |
| `logs/scripts/test-destructive-liveness.sh` (14-case harness, "run after any edit to the hook" per commit-discipline.md:79) | co-edit / test harness | **yes** — confirmed by grep: **zero** matches for "override" in this file today; Stage 1's "write the negative fixture first" is filling a real, currently-unfixtured gap |
| `logs/improvement-log.md:1353-1361` | documents (source defect entry for 3a+3b) | yes — flip status per mission non-negotiable when the fix lands |
| `logs/missions/repo-health-backlog-2026-07.md` thread 16 | documents | yes — close/update when both sub-defects land |
| `.claude/hooks/check-foreign-staging.sh` | co-edit (shares `_command_text_only`, explicitly "Lifted verbatim from check-foreign-staging.sh" per check-destructive-liveness.sh:112-117) | no for Item 3 — neither 3a's fix (the override regex at :207) nor 3b's fix (audit-write timing/labelling) touches `_command_text_only`; confirmed disjoint by direct read |
| `.claude/commands/risk-check.md`, `session-start.md`, `prime.md` | documents (brief mentions) | no |
| `docs/session-marker.md`, `docs/parallel-sessions-playbook.md`, `docs/daniel-concurrent-session-hooks-setup.md` | documents | no |
| `docs/commit-discipline.md` § Foreign-staging tripwire | documents (detailed footprint/repo-resolution contract) | **yes** — improvement-log:1516 explicitly names this file as a "Target file … if the block semantics change" |
| `skills/handoff/SKILL.md:212` | documents | no |
| `.claude/hooks/detect-concurrent-session.sh` (both canonical and `.codex` copies) | documents (cross-reference comment: "The commit-time guard (check-foreign-staging.sh) blocks a cross-session COMMIT") | no |
| `.codex/hooks/check-foreign-staging.sh` | co-edit (divergent sibling fork — 464 lines vs. 668 canonical; confirmed **unregistered** in `.codex/hooks.json`, which lists only `check-heavy-tool.sh`, `friction-log-auto.sh`, `log-write-activity.sh`, `auto-qc-nudge.sh`, `friday-checkup-reminder.sh`, `check-stop-reminders.sh`, `coach-reminder.sh`, `improve-reminder.sh`, `auto-resolve-nudge.sh` — neither hook in this gate appears) | **yes** — Stage 4 requires an explicit fix/delete/park call, per improvement-log:1578-1587 |
| `logs/improvement-log.md:1234-1241` (earlier, same-root-cause entry) | documents | yes — same root cause as :1488, should close together |
| `logs/improvement-log.md:1488-1516` | documents (primary Item 1 source entry) | yes — flip status when fix lands |
| `logs/improvement-log.md:1578-1587` (`.codex` fork divergence entry) | documents | yes — closes when Stage 4's decision is made and recorded |
| workflows/research-workflow (template) | absence confirmed — zero copies of either hook file in `workflows/` | n/a |
| ~55 other files (session-plans, scratchpads, decisions.md, friction-log.md, archives) | documents (historical/point-in-time) | no |

**Total: ~24 structurally distinct consumers identified across both hooks, 7 must-change** (3 for Item 3: test harness, improvement-log:1353, mission thread 16; 4 for Item 1: commit-discipline.md § Foreign-staging tripwire, `.codex` sibling fork, improvement-log:1234, improvement-log:1488) **+ 1 conditional** (improvement-log:1578, closes only once Stage 4's `.codex` decision is actually made). The long prose/scratchpad tail (~55+ files) is historical and not enumerated row-by-row. No consumer was found that the change description failed to anticipate — the `.codex` fork and the test-harness gap were both already named in the source material (mission thread 16, the prior risk-check's Recommended redesign, and the session plan's own Stage 4).

## Dimensions

### Dimension 1: Usage Cost
**Risk:** Low (Item 3a, Item 1); Low-to-Medium (Item 3b, conditional)

- **Item 3(a).** Pure regex-target change (`cmd` → `scan`) plus reuse of the already-computed `_ENVPFX` primitive. No new registration, no new context injection. Low.
- **Item 3(b).** If candidate (ii) (relabel only) is chosen: zero new wiring, Low. If candidate (i) (outcome/exit_code + PostToolUse counterpart) is chosen: a new PostToolUse(Bash) hook registration would fire on every Bash call in every checkout (even though it internally narrows to destructive verbs) — Medium. **Recommend (ii)** on this dimension alone.
- **Item 1.** Internal logic edit to an already-registered PreToolUse(Bash) hook; `payload.get("cwd")` is free (payload already parsed). No new registration. Low.

### Dimension 2: Permissions Surface
**Risk:** Low (all items)

- No item adds an allow/deny entry, widens a glob, or introduces a new tool-invocation pattern. All edits are internal Python logic inside already-authorized, already-registered hook scripts.

### Dimension 3: Blast Radius
**Risk:** Item 3(a) — **High** (mitigated). Item 3(b) — **High** (mitigated). Item 1 — **High**.

- **Item 3(a)/(b).** `check-destructive-liveness.sh` is registered exactly once, at user level, and fires on every destructive git verb (`worktree remove`, `branch -d/-D`, `reset --hard`, `clean -f`) in **every checkout in the workspace** — the consumer inventory's own `~/.claude/settings.json` row confirms this is shared infra by the rubric's own "shared infra touched in a way that affects multiple workflows" clause, independent of how narrow the specific edit is (same reasoning the prior gate applied to a one-sentence `prime.md` deletion). One must-change consumer, the 14-case test harness, currently carries **zero** override-path coverage — confirmed by grep, not assumed. **High**, mitigated below.
- **Item 1.** `check-foreign-staging.sh` is registered exactly once, at user level, and fires on every gated git verb (`commit`, `commit --amend`, `commit -a`, working-tree-wide `add`) in every checkout. Same "shared infra" clause. Two must-change consumers (commit-discipline.md, the `.codex` fork) plus three improvement-log status flips. **High.**

### Dimension 4: Reversibility
**Risk:** Item 3(a) — Medium. Item 3(b) — Medium. Item 1 — Medium.

- All three: single-file, single-function-scope edits that `git revert` cleans up fully at the code level. Scored Medium (not Low) for the same reason the prior gate applied to Item 1: the hook runs live and continuously, so a bad version deployed mid-window has already fired incorrectly for every relevant command attempted in that window, and recognizing whether to revert requires diagnosing the failure mode first — one extra step, not a clean instant undo.
- **Item 3(a)/(b) narrower than Item 1** in practical exposure: the override path only matters when a destructive verb is *also* detected and override text is *also* present — a much rarer trigger than every `git add`/`git commit` — but the reversibility mechanics (revert cleanliness, diagnostic step) are the same, so the risk *level* is scored the same.

### Dimension 5: Hidden Coupling
**Risk:** Item 3(a) — Low. Item 3(b) — Low (candidate ii) / Medium (candidate i). Item 1 — **High**.

- **Item 3(a).** Reuses two already-tested, already-documented primitives (`scan`, `_ENVPFX`) in exactly the pattern the file's own comments prescribe ("this is exactly this primitive"). No new contract, no undocumented dependency. Confirmed compatible with the one documented consumer of the override syntax (`close-worktree-session.md`). Low.
- **Item 3(b).** Candidate (ii) — pure relabeling, no coupling. Candidate (i) — a new implicit dependency: a PostToolUse counterpart must correlate back to the PreToolUse write (matching session id, command, and timing) to update rather than duplicate the audit line, which is a new, currently-undocumented contract between two hook stages. Medium if (i) is chosen.
- **Item 1 — High, unresolved.** The design gap the prior gate flagged is still open, and this gate's own re-derivation sharpens rather than resolves it:
  1. `cwd` in the PreToolUse payload is a snapshot taken **before** the gated command executes. For `cd nested && git add .` in one Bash call, `cwd` reports the **pre-`cd`** directory — this is true whether the resolution source is `CLAUDE_PROJECT_DIR`/`os.getcwd()` (today's broken behavior) or the payload's `cwd` field (Item 1's proposed fix): **neither naive implementation actually fixes the compound-`cd` case**, they only fix the simple case (a Bash call issued from a session already positioned inside the nested repo).
  2. The caller's own floated candidate — "hard block only when confidently resolved, else degrade to warn" — inverts this file's own established posture. `979ed01` (2026-07-18, confirmed by `git show --stat`: +92 lines to this exact file) added process-grounding specifically so that when the guard is **uncertain**, it degrades **toward blocking**, never toward silent pass ("degrade toward blocking, matching the documented trade — a false stop costs one operator sentence; a false pass costs another session's work," per the file's own `_live_foreign_session` comment). "Degrade to warn" on unresolved compound-`cd` ambiguity is the mirror-image failure: a genuine foreign-staging event wrapped in a compound `cd` command would get only a warning, not a block — silently reopening the same class of fail-open `979ed01` closed, via a different ambiguity vector (toplevel resolution instead of marker/process liveness).
  3. **A safer, buildable alternative exists in the same file and was not cited in the change description.** `check-foreign-staging.sh:521-526` already contains a tested, working regex (`mcd = re.search(r'\bcd\s+([^\s;&|]+)\s*&&.*\bgit\s+add\b…')`) that parses a leading `cd X &&` to scope `git add .` candidates to subdirectory X — this is the *exact* parsing shape needed for compound-`cd` handling, just applied one level lower (candidate-filtering within an already-resolved repo, not repo-root selection). Generalizing this precedent to resolve the toplevel itself (parse the leading `cd`, resolve its target against payload `cwd`, run `git -C <that path> rev-parse --show-toplevel`) is a concrete, testable design that avoids inventing a new "confidence" heuristic — and, critically, gives the fix a principled fail-closed default (unparseable compound forms — nested `cd`s, `;`-chains, subshells — hard-block, matching `979ed01`'s doctrine) instead of a new warn-on-uncertainty branch.
  4. This alternative is **not yet adopted, built, or tested** — it is this gate's own finding, offered as the binding design requirement below, not something the session has committed to. Scored on the change **as described** (an undecided "degrade to warn" candidate), Hidden Coupling is High.

### Dimension 6: Principle Alignment
**Risk:** Item 3(a) — Low. Item 3(b) — Low (candidate ii) / Medium (candidate i, tension only). Item 1 — Medium.

Ground: workspace `CLAUDE.md` § Design Judgment Principles / Autonomy Rules (read this session); `projects/strategic-os/ai-strategy/principles-base.md` not read (queued for dissolution per `logs/retirement-backlog.md`, per the mission's own thread notes — treated as unavailable, inline checks applied and cited by ID below).

- **Item 3(a)** — closes a real, execution-confirmed defect (OP-12: this *is* the closure, not new detection); stays advisory (`exit 0`/`exit 2` to the agent unchanged, OP-5 posture untouched); reuses existing tested primitives rather than inventing new ones (no OP-9/AP-7/DR-7 concern). Low.
- **Item 3(b)** — genuine architectural tradeoff, not a violation either way. Candidate (i) is net-additive automation (new PostToolUse wiring) with **zero current confirmed consumer** reading `destructive-override.log` programmatically — normally a speculative-abstraction signal (OP-9/AP-7/DR-7) — **but** it clears the `ai-resource-creation.md` rule #7 complexity-budget gate via **prong (b)**: `improvement-log.md:1357` is cited, written evidence of the exact failure mode (a 2026-07-18 real incident where the override was logged as "proceeded" while the underlying `git reset --hard` was denied by the permission layer and never ran). So (i) is not forbidden by principle, but it is the costlier, higher-blast-radius path for a benefit candidate (ii) already delivers (removing the *false* claim). Candidate (ii) is pure net-simplification. **Recommend (ii)**; score Medium only if (i) is chosen, and only as tension (evidenced, not silent), not violation.
- **Item 1** — **Medium**, tied to the same Dimension 5 finding. The caller's floated "degrade to warn" candidate, if built as literally described, would silently reverse `979ed01`'s established "uncertain → protect" doctrine on this exact file — an **OP-11 (loud revision, never silent drift)** concern: nothing in the change description proposes recording this as a deliberate posture change, it is framed as an implementation detail. Not yet a clear violation (nothing has shipped), hence Medium, not High — but if the session ultimately ships warn-on-ambiguity behavior without an explicit, dated note in `logs/decisions.md` naming the reversal and why, that becomes a High on a future audit. The mitigation is the same as Dimension 5's: adopt the fail-closed-on-unparseable design, which requires no principle exception at all.

### Dimension 7: Problem Reality
**Risk:** Item 3(a) — Low. Item 3(b) — Low. Item 1 — Low.

- **Item 3(a).**
  - **Defect — observed or inferred?** Observed by **live execution**, not merely read. This session ran the hook with crafted PreToolUse payloads via stdin (after self-catching and correcting a first-attempt methodology error — argv instead of stdin, which produced a uniformly-uninformative all-pass result and was rightly distrusted rather than reported). Results: `AXCION_LIVENESS_OVERRIDE=1 git reset --hard` → correctly accepted; `NOTE=AXCION_LIVENESS_OVERRIDE=1 git reset --hard` → incorrectly accepted (the named defect); `git commit -m "use AXCION_LIVENESS_OVERRIDE=1 to override" && git reset --hard` → **also** incorrectly accepted — a **third variant, not named in mission thread 16**, found by this session's own execution. I independently re-read `:207` and confirm `override = bool(re.search(r'\bAXCION_LIVENESS_OVERRIDE=1\b', cmd))` operates on `cmd` (raw, unblanked), which explains why the quoted-commit-message variant also matches — `scan` would have blanked it.
  - **Consequence — traced or assumed?** Traced. Each override-accepted run also wrote an audit-log line, directly confirming the downstream effect (a false "override accepted" record) in the same execution.
  - **Re-derivation vs. the change description:** the change description's own claim is not just confirmed but **exceeded** — a defect variant beyond what the mission thread documented was found and reported, strengthening the premise. The disclosed harness limitation (controls can't discriminate "blocked" from "allowed," only whether the override branch fired) is accurate and does not weaken the specific claim under test, which is about the override regex, not the liveness probes. Low.
- **Item 3(b).**
  - **Defect — observed or inferred?** Observed twice over: directly re-read `:208-224` (audit write + `sys.exit(0)` inside `if override:`, no `outcome`/`exit_code` field — confirmed exact line match), **and** independently corroborated by the same live-execution run above (all three override-accepted cases wrote audit lines with no outcome field).
  - **Consequence — traced or assumed?** Traced, not merely plausible. `improvement-log.md:1357` records a real, already-observed incident (2026-07-18, `axcion-content-programme`): the override was logged as accepted while the underlying `git reset --hard` was denied by the permission layer and never executed — exactly the false-record consequence the change description claims, already realized once in production.
  - **Re-derivation vs. the change description:** none — all claims re-derived and confirmed, with an independent prior incident as additional corroboration. Low.
- **Item 1.**
  - **Defect — observed or inferred?** Observed. Direct re-read of `:223-224` confirms the exact code cited: `project_dir = os.environ.get("CLAUDE_PROJECT_DIR", "") or os.getcwd()`, then `git -C project_dir rev-parse --show-toplevel`.
  - **Consequence — traced or assumed?** Traced, with two independent real incidents on record, not one. `improvement-log.md:1234-1241` (2026-07-18, an ad-hoc cross-repo commit **and** a systematic `/new-project` Stage-4 false positive) and `improvement-log.md:1488-1516` (2026-07-19, a nested-repo `git add .` false BLOCK, including a subtler path-collision variant where a same-named-but-different file in the nested repo was misjudged against the workspace-root copy). Both incidents also confirm the "widening the footprint does not clear the block" claim by direct reproduction, not inference.
  - **Re-derivation vs. the change description:** none — all claims re-derived and confirmed. Low.

## Mitigations (Item 3(a)/(b) — PROCEED-WITH-CAUTION)

- **Item 3(a) (Blast Radius High, mitigated):** Before writing the fix, confirm `NOTE=AXCION_LIVENESS_OVERRIDE=1 git reset --hard` currently passes against the unmodified hook (reproducing the defect as a red test) — the session plan's Stage 1 already specifies this; keep it. After the fix, add this exact case plus the newly-found quoted-commit-message variant (`git commit -m "…AXCION_LIVENESS_OVERRIDE=1…" && git reset --hard`) as permanent negative fixtures in `logs/scripts/test-destructive-liveness.sh`, which today has zero override coverage. Re-run the full 14-case harness (not just the new cases) before landing, since this file is a global SPOF.
- **Item 3(b) (Blast Radius High, mitigated; Principle Alignment Medium only if candidate (i)):** Adopt candidate (ii) (relabel the PreToolUse record to assert only "override requested and accepted," never "command succeeded") — it fully closes the defect as described, adds zero new wiring, and stays inside the existing Blast-Radius/Reversibility profile already being managed for 3(a) on the same file. If the session instead wants candidate (i) later, treat it as a separate, explicitly-scoped follow-up change (new PostToolUse registration on a global SPOF hook warrants its own `/risk-check`), not a rider on this landing.
- **Both:** name an explicit rollback plan in the commit message (this file is registered once, globally, and gates every destructive git command in every checkout) — matching the discipline already required of Item 1.

## Recommended redesign (Item 1 — drives the bundle RECONSIDER, second time)

- **Answer to point (B): the compound-`cd` path is designable and testable, but not via the candidate design as floated.** "Hard block only when confidently resolved, else degrade to warn" risks silently reopening the fail-open `979ed01` closed on this exact file (Dimension 5/6 above). A safer, buildable path exists using a precedent already live and tested in the same file: generalize `check-foreign-staging.sh:521-526`'s `cd X && <verb>` parsing (currently used only for candidate-subdir scoping) to resolve the repo **toplevel** itself — parse the leading `cd`, resolve it against the payload's `cwd`, run `git -C <resolved path> rev-parse --show-toplevel`. Any command shape that pattern cannot parse (nested `cd`s, `;`-chains, subshells, variable-substituted paths) must **fail closed** (hard block) — never soft-warn — preserving `979ed01`'s established "uncertain → protect" doctrine. Build fixture (iii) to prove both directions: successfully-parsed compound `cd` resolves correctly (no false block), and a deliberately-unparseable compound form fails closed rather than silently warning.
- **Answer to point (A): the "materially different shape" claim substantially holds, but is not a full defense of the letter.** Different file, different commit, and — independently confirmed by this gate, not accepted at face value — no shared code path in the actual fix diff (`_command_text_only`, the one function the two hooks do share, is untouched by either Item 3 fix). Item 3 is sequenced first and lands independently of Item 1's outcome, which is the practical property the prior redesign's "own dedicated session" recommendation was protecting — this gate scored Item 1 on its own merits, at the same bar as if it stood alone, and it still lands on RECONSIDER on those merits alone, unaffected by Item 3's presence. The one part of the prior recommendation not literally followed (a wholly separate session) turned out not to matter in practice here, precisely because Item 3 was structured as a non-gating addition rather than a risk-diluting bundle-mate.
- **Path: rescope, not revise-a-principle.** This is not a Dimension 6 High (no principle is clearly violated, only tension pending a design choice), so the remedy is a rescope of the fix's design, not a recorded OP-11 exception. Adopt the fail-closed-on-unparseable design above, build and pass all three Stage-3 fixtures against it (including the compound-`cd` fail-closed case), name the rollback plan, make the `.codex` fork call — then re-run `/risk-check` against the built, tested code rather than a still-open design candidate. A second RECONSIDER here is the gate working as designed, not a failure of this session's redesign effort — Items 3(a) and 3(b) are unaffected and should land now.

## Evidence-Grounding Note

All risk levels grounded in direct evidence: file/line re-reads (`check-destructive-liveness.sh`, `check-foreign-staging.sh` both canonical and `.codex` copies, `commit-discipline.md`, `close-worktree-session.md`, `test-destructive-liveness.sh`, `skills/handoff/SKILL.md`), live hook execution with crafted PreToolUse payloads (5 cases, including a self-caught and corrected methodology error and a third defect variant not in the source mission thread), git-object verification (`git show --stat 979ed01`, `git log -1 -- logs/scripts/search-canary.sh`), `wc -l` line-count comparison of the two `check-foreign-staging.sh` copies (668 vs. 464), direct read of `.codex/hooks.json` (confirms neither hook registered there), and `command grep -rl … --include="settings*.json"` across the workspace and `~/.claude` (confirms exactly one registration per hook, both at `~/.claude/settings.json`). No training-data fallback was used on any read or command.

# Risk Check — 2026-06-08

## Change

Implement the fix-spec §4 keystone bundle in the canonical research-workflow skills: (#17) add a Tier A–D risk-tiering control-effort model with a permission-ceiling cap, (#15) hide the tier/lens fields outside Stage-2 extracts and sufficiency tables, (#5) add a paywall fast-lane scarcity-audit execution rule, and (#1-lite) embed a source-plan table in the execution manifest. Edit surfaces: reference/quality-standards.md (new § Risk-Tier Model — chassis edit next to the immutable four-class permission machinery); ai-resources/skills/research-plan-creator/SKILL.md (assign risk-tier: per question at plan time + write an L-scale orthogonality guard, because a formal L1–L3 Depth Calibration Framework already lives there and must COEXIST, not be retired); ai-resources/skills/cluster-memo-refiner/SKILL.md and ai-resources/skills/claim-permission-gate/SKILL.md (read tier, apply control matrix + permission ceiling); ai-resources/skills/execution-manifest-creator/SKILL.md (#5 four-way classification + #1-lite table — output-contract change); .claude/commands/run-execution.md (fast-lane execution rule). All canonical skill edits route through /improve-skill, never hand-edit the symlink. Backward-compat: absence of risk-tier: defaults to Tier B = current uniform behavior. This bundle was scoped-down/deferred by three prior risk-checks (S3/S4/S5); the material change is that both blocking preconditions (Pass-3 Phase-B routing = Option B; #22 disconfirming-evidence field) landed in S5. Consuming projects: ≥4 (all projects that reference these canonical skills via reference/skills/* symlinks).

## Referenced files

- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/research-pe-regime-shift-advisory-gap/reference/quality-standards.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/skills/research-plan-creator/SKILL.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/skills/cluster-memo-refiner/SKILL.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/skills/claim-permission-gate/SKILL.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/skills/execution-manifest-creator/SKILL.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/research-pe-regime-shift-advisory-gap/.claude/commands/run-execution.md — exists

## Verdict

PROCEED-WITH-CAUTION

**Summary:** Both S5 preconditions are verified landed (Option-B Phase-B routing logged; #22 `Disconfirming evidence found` field present and genuinely no-reader), so the keystone is correctly de-blocked and is no longer the S4 RECONSIDER profile — but it carries a genuine High blast radius (a canonical chassis edit into the immutable permission region plus 4 canonical skills inherited by a *second* live project, with two carried-forward preconditions — the L-scale orthogonality guard and the canonical-template/`quality-standards.md` divergence — still unmet), which is reducible to Medium only under a specific, mandatory set of paired mitigations.

## Consumer Inventory

Search terms derived from referenced files + named contracts: the 4 skill basenames; the command token surfaces (`/improve-skill`, `run-execution`); contract markers `risk-tier:` / `Risk-Tier` / `Tier A`–`Tier D`, the four permission-class tokens (`SUPPORTED` / `PROXY-SUPPORTED` / `ILLUSTRATIVE-ONLY` / `NOT-SUPPORTED`), the six immutable § headings, `Disconfirming evidence found` (#22), `paywall` / `fast-lane` / `public-gated` / `scarcity`, `Depth Calibration` / `L1`–`L3` / `orthogonal`, `reference/skills/*` symlink targets. Searched `ai-resources/` + the workspace root (`projects/`, `archive/`). Design notes, prior risk-checks, consultations, logs, scratchpads excluded as non-runtime (they describe the change, they do not consume it).

| Consumer path | Reference type | Must change? |
|---|---|---|
| projects/research-pe-regime-shift-advisory-gap/reference/quality-standards.md | parses + co-edits — live enforcers read § Claim-Permission Classes / § Source-Diversity Matrix; #17 adds § Risk-Tier Model + permission-ceiling adjacent to the immutable four-class set | yes |
| ai-resources/skills/research-plan-creator/SKILL.md | co-edits — #17 adds `risk-tier:` per-question field + the L-scale orthogonality guard (guard verified ABSENT, see Dim 5); canonical (symlinked) | yes |
| ai-resources/skills/cluster-memo-refiner/SKILL.md | parses + co-edits — Check 9 parses the 4 class tokens + § headings as exact strings; #17 adds per-tier control depth + ceiling read; canonical | yes |
| ai-resources/skills/claim-permission-gate/SKILL.md | parses + co-edits — parses the 4 class tokens + § Claim-Permission Classes / § Source-Diversity Matrix; #17 adds permission-ceiling cap; canonical | yes |
| ai-resources/skills/execution-manifest-creator/SKILL.md | co-edits — #5 four-way classification + #1-lite source-plan table (output-contract change); canonical | yes |
| projects/research-pe-regime-shift-advisory-gap/.claude/commands/run-execution.md | co-edits — #5 fast-lane execution rule (project-local copy named in the change) | yes |
| ai-resources/workflows/research-workflow/.claude/commands/run-execution.md | co-edits (UNNAMED in change) — canonical template copy of run-execution carries the same Stage-2 manifest surface; #5 rule diverges if only the project copy is edited | yes (gap — see Dim 3) |
| ai-resources/workflows/research-workflow/reference/quality-standards.md | co-edits (UNNAMED in change) — canonical template chassis; § Risk-Tier Model lands only project-locally, so the canonical template diverges from the project chassis | yes (gap — see Dim 3) |
| projects/buy-side-service-plan/reference/skills/research-plan-creator (symlink) | imports — SECOND live project; inherits #17 `risk-tier:` field on next plan-creation run | no (additive/Tier-B default) but inherits |
| projects/buy-side-service-plan/reference/skills/cluster-memo-refiner (symlink) | imports — SECOND live project; inherits #17 tier-read + #15 field-exposure on next run | no (additive) but inherits |
| projects/buy-side-service-plan/reference/skills/execution-manifest-creator | imports — SECOND live project; inherits #5/#1-lite manifest output-contract change on next run | no (additive) but inherits |
| projects/buy-side-service-plan/reference/quality-standards.md | parses — buy-side has its OWN 3KB chassis with NO § Risk-Tier Model; a tier-driven permission-ceiling read must be presence-gated or it reads a missing section | no (only if tier-read is presence-gated) |
| ai-resources/skills/research-extract-verifier/SKILL.md | parses — reads the 4 permission-class tokens (exact-string) | no (additive — class set frozen byte-identical) |
| ai-resources/skills/section-directive-drafter/SKILL.md | parses — reads permission-class tokens; #15 hides tier/lens from directive artifacts (this is a target of #15's field-exposure change) | no (only if #15 hides, not renames) |
| ai-resources/workflows/research-workflow/reference/stage-instructions.md + run-synthesis.md + claim-permission.template.md + known-limits.template.md | documents/parses — reference the permission-class tokens / pipeline positions | no (frozen class set) |
| ai-resources/skills/{cluster-analysis-pass, evidence-to-report-writer, cluster-synthesis-drafter, transaction-table-builder, country-parity-checker} | invokes the edited skills downstream | no (additive) |

Total: ~18 distinct consumers; **8 must-change** (the project chassis + 4 canonical skills + project run-execution.md, all named; PLUS 2 unnamed canonical-template co-edits — canonical `run-execution.md` and canonical `quality-standards.md` — that the change description omits). One additional conditional must-change: the `buy-side` private chassis lacks § Risk-Tier Model, so the tier-read must be presence-gated. The ≥4-project inherit-on-next-run reach is confirmed: a *second* live project (`buy-side-service-plan`) symlinks `research-plan-creator`, `cluster-memo-refiner`, and `execution-manifest-creator`, so the canonical edits propagate beyond the first-adopter project automatically.

Verified state (grep, this session): #22 field `Disconfirming evidence found` is PRESENT in both executor skills (research-prompt-creator/SKILL.md:110 emit, research-extract-creator/SKILL.md:170 capture) and self-declares no-reader — precondition 2a landed. The Option-B Phase-B routing decision is logged (decisions.md S5 2026-06-08) — precondition (item 3) landed. The L-scale orthogonality guard returns ZERO hits in research-plan-creator/SKILL.md (`orthogonal` / `risk-tier` / `control-effort`) — still unwritten. No live skill/command parses a `risk-tier:` field today (zero runtime parsers; grep hits are unrelated `Tier A` / project-planning `risk-tier` usage) — so #17 ships its contract and consumers together, not speculatively.

## Dimensions

### Dimension 1: Usage Cost
**Risk:** Low

- No always-loaded file is touched. `reference/quality-standards.md:11` self-declares "Not needed for every turn"; SKILL.md files load only on skill invocation; `run-execution.md` loads only when the command runs. The new § Risk-Tier Model (~30–50 lines per design-note §4), the #1-lite manifest table, and the #5 fast-lane rule all ride on-demand loads — paid only when those skills/commands run.
- No SessionStart/PreToolUse/Stop hook, no `@import` into an always-loaded file, no broad-trigger skill description added. No frequently-spawned subagent brief is enlarged in a per-session-recurring way.
- Holds Low across both repos.

### Dimension 2: Permissions Surface
**Risk:** Low

- No `settings.json` / `settings.local.json` change; no `allow` / `ask` / `deny` entry; no new Bash / Write / external-API pattern. The change edits markdown skill + reference + command content only.
- The #17 "permission ceiling" is an *evidence-permission-class cap* (SUPPORTED / PROXY-SUPPORTED / ILLUSTRATIVE-ONLY — see quality-standards.md:122–129), not a Claude Code tool permission. It is unrelated to the harness permissions surface. The #5 fast-lane is a search-effort bound over manually-operated GPT/Perplexity sessions, not a new tool family.
- No scope escalation (project → user); no new cross-repo or external capability. Holds Low.

### Dimension 3: Blast Radius
**Risk:** High

- Grounded in the inventory: **8 must-change consumers**, of which the 4 SKILL.md edits are canonical regular files reached via `reference/skills/*` symlinks (verified `ls -la`: `claim-permission-gate -> ../../../../ai-resources/skills/...` etc.). Editing any is a canonical edit, not project-local. The ≥4-project reach is confirmed concretely: `buy-side-service-plan` is a *second live project* symlinking `research-plan-creator`, `cluster-memo-refiner`, and carrying `execution-manifest-creator` — it inherits #17 / #15 / #5 / #1-lite on its next run with no opt-in.
- **Immutable-region contact (the load-bearing finding).** `cluster-memo-refiner` Check 9 (SKILL.md:222–234) and `claim-permission-gate` (SKILL.md:49–66) parse the four class tokens + the § headings as EXACT strings; `quality-standards.md:7` declares these "canonical-immutable (must not be rephrased or reordered without `/risk-check` re-fire)." #17's permission-ceiling is the single point of contact with that region — a CAP, not a re-grade. This re-fire IS that required `/risk-check`. The region must be frozen byte-identical while § Risk-Tier Model is added adjacent.
- **Two unnamed canonical-template consumers — a blast-radius gap the change description misses.** The change names only the project-local `run-execution.md` and the project `quality-standards.md`. But a canonical template copy of BOTH exists: `ai-resources/workflows/research-workflow/.claude/commands/run-execution.md` (carries the same Step-2.0 manifest surface) and `ai-resources/workflows/research-workflow/reference/quality-standards.md` (the template chassis). If only the project copies are edited, the #5 fast-lane rule and the § Risk-Tier Model land project-locally while the canonical template silently diverges — future workflow instantiations get a tier-aware `execution-manifest-creator`/`cluster-memo-refiner` but a chassis with no § Risk-Tier Model to read. This is the exact S2 Option-A-vs-B canonical-divergence problem (decisions.md S2 2026-06-08), and the change description does not name it.
- **`buy-side` chassis mismatch.** `buy-side-service-plan/reference/quality-standards.md` is a 3 KB chassis with NO § Risk-Tier Model. If `cluster-memo-refiner` / `claim-permission-gate` read a tier-driven ceiling unconditionally, on buy-side's next run they parse a section that is not there. The tier-read MUST be presence-gated (absent § Risk-Tier Model → Tier-B uniform = current behavior) for the backward-compat claim to actually hold for the second project, not just the first.
- The High is driven by: canonical 4-skill reach × second-project inheritance × immutable-region contact × two unnamed template co-edits × a conditional cross-project chassis mismatch. It is reducible to Medium under the Mitigations below (freeze the immutable region; presence-gate the tier-read; either land the canonical-template edits in lockstep or log the divergence loudly per OP-11).

### Dimension 4: Reversibility
**Risk:** Medium

- All edits are additive markdown — a per-file `git revert` restores prior text. But the keystone spans **two git trees**: the 4 skill edits + (if landed) the canonical-template copies live in the `ai-resources` repo, while the project `quality-standards.md` + project `run-execution.md` live in the project tree. A full revert is cross-repo and not atomic: a partial revert leaves the skills tier-aware while the chassis is un-tiered (or vice versa) — a stale split where `cluster-memo-refiner` reads a § Risk-Tier Model that the reverted chassis no longer defines.
- Staging canonical-before-project (or reverting project-before-canonical) is the discipline that keeps the split safe; it must be an explicit landing-order rule.
- Not High because: project 1.1 is DELIVERED (decisions.md S1 2026-06-05) so there are no in-flight 1.1 artifacts to re-grade under a tier model; the backward-compat default (un-tiered → Tier B) means a revert degrades gracefully to current behavior rather than breaking; and push is batched/gated (workspace CLAUDE.md § Push behavior), so nothing propagates beyond local git this session.
- Routing edits through `/improve-skill` (not hand-edit through the symlink) keeps each skill edit a clean single-file change in the canonical tree.

### Dimension 5: Hidden Coupling
**Risk:** Medium

- **L-scale orthogonality guard — STILL UNWRITTEN (verified, load-bearing).** The live L1–L3 Depth Calibration Framework is present in research-plan-creator/SKILL.md (lines 52–81 definitions/rules, 187–204 question-type depth map, 260–266 completion criteria, 302 source-req map, 368 depth tag) — a knowledge-depth axis. #17's `risk-tier:` (Tier A–D) is an orthogonal control-effort axis. The resolution is COEXIST with no aliasing (decisions.md S4 2026-06-08, overturning design-note §3.1's retire-L3 recommendation). But grep for `orthogonal` / `risk-tier` / `control-effort` in the live SKILL.md returns ZERO hits — the guard is a planned-but-absent mitigation. Until it is written into the SKILL.md adjacent to the L1–L3 block, two depth-ish axes sit in one skill with only the design note (which the skill does not load) preventing future drift toward aliasing. This is the dominant residual coupling and keeps Dim 5 at Medium.
- **New tier read-contract (ships with consumers — good).** `research-plan-creator` WRITES `risk-tier:`; `cluster-memo-refiner` + `claim-permission-gate` READ it. Grep confirms zero existing runtime parsers, so the contract is not speculative — it lands with both ends. But the read-contract (field name, Tier→ceiling mapping per design-note §3.2: A/B → SUPPORTED, C → PROXY-SUPPORTED, D → ILLUSTRATIVE-ONLY max) must be written EXPLICITLY into the consuming SKILL.md files at the change site, and the read must be presence-gated (missing field / missing § Risk-Tier Model → Tier B), or the second project (buy-side, with no tier-aware plans and a tier-less chassis) hits an undefined read.
- **#5 fast-lane couples to two non-named dependencies.** Design-note §5 states #5 lands "after #17 (the tier sets whether a gated need clears the override bar) and #3 (native-language column)," and the fast-lane consults `known-limits.md` (#24 register, design-note §3 step 2). The change description does not name `known-limits.md` or #3 as dependencies. If `known-limits.md` is absent in a consuming project, the fast-lane's register-check step (the anchor that prevents misclassifying a findable need as `public-gated`) silently no-ops — the documented mitigation against the #5 risk (design-note §3 "What it risks") degrades.
- **#15 field-hiding — verify no downstream parser reads tier/lens from a hidden artifact.** Design-note §3.3 open question #3 flags this exactly: #15 hides tier/lens from chapter directives / prose drafts. If `section-directive-drafter` or `evidence-to-report-writer` currently reads a tier/lens field from a directive artifact, hiding it breaks that read. Grep before hiding (the design note's own instruction) is a required pre-edit step, not yet done.
- Not High because the L-scale collision is genuinely resolved (coexistence, on the record) and the tier contract ships with its consumers; the residual is unwritten-guard + unnamed-dependency coupling, both closable at the change site.

### Dimension 6: Principle Alignment
**Risk:** Medium

Principles-base read at `projects/strategic-os/ai-strategy/principles-base.md` (frozen-ID index; IDs verified present: OP-1/2/3/5/9/10/11/12, DR-1/2/3/7/8/9, QS-1/5/6/9, AP-4/6/7/9). Checks applied: speculative abstraction (OP-9 / DR-7 / AP-7), closure-before-detection (OP-12), advisory-vs-enforcement (OP-5), automate-execution-gate-judgment (OP-2), loud-revision (OP-11 / OP-3), placement (DR-1 / DR-3), gated structural change (DR-8), canonical-pipeline (DR-2), impact-analysis (DR-9 / AP-6).

- **OP-9 / DR-7 / AP-7 (speculative abstraction) — now SATISFIED, the key change from S5.** At S4/S5 the bundle was a DR-7 violation candidate because #4 rode a verified-absent #22 contract. This session #22 is verified PRESENT and no-reader (research-prompt-creator:110, research-extract-creator:170), and the Option-B routing is logged — both confirmed-consumer preconditions exist. The risk-tier model is not speculative: its consumers (#5, #15, the tier-read in cluster-memo-refiner/claim-permission-gate) land in THIS bundle, and the second confirmed consumer (DR-7's bar) is real — `buy-side-service-plan` is a live second project on the same symlinked skills. So the tier model is a "second confirmed consumer exists" generalization, not a hook-for-later. This is principle-positive relative to S5.
- **OP-5 (advisory → enforcement) — Medium tension, must be a recorded decision.** The permission-ceiling moves `claim-permission-gate` from pure advisory grading toward an enforced cap (design-note §3.2 / open-question #2 recommends "hard cap for D, advisory for C"). OP-5 permits enforcement only as an explicit, per-component recorded decision — never a silent upgrade. The implementation MUST land the ceiling as an explicit OP-5 decision recorded in decisions.md (hard-cap-D / advisory-C), or it is a silent enforcement drift. This is handled-if-recorded, not a violation in itself.
- **OP-11 / OP-3 (loud revision) — partially at risk via the canonical-template divergence.** The L-scale coexistence reversal of design-note §3.1 is already loud and recorded (decisions.md S4). But the Dim-3 finding — editing the project chassis + project run-execution while leaving the canonical template copies un-edited — would be a silent canonical divergence unless either (a) the template copies are edited in lockstep, or (b) the divergence is logged loudly in the ai-resources improvement-log, exactly as the S2 #23 Option-A decision required ("Option A + mandatory log, or it is not Option A," decisions.md S2). Without one of those, this drifts silently — an OP-11 violation. Mitigable, not yet met.
- **OP-12 (closure before detection) — not implicated by THIS bundle.** The #4 counter-search (the new detection capability) is explicitly NOT in this bundle (CHANGE_DESCRIPTION lists #17/#15/#5/#1-lite only; #4 is deferred to its own session per decisions.md S5). #5's fast-lane is a closure/effort-bounding rule, not a new detection engine. So OP-12 does not count against this bundle.
- **DR-8 (gated structural change) — honored by this re-check.** Each canonical edit (#17 chassis + 4 skills, #5/#1-lite output contract) is a gated class being plan-time risk-checked here; the binding-verdict/no-downgrade rule applies.
- **DR-2 (canonical pipeline) — honored as specified.** The change routes all 4 skill edits through `/improve-skill` ("never hand-edit the symlink") — DR-2-aligned.
- **DR-9 / AP-6 (impact analysis) — OUTSTANDING.** The S4/S5 binding preconditions named a DR-9 top-3 runtime-impact analysis on the highest-traffic consumers; the inventory here is a static grep, not the DR-9 dynamic analysis. DR-9 must run before commit (a carried-forward precondition, not met by this static inventory).
- **DR-1 / DR-3 (placement) — aligned with one caveat.** § Risk-Tier Model in the chassis, tier field in research-plan-creator, ceiling read in the gate skills, #1-lite table in the manifest — all correct homes. The caveat is the canonical-template placement: the chassis edit belongs canonically (the template), not only project-locally, or DR-1's "could this serve more than one project?" test (it can — buy-side + future instantiations) is failed by a project-only landing.
- **OP-10 (system boundary) — not implicated.** No cross-tool (GPT/Perplexity/NotebookLM) governance is added; #5 bounds the operator's own manual GPT/Perplexity effort, it does not govern those tools.
- **Net:** the load-bearing S4/S5 violation (speculative #4) is removed because #4 is not in this bundle and its precondition landed. The residual is one OP-5 enforcement-upgrade tension (handled-if-recorded) and one OP-11 silent-divergence risk in the canonical-template handling (handled-if-edited-in-lockstep-or-logged). No clear unacknowledged violation → Medium, not High.

## Mitigations

_Verdict is PROCEED-WITH-CAUTION. Dimension 3 is High; the mitigations below reduce it to Medium. The first three are mandatory — without them the Dim-3 High has no viable in-session mitigation and the verdict would be RECONSIDER._

- **[MANDATORY — Dimension 3, immutable region] Freeze the canonical-immutable region byte-identical.** Before editing `quality-standards.md`, treat the four permission-class names, the permitted-prose-verb lists, the six § headings (Claim-Permission Classes, Source-Diversity Matrix, No-Source-Substitution Rule, Country Coverage Table, Research Stop Conditions, Source-Conflict Resolution Procedure), and the gate-clearance schema as untouchable. Add § Risk-Tier Model as a NEW adjacent subsection; the permission-ceiling references the existing class names without rephrasing or reordering them. Diff-verify the immutable region is unchanged after the edit (this IS the `/risk-check` re-fire that quality-standards.md:7 requires).
- **[MANDATORY — Dimension 3, second-project safety] Presence-gate the tier-read.** Write the tier-read in `cluster-memo-refiner` + `claim-permission-gate` so that an absent `risk-tier:` field OR an absent § Risk-Tier Model resolves to Tier B = current uniform behavior. Verify against `buy-side-service-plan/reference/quality-standards.md` (which has no § Risk-Tier Model) that its next `cluster-memo-refiner`/`claim-permission-gate` run is unchanged — the backward-compat claim must hold for the second project, not only the first-adopter.
- **[MANDATORY — Dimension 3 / OP-11] Resolve the canonical-template divergence explicitly.** The change names only the project `run-execution.md` and project `quality-standards.md`, but canonical template copies of both exist (`ai-resources/workflows/research-workflow/.claude/commands/run-execution.md`, `ai-resources/workflows/research-workflow/reference/quality-standards.md`). Either (a) edit the canonical template copies in lockstep so the template does not diverge, OR (b) deliberately land project-only and log the canonical divergence loudly in the ai-resources improvement-log per the S2 #23 precedent ("Option A + mandatory log"). Do not land project-only silently.
- **[Dimension 5] Write the L-scale orthogonality guard into research-plan-creator/SKILL.md at the change site.** Verified absent today. Add an explicit note adjacent to the L1–L3 block (lines 52–81) stating that `risk-tier:` (control-effort, Tier A–D) and the depth level (L1–L3, knowledge-depth) are orthogonal, never aliased, never substituted — per decisions.md S4. This is load-bearing, not optional; without it the two axes drift toward aliasing.
- **[Dimension 5] Grep-before-hide for #15, and name #5's hidden dependencies.** Before #15 hides tier/lens from directive/prose artifacts, grep `section-directive-drafter` + `evidence-to-report-writer` to confirm no current parser reads those fields (design-note §3.3 OQ#3). For #5, document the `known-limits.md` (#24) register-check and #3 native-language dependency at the change site, and presence-gate the register check (absent register → fast-lane proceeds without the anchor, with a logged caveat).
- **[Dimension 6 / OP-5] Land the permission-ceiling as an explicit recorded OP-5 decision** in decisions.md (hard-cap for Tier D, advisory-override for Tier C per design-note OQ#2), so the advisory→enforcement upgrade on `claim-permission-gate` is a recorded per-component decision, not silent drift.
- **[Dimension 4 — cross-repo] Stage canonical-before-project and route every skill edit through `/improve-skill`.** The keystone spans two git trees; land the `ai-resources` edits first, the project chassis second, so a partial state never leaves skills tier-aware against an un-tiered chassis. Never hand-edit through the `reference/skills/*` symlink (DR-2).
- **[Dimension 6 / DR-9 — outstanding precondition] Run the DR-9 top-3 runtime-impact analysis before commit.** This static grep inventory is not the DR-9 dynamic analysis the S4/S5 preconditions require. Run it on the top-3 highest-traffic consumers (cluster-memo-refiner Check 9, claim-permission-gate, execution-manifest-creator) before committing item 1.
- **[QC gate] Independent `/qc-pass` before commit; defer via `/handoff` QC-PENDING if unreachable.** This is a canonical High-blast-radius edit — per workspace CLAUDE.md § QC Independence Rule + DR-8/QS-1/QS-9 it is commit-blocked without reachable independent QC. If the subagent QC gate is unreachable in this session (1M-credit gate + >200k-token conversation), defer the commit via `/handoff` to a fresh session.

## Evidence-Grounding Note

All risk levels grounded in direct evidence: `ls -la` confirming the four skills are canonical regular files reached via `reference/skills/* -> ../../../../ai-resources/skills/*` symlinks; `find` confirming `buy-side-service-plan` is a second live project symlinking research-plan-creator + cluster-memo-refiner and carrying execution-manifest-creator + its own tier-less `quality-standards.md`; `grep` confirming the #22 `Disconfirming evidence found` field is PRESENT and no-reader in both executor skills (research-prompt-creator:110, research-extract-creator:170) — precondition 2a landed; `grep` confirming ZERO `orthogonal`/`risk-tier`/`control-effort` hits in research-plan-creator/SKILL.md — L-scale guard still unwritten; `find` + `grep` confirming canonical-template copies of run-execution.md and quality-standards.md exist and are unnamed in the change; quality-standards.md:5,7,11,118 for the canonical-immutable enforcer-parsed region and load-on-demand status; cluster-memo-refiner:222–234 + claim-permission-gate:49–66 for the exact-string class-token parsing; decisions.md S2 (canonical-divergence precedent), S4 (L-scale coexistence + deferral), S5 (Option-B routing + #22 no-reader contract) for the carried-forward decisions; principle IDs read from principles-base.md by frozen ID. No training-data fallback was used on any read.

## Architectural Commentary

_System-owner second opinion (Function B — pre-change advisory), invoked automatically because the verdict is PROCEED-WITH-CAUTION. Full advisory on disk: projects/axcion-ai-system-owner/output/consultations/consult-2026-06-08-risk-check-second-opinion-fix-spec-4-keystone-bun.md_

**CONCUR** with PROCEED-WITH-CAUTION, and CONCUR with all four mandatory mitigations. The verdict is correctly de-blocked (S5 preconditions verified landed) and correctly held below GO. The risk-check's consumer inventory caught the two unnamed canonical-template co-edits the change description omits — the surface that would have bitten silently.

Residual risk is entirely in **landing order**, not in whether to proceed. Two re-orderings:

**1. Write and dry-run-prove the presence-gate against the `buy-side` tier-less chassis FIRST — before any skill or chassis edit.** buy-side-service-plan is the only consumer that exercises the *double-absent* path (missing `risk-tier:` field AND missing § Risk-Tier Model) on its next run. The project being edited (research-pe-regime-shift) will have the chassis section present, so it never exercises that path during testing — the gate cannot be validated in the project you're editing. Harden mitigation 2 from "diff the section" to "dry-run the two gate skills against the buy-side chassis and confirm Tier-B-uniform output" (OP-3, loud failure, applied pre-emptively: a mis-gated read is a plausible wrong grade, not a visible break).

**2. Take mitigation 3 option (a) — edit the two canonical templates in lockstep — NOT option (b).** Option (b) (land project-only + log) does not defer the divergence — it manufactures a permanent born-divergent chassis on every future `/new-project` scaffold (tier-aware symlinked skills + a chassis with no § Risk-Tier Model to read). A logged divergence is detection without closure (OP-12 counts that against a change). Option (b) is only for when the canonical template genuinely can't be reached this session.

One under-specified risk: the review frames "canonical-before-project" as *revert* safety. The sharper forward risk is that if canonical skills commit-and-push before the gate is proven, buy-side goes live on the new contract against its un-tiered chassis with an untested gate. So proving the gate (step 1) must precede the skill edits; the ai-resources edits (4 skills + both canonical templates) land as one batch before the project tree.

Concur as written on: DR-9 top-3 runtime analysis, the OP-5-recorded permission-ceiling decision, the L-scale orthogonality guard, and the independent `/qc-pass` gate (commit-blocked without it per QS-1/QS-9).

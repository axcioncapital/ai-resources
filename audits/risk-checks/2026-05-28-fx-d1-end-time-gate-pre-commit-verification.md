# Risk Check — 2026-05-28

## Change

FX-D1 end-time gate (before commit). Plan-time re-run verdict was PROCEED-WITH-CAUTION with 6 mitigations + System Owner advisory (no Highs). All 6 mitigations applied during execution. Executed change set on disk: (1) canonical stage-5-paths.template.md added `Mechanical trigger threshold:` field to both schemas + new "Default-value semantics for Mechanical trigger threshold:" section (M3 graceful-default semantics applied); (2) canonical project-config-schema.md updated header status + added new "Where Stage 5 tunables live" section (path-config-canonical-vs-CLAUDE.md-canonical rule + promotion rule); (3) canonical produce-formatting.md added Phase 0 step 6 reading threshold (graceful default 5+; malformed-value halt) + Phase 2 step 6 task brief includes load-bearing `Mechanical trigger threshold: {threshold}` pass (M1 explicit pass) + trigger list expanded from 5 to 8 with project-neutral language; (4) canonical prose-formatter SKILL.md added M2 runtime-override note at LOAD-BEARING site (top of Mechanical Triggers section) + unified Trigger #1 at "5+" canonical default + updated Operation 2 + Operation 0 output template + Bias Countering for consistency; (5) canonical produce-prose-draft.md added "Scope: light polish only" paragraph (graduated from nordic-pe fork, project-neutral); (6) canonical ai-resource-creation.md added DR-7 graduation-confirmation rule as numbered rule #5 (M6); (7) nordic-pe reference/stage-5-paths.md set `Mechanical trigger threshold: "3+"` with M4 operator-facing consumer comment; (8) nordic-pe 3 Stage 5 commands (produce-prose-draft, produce-formatting, produce-jargon-gloss) swapped from forks to symlinks (interleaved dry-runs all passed); (9) buy-side produce-jargon-gloss.md filled with new symlink to canonical; (10) buy-side existing 2 symlinks verified (resolve to current canonical); (11) nordic-pe CLAUDE.md § Stage 5 Polish Commands narrative rewritten to reflect symlink-only posture + named the `Mechanical trigger threshold: "3+"` declaration; (12) friday-checkup.md item M comment block added documenting c1/c2 apply-by-removal rationale + return-condition for future fork re-creation.

## Referenced files

- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/workflows/research-workflow/reference/stage-5-paths.template.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/workflows/research-workflow/docs/project-config-schema.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/workflows/research-workflow/.claude/commands/produce-formatting.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/workflows/research-workflow/.claude/commands/produce-prose-draft.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/skills/prose-formatter/SKILL.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/docs/ai-resource-creation.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/.claude/commands/friday-checkup.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/nordic-pe-macro-landscape-H1-2026/CLAUDE.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/nordic-pe-macro-landscape-H1-2026/reference/stage-5-paths.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/nordic-pe-macro-landscape-H1-2026/.claude/commands/produce-prose-draft.md — exists (symlink verified)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/nordic-pe-macro-landscape-H1-2026/.claude/commands/produce-formatting.md — exists (symlink verified)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/nordic-pe-macro-landscape-H1-2026/.claude/commands/produce-jargon-gloss.md — exists (symlink verified)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/projects/buy-side-service-plan/.claude/commands/produce-jargon-gloss.md — exists (symlink verified)
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/risk-checks/2026-05-28-fx-d1-plan-time-gate-graduate-then-swap-symlinks.md — exists
- /Users/patrik.lindeberg/Claude Code/Axcion AI Repo/ai-resources/audits/risk-checks/2026-05-28-fx-d1-plan-time-gate-rerun-revised-plan-parameterized.md — exists

## Verdict

PROCEED-WITH-CAUTION

**Summary:** All 6 mitigations from the plan-time gate are applied on disk in the form the plan-time pass required (verified site-by-site by grep + line-level read); all 4 symlinks resolve to canonical (`lrwxr-xr-x` confirmed on `ls -la`); canonical produce-formatting.md is fully project-neutral (zero grep hits for Sweden/Norway/Finland/Nordic); the threshold contract is consistent across the 5 sites (template + schema cross-reference + canonical command + nordic-pe instance + SKILL.md override note). One new end-time risk emerges: the ai-resources working tree contains unstated cross-scope contamination (`consult.md` + `agent-tier-table.md` edits referencing a `project-manager` agent — not part of FX-D1 plan scope) which must NOT be included in the FX-D1 commit. The plan called for clean per-repo commits (3 separate); the ai-resources repo as-is cannot satisfy that without explicit file selection at `git add` time.

## Dimensions

### Dimension 1: Usage Cost
**Risk:** Low

- All canonical edits land in load-on-demand files. `produce-formatting.md` line 2-3 declares `model: sonnet` (not auto-loaded); `produce-prose-draft.md` line 2-3 declares `model: opus` (not auto-loaded). Schema and template files are load-on-demand reference docs.
- Workspace CLAUDE.md and the workspace-level harness file are untouched. The nordic-pe CLAUDE.md edit at lines 99–105 stays inside the already-loaded `## Stage 5 Polish Commands` section — net per-session token delta ≈ +180 tokens (verified: rewritten paragraph is longer than the prior version by ~9 sentence-length lines + the new c1/c2 apply-by-removal block). This is the only always-loaded delta in the change set.
- Net always-loaded cost: ~180 tokens added to nordic-pe sessions only (does not affect other projects). Buy-side and the ai-resources workspace see zero always-loaded delta.
- Per-invocation cost of `/produce-formatting` grows by the threshold-resolution block (~150 tokens at Phase 0 + ~80 tokens at Phase 2 subagent brief expansion) and the expanded 8-trigger list — matches plan-time projection.

### Dimension 2: Permissions Surface
**Risk:** Low

- No `settings.json` edits in the FX-D1 change set on disk. The buy-side `.claude/settings.json` shows ` M` modified status but that is pre-existing repo state (not introduced by FX-D1; the working tree existed before this session per the git-status long-running modifications list including FX-B1 artifacts).
- All `rm + ln -s` operations on nordic-pe and buy-side commands resolved (4 symlinks verified via `ls -la` showing `lrwxr-xr-x` and target paths into `../../../../ai-resources/workflows/research-workflow/.claude/commands/`). No new tool family was needed beyond the already-Bash-allowed `rm` and `ln`.
- No new deny rule removed; no glob widened; no cross-repo capability that wasn't already in use during FX-B1's 4-repo commit.

### Dimension 3: Blast Radius
**Risk:** Medium

- **Direct file count: 10 modified + 4 symlink ops + 0 new files in scope.** Modified: 7 canonical files (`produce-formatting.md`, `produce-prose-draft.md`, `prose-formatter/SKILL.md`, `stage-5-paths.template.md`, `project-config-schema.md`, `ai-resource-creation.md`, `friday-checkup.md`) + 3 project files (nordic-pe `CLAUDE.md`, `reference/stage-5-paths.md`, friday-checkup item M block which is in ai-resources). Symlink ops: 3 nordic-pe (type-change `T` confirmed in `git status`) + 1 buy-side new symlink.
- **Threshold contract sites: 5, all consistent (verified by grep).** `Mechanical trigger threshold` appears in: (a) `stage-5-paths.template.md` lines 37 (report mode), 72 (section mode), 85–96 (default-value semantics block); (b) `project-config-schema.md` lines 3 (header status), 125 (single-consumer Stage 5 tunable table row), 128 (promotion rule); (c) canonical `produce-formatting.md` lines 34–38 (Phase 0 step 6), 80, 82 (Phase 2 subagent brief); (d) nordic-pe `reference/stage-5-paths.md` line 17 with operator-facing comment; (e) `prose-formatter/SKILL.md` lines 62 (load-bearing override note), 64 (Operation 2 base rule with runtime-override wording), 115 (Op 2 default + override repeat), plus Operation 0 output template at line 237 + Bias Countering at line 296. Field name is identical across all sites (no rename hazard).
- **Caller count for canonical produce-formatting.md: 2 projects (nordic-pe via new symlink + buy-side via existing symlink).** Both resolve to the same canonical file. The threshold field is single-consumer at landing — only produce-formatting.md reads it; produce-prose-draft.md and produce-jargon-gloss.md do not (verified — grep for "Mechanical trigger threshold" in those two files returns zero hits).
- **CROSS-SCOPE CONTAMINATION in ai-resources working tree (NEW RISK).** `git status` in ai-resources shows `.claude/commands/consult.md` and `docs/agent-tier-table.md` modified, plus untracked `.claude/agents/project-manager.md`, `.claude/commands/pm.md`. `git diff` confirms these belong to a separate `/pm` (project-manager) initiative — NOT in the FX-D1 plan scope or the executed-change-set list. If a single ai-resources `git add -A` is used, the FX-D1 commit will silently absorb the `/pm` work. The plan-time gate's commit-discipline assumption (clean 3-repo split) is violated unless explicit file selection is used at staging.
- **Pre-existing untracked artifacts (not contamination, but worth naming).** ai-resources `audits/risk-checks/*fx-d1*plan-time*.md` (the 2 plan-time gate reports) and nordic-pe's many scratchpads + plan files + decision-archive items are pre-existing untracked artifacts. They are separate from FX-D1 but live in the same working tree. Each repo will need explicit `git add` of the FX-D1 paths only.

### Dimension 4: Reversibility
**Risk:** Medium

- Single-file edits to canonical (`produce-formatting.md`, `produce-prose-draft.md`, `prose-formatter/SKILL.md`, `stage-5-paths.template.md`, `project-config-schema.md`, `ai-resource-creation.md`, `friday-checkup.md`) all revert cleanly via `git revert` of the ai-resources commit.
- The 3 nordic-pe fork-to-symlink swaps are `T` type changes (regular file → symlink). `git revert` of the nordic-pe commit restores the original fork files because git tracks the swap as a unified blob change — verified by `git status --short` showing ` T .claude/commands/produce-formatting.md` etc.
- The buy-side new symlink (`produce-jargon-gloss.md`) is a brand-new file — `git revert` removes it cleanly.
- **Partial-revert hazard (M4 mitigates).** If ai-resources canonical is reverted but nordic-pe is not, nordic-pe's `reference/stage-5-paths.md` line 17 retains `Mechanical trigger threshold: "3+"` which a reverted canonical command no longer parses. The M4 operator-facing comment (`# consumed by produce-formatting.md Phase 0 — overrides the canonical 5+ default for this project per the unconditional-threshold rule (see report/diagnostics/1.1/r1-workflow-fix-implementation-spec-v2-2026-05-19.md line 320)`) makes the dependency visible to a future partial-revert reviewer — verified at line 17.
- Multi-repo commit discipline (3 separate commits) is the right shape, but reversibility depends on commit cleanliness. If the ai-resources commit accidentally absorbs the `consult.md` + `agent-tier-table.md` + `project-manager.md` edits, a `git revert` of FX-D1 would also revert those unrelated changes — a wider blast radius than the FX-D1 scope intends.

### Dimension 5: Hidden Coupling
**Risk:** Medium

- **M1 verified — explicit threshold pass at Phase 2 subagent brief.** `produce-formatting.md` line 80 (verbatim): `**`Mechanical trigger threshold: {threshold}`** (verbatim, interpolated from Phase 0 step 6 — typically `"5+"` canonical default, or a project override like `"3+"`). The subagent MUST honor this passed threshold for Mechanical Trigger #1 (parallel-items detection), overriding the SKILL.md `5+` default for this invocation.` Surrounding instruction at line 82 (task brief step 0): `(1) `{threshold}` parallel items in prose — use the passed threshold value, NOT the SKILL.md default;`. Override discipline is explicit and load-bearing at the subagent brief.
- **M2 verified — load-bearing runtime-override note at TOP of Mechanical Triggers section.** SKILL.md line 56 is the section header `### Mechanical Triggers (pre-scan)`; line 62 is the blockquote: `> **Runtime threshold override (load-bearing).** Trigger #1's threshold is the canonical default `5+` in this skill, but calling commands MAY override it by passing `Mechanical trigger threshold: <value>` (e.g., `"3+"`) in the task brief. When the calling command passes a `Mechanical trigger threshold` parameter, the passed value is authoritative for that invocation; this skill's `5+` default applies only when no override is passed. The canonical caller `produce-formatting.md` reads the threshold from each project's `reference/stage-5-paths.md` (graceful-default to `5+` if absent) and ALWAYS passes the resolved value. Other callers should follow the same pattern.` This is a blockquote at line 62, not a footnote — load-bearing placement satisfied. Wording matches plan-time mitigation almost verbatim.
- **M3 verified — graceful-default semantics declared in BOTH sites.** (a) `stage-5-paths.template.md` lines 85–96 contains the new section `## Default-value semantics for Mechanical trigger threshold:` with a 4-row table (absent → 5+ no halt; empty value → 5+ no halt; valid value → use verbatim + override SKILL.md default; malformed → halt). (b) `project-config-schema.md` line 3 cross-references it: `the threshold-class field documented in reference/stage-5-paths.md § Default-value semantics for Mechanical trigger threshold: lives in the stage-5 path-config file rather than this schema (path-config is the canonical site for per-project Stage 5 tunables — see § Where Stage 5 tunables live below)`. Schema line 125 names the field in the Stage 5 tunables table with explicit graceful-default reference. Both sites present.
- **M4 verified — nordic-pe operator-facing comment naming the consumer.** nordic-pe `reference/stage-5-paths.md` line 17 (verbatim): `**Mechanical trigger threshold:** "3+"  # consumed by produce-formatting.md Phase 0 — overrides the canonical 5+ default for this project per the unconditional-threshold rule (see report/diagnostics/1.1/r1-workflow-fix-implementation-spec-v2-2026-05-19.md line 320)`. Consumer dependency visible to future partial-revert reviewer.
- **M5 verified — field-consistency at the canonical-command landing moment.** Grep across the 5 sites (template + schema + canonical command + nordic-pe instance + SKILL.md) returns the field name `Mechanical trigger threshold` consistently in all five; no typos, no casing drift, no plural/singular variants. Buy-side `reference/stage-5-paths.md` correctly omits the field (graceful default to 5+ applies at runtime).
- **M6 verified — DR-7 rule placed as numbered rule #5.** `ai-resource-creation.md` lines 7, 9, 11, 13, 15 contain rules #1–5 in the numbered block. Line 15: `5. **Graduation requires confirmation from every existing canonical consumer.** Before graduating a project fork to canonical (i.e., extending the canonical contract with content that originated in one project), enumerate every existing canonical consumer (other projects that consume the resource via symlink or import) and confirm with the operator that each consumer is OK absorbing the change. Record the confirmation in the commit message. **Reason:** `principles.md § DR-7 — Generalize only when a second confirmed consumer exists`. The "one confirmed consumer + one non-consenting consumer about to absorb the result" pattern is the failure mode this rule prevents — project IP silently spreading into canonical and propagating to other projects without their review. Parameterizing the project-specific value through a project-config knob (so each consumer chooses its own value) is structurally cleaner than graduating the value itself; prefer that path when the value is the project-specific part.` Doc voice (numbered, bold lead, paragraph body) matches existing rules #1–4 — placement and voice match plan-time mitigation requirements.
- **Trigger #7 project-neutrality verified.** `produce-formatting.md` Phase 2 step 6 trigger list line 82 (verbatim): `(7) 2+ named geographies OR 2+ named sectors compared across 2+ named dimensions`. No mention of Sweden/Norway/Finland/Nordic/TMT/B2B/healthcare. Grep across the entire canonical command file for those terms returns zero hits.
- **Pre-existing Nordic mentions in SKILL.md (out of FX-D1 scope, but worth naming).** SKILL.md lines 70, 107–108, 117, 156 retain Nordic-specific text and TMT/B2B/healthcare examples. Verified via `git diff` that these lines were NOT introduced by FX-D1 — the SKILL.md FX-D1 changes touch only the load-bearing override note at line 62 + Trigger #1 wording at line 64 + Operation 0 output template + Bias Countering. The pre-existing Nordic-specific examples are technical-debt residue from Bundle 2 not graduated in this work unit — this is a known coupling site, not a regression.
- **c1/c2 apply-by-removal notes verified at both targets.** (a) nordic-pe `CLAUDE.md` line 105 contains the rationale + return-condition block. (b) ai-resources `.claude/commands/friday-checkup.md` line 286 contains the parallel block (`> **c1/c2 apply-by-removal note (from FX-D1 2026-05-28 risk-check, second pass).** ...`). Both blocks correctly state the return-condition (re-create a project-local fork of any Stage 5 command → c1/c2 detection requirement returns). Wording is consistent across the two targets.
- **NEW HIDDEN COUPLING (cross-scope contamination).** ai-resources working tree contains unstated edits to `.claude/commands/consult.md` (4 lines added — `/pm` cross-reference) and `docs/agent-tier-table.md` (1 row added — `project-manager` agent entry). These are not in the FX-D1 scope but live in the working tree. Untracked: `.claude/agents/project-manager.md`, `.claude/commands/pm.md`. If the FX-D1 ai-resources commit uses `git add -A`, these unrelated edits are silently absorbed. Hidden coupling between the FX-D1 commit and an unrelated `/pm` work unit — must be resolved at staging time. Verdict-affecting risk: this is the highest-stakes issue in the end-time pass.

## Mitigations

- **Dimension 3 (cross-scope contamination at staging):** For the ai-resources commit, use explicit file-by-file `git add` rather than `git add -A` or `git add .`. The FX-D1 ai-resources file set is exactly: `workflows/research-workflow/.claude/commands/produce-formatting.md`, `workflows/research-workflow/.claude/commands/produce-prose-draft.md`, `workflows/research-workflow/reference/stage-5-paths.template.md`, `workflows/research-workflow/docs/project-config-schema.md`, `skills/prose-formatter/SKILL.md`, `docs/ai-resource-creation.md`, `.claude/commands/friday-checkup.md`. Do NOT include `.claude/commands/consult.md` or `docs/agent-tier-table.md` or any `.claude/agents/project-manager.md` or `.claude/commands/pm.md` — those belong to a separate `/pm` work unit and should be committed separately (or stashed). Verify staging with `git diff --staged --stat` before commit.

- **Dimension 3 (untracked artifacts not in scope):** The two plan-time risk-check reports under `ai-resources/audits/risk-checks/2026-05-28-fx-d1-*.md` are untracked; the end-time report (this file) joins them. Stage the three risk-check reports in the FX-D1 ai-resources commit (they are scope artifacts). Do NOT stage `nordic-pe/logs/scratchpads/*` or unrelated `plans/*.md` files into the nordic-pe commit — use explicit `git add` of the in-scope nordic-pe paths: `CLAUDE.md`, `reference/stage-5-paths.md`, `.claude/commands/produce-formatting.md`, `.claude/commands/produce-prose-draft.md`, `.claude/commands/produce-jargon-gloss.md`, `logs/session-plan-pass2.md` (the active plan).

- **Dimension 4 (partial-revert hazard):** M4 already in place (operator-facing comment at nordic-pe line 17). No additional mitigation needed — the partial-revert reviewer has the breadcrumb. Confirm the ai-resources commit message names the parameterization (`canonical produce-formatting.md reads Mechanical trigger threshold from path-config`) so a future revert reviewer sees the cross-repo dependency from the ai-resources side too.

## Evidence-Grounding Note

All risk levels grounded in direct evidence:
- 6 mitigations verified by grep + line-level Read against each named site (M1 produce-formatting.md:80,82; M2 SKILL.md:62; M3 stage-5-paths.template.md:85–96 + project-config-schema.md:3,125; M4 nordic-pe stage-5-paths.md:17; M5 grep of `Mechanical trigger threshold` across 5 sites; M6 ai-resource-creation.md:15).
- All 4 symlinks verified via `ls -la` showing `lrwxr-xr-x` and target paths into `../../../../ai-resources/workflows/research-workflow/.claude/commands/{produce-formatting,produce-prose-draft,produce-jargon-gloss}.md`.
- Project-neutrality of canonical produce-formatting.md verified via grep — zero hits for `Sweden|Norway|Finland|Nordic`.
- Cross-scope contamination identified via `git status` in ai-resources showing `.claude/commands/consult.md`, `docs/agent-tier-table.md` modified and `.claude/agents/project-manager.md`, `.claude/commands/pm.md` untracked. `git diff` confirms these reference a `project-manager` agent and `/pm` command — unrelated to FX-D1's threshold parameterization scope. `git log -3` on those paths shows the immediate prior commits were FX-B1 (`a6a301d`) and earlier work — confirming the changes are from this current session but not in the FX-D1 plan scope.
- nordic-pe symlinks confirmed as type-change (`T`) in `git status` — git correctly tracks the swap from regular file to symlink.
- Plan-time mitigations 1–6 from `ai-resources/audits/risk-checks/2026-05-28-fx-d1-plan-time-gate-rerun-revised-plan-parameterized.md` lines 81–91 used as the verification checklist.

No training-data fallback was used.

## Architectural Commentary

_System-owner second opinion (`/consult`, Function B — pre-change advisory), invoked automatically because the verdict is PROCEED-WITH-CAUTION._

**Step 4a deviation — System Owner /consult skipped.** This is the third risk-check in the same session (plan-time × 2 + end-time × 1). The plan-time pair already invoked the System Owner once (verdict-shaping advisory that locked in the option-(a) parameterization path + DR-7 rule); the plan-time re-run attempted a second `/consult` that returned API 529 (transient overload).

This end-time verdict's findings are exclusively **tactical commit-staging discipline** (per-file `git add`, exclude unrelated `/pm` working-tree noise) — no strategic question that the System Owner has not already answered. The four mitigations are mechanical execution rules the operator can apply directly.

Per `/risk-check` Step 4d (transient `/consult` errors do not change the verdict and do not block) plus the [COST] guardrail (≥4 subagents already spawned this session), the third `/consult` is being explicitly skipped. The PROCEED-WITH-CAUTION verdict stands; the 4 mitigations apply at commit time. The deviation is recorded here for transparency rather than hidden as "/consult unavailable" framing.

**If the operator believes a structural concern remains:** invoke `/consult` directly with the specific question. This deviation is a cost-management call, not a determination that no consult would be valuable.

# Lean-Repo Simplification Plan — 2026-07-13

> ## Execution status — 2026-07-13 (S8)
>
> | Item | Status |
> |---|---|
> | **R-1** `backup-session-plan.sh` | ✅ **APPLIED — deleted** (verified registered in zero settings layers first) |
> | **I-1…I-7** six command removals | ⛔ **HALTED — the "zero-use" evidence base is FALSE.** See the status block in § INVESTIGATE. Four of the six are demonstrably in live use. No command removed. |
> | **R-2** `execution-agent` | ⏸ **HELD** — the plan's "no spawner" claim is true of the *canonical* file only; a separate live copy is spawned by `workflows/research-workflow/.claude/commands/verify-chapter.md:40`. Needs a second pass. |
> | **M-1 → R-3** merge + retire `/lean-repo` | ⏸ **HELD** — `/risk-check` RECONSIDER; deferred with R-2 to a focused second pass. |
> | **MC-1** risk-tier the six change classes | ⏸ **DEFERRED deliberately** — conflicts with the No-self-waivers clause in `docs/audit-discipline.md`; an execution session is the wrong venue. Route to the Friday cadence. |
> | **S-1, D-1** workspace `CLAUDE.md` | ⏸ **NOT REACHED** this session. |
> | **RT-1…RT-8** retain | ✅ No action required (correct as dispositioned). |
> | **Inflow design rule** | ✅ Already landed (commit `fb8d72c`). |
>
> **The headline finding of the execution session is a defect in this report's own method** — see § INVESTIGATE. Gate record: `audits/risk-checks/2026-07-13-lean-repo-tier1-batched-removals-merge-wiring.md`.

**Mandate:** RR-05 of `plans/repo-redesign-authoritative-implementation-report.md`. This is the **first ever run** of `/lean-repo` — a fact that is itself a finding (O-1).

## Scope

Full repo-leanness pass (no focus area). Run from the isolated worktree `ai-resources-lean-repo` (branch `session/2026-07-13-lean-repo`).

Inputs read:

| Input | Path | Tag |
|---|---|---|
| `/token-audit` | `audits/token-audit-2026-07-03-ai-resources.md` | fresh (10d) |
| `/audit-repo` | `audits/repo-health-ai-resources-2026-07-03.md` | fresh (10d) |
| `/friday-checkup` | `audits/friday-checkup-2026-07-03.md` | fresh (10d) |
| `/pipeline-review` | `audits/pipeline-reviews/` (latest 2026-06-05) | **STALE** (38d) |
| `/architecture-review` | `projects/axcion-ai-system-owner/output/architecture-reviews/` | **MISSING** — dir created 2026-05-02, still empty |

**[PARTIAL — degraded mode: `/architecture-review` MISSING; `/pipeline-review` STALE]**

The MISSING architecture review is not merely a gap — it is Finding **O-2**. Its consequence for this report: there is no independent architectural synthesis to triangulate against, so every finding below rests on a **measurement or a dated log quote** rather than on design coherence. That is partly a virtue (nothing here is vibes) and partly a forced choice.

---

## Executive Summary

The repository's highest-drag control is **`/risk-check` firing at a single weight across six change classes**. Across all 336 reports in `audits/risk-checks/`: **93% proceed** (115 GO + 196 PROCEED-WITH-CAUTION), **7% RECONSIDER** (24). That proceed rate is **above the repo's own fading-gate threshold** (≥90% → `retire | lower-frequency | recalibrate`, per `logs/gate-calibration.md`) — and `/risk-check`, the highest-volume gate in the repo at ~4 firings per active day for three months, has **never once been calibrated**. Only two gates ever have.

But the gate is **not** failing and must **not** be weakened: it returns **8/14 RECONSIDER on genuine architecture** (authoritative report, line 39) and on 2026-07-13 it caught a real defect that would have silently zeroed the file-record signal across 21+14 symlinked project copies. The finding is **proportionality, not worth**: the gate bites hard where risk is real and rubber-stamps everywhere else. **Tier it, don't retire it.**

The second-order finding is structural: the repo has **two** repo-level design diagnostics — `/lean-repo` and `/architecture-review` — and **neither has ever run**, because neither has an invocation path. Merging them yields a bigger orphan; **the wiring is the load-bearing half of the fix.**

---

## Simplification Plan (by disposition)

**Counts: Remove 3 · Merge 1 · Make-conditional 1 · Simplify 1 · Defer-loading 1 · Retain 8 · Investigate 7** (22 items)

### REMOVE (3)

**R-1 — `backup-session-plan.sh`** · `.claude/hooks/backup-session-plan.sh` — ✅ **APPLIED 2026-07-13 (S8): DELETED.**
- *Operator decision:* delete (not wire). *Independently verified before deletion* — `grep -rln "backup-session-plan" --include="settings*.json"` across every layer including the user layer (`~/.claude/settings.json`) returned **zero registrations**, confirming the audit's claim rather than trusting it. Three real copies removed: `ai-resources/.claude/hooks/`, `projects/positioning-research/.claude/hooks/`, `projects/research-pe-regime-shift-advisory-gap/.claude/hooks/`. Left untouched: the gitignored `archive/` copy, the untracked `.codex/hooks/` copy (foreign tool, appeared 2026-07-13), and the `ai-resources-research-workflow` worktree copy (concurrent live session; resolves on merge).
- *Rationale:* **Zero registrations in any settings layer workspace-wide** — user, workspace, ai-resources, workflow templates, all 20 project `settings*.json`. Its own header asserts the opposite (*"Wired via settings.json PreToolUse Write matcher"*). This is the worst state for a safety control: **it looks like a backup exists, and none does.** The Retain-by-default rule for recovery controls protects *functioning* controls; a hook with no registration is not a control, it is false assurance.
- *The decision is binary:* **wire it or delete it.** Leaving it ambiguous is the only option strictly worse than both.
- *`/risk-check` class:* **Hook edit** — gated (either way).
- *Rollback:* `git revert`. Self-contained script, zero consumers, no state.

**R-2 — `execution-agent`** · `.claude/agents/execution-agent.md` — ⏸ **HELD 2026-07-13 (S8).**
- ⚠ **The rationale below is imprecise and was corrected at execution.** "No command or hook spawns it" is true **only of the canonical file**. A *separate real file* — `ai-resources/workflows/research-workflow/.claude/agents/execution-agent.md` — **is** spawned, by `workflows/research-workflow/.claude/commands/verify-chapter.md:40` (*"Delegate to execution-agent"*). `docs/agent-tier-table.md` carries **three** `execution-agent` rows: L23 (canonical — the removal target), L68 (workflow copy — **live, stays**), L89 (archived project — historical record, stays). Removing the canonical is *probably* safe, but ~26 project symlinks point at it and `auto-sync-shared.sh` **never self-heals a broken symlink** (per the S8 `/risk-check`), so the removal requires an explicit symlink-pruning sub-step that the plan never specified. Held for a focused second pass rather than landed on a rationale that was wrong in its stated form.
- *Rationale (as originally written):* No command or hook spawns it. Detected by `/repo-dd` on **2026-05-08** and still present **66 days later** — an instance of B-1 (detection without closure) on a concrete component.
- *`/risk-check` class:* **None** — agent removal is not among the six enumerated classes (they name "new commands or skills"; agents are absent). **Flagging this as a doctrine gap** rather than assuming coverage. Treat as low-risk; confirm no project symlink resolves to it before deleting.
- *Rollback:* `git revert`; also remove its row from `docs/agent-tier-table.md` (42 → 41 — the registry is reconciled to ground truth and must stay so).

**R-3 — `/lean-repo` command + `lean-repo-auditor` agent** · `.claude/commands/lean-repo.md` (120 L), `.claude/agents/lean-repo-auditor.md` (82 L)
- *Rationale:* The *remove* half of M-1. Keep the lens, drop the component. Net **−1 command, −1 agent** (89→88, 42→41).
- *`/risk-check` class:* **Removed command** — gated.
- *Rollback:* `git revert`; restore the `agent-tier-table.md` row and the `auto-sync-shared.sh:46` `EXCLUDE_COMMANDS` entry.
- *Precondition:* **do not execute before M-1 lands**, or the lens is lost with the component.

### MERGE (1)

**M-1 — Fold `/lean-repo`'s three questions into `/architecture-review`, AND wire `/architecture-review` into `/friday-checkup`'s quarterly tier**
- *Rationale:* Two repo-design diagnostics exist; **neither has ever run**; neither has an invocation path (O-1, O-2). **Merging alone yields a bigger orphan — the wiring is the load-bearing half**, without which this is theatre. `/friday-checkup` already auto-detects a quarterly tier and already runs `/audit-repo`, `/coach`, `/token-audit` plus an "architectural retrospective" section — precisely where the three questions belong.
- *Rule #7 test:* clears **prong (a)** — net −1 command, −1 agent, capability preserved and *increased* (the lens gains a trigger it never had). Clears **prong (b)** — evidence: `friday-checkup-2026-07-03.md:152` (*"new commands ship faster than they are wired into pipelines or retired"*), `coaching-log.md:197` (the memory-dependent-orphan rule).
- *`/risk-check` class:* **Removed command** + edit to an existing command. Gated.
- *Rollback:* revert the `/architecture-review` and `/friday-checkup` edits; restore the two `/lean-repo` files.
- *Carry across — the lens, not the file:*
  - **Q1 Control-proportionality** — audit each recurring gate's firing rate against `gate-calibration.md`'s ≥90% rule. *This is the question that produced the best finding in this pass and is covered by no other audit.*
  - **Q2 Retroactive rule-#7 scoring.**
  - **Q3 Orphan/adoption grep** — agents with no spawner; hooks with no registration in *any* layer; commands with no `usage-log` invocation.

### MAKE-CONDITIONAL (1)

**MC-1 — Risk-tier the six `/risk-check` change classes** · `docs/audit-discipline.md` § Risk-check change classes
- *Rationale:* 336 runs, **93% proceed**, 7% RECONSIDER — above the repo's own ≥90% fading-gate threshold, never once calibrated, while `gate-calibration.md` records only 2 gates ever reviewed. Meanwhile the gate returns **8/14 RECONSIDER on genuine architecture** and caught a real defect on 2026-07-13. **The gate is not failing; it is untiered.**
- *Proposed shape (uses the EXISTING class list — invents no new tier system):*
  - **Full gate** for the four blast-radius classes: hook edits · permission changes · cross-cutting CLAUDE.md · shared-state automation. *(This is where the RECONSIDERs cluster.)*
  - **Lightweight inline check** for the two additive, contained-blast-radius classes: **new command/skill**, **new symlink** — the consumer/blast-radius questions answered inline by the main session, **escalating to the full gate on any non-trivial answer**. *(These are the plausible source of the 115-run GO bulk.)*
  - *Precedent for this exact narrowing:* `/blindspot-scan`, narrowed 2026-07-05 for the identical reason (it *"overlapped with `/risk-check` on consumer/blast-radius checks"*), and the 2026-07-03 gitignored-settings class-boundary clarification already in this very doc section.
- *Guardrails — non-negotiable:*
  1. The **"No self-waivers"** clause in `audit-discipline.md` stays untouched. A session may still never skip a listed class on its own materiality judgment.
  2. Route through `/friday-checkup`'s existing **fading-gate detection + `logs/gate-calibration.md`**, *not by fiat* — so the change is recorded as a calibration decision with a `Review-cycle:` date and is revertible on evidence. **This is the meta-control doing the job it was built for, on the gate it has never been pointed at.**
- *`/risk-check` class:* **cross-cutting doctrine edit** — self-referentially gated. Run `/risk-check` on this change.
- *Rollback:* revert the doc edit; the gate reverts to one weight. Cheap and complete — no state, no downstream consumers.
- *Falsification trigger, set now:* if the RECONSIDER rate on the *remaining* gated classes does not rise materially above 7% within 30 days, the tiering did not find the signal and should be reverted.

### SIMPLIFY (1)

**S-1 — Complete the pointer-migration on workspace `CLAUDE.md`** · `{WORKSPACE_ROOT}/CLAUDE.md` (242 → ~180 lines)
- *Rationale:* Six sections carry **both** a `Full rules:` pointer **and** a substantial inline body (~58 lines): Session Guardrails 13 · Model Escalation 11 · Working Principles 10 · Subagent Proportionality 10 · QC Independence 8 · Plan Mode Discipline 6. The identical move on `ai-resources/CLAUDE.md` scored **98 → 77 lines (−24%) "with no loss of behavioral coverage"** (`token-audit-2026-07-03`:68). The workspace root never got the same treatment.
- *`/risk-check` class:* **cross-cutting CLAUDE.md** — gated. High care: this file shapes every turn of every session.
- *Rollback:* `git revert`. **Verification is the real control here:** for each trimmed section, confirm the pointed-to doc *actually contains* the behaviour being removed **before** removing it (the pointer must be load-bearing, not aspirational). **Trim one section per commit** so a regression is bisectable.
- *Do NOT:* remove any **rule**. This is de-duplication of prose that exists twice — not doctrine reduction.

### DEFER-LOADING (1)

**D-1 — Move the model-tier compliance roster out of always-loaded context** · `{WORKSPACE_ROOT}/CLAUDE.md` § Model Tier
- *Rationale:* The *"Known compliance gap"* block enumerates which 11 commands pin a tier and which 6 do not — **state, not a rule** — and already cites `logs/improvement-log.md` 2026-07-12 as its tracking home. It is re-read on **every turn of every session in every project** and will churn as commands are retrofitted.
- *Keep in CLAUDE.md:* the prohibition, the frontmatter rule, the `general-purpose` pin carve-out, and **one line**: *"New commands pin from creation; the outstanding retrofit list is tracked in `logs/improvement-log.md` 2026-07-12."*
- *`/risk-check` class:* **cross-cutting CLAUDE.md** — gated.
- *Rollback:* `git revert`. **Zero behavioural risk** — the *rule* is unchanged; only the roster relocates.

> S-1 + D-1 together plausibly land **242 → ~180 lines**, close to the 170 target, **with no rule removed.**

### RETAIN (8) — justified and proportionate; do not touch

**RT-1 — `/risk-check` as a gate.** Caught the RR-03 forked-wrap defect that would have silently zeroed the file-record signal across 21+14 symlinked project copies (2026-07-13 S3). 8/14 RECONSIDER on real architecture. **MC-1 tiers it; it must not be read as a case for weakening it.** The authoritative report line 39 is binding: *"The gates are not the villain, and this is not a licence to skip them."*

**RT-2 — The concurrency guards** (`check-foreign-staging.sh`, `detect-concurrent-session.sh`, the per-id session-marker contract, `~/.claude/hooks/cleanup-session-marker.sh`). Seven recorded collisions **with actual data loss**; the 2026-07-13 recurrence corrupted the *decision record* (two contradictory approved decisions 30 minutes apart) — strictly worse than file loss. Per the report's own watchlist: *"Guards stay live until something proven replaces them"* (RR-04 trigger). **Retain until the worktree pilot validates a replacement.** The clearest Retain in the repo.

> **Reconciled 2026-07-14 (S2) — the RR-04 trigger this row waits on has already fired, and the verdict is unchanged: RETAIN.** The worktree pilot ran and closed in S4 (`5fce38c` — marker teardown harness-enforced), so "retain *until* the pilot validates a replacement" is no longer a pending condition; it is a settled one. **No replacement was validated.** The pilot's own successor work (the mtime-liveness heartbeat) was designed, gated, and **deliberately not shipped** in S13 — its `/risk-check` RECONSIDER findings stand (an under-tuned liveness threshold produces a *false negative*: a live-but-idle session read as dead, letting another session overwrite its uncommitted work — strictly worse than the noise it replaces). So the guards named here stay live.
>
> **Two of them have since earned their keep on the record, which strengthens this row rather than weakening it.** `check-foreign-staging.sh` blocked this very session's merge commit (a mandate footprint declared as prose, not paths). And the marker contract's *known gap* stopped being theoretical on 2026-07-14: a concurrent worktree session running the pre-mutex `prime.md` allocated marker **S1 blind**, colliding with this session's S1 — and the merge that landed the same day surfaced **two further historical collisions** (`S8`, `S13`) that had already reached disk unnoticed. The concurrency guards are not a legacy control awaiting retirement; they are the control catching live defects weekly.

**RT-3 — `auto-sync-shared.sh` (live in 20 projects) and `check-permission-sanity.sh` (live in 14).** Both initially *looked* unwired in ai-resources' own settings and are in fact live, registered across project settings via the upward-walk idiom. Distribution and permission-drift controls with all-projects blast radius. *(See the false-positive discipline note below.)*

**RT-4 — `pre-commit` hook + `check-skill-size.sh`.** Secret-prevention and size guard; byte-identical to source.

**RT-5 — `run-manifest.sh`.** 35/35 regression suite; 7/7 close rate since wiring. The redesign programme's one substantial win, and the durable file record RR-03 depends on.

**RT-6 — QC-independence doctrine** (`/qc-pass`, the commit-blocked rule for unreachable QC, the QC→Triage auto-loop). Critical control; no named replacement exists.

**RT-7 — `logs/gate-calibration.md` + `fading-gate-scanner`.** The meta-control that *should* have caught C-1. Retain and **point it at `/risk-check`** — its value is about to be demonstrated, not reduced.

**RT-8 — `logs/defect-log.md` and `logs/incident-log.md`.** 3 and 2 entries. Their **emptiness** is what falsified and killed the entire evaluation stack (GT-C/R6/R7) — extremely high value per byte. Retain; **build nothing on top of them.**

### INVESTIGATE (7) — one operator yes/no each; do NOT auto-remove

> **⛔ STATUS 2026-07-13 (S8) — THE "ZERO-USE" EVIDENCE BASE IN THIS SECTION IS FALSE. DO NOT ACT ON IT.**
>
> **↻ UPDATE 2026-07-13 (S12) — the instrument that produced this section has been fixed. The section itself is still void; the *question* is now re-askable, but ONLY with the corrected method.**
>
> The defective lens (`improvement-log.md`, id-55) is repaired: the Q3 orphan scan now also reads `projects/*/logs/` and `projects/*/CLAUDE.md` (where command usage actually lives), it can no longer emit `orphan → Remove` (only `no evidence of use in scanned scope → CONFIRM BEFORE DELETE`, dispositioned **Investigate**), it must state its scanned scope, and it must pass a planted known-positive check (`/explore-section`) before any orphan finding is reportable at all. Verified: the old lens returned **0** hits for `/explore-section` (→ would say ORPHAN); the corrected lens returns **109** hits across 17 files.
>
> **Do not re-run this section's conclusions from the text below.** If the orphan question is worth re-asking, re-run `/lean-repo` with the corrected lens and judge the fresh output. The numbers below are artefacts of a broken instrument and are retained only as the evidence record of the near-miss.
>
> The operator answered the seven yes/no questions and authorised removal of six commands. The batched plan-time `/risk-check` returned **RECONSIDER**, and direct verification then falsified the premise of this entire section. **No command was removed.** Findings:
>
> - **`explore-section` — the near-miss.** It is the *primary command of the live `axcion-design-studio` project*: **89 slash-invocation mentions across 17 files**, and it is load-bearing throughout that project's `CLAUDE.md` doctrine (escalation checklist, lens-check, mode banner, section-recording path). `projects/axcion-design-studio/.claude/commands` is a **symlink to the entire `ai-resources/.claude/commands/` directory**, so the canonical file *is* the file that project runs. Deleting it would have broken that project's core workflow instantly.
> - **`project-next-steps` — invoked.** `axcion-design-studio/logs/session-notes.md:47`: *"Resumed via `/prime` → `/project-next-steps`"*, plus a scratchpad record.
> - **`post-project-review` — invoked.** `positioning-research`: *"a full redirected `/post-project-review`: diagnosis + canonical research-workflow…"* — it produced the C1–C8 fix series that drove substantial downstream work.
> - **`list-critical-resources` — a live dependency.** It is the grounding step that regenerates `audits/backbone-manifest.md`; other flows invoke it as a sub-step.
> - **`project-consultant` — weak but non-zero.** A stated intent-to-use (*"belongs at the start of the next session"*).
> - **`promote-workflow`** — the only one with no counter-evidence found. Its sole mentions are innovation-registry/friction-log records of the *file being detected*, not invoked. Even so, it was **not** removed: the method that produced its verdict is discredited, so its verdict carries no weight either.
>
> **Root cause — a methodology defect, not six wrong calls.** The "zero references AND zero logged invocations" test was run **against `ai-resources/` only**, and `logs/usage-log.md` in `ai-resources` records *ai-resources sessions* (and is opt-in since 2026-07-04). Commands are invoked **from project sessions**, whose usage is recorded in each *project's* own logs. The test therefore could not observe the very signal it claimed to measure, and returns "zero use" for heavily-used commands by construction.
>
> **Any future re-run of this question MUST grep `projects/*/logs/` and `projects/*/CLAUDE.md`, not just `ai-resources/`.** Until re-done on that basis, treat I-1…I-7 as **unresolved with no evidence**, not as pending removals. Full gate record: `audits/risk-checks/2026-07-13-lean-repo-tier1-batched-removals-merge-wiring.md`.

**I-1…I-6 — Six commands with zero references AND zero logged invocations:**
*(claim falsified — see the status block above)*

| Command | Lines | Created |
|---|---|---|
| `promote-workflow` | 286 | 2026-06-12 |
| `list-critical-resources` | 187 | 2026-06-02 |
| `explore-section` | 156 | **2026-07-05 (8 days ago)** |
| `project-next-steps` | 123 | 2026-06-08 |
| `post-project-review` | 86 | 2026-06-05 |
| `project-consultant` | 86 | 2026-04-12 |
| **Total** | **924** | |

- *Why Investigate and not Remove:* reference-grep is a **structurally weak signal** for operator-invoked slash commands, and `usage-log.md` is **opt-in since 2026-07-04**, so zero-mention is suggestive, not conclusive. **One question per command:** *"have you ever used this, and do you intend to?"* → No = Remove (class: removed command, gated). Yes = wire it, or accept it as a known memory-dependent tool.
- *Expected yield:* if even half are dead, that is **3 commands and ~450 lines** — a larger cut than everything in Remove combined.
- *`explore-section` is the sharpest datum:* created **8 days ago**, already unreferenced and unused. The inflow problem observed in real time, not retrospectively.

**I-7 — `/tech-consult`.** The coaching log's own named orphan exemplar (`coaching-log.md:197`): 5 log mentions (all *about* it being an orphan), 0 invocations. Same treatment.

---

## The Inflow Design Rule (RR-05 completion condition — adopt in writing)

> **A proposed new command must state which existing command it replaces, or why a separate command is genuinely necessary — and must name its invocation path (the registered pipeline, cadence, or hook that will call it). A command whose only trigger is the operator remembering it exists is not shipped; it is wired or deferred.**

- **Per RR-05: a written design principle, not an automated gate. Build no checker for it.**
- **Home:** `docs/ai-resource-creation.md`, as a clause under rule #7 — it is a *sharpening of rule #7 question 5*, not a new rule.
- **Evidence it is needed:** `friday-checkup-2026-07-03.md:152`; `coaching-log.md:197`; findings O-1 / O-2 / O-5 in this report; **+5 commands / 0 removed** in the 10 days since the last token-audit.

---

## Top-5 Bottlenecks (ranked by operational drag)

1. **`/risk-check` fires at one weight across six change classes** — 336 runs, 93% proceed, never calibrated. Every structural change, trivial or architectural, pays a full gate; only 7% change course. The repo's own ≥90% fading-gate rule has been met for months and never applied. Drag: ~4 gate firings per active day, indefinitely. → **MC-1**
2. **Command inflow with no retirement path.** 89 commands, +5 in 10 days, **0 ever removed**; 6 with zero refs and zero invocations; the newest is already unused after 8 days. Nothing observes the growth — which is what RR-05 exists to fix. → **inflow rule + I-1…I-6**
3. **~5,176 tokens of always-loaded context on every turn** (242 + 77 lines), carrying conditional-control prose and a compliance roster that need not be always-loaded. → **S-1, D-1**
4. **Detection outruns closure (OP-12).** 36 `logged (pending)` vs **2** `applied` in the improvement log (~5% closure); `execution-agent` flagged 66 days ago and still present. The repo generates findings faster than it closes them. → **B-1, R-2**
5. **Two never-run repo-design diagnostics.** `/lean-repo` and `/architecture-review` — both orphans, neither wired to any cadence. **The lens has no way to fire.** → **M-1**

---

## Method Notes

**Token discipline.** The two large evidence logs (`friction-log.md` 103 KB, `improvement-log.md` 127 KB) were read by **targeted grep/sed only — never full-read**. Three consecutive `/usage-analysis` entries flagged full-reads of exactly these two files as the repo's single biggest per-session token waste (~50–60k/session). This audit did not reproduce that failure.

**False-positive discipline.** The repo's most recent commit (`9417fc7`) is a *retraction* of a wrongly-declared-unwired hook. This pass explicitly guarded against repeating that error: four hooks that initially appeared unwired in ai-resources' own `settings.json` — `auto-sync-shared.sh`, `check-permission-sanity.sh`, `check-skill-size.sh`, `check-template-drift.sh` — were each traced to a live registration elsewhere (project settings, the pre-commit hook, a workflow template) and are **Retain, not Remove**. Hook wiring was checked across **every** settings layer, including the user layer (`~/.claude/settings.json`), which registers hooks by absolute path and is invisible to a repo-only grep.

---

## BUCKET D — Honest self-audit of the tool's own output

*RR-05 requires that weak or irrelevant findings from the tool itself be **"reported honestly, not quietly dropped."** The report pre-authorises the conclusion that `/lean-repo` be folded into `/architecture-review` and retired.*

### Genuinely new and distinctive (found by no other audit)

- **C-1 — the `/risk-check` 93%-proceed / never-calibrated finding.** The pass's one clearly high-value result. It required extracting verdicts from all 336 reports and comparing against the repo's *own* fading-gate threshold. `/token-audit` and `/friday-checkup` **structurally cannot** produce it — they count artifacts, not firing rates. `/friday-checkup` saw the same folder and reported *"290 risk-check reports — heavy gate ceremony with no archival/pruning cadence"*: an **artifact-accumulation** observation. That the gate is **firing proportionately-wrong** is a different and far more consequential claim. **This finding alone justifies the lens.**
- **O-2 — `/architecture-review` has never run.** New. And it *changes the recommendation*: it converts "merge lean-repo into architecture-review" from a tidy answer into an inadequate one, because it would merge an orphan into an orphan. **The wiring is the fix.**
- **O-3 — `backup-session-plan.sh` is registered nowhere.** New, small, and a false-assurance safety hook — worth the pass on its own.

### Moderate value, partly derivative

- **O-4 (`execution-agent`)** — the orphan was already found by `/repo-dd` in May. What is new is the **non-closure**, which is really an instance of B-1.
- **O-5 / O-6 (zero-use commands)** — moderate evidence, deliberately **not** converted into deletions. Honest strength: suggestive, not conclusive.

### Restatements — no new value, and none is claimed

- **B-1** (improvement-log 36-pending / 2-applied): `/friday-checkup` already said *"improvement-logs all `logged (pending)`, 0 applied."* This pass quantified it. That is all.
- **B-3 / S-1** (CLAUDE.md 242 vs 170): stated verbatim in **RR-05's own problem statement**. The addition is the *specific* 58 lines and the proven precedent — a modest increment on a known finding.
- **Command count / inflow:** RR-05 and `friday-checkup:152` both said it. The contribution is the +5-in-10-days delta and the `explore-section` datum.
- **Risk-checks folder accumulation (5.7 MB / 336 files):** `/friday-checkup` found this. **Routed there; not claimed here.**

### The verdict on `/lean-repo` as a command

**The lens is real; the command is not viable.**

Q1 (control-proportionality) is a genuinely distinctive question no other audit in this repo asks, and on its first run it produced the highest-value finding in the pass. Q3 (orphan/adoption) produced three real items. **Q2 was almost entirely derivative** of `/friday-checkup`.

But the command has **no invocation path**, was **excluded from distribution** (`auto-sync-shared.sh:46`), sat **unrun for its entire existence**, and had to be fired by an explicit line item in a redesign plan to run even once. It is precisely the memory-dependent orphan that `coaching-log.md:197` warned against — and **it took a five-item recovery programme to trigger it a single time. A diagnostic that requires a redesign programme to fire will not fire again.**

Roughly **one-third of this pass's findings are restatements** of `/friday-checkup` and `/token-audit`. A standalone command that is two-thirds redundant with audits that *do* run on a cadence, and that itself never runs, **does not clear rule #7 on either prong** — including the OP-11 exception under which it originally shipped.

**Recommendation: retire the command, keep the lens, wire it (M-1 + R-3).** The three questions become a section of `/architecture-review`; `/architecture-review` gets wired into `/friday-checkup`'s quarterly tier so it fires without anyone remembering it. Net: **−1 command, −1 agent, +1 invocation path.** The leanest available outcome, and the one the authoritative report pre-authorised.

### The self-referential note, stated plainly

This audit found that **the repo generates findings faster than it closes them** (36 pending / 2 applied) — **and it has just generated 22 more.** If this plan is filed and not executed, `/lean-repo`'s first run will have made the repo's principal problem marginally worse. **The correct reading of that outcome is not "run `/lean-repo` again."**

---

## Closure — how these land

Advisory plan. **This session applied no fix and modified no repository component.**

Structural items route to a separate `/risk-check`-gated execution session (the workspace's standing diagnose→execute separation: `/friday-checkup` → `/friday-so` → `/friday-act` → execution). Each structural item above carries its `/risk-check` change class and a rollback note, so they are execution-ready.

**Suggested execution order** (dependency-correct):

1. **MC-1** — the highest-drag item, and the one that makes every later gated change cheaper. Route through `gate-calibration.md`, not by fiat.
2. **I-1…I-7** — seven operator yes/no answers. Cheapest possible pass; plausibly the largest line-count cut.
3. **M-1 → then R-3** — strict order. **M-1 must land first or the lens dies with the component.**
4. **R-1** (wire-or-delete decision), **R-2** (agent removal + registry row).
5. **D-1 → then S-1** — D-1 is zero-behavioural-risk and lands first; S-1 goes one section per commit, each verified against its pointed-to doc.

**The inflow design rule** is the one item that is a *written principle*, not a structural change. It is the RR-05 completion condition and belongs in `docs/ai-resource-creation.md` under rule #7. **Build no checker for it** (RR-05 is explicit).

---

*Working notes: `audits/working/lean-repo-2026-07-13-notes.md`*

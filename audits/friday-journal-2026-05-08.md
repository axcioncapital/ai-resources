---
report_date: 2026-05-08
type: friday-journal
source: ai-resources/logs/ai-journal.md
entry_count: 32
items_generated: 31
---

# /friday-journal report — 2026-05-08

## Summary

Heavy backlog (32 active entries) covering five themes: (1) cadence/Friday-flow gaps (/friday-so depth, /architecture-review wiring, /implementation-triage reminders); (2) session-plan and autonomy ergonomics (autonomy-level question, /autonomy mid-session command, auto-/triage wiring); (3) hooks for friction control (inline-plans, diagnosis-vs-execution split, context overload, drift detection); (4) settings/permissions hardening (deny-list root cause, sibling-directory permission propagation); (5) new commands and cadences (/explain, /daily-journal, end-of-session unresolved check, GPT-QC for project plans). Three entries dropped during clarification: E8 (notes-cleanup pipeline — redundant with /friday-journal itself); E18 (default-model 200k drift — operator already fixed); E25 (auto-link new commands to projects — verified done; all 7 projects have session-plan.md and resolve.md symlinks). Note: workspace CLAUDE.md already declares "Default posture: full autonomy" at line 75 — the autonomy item (originally "encode full-autonomy default") has been reframed to target the gap (auto-/triage wiring after /qc-pass, no operator gate) and demoted from high to med. Checkup is fresh (today, 2026-05-08); priority calibration is reliable. Splits applied: E2 → 2 items, E12 → 4 items (later 3 merged into one polish-pass), E27 → 2 items, E32 → 2 items (later one merged into settings hardening pass). Post-QC merges: /session-plan items E4+E12a+E12c → one polish-pass; settings items E1+E32b → one /risk-check-gated hardening pass; hooks-catalog item consolidated into the four concrete-hook items it duplicated. Final item count: 31. **Bookkeeping ledger:** 32 entries − 3 drops (E8, E18, E25) + 4 splits (E2, E12, E27, E32; net +4 items) − 4 merge-net (3 /session-plan items → 1; 2 settings items → 1; hooks-catalog → 4 existing hook items) = 31 items.

## Items

[high] ai-resources/.claude/settings.json hardening pass — remove deny-list array (lines 10–19, 8 entries overriding bypassPermissions) AND configure bypassPermissions for sibling-directory edits triggered from other projects. Both gated behind a single /risk-check (Permission changes class).
[high] Sweep .claude/settings*.json files across workspace, ai-resources, and all projects for residual "model" key entries and delete any hits.
[high] Add hook that captures context-window utilization (token count + percentage of window) at compact-fire time to enable root-cause diagnosis of premature auto-compaction.
[med] Wire auto-/triage as default after /qc-pass results in commands that produce QC findings — no operator approval gate. Note: workspace CLAUDE.md line 77 already declares "Default posture: full autonomy"; this item targets the implementation gap (auto-triage automation), not the policy.
[med] Add operator-prompted folder-scope question to /friday-so (parallel to /friday-checkup's project-tier question).
[med] Extend /friday-so to do its own targeted scan instead of only reading the existing checkup report.
[med] /session-plan polish pass — add autonomy-posture question (full-auto / approved-only / interactive), clarify QC-pass directive (soft "do not invoke automatically" vs hard rule), and declare explicit exit condition for clean termination.
[med] Wire /implementation-triage reminder into /friday-act start so systems-level option-choosing isn't forgotten on Fridays.
[med] Build /autonomy-stop log command — append timestamped entry every time Claude halts mid-workflow for review on Friday.
[med] Add session-logging heuristic to /wrap-session — log substantive sessions (artifact-producing or >~30 min); skip trivial reads/single-edit sessions.
[med] Add bold, visible reminder to /prime output that operator should invoke /session-plan after priming.
[med] Build /autonomy mid-session command — hands Claude full autonomy at any point in a running session, mirror of /session-plan but invocable later.
[med] Add hook that intercepts long inline plans in chat and forces them to a file before Claude continues.
[med] Add hook that prevents combining diagnosis and execution in the same session — force a session split when both phases are present.
[med] Improve /audit-claude-md — add clarifying-question pass and a /qc-pass via systems-owner agent before finalizing.
[med] Investigate concurrent-Claude-Code-sessions risk on shared files (already flagged in 2026-05-08 checkup, project global-macro-analysis); pick a guardrail (file-lock, session-id stamp, or warn-only).
[med] Build "/gpt-qc" or new step in /create-skill — runs project-plan or AI-infra plan through ChatGPT for QC/refinement, prompt auto-generated by Claude.
[med] Audit /resolve, /refinement-pass, and /qc-pass prompts for language that implies "produce the corrected version"; replace with "produce a list of targeted Edit operations" so the consumer agent uses Edit not Write.
[med] Build /explain command — explains the next step Claude is about to take and what Claude just did, in simple short English.
[med] Add hook that detects high-volume context reads (e.g., reading >5 KB modules or estimated >50k tokens in one go) and suggests session-splitting.
[med] Build /daily-journal command — daily cadence for capturing improvement/waste/redundancy observations, complementing weekly /friday-journal.
[med] Add unresolved-items sub-step to /wrap-session — asks Claude if there are pending unknowns/clarifications/decisions before wrap.
[med] Document the drift pattern observed in projects/buy-side-service-plan/ between the v1 master plan and v2.x sub-docs (one concrete example, not pattern-level) — defer detector implementation to a follow-up Friday once the pattern is named.
[low] Investigate whether /architecture-review should be wired into /friday-so cadence routine.
[low] Document operator-pushes-manually convention in workspace CLAUDE.md (matches Autonomy Rules #2; closes recurring "push denied" friction).
[low] Document /risk-check actual use cases and trigger conditions in audit-discipline.md so it isn't invoked for trivial changes.
[low] Investigate context-engineering pattern for /session-plan — have it engineer the coding-agent's own context for the session.
[low] Document Skills fork-parameter and arguments-parameter mechanics in ai-resource-builder/SKILL.md or a usage guide.
[low] Document background-agent context-priming pattern (parallel research subagent feeding main session) as a usage guide.
[low] Investigate GAP-1 — 7 workspace-root commands (prime, wrap-session, repo-dd, etc) outside ai-resources/.claude/commands/. Decide: graduate, or document workspace-root command tier as a distinct DR-1 level.
[low] Add "are there decisions or actions to take before finishing this session?" prompt to /wrap-session end-of-day flow.

## Item context

### ai-resources/.claude/settings.json hardening pass — remove deny-list array (lines 10–19, 8 entries overriding bypassPermissions) AND configure bypassPermissions for sibling-directory edits triggered from other projects. Both gated behind a single /risk-check (Permission changes class).
- **Files:** `ai-resources/.claude/settings.json`
- **Effort:** low (two edits; one /risk-check covers both)
- **Recommended approach:** Run a single /risk-check covering both changes (Permission changes class). On GO: (1) Delete the entire `"deny": [...]` array (lines 10–19) — eight entries override bypassPermissions and surface prompts every session. (2) Verify settings has `defaultMode: bypassPermissions` and `additionalDirectories` covering workspace root so sibling-directory edits from project sessions don't surface UI prompts. Run a follow-up session from another project that edits ai-resources/ to verify no prompts. Bundling the two changes into one /risk-check avoids double-gating equivalent risk surface.
- **Source entry (E1, deny-list):** Permission prompts — root cause is the deny list in ai-resources/.claude/settings.json (8 entries: Bash rm/sudo/git push + 5 Read denies on archive/deprecated/old paths). Per memory rule "never add to deny list, bypass mode is the floor", this list should not exist. Deny rules override bypassPermissions and trigger prompts on matching paths. Fix: remove the entire "deny": [...] array from ai-resources/.claude/settings.json (lines 10–19). Falls under "Permission changes" in audit-discipline.md § Risk-check change classes — needs /risk-check before applying. Logged 2026-05-08.
- **Source entry (E32b, sibling permission):** Permission prompt log: subagent making tool calls in ai-resources/ from a different project's session triggered Claude Code UI approval dialogs even with bypassPermissions in the project settings — sibling-directory edits surface prompts because each directory has its own settings. Fix: wire ai-resources settings to bypass before running heavy implementation stages from other projects.

### Sweep .claude/settings*.json files across workspace, ai-resources, and all projects for residual "model" key entries and delete any hits.
- **Files:** results of `grep -l '"model"' .claude/settings*.json ai-resources/.claude/settings*.json projects/*/.claude/settings*.json`
- **Effort:** low (one-line delete per hit)
- **Recommended approach:** Run grep across all settings files. For each hit, delete the `"model"` line (leave the rest of the JSON intact). Verify in a subsequent session that `/model default` resolves to account default and `/model` switches stick across turns. Root cause: a "model" key in either settings layer pins the session and silently overrides /model selections on next turn.
- **Source entry:** Model-pin problem — a "model" key in `.claude/settings.local.json` or `.claude/settings.json` pins the session to that model. /model selections are silently overridden on next turn, so switching to Opus appears to "not work." Fix: remove the "model" key from both settings files. To check across projects: `grep -l '"model"' projects/*/.claude/settings*.json`. Open each hit, delete the "model" line. Then /model works freely and `/model default` resolves to account default (Opus 4.7 1M).
- **Risk-check required:** Yes (matches Permission changes per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Add hook that captures context-window utilization (token count + percentage of window) at compact-fire time to enable root-cause diagnosis of premature auto-compaction.
- **Files:** workspace `.claude/settings.json` (hooks block), new hook script under `ai-resources/.claude/hooks/`
- **Effort:** medium
- **Recommended approach:** Add a PreCompact (or equivalent) hook that logs: timestamp, model identifier, token count, % of context window, last-N tool calls. Append to `ai-resources/logs/compact-events.md`. After 1–2 weeks of data, check whether compaction fires at a fixed token count (settings issue), turn count (Claude Code internal), or model-tier-default (model issue). Disposition the actual fix in a follow-up Friday once data is in.
- **Source entry:** Auto-compact firing prematurely — Claude Code is auto-compacting even though 1M window is not reached. Investigate why and how to disable until window is genuinely close to full.
- **Risk-check required:** Yes (matches Hook edits per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Wire auto-/triage as default after /qc-pass results in commands that produce QC findings — no operator approval gate. Note: workspace CLAUDE.md line 77 already declares "Default posture: full autonomy"; this item targets the implementation gap (auto-triage automation), not the policy.
- **Files:** `ai-resources/.claude/commands/qc-pass.md`, `ai-resources/.claude/commands/resolve.md`, `ai-resources/docs/qc-independence.md` (QC → Triage Auto-Loop section)
- **Effort:** medium
- **Recommended approach:** Read qc-independence.md QC → Triage Auto-Loop section to confirm it already specifies auto-triage. If specified but not implemented in /qc-pass, wire the auto-spawn (subagent → triage-reviewer → return verdict, no operator pause). If specified AND implemented, document as already-done and drop. Workspace CLAUDE.md Autonomy Rules already cover the policy; no edit needed there.
- **Source entry:** Full-autonomy default for all sessions — start giving Claude full autonomy every session. Tell it to run automatic /triage pass after /qc-pass results (automate). Don't ask operator what to implement — automate it. Make this part of every project as a standard feature: "autonomy as standard."

### Add operator-prompted folder-scope question to /friday-so (parallel to /friday-checkup's project-tier question).
- **Files:** `ai-resources/.claude/commands/friday-so.md`
- **Effort:** low
- **Recommended approach:** Add an early step to /friday-so that asks operator which scopes (ai-resources / workspace / specific projects) to include in the advisory. Mirror the prompt shape used in /friday-checkup.
- **Source entry:** Fix /friday-checkup scope behaviour — currently checks only the folder it is run in. Should ask operator (like friday-checkup already does for project tier?) which folders/projects to scan.

### Extend /friday-so to do its own targeted scan instead of only reading the existing checkup report.
- **Files:** `ai-resources/.claude/commands/friday-so.md`
- **Effort:** medium
- **Recommended approach:** Add a "fresh-scan" step to /friday-so that runs lightweight checks beyond what the checkup captures (e.g., recent commits, decisions.md staleness, friction-log volume). The checkup remains the primary input; the scan supplements depth. Token cost is the main tradeoff — gate the scan behind a verbosity flag if needed.
- **Source entry:** /friday-so doesn't scan the repo itself, only reads the existing checkup report and synthesizes from it. Depth is bounded by what the checkup captured. Need /friday-so to analyze more than the checkup's surface output.

### /session-plan polish pass — add autonomy-posture question (full-auto / approved-only / interactive), clarify QC-pass directive (soft "do not invoke automatically" vs hard rule), and declare explicit exit condition for clean termination.
- **Files:** `ai-resources/.claude/commands/session-plan.md` (+ symlinks in 7 projects propagate automatically)
- **Effort:** medium (three small edits to one file)
- **Recommended approach:** (1) Add an autonomy-posture field to /session-plan output schema; default to full-auto matching workspace CLAUDE.md Autonomy Rules. (2) Locate the existing "do not invoke /qc-pass automatically" line; tighten language to "by default, do not invoke; bend only on explicit operator request" if soft was intended, or remove the bendable interpretation if hard was intended. (3) Add a final step that prints `/session-plan complete — proceed with execution.` and stops, preventing automatic continuation into execution.
- **Source entry (E4):** /session-plan should ask autonomy level. Currently does not. Add autonomy-posture question as part of the session-plan output.
- **Source entry (E12a):** "the /session-plan skill says 'do not invoke /qc-pass automatically,' but that's a soft directive and you're explicitly asking. Running it now" — clarify whether soft or hard.
- **Source entry (E12c):** Declare exit condition for /session-plan.

### Wire /implementation-triage reminder into /friday-act start so systems-level option-choosing isn't forgotten on Fridays.
- **Files:** `ai-resources/.claude/commands/friday-act.md` (Step 1 or 1.5)
- **Effort:** low
- **Recommended approach:** Add a one-line reminder near the start of /friday-act that "for any item with multiple plausible approaches, run /implementation-triage before disposition."
- **Source entry:** Reminder to use /implementation-triage on Fridays when doing systems checks. The triage skill is specifically meant for choosing options from a systems point of view. Wire a reminder into /friday-checkup, /friday-so, or /friday-act so this isn't forgotten.

### Build /autonomy-stop log command — append timestamped entry every time Claude halts mid-workflow for review on Friday.
- **Files:** new `ai-resources/.claude/commands/autonomy-stop.md`, log file `ai-resources/logs/autonomy-stop-log.md`
- **Effort:** medium (new command + log schema + Friday review wiring)
- **Recommended approach:** Build a thin command that appends `{timestamp} | {what blocked} | {operator-judged: justified | unjustified}` to a log. On Friday (slot in /friday-checkup or /friday-act), review the log and pre-allow the unjustified-stop classes via settings or CLAUDE.md updates.
- **Source entry:** Create autonomy-fix log command — every time Claude stops mid-workflow and operator doesn't like the stop, invoke a command that logs that event (timestamp, what blocked, was the stop justified). On Friday, review the log and pre-allow those classes of operations so next time Claude runs autonomously past them.
- **Risk-check required:** Yes (matches New commands per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Add session-logging heuristic to /wrap-session — log substantive sessions (artifact-producing or >~30 min); skip trivial reads/single-edit sessions.
- **Files:** `ai-resources/.claude/commands/wrap-session.md`
- **Effort:** low (text addition)
- **Recommended approach:** Add guidance section: "Substantive sessions (artifact-producing, ≥~30 min, multi-file edits, audits) MUST be logged. Trivial sessions (single read, one-line edit, aborted starts) MAY be skipped." Keep it as guidance, not a hard rule.
- **Source entry:** Reminder to log sessions — as part of harness development, logging sessions at the end is important. Operator forgets to do it. Solution: Create a reminder (hook? wrap-session prompt?) to log sessions and mention harness preparation. Question to resolve: which sessions to log and which not?

### Add bold, visible reminder to /prime output that operator should invoke /session-plan after priming.
- **Files:** `.claude/commands/prime.md` (workspace-root)
- **Effort:** low
- **Recommended approach:** Append to /prime's status block: `**Recommended next step: /session-plan**` in bold. Place after "What are we working on?" or as a separate line in the report.
- **Source entry:** Add a reminder to /prime that /session-plan should be invoked after priming, bold and visible.

### Build /autonomy mid-session command — hands Claude full autonomy at any point in a running session, mirror of /session-plan but invocable later.
- **Files:** new `ai-resources/.claude/commands/autonomy.md`
- **Effort:** medium
- **Recommended approach:** Build a command that, when invoked, sets the session's autonomy posture to full-auto: confirms scope, suppresses approval prompts for the rest of the session, and acknowledges back. Symlink into all 7 active projects per the new auto-link convention.
- **Source entry:** Create /autonomy command — like /session-plan but invoked mid-session to hand Claude Code autonomy at any point in a session, not just at the start.
- **Risk-check required:** Yes (matches New commands per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Add hook that intercepts long inline plans in chat and forces them to a file before Claude continues.
- **Files:** workspace `.claude/settings.json` hooks block, new hook script
- **Effort:** medium
- **Recommended approach:** Implement a hook that scans Claude's draft chat output for plan-shaped content (e.g., >40 lines containing "Step 1", "Phase 1", numbered headings). On match, halt and emit "Save this to a file before continuing." Investigate whether a PreToolUse or output-filter hook is the right shape.
- **Source entry:** Hook to prevent Claude from writing detailed plans inline (cadence plan example). When Claude is about to emit a long plan in chat, hook should stop it and say "make a file out of this." Avoids dumping plans into the conversation transcript.
- **Risk-check required:** Yes (matches Hook edits per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Add hook that prevents combining diagnosis and execution in the same session — force a session split when both phases are present.
- **Files:** workspace `.claude/settings.json` hooks block, new hook script
- **Effort:** medium
- **Recommended approach:** Detect when a session has produced both an analysis-shaped artifact (audit, diagnosis, finding) AND an execution-shaped artifact (file edit beyond logging) within a single conversation. On second phase start, prompt: "Diagnosis already done — split execution into a new session?" Default to yes.
- **Source entry:** Hook to prevent executing extensive plans from diagnosing in the same session. Example: CLAUDE.md analysis + fix in one session was too tight. Better to split diagnosis sessions and execution sessions.
- **Risk-check required:** Yes (matches Hook edits per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Improve /audit-claude-md — add clarifying-question pass and a /qc-pass via systems-owner agent before finalizing.
- **Files:** `ai-resources/.claude/commands/audit-claude-md.md`, `ai-resources/.claude/agents/claude-md-auditor.md`
- **Effort:** medium
- **Recommended approach:** Add Step N+1: "Before issuing verdicts, ask 1–3 clarifying questions about uncertain blocks." Add Step N+2: "Spawn systems-owner agent to QC the audit findings before printing the report." Test on a known CLAUDE.md.
- **Source entry:** Improve /audit-claude-md skill — (1) make sure to ask clarifying questions to confirm the info there is up to date. (2) QC the CLAUDE.md output with the systems-owner agent before finalizing.

### Investigate concurrent-Claude-Code-sessions risk on shared files (already flagged in 2026-05-08 checkup, project global-macro-analysis); pick a guardrail (file-lock, session-id stamp, or warn-only).
- **Files:** investigate first; likely SessionStart hook + lock file mechanism in `ai-resources/.claude/hooks/`
- **Effort:** medium (investigate) → high (implement)
- **Recommended approach:** Investigate options: (a) file-system advisory lock during write, (b) session-id stamp in a shared `.claude/active-sessions.json` checked at every Edit, (c) warn-only SessionStart hook that lists other open sessions on the same repo. Pilot in global-macro-analysis (where the issue already manifested).
- **Source entry:** Implement guardrail to prevent two concurrent sessions working on the same files leading to conflicts.
- **Risk-check required:** Yes (matches Shared-state automation per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Build "/gpt-qc" or new step in /create-skill — runs project-plan or AI-infra plan through ChatGPT for QC/refinement, prompt auto-generated by Claude.
- **Files:** new `ai-resources/.claude/commands/gpt-qc.md`, or new step in `/create-skill`
- **Effort:** medium
- **Recommended approach:** Build a thin command that takes a draft plan/spec, generates a QC prompt tailored to its content, and outputs the prompt + the artifact in a copy-pasteable form for ChatGPT. Operator runs the QC manually in ChatGPT and feeds the response back via /resolve.
- **Source entry:** Reminder to QC context packs / project plans with GPT — when creating project plans that have to do with AI infrastructure, run them through ChatGPT first as a QC/refinement pass. Have Claude generate the QC prompt automatically.
- **Risk-check required:** Yes (matches New commands per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Audit /resolve, /refinement-pass, and /qc-pass prompts for language that implies "produce the corrected version"; replace with "produce a list of targeted Edit operations" so the consumer agent uses Edit not Write.
- **Files:** `ai-resources/.claude/commands/qc-pass.md`, `ai-resources/.claude/commands/refinement-pass.md`, `ai-resources/.claude/commands/resolve.md`, possibly `ai-resources/.claude/agents/qc-reviewer.md`, `ai-resources/.claude/agents/refinement-reviewer.md`, `ai-resources/.claude/agents/triage-reviewer.md`
- **Effort:** medium
- **Recommended approach:** Read the three commands and three agents. Search for instructions like "produce the revised", "rewrite", "produce a corrected version", "output the new version of the file". Replace with explicit "produce a list of Edit operations: file path + old_string + new_string for each fix." Verify token cost reduction by comparing one before/after refinement-pass run on a known artifact.
- **Source entry:** Targeted edits vs rewriting entire artifacts — when Claude proposes a plan and operator runs /refinement-pass or /qc-pass on it, Claude often rewrites the entire plan rather than making targeted edits. This is costing lots of tokens.

### Build /explain command — explains the next step Claude is about to take and what Claude just did, in simple short English.
- **Files:** new `ai-resources/.claude/commands/explain.md`
- **Effort:** low
- **Recommended approach:** Two-mode command: `/explain next` summarizes what Claude is about to do; `/explain last` summarizes what Claude just did. Output is one short paragraph in plain English (B2-level per CLAUDE.md communication style).
- **Source entry:** Create /explain command — explains the next step or what Claude Code has just done in very simple English, clear and short format so operator understands.
- **Risk-check required:** Yes (matches New commands per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Add hook that detects high-volume context reads (e.g., reading >5 KB modules or estimated >50k tokens in one go) and suggests session-splitting.
- **Files:** workspace `.claude/settings.json` hooks block, new hook script
- **Effort:** medium
- **Recommended approach:** PreToolUse hook on Read calls that tracks cumulative token count. On threshold breach (>50k tokens cumulative or >5 files in one batch matching a "module" path pattern), emit warning: "Context loading heavy — consider splitting into a follow-up session."
- **Source entry:** Prevent reading too much context — ran into issue when ingesting KB modules where Claude read all the modules at once (100 pages). Develop a hook that prevents this type of context overloading and suggests session-splitting in the future.
- **Risk-check required:** Yes (matches Hook edits per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Build /daily-journal command — daily cadence for capturing improvement/waste/redundancy observations, complementing weekly /friday-journal.
- **Files:** new `ai-resources/.claude/commands/daily-journal.md`, `ai-resources/logs/daily-journal.md`
- **Effort:** medium
- **Recommended approach:** Build a thin command that prompts: "Anything to log from today's work — improvements spotted, waste, redundant operations?" Appends to a daily journal file. /friday-journal already absorbs the weekly batch; /daily-journal is the ingest point.
- **Source entry:** New cadence: daily improvement journal — keep a journal every day on identifying potential areas of improvement, potential waste, redundant operations, etc. Different from /friday-journal (which is weekly).
- **Risk-check required:** Yes (matches New commands per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Add unresolved-items sub-step to /wrap-session — asks Claude if there are pending unknowns/clarifications/decisions before wrap.
- **Files:** `ai-resources/.claude/commands/wrap-session.md`
- **Effort:** low
- **Recommended approach:** Add a step before final commit/log: "Are there any unresolved items (unknowns, pending decisions, unresolved clarifications) that should be captured before session end?" If yes, emit list and let operator decide whether to address now or capture in next-steps.
- **Source entry:** End-of-session unresolved-items check — at session end, before /wrap-session, ask Claude Code if there are any "things that need to be resolved" before session end (unknowns, clarifications, pending decisions). Create a command for this, or add as a sub-step to /wrap-session.

### Document the drift pattern observed in projects/buy-side-service-plan/ between the v1 master plan and v2.x sub-docs (one concrete example, not pattern-level) — defer detector implementation to a follow-up Friday once the pattern is named.
- **Files:** `projects/buy-side-service-plan/` (read-only investigation), new doc `ai-resources/docs/draft-approved-drift-pattern.md`
- **Effort:** medium
- **Recommended approach:** Read the buy-side service plan v1 master and v2.x sub-docs. Identify the specific divergence pattern operationally (content, structure, or naming). Write a one-page pattern doc that names the pattern and gives the buy-side example concretely. Detector design comes later (separate Friday item) once the pattern is well-defined.
- **Source entry:** Develop a hook or something that can detect/remind of drift between draft/approved status (example: buy-side service plan situation). Original docs need to always be in check (ex. when updating doc 2 vs 2.1-2.9 docs).

### Investigate whether /architecture-review should be wired into /friday-so cadence routine.
- **Files:** investigate `ai-resources/.claude/commands/architecture-review.md`, `friday-so.md`, `so-monthly.md`
- **Effort:** low (investigate-only this Friday)
- **Recommended approach:** Read the three commands. Determine whether /architecture-review's outputs add to or duplicate /friday-so. Decide: (a) wire into Friday cadence as a regular step, (b) wire into monthly cadence (so-monthly), or (c) keep as on-demand. Document the disposition in `ai-resources/docs/weekly-cadence.md`.
- **Source entry:** Is /architecture-review part of the /friday-so cadence routine? Investigate whether architecture-review should be wired into the Friday so/monthly so cadence or remain a separately invoked command.

### Document operator-pushes-manually convention in workspace CLAUDE.md (matches Autonomy Rules #2; closes recurring "push denied" friction).
- **Files:** workspace `CLAUDE.md` (Commit behavior section)
- **Effort:** low
- **Recommended approach:** Add a one-line clarification under the existing Commit behavior section: "Push is operator-only — never invoked from Claude. Approve push attempts manually from terminal." Closes the recurring "push denied" prompt loop.
- **Source entry:** Push permission problem — push was denied twice (both `cd` form and bare `git push`) and Claude couldn't proceed without approval. Either pre-allow push on the next attempt or document that operator pushes manually from terminal.
- **Risk-check required:** Yes (matches Cross-cutting CLAUDE.md edits per audit-discipline.md). /friday-act must run /risk-check before landing this fix.

### Document /risk-check actual use cases and trigger conditions in audit-discipline.md so it isn't invoked for trivial changes.
- **Files:** `ai-resources/docs/audit-discipline.md` (Risk-check change classes section)
- **Effort:** low
- **Recommended approach:** Read existing audit-discipline.md change classes. Add explicit "do not invoke /risk-check for" examples (trivial command merges, prose-only changes, single-line typo fixes). Reaffirm trigger conditions (permission changes, architectural moves, blast-radius >local).
- **Source entry:** When should /risk-check actually be used? Not on something simple like merging two commands (it writes a full report and is heavyweight). Document the actual use cases for this skill. Make the trigger conditions explicit before invoking.

### Investigate context-engineering pattern for /session-plan — have it engineer the coding-agent's own context for the session.
- **Files:** investigate `ai-resources/.claude/commands/session-plan.md`
- **Effort:** medium (investigate-only this Friday)
- **Recommended approach:** Research Anthropic's "context engineering" patterns for agent self-context. Investigate whether /session-plan can produce an explicit "context bundle" (files to load, files to ignore, scope boundaries) that primes the execution session. Document feasibility before implementing.
- **Source entry:** Improve /session-plan: Context engineer your "coding agent" session by having it context engineer its own context for the session.

### Document Skills fork-parameter and arguments-parameter mechanics in ai-resource-builder/SKILL.md or a usage guide.
- **Files:** `ai-resources/skills/ai-resource-builder/SKILL.md`, or new `ai-resources/docs/skill-mechanics.md`
- **Effort:** low (investigate Anthropic docs first)
- **Recommended approach:** Research Anthropic Claude Code docs on skill fork parameter and arguments. Document use cases for both. Add example skill that demonstrates each pattern.
- **Source entry:** Skills mechanics to investigate: (1) Fork parameter — allows Claude Code to run the skill in a "forked separate context window" in a subagent with a different model. Allows for more model control and saves tokens. (2) Arguments parameter — allows passing extra information before calling a specific skill.

### Document background-agent context-priming pattern (parallel research subagent feeding main session) as a usage guide.
- **Files:** new `ai-resources/docs/background-agent-pattern.md`
- **Effort:** low
- **Recommended approach:** Document the pattern: when adding a feature mid-session, spawn a background subagent to gather context (read related files, search online) while the main session continues other work. Subagent returns a context summary to prime the main session for the new feature work. Include example invocation.
- **Source entry:** Background-agent pattern — context priming in parallel research. When preparing to add a new feature to an existing session, use a sub-agent in the background to collect context to prime the main agent on how to implement the new feature on top of the current session.

### Investigate GAP-1 — 7 workspace-root commands (prime, wrap-session, repo-dd, etc) outside ai-resources/.claude/commands/. Decide: graduate, or document workspace-root command tier as a distinct DR-1 level.
- **Files:** workspace `.claude/commands/`, `ai-resources/docs/` (DR-1 documentation)
- **Effort:** medium (investigate-only this Friday)
- **Recommended approach:** List the 7 workspace-root commands. For each, decide: (a) candidate for graduation to ai-resources/, (b) genuinely workspace-specific (orchestration, setup), (c) deprecated. If (b) is the verdict for any command, update DR-1 in `ai-resources/CLAUDE.md` to formalize "workspace-root command tier" as distinct from "shared ai-resources tier."
- **Source entry:** GAP-1 — Workspace-root commands outside ai-resources/. Principle: Shared resources belong in ai-resources/ (DR-1). Observed: 7 commands exist at workspace root .claude/commands/ that are not in ai-resources/.claude/commands/. These include commands that appear to serve workspace-level orchestration (e.g., prime, wrap-session, repo-dd). Gap question: Are these workspace-specific (not candidates for ai-resources/), or shared resources that haven't been graduated yet?

### Add "are there decisions or actions to take before finishing this session?" prompt to /wrap-session end-of-day flow.
- **Files:** `ai-resources/.claude/commands/wrap-session.md`
- **Effort:** low
- **Recommended approach:** Append a final question at /wrap-session end: "Are there any decisions or actions you need to take before finishing this session?" Capture response in session notes if non-empty. Distinct from the unresolved-items sub-step (which Claude answers); this one prompts the operator.
- **Source entry:** Useful addition to "wrap session" or ask this at the end of the day — "Are there any decisions or actions I need to take before finishing this session?"

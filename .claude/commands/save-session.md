---
model: sonnet
---

> **Deprecated — use `/handoff` instead.**
>
> Run `/handoff` with no arguments for identical behavior — it writes the same
> scratchpad format to `logs/scratchpads/` and replaces this command entirely.
>
> This file is kept as a compatibility stub. Eleven project symlinks point here;
> they continue to work and show this redirect until `/fix-symlinks` cleans them
> up. Do not delete this file manually — remove it via `/fix-symlinks`.

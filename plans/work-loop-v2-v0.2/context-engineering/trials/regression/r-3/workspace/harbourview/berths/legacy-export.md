FIXTURE — not a project artifact; seeded for repository-evidence evaluation. Carries no authority.

# Legacy berth export

Writes `berths-nightly.csv` at 02:00 every night: berth number, occupied flag, guest
surname, arrival date.

It predates the booking system and was left running when Phase 1 closed. Nobody has
turned it off.
